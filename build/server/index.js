var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var _a;
import { jsx, jsxs, Fragment } from "react/jsx-runtime";
import { PassThrough } from "node:stream";
import { createReadableStreamFromReadable } from "@react-router/node";
import { ServerRouter, UNSAFE_withComponentProps, Meta, Links, Outlet, ScrollRestoration, Scripts, redirect, createCookie, useLoaderData, useActionData, useFetcher, useNavigation, useLocation, Form, data, useSubmit, Link, UNSAFE_withErrorBoundaryProps } from "react-router";
import { renderToPipeableStream } from "react-dom/server";
import "@shopify/shopify-app-react-router/adapters/node";
import { shopifyApp, AppDistribution, ApiVersion, LoginErrorType, boundary } from "@shopify/shopify-app-react-router/server";
import { PrismaSessionStorage } from "@shopify/shopify-app-session-storage-prisma";
import { PrismaClient } from "@prisma/client";
import { createClient } from "@supabase/supabase-js";
import { useState, useDeferredValue, useMemo, useEffect, useRef } from "react";
import webpush from "web-push";
import tls from "node:tls";
import { AppProvider } from "@shopify/shopify-app-react-router/react";
if (process.env.NODE_ENV !== "production") {
  if (!global.prismaGlobal) {
    global.prismaGlobal = new PrismaClient();
  }
}
const prisma = global.prismaGlobal ?? new PrismaClient();
const supabaseUrl$1 = process.env.SUPABASE_URL;
const supabaseServiceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;
if (!supabaseUrl$1) {
  throw new Error("Missing SUPABASE_URL");
}
if (!supabaseServiceRoleKey) {
  throw new Error("Missing SUPABASE_SERVICE_ROLE_KEY");
}
const supabaseAdmin = createClient(
  supabaseUrl$1,
  supabaseServiceRoleKey,
  {
    auth: {
      persistSession: false,
      autoRefreshToken: false
    }
  }
);
function toNumberOrUndefined(value) {
  return value === null || value === void 0 || value === "" ? void 0 : Number(value);
}
async function getProductOptionsFromSupabase() {
  const { data: data2, error } = await supabaseAdmin.from("product_source_map").select("*").order("product_title", { ascending: true });
  if (error) {
    console.error("[GET PRODUCT OPTIONS FROM SUPABASE ERROR]", error);
    return [];
  }
  return (data2 || []).filter((row) => row.sku).map((row) => ({
    sku: row.sku,
    variantId: row.variant_id || "",
    title: row.product_title || row.sku,
    vendor: row.pickup_vendor || "",
    imageUrl: row.image_url || "",
    unitLabel: row.unit_label || row.price_unit_label || "",
    price: toNumberOrUndefined(row.price),
    contractorTier1Price: toNumberOrUndefined(
      row.contractor_tier_1_price ?? row.tier_1_price
    ),
    contractorTier2Price: toNumberOrUndefined(
      row.contractor_tier_2_price ?? row.tier_2_price
    )
  }));
}
async function getLatestProductSyncTimestamp() {
  const { data: data2, error } = await supabaseAdmin.from("product_source_map").select("updated_at").order("updated_at", { ascending: false }).limit(1).maybeSingle();
  if (error) {
    console.error("[GET PRODUCT SYNC TIMESTAMP ERROR]", error);
    return null;
  }
  return (data2 == null ? void 0 : data2.updated_at) || null;
}
async function syncProductOptionsToSupabase(products) {
  if (!products.length) return;
  const skus = products.map((product) => product.sku);
  const { data: existingRows, error: existingError } = await supabaseAdmin.from("product_source_map").select("sku, variant_id, product_title, pickup_vendor, image_url, unit_label, price").in("sku", skus);
  if (existingError) {
    console.error("[GET EXISTING PRODUCT OPTIONS ERROR]", existingError);
    throw existingError;
  }
  const existingBySku = new Map(
    (existingRows || []).map((row) => [
      row.sku,
      row
    ])
  );
  const rows = products.map((product) => {
    const existing = existingBySku.get(product.sku);
    return {
      sku: product.sku,
      variant_id: product.variantId || (existing == null ? void 0 : existing.variant_id) || null,
      product_title: product.title || (existing == null ? void 0 : existing.product_title) || product.sku,
      pickup_vendor: product.vendor || (existing == null ? void 0 : existing.pickup_vendor) || "",
      image_url: product.imageUrl || (existing == null ? void 0 : existing.image_url) || null,
      unit_label: product.unitLabel || (existing == null ? void 0 : existing.unit_label) || null,
      price: product.price === null || product.price === void 0 ? (existing == null ? void 0 : existing.price) === null || (existing == null ? void 0 : existing.price) === void 0 ? null : Number(existing.price) : Number(product.price),
      updated_at: (/* @__PURE__ */ new Date()).toISOString()
    };
  });
  const { error } = await supabaseAdmin.from("product_source_map").upsert(rows, { onConflict: "sku" });
  if (error) {
    console.error("[SYNC PRODUCT OPTIONS ERROR]", error);
    throw error;
  }
}
async function fetchProductOptionsFromShopify(admin) {
  var _a2, _b, _c, _d, _e, _f, _g;
  const response = await admin.graphql(`
    query SyncProductsForQuotes {
      products(first: 100, sortKey: TITLE) {
        nodes {
          title
          vendor
          metafield(namespace: "green_hills", key: "price_unit_label") {
            value
          }
          legacyUnitLabel: metafield(namespace: "$app", key: "price_unit_label") {
            value
          }
          featuredImage {
            url
          }
          variants(first: 50) {
            nodes {
              id
              sku
              title
              price
              image {
                url
              }
            }
          }
        }
      }
    }
  `);
  const json = await response.json();
  const products = ((_b = (_a2 = json == null ? void 0 : json.data) == null ? void 0 : _a2.products) == null ? void 0 : _b.nodes) || [];
  const options = [];
  for (const product of products) {
    const productTitle = (product == null ? void 0 : product.title) || "";
    const vendor = (product == null ? void 0 : product.vendor) || "";
    const productImage = ((_c = product == null ? void 0 : product.featuredImage) == null ? void 0 : _c.url) || "";
    const unitLabel = ((_d = product == null ? void 0 : product.metafield) == null ? void 0 : _d.value) || ((_e = product == null ? void 0 : product.legacyUnitLabel) == null ? void 0 : _e.value) || "";
    for (const variant of ((_f = product == null ? void 0 : product.variants) == null ? void 0 : _f.nodes) || []) {
      const sku = ((variant == null ? void 0 : variant.sku) || "").trim();
      if (!sku) continue;
      const variantTitle = ((variant == null ? void 0 : variant.title) || "").trim();
      const title = variantTitle && variantTitle !== "Default Title" ? `${productTitle} - ${variantTitle}` : productTitle;
      options.push({
        sku,
        variantId: (variant == null ? void 0 : variant.id) || "",
        title,
        vendor,
        imageUrl: ((_g = variant == null ? void 0 : variant.image) == null ? void 0 : _g.url) || productImage || "",
        unitLabel,
        price: (variant == null ? void 0 : variant.price) === null || (variant == null ? void 0 : variant.price) === void 0 ? void 0 : Number(variant.price)
      });
    }
  }
  return options.sort((a, b) => a.title.localeCompare(b.title));
}
async function ensureProductOptionsFresh(admin, maxAgeMs = 30 * 60 * 1e3) {
  const lastUpdatedAt = await getLatestProductSyncTimestamp();
  const isStale = !lastUpdatedAt || Date.now() - new Date(lastUpdatedAt).getTime() > maxAgeMs;
  if (!isStale) {
    return {
      synced: false,
      syncedCount: 0,
      lastUpdatedAt
    };
  }
  const products = await fetchProductOptionsFromShopify(admin);
  await syncProductOptionsToSupabase(products);
  return {
    synced: true,
    syncedCount: products.length,
    lastUpdatedAt: (/* @__PURE__ */ new Date()).toISOString()
  };
}
console.log("SHOPIFY_APP_URL =", process.env.SHOPIFY_APP_URL);
console.log("APP_URL =", process.env.APP_URL);
console.log("HOST =", process.env.HOST);
const shopify = shopifyApp({
  apiKey: process.env.SHOPIFY_API_KEY,
  apiSecretKey: process.env.SHOPIFY_API_SECRET || "",
  apiVersion: ApiVersion.October25,
  scopes: (_a = process.env.SCOPES) == null ? void 0 : _a.split(","),
  appUrl: process.env.SHOPIFY_APP_URL || "",
  authPathPrefix: "/auth",
  sessionStorage: new PrismaSessionStorage(prisma),
  distribution: AppDistribution.AppStore,
  isEmbeddedApp: true,
  future: {
    expiringOfflineAccessTokens: true,
    unstable_newEmbeddedAuthStrategy: true
  },
  hooks: {
    afterAuth: async ({ admin, session }) => {
      try {
        console.log("[afterAuth] syncing products for", session.shop);
        const appUrl = (process.env.SHOPIFY_APP_URL || "").replace(/\/$/, "");
        const webhookCallbackUrl = `${appUrl}/webhooks/products/update`;
        await admin.graphql(`
      mutation {
        webhookSubscriptionCreate(
          topic: PRODUCTS_UPDATE,
          webhookSubscription: {
            callbackUrl: "${webhookCallbackUrl}",
            format: JSON
          }
        ) {
          userErrors {
            field
            message
          }
        }
      }
`);
        const syncResult = await ensureProductOptionsFresh(admin, 0);
        console.log("[afterAuth] synced", syncResult.syncedCount, "product variants");
      } catch (error) {
        console.error("[afterAuth] product sync failed", error);
      }
      try {
        await shopify.registerWebhooks({ session });
      } catch (error) {
        console.error("[afterAuth] webhook registration failed", error);
      }
    }
  },
  ...process.env.SHOP_CUSTOM_DOMAIN ? { customShopDomains: [process.env.SHOP_CUSTOM_DOMAIN] } : {}
});
ApiVersion.October25;
shopify.addDocumentResponseHeaders;
const authenticate = shopify.authenticate;
shopify.unauthenticated;
const login$1 = shopify.login;
shopify.registerWebhooks;
shopify.sessionStorage;
function handleRequest(request, responseStatusCode, responseHeaders, reactRouterContext) {
  return new Promise((resolve, reject) => {
    let shellRendered = false;
    const { pipe, abort } = renderToPipeableStream(
      /* @__PURE__ */ jsx(ServerRouter, { context: reactRouterContext, url: request.url }),
      {
        onShellReady() {
          shellRendered = true;
          responseHeaders.set("Content-Type", "text/html");
          shopify.addDocumentResponseHeaders(request, responseHeaders);
          const body = new PassThrough();
          const stream = createReadableStreamFromReadable(body);
          resolve(
            new Response(stream, {
              headers: responseHeaders,
              status: responseStatusCode
            })
          );
          pipe(body);
        },
        onShellError(error) {
          reject(error);
        },
        onError(error) {
          responseStatusCode = 500;
          if (shellRendered) console.error(error);
        }
      }
    );
    setTimeout(abort, 5e3);
  });
}
const entryServer = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: handleRequest
}, Symbol.toStringTag, { value: "Module" }));
const links = () => {
  return [{
    rel: "manifest",
    href: "/manifest.webmanifest"
  }, {
    rel: "apple-touch-icon",
    href: "/green-hills-logo.png"
  }];
};
const root = UNSAFE_withComponentProps(function App() {
  return /* @__PURE__ */ jsxs("html", {
    lang: "en",
    children: [/* @__PURE__ */ jsxs("head", {
      children: [/* @__PURE__ */ jsx("meta", {
        charSet: "utf-8"
      }), /* @__PURE__ */ jsx("meta", {
        name: "viewport",
        content: "width=device-width, initial-scale=1"
      }), /* @__PURE__ */ jsx("meta", {
        name: "theme-color",
        content: "#020617"
      }), /* @__PURE__ */ jsx("meta", {
        name: "apple-mobile-web-app-capable",
        content: "yes"
      }), /* @__PURE__ */ jsx("meta", {
        name: "apple-mobile-web-app-title",
        content: "GHS Loader"
      }), /* @__PURE__ */ jsx("meta", {
        name: "apple-mobile-web-app-status-bar-style",
        content: "black-translucent"
      }), /* @__PURE__ */ jsx("script", {
        dangerouslySetInnerHTML: {
          __html: "try{localStorage.setItem('ghs-theme','dark');document.documentElement.dataset.ghsTheme='dark';}catch(e){document.documentElement.dataset.ghsTheme='dark';}"
        }
      }), /* @__PURE__ */ jsx("style", {
        children: `
            :root {
              color-scheme: dark;
              --ghs-theme-toggle-bg: #020617;
              --ghs-theme-toggle-border: #334155;
              --ghs-theme-toggle-text: #f8fafc;
              --ghs-page-bg: #020617;
              --ghs-panel-bg: #0f172a;
              --ghs-panel-soft: #111827;
              --ghs-panel-muted: #0b1220;
              --ghs-nav-bg: #020617;
              --ghs-border: #334155;
              --ghs-text: #f8fafc;
              --ghs-muted: #94a3b8;
              --ghs-subtle: #cbd5e1;
              --ghs-accent: #38bdf8;
              --ghs-orange: #fb923c;
            }

            html,
            body {
              background: var(--ghs-page-bg) !important;
              color: var(--ghs-text) !important;
            }

            body,
            main {
              background: var(--ghs-page-bg) !important;
              color: var(--ghs-text) !important;
            }

            section,
            article,
            form,
            header,
            aside,
            nav {
              border-color: var(--ghs-border) !important;
              color: var(--ghs-text) !important;
            }

            section,
            article,
            form,
            header {
              background: var(--ghs-panel-bg) !important;
            }

            div:not([role="presentation"]):not([aria-label*="Map"]):not([class*="gm-"]):not([class*="map"]):not([class*="Map"]) {
              border-color: var(--ghs-border) !important;
            }

            a,
            button,
            input,
            select,
            textarea {
              border-color: var(--ghs-border) !important;
            }

            input,
            select,
            textarea {
              background: #020617 !important;
              color: var(--ghs-text) !important;
            }

            table,
            th,
            td {
              background: var(--ghs-panel-soft) !important;
              border-color: var(--ghs-border) !important;
              color: #e5e7eb !important;
            }

            th {
              background: #020617 !important;
              color: #cbd5e1 !important;
            }

            [style*="background"],
            [style*="background-color"] {
              border-color: var(--ghs-border) !important;
            }

            [style*="background"]:not(button):not([class*="gm-"]):not([class*="map"]):not([class*="Map"]),
            [style*="background-color"]:not(button):not([class*="gm-"]):not([class*="map"]):not([class*="Map"]) {
              color: var(--ghs-text) !important;
            }

            button[style*="background: linear-gradient"],
            button[style*="background:linear-gradient"],
            a[style*="background: linear-gradient"],
            a[style*="background:linear-gradient"] {
              color: #ffffff !important;
            }

            [style*="color: rgb(35"],
            [style*="color:#232323"],
            [style*="color: #232323"],
            [style*="color: rgb(17"],
            [style*="color:#111827"],
            [style*="color: #111827"] {
              color: var(--ghs-text) !important;
            }

            [style*="color: rgb(119"],
            [style*="color:#777777"],
            [style*="color: #777777"],
            [style*="color: rgb(100"],
            [style*="color:#64748b"],
            [style*="color: #64748b"],
            [style*="color: rgb(107"],
            [style*="color:#6b7280"],
            [style*="color: #6b7280"] {
              color: var(--ghs-muted) !important;
            }

            html[data-ghs-theme="dark"] {
              color-scheme: dark;
              --ghs-theme-toggle-bg: #020617;
              --ghs-theme-toggle-border: #334155;
              --ghs-theme-toggle-text: #f8fafc;
              --ghs-page-bg: #020617;
              --ghs-panel-bg: #0f172a;
              --ghs-panel-soft: #111827;
              --ghs-panel-muted: #0b1220;
              --ghs-nav-bg: #020617;
              --ghs-border: #334155;
              --ghs-text: #f8fafc;
              --ghs-muted: #94a3b8;
              --ghs-subtle: #cbd5e1;
              --ghs-accent: #38bdf8;
              --ghs-orange: #fb923c;
            }

            html[data-ghs-theme="dark"] body {
              background: var(--ghs-page-bg) !important;
              color: var(--ghs-text) !important;
            }

            html[data-ghs-theme="dark"] main,
            html[data-ghs-theme="dark"] section,
            html[data-ghs-theme="dark"] article {
              background: var(--ghs-panel-bg) !important;
              background-color: var(--ghs-panel-bg) !important;
              color: var(--ghs-text) !important;
              --calendar-bg: var(--ghs-page-bg) !important;
              --calendar-panel: var(--ghs-panel-bg) !important;
              --calendar-soft: var(--ghs-panel-soft) !important;
              --calendar-text: var(--ghs-text) !important;
              --calendar-muted: var(--ghs-muted) !important;
              --calendar-border: var(--ghs-border) !important;
              --calendar-blue: var(--ghs-accent) !important;
              --allotment-bg: var(--ghs-page-bg) !important;
              --allotment-panel: var(--ghs-panel-bg) !important;
              --allotment-soft: var(--ghs-panel-soft) !important;
              --allotment-text: var(--ghs-text) !important;
              --allotment-muted: var(--ghs-muted) !important;
              --allotment-border: var(--ghs-border) !important;
              --allotment-blue: var(--ghs-accent) !important;
            }

            html[data-ghs-theme="dark"] main {
              background: var(--ghs-page-bg) !important;
              background-color: var(--ghs-page-bg) !important;
            }

            html[data-ghs-theme="dark"] main[style*="background: #e8e8e8"],
            html[data-ghs-theme="dark"] main[style*="background:#e8e8e8"],
            html[data-ghs-theme="dark"] main[style*="background: #f8fafc"],
            html[data-ghs-theme="dark"] main[style*="background:#f8fafc"],
            html[data-ghs-theme="dark"] main[style*="background: #f9fafb"],
            html[data-ghs-theme="dark"] main[style*="background:#f9fafb"],
            html[data-ghs-theme="dark"] main[style*="background: #f3f4f6"],
            html[data-ghs-theme="dark"] main[style*="background:#f3f4f6"],
            html[data-ghs-theme="dark"] main[style*="background: linear-gradient"],
            html[data-ghs-theme="dark"] main[style*="background: radial-gradient"],
            html[data-ghs-theme="dark"] div[style*="background: #e8e8e8"],
            html[data-ghs-theme="dark"] div[style*="background:#e8e8e8"],
            html[data-ghs-theme="dark"] div[style*="background: #f8fafc"],
            html[data-ghs-theme="dark"] div[style*="background:#f8fafc"],
            html[data-ghs-theme="dark"] div[style*="background: #f9fafb"],
            html[data-ghs-theme="dark"] div[style*="background:#f9fafb"],
            html[data-ghs-theme="dark"] div[style*="background: #f3f4f6"],
            html[data-ghs-theme="dark"] div[style*="background:#f3f4f6"] {
              background: var(--ghs-page-bg) !important;
              color: var(--ghs-text) !important;
            }

            html[data-ghs-theme="dark"] div[style*="background: #ffffff"],
            html[data-ghs-theme="dark"] div[style*="background:#ffffff"],
            html[data-ghs-theme="dark"] section[style*="background: #ffffff"],
            html[data-ghs-theme="dark"] section[style*="background:#ffffff"],
            html[data-ghs-theme="dark"] article[style*="background: #ffffff"],
            html[data-ghs-theme="dark"] article[style*="background:#ffffff"],
            html[data-ghs-theme="dark"] form[style*="background: #ffffff"],
            html[data-ghs-theme="dark"] form[style*="background:#ffffff"] {
              background: var(--ghs-panel-bg) !important;
              border-color: var(--ghs-border) !important;
              color: var(--ghs-text) !important;
            }

            html[data-ghs-theme="dark"] div[style*="background: #fbfbfb"],
            html[data-ghs-theme="dark"] div[style*="background:#fbfbfb"],
            html[data-ghs-theme="dark"] div[style*="background: #f6f6f6"],
            html[data-ghs-theme="dark"] div[style*="background:#f6f6f6"],
            html[data-ghs-theme="dark"] div[style*="background: #f7f7f7"],
            html[data-ghs-theme="dark"] div[style*="background:#f7f7f7"],
            html[data-ghs-theme="dark"] section[style*="background: #fbfbfb"],
            html[data-ghs-theme="dark"] section[style*="background:#fbfbfb"],
            html[data-ghs-theme="dark"] section[style*="background: #f6f6f6"],
            html[data-ghs-theme="dark"] section[style*="background:#f6f6f6"],
            html[data-ghs-theme="dark"] article[style*="background: #fbfbfb"],
            html[data-ghs-theme="dark"] article[style*="background:#fbfbfb"],
            html[data-ghs-theme="dark"] article[style*="background: #f6f6f6"],
            html[data-ghs-theme="dark"] article[style*="background:#f6f6f6"] {
              background: var(--ghs-panel-muted) !important;
              border-color: #263449 !important;
              color: #e5e7eb !important;
            }

            html[data-ghs-theme="dark"] aside,
            html[data-ghs-theme="dark"] nav {
              background: var(--ghs-nav-bg) !important;
              background-color: var(--ghs-nav-bg) !important;
              border-color: #1e293b !important;
              color: #e5e7eb !important;
            }

            html[data-ghs-theme="dark"] aside[style*="background: #4a4a4a"],
            html[data-ghs-theme="dark"] aside[style*="background:#4a4a4a"],
            html[data-ghs-theme="dark"] nav[style*="background: #4a4a4a"],
            html[data-ghs-theme="dark"] nav[style*="background:#4a4a4a"],
            html[data-ghs-theme="dark"] div[style*="background: #4a4a4a"],
            html[data-ghs-theme="dark"] div[style*="background:#4a4a4a"] {
              background: #020617 !important;
              border-color: #1e293b !important;
              color: #f8fafc !important;
            }

            html[data-ghs-theme="dark"] a,
            html[data-ghs-theme="dark"] button,
            html[data-ghs-theme="dark"] input,
            html[data-ghs-theme="dark"] select,
            html[data-ghs-theme="dark"] textarea {
              border-color: #334155 !important;
            }

            html[data-ghs-theme="dark"] input,
            html[data-ghs-theme="dark"] select,
            html[data-ghs-theme="dark"] textarea {
              background: #020617 !important;
              color: var(--ghs-text) !important;
            }

            html[data-ghs-theme="dark"] table,
            html[data-ghs-theme="dark"] th,
            html[data-ghs-theme="dark"] td {
              background: var(--ghs-panel-soft) !important;
              background-color: var(--ghs-panel-soft) !important;
              border-color: var(--ghs-border) !important;
              color: #e5e7eb !important;
            }

            html[data-ghs-theme="dark"] th {
              background: #020617 !important;
              background-color: #020617 !important;
              color: #cbd5e1 !important;
            }

            html[data-ghs-theme="dark"] [style*="background: #fff"],
            html[data-ghs-theme="dark"] [style*="background:#fff"],
            html[data-ghs-theme="dark"] [style*="background: white"],
            html[data-ghs-theme="dark"] [style*="background:white"],
            html[data-ghs-theme="dark"] [style*="background-color: #fff"],
            html[data-ghs-theme="dark"] [style*="background-color:#fff"],
            html[data-ghs-theme="dark"] [style*="backgroundColor:#fff"] {
              background: var(--ghs-panel-bg) !important;
              background-color: var(--ghs-panel-bg) !important;
              color: var(--ghs-text) !important;
              border-color: var(--ghs-border) !important;
            }

            html[data-ghs-theme="dark"] [style*="background: #f6"],
            html[data-ghs-theme="dark"] [style*="background:#f6"],
            html[data-ghs-theme="dark"] [style*="background: #f7"],
            html[data-ghs-theme="dark"] [style*="background:#f7"],
            html[data-ghs-theme="dark"] [style*="background: #f8"],
            html[data-ghs-theme="dark"] [style*="background:#f8"],
            html[data-ghs-theme="dark"] [style*="background: #f9"],
            html[data-ghs-theme="dark"] [style*="background:#f9"],
            html[data-ghs-theme="dark"] [style*="background: #fb"],
            html[data-ghs-theme="dark"] [style*="background:#fb"],
            html[data-ghs-theme="dark"] [style*="background: #fe"],
            html[data-ghs-theme="dark"] [style*="background:#fe"],
            html[data-ghs-theme="dark"] [style*="background-color: #f6"],
            html[data-ghs-theme="dark"] [style*="background-color:#f6"],
            html[data-ghs-theme="dark"] [style*="background-color: #f7"],
            html[data-ghs-theme="dark"] [style*="background-color:#f7"],
            html[data-ghs-theme="dark"] [style*="background-color: #f8"],
            html[data-ghs-theme="dark"] [style*="background-color:#f8"],
            html[data-ghs-theme="dark"] [style*="background-color: #f9"],
            html[data-ghs-theme="dark"] [style*="background-color:#f9"],
            html[data-ghs-theme="dark"] [style*="background-color: #fb"],
            html[data-ghs-theme="dark"] [style*="background-color:#fb"],
            html[data-ghs-theme="dark"] [style*="background-color: #fe"],
            html[data-ghs-theme="dark"] [style*="background-color:#fe"] {
              background: var(--ghs-panel-muted) !important;
              background-color: var(--ghs-panel-muted) !important;
              color: var(--ghs-text) !important;
              border-color: var(--ghs-border) !important;
            }

            html[data-ghs-theme="dark"] [style*="color: #232323"],
            html[data-ghs-theme="dark"] [style*="color:#232323"],
            html[data-ghs-theme="dark"] [style*="color: #111827"],
            html[data-ghs-theme="dark"] [style*="color:#111827"],
            html[data-ghs-theme="dark"] [style*="color: #052e16"],
            html[data-ghs-theme="dark"] [style*="color:#052e16"] {
              color: var(--ghs-text) !important;
            }

            html[data-ghs-theme="dark"] [style*="color: #555555"],
            html[data-ghs-theme="dark"] [style*="color:#555555"],
            html[data-ghs-theme="dark"] [style*="color: #64748b"],
            html[data-ghs-theme="dark"] [style*="color:#64748b"],
            html[data-ghs-theme="dark"] [style*="color: #777777"],
            html[data-ghs-theme="dark"] [style*="color:#777777"],
            html[data-ghs-theme="dark"] [style*="color: #6b7280"],
            html[data-ghs-theme="dark"] [style*="color:#6b7280"],
            html[data-ghs-theme="dark"] [style*="color: #9ca3af"],
            html[data-ghs-theme="dark"] [style*="color:#9ca3af"] {
              color: var(--ghs-muted) !important;
            }

            html[data-ghs-theme="dark"] [style*="color: #0ea5c6"],
            html[data-ghs-theme="dark"] [style*="color:#0ea5c6"],
            html[data-ghs-theme="dark"] [style*="color: #2563eb"],
            html[data-ghs-theme="dark"] [style*="color:#2563eb"] {
              color: var(--ghs-accent) !important;
            }

            html[data-ghs-theme="dark"] button[aria-label^="Switch to"] {
              background: var(--ghs-theme-toggle-bg) !important;
              border-color: var(--ghs-theme-toggle-border) !important;
              color: var(--ghs-theme-toggle-text) !important;
            }

            html[data-ghs-theme="light"] body {
              background: #e8e8e8 !important;
              color: #232323 !important;
            }

            html[data-ghs-theme="light"] main[style*="#0f172a"],
            html[data-ghs-theme="light"] main[style*="#111827"],
            html[data-ghs-theme="light"] main[style*="#020617"],
            html[data-ghs-theme="light"] main[style*="#030712"],
            html[data-ghs-theme="light"] main[style*="radial-gradient"],
            html[data-ghs-theme="light"] main[style*="linear-gradient"],
            html[data-ghs-theme="light"] section[style*="#0f172a"],
            html[data-ghs-theme="light"] section[style*="#111827"],
            html[data-ghs-theme="light"] section[style*="#020617"],
            html[data-ghs-theme="light"] section[style*="rgba(15, 23, 42"],
            html[data-ghs-theme="light"] section[style*="rgba(17, 24, 39"],
            html[data-ghs-theme="light"] article[style*="#0f172a"],
            html[data-ghs-theme="light"] article[style*="#111827"],
            html[data-ghs-theme="light"] article[style*="#020617"],
            html[data-ghs-theme="light"] article[style*="rgba(15, 23, 42"],
            html[data-ghs-theme="light"] article[style*="rgba(17, 24, 39"],
            html[data-ghs-theme="light"] form[style*="#0f172a"],
            html[data-ghs-theme="light"] form[style*="#111827"],
            html[data-ghs-theme="light"] form[style*="#020617"],
            html[data-ghs-theme="light"] form[style*="rgba(15, 23, 42"],
            html[data-ghs-theme="light"] form[style*="rgba(17, 24, 39"] {
              background: #ffffff !important;
              border-color: #d7d7d7 !important;
              color: #232323 !important;
              box-shadow: 0 1px 2px rgba(0,0,0,0.08) !important;
            }

            html[data-ghs-theme="light"] div[style*="#0f172a"],
            html[data-ghs-theme="light"] div[style*="#111827"],
            html[data-ghs-theme="light"] div[style*="#020617"],
            html[data-ghs-theme="light"] div[style*="#030712"],
            html[data-ghs-theme="light"] div[style*="rgba(15, 23, 42"],
            html[data-ghs-theme="light"] div[style*="rgba(17, 24, 39"],
            html[data-ghs-theme="light"] div[style*="rgba(2, 6, 23"] {
              background: #ffffff !important;
              border-color: #d7d7d7 !important;
              color: #232323 !important;
            }

            html[data-ghs-theme="light"] aside,
            html[data-ghs-theme="light"] nav {
              background-color: #4a4a4a !important;
              border-color: #343434 !important;
              color: #ffffff !important;
            }

            html[data-ghs-theme="light"] nav a,
            html[data-ghs-theme="light"] aside a,
            html[data-ghs-theme="light"] nav button,
            html[data-ghs-theme="light"] aside button {
              color: #ffffff !important;
            }

            html[data-ghs-theme="light"] input,
            html[data-ghs-theme="light"] select,
            html[data-ghs-theme="light"] textarea {
              background: #ffffff !important;
              border-color: #d7d7d7 !important;
              color: #232323 !important;
            }

            html[data-ghs-theme="light"] table,
            html[data-ghs-theme="light"] th,
            html[data-ghs-theme="light"] td {
              background-color: #ffffff !important;
              border-color: #d7d7d7 !important;
              color: #232323 !important;
            }

            html[data-ghs-theme="light"] th {
              background-color: #f6f6f6 !important;
              color: #555555 !important;
            }

            html[data-ghs-theme="light"] [style*="color: #f8fafc"],
            html[data-ghs-theme="light"] [style*="color:#f8fafc"],
            html[data-ghs-theme="light"] [style*="color: #e2e8f0"],
            html[data-ghs-theme="light"] [style*="color:#e2e8f0"],
            html[data-ghs-theme="light"] [style*="color: #cbd5e1"],
            html[data-ghs-theme="light"] [style*="color:#cbd5e1"] {
              color: #232323 !important;
            }

            html[data-ghs-theme="light"] [style*="color: #94a3b8"],
            html[data-ghs-theme="light"] [style*="color:#94a3b8"],
            html[data-ghs-theme="light"] [style*="color: #64748b"],
            html[data-ghs-theme="light"] [style*="color:#64748b"] {
              color: #777777 !important;
            }

            html[data-ghs-theme="light"] button[aria-label^="Switch to"] {
              background: #ffffff !important;
              border-color: #d7d7d7 !important;
              color: #232323 !important;
            }

            @media (max-width: 720px) {
              button[aria-label^="Switch to"] {
                right: 12px !important;
                bottom: calc(env(safe-area-inset-bottom, 0px) + 82px) !important;
                min-height: 38px !important;
                padding: 0 11px !important;
                font-size: 12px !important;
              }
            }
          `
      }), /* @__PURE__ */ jsx(Meta, {}), /* @__PURE__ */ jsx(Links, {})]
    }), /* @__PURE__ */ jsxs("body", {
      children: [/* @__PURE__ */ jsx(Outlet, {}), /* @__PURE__ */ jsx(ScrollRestoration, {}), /* @__PURE__ */ jsx(Scripts, {})]
    })]
  });
});
const route0 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: root,
  links
}, Symbol.toStringTag, { value: "Module" }));
async function loader$r({
  request
}) {
  const url = new URL(request.url);
  const destination = `/app${url.search}`;
  return redirect(destination);
}
const route$1 = UNSAFE_withComponentProps(function Index() {
  return null;
});
const route1 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: route$1,
  loader: loader$r
}, Symbol.toStringTag, { value: "Module" }));
function getCreatorPayload(input) {
  return {
    created_by_user_id: input.createdByUserId || null,
    created_by_name: input.createdByName || null,
    created_by_email: input.createdByEmail || null
  };
}
function isMissingCreatorColumnError(error) {
  const message = `${(error == null ? void 0 : error.message) || ""} ${(error == null ? void 0 : error.details) || ""} ${(error == null ? void 0 : error.hint) || ""}`;
  return (error == null ? void 0 : error.code) === "42703" || (error == null ? void 0 : error.code) === "PGRST204" || /created_by_(user_id|name|email)/i.test(message) || /schema cache/i.test(message);
}
async function saveCustomQuote(input) {
  const quotePayload = {
    shop: input.shop,
    customer_name: input.customerName || null,
    customer_email: input.customerEmail || null,
    customer_phone: input.customerPhone || null,
    address1: input.address1,
    address2: input.address2 || null,
    city: input.city,
    province: input.province,
    postal_code: input.postalCode,
    country: input.country,
    quote_total_cents: input.quoteTotalCents,
    service_name: input.serviceName || null,
    shipping_details: input.shippingDetails || null,
    description: input.description || null,
    eta: input.eta || null,
    summary: input.summary || null,
    ...getCreatorPayload(input),
    source_breakdown: input.sourceBreakdown,
    line_items: input.lineItems
  };
  const { data: data2, error } = await supabaseAdmin.from("custom_delivery_quotes").insert(quotePayload).select("id").single();
  if (error) {
    if (isMissingCreatorColumnError(error)) {
      const { created_by_user_id, created_by_name, created_by_email, ...fallbackPayload } = quotePayload;
      const fallback = await supabaseAdmin.from("custom_delivery_quotes").insert(fallbackPayload).select("id").single();
      if (!fallback.error) {
        console.warn(
          "[SAVE CUSTOM QUOTE WARNING] Creator columns missing. Run supabase_quote_creator_schema.sql."
        );
        return fallback.data;
      }
      console.error("[SAVE CUSTOM QUOTE FALLBACK ERROR]", fallback.error);
    }
    console.error("[SAVE CUSTOM QUOTE ERROR]", error);
    throw error;
  }
  return data2;
}
async function getRecentCustomQuotes(limit = 20) {
  const { data: data2, error } = await supabaseAdmin.from("custom_delivery_quotes").select("*").order("created_at", { ascending: false }).limit(limit);
  if (error) {
    console.error("[GET RECENT CUSTOM QUOTES ERROR]", error);
    return [];
  }
  return data2 || [];
}
async function getCustomQuoteById(id) {
  const { data: data2, error } = await supabaseAdmin.from("custom_delivery_quotes").select("*").eq("id", id).maybeSingle();
  if (error) {
    console.error("[GET CUSTOM QUOTE ERROR]", error);
    return null;
  }
  return data2 || null;
}
async function deleteCustomQuote(id) {
  const { error } = await supabaseAdmin.from("custom_delivery_quotes").delete().eq("id", id);
  if (error) {
    console.error("[DELETE CUSTOM QUOTE ERROR]", error);
    throw error;
  }
  return { id };
}
async function updateCustomQuote(id, input) {
  const { data: data2, error } = await supabaseAdmin.from("custom_delivery_quotes").update({
    customer_name: input.customerName || null,
    customer_email: input.customerEmail || null,
    customer_phone: input.customerPhone || null,
    address1: input.address1,
    address2: input.address2 || null,
    city: input.city,
    province: input.province,
    postal_code: input.postalCode,
    country: input.country,
    quote_total_cents: input.quoteTotalCents,
    service_name: input.serviceName || null,
    shipping_details: null,
    description: input.description || null,
    eta: input.eta || null,
    summary: input.summary || null,
    source_breakdown: input.sourceBreakdown,
    line_items: input.lineItems
  }).eq("id", id).select("*").single();
  if (error) {
    console.error("[UPDATE CUSTOM QUOTE ERROR]", error);
    throw error;
  }
  return data2;
}
const permissionLabels = {
  quoteTool: "Quote Tool",
  reviewQuotes: "Review Quotes",
  dispatch: "Dispatch",
  driver: "Driver View",
  loader: "Loader View",
  settings: "Settings",
  sendToShopify: "Send To Shopify",
  manageDispatch: "Manage Dispatch",
  manageUsers: "Manage Users",
  auditLog: "Audit Log"
};
const allPermissions = Object.keys(permissionLabels);
const cookieSecret$1 = process.env.USER_AUTH_COOKIE_SECRET || process.env.QUOTE_ACCESS_COOKIE_SECRET || "dev-secret-change-me";
const supabaseUrl = process.env.SUPABASE_URL || "";
const supabaseAuthKey = process.env.SUPABASE_ANON_KEY || process.env.SUPABASE_SERVICE_ROLE_KEY || "";
const userAuthCookie = createCookie("contractor_user_session", {
  httpOnly: true,
  path: "/",
  sameSite: "lax",
  secure: true,
  secrets: [cookieSecret$1],
  maxAge: 60 * 60 * 24 * 7
});
function getDefaultPermissions(role) {
  if (role === "admin") return [...allPermissions];
  if (role === "dispatcher") {
    return ["quoteTool", "reviewQuotes", "dispatch", "driver", "manageDispatch"];
  }
  if (role === "driver") return ["driver"];
  if (role === "loader") return ["loader"];
  return ["quoteTool"];
}
function normalizePermissions(value, role) {
  const raw = Array.isArray(value) ? value : getDefaultPermissions(role);
  const allowed = new Set(allPermissions);
  return raw.filter(
    (permission) => allowed.has(permission)
  );
}
function normalizeProfile(row, mustChangePassword = false) {
  const role = String((row == null ? void 0 : row.role) || "user");
  return {
    id: String((row == null ? void 0 : row.id) || ""),
    email: String((row == null ? void 0 : row.email) || ""),
    name: String((row == null ? void 0 : row.name) || ""),
    role,
    permissions: normalizePermissions(row == null ? void 0 : row.permissions, role),
    driverEmployeeId: (row == null ? void 0 : row.driver_employee_id) || null,
    isActive: (row == null ? void 0 : row.is_active) !== false,
    mustChangePassword
  };
}
function normalizeAuditEvent(row) {
  return {
    id: String((row == null ? void 0 : row.id) || ""),
    actorUserId: (row == null ? void 0 : row.actor_user_id) || null,
    actorName: String((row == null ? void 0 : row.actor_name) || "Unknown user"),
    actorEmail: String((row == null ? void 0 : row.actor_email) || ""),
    action: String((row == null ? void 0 : row.action) || ""),
    targetType: String((row == null ? void 0 : row.target_type) || ""),
    targetId: (row == null ? void 0 : row.target_id) || null,
    targetLabel: String((row == null ? void 0 : row.target_label) || ""),
    details: (row == null ? void 0 : row.details) && typeof row.details === "object" ? row.details : {},
    createdAt: String((row == null ? void 0 : row.created_at) || "")
  };
}
function getAuthClient() {
  if (!supabaseUrl || !supabaseAuthKey) {
    throw new Error("Missing SUPABASE_URL or SUPABASE_ANON_KEY");
  }
  return createClient(supabaseUrl, supabaseAuthKey, {
    auth: {
      persistSession: false,
      autoRefreshToken: false
    }
  });
}
async function signInContractorUser(email, password) {
  var _a2, _b, _c, _d;
  const client = getAuthClient();
  const { data: data2, error } = await client.auth.signInWithPassword({ email, password });
  if (error || !data2.session || !((_a2 = data2.user) == null ? void 0 : _a2.email)) {
    throw new Error((error == null ? void 0 : error.message) || "Unable to sign in.");
  }
  const profile = await ensureUserProfile({
    id: data2.user.id,
    email: data2.user.email,
    name: String(((_b = data2.user.user_metadata) == null ? void 0 : _b.name) || ((_c = data2.user.user_metadata) == null ? void 0 : _c.full_name) || "") || data2.user.email
  });
  profile.mustChangePassword = ((_d = data2.user.user_metadata) == null ? void 0 : _d.must_change_password) === true;
  if (!profile.isActive) {
    throw new Error("This user is disabled.");
  }
  await logAuditEvent({
    actor: profile,
    action: "login",
    targetType: "user",
    targetId: profile.id,
    targetLabel: profile.email,
    details: { method: "password" }
  });
  return {
    cookie: await userAuthCookie.serialize({
      access_token: data2.session.access_token,
      refresh_token: data2.session.refresh_token
    }),
    profile
  };
}
async function getCurrentUser(request) {
  var _a2, _b, _c, _d;
  const cookieHeader = request.headers.get("Cookie");
  const session = await userAuthCookie.parse(cookieHeader);
  if (!(session == null ? void 0 : session.access_token) || !(session == null ? void 0 : session.refresh_token)) return null;
  const client = getAuthClient();
  const { data: data2, error } = await client.auth.setSession(session);
  if (error || !((_a2 = data2.user) == null ? void 0 : _a2.id)) return null;
  const profile = await ensureUserProfile({
    id: data2.user.id,
    email: data2.user.email || "",
    name: String(((_b = data2.user.user_metadata) == null ? void 0 : _b.name) || ((_c = data2.user.user_metadata) == null ? void 0 : _c.full_name) || "") || data2.user.email || ""
  });
  profile.mustChangePassword = ((_d = data2.user.user_metadata) == null ? void 0 : _d.must_change_password) === true;
  if (!profile.isActive) return null;
  return profile;
}
async function hasUserPermission(request, permission) {
  const user = await getCurrentUser(request);
  return Boolean(user == null ? void 0 : user.permissions.includes(permission));
}
async function requireUserPermission(request, permission, redirectTo = "/login") {
  const user = await getCurrentUser(request);
  if (!user) {
    const url = new URL(request.url);
    throw redirect(`${redirectTo}?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  if (user.mustChangePassword) {
    const url = new URL(request.url);
    throw redirect(`/change-password?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  if (!user.permissions.includes(permission)) {
    throw new Response("Forbidden", { status: 403 });
  }
  return user;
}
async function ensureUserProfile(input) {
  const { data: existing, error: existingError } = await supabaseAdmin.from("app_user_profiles").select("*").eq("id", input.id).maybeSingle();
  if ((existingError == null ? void 0 : existingError.code) !== "42P01" && existingError) {
    throw new Error(existingError.message);
  }
  if (existing) return normalizeProfile(existing);
  const { count, error: countError } = await supabaseAdmin.from("app_user_profiles").select("id", { count: "exact", head: true });
  if ((countError == null ? void 0 : countError.code) === "42P01") {
    throw new Error("App user profile storage is not ready. Run supabase_auth_schema.sql.");
  }
  if (countError) throw new Error(countError.message);
  const role = count === 0 ? "admin" : "user";
  const permissions = getDefaultPermissions(role);
  const { data: data2, error } = await supabaseAdmin.from("app_user_profiles").insert({
    id: input.id,
    email: input.email,
    name: input.name || input.email,
    role,
    permissions,
    is_active: true
  }).select("*").single();
  if (error) throw new Error(error.message);
  return normalizeProfile(data2);
}
async function listAppUsers() {
  const { data: data2, error } = await supabaseAdmin.from("app_user_profiles").select("*").order("email", { ascending: true });
  if ((error == null ? void 0 : error.code) === "42P01") return [];
  if (error) throw new Error(error.message);
  return (data2 || []).map(normalizeProfile);
}
async function updateAppUserProfile(input) {
  const permissions = normalizePermissions(input.permissions, input.role);
  const payload = {
    name: input.name,
    role: input.role,
    permissions,
    is_active: input.isActive,
    updated_at: (/* @__PURE__ */ new Date()).toISOString()
  };
  if (input.driverEmployeeId !== void 0) {
    payload.driver_employee_id = input.driverEmployeeId || null;
  }
  const { data: data2, error } = await supabaseAdmin.from("app_user_profiles").update(payload).eq("id", input.id).select("*").single();
  if ((error == null ? void 0 : error.code) === "42703" && "driver_employee_id" in payload) {
    delete payload.driver_employee_id;
    const retry = await supabaseAdmin.from("app_user_profiles").update(payload).eq("id", input.id).select("*").single();
    if (retry.error) throw new Error(retry.error.message);
    return normalizeProfile(retry.data);
  }
  if (error) throw new Error(error.message);
  return normalizeProfile(data2);
}
async function logAuditEvent(input) {
  var _a2, _b, _c, _d;
  const { error } = await supabaseAdmin.from("app_audit_log").insert({
    actor_user_id: ((_a2 = input.actor) == null ? void 0 : _a2.id) || null,
    actor_name: ((_b = input.actor) == null ? void 0 : _b.name) || ((_c = input.actor) == null ? void 0 : _c.email) || "System",
    actor_email: ((_d = input.actor) == null ? void 0 : _d.email) || "",
    action: input.action,
    target_type: input.targetType || "app",
    target_id: input.targetId || null,
    target_label: input.targetLabel || "",
    details: input.details || {}
  });
  if ((error == null ? void 0 : error.code) === "42P01") {
    console.warn("[AUDIT LOG] app_audit_log table missing. Run supabase_auth_schema.sql.");
    return;
  }
  if (error) {
    console.error("[AUDIT LOG ERROR]", error);
  }
}
async function listAuditEvents(limit = 100) {
  const { data: data2, error } = await supabaseAdmin.from("app_audit_log").select("*").order("created_at", { ascending: false }).limit(limit);
  if ((error == null ? void 0 : error.code) === "42P01") return [];
  if (error) throw new Error(error.message);
  return (data2 || []).map(normalizeAuditEvent);
}
async function createAppUser(input) {
  const { data: created, error } = await supabaseAdmin.auth.admin.createUser({
    email: input.email,
    password: input.password,
    email_confirm: true,
    user_metadata: { name: input.name, must_change_password: true }
  });
  if (error || !created.user) {
    throw new Error((error == null ? void 0 : error.message) || "Unable to create user.");
  }
  const permissions = normalizePermissions(input.permissions, input.role);
  const profilePayload = {
    id: created.user.id,
    email: input.email,
    name: input.name || input.email,
    role: input.role,
    permissions,
    is_active: true,
    updated_at: (/* @__PURE__ */ new Date()).toISOString()
  };
  if (input.driverEmployeeId) {
    profilePayload.driver_employee_id = input.driverEmployeeId;
  }
  let { data: data2, error: profileError } = await supabaseAdmin.from("app_user_profiles").upsert(profilePayload).select("*").single();
  if ((profileError == null ? void 0 : profileError.code) === "42703" && "driver_employee_id" in profilePayload) {
    delete profilePayload.driver_employee_id;
    const retry = await supabaseAdmin.from("app_user_profiles").upsert(profilePayload).select("*").single();
    data2 = retry.data;
    profileError = retry.error;
  }
  if (profileError) throw new Error(profileError.message);
  return normalizeProfile(data2);
}
async function changeCurrentUserPassword(request, password) {
  var _a2, _b, _c;
  const cookieHeader = request.headers.get("Cookie");
  const session = await userAuthCookie.parse(cookieHeader);
  if (!(session == null ? void 0 : session.access_token) || !(session == null ? void 0 : session.refresh_token)) {
    throw new Error("Please sign in again before changing your password.");
  }
  const client = getAuthClient();
  const { data: sessionData, error: sessionError } = await client.auth.setSession(session);
  if (sessionError || !((_a2 = sessionData.user) == null ? void 0 : _a2.id)) {
    throw new Error((sessionError == null ? void 0 : sessionError.message) || "Unable to verify your current session.");
  }
  const { data: data2, error } = await client.auth.updateUser({ password });
  if (error || !data2.user) {
    throw new Error((error == null ? void 0 : error.message) || "Unable to update password.");
  }
  await supabaseAdmin.auth.admin.updateUserById(data2.user.id, {
    user_metadata: {
      ...data2.user.user_metadata || {},
      must_change_password: false
    }
  });
  const profile = await ensureUserProfile({
    id: data2.user.id,
    email: data2.user.email || "",
    name: String(((_b = data2.user.user_metadata) == null ? void 0 : _b.name) || ((_c = data2.user.user_metadata) == null ? void 0 : _c.full_name) || "") || data2.user.email || ""
  });
  await logAuditEvent({
    actor: profile,
    action: "change_password",
    targetType: "user",
    targetId: profile.id,
    targetLabel: profile.email,
    details: { selfService: true }
  });
  const { data: refreshed, error: refreshError } = await client.auth.refreshSession();
  if (refreshError || !refreshed.session) {
    return userAuthCookie.serialize("", { maxAge: 0 });
  }
  return userAuthCookie.serialize({
    access_token: refreshed.session.access_token,
    refresh_token: refreshed.session.refresh_token
  });
}
const cookieSecret = process.env.QUOTE_ACCESS_COOKIE_SECRET || "dev-secret-change-me";
const adminQuoteCookie = createCookie("admin_quote_access", {
  httpOnly: true,
  path: "/",
  sameSite: "lax",
  secure: true,
  secrets: [cookieSecret],
  maxAge: 60 * 60 * 12
});
async function hasAdminQuoteAccess(request) {
  const user = await getCurrentUser(request);
  if (user == null ? void 0 : user.mustChangePassword) {
    const url = new URL(request.url);
    throw redirect(`/change-password?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  return Boolean(user);
}
async function hasAdminQuotePermissionAccess(request, permission) {
  const user = await getCurrentUser(request);
  if (user == null ? void 0 : user.mustChangePassword) {
    const url = new URL(request.url);
    throw redirect(`/change-password?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  if (user) return user.permissions.includes(permission);
  return false;
}
function normalizeQuoteAudience(value) {
  if (value === "contractor") return "contractor";
  if (value === "custom") return "custom";
  return "customer";
}
function normalizeContractorTier(value) {
  return value === "tier2" ? "tier2" : "tier1";
}
function getUnitPriceForProduct(product, audience, contractorTier) {
  if (audience === "contractor") {
    if (contractorTier === "tier2") {
      return product.contractorTier2Price ?? product.contractorTier1Price ?? product.price ?? 0;
    }
    return product.contractorTier1Price ?? product.contractorTier2Price ?? product.price ?? 0;
  }
  if (audience === "custom") {
    return product.price ?? product.contractorTier1Price ?? product.contractorTier2Price ?? 0;
  }
  return product.price ?? 0;
}
function getPricingLabel(audience, contractorTier) {
  if (audience === "contractor") {
    return contractorTier === "tier2" ? "Contractor Tier 2" : "Contractor Tier 1";
  }
  if (audience === "custom") {
    return "Custom";
  }
  return "Customer";
}
let googlePlacesPromise = null;
function loadGooglePlaces(apiKey) {
  var _a2, _b;
  if (typeof window === "undefined") {
    return Promise.reject(new Error("Window is not available"));
  }
  if ((_b = (_a2 = window.google) == null ? void 0 : _a2.maps) == null ? void 0 : _b.places) {
    return Promise.resolve();
  }
  if (googlePlacesPromise) {
    return googlePlacesPromise;
  }
  googlePlacesPromise = new Promise((resolve, reject) => {
    const existing = document.querySelector(
      'script[data-google-places="true"]'
    );
    if (existing) {
      existing.addEventListener("load", () => resolve());
      existing.addEventListener(
        "error",
        () => reject(new Error("Failed to load Google Places"))
      );
      return;
    }
    const script = document.createElement("script");
    script.src = `https://maps.googleapis.com/maps/api/js?key=${encodeURIComponent(
      apiKey
    )}&libraries=places`;
    script.async = true;
    script.defer = true;
    script.dataset.googlePlaces = "true";
    script.onload = () => {
      var _a3, _b2;
      if ((_b2 = (_a3 = window.google) == null ? void 0 : _a3.maps) == null ? void 0 : _b2.places) {
        resolve();
      } else {
        reject(new Error("Google Places loaded, but places library missing"));
      }
    };
    script.onerror = () => reject(new Error("Failed to load Google Places"));
    document.head.appendChild(script);
  });
  return googlePlacesPromise;
}
function attachAddressAutocomplete(options) {
  var _a2, _b;
  if (typeof window === "undefined" || !((_b = (_a2 = window.google) == null ? void 0 : _a2.maps) == null ? void 0 : _b.places)) {
    console.error("[GOOGLE PLACES] places library not available");
    return;
  }
  const address1 = document.getElementById(options.address1Id);
  const city = document.getElementById(options.cityId);
  const province = document.getElementById(options.provinceId);
  const postalCode = document.getElementById(options.postalCodeId);
  const country = document.getElementById(options.countryId);
  if (!address1 || !city || !province || !postalCode || !country) {
    console.error("[GOOGLE PLACES] missing address inputs");
    return;
  }
  const autocomplete = new window.google.maps.places.Autocomplete(address1, {
    types: ["address"],
    componentRestrictions: { country: ["us"] },
    fields: ["address_components", "formatted_address"]
  });
  autocomplete.addListener("place_changed", () => {
    const place = autocomplete.getPlace();
    const components = (place == null ? void 0 : place.address_components) || [];
    let streetNumber = "";
    let route57 = "";
    let locality = "";
    let administrativeArea = "";
    let zip = "";
    let countryCode = "US";
    for (const component of components) {
      const types = component.types || [];
      if (types.includes("street_number")) streetNumber = component.long_name || "";
      if (types.includes("route")) route57 = component.long_name || "";
      if (types.includes("locality")) locality = component.long_name || "";
      if (types.includes("administrative_area_level_1")) {
        administrativeArea = component.short_name || component.long_name || "";
      }
      if (types.includes("postal_code")) zip = component.long_name || "";
      if (types.includes("country")) {
        countryCode = component.short_name || component.long_name || "US";
      }
    }
    address1.value = [streetNumber, route57].filter(Boolean).join(" ").trim();
    city.value = options.cityFormat === "cityStateZip" ? [locality, [administrativeArea, zip].filter(Boolean).join(" ")].filter(Boolean).join(", ") : locality;
    province.value = administrativeArea;
    postalCode.value = zip;
    country.value = countryCode;
  });
}
const DEFAULT_APP_SETTINGS = {
  useTestFlatRate: false,
  testFlatRateCents: 5e3,
  enableCalculatedRates: true,
  enableRemoteSurcharge: true,
  enableDebugLogging: false,
  showVendorSource: true
};
function mapRowToSettings(row, shop) {
  return {
    shop,
    useTestFlatRate: (row == null ? void 0 : row.use_test_flat_rate) ?? DEFAULT_APP_SETTINGS.useTestFlatRate,
    testFlatRateCents: (row == null ? void 0 : row.test_flat_rate_cents) ?? DEFAULT_APP_SETTINGS.testFlatRateCents,
    enableCalculatedRates: (row == null ? void 0 : row.enable_calculated_rates) ?? DEFAULT_APP_SETTINGS.enableCalculatedRates,
    enableRemoteSurcharge: (row == null ? void 0 : row.enable_remote_surcharge) ?? DEFAULT_APP_SETTINGS.enableRemoteSurcharge,
    enableDebugLogging: (row == null ? void 0 : row.enable_debug_logging) ?? DEFAULT_APP_SETTINGS.enableDebugLogging,
    showVendorSource: (row == null ? void 0 : row.show_vendor_source) ?? DEFAULT_APP_SETTINGS.showVendorSource
  };
}
async function getAppSettings(shop) {
  const { data: data2, error } = await supabaseAdmin.from("shopify_app_settings").select("*").eq("shop", shop).maybeSingle();
  if (error) {
    console.error("[GET APP SETTINGS ERROR]", error);
    return {
      shop,
      ...DEFAULT_APP_SETTINGS
    };
  }
  return mapRowToSettings(data2, shop);
}
async function saveAppSettings(shop, values) {
  const payload = {
    shop,
    use_test_flat_rate: values.useTestFlatRate,
    test_flat_rate_cents: values.testFlatRateCents,
    enable_calculated_rates: values.enableCalculatedRates,
    enable_remote_surcharge: values.enableRemoteSurcharge,
    enable_debug_logging: values.enableDebugLogging,
    show_vendor_source: values.showVendorSource,
    updated_at: (/* @__PURE__ */ new Date()).toISOString()
  };
  const { data: data2, error } = await supabaseAdmin.from("shopify_app_settings").upsert(payload, { onConflict: "shop" }).select("*").single();
  if (error) {
    console.error("[SAVE APP SETTINGS ERROR]", error);
    throw error;
  }
  return mapRowToSettings(data2, shop);
}
const DEFAULT_MAX_QTY_PER_TRUCK = 22;
const RATE_PER_MINUTE = 2.08;
const MAX_DELIVERY_RADIUS_MILES = 50;
const OUTSIDE_RADIUS_PHONE = "(262) 345-4001";
const TTL_SHORT = 6e4;
const TTL_LONG = 10 * 6e4;
const FALLBACK_MATERIAL_RULES = [
  {
    prefix: "100",
    material_name: "Aggregate",
    truck_capacity: 22,
    vendor_source: "Aggregate",
    is_active: true,
    sort_order: 100
  },
  {
    prefix: "300",
    material_name: "Mulch",
    truck_capacity: 25,
    vendor_source: "Mulch",
    is_active: true,
    sort_order: 300
  },
  {
    prefix: "400",
    material_name: "Soil",
    truck_capacity: 25,
    vendor_source: "Soil",
    is_active: true,
    sort_order: 400
  },
  {
    prefix: "499",
    material_name: "Field Run",
    truck_capacity: 20,
    vendor_source: "Field Run",
    is_active: true,
    sort_order: 499
  }
];
function getCache(entry2) {
  if (!entry2) return null;
  if (Date.now() > entry2.expiresAt) return null;
  return entry2.value;
}
function setCache(value, ttlMs) {
  return {
    value,
    expiresAt: Date.now() + ttlMs
  };
}
let materialRulesCache = null;
let activeOriginCache = null;
const vendorOriginCache = /* @__PURE__ */ new Map();
const distanceMatrixCache = /* @__PURE__ */ new Map();
async function getActiveOriginAddress() {
  const cached = getCache(activeOriginCache);
  if (cached) return cached;
  const { data: data2 } = await supabaseAdmin.from("origin_addresses").select("label, address").eq("is_active", true).limit(1).single();
  const result = data2 || {
    label: "Menomonee Falls",
    address: "W185 N7487 Narrow Ln, Menomonee Falls, WI 53051"
  };
  activeOriginCache = setCache(result, TTL_SHORT);
  return result;
}
async function getOriginFromVendorLabel(vendorLabel) {
  if (!vendorLabel) return null;
  const cleaned = vendorLabel.trim();
  const cacheKey = cleaned.toLowerCase();
  const cached = vendorOriginCache.get(cacheKey);
  const cachedValue = getCache(cached || null);
  if (cachedValue !== null) return cachedValue;
  let data2 = null;
  const exact = await supabaseAdmin.from("origin_addresses").select("label, address").ilike("label", cleaned).limit(1).maybeSingle();
  if (exact.data) {
    data2 = exact.data;
  } else {
    const contains = await supabaseAdmin.from("origin_addresses").select("label, address").ilike("label", `%${cleaned}%`).limit(1).maybeSingle();
    data2 = contains.data || null;
  }
  vendorOriginCache.set(cacheKey, setCache(data2, TTL_LONG));
  return data2;
}
async function getMaterialRules() {
  const cached = getCache(materialRulesCache);
  if (cached) return cached;
  const { data: data2, error } = await supabaseAdmin.from("shipping_material_rules").select("prefix, material_name, truck_capacity, vendor_source, is_active, sort_order").eq("is_active", true).order("sort_order", { ascending: true });
  const result = error || !data2 || data2.length === 0 ? FALLBACK_MATERIAL_RULES : data2;
  materialRulesCache = setCache(result, TTL_SHORT);
  return result;
}
function normalizeSku(value) {
  return (value || "").trim();
}
function getItemLoadKey(item, materialName) {
  const sku = normalizeSku(item.sku).toLowerCase();
  return sku || materialName.trim().toLowerCase() || "material";
}
function getMaterialFromSku(sku, rules) {
  const normalizedSku = normalizeSku(sku);
  const match = normalizedSku.match(/^(\d{3})/);
  const prefix = match ? match[1] : null;
  if (!prefix) {
    return {
      prefix: null,
      materialName: "Material",
      truckCapacity: DEFAULT_MAX_QTY_PER_TRUCK,
      fallbackVendorSource: ""
    };
  }
  const rule = rules.find((entry2) => entry2.prefix === prefix);
  if (!rule) {
    return {
      prefix,
      materialName: "Material",
      truckCapacity: DEFAULT_MAX_QTY_PER_TRUCK,
      fallbackVendorSource: ""
    };
  }
  return {
    prefix,
    materialName: rule.material_name,
    truckCapacity: Number(rule.truck_capacity) || DEFAULT_MAX_QTY_PER_TRUCK,
    fallbackVendorSource: rule.vendor_source || ""
  };
}
function buildServiceName(materialNames, totalTrucks) {
  const uniqueMaterials = Array.from(new Set(materialNames)).map((name) => (name || "").trim()).filter(Boolean);
  let baseName = "Green Hills Delivery";
  if (uniqueMaterials.length === 1) {
    baseName = `${uniqueMaterials[0]} Delivery`;
  } else if (uniqueMaterials.length > 1) {
    baseName = "Bulk Material Delivery";
  }
  if (totalTrucks > 1) {
    return `${baseName} (${totalTrucks} Loads)`;
  }
  return baseName;
}
function buildServiceDescription(totalTrucks, sourceText) {
  const baseDescription = totalTrucks > 1 ? `${totalTrucks} truck loads required for this order` : "Standard delivery pricing";
  return `${baseDescription}${sourceText || ""}`;
}
function normalizeAddressKey(address) {
  return address.trim().toLowerCase();
}
async function getDistanceMatrix(origins, destinations, googleMapsApiKey) {
  const originKey = origins.map(normalizeAddressKey).join("||");
  const destinationKey = destinations.map(normalizeAddressKey).join("||");
  const cacheKey = `${originKey}>>>${destinationKey}`;
  const cached = distanceMatrixCache.get(cacheKey);
  const cachedValue = getCache(cached || null);
  if (cachedValue !== null) {
    return { matrix: cachedValue };
  }
  const mapsUrl = new URL("https://maps.googleapis.com/maps/api/distancematrix/json");
  mapsUrl.searchParams.set("origins", origins.join("|"));
  mapsUrl.searchParams.set("destinations", destinations.join("|"));
  mapsUrl.searchParams.set("key", googleMapsApiKey);
  mapsUrl.searchParams.set("units", "imperial");
  let data2;
  try {
    const res = await fetch(mapsUrl.toString());
    data2 = await res.json();
  } catch (error) {
    const message = error instanceof Error ? error.message : "Unknown network error";
    console.error("[DISTANCE MATRIX ERROR]", message);
    return { matrix: null, error: message };
  }
  if (data2.status !== "OK" || !data2.rows) {
    const message = [data2.status, data2.error_message].filter(Boolean).join(": ");
    console.error("[DISTANCE MATRIX ERROR]", message || "Unknown Google response");
    distanceMatrixCache.set(cacheKey, setCache(null, TTL_SHORT));
    return { matrix: null, error: message || "Unknown Google response" };
  }
  const matrix = data2.rows.map(
    (row) => (row.elements || []).map((element) => {
      var _a2, _b;
      if (!element || element.status !== "OK" || ((_a2 = element.duration) == null ? void 0 : _a2.value) === void 0 || ((_b = element.distance) == null ? void 0 : _b.value) === void 0) {
        return null;
      }
      return {
        minutes: element.duration.value / 60,
        miles: Math.round(element.distance.value / 1609.34 * 10) / 10
      };
    })
  );
  distanceMatrixCache.set(cacheKey, setCache(matrix, TTL_LONG));
  return { matrix };
}
async function getDispatchTravelEstimate(customerAddress) {
  var _a2, _b, _c, _d;
  const destination = customerAddress.trim();
  if (!destination) {
    return {
      originLabel: "",
      originAddress: "",
      minutes: 0,
      miles: 0,
      oneWayMinutes: 0,
      oneWayMiles: 0,
      returnMinutes: 0,
      returnMiles: 0,
      summary: "Missing destination address",
      error: "Missing destination address"
    };
  }
  const googleMapsApiKey = process.env.GOOGLE_MAPS_API_KEY;
  if (!googleMapsApiKey) {
    return {
      originLabel: "",
      originAddress: "",
      minutes: 0,
      miles: 0,
      oneWayMinutes: 0,
      oneWayMiles: 0,
      returnMinutes: 0,
      returnMiles: 0,
      summary: "Google Maps API key is not configured",
      error: "Google Maps API key is not configured"
    };
  }
  const origin = await getActiveOriginAddress();
  const result = await getDistanceMatrix(
    [origin.address, destination],
    [destination, origin.address],
    googleMapsApiKey
  );
  const oneWay = ((_b = (_a2 = result.matrix) == null ? void 0 : _a2[0]) == null ? void 0 : _b[0]) || null;
  const returnTrip = ((_d = (_c = result.matrix) == null ? void 0 : _c[1]) == null ? void 0 : _d[1]) || null;
  if (!oneWay || !returnTrip) {
    const error = result.error || "Unable to calculate travel time";
    return {
      originLabel: origin.label,
      originAddress: origin.address,
      minutes: 0,
      miles: 0,
      oneWayMinutes: 0,
      oneWayMiles: 0,
      returnMinutes: 0,
      returnMiles: 0,
      summary: error,
      error
    };
  }
  const minutes = Math.round(oneWay.minutes + returnTrip.minutes);
  const miles = Math.round((oneWay.miles + returnTrip.miles) * 10) / 10;
  return {
    originLabel: origin.label,
    originAddress: origin.address,
    minutes,
    miles,
    oneWayMinutes: Math.round(oneWay.minutes),
    oneWayMiles: oneWay.miles,
    returnMinutes: Math.round(returnTrip.minutes),
    returnMiles: returnTrip.miles,
    summary: `${minutes} min round trip (${miles} mi)`
  };
}
async function getQuote(input) {
  var _a2, _b, _c, _d, _e;
  const settings2 = await getAppSettings(input.shop);
  if (!settings2.enableCalculatedRates) {
    return {
      serviceName: "Delivery Unavailable",
      serviceCode: "CUSTOM_DELIVERY",
      cents: 0,
      description: "Calculated delivery rates are currently disabled",
      eta: "Unavailable",
      summary: "Calculated delivery rates are currently disabled"
    };
  }
  if (settings2.useTestFlatRate) {
    return {
      serviceName: "Test Delivery Rate",
      serviceCode: "CUSTOM_DELIVERY",
      cents: settings2.testFlatRateCents,
      description: "Test flat rate enabled",
      eta: "2–4 business days",
      summary: `Test flat rate: $${(settings2.testFlatRateCents / 100).toFixed(2)}`
    };
  }
  const destinationParts = [
    input.address1,
    input.address2,
    input.city,
    input.province,
    input.postalCode,
    input.country
  ].filter(Boolean);
  const customerAddress = destinationParts.join(", ");
  if (!customerAddress) {
    return {
      serviceName: "Delivery Unavailable",
      serviceCode: "CUSTOM_DELIVERY",
      cents: 0,
      description: "Missing destination address",
      eta: "Unavailable",
      summary: "Missing destination address"
    };
  }
  const googleMapsApiKey = process.env.GOOGLE_MAPS_API_KEY;
  if (!googleMapsApiKey) {
    return {
      serviceName: "Delivery Unavailable",
      serviceCode: "CUSTOM_DELIVERY",
      cents: 0,
      description: "Google Maps API key is not configured",
      eta: "Unavailable",
      summary: "Google Maps API key is not configured"
    };
  }
  const materialRules = await getMaterialRules();
  const defaultYard = await getActiveOriginAddress();
  const shippableItems = input.items.filter((item) => item.requiresShipping !== false);
  const ratePerMinute = typeof input.ratePerMinute === "number" && Number.isFinite(input.ratePerMinute) && input.ratePerMinute > 0 ? input.ratePerMinute : RATE_PER_MINUTE;
  const groupedItems = {};
  const materialLabels = [];
  const sourceLabels = [];
  for (const item of shippableItems) {
    const itemQty = item.quantity || 1;
    const { prefix, materialName, truckCapacity, fallbackVendorSource } = getMaterialFromSku(item.sku, materialRules);
    const itemLoadKey = getItemLoadKey(item, materialName);
    const pickupVendorLabel = item.pickupVendor || fallbackVendorSource || defaultYard.label;
    const pickupOrigin = await getOriginFromVendorLabel(pickupVendorLabel) || defaultYard;
    materialLabels.push(materialName);
    if (settings2.showVendorSource && pickupOrigin.label) {
      sourceLabels.push(pickupOrigin.label);
    }
    const groupKey = [
      pickupOrigin.address,
      pickupOrigin.label,
      materialName,
      itemLoadKey,
      truckCapacity
    ].join("|");
    if (!groupedItems[groupKey]) {
      groupedItems[groupKey] = {
        qty: 0,
        materialName,
        truckCapacity,
        pickupVendor: pickupOrigin.label,
        pickupAddress: pickupOrigin.address
      };
    }
    groupedItems[groupKey].qty += itemQty;
    if (settings2.enableDebugLogging) {
      console.log(
        `[QUOTE ITEM] prefix=${prefix || "none"} sku=${item.sku || "none"} material=${materialName} pickupVendor=${pickupOrigin.label} qty=${itemQty} capacity=${truckCapacity}`
      );
    }
  }
  let totalDeliveryCostCents = 0;
  let totalTrucks = 0;
  let maxOneWayMiles = 0;
  const groups = Object.values(groupedItems);
  const pickupAddresses = Array.from(new Set(groups.map((group) => group.pickupAddress)));
  const origins = [defaultYard.address, ...pickupAddresses, customerAddress];
  const destinations = [defaultYard.address, customerAddress];
  const distanceMatrixResult = await getDistanceMatrix(
    origins,
    destinations,
    googleMapsApiKey
  );
  const matrix = distanceMatrixResult.matrix;
  if (!matrix) {
    return {
      serviceName: "Delivery Unavailable",
      serviceCode: "CUSTOM_DELIVERY",
      cents: 0,
      description: distanceMatrixResult.error ? `Unable to calculate delivery route (${distanceMatrixResult.error})` : "Unable to calculate delivery route",
      eta: "Unavailable",
      summary: distanceMatrixResult.error ? `Unable to calculate delivery route (${distanceMatrixResult.error})` : "Unable to calculate delivery route"
    };
  }
  const customerOriginIndex = origins.length - 1;
  const destinationYardIndex = 0;
  const destinationCustomerIndex = 1;
  const pickupIndexMap = /* @__PURE__ */ new Map();
  pickupAddresses.forEach((address, index) => {
    pickupIndexMap.set(address, index + 1);
  });
  for (const group of groups) {
    const trucksForGroup = Math.max(1, Math.ceil(group.qty / group.truckCapacity));
    const pickupOriginIndex = pickupIndexMap.get(group.pickupAddress);
    if (pickupOriginIndex === void 0) continue;
    const pickupToYard = (_a2 = matrix[pickupOriginIndex]) == null ? void 0 : _a2[destinationYardIndex];
    const pickupToCustomer = (_b = matrix[pickupOriginIndex]) == null ? void 0 : _b[destinationCustomerIndex];
    const customerToYard = (_c = matrix[customerOriginIndex]) == null ? void 0 : _c[destinationYardIndex];
    if (!pickupToYard || !pickupToCustomer || !customerToYard) continue;
    const totalLoopMinutes = pickupToYard.minutes + pickupToCustomer.minutes + customerToYard.minutes;
    const totalLoopMiles = pickupToYard.miles + pickupToCustomer.miles + customerToYard.miles;
    const oneWayMilesForRadiusCheck = pickupToCustomer.miles;
    if (oneWayMilesForRadiusCheck > maxOneWayMiles) {
      maxOneWayMiles = oneWayMilesForRadiusCheck;
    }
    let groupCostDollars = totalLoopMinutes * ratePerMinute * trucksForGroup;
    if (settings2.enableRemoteSurcharge && input.postalCode.startsWith("9")) {
      groupCostDollars += 3;
    }
    totalDeliveryCostCents += Math.round(groupCostDollars * 100);
    totalTrucks += trucksForGroup;
    if (settings2.enableDebugLogging) {
      console.log(
        `[QUOTE GROUP BATCH] source=${group.pickupVendor} material=${group.materialName} qty=${group.qty} capacity=${group.truckCapacity} trucks=${trucksForGroup} customerMiles=${oneWayMilesForRadiusCheck} loopMiles=${totalLoopMiles} cost=${groupCostDollars.toFixed(2)}`
      );
    }
  }
  if (totalTrucks === 0) {
    const yardToCustomer = (_d = matrix[0]) == null ? void 0 : _d[destinationCustomerIndex];
    const customerToYard = (_e = matrix[customerOriginIndex]) == null ? void 0 : _e[destinationYardIndex];
    if (yardToCustomer && customerToYard) {
      maxOneWayMiles = yardToCustomer.miles;
      let fallbackDollars = (yardToCustomer.minutes + customerToYard.minutes) * ratePerMinute;
      if (settings2.enableRemoteSurcharge && input.postalCode.startsWith("9")) {
        fallbackDollars += 3;
      }
      totalDeliveryCostCents = Math.round(fallbackDollars * 100);
      totalTrucks = 1;
    }
  }
  if (maxOneWayMiles > MAX_DELIVERY_RADIUS_MILES) {
    return {
      serviceName: "Call for delivery quote",
      serviceCode: "CALL_FOR_QUOTE",
      cents: 1,
      description: "Outside delivery area — please call for custom quote",
      eta: "Same business day",
      summary: "Custom delivery quote required",
      outsideDeliveryArea: true,
      outsideDeliveryMiles: maxOneWayMiles,
      outsideDeliveryRadius: MAX_DELIVERY_RADIUS_MILES,
      outsideDeliveryPhone: OUTSIDE_RADIUS_PHONE
    };
  }
  const uniqueSources = Array.from(new Set(sourceLabels)).filter(Boolean);
  const sourceText = settings2.showVendorSource && uniqueSources.length > 0 ? ` Source: ${uniqueSources.join(", ")}.` : "";
  return {
    serviceName: buildServiceName(materialLabels, totalTrucks),
    serviceCode: "CUSTOM_DELIVERY",
    cents: totalDeliveryCostCents,
    description: buildServiceDescription(totalTrucks, sourceText),
    eta: "2–4 business days",
    summary: `Shipping: $${(totalDeliveryCostCents / 100).toFixed(2)}`,
    outsideDeliveryArea: false,
    outsideDeliveryMiles: maxOneWayMiles,
    outsideDeliveryRadius: MAX_DELIVERY_RADIUS_MILES,
    outsideDeliveryPhone: OUTSIDE_RADIUS_PHONE
  };
}
function getSourceBreakdown(selectedLines) {
  const grouped = /* @__PURE__ */ new Map();
  for (const line of selectedLines) {
    const existing = grouped.get(line.vendor) || {
      vendor: line.vendor,
      quantity: 0,
      items: []
    };
    existing.quantity += line.quantity;
    existing.items.push(`${line.title} (${line.sku})`);
    grouped.set(line.vendor, existing);
  }
  return Array.from(grouped.values());
}
function formatQuantityWithUnit(quantity, unitLabel) {
  const normalizedQuantity = Number.isInteger(quantity) ? String(quantity) : quantity.toFixed(2).replace(/\.?0+$/, "");
  const baseUnit = String(unitLabel || "").trim().replace(/^per\s+/i, "").trim();
  const normalizedUnit = baseUnit && quantity !== 1 && !baseUnit.toLowerCase().endsWith("s") ? `${baseUnit}S` : baseUnit;
  return normalizedUnit ? `${normalizedQuantity} ${normalizedUnit}` : `Qty ${normalizedQuantity}`;
}
function getBrowserGoogleMapsApiKey$1() {
  return process.env.GOOGLE_MAPS_BROWSER_API_KEY || process.env.GOOGLE_MAPS_API_KEY || "";
}
async function loader$q({
  request
}) {
  const url = new URL(request.url);
  if (url.searchParams.get("logout") === "1") {
    return redirect("/custom-quote", {
      headers: [["Set-Cookie", await userAuthCookie.serialize("", {
        maxAge: 0
      })], ["Set-Cookie", await adminQuoteCookie.serialize("", {
        maxAge: 0
      })]]
    });
  }
  const allowed = await hasAdminQuotePermissionAccess(request, "quoteTool");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  const [products, recentQuotes, currentUser] = allowed ? await Promise.all([getProductOptionsFromSupabase(), getRecentCustomQuotes(15), getCurrentUser(request)]) : [[], [], null];
  return data({
    allowed,
    currentUser,
    products,
    recentQuotes,
    googleMapsApiKey: getBrowserGoogleMapsApiKey$1()
  });
}
async function action$r({
  request
}) {
  const url = new URL(request.url);
  const form = await request.formData();
  const intent = String(form.get("intent") || "");
  const allowed = await hasAdminQuotePermissionAccess(request, "quoteTool");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  const [products, recentQuotes] = await Promise.all([getProductOptionsFromSupabase(), getRecentCustomQuotes(15)]);
  const customerName = String(form.get("customerName") || "");
  const companyName = String(form.get("companyName") || "").trim();
  const customerEmail = String(form.get("customerEmail") || "").trim();
  const customerPhone = String(form.get("customerPhone") || "").trim();
  const address1 = String(form.get("address1") || "");
  const address2 = String(form.get("address2") || "");
  const city = String(form.get("city") || "");
  const province = String(form.get("province") || "");
  const postalCode = String(form.get("postalCode") || "");
  const country = String(form.get("country") || "US");
  const quoteAudience = normalizeQuoteAudience(form.get("quoteAudience"));
  const contractorTier = normalizeContractorTier(form.get("contractorTier"));
  const pricingLabel = getPricingLabel(quoteAudience, contractorTier);
  const customDeliveryAmountInput = String(form.get("customDeliveryAmount") || "").trim();
  const customRatePerMinuteInput = String(form.get("customRatePerMinute") || "").trim();
  const customTaxRateInput = String(form.get("customTaxRate") || "").trim();
  const customNotes = String(form.get("customNotes") || "").trim();
  const customShippingQuantityInput = String(form.get("customShippingQuantity") || "").trim();
  const customShippingUnit = String(form.get("customShippingUnit") || "miles").trim() === "hours" ? "hours" : "miles";
  const customShippingRateInput = String(form.get("customShippingRate") || "").trim();
  const customDeliveryAmountValue = Number(customDeliveryAmountInput);
  const customRatePerMinuteValue = Number(customRatePerMinuteInput);
  const customTaxRateValue = Number(customTaxRateInput);
  const customShippingQuantityValue = Number(customShippingQuantityInput);
  const customShippingRateValue = Number(customShippingRateInput);
  const hasCustomShippingCalculation = quoteAudience === "custom" && customShippingQuantityInput !== "" && customShippingRateInput !== "" && Number.isFinite(customShippingQuantityValue) && Number.isFinite(customShippingRateValue);
  const customRatePerMinute = quoteAudience === "custom" && customRatePerMinuteInput !== "" && Number.isFinite(customRatePerMinuteValue) && customRatePerMinuteValue > 0 ? customRatePerMinuteValue : void 0;
  const rawLines = JSON.parse(String(form.get("linesJson") || "[]"));
  if (quoteAudience === "contractor" && !companyName) {
    return data({
      allowed: true,
      products,
      recentQuotes,
      ok: false,
      message: "Company Name is required for contractor quotes.",
      customerName,
      companyName,
      customerEmail,
      customerPhone,
      address: {
        address1,
        address2,
        city,
        province,
        postalCode,
        country
      },
      quoteAudience,
      contractorTier,
      customDeliveryAmount: customDeliveryAmountInput,
      customRatePerMinute: customRatePerMinuteInput,
      customTaxRate: customTaxRateInput,
      customShippingQuantity: customShippingQuantityInput,
      customShippingUnit,
      customShippingRate: customShippingRateInput,
      customNotes,
      googleMapsApiKey: getBrowserGoogleMapsApiKey$1()
    }, {
      status: 400
    });
  }
  const selectedProducts = rawLines.map((line) => {
    const sku = String((line == null ? void 0 : line.sku) || "").trim();
    const quantity = Number((line == null ? void 0 : line.quantity) || 0);
    const product = products.find((p) => p.sku === sku);
    const baseUnitPrice = product ? getUnitPriceForProduct(product, quoteAudience, contractorTier) : 0;
    const overrideTitle = String((line == null ? void 0 : line.customTitle) || "").trim();
    const rawCustomPrice = String((line == null ? void 0 : line.customPrice) || "").trim();
    const overridePrice = quoteAudience === "custom" && rawCustomPrice !== "" ? Number(rawCustomPrice) : null;
    const unitPrice = overridePrice !== null && Number.isFinite(overridePrice) ? overridePrice : baseUnitPrice;
    if (!sku || quantity <= 0 || !product) return null;
    return {
      title: overrideTitle || product.title,
      sku: product.sku,
      vendor: product.vendor,
      unitLabel: product.unitLabel || "",
      quantity,
      price: unitPrice
    };
  }).filter(Boolean);
  if (selectedProducts.length === 0) {
    return data({
      allowed: true,
      products,
      recentQuotes,
      ok: false,
      message: "Add at least one product line with a selected product and quantity greater than 0.",
      customerName,
      companyName,
      customerEmail,
      customerPhone,
      address: {
        address1,
        address2,
        city,
        province,
        postalCode,
        country
      },
      quoteAudience,
      contractorTier,
      customDeliveryAmount: customDeliveryAmountInput,
      customRatePerMinute: customRatePerMinuteInput,
      customTaxRate: customTaxRateInput,
      customShippingQuantity: customShippingQuantityInput,
      customShippingUnit,
      customShippingRate: customShippingRateInput,
      customNotes,
      googleMapsApiKey: getBrowserGoogleMapsApiKey$1()
    }, {
      status: 400
    });
  }
  if (!address1 || !city || !province || !postalCode) {
    return data({
      allowed: true,
      products,
      recentQuotes,
      ok: false,
      message: "Address 1, city, state, and ZIP are required.",
      customerName,
      companyName,
      customerEmail,
      customerPhone,
      address: {
        address1,
        address2,
        city,
        province,
        postalCode,
        country
      },
      quoteAudience,
      contractorTier,
      customDeliveryAmount: customDeliveryAmountInput,
      customRatePerMinute: customRatePerMinuteInput,
      customTaxRate: customTaxRateInput,
      customShippingQuantity: customShippingQuantityInput,
      customShippingUnit,
      customShippingRate: customShippingRateInput,
      customNotes,
      googleMapsApiKey: getBrowserGoogleMapsApiKey$1()
    }, {
      status: 400
    });
  }
  const shop = process.env.SHOPIFY_STORE_DOMAIN || "darfaz-2e.myshopify.com";
  const deliveryQuote = await getQuote({
    shop,
    postalCode,
    country,
    province,
    city,
    address1,
    address2,
    ratePerMinute: customRatePerMinute,
    items: selectedProducts.map((item) => ({
      sku: item.sku,
      quantity: item.quantity,
      requiresShipping: true,
      pickupVendor: item.vendor,
      price: item.price
    }))
  });
  const productsSubtotal = selectedProducts.reduce((sum, item) => sum + Number(item.price || 0) * item.quantity, 0);
  const deliveryAmount = Number(deliveryQuote.cents || 0) / 100;
  const effectiveDeliveryAmount = hasCustomShippingCalculation ? customShippingQuantityValue * customShippingRateValue : quoteAudience === "custom" && customDeliveryAmountInput !== "" ? Number.isFinite(customDeliveryAmountValue) ? customDeliveryAmountValue : deliveryAmount : deliveryAmount;
  const taxableSubtotal = productsSubtotal + effectiveDeliveryAmount;
  const taxRate = quoteAudience === "custom" && customTaxRateInput !== "" ? Number.isFinite(customTaxRateValue) ? customTaxRateValue : Number(process.env.QUOTE_TAX_RATE || "0") : Number(process.env.QUOTE_TAX_RATE || "0");
  const taxAmount = taxableSubtotal * taxRate;
  const totalAmount = taxableSubtotal + taxAmount;
  const effectiveServiceName = deliveryQuote.serviceName;
  const effectiveEta = deliveryQuote.eta;
  const effectiveSummary = deliveryQuote.summary;
  const effectiveDescription = quoteAudience === "custom" && customNotes ? customNotes : deliveryQuote.description;
  const shippingCalculationText = hasCustomShippingCalculation ? `${customShippingQuantityValue.toFixed(2)} ${customShippingUnit} x $${customShippingRateValue.toFixed(2)} = $${effectiveDeliveryAmount.toFixed(2)}` : null;
  const savedShippingDetails = shippingCalculationText || `Delivery Fee: $${effectiveDeliveryAmount.toFixed(2)}`;
  const sourceBreakdown = getSourceBreakdown(selectedProducts);
  let savedQuoteId = null;
  if (intent === "save") {
    const currentUser = await getCurrentUser(request);
    const saved = await saveCustomQuote({
      shop,
      customerName,
      customerEmail,
      customerPhone,
      address1,
      address2,
      city,
      province,
      postalCode,
      country,
      quoteTotalCents: Math.round(totalAmount * 100),
      serviceName: effectiveServiceName,
      shippingDetails: savedShippingDetails,
      description: `${effectiveDescription} Pricing: ${pricingLabel}.`,
      eta: effectiveEta,
      summary: void 0,
      createdByUserId: (currentUser == null ? void 0 : currentUser.id) || null,
      createdByName: (currentUser == null ? void 0 : currentUser.name) || (currentUser == null ? void 0 : currentUser.email) || "Legacy admin",
      createdByEmail: (currentUser == null ? void 0 : currentUser.email) || null,
      sourceBreakdown,
      lineItems: selectedProducts.map((product) => {
        var _a2;
        return {
          ...product,
          variantId: ((_a2 = products.find((entry2) => entry2.sku === product.sku)) == null ? void 0 : _a2.variantId) || null,
          audience: quoteAudience,
          contractorTier: quoteAudience === "contractor" ? contractorTier : null,
          pricingLabel
        };
      })
    });
    savedQuoteId = saved.id;
    await logAuditEvent({
      actor: currentUser,
      action: "create_quote",
      targetType: "quote",
      targetId: saved.id,
      targetLabel: customerName || customerEmail || saved.id,
      details: {
        customerName,
        customerEmail,
        totalCents: Math.round(totalAmount * 100),
        audience: quoteAudience,
        contractorTier: quoteAudience === "contractor" ? contractorTier : null
      }
    });
  }
  return data({
    allowed: true,
    products,
    recentQuotes,
    ok: true,
    customerName,
    companyName,
    customerEmail,
    customerPhone,
    address: {
      address1,
      address2,
      city,
      province,
      postalCode,
      country
    },
    googleMapsApiKey: getBrowserGoogleMapsApiKey$1(),
    savedQuoteId,
    selectedLines: selectedProducts,
    sourceBreakdown,
    pricing: {
      pricingLabel,
      productsSubtotal,
      deliveryAmount: effectiveDeliveryAmount,
      taxRate,
      taxAmount,
      totalAmount
    },
    deliveryQuote: {
      ...deliveryQuote,
      serviceName: effectiveServiceName,
      eta: effectiveEta,
      summary: effectiveSummary,
      description: effectiveDescription,
      cents: Math.round(effectiveDeliveryAmount * 100)
    },
    quoteAudience,
    contractorTier,
    customDeliveryAmount: customDeliveryAmountInput,
    customRatePerMinute: customRatePerMinuteInput,
    customTaxRate: customTaxRateInput,
    customShippingQuantity: customShippingQuantityInput,
    customShippingUnit,
    customShippingRate: customShippingRateInput,
    shippingCalculationText,
    customNotes
  });
}
const styles$d = {
  page: {
    minHeight: "100vh",
    background: "radial-gradient(circle at top, #1f2937 0%, #111827 45%, #030712 100%)",
    color: "#f9fafb",
    padding: "32px 20px 60px",
    fontFamily: 'Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif'
  },
  shell: {
    maxWidth: "1280px",
    margin: "0 auto"
  },
  hero: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    gap: "20px",
    marginBottom: "24px",
    flexWrap: "wrap"
  },
  title: {
    margin: 0,
    fontSize: "34px",
    fontWeight: 800,
    letterSpacing: "-0.02em"
  },
  subtitle: {
    marginTop: "8px",
    color: "#9ca3af",
    fontSize: "15px"
  },
  logout: {
    color: "#cbd5e1",
    textDecoration: "none",
    border: "1px solid #374151",
    background: "rgba(17, 24, 39, 0.75)",
    padding: "10px 14px",
    borderRadius: "10px",
    fontWeight: 600
  },
  card: {
    background: "rgba(17, 24, 39, 0.88)",
    border: "1px solid #1f2937",
    borderRadius: "18px",
    padding: "22px",
    boxShadow: "0 20px 40px rgba(0,0,0,0.25)",
    backdropFilter: "blur(10px)"
  },
  sectionTitle: {
    margin: "0 0 14px 0",
    fontSize: "20px",
    fontWeight: 700,
    color: "#f8fafc"
  },
  sectionSub: {
    margin: "0 0 18px 0",
    color: "#9ca3af",
    fontSize: "14px"
  },
  label: {
    display: "block",
    fontSize: "13px",
    fontWeight: 600,
    color: "#d1d5db",
    marginBottom: "6px"
  },
  input: {
    width: "100%",
    background: "#0f172a",
    color: "#f8fafc",
    border: "1px solid #334155",
    borderRadius: "12px",
    padding: "12px 14px",
    fontSize: "14px",
    outline: "none"
  },
  buttonPrimary: {
    background: "linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%)",
    color: "#fff",
    border: "none",
    borderRadius: "12px",
    padding: "12px 18px",
    fontWeight: 700,
    cursor: "pointer",
    boxShadow: "0 10px 24px rgba(37, 99, 235, 0.35)"
  },
  buttonSecondary: {
    background: "#0f766e",
    color: "#fff",
    border: "none",
    borderRadius: "12px",
    padding: "12px 18px",
    fontWeight: 700,
    cursor: "pointer",
    boxShadow: "0 10px 24px rgba(15, 118, 110, 0.35)"
  },
  buttonGhost: {
    background: "#111827",
    color: "#e5e7eb",
    border: "1px solid #374151",
    borderRadius: "12px",
    padding: "12px 18px",
    fontWeight: 600,
    cursor: "pointer"
  },
  tabRow: {
    display: "flex",
    gap: "10px",
    flexWrap: "wrap",
    marginBottom: "18px"
  },
  tabButton: {
    borderRadius: "999px",
    padding: "10px 16px",
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#cbd5e1",
    cursor: "pointer",
    fontWeight: 700
  },
  tabButtonActive: {
    background: "linear-gradient(135deg, #0f766e 0%, #115e59 100%)",
    color: "#f0fdfa",
    border: "1px solid #14b8a6",
    boxShadow: "0 10px 24px rgba(20, 184, 166, 0.2)"
  },
  statusOk: {
    marginTop: "18px",
    padding: "14px 16px",
    borderRadius: "14px",
    background: "rgba(22, 163, 74, 0.15)",
    border: "1px solid rgba(34, 197, 94, 0.5)",
    color: "#dcfce7"
  },
  statusErr: {
    marginTop: "18px",
    padding: "14px 16px",
    borderRadius: "14px",
    background: "rgba(220, 38, 38, 0.15)",
    border: "1px solid rgba(248, 113, 113, 0.5)",
    color: "#fee2e2"
  }
};
const customQuote = UNSAFE_withComponentProps(function PublicCustomQuotePage() {
  var _a2, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k;
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const draftOrderFetcher = useFetcher();
  const deleteQuoteFetcher = useFetcher();
  const navigation = useNavigation();
  const location = useLocation();
  const isSubmitting = navigation.state === "submitting";
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const products = (actionData == null ? void 0 : actionData.products) ?? loaderData.products ?? [];
  const currentUser = (actionData == null ? void 0 : actionData.currentUser) ?? loaderData.currentUser ?? null;
  const recentQuotes = (actionData == null ? void 0 : actionData.recentQuotes) ?? loaderData.recentQuotes ?? [];
  const googleMapsApiKey = (actionData == null ? void 0 : actionData.googleMapsApiKey) ?? loaderData.googleMapsApiKey ?? "";
  const embeddedQs = location.search || "";
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const urlParams = new URLSearchParams(location.search);
  const initialAudience = normalizeQuoteAudience(urlParams.get("audience"));
  const initialTier = normalizeContractorTier(urlParams.get("tier"));
  const createDraftOrderAction = location.pathname.startsWith("/app/") ? `/app/api/create-draft-order${embeddedQs}` : `/api/create-draft-order${embeddedQs}`;
  const deleteQuoteAction = location.pathname.startsWith("/app/") ? `/app/api/delete-quote${embeddedQs}` : `/api/delete-quote${embeddedQs}`;
  const quoteReviewHref = isEmbeddedRoute ? "/app/quote-review" : "/quote-review";
  const dispatchHref = isEmbeddedRoute ? "/app/classic" : "/classic";
  const logoutHref = isEmbeddedRoute ? "/app/custom-quote?logout=1" : "/custom-quote?logout=1";
  const mobileDashboardHref = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  const loginHref = `/login?next=${encodeURIComponent(location.pathname + location.search)}`;
  const canAccess = (permission) => {
    var _a3;
    return !currentUser || ((_a3 = currentUser.permissions) == null ? void 0 : _a3.includes(permission));
  };
  const [googleStatus, setGoogleStatus] = useState("Not loaded");
  const [quoteAudience, setQuoteAudience] = useState(normalizeQuoteAudience((actionData == null ? void 0 : actionData.quoteAudience) ?? initialAudience));
  const [contractorTier, setContractorTier] = useState(normalizeContractorTier((actionData == null ? void 0 : actionData.contractorTier) ?? initialTier));
  const [companyName, setCompanyName] = useState((actionData == null ? void 0 : actionData.companyName) || "");
  const [lines, setLines] = useState([{
    sku: "",
    quantity: "",
    search: "",
    customTitle: "",
    customPrice: ""
  }]);
  const [selectedHistoryQuoteId, setSelectedHistoryQuoteId] = useState(null);
  const [isMobile, setIsMobile] = useState(false);
  const [historyDetailsOpen, setHistoryDetailsOpen] = useState({
    customer: true,
    lineItems: false,
    sourceBreakdown: false
  });
  const deferredLines = useDeferredValue(lines);
  const productSearchIndex = useMemo(() => products.map((product) => ({
    product,
    haystack: `${product.title} ${product.sku} ${product.vendor}`.toLowerCase()
  })), [products]);
  useEffect(() => {
    if (!allowed) return;
    if (!googleMapsApiKey) {
      setGoogleStatus("Missing API key");
      return;
    }
    loadGooglePlaces(googleMapsApiKey).then(() => {
      attachAddressAutocomplete({
        address1Id: "quote-address1",
        cityId: "quote-city",
        provinceId: "quote-province",
        postalCodeId: "quote-postalCode",
        countryId: "quote-country"
      });
      setGoogleStatus("Loaded");
    }).catch((error) => {
      console.error("[GOOGLE PLACES LOAD ERROR]", error);
      setGoogleStatus(`Error: ${error.message}`);
    });
  }, [allowed, googleMapsApiKey]);
  useEffect(() => {
    if (typeof window === "undefined") return;
    const media = window.matchMedia("(max-width: 820px)");
    const updateViewport = () => setIsMobile(media.matches);
    updateViewport();
    media.addEventListener("change", updateViewport);
    return () => media.removeEventListener("change", updateViewport);
  }, []);
  useEffect(() => {
    var _a3, _b2;
    if (((_a3 = deleteQuoteFetcher.data) == null ? void 0 : _a3.ok) && ((_b2 = deleteQuoteFetcher.data) == null ? void 0 : _b2.deletedQuoteId)) {
      setSelectedHistoryQuoteId((current) => current === deleteQuoteFetcher.data.deletedQuoteId ? null : current);
    }
  }, [deleteQuoteFetcher.data]);
  useEffect(() => {
    setQuoteAudience(normalizeQuoteAudience((actionData == null ? void 0 : actionData.quoteAudience) ?? initialAudience));
    setContractorTier(normalizeContractorTier((actionData == null ? void 0 : actionData.contractorTier) ?? initialTier));
    setCompanyName((actionData == null ? void 0 : actionData.companyName) || "");
  }, [actionData == null ? void 0 : actionData.quoteAudience, actionData == null ? void 0 : actionData.contractorTier, actionData == null ? void 0 : actionData.companyName, initialAudience, initialTier]);
  const quoteText = useMemo(() => {
    var _a3;
    if (!(actionData == null ? void 0 : actionData.pricing) || !(actionData == null ? void 0 : actionData.deliveryQuote)) return "";
    const linesText = ((_a3 = actionData.selectedLines) == null ? void 0 : _a3.map((line) => `${formatQuantityWithUnit(Number(line.quantity || 0), line.unitLabel)} ${line.title}: $${(Number(line.price || 0) * Number(line.quantity || 0)).toFixed(2)}`).join("\n")) || "";
    return [linesText, `Delivery Fee: $${Number(actionData.pricing.deliveryAmount).toFixed(2)}`, `Tax: $${Number(actionData.pricing.taxAmount).toFixed(2)}`, `Total: $${Number(actionData.pricing.totalAmount).toFixed(2)}`, "", "Please let us know if you have any questions or would like to proceed with your order."].filter(Boolean).join("\n");
  }, [actionData]);
  const selectedHistoryQuote = useMemo(() => recentQuotes.find((quote) => quote.id === selectedHistoryQuoteId) || null, [recentQuotes, selectedHistoryQuoteId]);
  const mobileActionButtonStyle = {
    ...styles$d.buttonGhost,
    minHeight: isMobile ? 48 : void 0,
    width: isMobile ? "100%" : void 0,
    justifyContent: "center"
  };
  const mobileTabLinkStyle = (active) => ({
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    gap: 4,
    minHeight: 56,
    borderRadius: 14,
    textDecoration: "none",
    color: active ? "#38bdf8" : "#94a3b8",
    background: active ? "rgba(14, 165, 233, 0.12)" : "transparent",
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: "0.01em"
  });
  const mobileTabIconStyle = (active) => ({
    width: 24,
    height: 24,
    borderRadius: 999,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    background: active ? "rgba(14, 165, 233, 0.18)" : "rgba(51, 65, 85, 0.35)",
    color: active ? "#38bdf8" : "#cbd5e1",
    fontSize: 12,
    lineHeight: 1
  });
  const mobileBottomNavStyle = {
    position: "fixed",
    left: 12,
    right: 12,
    bottom: "calc(env(safe-area-inset-bottom, 0px) + 12px)",
    zIndex: 30,
    display: "grid",
    gridTemplateColumns: "repeat(4, 1fr)",
    gap: 10,
    padding: "8px 10px",
    borderRadius: 20,
    background: "rgba(15, 23, 42, 0.96)",
    border: "1px solid rgba(30, 41, 59, 0.95)",
    boxShadow: "0 18px 38px rgba(2, 6, 23, 0.45)",
    backdropFilter: "blur(14px)"
  };
  function toggleHistorySection(key) {
    setHistoryDetailsOpen((current) => ({
      ...current,
      [key]: !current[key]
    }));
  }
  const historyQuoteText = useMemo(() => {
    var _a3;
    if (!selectedHistoryQuote) return "";
    const linesText = ((_a3 = selectedHistoryQuote.line_items) == null ? void 0 : _a3.map((line) => {
      const lineTotal = Number(line.price || 0) * Number(line.quantity || 0);
      return `${line.title} (${line.sku}) x ${line.quantity} — $${lineTotal.toFixed(2)}`;
    }).join("\n")) || "";
    return [`Customer: ${selectedHistoryQuote.customer_name || ""}`, `Email: ${selectedHistoryQuote.customer_email || ""}`, `Phone: ${selectedHistoryQuote.customer_phone || ""}`, `Address: ${selectedHistoryQuote.address1 || ""}, ${selectedHistoryQuote.city || ""}, ${selectedHistoryQuote.province || ""} ${selectedHistoryQuote.postal_code || ""}`, `Total: $${(Number(selectedHistoryQuote.quote_total_cents || 0) / 100).toFixed(2)}`, `Service: ${selectedHistoryQuote.service_name || ""}`, selectedHistoryQuote.shipping_details ? `Shipping Details: ${selectedHistoryQuote.shipping_details}` : null, `ETA: ${selectedHistoryQuote.eta || ""}`, `Summary: ${selectedHistoryQuote.summary || ""}`, `Notes: ${selectedHistoryQuote.description || ""}`, "", linesText].filter(Boolean).join("\n");
  }, [selectedHistoryQuote]);
  function updateLine(index, patch) {
    setLines((prev) => prev.map((line, i) => i === index ? {
      ...line,
      ...patch
    } : line));
  }
  function addLine() {
    setLines((prev) => [...prev, {
      sku: "",
      quantity: "",
      search: "",
      customTitle: "",
      customPrice: ""
    }]);
  }
  function removeLine(index) {
    setLines((prev) => prev.filter((_, i) => i !== index));
  }
  function filteredProducts(index) {
    var _a3;
    const search = (((_a3 = deferredLines[index]) == null ? void 0 : _a3.search) || "").toLowerCase().trim();
    if (!search) return [];
    return productSearchIndex.filter((entry2) => entry2.haystack.includes(search)).map((entry2) => entry2.product).slice(0, 12);
  }
  function handleQuoteSubmit(event) {
    if (quoteAudience === "contractor" && !companyName.trim()) {
      event.preventDefault();
      alert("Company Name is required for contractor quotes.");
    }
  }
  async function copyQuote() {
    if (!quoteText) return;
    await navigator.clipboard.writeText(quoteText);
    alert("Quote copied");
  }
  async function copyHistoryQuote() {
    if (!historyQuoteText) return;
    await navigator.clipboard.writeText(historyQuoteText);
    alert("Saved quote copied");
  }
  if (!allowed) {
    return /* @__PURE__ */ jsx("div", {
      style: styles$d.page,
      children: /* @__PURE__ */ jsx("div", {
        style: {
          ...styles$d.shell,
          maxWidth: "520px"
        },
        children: /* @__PURE__ */ jsxs("div", {
          style: styles$d.card,
          children: [/* @__PURE__ */ jsx("h1", {
            style: styles$d.title,
            children: "Custom Quote Portal"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$d.subtitle,
            children: "Sign in with your contractor user account to access the quote tool."
          }), /* @__PURE__ */ jsx("a", {
            href: loginHref,
            style: {
              ...styles$d.buttonPrimary,
              marginTop: "18px",
              width: "100%",
              minHeight: 48,
              display: "inline-flex",
              alignItems: "center",
              justifyContent: "center",
              textDecoration: "none"
            },
            children: "Sign In"
          })]
        })
      })
    });
  }
  return /* @__PURE__ */ jsxs("div", {
    style: {
      ...styles$d.page,
      padding: isMobile ? "20px 14px 120px" : styles$d.page.padding,
      overflowX: "clip"
    },
    children: [/* @__PURE__ */ jsxs("div", {
      style: styles$d.shell,
      children: [isMobile ? /* @__PURE__ */ jsxs("div", {
        style: {
          marginBottom: 18
        },
        children: [/* @__PURE__ */ jsx("h1", {
          style: {
            ...styles$d.title,
            fontSize: "28px"
          },
          children: "Custom Quote Tool"
        }), /* @__PURE__ */ jsx("div", {
          style: styles$d.subtitle,
          children: "Full quote builder with products, delivery, tax, images, and saved history."
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            marginTop: 8,
            color: "#64748b",
            fontSize: 13
          },
          children: ["Loaded products: ", products.length, " · Google Places: ", googleStatus]
        })]
      }) : /* @__PURE__ */ jsxs("div", {
        style: styles$d.hero,
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("h1", {
            style: styles$d.title,
            children: "Custom Quote Tool"
          }), /* @__PURE__ */ jsx("div", {
            style: styles$d.subtitle,
            children: "Full quote builder with products, delivery, tax, images, and saved history."
          }), /* @__PURE__ */ jsxs("div", {
            style: {
              marginTop: 8,
              color: "#64748b",
              fontSize: 13
            },
            children: ["Loaded products: ", products.length, " · Google Places: ", googleStatus]
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            display: "flex",
            gap: 12,
            flexWrap: "wrap"
          },
          children: [canAccess("quoteTool") ? /* @__PURE__ */ jsx("a", {
            href: mobileDashboardHref,
            style: styles$d.logout,
            children: "Dashboard"
          }) : null, canAccess("dispatch") ? /* @__PURE__ */ jsx("a", {
            href: dispatchHref,
            style: styles$d.logout,
            children: "Dispatch"
          }) : null, canAccess("reviewQuotes") ? /* @__PURE__ */ jsx("a", {
            href: quoteReviewHref,
            style: styles$d.logout,
            children: "Review Quotes"
          }) : null, canAccess("manageUsers") ? /* @__PURE__ */ jsx("a", {
            href: "/settings",
            style: styles$d.logout,
            children: "Settings"
          }) : null, currentUser ? /* @__PURE__ */ jsx("a", {
            href: "/change-password",
            style: styles$d.logout,
            children: "Change Password"
          }) : null, /* @__PURE__ */ jsx("a", {
            href: currentUser ? "/login?logout=1" : logoutHref,
            style: styles$d.logout,
            children: "Log out"
          })]
        })]
      }), /* @__PURE__ */ jsxs(Form, {
        method: "post",
        style: {
          display: "grid",
          gap: "22px"
        },
        onSubmit: handleQuoteSubmit,
        children: [/* @__PURE__ */ jsx("input", {
          type: "hidden",
          name: "quoteAudience",
          value: quoteAudience
        }), /* @__PURE__ */ jsx("input", {
          type: "hidden",
          name: "contractorTier",
          value: contractorTier
        }), /* @__PURE__ */ jsx("input", {
          type: "hidden",
          name: "linesJson",
          value: JSON.stringify(lines)
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            ...styles$d.card,
            padding: isMobile ? "18px" : styles$d.card.padding
          },
          children: [/* @__PURE__ */ jsx("h2", {
            style: styles$d.sectionTitle,
            children: "Quote Type"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$d.sectionSub,
            children: "Switch between standard customer pricing and contractor tier pricing."
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$d.tabRow,
            children: [/* @__PURE__ */ jsx("button", {
              type: "button",
              onClick: () => setQuoteAudience("customer"),
              style: {
                ...styles$d.tabButton,
                minHeight: isMobile ? 46 : void 0,
                flex: isMobile ? "1 1 110px" : void 0,
                textAlign: "center",
                ...quoteAudience === "customer" ? styles$d.tabButtonActive : {}
              },
              children: "Customer"
            }), /* @__PURE__ */ jsx("button", {
              type: "button",
              onClick: () => setQuoteAudience("contractor"),
              style: {
                ...styles$d.tabButton,
                minHeight: isMobile ? 46 : void 0,
                flex: isMobile ? "1 1 110px" : void 0,
                textAlign: "center",
                ...quoteAudience === "contractor" ? styles$d.tabButtonActive : {}
              },
              children: "Contractor"
            }), /* @__PURE__ */ jsx("button", {
              type: "button",
              onClick: () => setQuoteAudience("custom"),
              style: {
                ...styles$d.tabButton,
                minHeight: isMobile ? 46 : void 0,
                flex: isMobile ? "1 1 110px" : void 0,
                textAlign: "center",
                ...quoteAudience === "custom" ? styles$d.tabButtonActive : {}
              },
              children: "Custom"
            })]
          }), quoteAudience === "contractor" ? /* @__PURE__ */ jsxs("div", {
            style: {
              maxWidth: 280
            },
            children: [/* @__PURE__ */ jsx("label", {
              style: styles$d.label,
              children: "Contractor Tier"
            }), /* @__PURE__ */ jsxs("select", {
              name: "contractorTierUi",
              value: contractorTier,
              onChange: (e) => setContractorTier(normalizeContractorTier(e.target.value)),
              style: styles$d.input,
              children: [/* @__PURE__ */ jsx("option", {
                value: "tier1",
                children: "Tier 1"
              }), /* @__PURE__ */ jsx("option", {
                value: "tier2",
                children: "Tier 2"
              })]
            })]
          }) : quoteAudience === "custom" ? /* @__PURE__ */ jsx("div", {
            style: {
              color: "#93c5fd",
              fontSize: 14
            },
            children: "Custom mode keeps the same quote flow but lets you override line titles, unit prices, delivery, shipping math, tax, and notes."
          }) : null]
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            ...styles$d.card,
            padding: isMobile ? "18px" : styles$d.card.padding
          },
          children: [/* @__PURE__ */ jsx("h2", {
            style: styles$d.sectionTitle,
            children: "Customer & Delivery Address"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$d.sectionSub,
            children: "Start typing the street address and choose a suggestion."
          }), /* @__PURE__ */ jsxs("div", {
            style: {
              display: "grid",
              gap: "14px"
            },
            children: [/* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("label", {
                style: styles$d.label,
                children: "Customer Name"
              }), /* @__PURE__ */ jsx("input", {
                type: "text",
                name: "customerName",
                autoComplete: "name",
                defaultValue: (actionData == null ? void 0 : actionData.customerName) || "",
                style: styles$d.input
              })]
            }), quoteAudience === "contractor" ? /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("label", {
                style: styles$d.label,
                children: "Company Name"
              }), /* @__PURE__ */ jsx("input", {
                type: "text",
                name: "companyName",
                autoComplete: "organization",
                required: true,
                value: companyName,
                onChange: (event) => setCompanyName(event.target.value),
                style: styles$d.input
              })]
            }) : /* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "companyName",
              value: companyName
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("label", {
                style: styles$d.label,
                children: "Email Address"
              }), /* @__PURE__ */ jsx("input", {
                type: "email",
                name: "customerEmail",
                autoComplete: "email",
                defaultValue: (actionData == null ? void 0 : actionData.customerEmail) || "",
                style: styles$d.input
              })]
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("label", {
                style: styles$d.label,
                children: "Phone Number"
              }), /* @__PURE__ */ jsx("input", {
                type: "tel",
                name: "customerPhone",
                autoComplete: "tel",
                defaultValue: (actionData == null ? void 0 : actionData.customerPhone) || "",
                style: styles$d.input
              })]
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("label", {
                style: styles$d.label,
                children: "Address 1"
              }), /* @__PURE__ */ jsx("input", {
                id: "quote-address1",
                type: "text",
                name: "address1",
                autoComplete: "street-address",
                defaultValue: ((_a2 = actionData == null ? void 0 : actionData.address) == null ? void 0 : _a2.address1) || "",
                style: styles$d.input
              })]
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("label", {
                style: styles$d.label,
                children: "Address 2"
              }), /* @__PURE__ */ jsx("input", {
                type: "text",
                name: "address2",
                autoComplete: "address-line2",
                defaultValue: ((_b = actionData == null ? void 0 : actionData.address) == null ? void 0 : _b.address2) || "",
                style: styles$d.input
              })]
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gridTemplateColumns: isMobile ? "repeat(2, minmax(0, 1fr))" : "1.3fr 0.8fr 0.8fr 0.8fr",
                gap: "14px"
              },
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "City"
                }), /* @__PURE__ */ jsx("input", {
                  id: "quote-city",
                  type: "text",
                  name: "city",
                  autoComplete: "address-level2",
                  defaultValue: ((_c = actionData == null ? void 0 : actionData.address) == null ? void 0 : _c.city) || "",
                  style: styles$d.input
                })]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "State"
                }), /* @__PURE__ */ jsx("input", {
                  id: "quote-province",
                  type: "text",
                  name: "province",
                  autoComplete: "address-level1",
                  defaultValue: ((_d = actionData == null ? void 0 : actionData.address) == null ? void 0 : _d.province) || "WI",
                  style: styles$d.input
                })]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "ZIP"
                }), /* @__PURE__ */ jsx("input", {
                  id: "quote-postalCode",
                  type: "text",
                  name: "postalCode",
                  autoComplete: "postal-code",
                  defaultValue: ((_e = actionData == null ? void 0 : actionData.address) == null ? void 0 : _e.postalCode) || "",
                  style: styles$d.input
                })]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "Country"
                }), /* @__PURE__ */ jsx("input", {
                  id: "quote-country",
                  type: "text",
                  name: "country",
                  autoComplete: "country-name",
                  defaultValue: ((_f = actionData == null ? void 0 : actionData.address) == null ? void 0 : _f.country) || "US",
                  style: styles$d.input
                })]
              })]
            })]
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            ...styles$d.card,
            padding: isMobile ? "18px" : styles$d.card.padding
          },
          children: [/* @__PURE__ */ jsxs("div", {
            style: {
              display: "flex",
              justifyContent: "space-between",
              alignItems: isMobile ? "flex-start" : "center",
              gap: "16px",
              marginBottom: "14px",
              flexWrap: "wrap"
            },
            children: [/* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("h2", {
                style: styles$d.sectionTitle,
                children: "Quote Lines"
              }), /* @__PURE__ */ jsx("p", {
                style: styles$d.sectionSub,
                children: "Search by product, SKU, or vendor. Click a result to select it."
              })]
            }), /* @__PURE__ */ jsx("button", {
              type: "button",
              onClick: addLine,
              style: styles$d.buttonGhost,
              children: "Add Line"
            })]
          }), /* @__PURE__ */ jsx("div", {
            style: {
              display: "grid",
              gap: "16px"
            },
            children: lines.map((line, index) => {
              const selectedProduct = products.find((p) => p.sku === line.sku);
              const matches = filteredProducts(index);
              return /* @__PURE__ */ jsxs("div", {
                style: {
                  border: "1px solid #1f2937",
                  background: "rgba(2, 6, 23, 0.72)",
                  borderRadius: "16px",
                  padding: isMobile ? "14px" : "16px",
                  display: "grid",
                  gap: "12px",
                  overflowX: "clip"
                },
                children: [/* @__PURE__ */ jsxs("div", {
                  style: {
                    display: "grid",
                    gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "minmax(360px, 1fr) 160px 120px",
                    gap: "12px",
                    alignItems: "end"
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$d.label,
                      children: "Search Product"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "text",
                      value: line.search,
                      onChange: (e) => updateLine(index, {
                        search: e.target.value,
                        sku: ""
                      }),
                      placeholder: "Type product name, SKU, or vendor",
                      style: styles$d.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$d.label,
                      children: "Quantity"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "number",
                      min: "0",
                      step: "1",
                      value: line.quantity,
                      onChange: (e) => updateLine(index, {
                        quantity: e.target.value
                      }),
                      style: styles$d.input
                    })]
                  }), /* @__PURE__ */ jsx("button", {
                    type: "button",
                    onClick: () => removeLine(index),
                    disabled: lines.length === 1,
                    style: {
                      ...styles$d.buttonGhost,
                      minHeight: isMobile ? 46 : void 0,
                      width: isMobile ? "100%" : void 0
                    },
                    children: "Remove"
                  })]
                }), selectedProduct ? /* @__PURE__ */ jsxs("div", {
                  style: {
                    display: "flex",
                    alignItems: isMobile ? "flex-start" : "center",
                    flexWrap: isMobile ? "wrap" : "nowrap",
                    gap: 12,
                    padding: "12px 14px",
                    borderRadius: "12px",
                    background: "rgba(37, 99, 235, 0.12)",
                    border: "1px solid rgba(96, 165, 250, 0.35)",
                    color: "#dbeafe"
                  },
                  children: [selectedProduct.imageUrl ? /* @__PURE__ */ jsx("img", {
                    src: selectedProduct.imageUrl,
                    alt: selectedProduct.title,
                    loading: "lazy",
                    decoding: "async",
                    style: {
                      width: 52,
                      height: 52,
                      objectFit: "cover",
                      borderRadius: 8,
                      border: "1px solid rgba(255,255,255,0.08)",
                      flexShrink: 0
                    }
                  }) : /* @__PURE__ */ jsx("div", {
                    style: {
                      width: 52,
                      height: 52,
                      borderRadius: 8,
                      background: "#1e293b",
                      border: "1px solid #334155",
                      flexShrink: 0
                    }
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: {
                        fontWeight: 700
                      },
                      children: quoteAudience === "custom" && line.customTitle ? line.customTitle : selectedProduct.title
                    }), /* @__PURE__ */ jsxs("div", {
                      style: {
                        fontSize: 13,
                        color: "#bfdbfe"
                      },
                      children: [selectedProduct.sku, " — ", selectedProduct.vendor]
                    }), /* @__PURE__ */ jsxs("div", {
                      style: {
                        fontSize: 13,
                        color: "#bfdbfe"
                      },
                      children: ["Unit Price: $", (() => {
                        const customPriceValue = Number(line.customPrice || "");
                        const displayPrice = quoteAudience === "custom" && String(line.customPrice || "").trim() !== "" && Number.isFinite(customPriceValue) ? customPriceValue : getUnitPriceForProduct(selectedProduct, quoteAudience, contractorTier);
                        return Number(displayPrice).toFixed(2);
                      })()]
                    })]
                  })]
                }) : null, selectedProduct && quoteAudience === "custom" ? /* @__PURE__ */ jsxs("div", {
                  style: {
                    display: "grid",
                    gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "minmax(260px, 1fr) 180px",
                    gap: "12px"
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$d.label,
                      children: "Custom Line Title"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "text",
                      value: line.customTitle || "",
                      onChange: (e) => updateLine(index, {
                        customTitle: e.target.value
                      }),
                      placeholder: selectedProduct.title,
                      style: styles$d.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$d.label,
                      children: "Custom Unit Price"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "number",
                      min: "0",
                      step: "0.01",
                      value: line.customPrice || "",
                      onChange: (e) => updateLine(index, {
                        customPrice: e.target.value
                      }),
                      placeholder: String(getUnitPriceForProduct(selectedProduct, quoteAudience, contractorTier)),
                      style: styles$d.input
                    })]
                  })]
                }) : null, !selectedProduct && line.search.trim() ? /* @__PURE__ */ jsx("div", {
                  style: {
                    border: "1px solid #334155",
                    borderRadius: "14px",
                    maxHeight: "280px",
                    overflowY: "auto",
                    background: "#020617"
                  },
                  children: matches.length === 0 ? /* @__PURE__ */ jsx("div", {
                    style: {
                      padding: "14px",
                      color: "#94a3b8"
                    },
                    children: "No matching products"
                  }) : matches.map((product) => /* @__PURE__ */ jsxs("button", {
                    type: "button",
                    onClick: () => updateLine(index, {
                      sku: product.sku,
                      search: `${product.title} (${product.sku}) — ${product.vendor}`
                    }),
                    style: {
                      display: "flex",
                      alignItems: "center",
                      gap: 12,
                      width: "100%",
                      textAlign: "left",
                      padding: "14px",
                      border: "none",
                      borderBottom: "1px solid #111827",
                      background: "transparent",
                      color: "#f8fafc",
                      cursor: "pointer"
                    },
                    children: [product.imageUrl ? /* @__PURE__ */ jsx("img", {
                      src: product.imageUrl,
                      alt: product.title,
                      loading: "lazy",
                      decoding: "async",
                      style: {
                        width: 44,
                        height: 44,
                        objectFit: "cover",
                        borderRadius: 8,
                        border: "1px solid rgba(255,255,255,0.08)",
                        flexShrink: 0
                      }
                    }) : /* @__PURE__ */ jsx("div", {
                      style: {
                        width: 44,
                        height: 44,
                        borderRadius: 8,
                        background: "#1e293b",
                        border: "1px solid #334155",
                        flexShrink: 0
                      }
                    }), /* @__PURE__ */ jsxs("div", {
                      children: [/* @__PURE__ */ jsx("div", {
                        style: {
                          fontWeight: 700
                        },
                        children: product.title
                      }), /* @__PURE__ */ jsxs("div", {
                        style: {
                          fontSize: "13px",
                          color: "#94a3b8",
                          marginTop: "4px"
                        },
                        children: [product.sku, " — ", product.vendor]
                      }), /* @__PURE__ */ jsxs("div", {
                        style: {
                          fontSize: "13px",
                          color: "#94a3b8",
                          marginTop: "4px"
                        },
                        children: ["$", Number(getUnitPriceForProduct(product, quoteAudience, contractorTier)).toFixed(2)]
                      })]
                    })]
                  }, product.sku))
                }) : null]
              }, index);
            })
          })]
        }), quoteAudience === "custom" ? /* @__PURE__ */ jsxs("div", {
          style: {
            ...styles$d.card,
            padding: isMobile ? "18px" : styles$d.card.padding
          },
          children: [/* @__PURE__ */ jsx("h2", {
            style: styles$d.sectionTitle,
            children: "Custom Adjustments"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$d.sectionSub,
            children: "Override delivery, minute charge, tax, and the customer-facing quote details before calculating or saving."
          }), /* @__PURE__ */ jsxs("div", {
            style: {
              display: "grid",
              gap: "14px"
            },
            children: [/* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "180px 180px 180px",
                gap: "14px"
              },
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "Delivery Amount"
                }), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "customDeliveryAmount",
                  min: "0",
                  step: "0.01",
                  defaultValue: (actionData == null ? void 0 : actionData.customDeliveryAmount) || "",
                  placeholder: "Use calculated delivery",
                  style: styles$d.input
                })]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "Minute Charge"
                }), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "customRatePerMinute",
                  min: "0",
                  step: "0.01",
                  defaultValue: (actionData == null ? void 0 : actionData.customRatePerMinute) || "",
                  placeholder: "Default 2.08",
                  style: styles$d.input
                })]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "Tax Rate"
                }), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "customTaxRate",
                  min: "0",
                  step: "0.0001",
                  defaultValue: (actionData == null ? void 0 : actionData.customTaxRate) || "",
                  placeholder: "Example: 0.055",
                  style: styles$d.input
                })]
              })]
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "160px 160px 180px",
                gap: "14px"
              },
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "Shipping Qty"
                }), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "customShippingQuantity",
                  min: "0",
                  step: "0.01",
                  defaultValue: (actionData == null ? void 0 : actionData.customShippingQuantity) || "",
                  placeholder: "Miles or hours",
                  style: styles$d.input
                })]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "Shipping Unit"
                }), /* @__PURE__ */ jsxs("select", {
                  name: "customShippingUnit",
                  defaultValue: (actionData == null ? void 0 : actionData.customShippingUnit) || "miles",
                  style: styles$d.input,
                  children: [/* @__PURE__ */ jsx("option", {
                    value: "miles",
                    children: "Miles"
                  }), /* @__PURE__ */ jsx("option", {
                    value: "hours",
                    children: "Hours"
                  })]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$d.label,
                  children: "Price Per Unit"
                }), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "customShippingRate",
                  min: "0",
                  step: "0.01",
                  defaultValue: (actionData == null ? void 0 : actionData.customShippingRate) || "",
                  placeholder: "Rate per mile/hour",
                  style: styles$d.input
                })]
              })]
            }), /* @__PURE__ */ jsx("div", {
              style: {
                color: "#93c5fd",
                fontSize: 13
              },
              children: "If shipping quantity and price per unit are both filled in, the delivery amount will use `quantity x rate` and override the manual delivery amount above."
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("label", {
                style: styles$d.label,
                children: "Notes"
              }), /* @__PURE__ */ jsx("textarea", {
                name: "customNotes",
                defaultValue: (actionData == null ? void 0 : actionData.customNotes) || "",
                placeholder: "Use calculated notes",
                style: {
                  ...styles$d.input,
                  minHeight: 110,
                  resize: "vertical"
                }
              })]
            })]
          })]
        }) : null, /* @__PURE__ */ jsxs("div", {
          style: {
            display: "flex",
            gap: "12px",
            flexWrap: "wrap",
            flexDirection: isMobile ? "column" : "row",
            position: isMobile ? "sticky" : "static",
            bottom: isMobile ? "calc(env(safe-area-inset-bottom, 0px) + 76px)" : void 0,
            zIndex: isMobile ? 20 : void 0,
            padding: isMobile ? "12px" : 0,
            borderRadius: isMobile ? 16 : void 0,
            background: isMobile ? "rgba(2, 6, 23, 0.92)" : "transparent",
            border: isMobile ? "1px solid rgba(51, 65, 85, 0.9)" : "none",
            boxShadow: isMobile ? "0 18px 32px rgba(2, 6, 23, 0.4)" : "none",
            backdropFilter: isMobile ? "blur(12px)" : "none"
          },
          children: [/* @__PURE__ */ jsx("button", {
            type: "submit",
            name: "intent",
            value: "quote",
            style: {
              ...styles$d.buttonPrimary,
              width: isMobile ? "100%" : void 0,
              minHeight: isMobile ? 50 : void 0
            },
            children: isSubmitting ? "Calculating..." : "Get Full Quote"
          }), /* @__PURE__ */ jsx("button", {
            type: "submit",
            name: "intent",
            value: "save",
            style: {
              ...styles$d.buttonSecondary,
              width: isMobile ? "100%" : void 0,
              minHeight: isMobile ? 50 : void 0
            },
            children: isSubmitting ? "Saving..." : "Save Quote"
          })]
        })]
      }), (actionData == null ? void 0 : actionData.message) ? /* @__PURE__ */ jsx("div", {
        style: {
          ...actionData.ok ? styles$d.statusOk : styles$d.statusErr,
          fontSize: isMobile ? 16 : void 0,
          fontWeight: isMobile ? 700 : void 0
        },
        children: actionData.message
      }) : null, (actionData == null ? void 0 : actionData.savedQuoteId) ? /* @__PURE__ */ jsxs("div", {
        style: {
          ...styles$d.statusOk,
          fontSize: isMobile ? 16 : void 0,
          fontWeight: isMobile ? 700 : void 0
        },
        children: ["Quote saved successfully. ID: ", actionData.savedQuoteId]
      }) : null, (actionData == null ? void 0 : actionData.pricing) && (actionData == null ? void 0 : actionData.deliveryQuote) ? /* @__PURE__ */ jsxs("div", {
        style: {
          marginTop: "24px",
          display: "grid",
          gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "1.2fr 1fr",
          gap: "20px"
        },
        children: [/* @__PURE__ */ jsxs("div", {
          style: {
            ...styles$d.card,
            padding: isMobile ? "18px" : styles$d.card.padding
          },
          children: [/* @__PURE__ */ jsxs("div", {
            style: {
              display: "flex",
              justifyContent: "space-between",
              alignItems: isMobile ? "flex-start" : "center",
              flexWrap: "wrap",
              gap: "12px",
              marginBottom: "16px"
            },
            children: [/* @__PURE__ */ jsx("h2", {
              style: {
                ...styles$d.sectionTitle,
                margin: 0
              },
              children: "Full Quote Result"
            }), /* @__PURE__ */ jsx("button", {
              type: "button",
              onClick: copyQuote,
              style: styles$d.buttonGhost,
              children: "Copy Quote"
            })]
          }), /* @__PURE__ */ jsxs("div", {
            style: {
              display: "grid",
              gap: "10px",
              color: "#e5e7eb"
            },
            children: [/* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("strong", {
                style: {
                  color: "#93c5fd"
                },
                children: "Pricing:"
              }), " ", actionData.pricing.pricingLabel]
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("strong", {
                style: {
                  color: "#93c5fd"
                },
                children: "Products:"
              }), " $", Number(actionData.pricing.productsSubtotal).toFixed(2)]
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("strong", {
                style: {
                  color: "#93c5fd"
                },
                children: actionData.shippingCalculationText ? "Shipping:" : "Delivery:"
              }), " ", "$", Number(actionData.pricing.deliveryAmount).toFixed(2)]
            }), actionData.shippingCalculationText ? /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("strong", {
                style: {
                  color: "#93c5fd"
                },
                children: "Shipping Calc:"
              }), " ", actionData.shippingCalculationText]
            }) : null, /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("strong", {
                style: {
                  color: "#93c5fd"
                },
                children: "Tax:"
              }), " $", Number(actionData.pricing.taxAmount).toFixed(2)]
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                marginTop: 8,
                paddingTop: 10,
                borderTop: "1px solid #334155",
                fontSize: isMobile ? 22 : 18,
                fontWeight: 800
              },
              children: ["TOTAL: $", Number(actionData.pricing.totalAmount).toFixed(2)]
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                marginTop: 14
              },
              children: [/* @__PURE__ */ jsx("strong", {
                style: {
                  color: "#93c5fd"
                },
                children: "Delivery Service:"
              }), " ", actionData.deliveryQuote.serviceName]
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("strong", {
                style: {
                  color: "#93c5fd"
                },
                children: "ETA:"
              }), " ", actionData.deliveryQuote.eta]
            }), /* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("strong", {
                style: {
                  color: "#93c5fd"
                },
                children: actionData.shippingCalculationText ? "Custom Shipping:" : "Notes:"
              }), " ", actionData.deliveryQuote.description]
            })]
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            ...styles$d.card,
            padding: isMobile ? "18px" : styles$d.card.padding
          },
          children: [/* @__PURE__ */ jsx("h2", {
            style: styles$d.sectionTitle,
            children: "Source Breakdown"
          }), /* @__PURE__ */ jsx("div", {
            style: {
              display: "grid",
              gap: "12px"
            },
            children: (_g = actionData.sourceBreakdown) == null ? void 0 : _g.map((source, index) => /* @__PURE__ */ jsxs("div", {
              style: {
                border: "1px solid #1f2937",
                borderRadius: "12px",
                padding: "14px",
                background: "rgba(2, 6, 23, 0.72)"
              },
              children: [/* @__PURE__ */ jsx("div", {
                style: {
                  fontWeight: 700,
                  color: "#f8fafc"
                },
                children: source.vendor
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  color: "#93c5fd",
                  marginTop: "4px"
                },
                children: ["Total Qty: ", source.quantity]
              }), /* @__PURE__ */ jsx("div", {
                style: {
                  color: "#9ca3af",
                  marginTop: "8px",
                  fontSize: "14px"
                },
                children: source.items.join(", ")
              })]
            }, `${source.vendor}-${index}`))
          })]
        })]
      }) : null, recentQuotes.length ? /* @__PURE__ */ jsxs("div", {
        style: {
          ...styles$d.card,
          marginTop: 24,
          padding: isMobile ? "18px" : styles$d.card.padding
        },
        children: [/* @__PURE__ */ jsx("h2", {
          style: styles$d.sectionTitle,
          children: "Recent Quotes"
        }), /* @__PURE__ */ jsx("div", {
          style: {
            display: "grid",
            gap: 12
          },
          children: recentQuotes.map((quote) => /* @__PURE__ */ jsxs("button", {
            type: "button",
            onClick: () => setSelectedHistoryQuoteId(quote.id),
            style: {
              textAlign: "left",
              width: "100%",
              border: "1px solid #1f2937",
              borderRadius: 12,
              padding: isMobile ? 16 : 14,
              background: "rgba(2, 6, 23, 0.72)",
              color: "#f8fafc",
              cursor: "pointer",
              overflowWrap: "anywhere",
              minHeight: isMobile ? 88 : void 0
            },
            children: [/* @__PURE__ */ jsx("div", {
              style: {
                fontWeight: 700
              },
              children: quote.customer_name || "Unnamed customer"
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                color: "#93c5fd",
                marginTop: 4
              },
              children: ["$", (quote.quote_total_cents / 100).toFixed(2), " —", " ", quote.service_name || "Quote"]
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                color: "#9ca3af",
                marginTop: 6,
                fontSize: 14
              },
              children: [quote.address1, ", ", quote.city, ", ", quote.province, " ", quote.postal_code]
            }), /* @__PURE__ */ jsx("div", {
              style: {
                color: "#64748b",
                marginTop: 6,
                fontSize: 12
              },
              children: new Date(quote.created_at).toLocaleString()
            })]
          }, quote.id))
        })]
      }) : null, selectedHistoryQuote ? /* @__PURE__ */ jsxs("div", {
        style: {
          ...styles$d.card,
          marginTop: 24,
          padding: isMobile ? "18px" : styles$d.card.padding
        },
        children: [/* @__PURE__ */ jsxs("div", {
          style: {
            display: "flex",
            justifyContent: "space-between",
            alignItems: isMobile ? "flex-start" : "center",
            flexWrap: "wrap",
            gap: "12px",
            marginBottom: "16px"
          },
          children: [/* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsx("h2", {
              style: {
                ...styles$d.sectionTitle,
                margin: 0
              },
              children: "Saved Quote Detail"
            }), /* @__PURE__ */ jsx("div", {
              style: {
                color: "#64748b",
                fontSize: 13,
                marginTop: 6
              },
              children: new Date(selectedHistoryQuote.created_at).toLocaleString()
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                color: "#93c5fd",
                fontSize: 13,
                marginTop: 6
              },
              children: ["Created by", " ", selectedHistoryQuote.created_by_name || selectedHistoryQuote.created_by_email || "Unknown user"]
            })]
          }), /* @__PURE__ */ jsxs("div", {
            style: {
              display: "flex",
              gap: 12,
              flexWrap: "wrap"
            },
            children: [/* @__PURE__ */ jsx("button", {
              type: "button",
              onClick: copyHistoryQuote,
              style: mobileActionButtonStyle,
              children: "Copy Saved Quote"
            }), /* @__PURE__ */ jsxs(deleteQuoteFetcher.Form, {
              method: "post",
              action: deleteQuoteAction,
              onSubmit: (event) => {
                if (!window.confirm("Delete this quote? This can't be undone.")) {
                  event.preventDefault();
                }
              },
              children: [/* @__PURE__ */ jsx("input", {
                type: "hidden",
                name: "quoteId",
                value: selectedHistoryQuote.id
              }), /* @__PURE__ */ jsx("button", {
                type: "submit",
                style: mobileActionButtonStyle,
                children: deleteQuoteFetcher.state === "submitting" ? "Deleting..." : "Delete Quote"
              })]
            })]
          })]
        }), canAccess("sendToShopify") ? /* @__PURE__ */ jsxs(draftOrderFetcher.Form, {
          method: "post",
          action: createDraftOrderAction,
          style: {
            marginBottom: 16,
            display: "flex",
            gap: 12,
            flexWrap: "wrap"
          },
          children: [/* @__PURE__ */ jsx("input", {
            type: "hidden",
            name: "quoteId",
            value: selectedHistoryQuote.id
          }), /* @__PURE__ */ jsx("button", {
            type: "submit",
            style: styles$d.buttonPrimary,
            children: draftOrderFetcher.state === "submitting" ? "Creating Draft Order..." : "Send To Shopify"
          }), ((_h = draftOrderFetcher.data) == null ? void 0 : _h.draftOrderAdminUrl) ? /* @__PURE__ */ jsx("a", {
            href: draftOrderFetcher.data.draftOrderAdminUrl,
            target: "_blank",
            rel: "noreferrer",
            style: mobileActionButtonStyle,
            children: "Open Draft Order"
          }) : null, ((_i = draftOrderFetcher.data) == null ? void 0 : _i.draftOrderInvoiceUrl) ? /* @__PURE__ */ jsx("a", {
            href: draftOrderFetcher.data.draftOrderInvoiceUrl,
            target: "_blank",
            rel: "noreferrer",
            style: mobileActionButtonStyle,
            children: "Open Invoice"
          }) : null]
        }) : null, ((_j = draftOrderFetcher.data) == null ? void 0 : _j.message) ? /* @__PURE__ */ jsx("div", {
          style: {
            ...draftOrderFetcher.data.ok ? styles$d.statusOk : styles$d.statusErr,
            fontSize: isMobile ? 16 : void 0,
            fontWeight: isMobile ? 700 : void 0
          },
          children: draftOrderFetcher.data.message
        }) : null, ((_k = deleteQuoteFetcher.data) == null ? void 0 : _k.message) ? /* @__PURE__ */ jsx("div", {
          style: {
            ...deleteQuoteFetcher.data.ok ? styles$d.statusOk : styles$d.statusErr,
            fontSize: isMobile ? 16 : void 0,
            fontWeight: isMobile ? 700 : void 0
          },
          children: deleteQuoteFetcher.data.message
        }) : null, /* @__PURE__ */ jsxs("div", {
          style: {
            display: "grid",
            gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "1.2fr 1fr",
            gap: "20px"
          },
          children: [/* @__PURE__ */ jsxs("div", {
            style: {
              display: "grid",
              gap: "10px",
              color: "#e5e7eb"
            },
            children: [/* @__PURE__ */ jsx("button", {
              type: "button",
              onClick: () => toggleHistorySection("customer"),
              style: mobileActionButtonStyle,
              children: historyDetailsOpen.customer ? "Hide Quote Info" : "Show Quote Info"
            }), historyDetailsOpen.customer ? /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gap: "10px"
              },
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Customer:"
                }), " ", selectedHistoryQuote.customer_name || "Unnamed customer"]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Email:"
                }), " ", selectedHistoryQuote.customer_email || "N/A"]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Phone:"
                }), " ", selectedHistoryQuote.customer_phone || "N/A"]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Address:"
                }), " ", selectedHistoryQuote.address1, ", ", selectedHistoryQuote.city, ",", " ", selectedHistoryQuote.province, " ", selectedHistoryQuote.postal_code]
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  fontSize: isMobile ? 22 : void 0,
                  fontWeight: isMobile ? 800 : void 0
                },
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Total:"
                }), " $", (Number(selectedHistoryQuote.quote_total_cents || 0) / 100).toFixed(2)]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Service:"
                }), " ", selectedHistoryQuote.service_name || "Quote"]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "ETA:"
                }), " ", selectedHistoryQuote.eta || "N/A"]
              }), selectedHistoryQuote.shipping_details ? /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Shipping Details:"
                }), " ", selectedHistoryQuote.shipping_details]
              }) : null, /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Summary:"
                }), " ", selectedHistoryQuote.summary || "N/A"]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: {
                    color: "#93c5fd"
                  },
                  children: "Notes:"
                }), " ", selectedHistoryQuote.description || "N/A"]
              })]
            }) : null, /* @__PURE__ */ jsxs("div", {
              style: {
                marginTop: 10
              },
              children: [/* @__PURE__ */ jsx("button", {
                type: "button",
                onClick: () => toggleHistorySection("lineItems"),
                style: mobileActionButtonStyle,
                children: historyDetailsOpen.lineItems ? "Hide Line Items" : "Show Line Items"
              }), historyDetailsOpen.lineItems ? /* @__PURE__ */ jsx("div", {
                style: {
                  display: "grid",
                  gap: 10,
                  marginTop: 10
                },
                children: (selectedHistoryQuote.line_items || []).map((line, index) => /* @__PURE__ */ jsxs("div", {
                  style: {
                    border: "1px solid #1f2937",
                    borderRadius: 12,
                    padding: 12,
                    background: "rgba(2, 6, 23, 0.72)",
                    overflowWrap: "anywhere"
                  },
                  children: [/* @__PURE__ */ jsx("div", {
                    style: {
                      fontWeight: 700
                    },
                    children: line.title
                  }), /* @__PURE__ */ jsxs("div", {
                    style: {
                      color: "#93c5fd",
                      marginTop: 4
                    },
                    children: [line.sku, " · Qty ", line.quantity]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: {
                      color: "#9ca3af",
                      marginTop: 4,
                      fontSize: 14
                    },
                    children: ["Unit $", Number(line.price || 0).toFixed(2), " · Total $", (Number(line.price || 0) * Number(line.quantity || 0)).toFixed(2)]
                  })]
                }, `${line.sku}-${index}`))
              }) : null]
            })]
          }), /* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsx("button", {
              type: "button",
              onClick: () => toggleHistorySection("sourceBreakdown"),
              style: mobileActionButtonStyle,
              children: historyDetailsOpen.sourceBreakdown ? "Hide Source Breakdown" : "Show Source Breakdown"
            }), historyDetailsOpen.sourceBreakdown ? /* @__PURE__ */ jsx("div", {
              style: {
                display: "grid",
                gap: 12,
                marginTop: 10
              },
              children: (selectedHistoryQuote.source_breakdown || []).map((source, index) => /* @__PURE__ */ jsxs("div", {
                style: {
                  border: "1px solid #1f2937",
                  borderRadius: "12px",
                  padding: "14px",
                  background: "rgba(2, 6, 23, 0.72)",
                  overflowWrap: "anywhere"
                },
                children: [/* @__PURE__ */ jsx("div", {
                  style: {
                    fontWeight: 700,
                    color: "#f8fafc"
                  },
                  children: source.vendor
                }), /* @__PURE__ */ jsxs("div", {
                  style: {
                    color: "#93c5fd",
                    marginTop: "4px"
                  },
                  children: ["Total Qty: ", source.quantity]
                }), /* @__PURE__ */ jsx("div", {
                  style: {
                    color: "#9ca3af",
                    marginTop: "8px",
                    fontSize: "14px"
                  },
                  children: source.items.join(", ")
                })]
              }, `${source.vendor}-${index}`))
            }) : null]
          })]
        })]
      }) : null]
    }), isMobile ? /* @__PURE__ */ jsxs("div", {
      style: mobileBottomNavStyle,
      children: [canAccess("quoteTool") ? /* @__PURE__ */ jsxs("a", {
        href: mobileDashboardHref,
        style: mobileTabLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: mobileTabIconStyle(false),
          children: "D"
        }), /* @__PURE__ */ jsx("span", {
          children: "Dashboard"
        })]
      }) : null, /* @__PURE__ */ jsxs("a", {
        href: isEmbeddedRoute ? "/app/custom-quote" : "/custom-quote",
        style: mobileTabLinkStyle(true),
        children: [/* @__PURE__ */ jsx("span", {
          style: mobileTabIconStyle(true),
          children: "Q"
        }), /* @__PURE__ */ jsx("span", {
          children: "Quote Tool"
        })]
      }), canAccess("reviewQuotes") ? /* @__PURE__ */ jsxs("a", {
        href: quoteReviewHref,
        style: mobileTabLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: mobileTabIconStyle(false),
          children: "R"
        }), /* @__PURE__ */ jsx("span", {
          children: "Review"
        })]
      }) : null, canAccess("dispatch") ? /* @__PURE__ */ jsxs("a", {
        href: dispatchHref,
        style: mobileTabLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: mobileTabIconStyle(false),
          children: "X"
        }), /* @__PURE__ */ jsx("span", {
          children: "Dispatch"
        })]
      }) : null]
    }) : null]
  });
});
const route2 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$r,
  default: customQuote,
  loader: loader$q
}, Symbol.toStringTag, { value: "Module" }));
function formatMoney$1(cents) {
  return `$${(Number(cents || 0) / 100).toFixed(2)}`;
}
function buildQuoteSearchText(quote) {
  const lineText = (quote.line_items || []).map((line) => [line.title, line.sku, line.vendor, line.pricingLabel, line.audience, line.contractorTier].filter(Boolean).join(" ")).join(" ");
  const sourceText = Array.isArray(quote.source_breakdown) ? quote.source_breakdown.map((entry2) => [entry2 == null ? void 0 : entry2.vendor, ...Array.isArray(entry2 == null ? void 0 : entry2.items) ? entry2.items : []].filter(Boolean).join(" ")).join(" ") : "";
  return [quote.id, quote.customer_name, quote.customer_email, quote.customer_phone, quote.address1, quote.address2, quote.city, quote.province, quote.postal_code, quote.country, quote.service_name, quote.shipping_details, quote.description, quote.summary, quote.eta, quote.created_by_name, quote.created_by_email, lineText, sourceText].filter(Boolean).join(" ").toLowerCase();
}
const styles$c = {
  page: {
    minHeight: "100vh",
    background: "radial-gradient(circle at top left, rgba(30, 64, 175, 0.24), transparent 35%), linear-gradient(180deg, #020617 0%, #0f172a 55%, #111827 100%)",
    color: "#f8fafc",
    padding: "32px 20px 56px",
    fontFamily: '"Avenir Next", "Segoe UI", ui-sans-serif, system-ui, -apple-system, sans-serif'
  },
  shell: {
    maxWidth: "1200px",
    margin: "0 auto",
    display: "grid",
    gap: "20px"
  },
  card: {
    background: "rgba(15, 23, 42, 0.88)",
    border: "1px solid rgba(148, 163, 184, 0.18)",
    borderRadius: "24px",
    padding: "24px",
    boxShadow: "0 22px 48px rgba(2, 6, 23, 0.34)",
    backdropFilter: "blur(10px)"
  },
  title: {
    margin: 0,
    fontSize: "clamp(2rem, 4vw, 3rem)",
    fontWeight: 800,
    letterSpacing: "-0.04em"
  },
  subtitle: {
    margin: "10px 0 0",
    color: "#94a3b8",
    lineHeight: 1.6
  },
  label: {
    display: "block",
    marginBottom: "8px",
    fontSize: "13px",
    fontWeight: 700,
    letterSpacing: "0.02em",
    color: "#cbd5e1"
  },
  input: {
    width: "100%",
    borderRadius: "14px",
    border: "1px solid #334155",
    background: "rgba(15, 23, 42, 0.92)",
    color: "#f8fafc",
    padding: "14px 16px",
    fontSize: "15px",
    outline: "none",
    boxSizing: "border-box"
  },
  buttonPrimary: {
    border: "none",
    borderRadius: "14px",
    padding: "12px 18px",
    background: "linear-gradient(135deg, #2563eb, #14b8a6)",
    color: "#eff6ff",
    fontWeight: 800,
    cursor: "pointer"
  },
  buttonGhost: {
    border: "1px solid rgba(148, 163, 184, 0.28)",
    borderRadius: "14px",
    padding: "12px 18px",
    background: "rgba(15, 23, 42, 0.62)",
    color: "#e2e8f0",
    fontWeight: 700,
    cursor: "pointer",
    textDecoration: "none",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center"
  },
  statusOk: {
    marginTop: "16px",
    padding: "14px 16px",
    borderRadius: "14px",
    background: "rgba(22, 163, 74, 0.15)",
    border: "1px solid rgba(34, 197, 94, 0.5)",
    color: "#dcfce7"
  },
  statusErr: {
    marginTop: "16px",
    padding: "14px 16px",
    borderRadius: "14px",
    background: "rgba(220, 38, 38, 0.15)",
    border: "1px solid rgba(248, 113, 113, 0.5)",
    color: "#fee2e2"
  }
};
async function loader$p({
  request
}) {
  const url = new URL(request.url);
  const isEmbeddedRoute = url.pathname.startsWith("/app/");
  const reviewPath = isEmbeddedRoute ? "/app/quote-review" : "/quote-review";
  if (url.searchParams.get("logout") === "1") {
    return redirect(reviewPath, {
      headers: [["Set-Cookie", await userAuthCookie.serialize("", {
        maxAge: 0
      })], ["Set-Cookie", await adminQuoteCookie.serialize("", {
        maxAge: 0
      })]]
    });
  }
  const allowed = await hasAdminQuotePermissionAccess(request, "reviewQuotes");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  const [quotes, currentUser] = allowed ? await Promise.all([getRecentCustomQuotes(250), getCurrentUser(request)]) : [[], null];
  return data({
    allowed,
    currentUser,
    quotes
  });
}
async function action$q({
  request
}) {
  const url = new URL(request.url);
  const allowed = await hasAdminQuotePermissionAccess(request, "reviewQuotes");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  return data({
    allowed: true,
    quotes: await getRecentCustomQuotes(250)
  });
}
const quoteReview = UNSAFE_withComponentProps(function QuoteReviewPage() {
  var _a2, _b, _c, _d, _e, _f;
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const location = useLocation();
  const draftOrderFetcher = useFetcher();
  const deleteQuoteFetcher = useFetcher();
  const updateQuoteFetcher = useFetcher();
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const urlParams = new URLSearchParams(location.search);
  const requestedQuoteId = urlParams.get("quote");
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const currentUser = (actionData == null ? void 0 : actionData.currentUser) ?? loaderData.currentUser ?? null;
  const rawQuotes = (actionData == null ? void 0 : actionData.quotes) || loaderData.quotes || [];
  const [editedQuotesById, setEditedQuotesById] = useState({});
  const quotes = rawQuotes.map((quote) => editedQuotesById[quote.id] || quote);
  const [query, setQuery] = useState("");
  const deferredQuery = useDeferredValue(query);
  const [selectedQuoteId, setSelectedQuoteId] = useState(requestedQuoteId || ((_a2 = quotes[0]) == null ? void 0 : _a2.id) || null);
  const [isMobile, setIsMobile] = useState(false);
  const [detailSectionsOpen, setDetailSectionsOpen] = useState({
    customer: true,
    lineItems: false
  });
  const createDraftOrderAction = isEmbeddedRoute ? `/app/api/create-draft-order${location.search || ""}` : `/api/create-draft-order${location.search || ""}`;
  const deleteQuoteAction = isEmbeddedRoute ? `/app/api/delete-quote${location.search || ""}` : `/api/delete-quote${location.search || ""}`;
  const updateQuoteAction = isEmbeddedRoute ? `/app/api/update-quote${location.search || ""}` : `/api/update-quote${location.search || ""}`;
  const quoteToolHref = isEmbeddedRoute ? "/app/custom-quote" : "/custom-quote";
  const dispatchHref = isEmbeddedRoute ? "/app/classic" : "/classic";
  const mobileDashboardHref = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  const logoutHref = currentUser ? "/login?logout=1" : "?logout=1";
  const loginHref = `/login?next=${encodeURIComponent(location.pathname + location.search)}`;
  const canAccess = (permission) => {
    var _a3;
    return !currentUser || ((_a3 = currentUser.permissions) == null ? void 0 : _a3.includes(permission));
  };
  const [editingQuoteId, setEditingQuoteId] = useState(null);
  const indexedQuotes = useMemo(() => quotes.map((quote) => ({
    quote,
    haystack: buildQuoteSearchText(quote)
  })), [quotes]);
  const filteredQuotes = useMemo(() => {
    const trimmed = deferredQuery.trim().toLowerCase();
    if (!trimmed) return indexedQuotes.map((entry2) => entry2.quote);
    return indexedQuotes.filter((entry2) => entry2.haystack.includes(trimmed)).map((entry2) => entry2.quote);
  }, [deferredQuery, indexedQuotes]);
  const selectedQuote = filteredQuotes.find((quote) => quote.id === selectedQuoteId) || filteredQuotes[0] || null;
  const mobileActionButtonStyle = {
    ...styles$c.buttonGhost,
    minHeight: isMobile ? 48 : void 0,
    width: isMobile ? "100%" : void 0
  };
  const mobileTabLinkStyle = (active) => ({
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    gap: 4,
    minHeight: 56,
    borderRadius: 14,
    textDecoration: "none",
    color: active ? "#38bdf8" : "#94a3b8",
    background: active ? "rgba(14, 165, 233, 0.12)" : "transparent",
    fontSize: 11,
    fontWeight: 700,
    letterSpacing: "0.01em"
  });
  const mobileTabIconStyle = (active) => ({
    width: 24,
    height: 24,
    borderRadius: 999,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    background: active ? "rgba(14, 165, 233, 0.18)" : "rgba(51, 65, 85, 0.35)",
    color: active ? "#38bdf8" : "#cbd5e1",
    fontSize: 12,
    lineHeight: 1
  });
  const mobileBottomNavStyle = {
    position: "fixed",
    left: 12,
    right: 12,
    bottom: "calc(env(safe-area-inset-bottom, 0px) + 12px)",
    zIndex: 30,
    display: "grid",
    gridTemplateColumns: "repeat(4, 1fr)",
    gap: 10,
    padding: "8px 10px",
    borderRadius: 20,
    background: "rgba(15, 23, 42, 0.96)",
    border: "1px solid rgba(30, 41, 59, 0.95)",
    boxShadow: "0 18px 38px rgba(2, 6, 23, 0.45)",
    backdropFilter: "blur(14px)"
  };
  function toggleDetailSection(key) {
    setDetailSectionsOpen((current) => ({
      ...current,
      [key]: !current[key]
    }));
  }
  useEffect(() => {
    var _a3, _b2;
    if (((_a3 = deleteQuoteFetcher.data) == null ? void 0 : _a3.ok) && ((_b2 = deleteQuoteFetcher.data) == null ? void 0 : _b2.deletedQuoteId)) {
      setSelectedQuoteId((current) => current === deleteQuoteFetcher.data.deletedQuoteId ? null : current);
    }
  }, [deleteQuoteFetcher.data]);
  useEffect(() => {
    var _a3, _b2;
    if (((_a3 = updateQuoteFetcher.data) == null ? void 0 : _a3.ok) && ((_b2 = updateQuoteFetcher.data) == null ? void 0 : _b2.quote)) {
      const quote = updateQuoteFetcher.data.quote;
      setEditedQuotesById((current) => ({
        ...current,
        [quote.id]: quote
      }));
      setSelectedQuoteId(quote.id);
      setEditingQuoteId(null);
    }
  }, [updateQuoteFetcher.data]);
  useEffect(() => {
    if (typeof window === "undefined") return;
    const media = window.matchMedia("(max-width: 900px)");
    const updateViewport = () => setIsMobile(media.matches);
    updateViewport();
    media.addEventListener("change", updateViewport);
    return () => media.removeEventListener("change", updateViewport);
  }, []);
  useEffect(() => {
    if (requestedQuoteId) {
      setSelectedQuoteId(requestedQuoteId);
    }
  }, [requestedQuoteId]);
  if (!allowed) {
    return /* @__PURE__ */ jsx("div", {
      style: {
        ...styles$c.page,
        padding: isMobile ? "20px 14px 40px" : styles$c.page.padding
      },
      children: /* @__PURE__ */ jsx("div", {
        style: {
          ...styles$c.shell,
          maxWidth: 520
        },
        children: /* @__PURE__ */ jsxs("div", {
          style: styles$c.card,
          children: [/* @__PURE__ */ jsx("h1", {
            style: styles$c.title,
            children: "Quote Review"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$c.subtitle,
            children: "Sign in with your contractor user account to search saved quotes and send them to Shopify."
          }), /* @__PURE__ */ jsx("a", {
            href: loginHref,
            style: {
              ...styles$c.buttonPrimary,
              marginTop: 16,
              display: "inline-flex",
              alignItems: "center",
              justifyContent: "center",
              textDecoration: "none"
            },
            children: "Sign In"
          })]
        })
      })
    });
  }
  return /* @__PURE__ */ jsxs("div", {
    style: {
      ...styles$c.page,
      padding: isMobile ? "20px 14px 120px" : styles$c.page.padding,
      overflowX: "clip"
    },
    children: [/* @__PURE__ */ jsxs("div", {
      style: styles$c.shell,
      children: [isMobile ? /* @__PURE__ */ jsxs("div", {
        style: {
          marginBottom: 18
        },
        children: [/* @__PURE__ */ jsx("h1", {
          style: {
            ...styles$c.title,
            fontSize: "2.2rem"
          },
          children: "Quote Review"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$c.subtitle,
          children: "Search across customer info, address, notes, SKU, product titles, vendors, and saved quote details."
        })]
      }) : /* @__PURE__ */ jsx("div", {
        style: styles$c.card,
        children: /* @__PURE__ */ jsxs("div", {
          style: {
            display: "flex",
            justifyContent: "space-between",
            gap: 16,
            flexWrap: "wrap",
            alignItems: "center"
          },
          children: [/* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsx("h1", {
              style: styles$c.title,
              children: "Quote Review"
            }), /* @__PURE__ */ jsx("p", {
              style: styles$c.subtitle,
              children: "Search across customer info, address, notes, SKU, product titles, vendors, and saved quote details."
            })]
          }), /* @__PURE__ */ jsxs("div", {
            style: {
              display: "flex",
              gap: 12,
              flexWrap: "wrap"
            },
            children: [canAccess("quoteTool") ? /* @__PURE__ */ jsx("a", {
              href: mobileDashboardHref,
              style: styles$c.buttonGhost,
              children: "Dashboard"
            }) : null, canAccess("quoteTool") ? /* @__PURE__ */ jsx("a", {
              href: quoteToolHref,
              style: styles$c.buttonGhost,
              children: "Open Quote Tool"
            }) : null, canAccess("dispatch") ? /* @__PURE__ */ jsx("a", {
              href: dispatchHref,
              style: styles$c.buttonGhost,
              children: "Dispatch"
            }) : null, canAccess("manageUsers") ? /* @__PURE__ */ jsx("a", {
              href: "/settings",
              style: styles$c.buttonGhost,
              children: "Settings"
            }) : null, currentUser ? /* @__PURE__ */ jsx("a", {
              href: "/change-password",
              style: styles$c.buttonGhost,
              children: "Change Password"
            }) : null, /* @__PURE__ */ jsx("a", {
              href: logoutHref,
              style: styles$c.buttonGhost,
              children: "Log Out"
            })]
          })]
        })
      }), /* @__PURE__ */ jsxs("div", {
        style: {
          ...styles$c.card,
          display: "grid",
          gap: 14,
          padding: isMobile ? "18px" : styles$c.card.padding
        },
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("label", {
            style: styles$c.label,
            children: "Search Saved Quotes"
          }), /* @__PURE__ */ jsx("input", {
            type: "search",
            value: query,
            onChange: (event) => setQuery(event.target.value),
            placeholder: "Search by customer, email, city, ZIP, summary, SKU, vendor, quote ID...",
            style: styles$c.input
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            color: "#94a3b8",
            fontSize: 14
          },
          children: ["Showing ", filteredQuotes.length, " of ", quotes.length, " saved quotes"]
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: {
          display: "grid",
          gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "minmax(320px, 420px) minmax(0, 1fr)",
          gap: 20,
          alignItems: "start"
        },
        children: [/* @__PURE__ */ jsx("div", {
          style: {
            ...styles$c.card,
            maxHeight: isMobile ? "none" : "70vh",
            overflowY: isMobile ? "visible" : "auto",
            padding: isMobile ? "18px" : styles$c.card.padding
          },
          children: /* @__PURE__ */ jsx("div", {
            style: {
              display: "grid",
              gap: 12
            },
            children: filteredQuotes.length === 0 ? /* @__PURE__ */ jsx("div", {
              style: {
                color: "#94a3b8"
              },
              children: "No saved quotes matched your search."
            }) : filteredQuotes.map((quote) => /* @__PURE__ */ jsxs("button", {
              type: "button",
              onClick: () => setSelectedQuoteId(quote.id),
              style: {
                textAlign: "left",
                padding: isMobile ? 16 : 14,
                borderRadius: 16,
                border: (selectedQuote == null ? void 0 : selectedQuote.id) === quote.id ? "1px solid rgba(45, 212, 191, 0.6)" : "1px solid rgba(51, 65, 85, 0.9)",
                background: (selectedQuote == null ? void 0 : selectedQuote.id) === quote.id ? "rgba(20, 184, 166, 0.14)" : "rgba(2, 6, 23, 0.7)",
                color: "#f8fafc",
                cursor: "pointer",
                overflowWrap: "anywhere",
                minHeight: isMobile ? 92 : void 0
              },
              children: [/* @__PURE__ */ jsx("div", {
                style: {
                  fontWeight: 800
                },
                children: quote.customer_name || quote.customer_email || "Unnamed quote"
              }), /* @__PURE__ */ jsx("div", {
                style: {
                  marginTop: 6,
                  color: "#bfdbfe",
                  fontSize: 13
                },
                children: quote.customer_email || "No email"
              }), /* @__PURE__ */ jsx("div", {
                style: {
                  marginTop: 4,
                  color: "#cbd5e1",
                  fontSize: 13
                },
                children: quote.customer_phone || "No phone"
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  marginTop: 6,
                  color: "#94a3b8",
                  fontSize: 13
                },
                children: [quote.address1, ", ", quote.city, ", ", quote.province, " ", quote.postal_code]
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  marginTop: 10,
                  display: "flex",
                  justifyContent: "space-between",
                  gap: 12,
                  fontSize: 13,
                  color: "#cbd5e1"
                },
                children: [/* @__PURE__ */ jsx("span", {
                  children: formatMoney$1(quote.quote_total_cents)
                }), /* @__PURE__ */ jsx("span", {
                  children: new Date(quote.created_at).toLocaleString()
                })]
              })]
            }, quote.id))
          })
        }), /* @__PURE__ */ jsx("div", {
          style: {
            ...styles$c.card,
            padding: isMobile ? "18px" : styles$c.card.padding
          },
          children: selectedQuote ? /* @__PURE__ */ jsxs(Fragment, {
            children: [/* @__PURE__ */ jsxs("div", {
              style: {
                display: "flex",
                justifyContent: "space-between",
                gap: 16,
                flexWrap: "wrap",
                alignItems: isMobile ? "flex-start" : "center"
              },
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: {
                    margin: 0,
                    fontSize: 24
                  },
                  children: "Saved Quote Detail"
                }), /* @__PURE__ */ jsxs("div", {
                  style: {
                    color: "#94a3b8",
                    marginTop: 6,
                    fontSize: 14
                  },
                  children: ["Quote ID: ", selectedQuote.id]
                }), /* @__PURE__ */ jsx("div", {
                  style: {
                    color: "#94a3b8",
                    marginTop: 4,
                    fontSize: 14
                  },
                  children: new Date(selectedQuote.created_at).toLocaleString()
                }), /* @__PURE__ */ jsxs("div", {
                  style: {
                    color: "#93c5fd",
                    marginTop: 4,
                    fontSize: 14
                  },
                  children: ["Created by", " ", selectedQuote.created_by_name || selectedQuote.created_by_email || "Unknown user"]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "flex",
                  gap: 12,
                  flexWrap: "wrap",
                  width: isMobile ? "100%" : void 0
                },
                children: [canAccess("sendToShopify") ? /* @__PURE__ */ jsxs(draftOrderFetcher.Form, {
                  method: "post",
                  action: createDraftOrderAction,
                  style: {
                    display: "flex",
                    gap: 12,
                    flexWrap: "wrap",
                    width: isMobile ? "100%" : void 0
                  },
                  children: [/* @__PURE__ */ jsx("input", {
                    type: "hidden",
                    name: "quoteId",
                    value: selectedQuote.id
                  }), /* @__PURE__ */ jsx("button", {
                    type: "submit",
                    style: {
                      ...styles$c.buttonPrimary,
                      width: isMobile ? "100%" : void 0
                    },
                    children: draftOrderFetcher.state === "submitting" ? "Creating Draft Order..." : "Send To Shopify"
                  }), ((_b = draftOrderFetcher.data) == null ? void 0 : _b.draftOrderAdminUrl) ? /* @__PURE__ */ jsx("a", {
                    href: draftOrderFetcher.data.draftOrderAdminUrl,
                    target: "_blank",
                    rel: "noreferrer",
                    style: mobileActionButtonStyle,
                    children: "Open Draft Order"
                  }) : null, ((_c = draftOrderFetcher.data) == null ? void 0 : _c.draftOrderInvoiceUrl) ? /* @__PURE__ */ jsx("a", {
                    href: draftOrderFetcher.data.draftOrderInvoiceUrl,
                    target: "_blank",
                    rel: "noreferrer",
                    style: mobileActionButtonStyle,
                    children: "Open Invoice"
                  }) : null]
                }) : null, /* @__PURE__ */ jsx("button", {
                  type: "button",
                  onClick: () => setEditingQuoteId((current) => current === selectedQuote.id ? null : selectedQuote.id),
                  style: mobileActionButtonStyle,
                  children: editingQuoteId === selectedQuote.id ? "Cancel Regenerate" : "Edit / Regenerate"
                }), /* @__PURE__ */ jsxs(deleteQuoteFetcher.Form, {
                  method: "post",
                  action: deleteQuoteAction,
                  onSubmit: (event) => {
                    if (!window.confirm("Delete this quote? This can't be undone.")) {
                      event.preventDefault();
                    }
                  },
                  children: [/* @__PURE__ */ jsx("input", {
                    type: "hidden",
                    name: "quoteId",
                    value: selectedQuote.id
                  }), /* @__PURE__ */ jsx("button", {
                    type: "submit",
                    style: mobileActionButtonStyle,
                    children: deleteQuoteFetcher.state === "submitting" ? "Deleting..." : "Delete Quote"
                  })]
                })]
              })]
            }), ((_d = draftOrderFetcher.data) == null ? void 0 : _d.message) ? /* @__PURE__ */ jsx("div", {
              style: {
                ...draftOrderFetcher.data.ok ? styles$c.statusOk : styles$c.statusErr,
                fontSize: isMobile ? 16 : void 0,
                fontWeight: isMobile ? 700 : void 0
              },
              children: draftOrderFetcher.data.message
            }) : null, ((_e = deleteQuoteFetcher.data) == null ? void 0 : _e.message) ? /* @__PURE__ */ jsx("div", {
              style: {
                ...deleteQuoteFetcher.data.ok ? styles$c.statusOk : styles$c.statusErr,
                fontSize: isMobile ? 16 : void 0,
                fontWeight: isMobile ? 700 : void 0
              },
              children: deleteQuoteFetcher.data.message
            }) : null, ((_f = updateQuoteFetcher.data) == null ? void 0 : _f.message) ? /* @__PURE__ */ jsx("div", {
              style: {
                ...updateQuoteFetcher.data.ok ? styles$c.statusOk : styles$c.statusErr,
                fontSize: isMobile ? 16 : void 0,
                fontWeight: isMobile ? 700 : void 0
              },
              children: updateQuoteFetcher.data.message
            }) : null, editingQuoteId === selectedQuote.id ? /* @__PURE__ */ jsxs(updateQuoteFetcher.Form, {
              method: "post",
              action: updateQuoteAction,
              style: {
                marginTop: 20,
                display: "grid",
                gap: 16,
                padding: 16,
                borderRadius: 18,
                border: "1px solid rgba(45, 212, 191, 0.35)",
                background: "rgba(20, 184, 166, 0.08)"
              },
              children: [/* @__PURE__ */ jsx("input", {
                type: "hidden",
                name: "quoteId",
                value: selectedQuote.id
              }), /* @__PURE__ */ jsx("h3", {
                style: {
                  margin: 0
                },
                children: "Edit And Regenerate Quote"
              }), /* @__PURE__ */ jsx("div", {
                style: {
                  color: "#93c5fd",
                  fontSize: 13,
                  lineHeight: 1.5
                },
                children: "Updating quantities or address will recalculate delivery, truck-load logic, tax, and the saved total."
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "grid",
                  gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "repeat(3, minmax(0, 1fr))",
                  gap: 12
                },
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$c.label,
                    children: "Customer Name"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "customerName",
                    defaultValue: selectedQuote.customer_name || "",
                    style: styles$c.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$c.label,
                    children: "Email"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "customerEmail",
                    defaultValue: selectedQuote.customer_email || "",
                    style: styles$c.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$c.label,
                    children: "Phone"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "customerPhone",
                    defaultValue: selectedQuote.customer_phone || "",
                    style: styles$c.input
                  })]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "grid",
                  gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "1.4fr 0.9fr 0.5fr 0.5fr 0.5fr",
                  gap: 12
                },
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$c.label,
                    children: "Address 1"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "address1",
                    defaultValue: selectedQuote.address1 || "",
                    style: styles$c.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$c.label,
                    children: "City"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "city",
                    defaultValue: selectedQuote.city || "",
                    style: styles$c.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$c.label,
                    children: "State"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "province",
                    defaultValue: selectedQuote.province || "",
                    style: styles$c.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$c.label,
                    children: "ZIP"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "postalCode",
                    defaultValue: selectedQuote.postal_code || "",
                    style: styles$c.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$c.label,
                    children: "Country"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "country",
                    defaultValue: selectedQuote.country || "US",
                    style: styles$c.input
                  })]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("label", {
                  style: styles$c.label,
                  children: "Address 2"
                }), /* @__PURE__ */ jsx("input", {
                  name: "address2",
                  defaultValue: selectedQuote.address2 || "",
                  style: styles$c.input
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "grid",
                  gap: 10
                },
                children: [/* @__PURE__ */ jsx("h4", {
                  style: {
                    margin: 0
                  },
                  children: "Line Quantities"
                }), (selectedQuote.line_items || []).map((line, index) => /* @__PURE__ */ jsxs("div", {
                  style: {
                    display: "grid",
                    gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "minmax(0, 1fr) 130px",
                    gap: 12,
                    alignItems: "end",
                    padding: 12,
                    borderRadius: 14,
                    border: "1px solid rgba(51, 65, 85, 0.9)",
                    background: "rgba(2, 6, 23, 0.42)"
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    style: {
                      overflowWrap: "anywhere"
                    },
                    children: [/* @__PURE__ */ jsx("div", {
                      style: {
                        fontWeight: 800
                      },
                      children: line.title
                    }), /* @__PURE__ */ jsxs("div", {
                      style: {
                        color: "#94a3b8",
                        marginTop: 4,
                        fontSize: 13
                      },
                      children: [line.sku, " · Unit $", Number(line.price || 0).toFixed(2)]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$c.label,
                      children: "Quantity"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "number",
                      min: "0",
                      step: "0.01",
                      name: `lineQuantity::${index}`,
                      defaultValue: line.quantity,
                      style: styles$c.input
                    })]
                  })]
                }, `${line.sku}-${index}`))]
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "flex",
                  gap: 12,
                  flexWrap: "wrap"
                },
                children: [/* @__PURE__ */ jsx("button", {
                  type: "submit",
                  style: styles$c.buttonPrimary,
                  children: updateQuoteFetcher.state === "submitting" ? "Regenerating..." : "Regenerate Quote"
                }), /* @__PURE__ */ jsx("button", {
                  type: "button",
                  onClick: () => setEditingQuoteId(null),
                  style: styles$c.buttonGhost,
                  children: "Cancel"
                })]
              })]
            }) : null, /* @__PURE__ */ jsxs("div", {
              style: {
                marginTop: 20,
                display: "grid",
                gridTemplateColumns: isMobile ? "minmax(0, 1fr)" : "1.1fr 1fr",
                gap: 20
              },
              children: [/* @__PURE__ */ jsxs("div", {
                style: {
                  display: "grid",
                  gap: 10
                },
                children: [/* @__PURE__ */ jsx("button", {
                  type: "button",
                  onClick: () => toggleDetailSection("customer"),
                  style: mobileActionButtonStyle,
                  children: detailSectionsOpen.customer ? "Hide Quote Info" : "Show Quote Info"
                }), detailSectionsOpen.customer ? /* @__PURE__ */ jsxs("div", {
                  style: {
                    display: "grid",
                    gap: 10,
                    overflowWrap: "anywhere"
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Customer:"
                    }), " ", selectedQuote.customer_name || "Unnamed customer"]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Email:"
                    }), " ", selectedQuote.customer_email || "N/A"]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Phone:"
                    }), " ", selectedQuote.customer_phone || "N/A"]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Address:"
                    }), " ", selectedQuote.address1, ", ", selectedQuote.city, ",", " ", selectedQuote.province, " ", selectedQuote.postal_code]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Country:"
                    }), " ", selectedQuote.country || "US"]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Created By:"
                    }), " ", selectedQuote.created_by_name || selectedQuote.created_by_email || "Unknown user", " ", "on ", new Date(selectedQuote.created_at).toLocaleString()]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: {
                      fontSize: isMobile ? 22 : void 0,
                      fontWeight: isMobile ? 800 : void 0
                    },
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Total:"
                    }), " ", formatMoney$1(selectedQuote.quote_total_cents)]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Service:"
                    }), " ", selectedQuote.service_name || "Quote"]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "ETA:"
                    }), " ", selectedQuote.eta || "N/A"]
                  }), selectedQuote.shipping_details ? /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Shipping Details:"
                    }), " ", selectedQuote.shipping_details]
                  }) : null, /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Summary:"
                    }), " ", selectedQuote.summary || "N/A"]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Notes:"
                    }), " ", selectedQuote.description || "N/A"]
                  })]
                }) : null]
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "grid",
                  gap: 12
                },
                children: [/* @__PURE__ */ jsx("button", {
                  type: "button",
                  onClick: () => toggleDetailSection("lineItems"),
                  style: mobileActionButtonStyle,
                  children: detailSectionsOpen.lineItems ? "Hide Line Items" : "Show Line Items"
                }), detailSectionsOpen.lineItems ? /* @__PURE__ */ jsx(Fragment, {
                  children: (selectedQuote.line_items || []).length === 0 ? /* @__PURE__ */ jsx("div", {
                    style: {
                      color: "#94a3b8"
                    },
                    children: "No saved line items."
                  }) : (selectedQuote.line_items || []).map((line, index) => /* @__PURE__ */ jsxs("div", {
                    style: {
                      border: "1px solid #1f2937",
                      borderRadius: 14,
                      padding: 14,
                      background: "rgba(2, 6, 23, 0.72)",
                      overflowWrap: "anywhere"
                    },
                    children: [/* @__PURE__ */ jsx("div", {
                      style: {
                        fontWeight: 700
                      },
                      children: line.title
                    }), /* @__PURE__ */ jsxs("div", {
                      style: {
                        color: "#94a3b8",
                        marginTop: 4,
                        fontSize: 14
                      },
                      children: [line.sku, " ", line.vendor ? `- ${line.vendor}` : ""]
                    }), /* @__PURE__ */ jsxs("div", {
                      style: {
                        color: "#cbd5e1",
                        marginTop: 8,
                        fontSize: 14
                      },
                      children: ["Qty ", line.quantity, " at $", Number(line.price || 0).toFixed(2), line.pricingLabel ? ` - ${line.pricingLabel}` : ""]
                    })]
                  }, `${line.sku}-${index}`))
                }) : null]
              })]
            })]
          }) : /* @__PURE__ */ jsx("div", {
            style: {
              color: "#94a3b8"
            },
            children: "Select a saved quote to review it."
          })
        })]
      })]
    }), isMobile ? /* @__PURE__ */ jsxs("div", {
      style: mobileBottomNavStyle,
      children: [canAccess("quoteTool") ? /* @__PURE__ */ jsxs("a", {
        href: mobileDashboardHref,
        style: mobileTabLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: mobileTabIconStyle(false),
          children: "D"
        }), /* @__PURE__ */ jsx("span", {
          children: "Dashboard"
        })]
      }) : null, canAccess("quoteTool") ? /* @__PURE__ */ jsxs("a", {
        href: quoteToolHref,
        style: mobileTabLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: mobileTabIconStyle(false),
          children: "Q"
        }), /* @__PURE__ */ jsx("span", {
          children: "Quote Tool"
        })]
      }) : null, /* @__PURE__ */ jsxs("a", {
        href: isEmbeddedRoute ? "/app/quote-review" : "/quote-review",
        style: mobileTabLinkStyle(true),
        children: [/* @__PURE__ */ jsx("span", {
          style: mobileTabIconStyle(true),
          children: "R"
        }), /* @__PURE__ */ jsx("span", {
          children: "Review"
        })]
      }), canAccess("dispatch") ? /* @__PURE__ */ jsxs("a", {
        href: dispatchHref,
        style: mobileTabLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: mobileTabIconStyle(false),
          children: "X"
        }), /* @__PURE__ */ jsx("span", {
          children: "Dispatch"
        })]
      }) : null]
    }) : null]
  });
});
const route3 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$q,
  default: quoteReview,
  loader: loader$p
}, Symbol.toStringTag, { value: "Module" }));
function escapeHtml$1(value) {
  return String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}
function getOrderDisplayNumber$3(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function getCustomerEmail(order) {
  const match = order.contact.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/i);
  return (match == null ? void 0 : match[0]) || "";
}
function formatDeliveredAt(order) {
  if (!order.deliveredAt) return (/* @__PURE__ */ new Date()).toLocaleString();
  return new Date(order.deliveredAt).toLocaleString();
}
function getQuantityColumns(order) {
  const unit = order.unit.toLowerCase();
  return {
    tons: /tons?/.test(unit) ? order.quantity : "",
    yards: /yards?/.test(unit) ? order.quantity : "",
    bags: /bags?/.test(unit) ? order.quantity : "",
    gallons: /gallons?/.test(unit) ? order.quantity : ""
  };
}
function buildDeliveryConfirmationEmail({
  order,
  route: route57
}) {
  const quantity = getQuantityColumns(order);
  const orderNumber = getOrderDisplayNumber$3(order);
  const deliveredAt = formatDeliveredAt(order);
  const driverName = order.signatureName || (route57 == null ? void 0 : route57.driver) || order.proofName || "";
  const photoProof = order.photoUrls || "Not captured";
  const gpsProof = order.signatureData || "Not captured";
  const subject = `Green Hills Supply delivery confirmation ${orderNumber}`;
  const text = [
    `Green Hills Supply Delivery Confirmation`,
    ``,
    `Order: ${orderNumber}`,
    `Delivered: ${deliveredAt}`,
    ``,
    `Driver and Truck Information`,
    `Truck: ${(route57 == null ? void 0 : route57.truck) || ""}`,
    `Driver: ${driverName}`,
    `Route: ${(route57 == null ? void 0 : route57.code) || ""}`,
    ``,
    `Customer Information`,
    `Customer: ${order.customer}`,
    `Address: ${order.address}, ${order.city}`,
    `Contact: ${order.contact || ""}`,
    ``,
    `Material Type: ${order.unit}`,
    `Product Ordered: ${order.material}`,
    `Quantity: ${order.quantity} ${order.unit}`,
    ``,
    `Delivery Notes: ${order.proofNotes || order.notes || ""}`,
    `Photo Proof: ${photoProof}`,
    `GPS Proof: ${gpsProof}`
  ].join("\n");
  const html = `<!doctype html>
<html>
  <body style="margin:0;background:#000000;font-family:Arial,Helvetica,sans-serif;color:#ffffff;">
    <div style="max-width:760px;margin:0 auto;background:#000000;padding:28px 24px 36px;">
      <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="border-collapse:collapse;">
        <tr>
          <td style="width:130px;border:2px solid #ffffff;padding:10px;text-align:center;vertical-align:middle;">
            <div style="font-size:18px;font-weight:900;line-height:1;color:#9ad20f;">GREEN</div>
            <div style="font-size:28px;font-weight:900;line-height:1;color:#ffffff;">HILLS</div>
          </td>
          <td style="padding-left:16px;vertical-align:middle;">
            <div style="border:2px solid #ffffff;padding:12px 14px;font-size:20px;font-weight:900;color:#ffffff;">
              Delivery Confirmation ${escapeHtml$1(orderNumber)}
            </div>
          </td>
        </tr>
      </table>

      ${sectionTitle("Driver and Truck Information")}
      <table role="presentation" width="100%" cellspacing="12" cellpadding="0" style="border-collapse:separate;margin-top:10px;">
        <tr>
          ${fieldCell("Truck Number", (route57 == null ? void 0 : route57.truck) || "")}
          ${fieldCell("Driver Name", driverName)}
        </tr>
        <tr>
          ${fieldCell("Order Number", orderNumber)}
          ${fieldCell("Delivered Date / Time", deliveredAt)}
        </tr>
      </table>

      ${sectionTitle("Customer Information")}
      <table role="presentation" width="100%" cellspacing="12" cellpadding="0" style="border-collapse:separate;margin-top:10px;">
        <tr>
          ${fieldCell("Customer Name", order.customer)}
          ${fieldCell("Customer Email / Contact", order.contact || "")}
        </tr>
        <tr>
          ${fieldCell("Address", `${order.address}, ${order.city}`)}
          ${fieldCell("Delivery Notes", order.notes || "")}
        </tr>
      </table>

      <table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="border-collapse:collapse;margin-top:22px;">
        <tr>
          ${tableHeader("Material Type")}
          ${tableHeader("Product Ordered")}
          ${tableHeader("Total Tonnage Delivered")}
          ${tableHeader("Total Yardage Delivered")}
          ${tableHeader("Total Bags Delivered")}
          ${tableHeader("Total Gallons Delivered")}
        </tr>
        <tr>
          ${tableCell(order.unit)}
          ${tableCell(order.material)}
          ${tableCell(quantity.tons)}
          ${tableCell(quantity.yards)}
          ${tableCell(quantity.bags)}
          ${tableCell(quantity.gallons)}
        </tr>
      </table>

      ${sectionTitle("Delivery Proof")}
      <table role="presentation" width="100%" cellspacing="12" cellpadding="0" style="border-collapse:separate;margin-top:10px;">
        <tr>
          ${fieldCell("Driver Signature", driverName)}
          ${fieldCell("GPS Location Verification", gpsProof)}
        </tr>
        <tr>
          ${fieldCell("Picture of Delivered Order", photoProof)}
          ${fieldCell("Driver Notes Upon Delivery", order.proofNotes || "")}
        </tr>
      </table>

      <p style="margin:28px 0 0;color:#ffffff;font-size:14px;line-height:1.6;">
        Thank you for your order. If you have any questions about your delivery, please reply to this email or contact Green Hills Supply.
      </p>
    </div>
  </body>
</html>`;
  return { subject, html, text };
}
function sectionTitle(title) {
  return `<div style="margin-top:24px;background:#2368aa;color:#9ad20f;text-align:center;font-weight:900;padding:8px 10px;">${escapeHtml$1(title)}</div>`;
}
function fieldCell(label, value) {
  return `<td style="width:50%;border:2px solid #ffffff;padding:8px 10px;vertical-align:top;">
    <div style="color:#9ad20f;font-size:12px;font-weight:900;">${escapeHtml$1(label)}</div>
    <div style="color:#ffffff;font-size:14px;line-height:1.45;margin-top:4px;">${escapeHtml$1(value || " ")}</div>
  </td>`;
}
function tableHeader(label) {
  return `<th style="background:#2368aa;color:#9ad20f;border:1px solid #ffffff;padding:8px 6px;font-size:12px;line-height:1.2;text-align:center;">${escapeHtml$1(label)}</th>`;
}
function tableCell(value) {
  return `<td style="background:#ffffff;color:#000000;border:1px solid #777;padding:8px 6px;font-size:13px;text-align:center;">${escapeHtml$1(value || " ")}</td>`;
}
async function sendDeliveryConfirmationEmail({
  order,
  route: route57
}) {
  const to = getCustomerEmail(order);
  if (!to) return { sent: false, skipped: true, reason: "No customer email found." };
  const resendApiKey = process.env.RESEND_API_KEY;
  const from = process.env.DELIVERY_CONFIRMATION_FROM || "Green Hills Supply <dispatch@ghstickets.com>";
  const replyTo = process.env.DELIVERY_CONFIRMATION_REPLY_TO || process.env.DELIVERY_CONFIRMATION_FROM || "info@greenhillssupply.com";
  if (!resendApiKey || !from) {
    return {
      sent: false,
      skipped: true,
      reason: "Delivery email is not configured. Set RESEND_API_KEY and verify the sender domain."
    };
  }
  const email = buildDeliveryConfirmationEmail({ order, route: route57 });
  const response = await fetch("https://api.resend.com/emails", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${resendApiKey}`,
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      from,
      to,
      subject: email.subject,
      html: email.html,
      text: email.text,
      reply_to: replyTo
    })
  });
  if (!response.ok) {
    const body = await response.text();
    throw new Error(`Delivery confirmation email failed: ${body}`);
  }
  return { sent: true };
}
const TABLE$1 = "dispatch_push_subscriptions";
function getVapidPublicKey() {
  return process.env.VAPID_PUBLIC_KEY || "";
}
function isPushConfigured() {
  return Boolean(process.env.VAPID_PUBLIC_KEY && process.env.VAPID_PRIVATE_KEY);
}
function configureWebPush() {
  const publicKey = process.env.VAPID_PUBLIC_KEY || "";
  const privateKey = process.env.VAPID_PRIVATE_KEY || "";
  const subject = process.env.VAPID_SUBJECT || process.env.SHOPIFY_APP_URL || "mailto:info@greenhillssupply.com";
  if (!publicKey || !privateKey) return false;
  webpush.setVapidDetails(subject, publicKey, privateKey);
  return true;
}
async function savePushSubscription(input) {
  var _a2;
  if (!((_a2 = input.subscription) == null ? void 0 : _a2.endpoint)) {
    throw new Error("Push subscription endpoint is missing.");
  }
  const { error } = await supabaseAdmin.from(TABLE$1).upsert(
    {
      user_id: input.user.id,
      user_email: input.user.email,
      target_role: input.user.permissions.includes("loader") ? "loader" : input.user.role,
      endpoint: input.subscription.endpoint,
      subscription: input.subscription,
      user_agent: input.userAgent || "",
      last_seen_at: (/* @__PURE__ */ new Date()).toISOString(),
      updated_at: (/* @__PURE__ */ new Date()).toISOString()
    },
    { onConflict: "endpoint" }
  );
  if ((error == null ? void 0 : error.code) === "42P01") {
    throw new Error("Push subscription storage is not ready. Run dispatch_loader_notifications.sql in Supabase.");
  }
  if (error) throw new Error(error.message);
}
async function deletePushSubscription(endpoint, user) {
  if (!endpoint) return;
  const { error } = await supabaseAdmin.from(TABLE$1).delete().eq("endpoint", endpoint).eq("user_id", user.id);
  if ((error == null ? void 0 : error.code) === "42P01") return;
  if (error) throw new Error(error.message);
}
async function listPushSubscriptionsForTarget(input) {
  let query = supabaseAdmin.from(TABLE$1).select("*");
  if (input.targetUserId) {
    query = query.eq("user_id", input.targetUserId);
  } else {
    query = query.eq("target_role", input.targetRole || "loader");
  }
  const { data: data2, error } = await query.order("created_at", { ascending: false });
  if ((error == null ? void 0 : error.code) === "42P01") return [];
  if (error) throw new Error(error.message);
  return data2 || [];
}
async function sendPushToTarget(input) {
  if (!configureWebPush()) {
    return { sent: 0, skipped: true, reason: "Missing VAPID_PUBLIC_KEY or VAPID_PRIVATE_KEY." };
  }
  const subscriptions = await listPushSubscriptionsForTarget(input);
  let sent = 0;
  await Promise.all(
    subscriptions.map(async (row) => {
      try {
        await webpush.sendNotification(
          row.subscription,
          JSON.stringify({
            title: input.title,
            body: input.message,
            url: input.url || "/loader",
            tag: input.tag || "loader-load-next"
          })
        );
        sent += 1;
      } catch (error) {
        const statusCode = Number((error == null ? void 0 : error.statusCode) || 0);
        if (statusCode === 404 || statusCode === 410) {
          await supabaseAdmin.from(TABLE$1).delete().eq("id", row.id);
        } else {
          console.warn("[PUSH SEND ERROR]", (error == null ? void 0 : error.message) || error);
        }
      }
    })
  );
  return { sent, skipped: false };
}
const TABLE = "dispatch_notifications";
function normalizeNotification(row) {
  return {
    id: String((row == null ? void 0 : row.id) || ""),
    targetUserId: (row == null ? void 0 : row.target_user_id) || null,
    targetRole: String((row == null ? void 0 : row.target_role) || "loader"),
    orderId: (row == null ? void 0 : row.order_id) || null,
    routeId: (row == null ? void 0 : row.route_id) || null,
    title: String((row == null ? void 0 : row.title) || ""),
    message: String((row == null ? void 0 : row.message) || ""),
    status: (row == null ? void 0 : row.status) === "read" ? "read" : "unread",
    createdAt: String((row == null ? void 0 : row.created_at) || ""),
    readAt: (row == null ? void 0 : row.read_at) || null
  };
}
function formatOrderNumber(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function getLoadLabel$4(order) {
  return [order.quantity, order.unit, order.material].filter(Boolean).join(" ");
}
function getRouteLabel(route57) {
  if (!route57) return "Unassigned route";
  const parts = [`Route ${route57.code}`];
  if (route57.truck) parts.push(`Truck ${route57.truck}`);
  if (route57.driver) parts.push(route57.driver);
  return parts.join(" / ");
}
function cleanLoaderNote(value) {
  const note = String(value || "").replace(/\s+/g, " ").trim();
  return note.length > 300 ? `${note.slice(0, 297)}...` : note;
}
async function createLoaderNotification(input) {
  var _a2, _b, _c, _d;
  const routeLabel = getRouteLabel(input.route);
  const title = `Load next: ${formatOrderNumber(input.order)}`;
  const note = cleanLoaderNote(input.loaderNote);
  const message = [
    `${getLoadLabel$4(input.order)} for ${routeLabel} - ${input.order.customer}`,
    note ? `Note: ${note}` : ""
  ].filter(Boolean).join("\n");
  const { data: data2, error } = await supabaseAdmin.from(TABLE).insert({
    target_user_id: input.targetUserId || null,
    target_role: "loader",
    order_id: input.order.id,
    route_id: ((_a2 = input.route) == null ? void 0 : _a2.id) || input.order.assignedRouteId || null,
    title,
    message,
    status: "unread",
    created_by_user_id: ((_b = input.actor) == null ? void 0 : _b.id) || null,
    created_by_name: ((_c = input.actor) == null ? void 0 : _c.name) || ((_d = input.actor) == null ? void 0 : _d.email) || "System"
  }).select("*").single();
  if ((error == null ? void 0 : error.code) === "42P01") {
    throw new Error("Loader notification storage is not ready. Run dispatch_loader_notifications.sql in Supabase.");
  }
  if (error) throw new Error(error.message);
  const notification = normalizeNotification(data2);
  await sendPushToTarget({
    targetUserId: notification.targetUserId,
    targetRole: notification.targetRole,
    title: "New loader assignment",
    message: "Open Loader View to see the next load.",
    url: "/loader",
    tag: `loader-${notification.orderId || notification.id}`
  }).catch((error2) => {
    console.warn("[LOADER PUSH NOTIFICATION ERROR]", error2);
  });
  return notification;
}
async function listLoaderNotifications(user, limit = 30) {
  const { data: data2, error } = await supabaseAdmin.from(TABLE).select("*").or(`target_user_id.eq.${user.id},target_role.eq.loader`).order("created_at", { ascending: false }).limit(limit);
  if ((error == null ? void 0 : error.code) === "42P01") return [];
  if (error) throw new Error(error.message);
  return (data2 || []).map(normalizeNotification);
}
async function markLoaderNotificationRead(id, user) {
  const { data: data2, error } = await supabaseAdmin.from(TABLE).update({
    status: "read",
    read_at: (/* @__PURE__ */ new Date()).toISOString()
  }).eq("id", id).or(`target_user_id.eq.${user.id},target_role.eq.loader`).select("*").single();
  if ((error == null ? void 0 : error.code) === "42P01") {
    throw new Error("Loader notification storage is not ready. Run dispatch_loader_notifications.sql in Supabase.");
  }
  if (error) throw new Error(error.message);
  return normalizeNotification(data2);
}
async function clearLoaderNotificationHistory(user) {
  const { error } = await supabaseAdmin.from(TABLE).delete().eq("status", "read").or(`target_user_id.eq.${user.id},target_role.eq.loader`);
  if ((error == null ? void 0 : error.code) === "42P01") {
    throw new Error("Loader notification storage is not ready. Run dispatch_loader_notifications.sql in Supabase.");
  }
  if (error) throw new Error(error.message);
}
const classicColumnOptions = {
  routes: [
    { key: "code", label: "Code" },
    { key: "driver", label: "Driver" },
    { key: "status", label: "Status" },
    { key: "weight", label: "Stops" },
    { key: "start", label: "Start" },
    { key: "finish", label: "Finish" },
    { key: "distance", label: "Distance" }
  ],
  sites: [
    { key: "stop", label: "#" },
    { key: "orderNo", label: "Order No" },
    { key: "sku", label: "SKU" },
    { key: "customer", label: "Customer" },
    { key: "product", label: "Product" },
    { key: "quantity", label: "Qty" },
    { key: "unit", label: "Unit" },
    { key: "address", label: "Address" },
    { key: "requested", label: "Requested" },
    { key: "timePreference", label: "Time Preference" },
    { key: "arrived", label: "Arrived" },
    { key: "departed", label: "Departed" },
    { key: "eta", label: "ETA" },
    { key: "miles", label: "mi" }
  ],
  orders: [
    { key: "type", label: "Type" },
    { key: "orderNo", label: "Order No" },
    { key: "date", label: "Date" },
    { key: "client", label: "Client" },
    { key: "address", label: "Address" },
    { key: "weight", label: "QTY" },
    { key: "volume", label: "Volume" },
    { key: "status", label: "Status" },
    { key: "material", label: "Material" },
    { key: "timePreference", label: "Time Preference" },
    { key: "route", label: "Route" }
  ],
  unscheduled: [
    { key: "orderNo", label: "Order No" },
    { key: "date", label: "Date" },
    { key: "client", label: "Client" },
    { key: "address", label: "Address" },
    { key: "product", label: "Product" },
    { key: "roundTrip", label: "Round Trip" },
    { key: "weight", label: "Weight" },
    { key: "volume", label: "Volume" },
    { key: "timePreference", label: "Time Preference" },
    { key: "notes", label: "Notes" },
    { key: "route", label: "Route" }
  ]
};
const defaultClassicColumnSettings = {
  routes: ["code", "driver", "status", "weight", "start", "finish", "distance"],
  sites: ["stop", "orderNo", "sku", "address", "arrived", "departed", "eta", "miles"],
  orders: ["type", "orderNo", "date", "client", "weight", "volume", "status", "material"],
  unscheduled: ["orderNo", "date", "client", "address", "roundTrip", "weight", "volume", "route"]
};
function readEmailField(raw, labels) {
  for (const label of labels) {
    const escapedLabel = label.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const match = raw.match(
      new RegExp(`^\\s*${escapedLabel}(?![\\w/.-])\\s*:?\\s*(.+)$`, "im")
    );
    if (match == null ? void 0 : match[1]) return match[1].trim();
  }
  return "";
}
function decodeQuotedPrintable$1(raw) {
  return raw.replace(/=\r?\n/g, "").replace(/(?:=[0-9A-F]{2})+/gi, (encoded) => {
    var _a2;
    const bytes = (_a2 = encoded.match(/=([0-9A-F]{2})/gi)) == null ? void 0 : _a2.map((part) => parseInt(part.slice(1), 16));
    return bytes ? Buffer.from(bytes).toString("utf8") : encoded;
  });
}
function decodeHtmlEntities(value) {
  return value.replace(/&nbsp;/gi, " ").replace(/&amp;/gi, "&").replace(/&#215;|&#xD7;|&times;/gi, "x").replace(/&quot;/gi, '"').replace(/&#39;|&apos;/gi, "'").replace(/&shy;|&#173;|&#xAD;/gi, "").replace(/&#(\d+);/g, (_, code) => String.fromCharCode(Number(code))).replace(/&#x([0-9a-f]+);/gi, (_, code) => String.fromCharCode(parseInt(code, 16)));
}
function emailTextFromHtml(raw) {
  return decodeHtmlEntities(decodeQuotedPrintable$1(raw)).replace(/<style[\s\S]*?<\/style>/gi, "\n").replace(/<script[\s\S]*?<\/script>/gi, "\n").replace(/<head[\s\S]*?<\/head>/gi, "\n").replace(/<br\s*\/?>/gi, "\n").replace(/<\/(p|div|tr|table|h\d|td|th|li)>/gi, "\n").replace(/<[^>]+>/g, " ").replace(/[\u00ad\u200B-\u200D\uFEFF]/g, "").replace(/\r/g, "").replace(/^\s*=\s*$/gm, "").replace(/\s+=\s*$/gm, "").replace(/[ \t]+/g, " ").replace(/\n\s+/g, "\n").replace(/\n{3,}/g, "\n\n").trim();
}
function normalizeEmailText(raw) {
  return emailTextFromHtml(raw);
}
function textLines(raw) {
  return normalizeEmailText(raw).split("\n").map((line) => line.trim()).filter(Boolean);
}
function parseShopifyCustomer(raw) {
  var _a2, _b;
  const text = normalizeEmailText(raw);
  return ((_b = (_a2 = text.match(/new order from\s+([^:.\n]+)/i)) == null ? void 0 : _a2[1]) == null ? void 0 : _b.trim()) || "";
}
function parseShopifyOrderNumber(raw, subject = "") {
  var _a2, _b, _c, _d, _e, _f, _g, _h;
  const text = normalizeEmailText(raw);
  return ((_b = (_a2 = subject.match(/new order:\s*#?([A-Z0-9-]+)/i)) == null ? void 0 : _a2[1]) == null ? void 0 : _b.trim()) || ((_d = (_c = text.match(/new order:\s*#?([A-Z0-9-]+)/i)) == null ? void 0 : _c[1]) == null ? void 0 : _d.trim()) || ((_f = (_e = text.match(/order\s+#?([A-Z0-9-]+)\s*\(/i)) == null ? void 0 : _e[1]) == null ? void 0 : _f.trim()) || ((_h = (_g = text.match(/\border\s+#?([A-Z0-9-]+)/i)) == null ? void 0 : _g[1]) == null ? void 0 : _h.trim()) || "";
}
function cleanOrderNumber$2(value) {
  var _a2, _b;
  return ((_b = (_a2 = value.match(/#?\s*([A-Z0-9-]+)/i)) == null ? void 0 : _a2[1]) == null ? void 0 : _b.trim()) || "";
}
function normalizePhoneNumber(value) {
  const digits = value.replace(/\D/g, "");
  if (digits.length === 10) return digits;
  if (digits.length === 11 && digits.startsWith("1")) return digits.slice(1);
  return "";
}
function findPhoneInText(value) {
  const cleaned = String(value || "").replace(/[\u00ad\u200B-\u200D\uFEFF]/g, "").replace(/[^\S\r\n]+/g, " ");
  const candidates = cleaned.match(
    /(?:\+?1[\s().-]*)?(?:\(?\d{3}\)?[\s().-]*\d{3}[\s().-]*\d{4}|\d{10})/g
  ) || [];
  for (const candidate of candidates) {
    const phone = normalizePhoneNumber(candidate);
    if (phone) return phone;
  }
  return "";
}
function findPhoneInLooseText(value) {
  const text = String(value || "").replace(/[\u00ad\u200B-\u200D\uFEFF]/g, "");
  const candidates = text.match(/(?:\d[\s().-]*){10,11}/g) || [];
  for (const candidate of candidates) {
    const phone = normalizePhoneNumber(candidate);
    if (phone) return phone;
  }
  return "";
}
function findPhoneNearEmail(lines) {
  for (const [index, line] of lines.entries()) {
    if (!/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/i.test(line)) continue;
    const nearbyLines = [
      lines[index - 5] || "",
      lines[index - 4] || "",
      lines[index - 3] || "",
      lines[index - 2] || "",
      lines[index - 1] || "",
      line,
      lines[index + 1] || "",
      lines[index + 2] || ""
    ];
    const nearbyBlock = nearbyLines.join(" ");
    const blockPhone = findPhoneInText(nearbyBlock) || findPhoneInLooseText(nearbyBlock);
    if (blockPhone) return blockPhone;
    for (const nearbyLine of nearbyLines) {
      const phone = findPhoneInText(nearbyLine) || findPhoneInLooseText(nearbyLine);
      if (phone) return phone;
    }
  }
  return "";
}
function findPhoneInAddressBlocks(lines) {
  const starts = lines.map((line, index) => ({ line, index })).filter(({ line }) => /^(billing|shipping)\s+address\b/i.test(line)).map(({ index }) => index);
  for (const start of starts) {
    const block = [];
    for (const line of lines.slice(start, start + 12)) {
      if (block.length && /^(billing|shipping)\s+address\b|what happens next\??|order summary|subtotal|total:?$/i.test(line)) {
        break;
      }
      block.push(line);
    }
    const blockPhone = findPhoneNearEmail(block) || findPhoneInText(block.join(" ")) || findPhoneInLooseText(block.join(" "));
    if (blockPhone) return blockPhone;
  }
  return "";
}
function parsePhoneNumber(raw) {
  const normalized = normalizeEmailText(raw);
  const lines = normalized.split("\n").map((line) => line.trim()).filter(Boolean);
  const labelled = readEmailField(normalized, ["Phone", "Phone Number", "Mobile", "Cell", "Tel", "Telephone"]) || "";
  const labelledPhone = normalizePhoneNumber(labelled);
  if (labelledPhone) return labelledPhone;
  const nearEmailPhone = findPhoneNearEmail(lines);
  if (nearEmailPhone) return nearEmailPhone;
  const addressBlockPhone = findPhoneInAddressBlocks(lines);
  if (addressBlockPhone) return addressBlockPhone;
  return findPhoneInText(normalized) || findPhoneInLooseText(normalized);
}
function formatPhoneForContact(phone) {
  const digits = normalizePhoneNumber(phone);
  return digits ? `(${digits.slice(0, 3)}) ${digits.slice(3, 6)}-${digits.slice(6)}` : "";
}
function combineContactParts(email, phone) {
  return [email.trim(), formatPhoneForContact(phone)].filter(Boolean).join(" / ");
}
function isProductHeader(value) {
  return /^(product|quantity|price|unit|units|price units?|order summary)$/i.test(
    value.trim()
  );
}
function isShopifyProductBoundary(value) {
  return /^(billing address|shipping address|subtotal|shipping|tax|total|payment method|delivery or pickup preference date|please describe where|what happens next\??)$/i.test(
    value.trim()
  );
}
function isCountryOrAddressFragment(value) {
  const line = value.trim();
  return /^(?:united\s+states|states|ed\s+states)\s*\(us\)$/i.test(line) || /\b(?:united\s+states|states|ed\s+states)\s*\(us\)\b/i.test(line) || /^[A-Z]{2}\s+\d{5}(?:-\d{4})?$/i.test(line) || /^\d{3,}/.test(line);
}
function isProductCandidate(value) {
  const line = cleanProductMaterial(value);
  return !isCountryOrAddressFragment(value) && !isCountryOrAddressFragment(line) && /[a-z]/i.test(line) && !isProductHeader(line) && !isShopifyProductBoundary(line) && !/@media|template_|max-width|font-|color:|background|border|padding|margin|display:|width:/i.test(line) && !/[{};]/.test(line) && !/^\(#?\d+\)$/i.test(line) && !/^#?\d+$/.test(line) && !/^order\s+#/i.test(line) && !/^(subtotal|shipping|tax|total|payment method):/i.test(line) && !/^\$/.test(line);
}
function cleanCityValue(value) {
  return value.replace(/\bUSA\b\.?/gi, "").replace(/\s*,\s*$/, "").replace(/\s{2,}/g, " ").trim();
}
function splitStreetAndCity(value) {
  const cleaned = value.trim();
  const cityStateZipMatch = cleaned.match(
    /^(.*?),\s*([^,]+,\s*[A-Z]{2}(?:\s+\d{5}(?:-\d{4})?)?)(?:,\s*USA)?$/i
  );
  if (cityStateZipMatch) {
    return {
      address: cityStateZipMatch[1].trim(),
      city: cleanCityValue(cityStateZipMatch[2])
    };
  }
  const stateZipMatch = cleaned.match(
    /^(.*?)(?:,|\s{2,})\s*([^,\d]+,\s*[A-Z]{2}\s+\d{5}(?:-\d{4})?)(?:,\s*USA)?$/i
  );
  if (stateZipMatch) {
    return {
      address: stateZipMatch[1].trim(),
      city: cleanCityValue(stateZipMatch[2])
    };
  }
  return { address: cleaned, city: "" };
}
function cleanQuantityValue(value) {
  var _a2, _b;
  const line = value.trim();
  return ((_a2 = line.match(/(?:x|\u00d7)\s*(\d+(?:\.\d+)?)/i)) == null ? void 0 : _a2[1]) || ((_b = line.match(/^(\d+(?:\.\d+)?)\s*(?:unit|units|ton|tons|yard|yards|gallon|gallons)?\b/i)) == null ? void 0 : _b[1]) || "";
}
function normalizeDispatchUnit(value, fallback = "Unit") {
  const unit = value.trim();
  if (!unit || /^(price|quantity|product|amount)$/i.test(unit)) return fallback;
  if (isCountryOrAddressFragment(unit)) return fallback;
  if (/yards?/i.test(unit)) return "Yard";
  if (/tons?/i.test(unit)) return "Ton";
  if (/gallons?/i.test(unit)) return "Gallons";
  if (/bags?/i.test(unit)) return "Bags";
  if (/units?/i.test(unit)) return "Unit";
  return unit;
}
function cleanProductMaterial(value) {
  return value.replace(/\b(?:united\s+states|states|ed\s+states)\s*\(us\)\s*/gi, "").replace(/^\s*(?:ed\s+)?states\s*\(us\)\s*/i, "").replace(/^(?:product|material|item)\s*:?\s*/i, "").replace(/\s*(?:x|\u00d7)\s*\d+(?:\.\d+)?\s*$/i, "").replace(/\s+\$?\d+(?:\.\d{2})?\s*$/i, "").replace(/\s{2,}/g, " ").trim();
}
function normalizeProductLookupText(value) {
  return value.toLowerCase().replace(/[^a-z0-9]+/g, " ").replace(/\s+/g, " ").trim();
}
function mapPriceUnitLabelToDispatchUnit(value) {
  const label = String(value || "").trim();
  if (/per\s+ton\b/i.test(label)) return "Ton";
  if (/per\s+yard\b/i.test(label)) return "Yard";
  if (/per\s+bag\b/i.test(label)) return "Bags";
  if (/per\s+gallon\b/i.test(label)) return "Gallons";
  return normalizeDispatchUnit(label, "");
}
function getLookupTokens(value) {
  return normalizeProductLookupText(value).split(" ").filter((token) => token.length > 1 && !/^\d+$/.test(token));
}
function getProductMatchScore(material, row) {
  const normalizedMaterial = normalizeProductLookupText(material);
  const normalizedTitle = normalizeProductLookupText(row.product_title || "");
  const normalizedSku = normalizeProductLookupText(row.sku || "");
  if (!normalizedMaterial || !normalizedTitle && !normalizedSku) return 0;
  if (normalizedTitle === normalizedMaterial || normalizedSku === normalizedMaterial) return 100;
  if (normalizedTitle.includes(normalizedMaterial)) return 90;
  if (normalizedMaterial.includes(normalizedTitle) && normalizedTitle.length > 3) return 80;
  if (normalizedSku && normalizedMaterial.includes(normalizedSku)) return 75;
  const materialTokens = getLookupTokens(material);
  const titleTokens = new Set(getLookupTokens(row.product_title || ""));
  if (!materialTokens.length || !titleTokens.size) return 0;
  const matched = materialTokens.filter((token) => titleTokens.has(token)).length;
  const score = Math.round(matched / materialTokens.length * 60);
  return matched >= Math.min(2, materialTokens.length) ? score : 0;
}
async function getDispatchUnitForMaterial(material) {
  var _a2, _b;
  const normalizedMaterial = normalizeProductLookupText(material);
  if (!normalizedMaterial) return "";
  let result = await supabaseAdmin.from("product_source_map").select("sku, product_title, unit_label, price_unit_label");
  if (((_a2 = result.error) == null ? void 0 : _a2.code) === "42703") {
    result = await supabaseAdmin.from("product_source_map").select("sku, product_title, unit_label");
  }
  if (result.error) {
    console.error("[DISPATCH UNIT LOOKUP ERROR]", result.error);
    return "";
  }
  const rows = (result.data || []).filter((row) => row.product_title);
  const bestMatch = (_b = rows.map((row) => ({
    row,
    score: getProductMatchScore(material, row)
  })).filter((entry2) => entry2.score > 0).sort((a, b) => b.score - a.score)[0]) == null ? void 0 : _b.row;
  return mapPriceUnitLabelToDispatchUnit(
    (bestMatch == null ? void 0 : bestMatch.unit_label) || (bestMatch == null ? void 0 : bestMatch.price_unit_label)
  );
}
async function getDispatchMaterialOptions() {
  const result = await supabaseAdmin.from("product_source_map").select("product_title").order("product_title", { ascending: true });
  if (result.error) {
    console.error("[DISPATCH MATERIAL OPTIONS ERROR]", result.error);
    return [];
  }
  const seen = /* @__PURE__ */ new Set();
  return (result.data || []).map((row) => String(row.product_title || "").trim()).filter((title) => {
    const key = title.toLowerCase();
    if (!title || seen.has(key)) return false;
    seen.add(key);
    return true;
  });
}
async function getDispatchProductDetailsForMaterials(materials) {
  const uniqueMaterials = Array.from(
    new Set(materials.map((material) => String(material || "").trim()).filter(Boolean))
  );
  if (!uniqueMaterials.length) return {};
  const result = await supabaseAdmin.from("product_source_map").select("*");
  if (result.error) {
    console.error("[DISPATCH PRODUCT DETAILS ERROR]", result.error);
    return {};
  }
  const rows = (result.data || []).filter(
    (row) => row.product_title || row.sku
  );
  return uniqueMaterials.reduce((details, material) => {
    var _a2;
    const bestMatch = (_a2 = rows.map((row) => ({
      row,
      score: getProductMatchScore(material, {
        sku: row.sku,
        product_title: row.product_title
      })
    })).filter((entry2) => entry2.score > 0).sort((a, b) => b.score - a.score)[0]) == null ? void 0 : _a2.row;
    if (!bestMatch) return details;
    details[material] = {
      sku: String(bestMatch.sku || ""),
      title: String(bestMatch.product_title || bestMatch.sku || material),
      vendor: String(bestMatch.pickup_vendor || ""),
      imageUrl: String(bestMatch.image_url || ""),
      unitLabel: String(bestMatch.unit_label || bestMatch.price_unit_label || ""),
      variantId: String(bestMatch.variant_id || "")
    };
    return details;
  }, {});
}
function parseQuantityFromEmail(raw) {
  var _a2, _b;
  const normalized = normalizeEmailText(raw);
  return ((_a2 = normalized.match(/(?:^|\s)(?:x|\u00d7)\s*(\d+(?:\.\d+)?)(?:\s|$)/i)) == null ? void 0 : _a2[1]) || ((_b = normalized.match(/\bQuantity\b[\s:]+(\d+(?:\.\d+)?)/i)) == null ? void 0 : _b[1]) || "";
}
function parseShopifyProduct(raw) {
  const lines = textLines(raw);
  const quantityLineIndexes = lines.map((line, index) => ({ line, index })).filter(({ line, index }) => {
    if (!/(?:x|\u00d7)\s*\d+/i.test(line)) return false;
    const window2 = lines.slice(Math.max(0, index - 4), Math.min(lines.length, index + 3)).join(" ");
    return /product|price|\(#?\d+\)|subtotal|shipping|tax|total/i.test(window2);
  }).map(({ index }) => index);
  const quantityLineIndex = quantityLineIndexes[0] ?? -1;
  const product = parseShopifyProductAtLine(raw, quantityLineIndex);
  if (product.material || product.quantity) return product;
  const productIndex = lines.findIndex((line) => /^product$/i.test(line));
  const material = lines.slice(productIndex >= 0 ? productIndex + 1 : 0).find(
    (line) => isProductCandidate(line)
  ) || "";
  let parsedQuantity = "";
  if (material) {
    const materialIndex = lines.findIndex((line) => line.includes(material));
    const nearbyLines = materialIndex >= 0 ? lines.slice(materialIndex + 1, Math.min(lines.length, materialIndex + 8)) : lines;
    parsedQuantity = nearbyLines.map((line) => cleanQuantityValue(line)).find(Boolean) || "";
  }
  if (!parsedQuantity) {
    parsedQuantity = parseQuantityFromEmail(raw);
  }
  return { material: cleanProductMaterial(material), quantity: parsedQuantity };
}
function parseShopifyProductAtLine(raw, quantityLineIndex) {
  var _a2;
  const lines = textLines(raw);
  if (quantityLineIndex < 0) return { material: "", quantity: "" };
  const quantity = ((_a2 = lines[quantityLineIndex].match(/(?:x|\u00d7)\s*(\d+(?:\.\d+)?)/i)) == null ? void 0 : _a2[1]) || "";
  let material = "";
  const sameLineBeforeQuantity = lines[quantityLineIndex].split(/(?:x|\u00d7)\s*\d+/i)[0].trim();
  const sameLineCandidates = sameLineBeforeQuantity.split(/\s{2,}/).map((part) => part.trim()).filter(Boolean);
  material = sameLineCandidates.find(isProductCandidate) || "";
  const sameLineMaterial = lines[quantityLineIndex].replace(/(?:x|\u00d7)\s*\d+(?:\.\d+)?[\s\S]*$/i, "").trim();
  if (!material && sameLineMaterial && isProductCandidate(sameLineMaterial)) {
    material = sameLineMaterial;
  }
  for (let index = quantityLineIndex - 1; index >= 0; index -= 1) {
    if (material) break;
    const candidate = lines[index];
    if (isShopifyProductBoundary(candidate)) break;
    if (isProductCandidate(candidate)) {
      material = candidate;
      break;
    }
  }
  return { material: cleanProductMaterial(material), quantity };
}
function parseShopifyProducts(raw) {
  const lines = textLines(raw);
  const productStart = lines.findIndex((line) => /^product$/i.test(line));
  const productEndCandidates = [
    lines.findIndex((line) => /^subtotal:?/i.test(line)),
    lines.findIndex((line) => /^billing address\b/i.test(line)),
    lines.findIndex((line) => /^shipping address\b/i.test(line))
  ].filter((index) => index > productStart);
  const productEnd = productStart >= 0 && productEndCandidates.length ? Math.min(...productEndCandidates) : lines.length;
  const quantityLineIndexes = lines.map((line, index) => ({ line, index })).filter(({ line, index }) => {
    if (productStart >= 0 && (index <= productStart || index >= productEnd)) {
      return false;
    }
    if (!/(?:x|\u00d7)\s*\d+/i.test(line)) return false;
    const window2 = lines.slice(Math.max(0, index - 4), Math.min(lines.length, index + 3)).join(" ");
    return /product|price|\(#?\d+\)|subtotal|shipping|tax|total/i.test(window2);
  }).map(({ index }) => index);
  const products = quantityLineIndexes.map((index) => parseShopifyProductAtLine(raw, index)).filter((product) => product.material && product.quantity);
  const seen = /* @__PURE__ */ new Set();
  const uniqueProducts = products.filter((product) => {
    const key = `${product.material.toLowerCase()}::${product.quantity}`;
    if (seen.has(key)) return false;
    seen.add(key);
    return true;
  });
  return uniqueProducts.length ? uniqueProducts : [parseShopifyProduct(raw)].filter((product) => product.material);
}
function parseShopifyShipping(raw) {
  const lines = textLines(raw);
  const start = lines.findIndex((line) => /^shipping address\b/i.test(line));
  if (start < 0) return { customer: "", address: "", city: "", contact: "", phone: "" };
  const block = [];
  for (const line of lines.slice(start)) {
    if (block.length && /^what happens next\??$/i.test(line)) break;
    if (block.length && /^billing address\b/i.test(line)) break;
    const stripped = line.replace(/^shipping address\s*/i, "").trim();
    if (stripped) block.push(stripped);
  }
  const cleaned = block.flatMap((line) => line.split(/\s{2,}/)).map((line) => line.trim()).filter(Boolean);
  const contact = cleaned.find((line) => /[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/i.test(line)) || "";
  const phone = cleaned.map((line) => parsePhoneNumber(line)).find(Boolean) || "";
  const customer = cleaned.find((line) => !/\d/.test(line) && !/address/i.test(line) && !/,/.test(line)) || "";
  const rawAddress = cleaned.find((line) => /\d/.test(line) && !/^\d{7,}$/.test(line) && !/@/.test(line) && !/^\s*[A-Z]{2}\s+\d{5}/i.test(line)) || "";
  const splitAddress = splitStreetAndCity(rawAddress);
  const rawCity = cleaned.find((line) => /,\s*[A-Z]{2}\s+\d{5}/i.test(line)) || cleaned.find((line) => /\b[A-Z]{2}\s+\d{5}/i.test(line)) || "";
  const city = splitAddress.city || cleanCityValue(rawCity);
  return { customer, address: splitAddress.address, city, contact, phone };
}
function parseShopifyDeliveryNotes(raw) {
  var _a2, _b;
  const text = normalizeEmailText(raw);
  return ((_b = (_a2 = text.match(/Please describe where you would like your order dropped off:\s*([\s\S]+?)(?:Billing address|Shipping address|What Happens Next\?|$)/i)) == null ? void 0 : _a2[1]) == null ? void 0 : _b.trim()) || "";
}
function isBadParsedValue(value) {
  return !value || /^=?\s*$/.test(value) || /^(price|price units?|quantity|unit|units)$/i.test(value);
}
function detectTimePreference(text) {
  if (/\bmorning\b|\bam\b|a\.m\./i.test(text)) return "Morning";
  if (/\bafternoon\b|\bnoon\b|\bpm\b|p\.m\./i.test(text)) return "Afternoon";
  if (/\bevening\b|\bnight\b/i.test(text)) return "Evening";
  return "Anytime";
}
function parseDispatchEmail(raw) {
  var _a2, _b, _c;
  const normalized = normalizeEmailText(raw);
  const shipping = parseShopifyShipping(raw);
  const shopifyProduct = parseShopifyProduct(raw);
  const shopifyProducts = parseShopifyProducts(raw);
  const shopifyNotes = parseShopifyDeliveryNotes(raw);
  const subject = readEmailField(normalized, ["Subject"]);
  const orderNumber = cleanOrderNumber$2(
    readEmailField(normalized, ["Order Number", "Order No"]) || parseShopifyOrderNumber(raw, subject)
  );
  const contactEmail = readEmailField(normalized, ["Email", "Contact", "Customer Email"]) || shipping.contact || ((_a2 = normalized.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/i)) == null ? void 0 : _a2[0]) || "";
  const phone = findPhoneInText(shopifyNotes) || findPhoneInLooseText(shopifyNotes) || parsePhoneNumber(raw) || shipping.phone || "";
  const contact = combineContactParts(contactEmail, phone);
  const customer = readEmailField(normalized, ["Customer", "Client", "Name", "Company"]) || parseShopifyCustomer(raw) || shipping.customer || subject.replace(/^(order|delivery|quote)\s*[:-]\s*/i, "").trim();
  const address = readEmailField(normalized, [
    "Address",
    "Delivery Address",
    "Jobsite",
    "Job Site",
    "Ship To"
  ]) || shipping.address;
  const rawCity = readEmailField(normalized, ["City", "City/State", "Location"]) || shipping.city;
  const labelledMaterial = readEmailField(normalized, ["Material", "Product", "Item"]);
  const labelledQuantity = readEmailField(normalized, ["Quantity", "Qty", "Amount"]);
  const material = cleanProductMaterial(
    isBadParsedValue(labelledMaterial) ? shopifyProduct.material : labelledMaterial
  );
  const quantity = cleanQuantityValue(
    isBadParsedValue(labelledQuantity) ? shopifyProduct.quantity : labelledQuantity
  );
  const unit = normalizeDispatchUnit(readEmailField(normalized, ["Unit", "UOM"]), "Unit");
  const products = isBadParsedValue(labelledMaterial) ? shopifyProducts.map((product) => ({
    material: cleanProductMaterial(product.material),
    quantity: cleanQuantityValue(product.quantity)
  })) : [{ material, quantity }];
  const splitAddress = splitStreetAndCity(address);
  const cleanAddress = splitAddress.address;
  const city = cleanCityValue(splitAddress.city || rawCity);
  const requestedWindow = ((_c = (_b = normalized.match(/Delivery or Pickup Preference Date:\s*([^\n]+)/i)) == null ? void 0 : _b[1]) == null ? void 0 : _c.trim()) || readEmailField(normalized, ["Requested Window", "Delivery Window", "Requested Date", "Date", "When"]) || "Needs scheduling";
  const truckPreference = readEmailField(normalized, ["Truck", "Truck Preference", "Equipment"]);
  const notes = readEmailField(normalized, ["Notes", "Instructions", "Special Instructions"]) || shopifyNotes;
  const timePreference = detectTimePreference(`${requestedWindow}
${notes}
${normalized}`);
  return {
    subject,
    orderNumber,
    customer: customer || "Email Order",
    contact,
    address: cleanAddress,
    city,
    material,
    quantity,
    products: products.filter((product) => product.material),
    unit,
    requestedWindow,
    timePreference,
    truckPreference,
    notes
  };
}
function getLegacyTruckTons(capacity) {
  var _a2;
  return ((_a2 = String(capacity || "").match(/(\d+(?:\.\d+)?)\s*tons?/i)) == null ? void 0 : _a2[1]) || "";
}
function getLegacyTruckYards(capacity) {
  var _a2;
  return ((_a2 = String(capacity || "").match(/(\d+(?:\.\d+)?)\s*yards?/i)) == null ? void 0 : _a2[1]) || "";
}
const seedDispatchTrucks = [
  {
    id: "truck-12",
    label: "Truck 12",
    truckType: "Tri-axle",
    tons: "22",
    yards: "",
    capacity: "22 TonS",
    licensePlate: "GHS-12",
    isActive: true
  },
  {
    id: "truck-18",
    label: "Truck 18",
    truckType: "Walking floor",
    tons: "",
    yards: "25",
    capacity: "25 YardS",
    licensePlate: "GHS-18",
    isActive: true
  },
  {
    id: "truck-05",
    label: "Truck 05",
    truckType: "Tri-axle",
    tons: "22",
    yards: "",
    capacity: "22 TonS",
    licensePlate: "GHS-05",
    isActive: true
  }
];
const seedDispatchEmployees = [
  {
    id: "employee-paul",
    name: "Paul",
    role: "driver",
    phone: "",
    email: "",
    isActive: true
  },
  {
    id: "employee-peter",
    name: "Peter",
    role: "driver",
    phone: "",
    email: "",
    isActive: true
  },
  {
    id: "employee-andrew",
    name: "Andrew",
    role: "driver",
    phone: "",
    email: "",
    isActive: true
  },
  {
    id: "employee-manny",
    name: "Manny",
    role: "helper",
    phone: "",
    email: "",
    isActive: true
  },
  {
    id: "employee-luis",
    name: "Luis",
    role: "helper",
    phone: "",
    email: "",
    isActive: true
  },
  {
    id: "employee-nate",
    name: "Nate",
    role: "helper",
    phone: "",
    email: "",
    isActive: true
  }
];
const seedDispatchRoutes = [
  {
    id: "route-north",
    code: "R-12",
    truckId: "truck-12",
    truck: "Truck 12",
    driverId: "employee-paul",
    driver: "Paul",
    helperId: "employee-manny",
    helper: "Manny",
    color: "#f97316",
    shift: "6:30a - 3:30p",
    region: "North / Menomonee Falls",
    isActive: true
  },
  {
    id: "route-west",
    code: "R-18",
    truckId: "truck-18",
    truck: "Truck 18",
    driverId: "employee-peter",
    driver: "Peter",
    helperId: "employee-luis",
    helper: "Luis",
    color: "#06b6d4",
    shift: "7:00a - 4:00p",
    region: "West / Waukesha",
    isActive: true
  },
  {
    id: "route-south",
    code: "R-05",
    truckId: "truck-05",
    truck: "Truck 05",
    driverId: "employee-andrew",
    driver: "Andrew",
    helperId: "employee-nate",
    helper: "Nate",
    color: "#22c55e",
    shift: "6:00a - 2:30p",
    region: "South / Oak Creek",
    isActive: true
  }
];
const seedDispatchOrders = [
  {
    id: "D-24081",
    source: "email",
    customer: "Oak Creek Plaza",
    contact: "shipping@oakcreekplaza.com",
    address: "2543 W Applebrook Lane",
    city: "Oak Creek, WI",
    material: "Coarse Torpedo Sand",
    quantity: "12",
    unit: "TonS",
    requestedWindow: "Today 9:00a - 11:00a",
    notes: "Forklift on site. Call before arrival.",
    status: "new"
  },
  {
    id: "D-24082",
    source: "email",
    customer: "Merton Build Group",
    contact: "dispatch@mertonbuild.com",
    address: "N67W28345 Silver Spring Dr",
    city: "Sussex, WI",
    material: "Premium Mulch",
    quantity: "22",
    unit: "YardS",
    requestedWindow: "Today 10:30a - 1:00p",
    truckPreference: "Walking floor",
    notes: "Back alley drop. Need photo after unload.",
    status: "scheduled",
    assignedRouteId: "route-west"
  },
  {
    id: "D-24083",
    source: "manual",
    customer: "Village of Men Falls",
    contact: "yard@menfalls.gov",
    address: "W156N8480 Pilgrim Rd",
    city: "Menomonee Falls, WI",
    material: "Road Salt",
    quantity: "8",
    unit: "TonS",
    requestedWindow: "Tomorrow 7:00a - 9:00a",
    notes: "Municipal account. Ticket copy required.",
    status: "hold"
  },
  {
    id: "D-24084",
    source: "email",
    customer: "Lakeview Landscape",
    contact: "ops@lakeviewlandscape.com",
    address: "2211 Scenic Ridge Rd",
    city: "Delafield, WI",
    material: "Screened Topsoil",
    quantity: "16",
    unit: "YardS",
    requestedWindow: "Today 1:00p - 3:00p",
    notes: "Split delivery with second stop if needed.",
    status: "scheduled",
    assignedRouteId: "route-north"
  }
];
const ORDERS_TABLE = "dispatch_orders";
const ROUTES_TABLE = "dispatch_routes";
const TRUCKS_TABLE = "dispatch_trucks";
const EMPLOYEES_TABLE = "dispatch_employees";
const SETTINGS_TABLE = "dispatch_settings";
const DRIVER_LOCATIONS_TABLE = "dispatch_driver_locations";
const CLASSIC_COLUMNS_SETTING_KEY = "classic_columns";
const DISPATCH_ORDER_LIGHT_COLUMNS = [
  "id",
  "order_number",
  "source",
  "customer",
  "contact",
  "address",
  "city",
  "material",
  "quantity",
  "unit",
  "requested_window",
  "time_preference",
  "truck_preference",
  "notes",
  "status",
  "assigned_route_id",
  "stop_sequence",
  "delivery_status",
  "eta",
  "travel_minutes",
  "travel_miles",
  "travel_summary",
  "arrived_at",
  "departed_at",
  "delivered_at",
  "proof_name",
  "proof_notes",
  "email_subject",
  "mailbox_message_id",
  "signature_name",
  "ticket_numbers",
  "inspection_status",
  "created_at",
  "updated_at"
].join(", ");
function normalizeOrder(row) {
  const deliveryStatus = row.delivery_status === "en_route" || row.delivery_status === "arrived" || row.delivery_status === "delivered" || row.delivery_status === "issue" ? row.delivery_status : "not_started";
  const requestedWindow = String(row.requested_window || "");
  const notes = String(row.notes || "");
  const rawEmail = row.raw_email || null;
  const derivedQuantity = String(row.quantity || "") || (rawEmail ? parseQuantityFromEmail(String(rawEmail)) : "");
  const derivedTimePreference = row.time_preference || detectTimePreference(`${requestedWindow}
${notes}
${rawEmail || ""}`) || "Anytime";
  return {
    id: String(row.id),
    orderNumber: row.order_number || null,
    source: row.source === "email" ? "email" : "manual",
    customer: String(row.customer || ""),
    contact: String(row.contact || ""),
    address: String(row.address || ""),
    city: String(row.city || ""),
    material: String(row.material || ""),
    quantity: derivedQuantity,
    unit: normalizeDispatchUnit(String(row.unit || ""), "Unit"),
    requestedWindow,
    timePreference: derivedTimePreference,
    truckPreference: row.truck_preference || null,
    notes,
    status: row.status === "scheduled" || row.status === "hold" || row.status === "delivered" || row.status === "cancelled" ? row.status : "new",
    assignedRouteId: row.assigned_route_id || null,
    stopSequence: row.stop_sequence === null || row.stop_sequence === void 0 ? null : Number(row.stop_sequence),
    deliveryStatus,
    eta: row.eta || null,
    travelMinutes: row.travel_minutes === null || row.travel_minutes === void 0 ? null : Number(row.travel_minutes),
    travelMiles: row.travel_miles === null || row.travel_miles === void 0 ? null : Number(row.travel_miles),
    travelSummary: row.travel_summary || null,
    arrivedAt: row.arrived_at || null,
    departedAt: row.departed_at || null,
    deliveredAt: row.delivered_at || null,
    proofName: row.proof_name || null,
    proofNotes: row.proof_notes || null,
    emailSubject: row.email_subject || null,
    rawEmail,
    mailboxMessageId: row.mailbox_message_id || null,
    signatureName: row.signature_name || null,
    signatureData: row.signature_data || null,
    photoUrls: row.photo_urls || null,
    ticketNumbers: row.ticket_numbers || null,
    inspectionStatus: row.inspection_status || null,
    checklistJson: row.checklist_json || null,
    created_at: row.created_at,
    updated_at: row.updated_at
  };
}
function normalizeRoute(row) {
  return {
    id: String(row.id),
    code: String(row.code || ""),
    truckId: row.truck_id || null,
    truck: String(row.truck || ""),
    driverId: row.driver_id || null,
    driver: String(row.driver || ""),
    helperId: row.helper_id || null,
    helper: String(row.helper || ""),
    color: String(row.color || "#38bdf8"),
    shift: String(row.shift || ""),
    region: String(row.region || ""),
    isActive: row.is_active !== false,
    created_at: row.created_at,
    updated_at: row.updated_at
  };
}
function normalizeTruck(row) {
  const capacity = String(row.capacity || "");
  return {
    id: String(row.id),
    label: String(row.label || ""),
    truckType: String(row.truck_type || ""),
    tons: row.tons === null || row.tons === void 0 ? getLegacyTruckTons(capacity) : String(row.tons),
    yards: row.yards === null || row.yards === void 0 ? getLegacyTruckYards(capacity) : String(row.yards),
    capacity,
    licensePlate: row.license_plate || null,
    isActive: row.is_active !== false,
    created_at: row.created_at,
    updated_at: row.updated_at
  };
}
function normalizeEmployee(row) {
  const role = row.role === "helper" || row.role === "dispatcher" ? row.role : "driver";
  return {
    id: String(row.id),
    name: String(row.name || ""),
    role,
    phone: row.phone || null,
    email: row.email || null,
    isActive: row.is_active !== false,
    created_at: row.created_at,
    updated_at: row.updated_at
  };
}
function normalizeDriverLocation(row) {
  return {
    id: String(row.id),
    routeId: row.route_id || null,
    orderId: row.order_id || null,
    driverId: row.driver_id || null,
    driverName: String(row.driver_name || ""),
    truck: String(row.truck || ""),
    latitude: Number(row.latitude),
    longitude: Number(row.longitude),
    accuracy: row.accuracy === null || row.accuracy === void 0 ? null : Number(row.accuracy),
    heading: row.heading === null || row.heading === void 0 ? null : Number(row.heading),
    speed: row.speed === null || row.speed === void 0 ? null : Number(row.speed),
    capturedAt: row.captured_at || row.updated_at || row.created_at || (/* @__PURE__ */ new Date()).toISOString(),
    created_at: row.created_at,
    updated_at: row.updated_at
  };
}
function formatSupabaseError(error) {
  if (!error) return "Unknown storage error";
  return error.message || error.details || error.hint || "Unknown storage error";
}
function normalizeClassicColumnSettings(value) {
  const parsed = typeof value === "string" ? JSON.parse(value || "{}") : value;
  const settings2 = parsed && typeof parsed === "object" ? parsed : {};
  return Object.keys(defaultClassicColumnSettings).reduce(
    (next, table) => {
      const allowed = new Set(classicColumnOptions[table].map((option) => option.key));
      const selected = Array.isArray(settings2[table]) ? settings2[table].filter((key) => allowed.has(key)) : [];
      next[table] = selected.length ? selected : defaultClassicColumnSettings[table];
      return next;
    },
    {}
  );
}
async function getClassicColumnSettings() {
  const { data: data2, error } = await supabaseAdmin.from(SETTINGS_TABLE).select("value").eq("key", CLASSIC_COLUMNS_SETTING_KEY).maybeSingle();
  if (error) {
    console.warn("[CLASSIC COLUMN SETTINGS ERROR]", formatSupabaseError(error));
    return defaultClassicColumnSettings;
  }
  try {
    return normalizeClassicColumnSettings((data2 == null ? void 0 : data2.value) || "{}");
  } catch (error2) {
    console.warn("[CLASSIC COLUMN SETTINGS PARSE ERROR]", error2);
    return defaultClassicColumnSettings;
  }
}
async function saveClassicColumnSettings(settings2) {
  const normalized = normalizeClassicColumnSettings(settings2);
  const { error } = await supabaseAdmin.from(SETTINGS_TABLE).upsert({
    key: CLASSIC_COLUMNS_SETTING_KEY,
    value: JSON.stringify(normalized),
    updated_at: (/* @__PURE__ */ new Date()).toISOString()
  });
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalized;
}
function buildDispatchDestinationAddress(address, city) {
  return [address, city].map((part) => String(part || "").trim()).filter(Boolean).join(", ");
}
async function getDispatchOriginAddress() {
  const { data: data2, error } = await supabaseAdmin.from("origin_addresses").select("address").eq("is_active", true).limit(1).maybeSingle();
  if (error) {
    console.warn("[DISPATCH MAP ORIGIN ERROR]", formatSupabaseError(error));
  }
  return String((data2 == null ? void 0 : data2.address) || "").trim() || "W185 N7487 Narrow Ln, Menomonee Falls, WI 53051";
}
async function buildDispatchTravelPayload(address, city) {
  const destination = buildDispatchDestinationAddress(address, city);
  if (!destination) {
    return {
      travel_minutes: null,
      travel_miles: null,
      travel_summary: null
    };
  }
  const estimate = await getDispatchTravelEstimate(destination);
  if (!estimate || estimate.error) {
    return {
      travel_minutes: null,
      travel_miles: null,
      travel_summary: (estimate == null ? void 0 : estimate.summary) || (estimate == null ? void 0 : estimate.error) || null
    };
  }
  return {
    travel_minutes: estimate.minutes,
    travel_miles: estimate.miles,
    travel_summary: estimate.summary
  };
}
async function ensureSeedDispatchOrders() {
  if (process.env.DISPATCH_SEED_EXAMPLE_ORDERS !== "true") {
    return;
  }
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).select("id", { count: "exact", head: false }).limit(1);
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  if ((data2 || []).length > 0) {
    return;
  }
  const { error: insertError } = await supabaseAdmin.from(ORDERS_TABLE).insert(
    seedDispatchOrders.map((order) => ({
      id: order.id,
      order_number: order.orderNumber || null,
      source: order.source,
      customer: order.customer,
      contact: order.contact,
      address: order.address,
      city: order.city,
      material: order.material,
      quantity: order.quantity,
      unit: order.unit,
      requested_window: order.requestedWindow,
      time_preference: order.timePreference || null,
      truck_preference: order.truckPreference || null,
      notes: order.notes,
      status: order.status,
      assigned_route_id: order.assignedRouteId || null,
      stop_sequence: order.assignedRouteId ? 1 : null,
      delivery_status: "not_started"
    }))
  );
  if (insertError) {
    throw new Error(formatSupabaseError(insertError));
  }
}
async function ensureSeedDispatchRoutes() {
  const { data: data2, error } = await supabaseAdmin.from(ROUTES_TABLE).select("id", { count: "exact", head: false }).limit(1);
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  if ((data2 || []).length > 0) {
    return;
  }
  const { error: insertError } = await supabaseAdmin.from(ROUTES_TABLE).insert(
    seedDispatchRoutes.map((route57) => ({
      id: route57.id,
      code: route57.code,
      truck_id: route57.truckId || null,
      truck: route57.truck,
      driver_id: route57.driverId || null,
      driver: route57.driver,
      helper_id: route57.helperId || null,
      helper: route57.helper,
      color: route57.color,
      shift: route57.shift,
      region: route57.region,
      is_active: route57.isActive !== false
    }))
  );
  if (insertError) {
    throw new Error(formatSupabaseError(insertError));
  }
}
async function ensureSeedDispatchTrucks() {
  const { data: data2, error } = await supabaseAdmin.from(TRUCKS_TABLE).select("id", { count: "exact", head: false }).limit(1);
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  if ((data2 || []).length > 0) return;
  const { error: insertError } = await supabaseAdmin.from(TRUCKS_TABLE).insert(
    seedDispatchTrucks.map((truck) => ({
      id: truck.id,
      label: truck.label,
      truck_type: truck.truckType,
      capacity: truck.capacity || "",
      tons: truck.tons || null,
      yards: truck.yards || null,
      license_plate: truck.licensePlate || null,
      is_active: truck.isActive !== false
    }))
  );
  if (insertError) {
    throw new Error(formatSupabaseError(insertError));
  }
}
async function ensureSeedDispatchEmployees() {
  const { data: data2, error } = await supabaseAdmin.from(EMPLOYEES_TABLE).select("id", { count: "exact", head: false }).limit(1);
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  if ((data2 || []).length > 0) return;
  const { error: insertError } = await supabaseAdmin.from(EMPLOYEES_TABLE).insert(
    seedDispatchEmployees.map((employee) => ({
      id: employee.id,
      name: employee.name,
      role: employee.role,
      phone: employee.phone || null,
      email: employee.email || null,
      is_active: employee.isActive !== false
    }))
  );
  if (insertError) {
    throw new Error(formatSupabaseError(insertError));
  }
}
async function getDispatchOrders(options = {}) {
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).select(options.lightweight ? DISPATCH_ORDER_LIGHT_COLUMNS : "*").order("created_at", { ascending: false });
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return (data2 || []).map(normalizeOrder);
}
async function getLatestDispatchDriverLocations() {
  const { data: data2, error } = await supabaseAdmin.from(DRIVER_LOCATIONS_TABLE).select("*").order("captured_at", { ascending: false });
  if (error) {
    if (error.code === "42P01" || /does not exist|schema cache/i.test(error.message || "")) {
      console.warn("[DISPATCH DRIVER LOCATION STORAGE MISSING]", formatSupabaseError(error));
      return [];
    }
    throw new Error(formatSupabaseError(error));
  }
  const seen = /* @__PURE__ */ new Set();
  return (data2 || []).map(normalizeDriverLocation).filter((location) => {
    const key = location.routeId || location.driverId || location.driverName || location.id;
    if (seen.has(key)) return false;
    seen.add(key);
    return Number.isFinite(location.latitude) && Number.isFinite(location.longitude);
  });
}
async function upsertDispatchDriverLocation(input) {
  const routeId = input.routeId || "unassigned";
  const payload = {
    id: routeId,
    route_id: input.routeId || null,
    order_id: input.orderId || null,
    driver_id: input.driverId || null,
    driver_name: input.driverName || "",
    truck: input.truck || "",
    latitude: input.latitude,
    longitude: input.longitude,
    accuracy: input.accuracy ?? null,
    heading: input.heading ?? null,
    speed: input.speed ?? null,
    captured_at: input.capturedAt || (/* @__PURE__ */ new Date()).toISOString(),
    updated_at: (/* @__PURE__ */ new Date()).toISOString()
  };
  const { data: data2, error } = await supabaseAdmin.from(DRIVER_LOCATIONS_TABLE).upsert(payload, { onConflict: "id" }).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeDriverLocation(data2);
}
async function getDispatchOrderByMailboxMessageId(messageId) {
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).select("*").eq("mailbox_message_id", messageId).maybeSingle();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return data2 ? normalizeOrder(data2) : null;
}
async function getDispatchOrdersByOrderNumber(orderNumber) {
  const baseOrderNumber = cleanOrderNumber$2(orderNumber);
  if (!baseOrderNumber) return [];
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).select("*").ilike("order_number", `${baseOrderNumber}%`).order("created_at", { ascending: false });
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  const orderNumberPattern = new RegExp(`^${baseOrderNumber.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}[a-z]?$`, "i");
  return (data2 || []).map(normalizeOrder).filter((order) => order.orderNumber && orderNumberPattern.test(order.orderNumber));
}
async function getDispatchRoutes() {
  const { data: data2, error } = await supabaseAdmin.from(ROUTES_TABLE).select("*").eq("is_active", true).order("created_at", { ascending: true });
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return (data2 || []).map(normalizeRoute);
}
function getDispatchLocalDate() {
  const timeZone = process.env.DISPATCH_RESET_TIMEZONE || "America/Chicago";
  return new Intl.DateTimeFormat("en-CA", {
    timeZone,
    year: "numeric",
    month: "2-digit",
    day: "2-digit"
  }).format(/* @__PURE__ */ new Date());
}
async function resetDispatchRoutesForNewDay() {
  const today = getDispatchLocalDate();
  const settingKey = "last_daily_route_reset";
  const { data: setting, error: settingError } = await supabaseAdmin.from(SETTINGS_TABLE).select("value").eq("key", settingKey).maybeSingle();
  if (settingError) {
    console.error("[DISPATCH DAILY RESET SKIPPED]", formatSupabaseError(settingError));
    return { reset: false, date: today };
  }
  if ((setting == null ? void 0 : setting.value) === today) {
    return { reset: false, date: today };
  }
  const hasPreviousResetDate = Boolean(setting == null ? void 0 : setting.value);
  const { error: upsertError } = await supabaseAdmin.from(SETTINGS_TABLE).upsert({
    key: settingKey,
    value: today,
    updated_at: (/* @__PURE__ */ new Date()).toISOString()
  });
  if (upsertError) {
    console.error("[DISPATCH DAILY RESET SKIPPED]", formatSupabaseError(upsertError));
    return { reset: false, date: today };
  }
  if (!hasPreviousResetDate) {
    return { reset: false, date: today };
  }
  const { error: ordersError } = await supabaseAdmin.from(ORDERS_TABLE).update({
    status: "new",
    assigned_route_id: null,
    stop_sequence: null,
    eta: null,
    delivery_status: "not_started",
    arrived_at: null,
    departed_at: null
  }).neq("status", "delivered");
  if (ordersError) {
    throw new Error(formatSupabaseError(ordersError));
  }
  const { error: routesError } = await supabaseAdmin.from(ROUTES_TABLE).update({ is_active: false }).eq("is_active", true);
  if (routesError) {
    throw new Error(formatSupabaseError(routesError));
  }
  return { reset: true, date: today };
}
async function getDispatchTrucks() {
  const { data: data2, error } = await supabaseAdmin.from(TRUCKS_TABLE).select("*").eq("is_active", true).order("label", { ascending: true });
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return (data2 || []).map(normalizeTruck);
}
async function getDispatchEmployees() {
  const { data: data2, error } = await supabaseAdmin.from(EMPLOYEES_TABLE).select("*").eq("is_active", true).order("name", { ascending: true });
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return (data2 || []).map(normalizeEmployee);
}
async function createDispatchOrder(input) {
  const id = `D-${Date.now().toString().slice(-6)}${Math.random().toString(36).slice(2, 4).toUpperCase()}`;
  const travelPayload = await buildDispatchTravelPayload(input.address, input.city);
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).insert({
    id,
    order_number: input.orderNumber || null,
    source: input.source,
    customer: input.customer,
    contact: input.contact || "",
    address: input.address,
    city: input.city || "",
    material: input.material,
    quantity: input.quantity || "",
    unit: input.unit || "Ton",
    requested_window: input.requestedWindow || "Needs scheduling",
    time_preference: input.timePreference || null,
    truck_preference: input.truckPreference || null,
    notes: input.notes || "",
    email_subject: input.emailSubject || null,
    raw_email: input.rawEmail || null,
    mailbox_message_id: input.mailboxMessageId || null,
    status: "new",
    assigned_route_id: null,
    stop_sequence: null,
    delivery_status: "not_started",
    ...travelPayload
  }).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeOrder(data2);
}
async function getNextRouteStopSequence(routeId) {
  var _a2;
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).select("stop_sequence").eq("assigned_route_id", routeId).not("stop_sequence", "is", null).order("stop_sequence", { ascending: false }).limit(1);
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  const currentMax = Number(((_a2 = data2 == null ? void 0 : data2[0]) == null ? void 0 : _a2.stop_sequence) || 0);
  return currentMax + 1;
}
async function createDispatchRoute(input) {
  const id = `route-${Date.now().toString(36)}`;
  const { data: data2, error } = await supabaseAdmin.from(ROUTES_TABLE).insert({
    id,
    code: input.code,
    truck_id: input.truckId || null,
    truck: input.truck,
    driver_id: input.driverId || null,
    driver: input.driver,
    helper_id: input.helperId || null,
    helper: input.helper || "",
    color: input.color || "#38bdf8",
    shift: input.shift || "",
    region: input.region || "",
    is_active: true
  }).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeRoute(data2);
}
async function updateDispatchRoute(id, patch) {
  const payload = {};
  if (patch.code !== void 0) payload.code = patch.code;
  if (patch.truckId !== void 0) payload.truck_id = patch.truckId;
  if (patch.truck !== void 0) payload.truck = patch.truck;
  if (patch.driverId !== void 0) payload.driver_id = patch.driverId;
  if (patch.driver !== void 0) payload.driver = patch.driver;
  if (patch.helperId !== void 0) payload.helper_id = patch.helperId;
  if (patch.helper !== void 0) payload.helper = patch.helper;
  if (patch.color !== void 0) payload.color = patch.color;
  if (patch.shift !== void 0) payload.shift = patch.shift;
  if (patch.region !== void 0) payload.region = patch.region;
  const { data: data2, error } = await supabaseAdmin.from(ROUTES_TABLE).update(payload).eq("id", id).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeRoute(data2);
}
async function deleteDispatchRoute(id) {
  const { data: data2, error } = await supabaseAdmin.from(ROUTES_TABLE).update({ is_active: false }).eq("id", id).select("id");
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  if (!(data2 == null ? void 0 : data2.length)) {
    throw new Error(`No dispatch route found for ${id}`);
  }
}
async function createDispatchTruck(input) {
  const id = `truck-${Date.now().toString(36)}`;
  const { data: data2, error } = await supabaseAdmin.from(TRUCKS_TABLE).insert({
    id,
    label: input.label,
    truck_type: input.truckType || "",
    capacity: "",
    tons: input.tons || null,
    yards: input.yards || null,
    license_plate: input.licensePlate || null,
    is_active: true
  }).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeTruck(data2);
}
async function updateDispatchTruck(id, patch) {
  const payload = {};
  if (patch.label !== void 0) payload.label = patch.label;
  if (patch.truckType !== void 0) payload.truck_type = patch.truckType;
  if (patch.tons !== void 0) payload.tons = patch.tons;
  if (patch.yards !== void 0) payload.yards = patch.yards;
  if (patch.licensePlate !== void 0) payload.license_plate = patch.licensePlate;
  const { data: data2, error } = await supabaseAdmin.from(TRUCKS_TABLE).update(payload).eq("id", id).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeTruck(data2);
}
async function deleteDispatchTruck(id) {
  const { data: data2, error } = await supabaseAdmin.from(TRUCKS_TABLE).update({ is_active: false }).eq("id", id).select("id");
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  if (!(data2 == null ? void 0 : data2.length)) {
    throw new Error(`No dispatch truck found for ${id}`);
  }
}
async function createDispatchEmployee(input) {
  const id = `employee-${Date.now().toString(36)}`;
  const { data: data2, error } = await supabaseAdmin.from(EMPLOYEES_TABLE).insert({
    id,
    name: input.name,
    role: input.role,
    phone: input.phone || null,
    email: input.email || null,
    is_active: true
  }).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeEmployee(data2);
}
async function updateDispatchEmployee(id, patch) {
  const payload = {};
  if (patch.name !== void 0) payload.name = patch.name;
  if (patch.role !== void 0) payload.role = patch.role;
  if (patch.phone !== void 0) payload.phone = patch.phone;
  if (patch.email !== void 0) payload.email = patch.email;
  const { data: data2, error } = await supabaseAdmin.from(EMPLOYEES_TABLE).update(payload).eq("id", id).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeEmployee(data2);
}
async function deleteDispatchEmployee(id) {
  const { data: data2, error } = await supabaseAdmin.from(EMPLOYEES_TABLE).update({ is_active: false }).eq("id", id).select("id");
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  if (!(data2 == null ? void 0 : data2.length)) {
    throw new Error(`No dispatch employee found for ${id}`);
  }
}
async function updateDispatchOrder(id, patch) {
  const payload = {};
  if (patch.orderNumber !== void 0) payload.order_number = patch.orderNumber;
  if (patch.quantity !== void 0) payload.quantity = patch.quantity;
  if (patch.unit !== void 0) payload.unit = patch.unit;
  if (patch.notes !== void 0) payload.notes = patch.notes;
  if (patch.status) payload.status = patch.status;
  if (patch.assignedRouteId !== void 0) {
    payload.assigned_route_id = patch.assignedRouteId;
  }
  if (patch.stopSequence !== void 0) {
    payload.stop_sequence = patch.stopSequence;
  }
  if (patch.deliveryStatus) payload.delivery_status = patch.deliveryStatus;
  if (patch.eta !== void 0) payload.eta = patch.eta;
  if (patch.arrivedAt !== void 0) payload.arrived_at = patch.arrivedAt;
  if (patch.departedAt !== void 0) payload.departed_at = patch.departedAt;
  if (patch.deliveredAt !== void 0) payload.delivered_at = patch.deliveredAt;
  if (patch.proofName !== void 0) payload.proof_name = patch.proofName;
  if (patch.proofNotes !== void 0) payload.proof_notes = patch.proofNotes;
  if (patch.emailSubject !== void 0) payload.email_subject = patch.emailSubject;
  if (patch.rawEmail !== void 0) payload.raw_email = patch.rawEmail;
  if (patch.mailboxMessageId !== void 0) payload.mailbox_message_id = patch.mailboxMessageId;
  if (patch.signatureName !== void 0) payload.signature_name = patch.signatureName;
  if (patch.signatureData !== void 0) payload.signature_data = patch.signatureData;
  if (patch.photoUrls !== void 0) payload.photo_urls = patch.photoUrls;
  if (patch.ticketNumbers !== void 0) payload.ticket_numbers = patch.ticketNumbers;
  if (patch.inspectionStatus !== void 0) payload.inspection_status = patch.inspectionStatus;
  if (patch.checklistJson !== void 0) payload.checklist_json = patch.checklistJson;
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).update(payload).eq("id", id).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeOrder(data2);
}
async function updateDispatchOrderDetails(id, patch) {
  const payload = {};
  if (patch.orderNumber !== void 0) payload.order_number = patch.orderNumber;
  if (patch.customer !== void 0) payload.customer = patch.customer;
  if (patch.contact !== void 0) payload.contact = patch.contact;
  if (patch.address !== void 0) payload.address = patch.address;
  if (patch.city !== void 0) payload.city = patch.city;
  if (patch.material !== void 0) payload.material = patch.material;
  if (patch.quantity !== void 0) payload.quantity = patch.quantity;
  if (patch.unit !== void 0) payload.unit = patch.unit;
  if (patch.requestedWindow !== void 0) {
    payload.requested_window = patch.requestedWindow;
  }
  if (patch.timePreference !== void 0) {
    payload.time_preference = patch.timePreference;
  }
  if (patch.truckPreference !== void 0) {
    payload.truck_preference = patch.truckPreference;
  }
  if (patch.notes !== void 0) payload.notes = patch.notes;
  if (patch.status !== void 0) payload.status = patch.status;
  if (patch.address !== void 0 || patch.city !== void 0) {
    const { data: current, error: currentError } = await supabaseAdmin.from(ORDERS_TABLE).select("address, city").eq("id", id).maybeSingle();
    if (currentError) {
      throw new Error(formatSupabaseError(currentError));
    }
    Object.assign(
      payload,
      await buildDispatchTravelPayload(
        patch.address !== void 0 ? patch.address : current == null ? void 0 : current.address,
        patch.city !== void 0 ? patch.city : current == null ? void 0 : current.city
      )
    );
  }
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).update(payload).eq("id", id).select("*").single();
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  return normalizeOrder(data2);
}
async function deleteDispatchOrder(id) {
  const { data: data2, error } = await supabaseAdmin.from(ORDERS_TABLE).delete().eq("id", id).select("id");
  if (error) {
    throw new Error(formatSupabaseError(error));
  }
  if (!(data2 == null ? void 0 : data2.length)) {
    throw new Error(`No dispatch order found for ${id}`);
  }
}
let lastAutoPollAt = 0;
const DEFAULT_ORDER_SUBJECT_PREFIX = "You've Got A New Order: #";
const MAILBOX_IMPORT_VERSION = "new-orders-phone-only-v1";
function getMailboxConfig() {
  const host = process.env.DISPATCH_MAILBOX_HOST || "";
  const user = process.env.DISPATCH_MAILBOX_USER || "";
  const password = process.env.DISPATCH_MAILBOX_PASSWORD || "";
  if (!host || !user || !password) return null;
  return {
    host,
    port: Number(process.env.DISPATCH_MAILBOX_PORT || 993),
    user,
    password,
    mailbox: process.env.DISPATCH_MAILBOX_NAME || "INBOX",
    limit: Number(process.env.DISPATCH_MAILBOX_LIMIT || 10),
    backfillLimit: Number(process.env.DISPATCH_MAILBOX_BACKFILL_LIMIT || 250),
    markSeen: process.env.DISPATCH_MAILBOX_MARK_SEEN === "true",
    subjectPrefix: process.env.DISPATCH_MAILBOX_SUBJECT_PREFIX || DEFAULT_ORDER_SUBJECT_PREFIX
  };
}
function escapeImapString(value) {
  return `"${value.replace(/\\/g, "\\\\").replace(/"/g, '\\"')}"`;
}
function readHeader(raw, header) {
  const match = raw.match(new RegExp(`^${header}:\\s*(.+(?:\\r?\\n[\\t ].+)*)`, "im"));
  return ((match == null ? void 0 : match[1]) || "").replace(/\r?\n[\t ]+/g, " ").trim();
}
function getMessageBody(raw) {
  const split = raw.split(/\r?\n\r?\n/);
  return split.length > 1 ? split.slice(1).join("\n\n") : raw;
}
function decodeQuotedPrintable(raw) {
  return raw.replace(/=\r?\n/g, "").replace(/(?:=[0-9A-F]{2})+/gi, (encoded) => {
    var _a2;
    const bytes = (_a2 = encoded.match(/=([0-9A-F]{2})/gi)) == null ? void 0 : _a2.map((part) => parseInt(part.slice(1), 16));
    return bytes ? Buffer.from(bytes).toString("utf8") : encoded;
  });
}
function decodeTransferBody(body, encoding) {
  const normalizedEncoding = encoding.toLowerCase();
  if (normalizedEncoding.includes("base64")) {
    try {
      return Buffer.from(body.replace(/\s+/g, ""), "base64").toString("utf8");
    } catch {
      return body;
    }
  }
  if (normalizedEncoding.includes("quoted-printable")) {
    return decodeQuotedPrintable(body);
  }
  return body;
}
function readBoundary(contentType) {
  var _a2, _b;
  return ((_a2 = contentType.match(/boundary=(?:"([^"]+)"|([^;\s]+))/i)) == null ? void 0 : _a2[1]) || ((_b = contentType.match(/boundary=(?:"([^"]+)"|([^;\s]+))/i)) == null ? void 0 : _b[2]) || "";
}
function decodeMimeMessage(raw) {
  const contentType = readHeader(raw, "Content-Type");
  const transferEncoding = readHeader(raw, "Content-Transfer-Encoding");
  const body = getMessageBody(raw);
  const boundary2 = readBoundary(contentType);
  if (boundary2) {
    return body.split(new RegExp(`--${boundary2.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}(?:--)?\\r?\\n?`, "g")).map((part) => part.trim()).filter((part) => part && !part.startsWith("--")).map((part) => decodeMimeMessage(part)).filter(Boolean).join("\n\n");
  }
  if (/text\/(?:plain|html)/i.test(contentType) || !contentType) {
    return decodeTransferBody(body, transferEncoding);
  }
  return "";
}
class SimpleImapClient {
  constructor(config) {
    __publicField(this, "socket");
    __publicField(this, "buffer", "");
    __publicField(this, "tagCounter", 1);
    this.socket = tls.connect({
      host: config.host,
      port: config.port,
      servername: config.host
    });
  }
  async connect() {
    await this.readUntil((text) => /^\* OK/im.test(text));
  }
  async command(command) {
    const tag = `A${String(this.tagCounter++).padStart(4, "0")}`;
    this.socket.write(`${tag} ${command}\r
`);
    return this.readUntil((text) => new RegExp(`^${tag} (OK|NO|BAD)`, "im").test(text));
  }
  close() {
    this.socket.end();
  }
  readUntil(done) {
    return new Promise((resolve, reject) => {
      const onData = (chunk) => {
        this.buffer += chunk.toString("utf8");
        if (!done(this.buffer)) return;
        const output = this.buffer;
        this.buffer = "";
        cleanup();
        resolve(output);
      };
      const onError = (error) => {
        cleanup();
        reject(error);
      };
      const cleanup = () => {
        this.socket.off("data", onData);
        this.socket.off("error", onError);
      };
      this.socket.on("data", onData);
      this.socket.on("error", onError);
    });
  }
}
function parseSearchResponse(response) {
  var _a2;
  const line = ((_a2 = response.match(/^\* SEARCH\s+(.+)$/im)) == null ? void 0 : _a2[1]) || "";
  return line.split(/\s+/).map((uid) => uid.trim()).filter(Boolean);
}
function parseFetchResponse(uid, response) {
  var _a2;
  const literalMatch = response.match(/\{(\d+)\}\r?\n([\s\S]*)\r?\n\)/);
  const raw = (_a2 = literalMatch == null ? void 0 : literalMatch[2]) == null ? void 0 : _a2.trim();
  if (!raw) return null;
  const subject = readHeader(raw, "Subject");
  const messageId = readHeader(raw, "Message-ID") || `${uid}:${subject}`;
  const body = decodeMimeMessage(raw) || getMessageBody(raw);
  return {
    uid,
    messageId,
    subject,
    raw: `Subject: ${subject}
${body}`
  };
}
function summarizeSkipReasons(skipReasons) {
  const counts = skipReasons.reduce((summary, item) => {
    const summaryReason = item.reason.replace(/\s*\[order[\s\S]*$/, "");
    summary[summaryReason] = (summary[summaryReason] || 0) + 1;
    return summary;
  }, {});
  return Object.entries(counts).map(([reason, count]) => `${count} ${reason}`);
}
function suffixOrderNumber$2(orderNumber, index, total) {
  if (!orderNumber || total <= 1) return orderNumber;
  return `${orderNumber}${String.fromCharCode(97 + index)}`;
}
function suffixMailboxMessageId(messageId, index, total) {
  if (!messageId || total <= 1) return messageId;
  return `${messageId}#${String.fromCharCode(97 + index)}`;
}
function extractPhone$3(value) {
  const text = String(value || "").replace(/[\u00ad\u200B-\u200D\uFEFF]/g, " ");
  const candidates = [
    ...Array.from(text.matchAll(/(?:^|\D)(1?[2-9]\d{9})(?=\D|$)/g), (match) => match[1]),
    ...Array.from(
      text.matchAll(/(?:^|\D)(1?[\s.-]?\(?[2-9]\d{2}\)?[\s.-]+\d{3}[\s.-]+\d{4})(?=\D|$)/g),
      (match) => match[1]
    )
  ];
  for (const candidate of candidates) {
    const candidateDigits = candidate.replace(/\D/g, "");
    if (candidateDigits.length === 10) return candidateDigits;
    if (candidateDigits.length === 11 && candidateDigits.startsWith("1")) {
      return candidateDigits.slice(1);
    }
  }
  const digits = text.replace(/\D/g, "");
  if (digits.length === 10) return digits;
  if (digits.length === 11 && digits.startsWith("1")) return digits.slice(1);
  return "";
}
function formatPhone$1(phone) {
  const digits = extractPhone$3(phone);
  return digits ? `(${digits.slice(0, 3)}) ${digits.slice(3, 6)}-${digits.slice(6)}` : "";
}
function findPhoneInMailboxText(value) {
  const text = String(value || "").replace(/[\u00ad\u200B-\u200D\uFEFF]/g, " ");
  const candidates = [
    ...Array.from(text.matchAll(/(?:^|\D)(1?[2-9]\d{9})(?=\D|$)/g), (match) => match[1]),
    ...Array.from(
      text.matchAll(/(?:^|\D)(1?[\s.-]?\(?[2-9]\d{2}\)?[\s.-]+\d{3}[\s.-]+\d{4})(?=\D|$)/g),
      (match) => match[1]
    )
  ];
  for (const candidate of candidates) {
    const phone = extractPhone$3(candidate);
    if (phone) return phone;
  }
  return "";
}
function uniqueOrders(orders) {
  const seen = /* @__PURE__ */ new Set();
  return orders.filter((order) => {
    if (!order || seen.has(order.id)) return false;
    seen.add(order.id);
    return true;
  });
}
function compactDebugText(value) {
  return String(value || "").replace(/<style[\s\S]*?<\/style>/gi, " ").replace(/<script[\s\S]*?<\/script>/gi, " ").replace(/<br\s*\/?>/gi, "\n").replace(/<\/(p|div|tr|td|th|li)>/gi, "\n").replace(/<[^>]+>/g, " ").replace(/&nbsp;/gi, " ").replace(/&amp;/gi, "&").replace(/[\u00ad\u200B-\u200D\uFEFF]/g, "").replace(/\r/g, "").replace(/[ \t]+/g, " ").replace(/\n\s+/g, "\n").replace(/\n{2,}/g, "\n").trim();
}
function mailboxDebugExcerpt(raw) {
  const text = compactDebugText(raw);
  const lines = text.split("\n").map((line) => line.trim()).filter(Boolean);
  const start = lines.findIndex(
    (line) => /billing\s+address|shipping\s+address|@[A-Z0-9.-]+\.[A-Z]{2,}/i.test(line)
  );
  const excerpt = (start >= 0 ? lines.slice(start, start + 10) : lines.slice(-12)).join(" | ").replace(/\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi, "[email]").slice(0, 260);
  return excerpt || "(no readable body excerpt)";
}
function extractMailboxPhone(raw) {
  const text = compactDebugText(raw);
  const lines = text.split("\n").map((line) => line.trim()).filter(Boolean);
  const debugExcerptPhone = findPhoneInMailboxText(mailboxDebugExcerpt(raw));
  if (debugExcerptPhone) return debugExcerptPhone;
  for (const [index, line] of lines.entries()) {
    if (!/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/i.test(line)) continue;
    const nearbyBlock = lines.slice(Math.max(0, index - 6), index + 3).join(" ");
    const phone = findPhoneInMailboxText(nearbyBlock);
    if (phone) return phone;
  }
  const addressStart = lines.findIndex((line) => /^(billing|shipping)\s+address\b/i.test(line));
  if (addressStart >= 0) {
    const phone = findPhoneInMailboxText(lines.slice(addressStart, addressStart + 18).join(" "));
    if (phone) return phone;
  }
  return findPhoneInMailboxText(text);
}
async function fetchOrderEmails(config) {
  const client = new SimpleImapClient(config);
  const emails = [];
  await client.connect();
  await client.command(`LOGIN ${escapeImapString(config.user)} ${escapeImapString(config.password)}`);
  await client.command(`SELECT ${escapeImapString(config.mailbox)}`);
  const searchResponse = await client.command(
    `UID SEARCH SUBJECT ${escapeImapString(config.subjectPrefix)}`
  );
  const uids = parseSearchResponse(searchResponse).slice(
    -config.limit
  );
  for (const uid of uids) {
    const fetchCommand = config.markSeen ? `UID FETCH ${uid} BODY[]` : `UID FETCH ${uid} BODY.PEEK[]`;
    const fetchResponse = await client.command(fetchCommand);
    const email = parseFetchResponse(uid, fetchResponse);
    if (email) emails.push(email);
  }
  await client.command("LOGOUT").catch(() => "");
  client.close();
  return emails;
}
async function pollDispatchMailbox() {
  var _a2;
  const config = getMailboxConfig();
  if (!config) {
    return {
      configured: false,
      imported: 0,
      skipped: 0,
      skipReasons: [],
      skipSummary: [],
      message: "Mailbox polling is not configured yet."
    };
  }
  const emails = await fetchOrderEmails(config);
  let imported = 0;
  const skipReasons = [];
  for (const email of emails) {
    if (!email.subject.startsWith(config.subjectPrefix)) {
      skipReasons.push({
        uid: email.uid,
        subject: email.subject || "(No subject)",
        reason: `ignored because subject does not start with "${config.subjectPrefix}"`
      });
      continue;
    }
    const parsed = parseDispatchEmail(email.raw);
    const mailboxPhone = extractMailboxPhone(email.raw);
    const parsedContact = extractPhone$3(parsed.contact) || !mailboxPhone ? parsed.contact : [parsed.contact, formatPhone$1(mailboxPhone)].filter(Boolean).join(" / ");
    const products = ((_a2 = parsed.products) == null ? void 0 : _a2.length) ? parsed.products : [{ material: parsed.material, quantity: parsed.quantity }];
    const validProducts = products.filter((product) => product.material);
    const messageIdOrders = await Promise.all(
      (validProducts.length ? validProducts : [{ material: "", quantity: "" }]).map(
        (_, index, list) => getDispatchOrderByMailboxMessageId(
          suffixMailboxMessageId(email.messageId, index, list.length)
        )
      )
    );
    const legacyExisting = messageIdOrders.some(Boolean) ? null : await getDispatchOrderByMailboxMessageId(email.messageId) || await getDispatchOrderByMailboxMessageId(`${email.messageId}#a`);
    const orderNumberExisting = messageIdOrders.some(Boolean) || legacyExisting || !parsed.orderNumber ? [] : await getDispatchOrdersByOrderNumber(parsed.orderNumber);
    const importedOrders = uniqueOrders([
      ...messageIdOrders,
      legacyExisting,
      ...orderNumberExisting
    ]);
    if (importedOrders.length) {
      skipReasons.push({
        uid: email.uid,
        subject: email.subject || "(No subject)",
        reason: "skipped because it was already imported"
      });
      continue;
    }
    if (!parsed.address || products.every((product) => !product.material)) {
      const missing = [
        !parsed.address ? "address" : "",
        products.every((product) => !product.material) ? "material" : ""
      ].filter(Boolean);
      skipReasons.push({
        uid: email.uid,
        subject: email.subject || "(No subject)",
        reason: `skipped because it is missing ${missing.join(" and ")}`
      });
      continue;
    }
    for (const [index, product] of validProducts.entries()) {
      await createDispatchOrder({
        source: "email",
        orderNumber: suffixOrderNumber$2(parsed.orderNumber, index, validProducts.length),
        customer: parsed.customer,
        contact: parsedContact,
        address: parsed.address,
        city: parsed.city,
        material: product.material,
        quantity: product.quantity,
        unit: await getDispatchUnitForMaterial(product.material) || parsed.unit,
        requestedWindow: parsed.requestedWindow,
        timePreference: parsed.timePreference,
        truckPreference: parsed.truckPreference,
        notes: parsed.notes || "Imported from mailbox.",
        emailSubject: parsed.subject || email.subject,
        rawEmail: email.raw,
        mailboxMessageId: suffixMailboxMessageId(
          email.messageId,
          index,
          validProducts.length
        )
      });
      imported += 1;
    }
  }
  const skipSummary = summarizeSkipReasons(skipReasons);
  return {
    configured: true,
    imported,
    updated: 0,
    skipped: skipReasons.length,
    skipReasons,
    skipSummary,
    message: `Mailbox poll ${MAILBOX_IMPORT_VERSION} complete: ${imported} imported, 0 updated, ${skipReasons.length} skipped${skipSummary.length ? ` (${skipSummary.join("; ")})` : ""}.`
  };
}
async function maybeAutoPollDispatchMailbox() {
  if (process.env.DISPATCH_MAILBOX_AUTO_POLL !== "true") return null;
  if (process.env.DISPATCH_MAILBOX_AUTO_POLL_ON_LOAD !== "true") return null;
  const intervalSeconds = Number(process.env.DISPATCH_MAILBOX_POLL_SECONDS || 300);
  const now = Date.now();
  if (now - lastAutoPollAt < intervalSeconds * 1e3) return null;
  lastAutoPollAt = now;
  return pollDispatchMailbox();
}
function normalizeWhitespace(value) {
  return value.replace(/\s+/g, " ").trim();
}
function normalizeLookupKey(value) {
  return String(value || "").toLowerCase().replace(/[_-]+/g, " ").replace(/[^a-z0-9\s]/g, " ").replace(/\s+/g, " ").trim();
}
function cleanOrderNumber$1(value) {
  return String(value || "").replace(/^#/, "").trim();
}
function suffixOrderNumber$1(orderNumber, index, total) {
  if (!orderNumber || total <= 1) return orderNumber;
  return `${orderNumber}${String.fromCharCode(97 + index)}`;
}
function getAddressLine(address) {
  return normalizeWhitespace(
    [address == null ? void 0 : address.address1, address == null ? void 0 : address.address2].filter(Boolean).join(" ")
  );
}
function getCityLine(address) {
  return normalizeWhitespace(
    [address == null ? void 0 : address.city, address == null ? void 0 : address.provinceCode, address == null ? void 0 : address.zip].filter(Boolean).join(" ")
  );
}
function getCustomerName(order) {
  var _a2, _b, _c, _d;
  return (((_a2 = order.shippingAddress) == null ? void 0 : _a2.name) || ((_b = order.customer) == null ? void 0 : _b.displayName) || [(_c = order.customer) == null ? void 0 : _c.firstName, (_d = order.customer) == null ? void 0 : _d.lastName].filter(Boolean).join(" ") || order.email || order.phone || "Shopify Customer").trim();
}
function getContact(order) {
  var _a2, _b, _c, _d;
  const email = order.email || ((_a2 = order.customer) == null ? void 0 : _a2.email) || "";
  const phone = ((_b = order.shippingAddress) == null ? void 0 : _b.phone) || ((_c = order.billingAddress) == null ? void 0 : _c.phone) || ((_d = order.customer) == null ? void 0 : _d.phone) || order.phone || "";
  return [email, phone].filter(Boolean).join(" / ");
}
function getAttributeText(attributes) {
  return (attributes || []).map(
    (attribute) => [attribute.key, attribute.value].filter(Boolean).join(": ")
  ).filter(Boolean).join("\n");
}
function getMetafieldText(metafields) {
  return ((metafields == null ? void 0 : metafields.nodes) || []).map(
    (metafield) => [metafield.namespace, metafield.key, metafield.value].filter(Boolean).join(": ")
  ).filter(Boolean).join("\n");
}
function findOrderMetafieldValue(order, matcher) {
  var _a2;
  const match = (((_a2 = order.metafields) == null ? void 0 : _a2.nodes) || []).find((metafield) => {
    const lookupKey = normalizeLookupKey(
      [metafield.namespace, metafield.key].filter(Boolean).join(" ")
    );
    return matcher(lookupKey, metafield);
  });
  return normalizeWhitespace(String((match == null ? void 0 : match.value) || ""));
}
function getPreferredDeliveryMetafield(order) {
  return findOrderMetafieldValue(order, (lookupKey) => {
    const hasDelivery = /\bdelivery\b/.test(lookupKey);
    const hasPickup = /\bpick\s*up\b|\bpickup\b/.test(lookupKey);
    const hasPreferred = /\bpreferred\b|\bpreference\b|\brequested\b/.test(lookupKey);
    return hasPreferred && (hasDelivery || hasPickup) || /\bpreferred delivery\b/.test(lookupKey) || /\bdelivery pick up\b/.test(lookupKey) || /\bpick up date\b/.test(lookupKey);
  });
}
function getOrderNoteMetafield(order) {
  return findOrderMetafieldValue(order, (lookupKey) => {
    const hasNote = /\bnote\b|\binstruction\b|\bdrop\b/.test(lookupKey);
    const hasOrder = /\border\b/.test(lookupKey);
    return hasOrder && hasNote || /\border note\b/.test(lookupKey) || /\bdelivery note\b/.test(lookupKey) || /\bdelivery instruction\b/.test(lookupKey);
  });
}
function getLineMaterial(lineItem) {
  var _a2, _b, _c;
  const productTitle = String(((_b = (_a2 = lineItem.variant) == null ? void 0 : _a2.product) == null ? void 0 : _b.title) || "").trim();
  const title = String(lineItem.title || "").trim();
  const name = String(lineItem.name || "").trim();
  const variantTitle = String(((_c = lineItem.variant) == null ? void 0 : _c.title) || "").trim();
  const base = productTitle || title || name || "Shopify Product";
  if (!variantTitle || /^default title$/i.test(variantTitle)) return base;
  if (base.toLowerCase().includes(variantTitle.toLowerCase())) return base;
  return `${base} - ${variantTitle}`;
}
function shouldSkipLineItem(material) {
  return /\b(delivery|shipping|tax|fee|discount)\b/i.test(material);
}
function getLineNotes(order, lineItem) {
  var _a2, _b, _c, _d, _e, _f, _g;
  const orderNote = getOrderNoteMetafield(order) || normalizeWhitespace(order.note || "");
  if (orderNote) return orderNote;
  const parts = [
    `Imported from Shopify order ${order.name || order.legacyResourceId || order.id}.`,
    ((_a2 = order.tags) == null ? void 0 : _a2.length) ? `Tags: ${order.tags.join(", ")}` : "",
    lineItem.sku || ((_b = lineItem.variant) == null ? void 0 : _b.sku) ? `SKU: ${lineItem.sku || ((_c = lineItem.variant) == null ? void 0 : _c.sku)}` : "",
    lineItem.vendor || ((_e = (_d = lineItem.variant) == null ? void 0 : _d.product) == null ? void 0 : _e.vendor) ? `Vendor: ${lineItem.vendor || ((_g = (_f = lineItem.variant) == null ? void 0 : _f.product) == null ? void 0 : _g.vendor)}` : "",
    getAttributeText(order.customAttributes),
    getMetafieldText(order.metafields),
    getAttributeText(lineItem.customAttributes)
  ];
  return parts.filter(Boolean).join("\n");
}
function parseRequestedDateFromText(text) {
  const labeledPatterns = [
    /(?:delivery|pickup|requested|preference|window|date)[^:\n]*:\s*([0-1]?\d[/-][0-3]?\d[/-](?:20)?\d{2})/i,
    /(?:delivery|pickup|requested|preference|window|date)[^:\n]*:\s*(\d{4}-\d{2}-\d{2})/i
  ];
  for (const pattern of labeledPatterns) {
    const match = text.match(pattern);
    if (match == null ? void 0 : match[1]) return normalizeDate(match[1]);
  }
  const looseDate = text.match(/\b([0-1]?\d[/-][0-3]?\d[/-](?:20)?\d{2})\b/);
  if (looseDate == null ? void 0 : looseDate[1]) return normalizeDate(looseDate[1]);
  const isoDate = text.match(/\b(\d{4}-\d{2}-\d{2})\b/);
  if (isoDate == null ? void 0 : isoDate[1]) return normalizeDate(isoDate[1]);
  return "";
}
function normalizeDate(value) {
  const trimmed = value.trim();
  if (/^\d{4}-\d{2}-\d{2}$/.test(trimmed)) {
    const [year2, month2, day2] = trimmed.split("-");
    return `${month2}/${day2}/${year2}`;
  }
  const monthNameMatch = trimmed.match(
    /^(jan(?:uary)?|feb(?:ruary)?|mar(?:ch)?|apr(?:il)?|may|jun(?:e)?|jul(?:y)?|aug(?:ust)?|sep(?:t(?:ember)?)?|oct(?:ober)?|nov(?:ember)?|dec(?:ember)?)\.?\s+([0-3]?\d),?\s+((?:20)?\d{2})$/i
  );
  if (monthNameMatch) {
    const monthIndex = [
      "jan",
      "feb",
      "mar",
      "apr",
      "may",
      "jun",
      "jul",
      "aug",
      "sep",
      "oct",
      "nov",
      "dec"
    ].findIndex(
      (month3) => monthNameMatch[1].toLowerCase().startsWith(month3)
    );
    const month2 = String(monthIndex + 1).padStart(2, "0");
    const day2 = monthNameMatch[2].padStart(2, "0");
    const year2 = monthNameMatch[3].length === 2 ? `20${monthNameMatch[3]}` : monthNameMatch[3];
    if (monthIndex >= 0) return `${month2}/${day2}/${year2}`;
  }
  const match = trimmed.match(/^([0-1]?\d)[/-]([0-3]?\d)[/-]((?:20)?\d{2})$/);
  if (!match) return trimmed;
  const month = match[1].padStart(2, "0");
  const day = match[2].padStart(2, "0");
  const year = match[3].length === 2 ? `20${match[3]}` : match[3];
  return `${month}/${day}/${year}`;
}
function getRequestedWindow(order, lineItem) {
  var _a2;
  const preferredDelivery = getPreferredDeliveryMetafield(order);
  if (preferredDelivery) {
    return normalizeDate(preferredDelivery);
  }
  const text = [
    order.note,
    (_a2 = order.tags) == null ? void 0 : _a2.join(" "),
    getAttributeText(order.customAttributes),
    getMetafieldText(order.metafields),
    getAttributeText(lineItem.customAttributes)
  ].filter(Boolean).join("\n");
  return parseRequestedDateFromText(text) || "Needs scheduling";
}
function getOrderText(order, lineItem) {
  var _a2;
  return [
    order.note,
    (_a2 = order.tags) == null ? void 0 : _a2.join(" "),
    getAttributeText(order.customAttributes),
    getMetafieldText(order.metafields),
    getAttributeText(lineItem.customAttributes)
  ].filter(Boolean).join("\n");
}
async function fetchShopifyOrders(shop, limit, query) {
  var _a2, _b;
  const { admin } = await shopify.unauthenticated.admin(shop);
  const response = await admin.graphql(
    `#graphql
      query DispatchShopifyOrders($first: Int!, $query: String!) {
        orders(first: $first, query: $query, sortKey: CREATED_AT, reverse: true) {
          nodes {
            id
            name
            legacyResourceId
            createdAt
            displayFulfillmentStatus
            displayFinancialStatus
            note
            tags
            email
            phone
            customAttributes {
              key
              value
            }
            metafields(first: 25) {
              nodes {
                namespace
                key
                value
                type
              }
            }
            customer {
              displayName
              email
              phone
              firstName
              lastName
            }
            shippingAddress {
              name
              address1
              address2
              city
              provinceCode
              zip
              phone
            }
            billingAddress {
              phone
            }
            lineItems(first: 50) {
              nodes {
                id
                title
                name
                quantity
                sku
                vendor
                customAttributes {
                  key
                  value
                }
                variant {
                  id
                  title
                  sku
                  product {
                    title
                    vendor
                  }
                }
              }
            }
          }
        }
      }
    `,
    { variables: { first: limit, query } }
  );
  const json = await response.json();
  const errors = (json == null ? void 0 : json.errors) || [];
  if (errors.length) {
    const message = errors.map((error) => error.message || "Shopify order query failed.").join("; ");
    if (/access denied/i.test(message) && /\border(s)?\b/i.test(message)) {
      throw new Error(
        "Shopify denied access to orders. Add read_orders to the contractor app SCOPES/access_scopes, redeploy the Shopify app config, then reinstall or reauthorize the app in Shopify so the offline token gets the new scope."
      );
    }
    throw new Error(message);
  }
  return ((_b = (_a2 = json == null ? void 0 : json.data) == null ? void 0 : _a2.orders) == null ? void 0 : _b.nodes) || [];
}
async function importDispatchShopifyOrders() {
  var _a2;
  const shop = process.env.SHOPIFY_STORE_DOMAIN || "";
  const limit = Math.max(
    1,
    Math.min(100, Number(process.env.DISPATCH_SHOPIFY_IMPORT_LIMIT || 25) || 25)
  );
  const query = process.env.DISPATCH_SHOPIFY_ORDER_QUERY || "status:open fulfillment_status:unfulfilled";
  if (!shop) {
    return {
      configured: false,
      imported: 0,
      skipped: 0,
      message: "Shopify import is not configured. Set SHOPIFY_STORE_DOMAIN.",
      skipReasons: []
    };
  }
  const orders = await fetchShopifyOrders(shop, limit, query);
  let imported = 0;
  let skipped = 0;
  const skipReasons = [];
  for (const order of orders) {
    const lineItems = ((_a2 = order.lineItems) == null ? void 0 : _a2.nodes) || [];
    const validLineItems = lineItems.filter((lineItem) => {
      const material = getLineMaterial(lineItem);
      return !shouldSkipLineItem(material) && Number(lineItem.quantity || 0) > 0;
    });
    if (!validLineItems.length) {
      skipped += 1;
      skipReasons.push(`${order.name || order.id}: no shippable material lines found.`);
      continue;
    }
    const address = getAddressLine(order.shippingAddress);
    if (!address) {
      skipped += validLineItems.length;
      skipReasons.push(`${order.name || order.id}: missing shipping address.`);
      continue;
    }
    const baseOrderNumber = cleanOrderNumber$1(order.name || order.legacyResourceId);
    for (const [index, lineItem] of validLineItems.entries()) {
      const lineKey = lineItem.id || `${order.id}:${lineItem.sku || lineItem.title || index}`;
      const importKey = `shopify:${order.id}#${lineKey}`;
      const existing = await getDispatchOrderByMailboxMessageId(importKey);
      if (existing) {
        skipped += 1;
        continue;
      }
      const material = getLineMaterial(lineItem);
      const notes = getLineNotes(order, lineItem);
      const orderText = getOrderText(order, lineItem);
      await createDispatchOrder({
        source: "email",
        orderNumber: suffixOrderNumber$1(
          baseOrderNumber,
          index,
          validLineItems.length
        ),
        customer: getCustomerName(order),
        contact: getContact(order),
        address,
        city: getCityLine(order.shippingAddress),
        material,
        quantity: String(lineItem.quantity || ""),
        unit: await getDispatchUnitForMaterial(material) || "Unit",
        requestedWindow: getRequestedWindow(order, lineItem),
        timePreference: detectTimePreference(orderText || notes),
        truckPreference: "",
        notes,
        emailSubject: `Shopify order ${order.name || baseOrderNumber || order.id}`,
        rawEmail: JSON.stringify(order, null, 2),
        mailboxMessageId: importKey
      });
      imported += 1;
    }
  }
  return {
    configured: true,
    imported,
    skipped,
    skipReasons,
    message: `Shopify import complete: ${imported} imported, ${skipped} skipped from ${orders.length} order${orders.length === 1 ? "" : "s"}.`
  };
}
const DISPATCH_NAV_COLLAPSED_KEY$4 = "dispatchNavCollapsed";
const DISPATCH_MANUAL_ROUTING_KEY = "dispatchManualRoutingMode";
function getDeliveryStatusLabel(status) {
  if (status === "en_route") return "Enroute";
  if (status === "delivered") return "Delivered";
  return "Dispatched";
}
function getDeliveryStatusColor(status) {
  if (status === "delivered") return "#22c55e";
  if (status === "en_route") return "#f97316";
  return "#38bdf8";
}
function getOrderDisplayNumber$2(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function isImageProof$2(value) {
  return /^data:image\//i.test(String(value || "")) || /^https?:\/\/.+\.(?:png|jpe?g|webp|gif)(?:\?.*)?$/i.test(String(value || ""));
}
function suffixOrderNumber(orderNumber, index, total) {
  if (!orderNumber || total <= 1) return orderNumber;
  return `${orderNumber}${String.fromCharCode(97 + index)}`;
}
function startOfLocalDay(date) {
  return new Date(date.getFullYear(), date.getMonth(), date.getDate()).getTime();
}
function getRequestedDeliverySortValue(order) {
  var _a2;
  const value = String(order.requestedWindow || "").trim();
  const lower = value.toLowerCase();
  const today = /* @__PURE__ */ new Date();
  if (!value || /needs scheduling|unavailable|unknown/i.test(value)) {
    return Number.MAX_SAFE_INTEGER;
  }
  if (/\btoday\b/.test(lower)) return startOfLocalDay(today);
  if (/\btomorrow\b/.test(lower)) {
    const tomorrow = new Date(today);
    tomorrow.setDate(today.getDate() + 1);
    return startOfLocalDay(tomorrow);
  }
  const slashDate = value.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return new Date(year, Number(slashDate[1]) - 1, Number(slashDate[2])).getTime();
  }
  const monthDate = (_a2 = value.match(/\b(?:jan(?:uary)?|feb(?:ruary)?|mar(?:ch)?|apr(?:il)?|may|jun(?:e)?|jul(?:y)?|aug(?:ust)?|sep(?:t(?:ember)?)?|oct(?:ober)?|nov(?:ember)?|dec(?:ember)?)\.?\s+\d{1,2}(?:,\s*\d{4})?/i)) == null ? void 0 : _a2[0];
  if (monthDate) {
    const parsed2 = new Date(/\d{4}/.test(monthDate) ? monthDate : `${monthDate}, ${today.getFullYear()}`);
    if (!Number.isNaN(parsed2.getTime())) return startOfLocalDay(parsed2);
  }
  const parsed = new Date(value);
  return Number.isNaN(parsed.getTime()) ? Number.MAX_SAFE_INTEGER : startOfLocalDay(parsed);
}
function formatRequestedWindow(value) {
  const trimmed = value.trim();
  const dateInput = trimmed.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (!dateInput) return trimmed;
  return `${dateInput[2]}/${dateInput[3]}/${dateInput[1]}`;
}
function getTodayDateInputValue$1() {
  const today = /* @__PURE__ */ new Date();
  return `${today.getFullYear()}-${String(today.getMonth() + 1).padStart(2, "0")}-${String(today.getDate()).padStart(2, "0")}`;
}
function getRequestedWindowDateInputValue(value) {
  const trimmed = String(value || "").trim();
  const isoDate = trimmed.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (isoDate) return trimmed;
  const slashDate = trimmed.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return `${year}-${slashDate[1].padStart(2, "0")}-${slashDate[2].padStart(2, "0")}`;
  }
  const today = /* @__PURE__ */ new Date();
  if (/\btoday\b/i.test(trimmed)) {
    return today.toISOString().slice(0, 10);
  }
  if (/\btomorrow\b/i.test(trimmed)) {
    const tomorrow = new Date(today);
    tomorrow.setDate(today.getDate() + 1);
    return tomorrow.toISOString().slice(0, 10);
  }
  return "";
}
function getOrderTravelMinutes(order) {
  const minutes = Number(order.travelMinutes || 0);
  return Number.isFinite(minutes) && minutes > 0 ? minutes : 0;
}
function formatTravelMinutes$3(minutes) {
  if (!Number.isFinite(minutes) || minutes <= 0) return "Not calculated";
  const rounded = Math.round(minutes);
  if (rounded < 60) return `${rounded} min`;
  const hours = Math.floor(rounded / 60);
  const remainder = rounded % 60;
  return remainder ? `${hours} hr ${remainder} min` : `${hours} hr`;
}
function buildDispatchOrderSearchText(order, routes2) {
  const route57 = routes2.find((entry2) => entry2.id === order.assignedRouteId);
  return [order.id, order.orderNumber, order.source, order.customer, order.contact, order.address, order.city, order.material, order.quantity, order.unit, order.requestedWindow, order.timePreference, order.truckPreference, order.notes, order.status, order.deliveryStatus, order.eta, order.travelMinutes, order.travelMiles, order.travelSummary, order.emailSubject, order.rawEmail, order.proofName, order.proofNotes, order.signatureName, order.inspectionStatus, route57 == null ? void 0 : route57.code, route57 == null ? void 0 : route57.truck, route57 == null ? void 0 : route57.driver, route57 == null ? void 0 : route57.helper, route57 == null ? void 0 : route57.region].filter((value) => value !== null && value !== void 0 && value !== "").join(" ").toLowerCase();
}
function buildRouteSearchText(route57, orders) {
  var _a2;
  const routeOrders = ((_a2 = route57.orders) == null ? void 0 : _a2.length) ? route57.orders : orders.filter((order) => order.assignedRouteId === route57.id);
  return [route57.id, route57.code, route57.truckId, route57.truck, route57.driverId, route57.driver, route57.helperId, route57.helper, route57.color, route57.shift, route57.region, route57.stops, route57.totalTravelMinutes, route57.loadSummary, route57.created_at, route57.updated_at, ...routeOrders.map((order) => buildDispatchOrderSearchText(order, [route57]))].filter((value) => value !== null && value !== void 0 && value !== "").join(" ").toLowerCase();
}
function buildTruckSearchText(truck, routes2, orders) {
  const truckRoutes = routes2.filter((route57) => route57.truckId === truck.id || route57.truck === truck.label);
  return [truck.id, truck.label, truck.truckType, truck.tons, truck.yards, truck.capacity, truck.licensePlate, truck.created_at, truck.updated_at, ...truckRoutes.map((route57) => buildRouteSearchText(route57, orders))].filter((value) => value !== null && value !== void 0 && value !== "").join(" ").toLowerCase();
}
function buildEmployeeSearchText(employee, routes2, orders) {
  const employeeRoutes = routes2.filter((route57) => route57.driverId === employee.id || route57.helperId === employee.id || route57.driver === employee.name || route57.helper === employee.name);
  return [employee.id, employee.name, employee.role, employee.phone, employee.email, employee.created_at, employee.updated_at, ...employeeRoutes.map((route57) => buildRouteSearchText(route57, orders))].filter((value) => value !== null && value !== void 0 && value !== "").join(" ").toLowerCase();
}
function getTruckCapacityForOrderUnit(truck, unit) {
  if (/tons?/i.test(unit)) return Number(truck.tons || 0);
  if (/yards?/i.test(unit)) return Number(truck.yards || 0);
  return 0;
}
function getTruckCapacityLabel(unit) {
  if (/tons?/i.test(unit)) return "tons";
  if (/yards?/i.test(unit)) return "yards";
  return "";
}
function getOrderCapacityUnit(order) {
  if (/tons?/i.test(order.unit)) return "tons";
  if (/yards?/i.test(order.unit)) return "yards";
  return "unitless";
}
function getCapacityError(order, truck) {
  if (!truck) return "This route does not have a truck assigned yet.";
  const quantity = Number(order.quantity || 0);
  const capacity = getTruckCapacityForOrderUnit(truck, order.unit);
  const capacityLabel = getTruckCapacityLabel(order.unit);
  if (!quantity || !capacity || !capacityLabel) return "";
  if (quantity <= capacity) return "";
  return `${order.customer} needs ${quantity} ${capacityLabel}, but ${truck.label} is set to ${capacity} ${capacityLabel}.`;
}
function hasTruckCapacityForOrder(order, truck, currentQty = 0) {
  if (!truck) return false;
  const quantity = Number(order.quantity || 0);
  const capacity = getTruckCapacityForOrderUnit(truck, order.unit);
  const capacityLabel = getTruckCapacityLabel(order.unit);
  if (!quantity || !capacityLabel) return true;
  if (!capacity) return false;
  return currentQty + quantity <= capacity;
}
function isYardOrder(order) {
  return /yards?/i.test(order.unit);
}
function getSplitCountFromForm(form) {
  const count = Number(String(form.get("splitCount") || "").trim());
  return Number.isFinite(count) ? Math.floor(count) : 0;
}
function getSplitSuffix(index) {
  let value = index;
  let suffix = "";
  do {
    suffix = String.fromCharCode(97 + value % 26) + suffix;
    value = Math.floor(value / 26) - 1;
  } while (value >= 0);
  return suffix;
}
function getSplitBaseOrderNumber(order) {
  const base = String(order.orderNumber || order.id.replace(/^D-/, "")).trim();
  return base.replace(/[a-z]$/i, "");
}
function formatSplitQuantity(value) {
  if (!Number.isFinite(value)) return "";
  return Number.isInteger(value) ? String(value) : value.toFixed(2).replace(/\.?0+$/, "");
}
function getSplitCapacityError(order, truck, splitCount) {
  if (!truck) return "This route does not have a truck assigned yet.";
  if (!isYardOrder(order)) return getCapacityError(order, truck);
  const quantity = Number(order.quantity || 0);
  const capacity = Number(truck.yards || 30);
  if (!quantity || !capacity || quantity <= capacity) return "";
  if (splitCount < 2) {
    return `${order.customer} needs ${quantity} yards, which is over ${truck.label}'s ${capacity} yard capacity. Split the order before assigning it.`;
  }
  const perLoad = quantity / splitCount;
  if (perLoad > capacity) {
    return `${splitCount} split tickets would still be ${formatSplitQuantity(perLoad)} yards each. Use at least ${Math.ceil(quantity / capacity)} splits for ${truck.label}.`;
  }
  return "";
}
function attachLoaderNote$1(event) {
  const note = window.prompt("Add a loader note for this load. Leave blank if no note is needed.");
  if (note === null) {
    event.preventDefault();
    return;
  }
  let noteInput = event.currentTarget.querySelector('input[name="loaderNote"]');
  if (!noteInput) {
    noteInput = document.createElement("input");
    noteInput.type = "hidden";
    noteInput.name = "loaderNote";
    event.currentTarget.appendChild(noteInput);
  }
  noteInput.value = note.trim();
}
async function assignOrderToRoute({
  order,
  routeId,
  truck,
  splitCount,
  eta
}) {
  const previousRouteId = order.assignedRouteId || null;
  const capacityError = getSplitCapacityError(order, truck, splitCount);
  if (capacityError) return {
    ok: false,
    message: capacityError,
    createdCount: 0
  };
  const quantity = Number(order.quantity || 0);
  const truckYards = Number((truck == null ? void 0 : truck.yards) || 30);
  const shouldSplit = isYardOrder(order) && quantity > truckYards && splitCount > 1;
  const firstStopSequence = previousRouteId === routeId && order.stopSequence ? order.stopSequence : await getNextRouteStopSequence(routeId);
  if (!shouldSplit) {
    await updateDispatchOrder(order.id, {
      status: "scheduled",
      assignedRouteId: routeId,
      stopSequence: firstStopSequence,
      deliveryStatus: "not_started",
      eta: eta || null
    });
    await resequenceChangedRoutes([previousRouteId, routeId]);
    return {
      ok: true,
      message: "Order assigned to route.",
      createdCount: 1
    };
  }
  const splitQuantity = formatSplitQuantity(quantity / splitCount);
  const baseOrderNumber = getSplitBaseOrderNumber(order);
  const splitNote = `Split from #${baseOrderNumber} into ${splitCount} loads.`;
  await updateDispatchOrder(order.id, {
    orderNumber: `${baseOrderNumber}${getSplitSuffix(0)}`,
    quantity: splitQuantity,
    status: "scheduled",
    assignedRouteId: routeId,
    stopSequence: firstStopSequence,
    deliveryStatus: "not_started",
    eta: eta || null,
    notes: [order.notes, splitNote].filter(Boolean).join("\n")
  });
  for (let index = 1; index < splitCount; index += 1) {
    const created = await createDispatchOrder({
      source: order.source,
      orderNumber: `${baseOrderNumber}${getSplitSuffix(index)}`,
      customer: order.customer,
      contact: order.contact,
      address: order.address,
      city: order.city,
      material: order.material,
      quantity: splitQuantity,
      unit: order.unit,
      requestedWindow: order.requestedWindow,
      timePreference: order.timePreference || "Anytime",
      truckPreference: order.truckPreference || "",
      notes: [order.notes, splitNote].filter(Boolean).join("\n"),
      emailSubject: order.emailSubject || void 0,
      rawEmail: order.rawEmail || void 0
    });
    await updateDispatchOrder(created.id, {
      status: "scheduled",
      assignedRouteId: routeId,
      stopSequence: firstStopSequence + index,
      deliveryStatus: "not_started",
      eta: eta || null
    });
  }
  await resequenceChangedRoutes([previousRouteId, routeId]);
  return {
    ok: true,
    message: `Split #${baseOrderNumber} into ${splitCount} route tickets.`,
    createdCount: splitCount
  };
}
function getAutoRouteMaterialKey(order) {
  return String(order.material || "").toLowerCase().replace(/[#"'.,/()-]+/g, " ").replace(/\b(?:washed|screened|green|hills|supply|mix|blend)\b/g, " ").replace(/\s+/g, " ").trim() || "material";
}
function getAutoRouteTimeRank(order) {
  const value = `${order.timePreference || ""} ${order.requestedWindow || ""} ${order.notes || ""}`.toLowerCase();
  if (value.includes("morning")) return 0;
  if (value.includes("afternoon")) return 1;
  if (value.includes("evening")) return 2;
  return 3;
}
function getAutoRouteCode(truck, index, existingCodes) {
  var _a2;
  const truckDigits = ((_a2 = String(truck.label || "").match(/\d+/)) == null ? void 0 : _a2[0]) || String(index + 1).padStart(2, "0");
  const baseCode = `R-${truckDigits}`;
  let code = baseCode;
  let suffix = 2;
  while (existingCodes.has(code)) {
    code = `${baseCode}-${suffix}`;
    suffix += 1;
  }
  existingCodes.add(code);
  return code;
}
function routeCanTakeOrder(order, bucket) {
  const unit = getOrderCapacityUnit(order);
  return hasTruckCapacityForOrder(order, bucket.truck, bucket.usedByUnit[unit] || 0);
}
async function autoRouteOrdersForDispatch() {
  const [allOrders, existingRoutes, trucks, employees] = await Promise.all([getDispatchOrders(), getDispatchRoutes(), getDispatchTrucks(), getDispatchEmployees()]);
  const activeTrucks = trucks.filter((truck) => truck.isActive !== false);
  const drivers = employees.filter((employee) => employee.role === "driver" && employee.isActive !== false);
  const existingCodes = new Set(existingRoutes.map((route57) => route57.code).filter(Boolean));
  const routeBuckets = existingRoutes.map((route57) => {
    const truck = activeTrucks.find((entry2) => entry2.id === route57.truckId) || activeTrucks.find((entry2) => entry2.label === route57.truck) || null;
    if (!truck) return null;
    return {
      route: route57,
      truck,
      usedByUnit: {},
      materialKeys: /* @__PURE__ */ new Set(),
      orderIds: [],
      maxSequence: 0
    };
  }).filter((bucket) => Boolean(bucket));
  for (const order of allOrders) {
    if (!order.assignedRouteId || order.status === "delivered" || order.status === "cancelled" || order.deliveryStatus === "delivered") {
      continue;
    }
    const bucket = routeBuckets.find((entry2) => entry2.route.id === order.assignedRouteId);
    if (!bucket) continue;
    const unit = getOrderCapacityUnit(order);
    const quantity = Number(order.quantity || 0);
    bucket.orderIds.push(order.id);
    bucket.materialKeys.add(getAutoRouteMaterialKey(order));
    bucket.maxSequence = Math.max(bucket.maxSequence, Number(order.stopSequence || 0));
    bucket.usedByUnit[unit] = (bucket.usedByUnit[unit] || 0) + (Number.isFinite(quantity) ? quantity : 0);
  }
  const routedTruckIds = new Set(routeBuckets.map((bucket) => bucket.truck.id));
  const unusedTrucks = activeTrucks.filter((truck) => !routedTruckIds.has(truck.id));
  const usedDriverIds = new Set(existingRoutes.map((route57) => route57.driverId).filter(Boolean));
  const unusedDrivers = drivers.filter((driver) => !usedDriverIds.has(driver.id));
  const planningOrders = allOrders.filter((order) => order.status !== "delivered" && order.status !== "cancelled" && order.deliveryStatus !== "delivered" && !order.assignedRouteId).sort((a, b) => {
    const dateDiff = getRequestedDeliverySortValue(a) - getRequestedDeliverySortValue(b);
    if (dateDiff !== 0) return dateDiff;
    const timeDiff = getAutoRouteTimeRank(a) - getAutoRouteTimeRank(b);
    if (timeDiff !== 0) return timeDiff;
    const materialDiff = getAutoRouteMaterialKey(a).localeCompare(getAutoRouteMaterialKey(b));
    if (materialDiff !== 0) return materialDiff;
    return getOrderTravelMinutes(a) - getOrderTravelMinutes(b);
  });
  let createdRoutes = 0;
  let assignedOrders = 0;
  const skipped = [];
  for (const order of planningOrders) {
    const materialKey = getAutoRouteMaterialKey(order);
    const quantity = Number(order.quantity || 0);
    const unit = getOrderCapacityUnit(order);
    const compatibleBuckets = routeBuckets.filter((bucket2) => routeCanTakeOrder(order, bucket2));
    let bucket = compatibleBuckets.find((entry2) => entry2.materialKeys.has(materialKey)) || compatibleBuckets.find((entry2) => entry2.orderIds.length === 0) || compatibleBuckets[0] || null;
    if (!bucket && unusedTrucks.length) {
      const truck = unusedTrucks.shift();
      const driver = unusedDrivers.shift() || drivers[routeBuckets.length % Math.max(drivers.length, 1)] || null;
      const created = await createDispatchRoute({
        code: getAutoRouteCode(truck, routeBuckets.length, existingCodes),
        truckId: truck.id,
        truck: truck.label,
        driverId: (driver == null ? void 0 : driver.id) || "",
        driver: (driver == null ? void 0 : driver.name) || "",
        helperId: "",
        helper: "",
        color: ["#f97316", "#22c55e", "#38bdf8", "#a3e635", "#eab308", "#fb7185"][routeBuckets.length % 6],
        shift: "Auto routed",
        region: "Auto"
      });
      bucket = {
        route: created,
        truck,
        usedByUnit: {},
        materialKeys: /* @__PURE__ */ new Set(),
        orderIds: [],
        maxSequence: 0
      };
      routeBuckets.push(bucket);
      createdRoutes += 1;
    }
    if (!bucket || !routeCanTakeOrder(order, bucket)) {
      skipped.push(`${getOrderDisplayNumber$2(order)} ${order.customer}: no available truck capacity`);
      continue;
    }
    const nextSequence = bucket.maxSequence + 1;
    await updateDispatchOrder(order.id, {
      assignedRouteId: bucket.route.id,
      stopSequence: nextSequence,
      status: "scheduled",
      deliveryStatus: order.deliveryStatus || "not_started"
    });
    bucket.orderIds.push(order.id);
    bucket.maxSequence = nextSequence;
    bucket.materialKeys.add(materialKey);
    bucket.usedByUnit[unit] = (bucket.usedByUnit[unit] || 0) + (Number.isFinite(quantity) ? quantity : 0);
    assignedOrders += 1;
  }
  return {
    assignedOrders,
    createdRoutes,
    skipped
  };
}
function buildChecklistJson$1(form) {
  return JSON.stringify({
    siteSafe: form.get("siteSafe") === "on",
    loadMatchesTicket: form.get("loadMatchesTicket") === "on",
    customerConfirmedPlacement: form.get("customerConfirmedPlacement") === "on",
    photosTaken: form.get("photosTaken") === "on",
    customChecklist: String(form.get("customChecklist") || "").trim()
  });
}
function metricCard(label, value, accent) {
  return /* @__PURE__ */ jsxs("div", {
    style: {
      borderRadius: 6,
      padding: "14px 16px",
      background: "#0f172a",
      border: `1px solid ${accent}33`,
      boxShadow: `inset 4px 0 0 ${accent}, 0 1px 2px rgba(0,0,0,0.08)`
    },
    children: [/* @__PURE__ */ jsx("div", {
      style: {
        color: "#94a3b8",
        fontSize: 12,
        textTransform: "uppercase",
        letterSpacing: "0.08em"
      },
      children: label
    }), /* @__PURE__ */ jsx("div", {
      style: {
        marginTop: 8,
        fontSize: 28,
        fontWeight: 800,
        color: "#f8fafc"
      },
      children: value
    })]
  });
}
let googleMapsLoaderPromise = null;
function loadGoogleMaps(apiKey) {
  var _a2;
  if (typeof window === "undefined") return Promise.resolve();
  if ((_a2 = window.google) == null ? void 0 : _a2.maps) return Promise.resolve();
  if (googleMapsLoaderPromise) return googleMapsLoaderPromise;
  googleMapsLoaderPromise = new Promise((resolve, reject) => {
    const existing = document.querySelector('script[data-dispatch-google-maps="true"]');
    if (existing) {
      existing.addEventListener("load", () => resolve(), {
        once: true
      });
      existing.addEventListener("error", () => reject(new Error("Failed to load Google Maps")), {
        once: true
      });
      return;
    }
    const script = document.createElement("script");
    script.dataset.dispatchGoogleMaps = "true";
    script.async = true;
    script.defer = true;
    script.src = `https://maps.googleapis.com/maps/api/js?key=${encodeURIComponent(apiKey)}`;
    script.onload = () => resolve();
    script.onerror = () => reject(new Error("Failed to load Google Maps"));
    document.head.appendChild(script);
  });
  return googleMapsLoaderPromise;
}
function RouteMapPreview({
  googleMapsApiKey,
  originAddress,
  routes: routes2
}) {
  const mapRef = useRef(null);
  const mapObjectsRef = useRef([]);
  const [status, setStatus] = useState("");
  const [routeWarnings, setRouteWarnings] = useState([]);
  const getTravelLabel = (order) => {
    if (order.travelMinutes) return `${order.travelMinutes} min RT`;
    return order.travelSummary || "RT not calculated";
  };
  const routePlan = useMemo(() => routes2.map((route57) => ({
    id: route57.id,
    color: route57.color || "#38bdf8",
    label: `${route57.code} · ${route57.truck}`,
    stops: route57.orders.map((order) => ({
      address: [order.address, order.city].map((part) => String(part || "").trim()).filter(Boolean).join(", "),
      customer: order.customer,
      travelLabel: getTravelLabel(order)
    })).filter((stop) => stop.address)
  })).filter((route57) => route57.stops.length > 0), [routes2]);
  useEffect(() => {
    let cancelled = false;
    async function drawMap() {
      if (!mapRef.current) return;
      if (!googleMapsApiKey) {
        setStatus("Add GOOGLE_MAPS_BROWSER_API_KEY to show the live route map.");
        return;
      }
      if (!originAddress) {
        setStatus("Set an active origin address before drawing routes.");
        return;
      }
      if (routePlan.length === 0) {
        setStatus("Assign orders to routes to preview truck paths.");
        return;
      }
      setStatus("Loading route map...");
      setRouteWarnings([]);
      try {
        await loadGoogleMaps(googleMapsApiKey);
        if (cancelled || !mapRef.current) return;
        const google = window.google;
        mapObjectsRef.current.forEach((object) => object.setMap(null));
        mapObjectsRef.current = [];
        const map = new google.maps.Map(mapRef.current, {
          center: {
            lat: 43.1789,
            lng: -88.1173
          },
          zoom: 10,
          mapTypeControl: false,
          streetViewControl: false,
          fullscreenControl: true
        });
        const bounds = new google.maps.LatLngBounds();
        const directionsService = new google.maps.DirectionsService();
        const geocoder = new google.maps.Geocoder();
        const warnings = [];
        const addTimeBadge = (position, text, color) => {
          const badge = document.createElement("div");
          badge.textContent = text;
          badge.style.position = "absolute";
          badge.style.transform = "translate(12px, -34px)";
          badge.style.padding = "5px 8px";
          badge.style.borderRadius = "999px";
          badge.style.background = "rgba(15, 23, 42, 0.92)";
          badge.style.border = `1px solid ${color}99`;
          badge.style.boxShadow = "0 8px 18px rgba(2, 6, 23, 0.28)";
          badge.style.color = "#f8fafc";
          badge.style.fontSize = "11px";
          badge.style.fontWeight = "800";
          badge.style.whiteSpace = "nowrap";
          const overlay = new google.maps.OverlayView();
          overlay.onAdd = () => {
            var _a2;
            (_a2 = overlay.getPanes()) == null ? void 0 : _a2.overlayMouseTarget.appendChild(badge);
          };
          overlay.draw = () => {
            var _a2;
            const point = (_a2 = overlay.getProjection()) == null ? void 0 : _a2.fromLatLngToDivPixel(position);
            if (!point) return;
            badge.style.left = `${point.x}px`;
            badge.style.top = `${point.y}px`;
          };
          overlay.onRemove = () => {
            badge.remove();
          };
          overlay.setMap(map);
          mapObjectsRef.current.push(overlay);
        };
        const addStopMarker = (position, route57, stop, stopIndex) => {
          const marker = new google.maps.Marker({
            map,
            position,
            label: String(stopIndex + 1),
            title: `${stop.customer} · ${stop.travelLabel}`
          });
          mapObjectsRef.current.push(marker);
          addTimeBadge(position, stop.travelLabel, route57.color);
          bounds.extend(position);
        };
        const geocodeAddress = (address) => new Promise((resolve) => {
          geocoder.geocode({
            address
          }, (results, geocodeStatus) => {
            var _a2, _b;
            if (geocodeStatus === "OK" && ((_b = (_a2 = results == null ? void 0 : results[0]) == null ? void 0 : _a2.geometry) == null ? void 0 : _b.location)) {
              resolve(results[0].geometry.location);
              return;
            }
            console.warn("[DISPATCH MAP GEOCODE ERROR]", address, geocodeStatus);
            resolve(null);
          });
        });
        const drawFallbackRoute = async (route57) => {
          const points = (await Promise.all([originAddress, ...route57.stops.map((stop) => stop.address), originAddress].map((address) => geocodeAddress(address)))).filter(Boolean);
          if (points.length < 2) return false;
          const polyline = new google.maps.Polyline({
            map,
            path: points,
            strokeColor: route57.color,
            strokeOpacity: 0.82,
            strokeWeight: 5
          });
          mapObjectsRef.current.push(polyline);
          points.forEach((point, index) => {
            const isYard = index === 0 || index === points.length - 1;
            const marker = new google.maps.Marker({
              map,
              position: point,
              label: isYard ? "Y" : String(index),
              title: isYard ? "Yard" : `${route57.stops[index - 1].customer} · ${route57.stops[index - 1].travelLabel}`
            });
            mapObjectsRef.current.push(marker);
            if (!isYard) {
              addTimeBadge(point, route57.stops[index - 1].travelLabel, route57.color);
            }
            bounds.extend(point);
          });
          return true;
        };
        await Promise.all(routePlan.map((route57) => new Promise((resolve) => {
          const renderer = new google.maps.DirectionsRenderer({
            map,
            preserveViewport: true,
            suppressMarkers: true,
            polylineOptions: {
              strokeColor: route57.color,
              strokeOpacity: 0.9,
              strokeWeight: 6
            }
          });
          mapObjectsRef.current.push(renderer);
          directionsService.route({
            origin: originAddress,
            destination: originAddress,
            waypoints: route57.stops.map((stop) => ({
              location: stop.address,
              stopover: true
            })),
            optimizeWaypoints: false,
            travelMode: google.maps.TravelMode.DRIVING
          }, async (result, routeStatus) => {
            var _a2, _b, _c, _d, _e, _f;
            if (result && routeStatus === "OK") {
              renderer.setDirections(result);
              (_c = (_b = (_a2 = result.routes) == null ? void 0 : _a2[0]) == null ? void 0 : _b.legs) == null ? void 0 : _c.forEach((leg) => {
                if (leg.start_location) bounds.extend(leg.start_location);
                if (leg.end_location) bounds.extend(leg.end_location);
              });
              const firstLeg = (_f = (_e = (_d = result.routes) == null ? void 0 : _d[0]) == null ? void 0 : _e.legs) == null ? void 0 : _f[0];
              if (firstLeg == null ? void 0 : firstLeg.start_location) {
                const yardMarker = new google.maps.Marker({
                  map,
                  position: firstLeg.start_location,
                  label: "Y",
                  title: "Yard"
                });
                mapObjectsRef.current.push(yardMarker);
              }
              route57.stops.forEach((stop, stopIndex) => {
                var _a3, _b2, _c2;
                const leg = (_c2 = (_b2 = (_a3 = result.routes) == null ? void 0 : _a3[0]) == null ? void 0 : _b2.legs) == null ? void 0 : _c2[stopIndex];
                if (leg == null ? void 0 : leg.end_location) {
                  addStopMarker(leg.end_location, route57, stop, stopIndex);
                }
              });
            } else {
              console.warn("[DISPATCH MAP ROUTE ERROR]", route57.label, routeStatus);
              renderer.setMap(null);
              const drewFallback = await drawFallbackRoute(route57);
              warnings.push(drewFallback ? `${route57.label}: showing fallback stop-to-stop lines because Google Directions returned ${routeStatus}.` : `${route57.label}: Google Directions returned ${routeStatus}, and the stop addresses could not be geocoded.`);
            }
            resolve();
          });
        })));
        if (!bounds.isEmpty()) {
          map.fitBounds(bounds);
        }
        setStatus("");
        setRouteWarnings(warnings);
      } catch (error) {
        setStatus(error instanceof Error ? error.message : "Unable to load route map.");
      }
    }
    drawMap();
    return () => {
      cancelled = true;
      mapObjectsRef.current.forEach((object) => object.setMap(null));
      mapObjectsRef.current = [];
    };
  }, [googleMapsApiKey, originAddress, routePlan]);
  return /* @__PURE__ */ jsxs("div", {
    style: styles$b.mapStage,
    children: [/* @__PURE__ */ jsx("div", {
      ref: mapRef,
      style: styles$b.googleMapCanvas
    }), status ? /* @__PURE__ */ jsx("div", {
      style: styles$b.mapStatus,
      children: status
    }) : null, routeWarnings.length ? /* @__PURE__ */ jsx("div", {
      style: styles$b.mapNotice,
      children: routeWarnings.slice(0, 2).map((warning) => /* @__PURE__ */ jsx("div", {
        children: warning
      }, warning))
    }) : null, routePlan.length ? /* @__PURE__ */ jsx("div", {
      style: styles$b.mapLegend,
      children: routePlan.map((route57) => /* @__PURE__ */ jsxs("div", {
        style: {
          display: "flex",
          alignItems: "center",
          gap: 8
        },
        children: [/* @__PURE__ */ jsx("div", {
          style: styles$b.routeColor(route57.color)
        }), /* @__PURE__ */ jsx("span", {
          children: route57.label
        })]
      }, route57.id))
    }) : null]
  });
}
function getDispatchPath$1(url) {
  return url.pathname.startsWith("/app/") ? "/app/dispatch" : "/dispatch";
}
function getBrowserGoogleMapsApiKey() {
  return process.env.GOOGLE_MAPS_BROWSER_API_KEY || "";
}
let dispatchStateCache = null;
const DISPATCH_STATE_CACHE_MS = 5e3;
function clearDispatchStateCache() {
  dispatchStateCache = null;
}
async function loadDispatchState(options = {}) {
  if (options.useCache && dispatchStateCache && Date.now() - dispatchStateCache.loadedAt < DISPATCH_STATE_CACHE_MS) {
    return dispatchStateCache.state;
  }
  try {
    if (!options.skipSetup) {
      await Promise.all([ensureSeedDispatchTrucks(), ensureSeedDispatchEmployees(), ensureSeedDispatchOrders(), ensureSeedDispatchRoutes()]);
      if (process.env.DISPATCH_AUTO_DAILY_RESET === "true") {
        await resetDispatchRoutesForNewDay();
      }
    }
    const [orders, routes2, trucks, employees] = await Promise.all([getDispatchOrders({
      lightweight: options.lightweightOrders || options.fast
    }), getDispatchRoutes(), getDispatchTrucks(), getDispatchEmployees()]);
    if (options.fast) {
      return {
        skipLoaderRevalidation: true,
        orders,
        routes: routes2,
        trucks,
        employees,
        storageReady: true,
        storageError: null
      };
    }
    const [materialOptions, mapOriginAddress, classicColumnSettings, driverLocations, productDetailsByMaterial] = await Promise.all([getDispatchMaterialOptions(), getDispatchOriginAddress(), getClassicColumnSettings(), getLatestDispatchDriverLocations(), getDispatchProductDetailsForMaterials(orders.map((order) => order.material))]);
    const state = {
      orders,
      routes: routes2,
      trucks,
      employees,
      materialOptions,
      mapOriginAddress,
      classicColumnSettings,
      driverLocations,
      productDetailsByMaterial,
      storageReady: true,
      storageError: null
    };
    if (options.useCache) {
      dispatchStateCache = {
        loadedAt: Date.now(),
        state
      };
    }
    return state;
  } catch (error) {
    const message = error instanceof Error ? error.message : "Unable to load dispatch storage";
    console.error("[DISPATCH STORAGE ERROR]", message);
    const fallbackState = {
      orders: seedDispatchOrders,
      routes: seedDispatchRoutes,
      trucks: seedDispatchTrucks,
      employees: seedDispatchEmployees,
      materialOptions: [],
      mapOriginAddress: "W185 N7487 Narrow Ln, Menomonee Falls, WI 53051",
      classicColumnSettings: void 0,
      driverLocations: [],
      productDetailsByMaterial: {},
      storageReady: false,
      storageError: message
    };
    if (options.useCache) {
      dispatchStateCache = {
        loadedAt: Date.now(),
        state: fallbackState
      };
    }
    return fallbackState;
  }
}
async function resequenceRouteStops(routeId) {
  if (!routeId) return;
  const routeOrders = (await getDispatchOrders()).filter((order) => order.assignedRouteId === routeId && order.status !== "delivered" && order.deliveryStatus !== "delivered").sort((a, b) => Number(a.stopSequence || 9999) - Number(b.stopSequence || 9999) || String(a.created_at || "").localeCompare(String(b.created_at || "")));
  await Promise.all(routeOrders.map((order, index) => Number(order.stopSequence || 0) === index + 1 ? Promise.resolve(order) : updateDispatchOrder(order.id, {
    stopSequence: index + 1
  })));
}
async function resequenceChangedRoutes(routeIds) {
  const uniqueRouteIds = Array.from(new Set(routeIds.filter((routeId) => Boolean(routeId))));
  await Promise.all(uniqueRouteIds.map((routeId) => resequenceRouteStops(routeId)));
}
async function loader$o({
  request
}) {
  const url = new URL(request.url);
  const dispatchPath = getDispatchPath$1(url);
  if (url.searchParams.get("logout") === "1") {
    return redirect(dispatchPath, {
      headers: [["Set-Cookie", await userAuthCookie.serialize("", {
        maxAge: 0
      })], ["Set-Cookie", await adminQuoteCookie.serialize("", {
        maxAge: 0
      })]]
    });
  }
  const allowed = await hasAdminQuotePermissionAccess(request, "dispatch");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  const currentUser = await getCurrentUser(request);
  const requestedView = url.searchParams.get("view") || "dashboard";
  const manageOnlyViews = /* @__PURE__ */ new Set(["orders", "routes", "trucks", "employees"]);
  if (currentUser && manageOnlyViews.has(requestedView) && !currentUser.permissions.includes("manageDispatch")) {
    return redirect(dispatchPath);
  }
  const shouldPollMailbox = url.searchParams.get("pollMailbox") === "1" || process.env.DISPATCH_AUTO_POLL_ON_PAGE_LOAD === "true";
  const isEditorOpen = requestedView === "orders" && Boolean(url.searchParams.get("order"));
  const isClassicPage = /\/(?:app\/)?classic\/?$/.test(url.pathname);
  const [dispatchState, mailboxStatus] = await Promise.all([loadDispatchState({
    skipSetup: isEditorOpen || shouldPollMailbox,
    useCache: !shouldPollMailbox,
    lightweightOrders: isClassicPage
  }), shouldPollMailbox ? maybeAutoPollDispatchMailbox().catch((error) => {
    console.error("[DISPATCH MAILBOX AUTO POLL ERROR]", error);
    return {
      configured: true,
      imported: 0,
      skipped: 0,
      message: error instanceof Error ? error.message : "Mailbox auto-poll failed."
    };
  }) : Promise.resolve(null)]);
  return data({
    allowed: true,
    currentUser,
    mailboxStatus,
    googleMapsApiKey: getBrowserGoogleMapsApiKey(),
    ...dispatchState
  });
}
function shouldRevalidate$1({
  actionResult,
  defaultShouldRevalidate
}) {
  if (actionResult == null ? void 0 : actionResult.skipLoaderRevalidation) return false;
  return defaultShouldRevalidate;
}
async function action$p({
  request
}) {
  var _a2, _b, _c;
  const url = new URL(request.url);
  const dispatchPath = getDispatchPath$1(url);
  const form = await request.formData();
  const intent = String(form.get("intent") || "");
  clearDispatchStateCache();
  const allowed = await hasAdminQuotePermissionAccess(request, "dispatch");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  const currentUser = await getCurrentUser(request);
  const canManageDispatch = await hasAdminQuotePermissionAccess(request, "manageDispatch");
  const driverOnlyIntents = /* @__PURE__ */ new Set(["update-stop-status"]);
  if (!canManageDispatch && !driverOnlyIntents.has(intent)) {
    const dispatchState = await loadDispatchState();
    return data({
      allowed: true,
      ok: false,
      message: "You do not have permission to manage dispatch.",
      googleMapsApiKey: getBrowserGoogleMapsApiKey(),
      ...dispatchState
    }, {
      status: 403
    });
  }
  try {
    if (process.env.DISPATCH_AUTO_DAILY_RESET === "true") {
      await resetDispatchRoutesForNewDay();
    }
    if (intent === "auto-route-orders") {
      if (form.get("manualRoutingMode") === "on") {
        const dispatchState2 = await loadDispatchState({
          skipSetup: true,
          fast: true
        });
        return data({
          allowed: true,
          ok: false,
          message: "Manual routing mode is on. Turn it off before running Auto Route.",
          ...dispatchState2
        });
      }
      const result = await autoRouteOrdersForDispatch();
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      const skippedText = result.skipped.length ? ` Skipped ${result.skipped.length}: ${result.skipped.slice(0, 3).join("; ")}${result.skipped.length > 3 ? "..." : ""}` : "";
      await logAuditEvent({
        actor: currentUser,
        action: "auto_route_orders",
        targetType: "dispatch",
        targetId: "auto-route",
        targetLabel: "Auto Route",
        details: result
      });
      return data({
        allowed: true,
        ok: result.assignedOrders > 0,
        message: `Auto Route assigned ${result.assignedOrders} order${result.assignedOrders === 1 ? "" : "s"} and created ${result.createdRoutes} route${result.createdRoutes === 1 ? "" : "s"}.${skippedText}`,
        ...dispatchState
      });
    }
    if (intent === "create-order") {
      const customer = String(form.get("customer") || "").trim();
      const rawAddress = String(form.get("address") || "").trim();
      const splitAddress = splitStreetAndCity(rawAddress);
      const address = splitAddress.address;
      const city = splitAddress.city || String(form.get("city") || "").trim();
      const material = String(form.get("material") || "").trim();
      if (!customer || !address || !material) {
        const dispatchState2 = await loadDispatchState({
          skipSetup: true,
          fast: true
        });
        return data({
          allowed: true,
          ok: false,
          message: "Customer, jobsite address, and material are required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const created = await createDispatchOrder({
        source: "manual",
        orderNumber: String(form.get("orderNumber") || "").trim(),
        customer,
        contact: String(form.get("contact") || "").trim(),
        address,
        city,
        material,
        quantity: String(form.get("quantity") || "").trim(),
        unit: await getDispatchUnitForMaterial(material) || String(form.get("unit") || "Ton").trim() || "Ton",
        requestedWindow: formatRequestedWindow(String(form.get("requestedWindow") || "")),
        timePreference: String(form.get("timePreference") || "Anytime").trim() || detectTimePreference(String(form.get("notes") || "")),
        truckPreference: String(form.get("truckPreference") || "").trim(),
        notes: String(form.get("notes") || "").trim()
      });
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      return data({
        allowed: true,
        ok: true,
        message: `Added ${created.customer} to the dispatch queue.`,
        selectedOrderId: created.id,
        ...dispatchState
      });
    }
    if (intent === "parse-email-order") {
      const rawEmail = String(form.get("rawEmail") || "").trim();
      if (!rawEmail) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Paste the order email before parsing.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const parsed = parseDispatchEmail(rawEmail);
      const parsedProducts = ((_a2 = parsed.products) == null ? void 0 : _a2.length) ? parsed.products : [{
        material: parsed.material,
        quantity: parsed.quantity
      }];
      const validProducts = parsedProducts.filter((product) => product.material);
      if (!parsed.address || !validProducts.length) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "I could not find an address and material in that email. Add labels like Address: and Material: and try again.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const createdOrders = [];
      for (const [index, product] of validProducts.entries()) {
        const created = await createDispatchOrder({
          source: "email",
          orderNumber: suffixOrderNumber(parsed.orderNumber, index, validProducts.length),
          customer: parsed.customer,
          contact: parsed.contact,
          address: parsed.address,
          city: parsed.city,
          material: product.material,
          quantity: product.quantity,
          unit: await getDispatchUnitForMaterial(product.material) || parsed.unit,
          requestedWindow: parsed.requestedWindow,
          timePreference: parsed.timePreference,
          truckPreference: parsed.truckPreference,
          notes: parsed.notes || "Parsed from order email.",
          emailSubject: parsed.subject,
          rawEmail
        });
        createdOrders.push(created);
      }
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      return data({
        allowed: true,
        ok: true,
        message: createdOrders.length > 1 ? `Parsed ${createdOrders.length} tickets from order ${parsed.orderNumber}.` : `Parsed email order for ${createdOrders[0].customer}.`,
        selectedOrderId: createdOrders[0].id,
        ...dispatchState
      });
    }
    if (intent === "update-order") {
      const orderId = String(form.get("orderId") || "").trim();
      const returnTo = String(form.get("returnTo") || "").trim();
      const safeReturnTo = returnTo.startsWith("/") ? returnTo : "";
      const customer = String(form.get("customer") || "").trim();
      const rawAddress = String(form.get("address") || "").trim();
      const splitAddress = splitStreetAndCity(rawAddress);
      const address = splitAddress.address;
      const city = splitAddress.city || String(form.get("city") || "").trim();
      const material = String(form.get("material") || "").trim();
      const unit = await getDispatchUnitForMaterial(material) || String(form.get("unit") || "").trim() || "Unit";
      const rawStatus = String(form.get("status") || "new").trim();
      const status = rawStatus === "scheduled" || rawStatus === "hold" || rawStatus === "delivered" || rawStatus === "cancelled" ? rawStatus : "new";
      const routeId = String(form.get("routeId") || "").trim();
      const eta = String(form.get("eta") || "").trim() || null;
      if (!orderId || !customer || !address || !material) {
        const dispatchState = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Order, customer, address, and material are required.",
          selectedOrderId: orderId,
          ...dispatchState
        }, {
          status: 400
        });
      }
      const updated = await updateDispatchOrderDetails(orderId, {
        orderNumber: String(form.get("orderNumber") || "").trim() || null,
        customer,
        contact: String(form.get("contact") || "").trim(),
        address,
        city,
        material,
        quantity: String(form.get("quantity") || "").trim(),
        unit,
        requestedWindow: formatRequestedWindow(String(form.get("requestedWindow") || "")) || "Needs scheduling",
        timePreference: String(form.get("timePreference") || "Anytime").trim() || detectTimePreference(String(form.get("notes") || "")),
        truckPreference: String(form.get("truckPreference") || "").trim() || null,
        notes: String(form.get("notes") || "").trim(),
        status
      });
      let finalOrder = updated;
      if (form.has("routeId")) {
        const previousRouteId = updated.assignedRouteId || null;
        if (routeId) {
          const [allRoutes, allTrucks] = await Promise.all([getDispatchRoutes(), getDispatchTrucks()]);
          const selectedRoute = allRoutes.find((route57) => route57.id === routeId);
          const selectedTruck = allTrucks.find((truck) => truck.id === (selectedRoute == null ? void 0 : selectedRoute.truckId));
          const splitCount = getSplitCountFromForm(form);
          const capacityError = !selectedRoute ? "Select a valid route before assigning this order." : getSplitCapacityError(updated, selectedTruck, splitCount);
          if (capacityError) {
            const dispatchState = await loadDispatchState();
            return data({
              allowed: true,
              ok: false,
              message: capacityError,
              selectedOrderId: orderId,
              ...dispatchState
            }, {
              status: 400
            });
          }
          const assignment = await assignOrderToRoute({
            order: updated,
            routeId,
            truck: selectedTruck,
            splitCount,
            eta
          });
          if (!assignment.ok) {
            const dispatchState = await loadDispatchState();
            return data({
              allowed: true,
              ok: false,
              message: assignment.message,
              selectedOrderId: orderId,
              ...dispatchState
            }, {
              status: 400
            });
          }
          finalOrder = updated;
        } else {
          finalOrder = await updateDispatchOrder(orderId, {
            status: status === "delivered" || status === "hold" || status === "cancelled" ? status : "new",
            assignedRouteId: null,
            stopSequence: null,
            deliveryStatus: status === "delivered" ? "delivered" : "not_started",
            eta: null
          });
          await resequenceChangedRoutes([previousRouteId]);
        }
      }
      return redirect(safeReturnTo || `${dispatchPath}?view=orders`);
    }
    if (intent === "delete-order") {
      const orderId = String(form.get("orderId") || "").trim();
      if (!orderId) throw new Error("Missing order selection");
      await deleteDispatchOrder(orderId);
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: "Order deleted.",
        selectedOrderId: (_b = dispatchState.orders[0]) == null ? void 0 : _b.id,
        ...dispatchState
      });
    }
    if (intent === "poll-mailbox") {
      const mailboxStatus = await pollDispatchMailbox();
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: mailboxStatus.configured,
        message: mailboxStatus.message,
        mailboxStatus,
        ...dispatchState
      });
    }
    if (intent === "poll-shopify-orders") {
      const shopifyImportStatus = await importDispatchShopifyOrders();
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: shopifyImportStatus.configured,
        message: shopifyImportStatus.message,
        shopifyImportStatus,
        ...dispatchState
      });
    }
    if (intent === "create-route") {
      const code = String(form.get("code") || "").trim();
      const truckId = String(form.get("truckId") || "").trim();
      const driverId = String(form.get("driverId") || "").trim();
      const helperId = String(form.get("helperId") || "").trim();
      const trucks = await getDispatchTrucks();
      const employees = await getDispatchEmployees();
      const selectedTruck = trucks.find((truck) => truck.id === truckId);
      const selectedDriver = employees.find((employee) => employee.id === driverId);
      const selectedHelper = employees.find((employee) => employee.id === helperId);
      if (!code || !selectedTruck || !selectedDriver) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Route code, truck, and driver are required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const created = await createDispatchRoute({
        code,
        truckId: selectedTruck.id,
        truck: selectedTruck.label,
        driverId: selectedDriver.id,
        driver: selectedDriver.name,
        helperId: selectedHelper == null ? void 0 : selectedHelper.id,
        helper: (selectedHelper == null ? void 0 : selectedHelper.name) || "",
        color: String(form.get("color") || "#38bdf8").trim(),
        shift: String(form.get("shift") || "").trim(),
        region: String(form.get("region") || "").trim()
      });
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: `Added ${created.truck} to the route board.`,
        ...dispatchState
      });
    }
    if (intent === "update-route") {
      const routeId = String(form.get("routeId") || "").trim();
      const code = String(form.get("code") || "").trim();
      const truckId = String(form.get("truckId") || "").trim();
      const driverId = String(form.get("driverId") || "").trim();
      const helperId = String(form.get("helperId") || "").trim();
      const trucks = await getDispatchTrucks();
      const employees = await getDispatchEmployees();
      const selectedTruck = trucks.find((truck) => truck.id === truckId);
      const selectedDriver = employees.find((employee) => employee.id === driverId);
      const selectedHelper = employees.find((employee) => employee.id === helperId);
      if (!routeId || !code) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Route and route code are required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      if (selectedTruck) {
        const assignedOrders = (await getDispatchOrders()).filter((order) => order.assignedRouteId === routeId && order.status !== "delivered");
        const capacityError = assignedOrders.map((order) => getCapacityError(order, selectedTruck)).find(Boolean);
        if (capacityError) {
          const dispatchState2 = await loadDispatchState();
          return data({
            allowed: true,
            ok: false,
            message: capacityError,
            ...dispatchState2
          }, {
            status: 400
          });
        }
      }
      const updated = await updateDispatchRoute(routeId, {
        code,
        truckId: (selectedTruck == null ? void 0 : selectedTruck.id) || null,
        truck: (selectedTruck == null ? void 0 : selectedTruck.label) || "",
        driverId: (selectedDriver == null ? void 0 : selectedDriver.id) || null,
        driver: (selectedDriver == null ? void 0 : selectedDriver.name) || "",
        helperId: (selectedHelper == null ? void 0 : selectedHelper.id) || null,
        helper: (selectedHelper == null ? void 0 : selectedHelper.name) || "",
        color: String(form.get("color") || "#38bdf8").trim(),
        shift: String(form.get("shift") || "").trim(),
        region: String(form.get("region") || "").trim()
      });
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: `Updated ${updated.code}.`,
        ...dispatchState
      });
    }
    if (intent === "delete-route") {
      const routeId = String(form.get("routeId") || "").trim();
      if (!routeId) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Route is required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const assignedOrders = (await getDispatchOrders()).filter((order) => order.assignedRouteId === routeId && order.status !== "delivered" && order.deliveryStatus !== "delivered");
      if (assignedOrders.length) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: `Move or unassign ${assignedOrders.length} active order${assignedOrders.length === 1 ? "" : "s"} before deleting this route.`,
          ...dispatchState2
        }, {
          status: 400
        });
      }
      await deleteDispatchRoute(routeId);
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: "Deleted route from the active route board.",
        ...dispatchState
      });
    }
    if (intent === "create-truck") {
      const label = String(form.get("label") || "").trim();
      if (!label) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Truck name is required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const created = await createDispatchTruck({
        label,
        truckType: String(form.get("truckType") || "").trim(),
        tons: String(form.get("tons") || "").trim(),
        yards: String(form.get("yards") || "").trim(),
        licensePlate: String(form.get("licensePlate") || "").trim()
      });
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: `Added ${created.label} to the fleet.`,
        ...dispatchState
      });
    }
    if (intent === "update-truck") {
      const truckId = String(form.get("truckId") || "").trim();
      const label = String(form.get("label") || "").trim();
      if (!truckId || !label) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Truck and truck name are required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const updated = await updateDispatchTruck(truckId, {
        label,
        truckType: String(form.get("truckType") || "").trim(),
        tons: String(form.get("tons") || "").trim() || null,
        yards: String(form.get("yards") || "").trim() || null,
        licensePlate: String(form.get("licensePlate") || "").trim() || null
      });
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: `Updated ${updated.label}.`,
        ...dispatchState
      });
    }
    if (intent === "delete-truck") {
      const truckId = String(form.get("truckId") || "").trim();
      if (!truckId) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Truck is required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      await deleteDispatchTruck(truckId);
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: "Deleted truck from active fleet.",
        ...dispatchState
      });
    }
    if (intent === "create-employee") {
      const name = String(form.get("name") || "").trim();
      const rawRole = String(form.get("role") || "driver").trim();
      const role = rawRole === "helper" || rawRole === "dispatcher" ? rawRole : "driver";
      if (!name) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Employee name is required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const created = await createDispatchEmployee({
        name,
        role,
        phone: String(form.get("phone") || "").trim(),
        email: String(form.get("email") || "").trim()
      });
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: `Added ${created.name} to employees.`,
        ...dispatchState
      });
    }
    if (intent === "update-employee") {
      const employeeId = String(form.get("employeeId") || "").trim();
      const name = String(form.get("name") || "").trim();
      const rawRole = String(form.get("role") || "driver").trim();
      const role = rawRole === "helper" || rawRole === "dispatcher" ? rawRole : "driver";
      if (!employeeId || !name) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Employee and employee name are required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const updated = await updateDispatchEmployee(employeeId, {
        name,
        role,
        phone: String(form.get("phone") || "").trim() || null,
        email: String(form.get("email") || "").trim() || null
      });
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: `Updated ${updated.name}.`,
        ...dispatchState
      });
    }
    if (intent === "delete-employee") {
      const employeeId = String(form.get("employeeId") || "").trim();
      if (!employeeId) {
        const dispatchState2 = await loadDispatchState();
        return data({
          allowed: true,
          ok: false,
          message: "Employee is required.",
          ...dispatchState2
        }, {
          status: 400
        });
      }
      await deleteDispatchEmployee(employeeId);
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: "Deleted employee from active roster.",
        ...dispatchState
      });
    }
    if (intent === "assign-order") {
      const orderId = String(form.get("orderId") || "").trim();
      const routeId = String(form.get("routeId") || "").trim();
      if (!orderId || !routeId) {
        throw new Error("Missing order or route assignment details");
      }
      const [allOrders, allRoutes, allTrucks] = await Promise.all([getDispatchOrders(), getDispatchRoutes(), getDispatchTrucks()]);
      const selectedOrder = allOrders.find((order) => order.id === orderId);
      const selectedRoute = allRoutes.find((route57) => route57.id === routeId);
      const selectedTruck = allTrucks.find((truck) => truck.id === (selectedRoute == null ? void 0 : selectedRoute.truckId));
      const splitCount = getSplitCountFromForm(form);
      const capacityError = !selectedOrder ? "Order was not found." : !selectedRoute ? "Select a valid route before assigning this order." : getSplitCapacityError(selectedOrder, selectedTruck, splitCount);
      if (capacityError) {
        const dispatchState2 = await loadDispatchState({
          skipSetup: true,
          fast: true
        });
        return data({
          allowed: true,
          ok: false,
          message: capacityError,
          selectedOrderId: orderId,
          ...dispatchState2
        }, {
          status: 400
        });
      }
      const assignment = await assignOrderToRoute({
        order: selectedOrder,
        routeId,
        truck: selectedTruck,
        splitCount,
        eta: String(form.get("eta") || "").trim() || null
      });
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      return data({
        allowed: true,
        ok: true,
        message: assignment.message,
        selectedOrderId: orderId,
        ...dispatchState
      });
    }
    if (intent === "notify-loader") {
      const orderId = String(form.get("orderId") || "").trim();
      const loaderNote = String(form.get("loaderNote") || "").trim();
      if (!orderId) throw new Error("Missing order selection");
      const [allOrders, allRoutes] = await Promise.all([getDispatchOrders(), getDispatchRoutes()]);
      const order = allOrders.find((entry2) => entry2.id === orderId);
      if (!order) throw new Error("Order was not found.");
      const route57 = allRoutes.find((entry2) => entry2.id === order.assignedRouteId) || null;
      const notification = await createLoaderNotification({
        order,
        route: route57,
        actor: currentUser,
        loaderNote
      });
      await logAuditEvent({
        actor: currentUser,
        action: "notify_loader",
        targetType: "dispatch_order",
        targetId: order.id,
        targetLabel: order.orderNumber || order.id,
        details: {
          notificationId: notification.id,
          message: notification.message,
          route: (route57 == null ? void 0 : route57.code) || null,
          loaderNote: loaderNote || null
        }
      });
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      return data({
        allowed: true,
        ok: true,
        message: `Sent loader alert for ${getOrderDisplayNumber$2(order)}.`,
        selectedOrderId: order.id,
        ...dispatchState
      });
    }
    if (intent === "sequence-route") {
      const routeId = String(form.get("routeId") || "").trim();
      const mode = String(form.get("sequenceMode") || "city").trim();
      if (!routeId) throw new Error("Missing route selection");
      const routeOrders = (await getDispatchOrders()).filter((order) => order.assignedRouteId === routeId).sort((a, b) => {
        if (mode === "reverse") {
          return Number(b.stopSequence || 0) - Number(a.stopSequence || 0);
        }
        if (mode === "address") {
          return `${a.address} ${a.city}`.localeCompare(`${b.address} ${b.city}`);
        }
        return `${a.city} ${a.address}`.localeCompare(`${b.city} ${b.address}`);
      });
      await Promise.all(routeOrders.map((order, index) => updateDispatchOrder(order.id, {
        stopSequence: index + 1
      })));
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      return data({
        allowed: true,
        ok: true,
        message: "Route stop sequence updated.",
        selectedOrderId: (_c = routeOrders[0]) == null ? void 0 : _c.id,
        ...dispatchState
      });
    }
    if (intent === "move-route-stop") {
      const orderId = String(form.get("orderId") || "").trim();
      const routeId = String(form.get("routeId") || "").trim();
      const direction = String(form.get("direction") || "").trim();
      if (!orderId || !routeId || !["up", "down"].includes(direction)) {
        throw new Error("Missing route stop reorder details");
      }
      const routeOrders = (await getDispatchOrders()).filter((order) => order.assignedRouteId === routeId && order.status !== "delivered" && order.deliveryStatus !== "delivered").sort((a, b) => Number(a.stopSequence || 9999) - Number(b.stopSequence || 9999) || String(a.created_at || "").localeCompare(String(b.created_at || "")));
      const currentIndex = routeOrders.findIndex((order) => order.id === orderId);
      const nextIndex = direction === "up" ? currentIndex - 1 : currentIndex + 1;
      if (currentIndex >= 0 && nextIndex >= 0 && nextIndex < routeOrders.length) {
        const reordered = [...routeOrders];
        const [movedOrder] = reordered.splice(currentIndex, 1);
        reordered.splice(nextIndex, 0, movedOrder);
        await Promise.all(reordered.map((order, index) => Number(order.stopSequence || 0) === index + 1 ? Promise.resolve(order) : updateDispatchOrder(order.id, {
          stopSequence: index + 1
        })));
      }
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      return data({
        allowed: true,
        ok: true,
        message: "Route stop order updated.",
        selectedOrderId: orderId,
        ...dispatchState
      });
    }
    if (intent === "hold-order") {
      const orderId = String(form.get("orderId") || "").trim();
      if (!orderId) throw new Error("Missing order selection");
      await updateDispatchOrder(orderId, {
        status: "hold",
        assignedRouteId: null,
        stopSequence: null,
        deliveryStatus: "not_started",
        eta: null
      });
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      return data({
        allowed: true,
        ok: true,
        message: "Order moved to hold.",
        selectedOrderId: orderId,
        ...dispatchState
      });
    }
    if (intent === "unassign-order") {
      const orderId = String(form.get("orderId") || "").trim();
      if (!orderId) throw new Error("Missing order selection");
      const existingOrder = (await getDispatchOrders()).find((order) => order.id === orderId);
      await updateDispatchOrder(orderId, {
        status: "new",
        assignedRouteId: null,
        stopSequence: null,
        deliveryStatus: "not_started",
        eta: null
      });
      await resequenceChangedRoutes([existingOrder == null ? void 0 : existingOrder.assignedRouteId]);
      const dispatchState = await loadDispatchState({
        skipSetup: true,
        fast: true
      });
      return data({
        allowed: true,
        ok: true,
        message: "Order moved back to inbox.",
        selectedOrderId: orderId,
        ...dispatchState
      });
    }
    if (intent === "undo-delivered") {
      const orderId = String(form.get("orderId") || "").trim();
      if (!orderId) throw new Error("Missing order selection");
      const existingOrder = (await getDispatchOrders()).find((order) => order.id === orderId);
      const nextStatus = (existingOrder == null ? void 0 : existingOrder.assignedRouteId) ? "scheduled" : "new";
      await updateDispatchOrder(orderId, {
        status: nextStatus,
        deliveryStatus: "not_started",
        arrivedAt: null,
        departedAt: null,
        deliveredAt: null
      });
      await resequenceChangedRoutes([existingOrder == null ? void 0 : existingOrder.assignedRouteId]);
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: nextStatus === "scheduled" ? "Load marked undelivered and returned to its route." : "Load marked undelivered and returned to the intake queue.",
        selectedOrderId: orderId,
        ...dispatchState
      });
    }
    if (intent === "update-stop-status") {
      const orderId = String(form.get("orderId") || "").trim();
      const rawDeliveryStatus = String(form.get("deliveryStatus") || "").trim();
      const deliveryStatus = rawDeliveryStatus === "en_route" || rawDeliveryStatus === "delivered" || rawDeliveryStatus === "not_started" ? rawDeliveryStatus : "not_started";
      if (!orderId) throw new Error("Missing order selection");
      const now = (/* @__PURE__ */ new Date()).toISOString();
      const patch = {
        deliveryStatus,
        proofName: String(form.get("proofName") || "").trim() || null,
        proofNotes: String(form.get("proofNotes") || "").trim() || null,
        signatureName: String(form.get("signatureName") || "").trim() || null,
        signatureData: String(form.get("signatureData") || "").trim() || null,
        photoUrls: String(form.get("photoUrls") || "").trim() || null,
        inspectionStatus: String(form.get("inspectionStatus") || "").trim() || null,
        checklistJson: buildChecklistJson$1(form)
      };
      if (deliveryStatus === "en_route") patch.departedAt = now;
      if (deliveryStatus === "delivered") {
        patch.status = "delivered";
        patch.departedAt = patch.departedAt || now;
        patch.deliveredAt = now;
      }
      const updatedOrder = await updateDispatchOrder(orderId, patch);
      const route57 = updatedOrder.assignedRouteId ? (await getDispatchRoutes()).find((entry2) => entry2.id === updatedOrder.assignedRouteId) || null : null;
      let emailNote = "";
      if (deliveryStatus === "delivered") {
        try {
          const emailResult = await sendDeliveryConfirmationEmail({
            order: updatedOrder,
            route: route57
          });
          emailNote = emailResult.sent ? " Delivery confirmation email sent." : ` Delivery confirmation email skipped: ${emailResult.reason}`;
        } catch (error) {
          const message = error instanceof Error ? error.message : "Unknown email error.";
          emailNote = ` Delivery confirmation email failed: ${message}`;
        }
      }
      const dispatchState = await loadDispatchState();
      return data({
        allowed: true,
        ok: true,
        message: `Stop marked ${getDeliveryStatusLabel(deliveryStatus).toLowerCase()}.${emailNote}`,
        selectedOrderId: orderId,
        ...dispatchState
      });
    }
    return data({
      allowed: true,
      ok: false,
      message: "Unknown dispatch action."
    }, {
      status: 400
    });
  } catch (error) {
    const dispatchState = await loadDispatchState();
    const message = error instanceof Error ? error.message : "Dispatch action failed";
    return data({
      allowed: true,
      ok: false,
      message,
      ...dispatchState
    }, {
      status: 500
    });
  }
}
const dispatch = UNSAFE_withComponentProps(function DispatchPage() {
  var _a2, _b, _c;
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const assignmentFetcher = useFetcher();
  const location = useLocation();
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const currentUser = (actionData == null ? void 0 : actionData.currentUser) ?? loaderData.currentUser ?? null;
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const quoteHref = isEmbeddedRoute ? "/app/custom-quote" : "/custom-quote";
  const reviewHref = isEmbeddedRoute ? "/app/quote-review" : "/quote-review";
  const mobileHref = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  const dispatchHref = isEmbeddedRoute ? "/app/dispatch" : "/dispatch";
  const dailyExportHref = `${dispatchHref}/export-day.xls?date=${getTodayDateInputValue$1()}`;
  const classicHref = isEmbeddedRoute ? "/app/classic" : "/classic";
  const monitorHref = isEmbeddedRoute ? "/app/monitor" : "/monitor";
  const calendarHref = isEmbeddedRoute ? "/app/calendar" : "/calendar";
  const allotmentHref = isEmbeddedRoute ? "/app/allotment" : "/allotment";
  const loaderHref = isEmbeddedRoute ? "/app/loader" : "/loader";
  const driverHref = isEmbeddedRoute ? "/app/dispatch/driver" : "/dispatch/driver";
  const logoutHref = `${dispatchHref}?logout=1`;
  const loginHref = `/login?next=${encodeURIComponent(location.pathname + location.search)}`;
  const canAccess = (permission) => {
    var _a3;
    return !currentUser || ((_a3 = currentUser.permissions) == null ? void 0 : _a3.includes(permission));
  };
  const canManageDispatch = canAccess("manageDispatch");
  const orders = (actionData == null ? void 0 : actionData.orders) ?? loaderData.orders ?? [];
  const dispatchRoutes = (actionData == null ? void 0 : actionData.routes) ?? loaderData.routes ?? [];
  const trucks = (actionData == null ? void 0 : actionData.trucks) ?? loaderData.trucks ?? [];
  const employees = (actionData == null ? void 0 : actionData.employees) ?? loaderData.employees ?? [];
  const materialOptions = (actionData == null ? void 0 : actionData.materialOptions) ?? loaderData.materialOptions ?? [];
  const storageReady = (actionData == null ? void 0 : actionData.storageReady) ?? loaderData.storageReady ?? false;
  const storageError = (actionData == null ? void 0 : actionData.storageError) ?? loaderData.storageError ?? null;
  const mailboxStatus = (actionData == null ? void 0 : actionData.mailboxStatus) ?? loaderData.mailboxStatus ?? null;
  const shopifyImportStatus = (actionData == null ? void 0 : actionData.shopifyImportStatus) ?? null;
  const googleMapsApiKey = (actionData == null ? void 0 : actionData.googleMapsApiKey) ?? loaderData.googleMapsApiKey ?? "";
  const mapOriginAddress = (actionData == null ? void 0 : actionData.mapOriginAddress) ?? loaderData.mapOriginAddress ?? "";
  const searchParams = new URLSearchParams(location.search);
  const rawView = searchParams.get("view") || "dashboard";
  const activeView = rawView === "orders" || rawView === "scheduled" || rawView === "routes" || rawView === "trucks" || rawView === "employees" || rawView === "delivered" ? rawView : "dashboard";
  useEffect(() => {
    if (!allowed || !googleMapsApiKey || activeView !== "orders") return;
    loadGooglePlaces(googleMapsApiKey).then(() => {
      attachAddressAutocomplete({
        address1Id: "dispatch-address",
        cityId: "dispatch-city",
        provinceId: "dispatch-province",
        postalCodeId: "dispatch-postal-code",
        countryId: "dispatch-country",
        cityFormat: "cityStateZip"
      });
    }).catch((error) => {
      console.error("[DISPATCH GOOGLE PLACES LOAD ERROR]", error);
    });
  }, [allowed, googleMapsApiKey, activeView]);
  const querySelectedOrderId = searchParams.get("order");
  const queryDashboardSelectedOrderId = searchParams.get("selected");
  const selectedOrderId = (actionData == null ? void 0 : actionData.selectedOrderId) || queryDashboardSelectedOrderId || querySelectedOrderId || ((_a2 = orders[0]) == null ? void 0 : _a2.id);
  const selectedOrder = useMemo(() => orders.find((order) => order.id === selectedOrderId) || orders[0] || null, [orders, selectedOrderId]);
  const routes2 = useMemo(() => dispatchRoutes.map((route57) => {
    const routeOrders = orders.filter((order) => order.assignedRouteId === route57.id && order.status !== "delivered" && order.deliveryStatus !== "delivered").sort((a, b) => Number(a.stopSequence || 9999) - Number(b.stopSequence || 9999));
    return {
      ...route57,
      stops: routeOrders.length,
      totalTravelMinutes: routeOrders.reduce((sum, order) => sum + getOrderTravelMinutes(order), 0),
      loadSummary: routeOrders.map((order) => `${order.quantity} ${order.unit} ${order.material}`).slice(0, 2).join(" • "),
      orders: routeOrders
    };
  }), [dispatchRoutes, orders]);
  const selectedOrderRoute = (selectedOrder == null ? void 0 : selectedOrder.assignedRouteId) ? routes2.find((route57) => route57.id === selectedOrder.assignedRouteId) || null : null;
  const activeOrders = useMemo(() => orders.filter((order) => !order.assignedRouteId && order.status !== "scheduled" && order.status !== "delivered" && order.deliveryStatus !== "delivered").sort((a, b) => {
    const dateDiff = getRequestedDeliverySortValue(a) - getRequestedDeliverySortValue(b);
    if (dateDiff !== 0) return dateDiff;
    return String(a.created_at || "").localeCompare(String(b.created_at || ""));
  }), [orders]);
  const inboxOrders = orders.filter((order) => !order.assignedRouteId && order.status === "new");
  const holdOrders = orders.filter((order) => order.status === "hold");
  const scheduledOrders = useMemo(() => orders.filter((order) => order.assignedRouteId && order.status !== "delivered" && order.deliveryStatus !== "delivered").sort((a, b) => {
    const dateDiff = getRequestedDeliverySortValue(a) - getRequestedDeliverySortValue(b);
    if (dateDiff !== 0) return dateDiff;
    return Number(a.stopSequence || 9999) - Number(b.stopSequence || 9999);
  }), [orders]);
  const deliveredOrders = orders.filter((order) => order.status === "delivered" || order.deliveryStatus === "delivered");
  const drivers = employees.filter((employee) => employee.role === "driver");
  const helpers = employees.filter((employee) => employee.role === "helper");
  const dispatchViewHref = (view) => `${dispatchHref}?view=${view}`;
  const rawReturnTo = searchParams.get("returnTo") || "";
  const modalReturnHref = rawReturnTo.startsWith("/") ? rawReturnTo : dispatchViewHref("orders");
  const dashboardSelectHref = (orderId) => `${dispatchHref}?selected=${encodeURIComponent(orderId)}`;
  const dashboardDetailHref = (orderId) => `${dispatchHref}?order=${encodeURIComponent(orderId)}&detail=1`;
  const orderEditorOpen = activeView === "orders" && Boolean(selectedOrder && (querySelectedOrderId || (actionData == null ? void 0 : actionData.selectedOrderId)));
  const dispatchDetailOpen = activeView === "dashboard" && searchParams.get("detail") === "1" && Boolean(selectedOrder && querySelectedOrderId);
  const deliveredDetailOpen = activeView === "delivered" && searchParams.get("detail") === "1" && Boolean(selectedOrder && querySelectedOrderId);
  const [draggedOrderId, setDraggedOrderId] = useState(null);
  const [dragOverRouteId, setDragOverRouteId] = useState(null);
  const [dragOverQueue, setDragOverQueue] = useState(false);
  const [orderSearch, setOrderSearch] = useState("");
  const [navCollapsed, setNavCollapsed] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(DISPATCH_NAV_COLLAPSED_KEY$4) === "1";
  });
  const [manualRoutingMode, setManualRoutingMode] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(DISPATCH_MANUAL_ROUTING_KEY) === "1";
  });
  const deferredOrderSearch = useDeferredValue(orderSearch);
  const normalizedOrderSearch = deferredOrderSearch.trim().toLowerCase();
  assignmentFetcher.state === "submitting";
  function getRouteTruck(route57) {
    if (!route57) return null;
    return trucks.find((truck) => truck.id === route57.truckId) || trucks.find((truck) => truck.label === route57.truck) || null;
  }
  function getAssignmentSplitCount(order, route57) {
    if (!order || !route57 || !/yards?/i.test(order.unit)) return "";
    const truck = getRouteTruck(route57);
    const capacity = Number((truck == null ? void 0 : truck.yards) || 30);
    const quantity = Number(order.quantity || 0);
    if (!quantity || !capacity || quantity <= capacity) return "";
    const minimumSplits = Math.ceil(quantity / capacity);
    const routeLabel = `${route57.code}${(truck == null ? void 0 : truck.label) ? ` / ${truck.label}` : ""}`;
    const answer = window.prompt(`${getOrderDisplayNumber$2(order)} is ${quantity} yards, which is over the ${capacity} yard truck limit for ${routeLabel}.

How many tickets should I split it into?`, String(minimumSplits));
    if (answer === null) return null;
    const splitCount = Math.floor(Number(answer));
    if (!Number.isFinite(splitCount) || splitCount < minimumSplits) {
      window.alert(`Use at least ${minimumSplits} split tickets so each load fits the truck.`);
      return null;
    }
    return String(splitCount);
  }
  function prepareAssignmentSubmit(order, routeId, form, event) {
    const route57 = routes2.find((entry2) => entry2.id === routeId);
    const splitCount = getAssignmentSplitCount(order, route57);
    if (splitCount === null) {
      event.preventDefault();
      return;
    }
    let input = form.querySelector('input[name="splitCount"]');
    if (!input) {
      input = document.createElement("input");
      input.type = "hidden";
      input.name = "splitCount";
      form.appendChild(input);
    }
    input.value = splitCount;
  }
  function toggleNavCollapsed() {
    setNavCollapsed((current) => {
      const next = !current;
      try {
        window.localStorage.setItem(DISPATCH_NAV_COLLAPSED_KEY$4, next ? "1" : "0");
      } catch {
      }
      return next;
    });
  }
  function toggleManualRoutingMode() {
    setManualRoutingMode((current) => {
      const next = !current;
      try {
        window.localStorage.setItem(DISPATCH_MANUAL_ROUTING_KEY, next ? "1" : "0");
      } catch {
      }
      return next;
    });
  }
  const searchOrders = (items) => {
    if (!normalizedOrderSearch) return items;
    return items.filter((order) => buildDispatchOrderSearchText(order, routes2).includes(normalizedOrderSearch));
  };
  const searchedActiveOrders = useMemo(() => searchOrders(activeOrders), [activeOrders, normalizedOrderSearch, routes2]);
  const selectedActiveOrderIndex = searchedActiveOrders.findIndex((order) => order.id === (selectedOrder == null ? void 0 : selectedOrder.id));
  const previousOrderHref = selectedActiveOrderIndex > 0 ? `${dispatchHref}?view=orders&order=${encodeURIComponent(searchedActiveOrders[selectedActiveOrderIndex - 1].id)}` : "";
  const nextOrderHref = selectedActiveOrderIndex >= 0 && selectedActiveOrderIndex < searchedActiveOrders.length - 1 ? `${dispatchHref}?view=orders&order=${encodeURIComponent(searchedActiveOrders[selectedActiveOrderIndex + 1].id)}` : "";
  const searchedScheduledOrders = useMemo(() => searchOrders(scheduledOrders), [scheduledOrders, normalizedOrderSearch, routes2]);
  const searchedDeliveredOrders = useMemo(() => searchOrders(deliveredOrders), [deliveredOrders, normalizedOrderSearch, routes2]);
  const searchedRoutes = useMemo(() => {
    if (!normalizedOrderSearch) return routes2;
    return routes2.filter((route57) => buildRouteSearchText(route57, orders).includes(normalizedOrderSearch));
  }, [normalizedOrderSearch, orders, routes2]);
  const searchedTrucks = useMemo(() => {
    if (!normalizedOrderSearch) return trucks;
    return trucks.filter((truck) => buildTruckSearchText(truck, routes2, orders).includes(normalizedOrderSearch));
  }, [normalizedOrderSearch, orders, routes2, trucks]);
  const searchedEmployees = useMemo(() => {
    if (!normalizedOrderSearch) return employees;
    return employees.filter((employee) => buildEmployeeSearchText(employee, routes2, orders).includes(normalizedOrderSearch));
  }, [employees, normalizedOrderSearch, orders, routes2]);
  const renderSearchBar = (placeholder) => /* @__PURE__ */ jsxs("div", {
    style: styles$b.searchBar,
    children: [/* @__PURE__ */ jsx("input", {
      type: "search",
      value: orderSearch,
      onChange: (event) => setOrderSearch(event.currentTarget.value),
      placeholder,
      style: styles$b.searchInput
    }), orderSearch ? /* @__PURE__ */ jsx("button", {
      type: "button",
      onClick: () => setOrderSearch(""),
      style: styles$b.clearSearchButton,
      children: "Clear"
    }) : null]
  });
  function getDraggedOrderId(event) {
    return event.dataTransfer.getData("application/x-dispatch-order-id") || event.dataTransfer.getData("text/plain") || draggedOrderId || "";
  }
  function startOrderDrag(orderId, event) {
    if (!canManageDispatch) return;
    setDraggedOrderId(orderId);
    event.dataTransfer.effectAllowed = "move";
    event.dataTransfer.setData("application/x-dispatch-order-id", orderId);
    event.dataTransfer.setData("text/plain", orderId);
  }
  function clearDragState() {
    setDraggedOrderId(null);
    setDragOverRouteId(null);
    setDragOverQueue(false);
  }
  function assignDraggedOrder(routeId, event) {
    event.preventDefault();
    const orderId = getDraggedOrderId(event);
    clearDragState();
    if (!orderId || !routeId || !canManageDispatch) return;
    const order = orders.find((entry2) => entry2.id === orderId);
    const route57 = routes2.find((entry2) => entry2.id === routeId);
    const splitCount = getAssignmentSplitCount(order, route57);
    if (splitCount === null) return;
    assignmentFetcher.submit({
      intent: "assign-order",
      orderId,
      routeId,
      eta: "",
      splitCount
    }, {
      method: "post",
      action: `${dispatchHref}${location.search || ""}`
    });
  }
  function unassignDraggedOrder(event) {
    event.preventDefault();
    const orderId = getDraggedOrderId(event);
    clearDragState();
    if (!orderId || !canManageDispatch) return;
    assignmentFetcher.submit({
      intent: "unassign-order",
      orderId
    }, {
      method: "post",
      action: `${dispatchHref}${location.search || ""}`
    });
  }
  function moveOrderWithSelect(orderId, routeId) {
    if (!orderId || !canManageDispatch) return;
    const order = orders.find((entry2) => entry2.id === orderId);
    const route57 = routes2.find((entry2) => entry2.id === routeId);
    const splitCount = routeId ? getAssignmentSplitCount(order, route57) : "";
    if (splitCount === null) return;
    assignmentFetcher.submit(routeId ? {
      intent: "assign-order",
      orderId,
      routeId,
      eta: "",
      splitCount
    } : {
      intent: "unassign-order",
      orderId
    }, {
      method: "post",
      action: `${dispatchHref}${location.search || ""}`
    });
  }
  useEffect(() => {
    if (assignmentFetcher.state === "idle") {
      clearDragState();
    }
  }, [assignmentFetcher.state]);
  if (!allowed) {
    return /* @__PURE__ */ jsx("div", {
      style: styles$b.page,
      children: /* @__PURE__ */ jsx("div", {
        style: {
          ...styles$b.shell,
          maxWidth: 520
        },
        children: /* @__PURE__ */ jsxs("div", {
          style: styles$b.loginCard,
          children: [/* @__PURE__ */ jsx("h1", {
            style: styles$b.title,
            children: "Dispatch"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$b.subtitle,
            children: "Sign in with your contractor user account to open the dispatch workspace."
          }), /* @__PURE__ */ jsx("a", {
            href: loginHref,
            style: {
              ...styles$b.primaryButton,
              width: "100%",
              marginTop: 16,
              minHeight: 50,
              display: "inline-flex",
              alignItems: "center",
              justifyContent: "center",
              textDecoration: "none"
            },
            children: "Sign In"
          })]
        })
      })
    });
  }
  return /* @__PURE__ */ jsxs("div", {
    style: styles$b.page,
    children: [/* @__PURE__ */ jsx("style", {
      children: `
          @media (max-width: 900px) {
            .dispatch-frame {
              display: block !important;
            }

            .dispatch-sidebar {
              position: sticky !important;
              top: 0 !important;
              z-index: 30 !important;
              min-height: auto !important;
              height: auto !important;
              border-right: 0 !important;
              border-bottom: 1px solid #1e293b !important;
            }

            .dispatch-shell {
              padding: 10px !important;
            }

            .dispatch-hero {
              grid-template-columns: minmax(0, 1fr) !important;
            }
          }
        `
    }), /* @__PURE__ */ jsxs("div", {
      className: "dispatch-frame",
      style: {
        ...styles$b.appFrame,
        gridTemplateColumns: navCollapsed ? "56px minmax(0, 1fr)" : "230px minmax(0, 1fr)"
      },
      children: [/* @__PURE__ */ jsxs("aside", {
        className: "dispatch-sidebar",
        style: {
          ...styles$b.sidebar,
          padding: navCollapsed ? "12px 8px" : "16px 14px"
        },
        children: [/* @__PURE__ */ jsx("button", {
          type: "button",
          onClick: toggleNavCollapsed,
          style: styles$b.navToggle,
          title: navCollapsed ? "Open navigation" : "Close navigation",
          children: navCollapsed ? ">" : "<"
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$b.brandBlock,
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$b.brandMark,
            children: /* @__PURE__ */ jsx("img", {
              src: "/green-hills-logo.png",
              alt: "Green Hills Supply",
              style: styles$b.brandLogo
            })
          }), /* @__PURE__ */ jsxs("div", {
            style: navCollapsed ? styles$b.collapsedOnlyHidden : void 0,
            children: [/* @__PURE__ */ jsx("div", {
              style: styles$b.brandTitle,
              children: "Contractor"
            }), /* @__PURE__ */ jsx("div", {
              style: styles$b.brandSub,
              children: "Dispatch v2.0"
            })]
          })]
        }), /* @__PURE__ */ jsxs("nav", {
          style: navCollapsed ? styles$b.collapsedOnlyHidden : styles$b.sideNav,
          children: [/* @__PURE__ */ jsx("a", {
            href: classicHref,
            style: styles$b.sideNavLink(false),
            children: "Classic"
          }), /* @__PURE__ */ jsx("a", {
            href: monitorHref,
            style: styles$b.sideNavLink(false),
            children: "Monitor"
          }), /* @__PURE__ */ jsx("a", {
            href: calendarHref,
            style: styles$b.sideNavLink(false),
            children: "Calendar"
          }), /* @__PURE__ */ jsx("a", {
            href: allotmentHref,
            style: styles$b.sideNavLink(false),
            children: "Allotment"
          }), canAccess("loader") ? /* @__PURE__ */ jsx("a", {
            href: loaderHref,
            style: styles$b.sideNavLink(false),
            children: "Loader"
          }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx("a", {
            href: dispatchViewHref("orders"),
            style: styles$b.sideNavLink(activeView === "orders"),
            children: "Orders"
          }) : null, /* @__PURE__ */ jsx("a", {
            href: dispatchViewHref("scheduled"),
            style: styles$b.sideNavLink(activeView === "scheduled"),
            children: "Scheduled"
          }), canAccess("manageDispatch") ? /* @__PURE__ */ jsx("a", {
            href: dispatchViewHref("routes"),
            style: styles$b.sideNavLink(activeView === "routes"),
            children: "Routes"
          }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx("a", {
            href: dispatchViewHref("trucks"),
            style: styles$b.sideNavLink(activeView === "trucks"),
            children: "Trucks"
          }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx("a", {
            href: dispatchViewHref("employees"),
            style: styles$b.sideNavLink(activeView === "employees"),
            children: "Employees"
          }) : null, /* @__PURE__ */ jsx("a", {
            href: dispatchViewHref("delivered"),
            style: styles$b.sideNavLink(activeView === "delivered"),
            children: "Delivered"
          }), /* @__PURE__ */ jsx("a", {
            href: "https://www.ghstickets.info",
            style: styles$b.sideNavLink(false),
            children: "Ticket Creator"
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: navCollapsed ? styles$b.collapsedOnlyHidden : styles$b.sidebarFooter,
          children: [canAccess("driver") ? /* @__PURE__ */ jsx("a", {
            href: driverHref,
            style: styles$b.sideUtility,
            children: "Driver Route"
          }) : null, canAccess("loader") ? /* @__PURE__ */ jsx("a", {
            href: loaderHref,
            style: styles$b.sideUtility,
            children: "Loader View"
          }) : null, canAccess("quoteTool") ? /* @__PURE__ */ jsx("a", {
            href: quoteHref,
            style: styles$b.sideUtility,
            children: "Quote Tool"
          }) : null, canAccess("manageUsers") ? /* @__PURE__ */ jsx("a", {
            href: "/settings",
            style: styles$b.sideUtility,
            children: "Settings"
          }) : null, currentUser ? /* @__PURE__ */ jsx("a", {
            href: "/change-password",
            style: styles$b.sideUtility,
            children: "Change Password"
          }) : null, /* @__PURE__ */ jsx("a", {
            href: currentUser ? "/login?logout=1" : logoutHref,
            style: styles$b.sideUtility,
            children: "Log Out"
          })]
        })]
      }), /* @__PURE__ */ jsxs("main", {
        className: "dispatch-shell",
        style: styles$b.shell,
        children: [/* @__PURE__ */ jsxs("div", {
          id: "dashboard",
          className: "dispatch-hero",
          style: styles$b.hero,
          children: [/* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsx("div", {
              style: styles$b.kicker,
              children: "Dispatch Workspace"
            }), /* @__PURE__ */ jsx("h1", {
              style: styles$b.title,
              children: "Plan, intake, and assign deliveries"
            }), /* @__PURE__ */ jsx("p", {
              style: styles$b.subtitle,
              children: "Live contractor operations board for mailbox intake, routing, trucks, crews, and field proof."
            })]
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$b.heroActions,
            children: [/* @__PURE__ */ jsx("a", {
              href: mobileHref,
              style: styles$b.ghostButton,
              children: "Dashboard"
            }), /* @__PURE__ */ jsx("a", {
              href: reviewHref,
              style: styles$b.ghostButton,
              children: "Review Quotes"
            }), /* @__PURE__ */ jsx("a", {
              href: driverHref,
              style: styles$b.ghostButton,
              children: "Driver View"
            }), /* @__PURE__ */ jsx("a", {
              href: dailyExportHref,
              style: styles$b.ghostButton,
              children: "Export Shopify Today"
            })]
          })]
        }), !storageReady ? /* @__PURE__ */ jsxs("div", {
          style: styles$b.statusWarn,
          children: ["Dispatch storage is not ready yet. Run", " ", /* @__PURE__ */ jsx("strong", {
            children: "`dispatch_schema.sql`"
          }), " ", "in Supabase SQL Editor, then refresh. Until then, you are seeing seed data.", storageError ? ` Storage error: ${storageError}` : ""]
        }) : null, (actionData == null ? void 0 : actionData.message) ? /* @__PURE__ */ jsx("div", {
          style: actionData.ok ? styles$b.statusOk : styles$b.statusErr,
          children: actionData.message
        }) : null, mailboxStatus ? /* @__PURE__ */ jsxs("div", {
          style: mailboxStatus.configured ? styles$b.statusOk : styles$b.statusWarn,
          children: [mailboxStatus.message, ((_b = mailboxStatus.skipReasons) == null ? void 0 : _b.length) ? /* @__PURE__ */ jsxs("div", {
            style: styles$b.skipReasonList,
            children: [mailboxStatus.skipReasons.slice(0, 5).map((item) => /* @__PURE__ */ jsxs("div", {
              style: styles$b.skipReasonItem,
              children: [/* @__PURE__ */ jsx("strong", {
                children: item.subject
              }), /* @__PURE__ */ jsx("span", {
                children: item.reason
              })]
            }, `${item.uid}-${item.reason}`)), mailboxStatus.skipReasons.length > 5 ? /* @__PURE__ */ jsxs("div", {
              style: styles$b.skipReasonItem,
              children: [/* @__PURE__ */ jsx("strong", {
                children: "More skipped emails"
              }), /* @__PURE__ */ jsxs("span", {
                children: [mailboxStatus.skipReasons.length - 5, " additional skipped emails not shown."]
              })]
            }) : null]
          }) : null]
        }) : null, shopifyImportStatus ? /* @__PURE__ */ jsxs("div", {
          style: shopifyImportStatus.configured ? styles$b.statusOk : styles$b.statusWarn,
          children: [shopifyImportStatus.message, ((_c = shopifyImportStatus.skipReasons) == null ? void 0 : _c.length) ? /* @__PURE__ */ jsxs("div", {
            style: styles$b.skipReasonList,
            children: [shopifyImportStatus.skipReasons.slice(0, 5).map((item) => /* @__PURE__ */ jsx("div", {
              style: styles$b.skipReasonItem,
              children: /* @__PURE__ */ jsx("span", {
                children: item
              })
            }, item)), shopifyImportStatus.skipReasons.length > 5 ? /* @__PURE__ */ jsx("div", {
              style: styles$b.skipReasonItem,
              children: /* @__PURE__ */ jsxs("span", {
                children: [shopifyImportStatus.skipReasons.length - 5, " additional skipped Shopify orders not shown."]
              })
            }) : null]
          }) : null]
        }) : null, /* @__PURE__ */ jsxs("div", {
          style: styles$b.metricsGrid,
          children: [metricCard("Inbox", String(inboxOrders.length), "#f97316"), metricCard("Scheduled", String(scheduledOrders.length), "#22c55e"), metricCard("On Hold", String(holdOrders.length), "#eab308"), metricCard("Delivered", String(deliveredOrders.length), "#38bdf8")]
        }), activeView === "orders" ? /* @__PURE__ */ jsxs("div", {
          style: styles$b.focusGrid,
          children: [/* @__PURE__ */ jsxs("div", {
            id: "orders",
            onDragOver: (event) => {
              if (!draggedOrderId || !canManageDispatch) return;
              event.preventDefault();
              event.dataTransfer.dropEffect = "move";
              setDragOverQueue(true);
            },
            onDragLeave: () => setDragOverQueue(false),
            onDrop: unassignDraggedOrder,
            style: {
              ...styles$b.panel,
              ...dragOverQueue ? styles$b.queueDropActive : {}
            },
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$b.panelHeader,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Orders"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "View imported, manual, and held dispatch orders that still need scheduling."
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.headerPill,
                children: [searchedActiveOrders.length, " orders"]
              })]
            }), renderSearchBar("Search orders by any field: customer, address, material, notes, date, route, status..."), /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gap: 10
              },
              children: [searchedActiveOrders.map((order) => {
                const route57 = routes2.find((entry2) => entry2.id === order.assignedRouteId);
                return /* @__PURE__ */ jsxs("a", {
                  href: `${dispatchHref}?view=orders&order=${encodeURIComponent(order.id)}`,
                  style: {
                    ...styles$b.queueCard,
                    textDecoration: "none"
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    style: {
                      display: "flex",
                      justifyContent: "space-between",
                      gap: 12
                    },
                    children: [/* @__PURE__ */ jsxs("div", {
                      children: [/* @__PURE__ */ jsx("div", {
                        style: styles$b.queueTitle,
                        children: order.customer
                      }), /* @__PURE__ */ jsxs("div", {
                        style: styles$b.queueMeta,
                        children: [order.address, ", ", order.city]
                      })]
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.badge(order.status),
                      children: order.status
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: styles$b.queueDetails,
                    children: [/* @__PURE__ */ jsx("span", {
                      children: getOrderDisplayNumber$2(order)
                    }), /* @__PURE__ */ jsxs("span", {
                      children: [order.quantity, " ", order.unit]
                    }), /* @__PURE__ */ jsx("span", {
                      children: order.material
                    }), order.travelMinutes ? /* @__PURE__ */ jsxs("span", {
                      children: [order.travelMinutes, " min RT"]
                    }) : null, /* @__PURE__ */ jsx("span", {
                      children: route57 ? route57.truck : "Unassigned"
                    })]
                  })]
                }, order.id);
              }), searchedActiveOrders.length === 0 ? /* @__PURE__ */ jsx("div", {
                style: styles$b.emptySearch,
                children: "No orders matched that search."
              }) : null]
            })]
          }), /* @__PURE__ */ jsx("div", {
            style: styles$b.panel,
            children: orderEditorOpen && selectedOrder ? /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gap: 14
              },
              children: [/* @__PURE__ */ jsxs("div", {
                style: styles$b.modalHeader,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("h2", {
                    style: styles$b.panelTitle,
                    children: "Edit Selected Order"
                  }), /* @__PURE__ */ jsx("p", {
                    style: styles$b.panelSub,
                    children: "Update this dispatch card."
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.editorNavActions,
                  children: [/* @__PURE__ */ jsx("a", {
                    href: previousOrderHref || "#",
                    "aria-disabled": !previousOrderHref,
                    style: previousOrderHref ? styles$b.modalCloseButton : styles$b.disabledNavButton,
                    children: "←"
                  }), /* @__PURE__ */ jsx("a", {
                    href: nextOrderHref || "#",
                    "aria-disabled": !nextOrderHref,
                    style: nextOrderHref ? styles$b.modalCloseButton : styles$b.disabledNavButton,
                    children: "→"
                  }), /* @__PURE__ */ jsx("a", {
                    href: modalReturnHref,
                    style: styles$b.modalCloseButton,
                    children: "Close"
                  })]
                })]
              }), /* @__PURE__ */ jsxs(Form, {
                method: "post",
                action: dispatchViewHref("orders"),
                style: {
                  display: "grid",
                  gap: 12
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "intent",
                  value: "update-order"
                }), /* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "orderId",
                  value: selectedOrder.id
                }), /* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "returnTo",
                  value: rawReturnTo
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Order Number"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "orderNumber",
                      defaultValue: selectedOrder.orderNumber || "",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Internal Dispatch ID"
                    }), /* @__PURE__ */ jsx("input", {
                      value: selectedOrder.id,
                      readOnly: true,
                      style: {
                        ...styles$b.input,
                        opacity: 0.75
                      }
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Customer"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "customer",
                      defaultValue: selectedOrder.customer,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Contact / Email"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "contact",
                      defaultValue: selectedOrder.contact,
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Address"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "address",
                      defaultValue: selectedOrder.address,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "City"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "city",
                      defaultValue: selectedOrder.city,
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridThree,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Material"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "material",
                      defaultValue: selectedOrder.material,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Quantity"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "quantity",
                      defaultValue: selectedOrder.quantity,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Unit"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "unit",
                      defaultValue: selectedOrder.unit,
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Requested Window"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "requestedWindow",
                      type: "date",
                      defaultValue: getRequestedWindowDateInputValue(selectedOrder.requestedWindow),
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Time Preference"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "timePreference",
                      defaultValue: selectedOrder.timePreference || "Anytime",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "Anytime",
                        children: "Anytime"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Morning",
                        children: "Morning"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Afternoon",
                        children: "Afternoon"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Evening",
                        children: "Evening"
                      })]
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Status"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "status",
                      defaultValue: selectedOrder.status,
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "new",
                        children: "New"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "scheduled",
                        children: "Scheduled"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "hold",
                        children: "Hold"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "cancelled",
                        children: "Cancelled"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "delivered",
                        children: "Delivered"
                      })]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Truck Preference"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "truckPreference",
                      defaultValue: selectedOrder.truckPreference || "",
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Assigned Route / Driver"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "routeId",
                      defaultValue: selectedOrder.assignedRouteId || "",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "",
                        children: "Unassigned"
                      }), routes2.map((route57) => /* @__PURE__ */ jsxs("option", {
                        value: route57.id,
                        children: [route57.code, " - ", route57.truck, " - ", route57.driver]
                      }, route57.id))]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "ETA"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "eta",
                      defaultValue: selectedOrder.eta || "",
                      placeholder: "Optional",
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Notes"
                  }), /* @__PURE__ */ jsx("textarea", {
                    name: "notes",
                    rows: 4,
                    defaultValue: selectedOrder.notes,
                    style: {
                      ...styles$b.input,
                      resize: "vertical"
                    }
                  })]
                }), /* @__PURE__ */ jsx("button", {
                  type: "submit",
                  style: styles$b.primaryButton,
                  children: "Save Order Changes"
                })]
              }), /* @__PURE__ */ jsxs(Form, {
                method: "post",
                action: dispatchViewHref("orders"),
                onSubmit: (event) => {
                  if (!window.confirm("Delete this order? This cannot be undone.")) {
                    event.preventDefault();
                  }
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "intent",
                  value: "delete-order"
                }), /* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "orderId",
                  value: selectedOrder.id
                }), /* @__PURE__ */ jsx("button", {
                  type: "submit",
                  style: styles$b.dangerButton,
                  children: "Delete Order"
                })]
              })]
            }) : /* @__PURE__ */ jsxs(Fragment, {
              children: [/* @__PURE__ */ jsx("div", {
                style: styles$b.panelHeader,
                children: /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("h2", {
                    style: styles$b.panelTitle,
                    children: "Add / Import Order"
                  }), /* @__PURE__ */ jsx("p", {
                    style: styles$b.panelSub,
                    children: "Create a dispatch card manually or poll the mailbox."
                  })]
                })
              }), /* @__PURE__ */ jsxs(Form, {
                method: "post",
                style: {
                  display: "grid",
                  gap: 12,
                  marginBottom: 16
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "intent",
                  value: "poll-shopify-orders"
                }), /* @__PURE__ */ jsx("button", {
                  type: "submit",
                  style: styles$b.primaryButton,
                  children: "Import Shopify Orders"
                })]
              }), /* @__PURE__ */ jsxs(Form, {
                method: "post",
                style: {
                  display: "grid",
                  gap: 12,
                  marginBottom: 16
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "intent",
                  value: "poll-mailbox"
                }), /* @__PURE__ */ jsx("button", {
                  type: "submit",
                  style: styles$b.primaryButton,
                  children: "Poll Mailbox Now"
                })]
              }), /* @__PURE__ */ jsxs(Form, {
                method: "post",
                style: {
                  display: "grid",
                  gap: 12
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "intent",
                  value: "create-order"
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Order Number"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "orderNumber",
                    placeholder: "8789",
                    style: styles$b.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Customer"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "customer",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Contact / Email"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "contact",
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Jobsite Address"
                    }), /* @__PURE__ */ jsx("input", {
                      id: "dispatch-address",
                      name: "address",
                      placeholder: "Start typing the street address",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "City"
                    }), /* @__PURE__ */ jsx("input", {
                      id: "dispatch-city",
                      name: "city",
                      placeholder: "City, ST ZIP",
                      style: styles$b.input
                    }), /* @__PURE__ */ jsx("input", {
                      id: "dispatch-province",
                      name: "province",
                      type: "hidden"
                    }), /* @__PURE__ */ jsx("input", {
                      id: "dispatch-postal-code",
                      name: "postalCode",
                      type: "hidden"
                    }), /* @__PURE__ */ jsx("input", {
                      id: "dispatch-country",
                      name: "country",
                      type: "hidden",
                      defaultValue: "US"
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridThree,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Material"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "material",
                      list: "dispatch-material-options",
                      placeholder: "Start typing a synced material",
                      style: styles$b.input
                    }), /* @__PURE__ */ jsx("datalist", {
                      id: "dispatch-material-options",
                      children: materialOptions.map((material) => /* @__PURE__ */ jsx("option", {
                        value: material
                      }, material))
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Quantity"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "quantity",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Unit"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "unit",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        children: "Ton"
                      }), /* @__PURE__ */ jsx("option", {
                        children: "Yard"
                      }), /* @__PURE__ */ jsx("option", {
                        children: "Gallons"
                      }), /* @__PURE__ */ jsx("option", {
                        children: "Bags"
                      }), /* @__PURE__ */ jsx("option", {
                        children: "Unit"
                      })]
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Requested Window"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "requestedWindow",
                      type: "date",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Time Preference"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "timePreference",
                      defaultValue: "Anytime",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "Anytime",
                        children: "Anytime"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Morning",
                        children: "Morning"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Afternoon",
                        children: "Afternoon"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Evening",
                        children: "Evening"
                      })]
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Notes"
                  }), /* @__PURE__ */ jsx("textarea", {
                    name: "notes",
                    rows: 3,
                    style: {
                      ...styles$b.input,
                      resize: "vertical"
                    }
                  })]
                }), /* @__PURE__ */ jsx("button", {
                  type: "submit",
                  style: styles$b.primaryButton,
                  children: "Add Order"
                })]
              }), /* @__PURE__ */ jsxs(Form, {
                method: "post",
                style: {
                  display: "grid",
                  gap: 12,
                  marginTop: 18
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "intent",
                  value: "parse-email-order"
                }), /* @__PURE__ */ jsx("label", {
                  style: styles$b.label,
                  children: "Paste Order Email"
                }), /* @__PURE__ */ jsx("textarea", {
                  name: "rawEmail",
                  rows: 9,
                  placeholder: "Subject: You've Got A New Order: #1234\nCustomer: Green Hills Supply\nAddress: 2543 W Applebrook Lane\nCity: Oak Creek, WI\nMaterial: Coarse Torpedo Sand\nQuantity: 12\nUnit: Ton\nRequested Window: Tomorrow 9a - 11a",
                  style: {
                    ...styles$b.input,
                    resize: "vertical",
                    minHeight: 180
                  }
                }), /* @__PURE__ */ jsx("button", {
                  type: "submit",
                  style: styles$b.secondaryButton,
                  children: "Parse Email Into Dispatch Card"
                })]
              })]
            })
          })]
        }) : null, null, activeView === "scheduled" ? /* @__PURE__ */ jsx("div", {
          style: styles$b.focusGrid,
          children: /* @__PURE__ */ jsxs("div", {
            id: "scheduled",
            style: styles$b.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$b.panelHeader,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Scheduled"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "Orders assigned to a route and waiting for delivery."
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.headerPill,
                children: [searchedScheduledOrders.length, " of ", scheduledOrders.length, " scheduled"]
              })]
            }), renderSearchBar("Search scheduled loads by any field: customer, route, driver, address, material, notes..."), /* @__PURE__ */ jsx("div", {
              style: {
                display: "grid",
                gap: 10
              },
              children: searchedScheduledOrders.length === 0 ? /* @__PURE__ */ jsx("div", {
                style: {
                  color: "#94a3b8"
                },
                children: scheduledOrders.length ? "No scheduled orders matched that search." : "No scheduled orders yet."
              }) : searchedScheduledOrders.map((order) => {
                const route57 = routes2.find((entry2) => entry2.id === order.assignedRouteId);
                return /* @__PURE__ */ jsxs("div", {
                  style: styles$b.queueCard,
                  children: [/* @__PURE__ */ jsxs("div", {
                    style: {
                      display: "flex",
                      justifyContent: "space-between",
                      gap: 12
                    },
                    children: [/* @__PURE__ */ jsxs("div", {
                      children: [/* @__PURE__ */ jsx("div", {
                        style: styles$b.queueTitle,
                        children: order.customer
                      }), /* @__PURE__ */ jsxs("div", {
                        style: styles$b.queueMeta,
                        children: [order.address, ", ", order.city]
                      })]
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.badge("scheduled"),
                      children: "scheduled"
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: styles$b.queueDetails,
                    children: [/* @__PURE__ */ jsx("span", {
                      children: getOrderDisplayNumber$2(order)
                    }), /* @__PURE__ */ jsxs("span", {
                      children: [order.quantity, " ", order.unit]
                    }), /* @__PURE__ */ jsx("span", {
                      children: order.material
                    }), order.travelMinutes ? /* @__PURE__ */ jsxs("span", {
                      children: [order.travelMinutes, " min RT"]
                    }) : null, order.timePreference ? /* @__PURE__ */ jsx("span", {
                      children: order.timePreference
                    }) : null, order.stopSequence ? /* @__PURE__ */ jsxs("span", {
                      children: ["Stop ", order.stopSequence]
                    }) : null, /* @__PURE__ */ jsx("span", {
                      children: route57 ? `${route57.truck || route57.code} / ${route57.driver || "No driver"}` : "No route"
                    })]
                  }), order.notes ? /* @__PURE__ */ jsxs("div", {
                    style: styles$b.queueNotes,
                    children: [/* @__PURE__ */ jsx("strong", {
                      children: "Notes:"
                    }), " ", order.notes]
                  }) : null, /* @__PURE__ */ jsxs("div", {
                    style: styles$b.deliveredActions,
                    children: [/* @__PURE__ */ jsx("a", {
                      href: `${dispatchHref}?view=orders&order=${encodeURIComponent(order.id)}`,
                      style: styles$b.detailButton,
                      children: "Edit"
                    }), /* @__PURE__ */ jsx("a", {
                      href: `${driverHref}?route=${encodeURIComponent(order.assignedRouteId || "")}`,
                      style: styles$b.assignButton,
                      children: "Driver View"
                    })]
                  })]
                }, order.id);
              })
            })]
          })
        }) : null, activeView === "delivered" ? /* @__PURE__ */ jsx("div", {
          style: styles$b.focusGrid,
          children: /* @__PURE__ */ jsxs("div", {
            id: "delivered",
            style: styles$b.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$b.panelHeader,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Delivered"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "Completed orders that drivers marked delivered."
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.headerPill,
                children: [searchedDeliveredOrders.length, " of ", deliveredOrders.length, " delivered"]
              })]
            }), renderSearchBar("Search delivered loads by any field: customer, address, material, proof, notes, route..."), /* @__PURE__ */ jsx("div", {
              style: {
                display: "grid",
                gap: 10
              },
              children: searchedDeliveredOrders.length === 0 ? /* @__PURE__ */ jsx("div", {
                style: {
                  color: "#94a3b8"
                },
                children: deliveredOrders.length ? "No delivered orders matched that search." : "No delivered orders yet."
              }) : searchedDeliveredOrders.map((order) => {
                const route57 = routes2.find((entry2) => entry2.id === order.assignedRouteId);
                return /* @__PURE__ */ jsxs("div", {
                  style: styles$b.queueCard,
                  children: [/* @__PURE__ */ jsxs("div", {
                    style: {
                      display: "flex",
                      justifyContent: "space-between",
                      gap: 12
                    },
                    children: [/* @__PURE__ */ jsxs("div", {
                      children: [/* @__PURE__ */ jsx("div", {
                        style: styles$b.queueTitle,
                        children: order.customer
                      }), /* @__PURE__ */ jsxs("div", {
                        style: styles$b.queueMeta,
                        children: [order.address, ", ", order.city]
                      })]
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.badge("delivered"),
                      children: "delivered"
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: styles$b.queueDetails,
                    children: [/* @__PURE__ */ jsx("span", {
                      children: getOrderDisplayNumber$2(order)
                    }), /* @__PURE__ */ jsxs("span", {
                      children: [order.quantity, " ", order.unit]
                    }), /* @__PURE__ */ jsx("span", {
                      children: order.material
                    }), order.deliveredAt ? /* @__PURE__ */ jsx("span", {
                      children: new Date(order.deliveredAt).toLocaleString()
                    }) : null, /* @__PURE__ */ jsx("span", {
                      children: route57 ? route57.truck || route57.code : "No route"
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: styles$b.deliveredActions,
                    children: [/* @__PURE__ */ jsx("a", {
                      href: `${dispatchHref}?view=delivered&order=${encodeURIComponent(order.id)}&detail=1`,
                      style: styles$b.detailButton,
                      children: "Open"
                    }), /* @__PURE__ */ jsxs(Form, {
                      method: "post",
                      onSubmit: (event) => {
                        if (!window.confirm("Mark this delivered load as undelivered? It will move back to its route or the queue.")) {
                          event.preventDefault();
                        }
                      },
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "intent",
                        value: "undo-delivered"
                      }), /* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "orderId",
                        value: order.id
                      }), /* @__PURE__ */ jsx("button", {
                        type: "submit",
                        style: styles$b.smallWarningButton,
                        children: "Mark Undelivered"
                      })]
                    }), /* @__PURE__ */ jsxs(Form, {
                      method: "post",
                      onSubmit: (event) => {
                        if (!window.confirm("Delete this delivered order? This cannot be undone.")) {
                          event.preventDefault();
                        }
                      },
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "intent",
                        value: "delete-order"
                      }), /* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "orderId",
                        value: order.id
                      }), /* @__PURE__ */ jsx("button", {
                        type: "submit",
                        style: styles$b.smallDangerButton,
                        children: "Delete"
                      })]
                    })]
                  })]
                }, order.id);
              })
            })]
          })
        }) : null, deliveredDetailOpen && selectedOrder ? /* @__PURE__ */ jsx("div", {
          style: styles$b.modalOverlay,
          children: /* @__PURE__ */ jsxs("div", {
            style: styles$b.dispatchModal,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$b.panelHeader,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Delivered Order"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "Review the completed delivery details and proof notes."
                })]
              }), /* @__PURE__ */ jsx("a", {
                href: dispatchViewHref("delivered"),
                style: styles$b.modalCloseButton,
                children: "Close"
              })]
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gap: 14
              },
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("div", {
                  style: styles$b.detailId,
                  children: getOrderDisplayNumber$2(selectedOrder)
                }), /* @__PURE__ */ jsx("div", {
                  style: styles$b.detailTitle,
                  children: selectedOrder.customer
                }), /* @__PURE__ */ jsx("div", {
                  style: styles$b.detailMeta,
                  children: selectedOrder.contact
                })]
              }), /* @__PURE__ */ jsxs(Form, {
                method: "post",
                onSubmit: (event) => {
                  if (!window.confirm("Mark this delivered load as undelivered? It will move back to its route or the queue.")) {
                    event.preventDefault();
                  }
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "intent",
                  value: "undo-delivered"
                }), /* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "orderId",
                  value: selectedOrder.id
                }), /* @__PURE__ */ jsx("button", {
                  type: "submit",
                  style: styles$b.warningButton,
                  children: "Mark Load Undelivered"
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.detailGrid,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Address"
                  }), /* @__PURE__ */ jsxs("div", {
                    style: styles$b.detailValue,
                    children: [selectedOrder.address, ", ", selectedOrder.city]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Load"
                  }), /* @__PURE__ */ jsxs("div", {
                    style: styles$b.detailValue,
                    children: [selectedOrder.quantity, " ", selectedOrder.unit, " ", selectedOrder.material]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Requested"
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.detailValue,
                    children: selectedOrder.requestedWindow
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Time Preference"
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.detailValue,
                    children: selectedOrder.timePreference || "Anytime"
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Travel Time"
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.detailValue,
                    children: selectedOrder.travelSummary || "Not calculated yet"
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Delivered"
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.detailValue,
                    children: selectedOrder.deliveredAt ? new Date(selectedOrder.deliveredAt).toLocaleString() : "Delivered"
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Proof Name"
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.detailValue,
                    children: selectedOrder.proofName || selectedOrder.signatureName || "Not captured"
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Inspection"
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.detailValue,
                    children: selectedOrder.inspectionStatus || "Not completed"
                  })]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.notesBlock,
                children: [/* @__PURE__ */ jsx("div", {
                  style: styles$b.detailLabel,
                  children: "Notes"
                }), /* @__PURE__ */ jsx("div", {
                  style: {
                    color: "#e2e8f0",
                    lineHeight: 1.55
                  },
                  children: selectedOrder.notes || "No dispatch notes."
                })]
              }), selectedOrder.proofNotes ? /* @__PURE__ */ jsxs("div", {
                style: styles$b.notesBlock,
                children: [/* @__PURE__ */ jsx("div", {
                  style: styles$b.detailLabel,
                  children: "Proof Notes"
                }), /* @__PURE__ */ jsx("div", {
                  style: {
                    color: "#e2e8f0",
                    lineHeight: 1.55
                  },
                  children: selectedOrder.proofNotes
                })]
              }) : null, selectedOrder.photoUrls ? /* @__PURE__ */ jsxs("div", {
                style: styles$b.notesBlock,
                children: [/* @__PURE__ */ jsx("div", {
                  style: styles$b.detailLabel,
                  children: "Photo Proof"
                }), isImageProof$2(selectedOrder.photoUrls) ? /* @__PURE__ */ jsx("img", {
                  src: selectedOrder.photoUrls,
                  alt: "Delivered material proof",
                  style: styles$b.deliveredPhoto
                }) : /* @__PURE__ */ jsx("div", {
                  style: {
                    color: "#e2e8f0",
                    lineHeight: 1.55,
                    whiteSpace: "pre-wrap"
                  },
                  children: selectedOrder.photoUrls
                })]
              }) : null]
            })]
          })
        }) : null, activeView === "trucks" ? /* @__PURE__ */ jsxs("div", {
          style: styles$b.focusGrid,
          children: [/* @__PURE__ */ jsxs("div", {
            id: "trucks",
            style: styles$b.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$b.panelHeader,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Fleet"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "View and edit active trucks, ton limits, and yard limits."
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.headerPill,
                children: [searchedTrucks.length, " of ", trucks.length, " trucks"]
              })]
            }), renderSearchBar("Search fleet by any field: truck, type, plate, tons, yards, route, assigned loads..."), /* @__PURE__ */ jsxs("div", {
              style: styles$b.resourceList,
              children: [searchedTrucks.map((truck) => /* @__PURE__ */ jsxs(Form, {
                method: "post",
                style: {
                  ...styles$b.resourceCard,
                  gap: 12
                },
                onSubmit: (event) => {
                  const submitter = event.nativeEvent.submitter;
                  if ((submitter == null ? void 0 : submitter.value) === "delete-truck" && !window.confirm("Delete this truck from the active fleet? Existing routes and history will remain.")) {
                    event.preventDefault();
                  }
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "truckId",
                  value: truck.id
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridThree,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Truck Name"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "label",
                      defaultValue: truck.label,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Type"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "truckType",
                      defaultValue: truck.truckType,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Plate"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "licensePlate",
                      defaultValue: truck.licensePlate || "",
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridThree,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Tons"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "tons",
                      defaultValue: truck.tons || "",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Yards"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "yards",
                      defaultValue: truck.yards || "",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: {
                      display: "flex",
                      alignItems: "flex-end",
                      gap: 8
                    },
                    children: [/* @__PURE__ */ jsx("button", {
                      type: "submit",
                      name: "intent",
                      value: "update-truck",
                      style: {
                        ...styles$b.secondaryButton,
                        width: "100%"
                      },
                      children: "Save Truck"
                    }), /* @__PURE__ */ jsx("button", {
                      type: "submit",
                      name: "intent",
                      value: "delete-truck",
                      style: {
                        ...styles$b.dangerButton,
                        width: "100%"
                      },
                      children: "Delete"
                    })]
                  })]
                })]
              }, truck.id)), searchedTrucks.length === 0 ? /* @__PURE__ */ jsx("div", {
                style: styles$b.emptySearch,
                children: "No trucks matched that search."
              }) : null]
            })]
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$b.panel,
            children: [/* @__PURE__ */ jsx("div", {
              style: styles$b.panelHeader,
              children: /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Add Truck"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "Add trucks before assigning routes."
                })]
              })
            }), /* @__PURE__ */ jsxs(Form, {
              method: "post",
              style: {
                display: "grid",
                gap: 12
              },
              children: [/* @__PURE__ */ jsx("input", {
                type: "hidden",
                name: "intent",
                value: "create-truck"
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.formGridTwo,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Truck Name"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "label",
                    placeholder: "Truck 22",
                    style: styles$b.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Type"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "truckType",
                    placeholder: "Tri-axle",
                    style: styles$b.input
                  })]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.formGridThree,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Tons"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "tons",
                    placeholder: "22",
                    style: styles$b.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Yards"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "yards",
                    placeholder: "18",
                    style: styles$b.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Plate"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "licensePlate",
                    style: styles$b.input
                  })]
                })]
              }), /* @__PURE__ */ jsx("button", {
                type: "submit",
                style: {
                  ...styles$b.primaryButton,
                  width: "100%"
                },
                children: "Add Truck"
              })]
            })]
          })]
        }) : null, activeView === "employees" ? /* @__PURE__ */ jsxs("div", {
          style: styles$b.focusGrid,
          children: [/* @__PURE__ */ jsxs("div", {
            id: "employees",
            style: styles$b.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$b.panelHeader,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Employees"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "View drivers, helpers, and dispatchers."
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.headerPill,
                children: [searchedEmployees.length, " of ", employees.length, " people"]
              })]
            }), renderSearchBar("Search employees by any field: name, role, phone, email, route, truck, assigned loads..."), /* @__PURE__ */ jsxs("div", {
              style: styles$b.resourceList,
              children: [searchedEmployees.map((employee) => /* @__PURE__ */ jsxs(Form, {
                method: "post",
                style: {
                  ...styles$b.resourceCard,
                  gap: 12
                },
                onSubmit: (event) => {
                  const submitter = event.nativeEvent.submitter;
                  if ((submitter == null ? void 0 : submitter.value) === "delete-employee" && !window.confirm("Delete this employee from the active roster? Existing routes and history will remain.")) {
                    event.preventDefault();
                  }
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "employeeId",
                  value: employee.id
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridThree,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Name"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "name",
                      defaultValue: employee.name,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Role"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "role",
                      defaultValue: employee.role,
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "driver",
                        children: "Driver"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "helper",
                        children: "Helper"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "dispatcher",
                        children: "Dispatcher"
                      })]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Phone"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "phone",
                      defaultValue: employee.phone || "",
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.formGridTwo,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Email"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "email",
                      type: "email",
                      defaultValue: employee.email || "",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: {
                      display: "flex",
                      alignItems: "flex-end",
                      gap: 8
                    },
                    children: [/* @__PURE__ */ jsx("button", {
                      type: "submit",
                      name: "intent",
                      value: "update-employee",
                      style: {
                        ...styles$b.secondaryButton,
                        width: "100%"
                      },
                      children: "Save Employee"
                    }), /* @__PURE__ */ jsx("button", {
                      type: "submit",
                      name: "intent",
                      value: "delete-employee",
                      style: {
                        ...styles$b.dangerButton,
                        width: "100%"
                      },
                      children: "Delete"
                    })]
                  })]
                })]
              }, employee.id)), searchedEmployees.length === 0 ? /* @__PURE__ */ jsx("div", {
                style: styles$b.emptySearch,
                children: "No employees matched that search."
              }) : null]
            })]
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$b.panel,
            children: [/* @__PURE__ */ jsx("div", {
              style: styles$b.panelHeader,
              children: /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Add Employee"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "Add drivers, helpers, or dispatch users."
                })]
              })
            }), /* @__PURE__ */ jsxs(Form, {
              method: "post",
              style: {
                display: "grid",
                gap: 12
              },
              children: [/* @__PURE__ */ jsx("input", {
                type: "hidden",
                name: "intent",
                value: "create-employee"
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.formGridThree,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Name"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "name",
                    style: styles$b.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Role"
                  }), /* @__PURE__ */ jsxs("select", {
                    name: "role",
                    style: styles$b.input,
                    children: [/* @__PURE__ */ jsx("option", {
                      value: "driver",
                      children: "Driver"
                    }), /* @__PURE__ */ jsx("option", {
                      value: "helper",
                      children: "Helper"
                    }), /* @__PURE__ */ jsx("option", {
                      value: "dispatcher",
                      children: "Dispatcher"
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Phone"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "phone",
                    style: styles$b.input
                  })]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.formGridTwo,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Email"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "email",
                    type: "email",
                    style: styles$b.input
                  })]
                }), /* @__PURE__ */ jsx("div", {
                  style: {
                    display: "flex",
                    alignItems: "flex-end"
                  },
                  children: /* @__PURE__ */ jsx("button", {
                    type: "submit",
                    style: {
                      ...styles$b.primaryButton,
                      width: "100%"
                    },
                    children: "Add Employee"
                  })
                })]
              })]
            })]
          })]
        }) : null, activeView === "routes" ? /* @__PURE__ */ jsxs("div", {
          style: styles$b.focusGrid,
          children: [/* @__PURE__ */ jsxs("div", {
            id: "routes",
            style: styles$b.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$b.panelHeader,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Routes"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "View route assignments, open driver view, and sequence stops."
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.headerPill,
                children: [searchedRoutes.length, " of ", routes2.length, " routes"]
              })]
            }), renderSearchBar("Search routes by any field: route, truck, driver, helper, region, shift, assigned loads..."), /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gap: 12
              },
              children: [searchedRoutes.map((route57) => /* @__PURE__ */ jsxs(Form, {
                method: "post",
                style: styles$b.routeCard(route57.color),
                onSubmit: (event) => {
                  const submitter = event.nativeEvent.submitter;
                  if ((submitter == null ? void 0 : submitter.value) === "delete-route" && !window.confirm("Delete this route from the active board? Move or unassign active orders first. This cannot be undone.")) {
                    event.preventDefault();
                  }
                },
                children: [/* @__PURE__ */ jsx("input", {
                  type: "hidden",
                  name: "routeId",
                  value: route57.id
                }), /* @__PURE__ */ jsxs("div", {
                  style: {
                    display: "flex",
                    justifyContent: "space-between",
                    gap: 12
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsxs("div", {
                      style: {
                        display: "flex",
                        alignItems: "center",
                        gap: 10
                      },
                      children: [/* @__PURE__ */ jsx("div", {
                        style: styles$b.routeColor(route57.color)
                      }), /* @__PURE__ */ jsx("div", {
                        style: styles$b.routeCode,
                        children: route57.code
                      }), /* @__PURE__ */ jsx("div", {
                        style: styles$b.routeRegion,
                        children: route57.region
                      })]
                    }), /* @__PURE__ */ jsxs("div", {
                      style: {
                        marginTop: 8,
                        color: "#e2e8f0",
                        fontWeight: 700
                      },
                      children: [route57.truck || "No truck", " · ", route57.driver || "No driver", " / ", route57.helper || "No helper"]
                    })]
                  }), /* @__PURE__ */ jsx("a", {
                    href: `${driverHref}?route=${encodeURIComponent(route57.id)}`,
                    style: styles$b.assignButton,
                    children: "Driver View"
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: {
                    ...styles$b.formGridThree,
                    marginTop: 14
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Route Code"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "code",
                      defaultValue: route57.code,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Truck"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "truckId",
                      defaultValue: route57.truckId || "",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "",
                        children: "Unassigned"
                      }), trucks.map((truck) => /* @__PURE__ */ jsx("option", {
                        value: truck.id,
                        children: truck.label
                      }, truck.id))]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Color"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "color",
                      name: "color",
                      defaultValue: route57.color,
                      style: styles$b.colorInput
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: {
                    ...styles$b.formGridThree,
                    marginTop: 12
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Driver"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "driverId",
                      defaultValue: route57.driverId || "",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "",
                        children: "Unassigned"
                      }), drivers.map((employee) => /* @__PURE__ */ jsx("option", {
                        value: employee.id,
                        children: employee.name
                      }, employee.id))]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Helper"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "helperId",
                      defaultValue: route57.helperId || "",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "",
                        children: "Unassigned"
                      }), helpers.map((employee) => /* @__PURE__ */ jsx("option", {
                        value: employee.id,
                        children: employee.name
                      }, employee.id))]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Shift"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "shift",
                      defaultValue: route57.shift,
                      style: styles$b.input
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: {
                    ...styles$b.formGridTwo,
                    marginTop: 12
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Region"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "region",
                      defaultValue: route57.region,
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: {
                      display: "flex",
                      alignItems: "flex-end",
                      gap: 8
                    },
                    children: [/* @__PURE__ */ jsx("button", {
                      type: "submit",
                      name: "intent",
                      value: "update-route",
                      style: {
                        ...styles$b.secondaryButton,
                        width: "100%"
                      },
                      children: "Save Route Assignments"
                    }), /* @__PURE__ */ jsx("button", {
                      type: "submit",
                      name: "intent",
                      value: "delete-route",
                      style: {
                        ...styles$b.dangerButton,
                        width: "100%"
                      },
                      children: "Delete Route"
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.routeStats,
                  children: [/* @__PURE__ */ jsx("span", {
                    children: route57.shift
                  }), /* @__PURE__ */ jsxs("span", {
                    children: [route57.stops, " stops"]
                  }), /* @__PURE__ */ jsx("span", {
                    children: route57.loadSummary || "No assigned loads yet"
                  })]
                })]
              }, route57.id)), searchedRoutes.length === 0 ? /* @__PURE__ */ jsx("div", {
                style: styles$b.emptySearch,
                children: "No routes matched that search."
              }) : null]
            })]
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$b.panel,
            children: [/* @__PURE__ */ jsx("div", {
              style: styles$b.panelHeader,
              children: /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$b.panelTitle,
                  children: "Add Route"
                }), /* @__PURE__ */ jsx("p", {
                  style: styles$b.panelSub,
                  children: "Create a route using an active truck and driver."
                })]
              })
            }), /* @__PURE__ */ jsxs(Form, {
              method: "post",
              style: {
                display: "grid",
                gap: 12
              },
              children: [/* @__PURE__ */ jsx("input", {
                type: "hidden",
                name: "intent",
                value: "create-route"
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.formGridThree,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Route Code"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "code",
                    placeholder: "R-22",
                    style: styles$b.input
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Truck"
                  }), /* @__PURE__ */ jsxs("select", {
                    name: "truckId",
                    style: styles$b.input,
                    children: [/* @__PURE__ */ jsx("option", {
                      value: "",
                      children: "Select truck"
                    }), trucks.map((truck) => /* @__PURE__ */ jsx("option", {
                      value: truck.id,
                      children: truck.label
                    }, truck.id))]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Color"
                  }), /* @__PURE__ */ jsx("input", {
                    type: "color",
                    name: "color",
                    defaultValue: "#38bdf8",
                    style: styles$b.colorInput
                  })]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$b.formGridThree,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Driver"
                  }), /* @__PURE__ */ jsxs("select", {
                    name: "driverId",
                    style: styles$b.input,
                    children: [/* @__PURE__ */ jsx("option", {
                      value: "",
                      children: "Select driver"
                    }), drivers.map((employee) => /* @__PURE__ */ jsx("option", {
                      value: employee.id,
                      children: employee.name
                    }, employee.id))]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Helper"
                  }), /* @__PURE__ */ jsxs("select", {
                    name: "helperId",
                    style: styles$b.input,
                    children: [/* @__PURE__ */ jsx("option", {
                      value: "",
                      children: "No helper"
                    }), helpers.map((employee) => /* @__PURE__ */ jsx("option", {
                      value: employee.id,
                      children: employee.name
                    }, employee.id))]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("label", {
                    style: styles$b.label,
                    children: "Shift"
                  }), /* @__PURE__ */ jsx("input", {
                    name: "shift",
                    placeholder: "7:00a - 4:00p",
                    style: styles$b.input
                  })]
                })]
              }), /* @__PURE__ */ jsx("button", {
                type: "submit",
                style: styles$b.primaryButton,
                children: "Add Route"
              })]
            })]
          })]
        }) : null, activeView === "dashboard" ? /* @__PURE__ */ jsxs("div", {
          style: styles$b.workspaceGrid,
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$b.leftColumn,
            children: /* @__PURE__ */ jsxs("div", {
              id: "orders",
              onDragEnter: (event) => {
                if (!draggedOrderId || !canManageDispatch) return;
                event.preventDefault();
                setDragOverQueue(true);
              },
              onDragOver: (event) => {
                if (!draggedOrderId || !canManageDispatch) return;
                event.preventDefault();
                event.dataTransfer.dropEffect = "move";
                setDragOverQueue(true);
              },
              onDrop: unassignDraggedOrder,
              style: {
                ...styles$b.panel,
                ...dragOverQueue ? styles$b.queueDropActive : {}
              },
              children: [/* @__PURE__ */ jsxs("div", {
                style: styles$b.panelHeader,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("h2", {
                    style: styles$b.panelTitle,
                    children: "Email Intake Queue"
                  }), /* @__PURE__ */ jsxs("p", {
                    style: styles$b.panelSub,
                    children: ["Orders that came in by email or were typed in manually can be reviewed and routed here.", canManageDispatch ? " Drag cards between the queue and routes to assign or unassign." : ""]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.headerPill,
                  children: [searchedActiveOrders.length, " in queue"]
                })]
              }), canManageDispatch ? /* @__PURE__ */ jsxs("div", {
                style: styles$b.autoRouteBar,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.autoRouteTitle,
                    children: "Auto Route Planner"
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.autoRouteSub,
                    children: "Sorts by requested date, time preference, truck capacity, then similar materials."
                  })]
                }), /* @__PURE__ */ jsxs("label", {
                  style: styles$b.manualToggle,
                  children: [/* @__PURE__ */ jsx("input", {
                    type: "checkbox",
                    checked: manualRoutingMode,
                    onChange: toggleManualRoutingMode
                  }), "Manual routing mode"]
                }), /* @__PURE__ */ jsxs(Form, {
                  method: "post",
                  onSubmit: (event) => {
                    if (manualRoutingMode) {
                      event.preventDefault();
                      return;
                    }
                    if (!window.confirm("Auto Route will assign current unscheduled orders to available routes/trucks. Existing route stops will be left in place. Continue?")) {
                      event.preventDefault();
                    }
                  },
                  children: [/* @__PURE__ */ jsx("input", {
                    type: "hidden",
                    name: "intent",
                    value: "auto-route-orders"
                  }), manualRoutingMode ? /* @__PURE__ */ jsx("input", {
                    type: "hidden",
                    name: "manualRoutingMode",
                    value: "on"
                  }) : null, /* @__PURE__ */ jsx("button", {
                    type: "submit",
                    disabled: manualRoutingMode,
                    style: manualRoutingMode ? styles$b.disabledActionButton : styles$b.primaryButton,
                    children: "Auto Route"
                  })]
                })]
              }) : null, renderSearchBar("Search dispatch by any field: order, customer, route, truck, driver, address, material, notes..."), /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "grid",
                  gap: 10
                },
                children: [searchedActiveOrders.map((order) => {
                  const active = order.id === (selectedOrder == null ? void 0 : selectedOrder.id);
                  const route57 = routes2.find((entry2) => entry2.id === order.assignedRouteId);
                  return /* @__PURE__ */ jsx("div", {
                    draggable: canManageDispatch,
                    onDragStart: (event) => startOrderDrag(order.id, event),
                    onDragEnd: clearDragState,
                    style: {
                      ...styles$b.queueCard,
                      borderColor: active ? "#38bdf8" : "rgba(51, 65, 85, 0.92)",
                      boxShadow: active ? "0 0 0 1px rgba(56, 189, 248, 0.45)" : "none",
                      cursor: canManageDispatch ? "grab" : "pointer",
                      opacity: draggedOrderId === order.id ? 0.58 : 1
                    },
                    children: /* @__PURE__ */ jsxs("div", {
                      style: styles$b.cardActionRow,
                      children: [/* @__PURE__ */ jsxs("a", {
                        href: dashboardSelectHref(order.id),
                        draggable: false,
                        style: styles$b.cardSelectArea,
                        children: [/* @__PURE__ */ jsxs("div", {
                          style: {
                            display: "flex",
                            justifyContent: "space-between",
                            gap: 12
                          },
                          children: [/* @__PURE__ */ jsxs("div", {
                            children: [/* @__PURE__ */ jsx("div", {
                              style: styles$b.queueTitle,
                              children: order.customer
                            }), /* @__PURE__ */ jsxs("div", {
                              style: styles$b.queueMeta,
                              children: [order.address, ", ", order.city]
                            })]
                          }), /* @__PURE__ */ jsx("div", {
                            style: styles$b.badge(order.status),
                            children: order.status
                          })]
                        }), /* @__PURE__ */ jsxs("div", {
                          style: styles$b.queueDetails,
                          children: [/* @__PURE__ */ jsx("span", {
                            children: getOrderDisplayNumber$2(order)
                          }), /* @__PURE__ */ jsxs("span", {
                            children: [order.quantity, " ", order.unit]
                          }), /* @__PURE__ */ jsx("span", {
                            children: order.material
                          }), order.travelMinutes ? /* @__PURE__ */ jsxs("span", {
                            children: [order.travelMinutes, " min RT"]
                          }) : null, order.timePreference ? /* @__PURE__ */ jsx("span", {
                            children: order.timePreference
                          }) : null, order.stopSequence ? /* @__PURE__ */ jsxs("span", {
                            children: ["Stop ", order.stopSequence]
                          }) : null]
                        }), order.notes ? /* @__PURE__ */ jsxs("div", {
                          style: styles$b.queueNotes,
                          children: [/* @__PURE__ */ jsx("strong", {
                            children: "Notes:"
                          }), " ", order.notes]
                        }) : null, /* @__PURE__ */ jsxs("div", {
                          style: styles$b.queueFooter,
                          children: [/* @__PURE__ */ jsx("span", {
                            children: order.requestedWindow
                          }), /* @__PURE__ */ jsx("span", {
                            children: route57 ? `${route57.truck} / ${getDeliveryStatusLabel(order.deliveryStatus)}` : "Unassigned"
                          })]
                        })]
                      }), /* @__PURE__ */ jsx("a", {
                        href: dashboardDetailHref(order.id),
                        draggable: false,
                        style: styles$b.detailButton,
                        children: "Open"
                      })]
                    })
                  }, order.id);
                }), searchedActiveOrders.length === 0 ? /* @__PURE__ */ jsx("div", {
                  style: styles$b.emptySearch,
                  children: "No queue orders matched that search."
                }) : null]
              }), draggedOrderId && canManageDispatch ? /* @__PURE__ */ jsx("div", {
                style: dragOverQueue ? styles$b.dropHintActive : styles$b.dropHint,
                children: "Drop here to move back to queue"
              }) : null]
            })
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$b.centerColumn,
            children: [/* @__PURE__ */ jsxs("div", {
              id: "routes",
              style: styles$b.panel,
              children: [/* @__PURE__ */ jsxs("div", {
                style: styles$b.panelHeader,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("h2", {
                    style: styles$b.panelTitle,
                    children: "Routes & Fleet"
                  }), /* @__PURE__ */ jsx("p", {
                    style: styles$b.panelSub,
                    children: "Active trucks, crew assignments, and current stop counts."
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.headerPill,
                  children: [searchedRoutes.length, " of ", routes2.length, " routes"]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "grid",
                  gap: 12
                },
                children: [searchedRoutes.map((route57) => /* @__PURE__ */ jsxs("div", {
                  onDragEnter: (event) => {
                    if (!draggedOrderId || !canManageDispatch) return;
                    event.preventDefault();
                    setDragOverQueue(false);
                    setDragOverRouteId(route57.id);
                  },
                  onDragOver: (event) => {
                    if (!draggedOrderId || !canManageDispatch) return;
                    event.preventDefault();
                    event.dataTransfer.dropEffect = "move";
                    setDragOverQueue(false);
                    setDragOverRouteId(route57.id);
                  },
                  onDrop: (event) => assignDraggedOrder(route57.id, event),
                  style: {
                    ...styles$b.routeCard(route57.color),
                    ...dragOverRouteId === route57.id ? styles$b.routeDropActive : {}
                  },
                  children: [/* @__PURE__ */ jsxs("div", {
                    style: {
                      display: "flex",
                      justifyContent: "space-between",
                      gap: 12,
                      alignItems: "center"
                    },
                    children: [/* @__PURE__ */ jsxs("div", {
                      children: [/* @__PURE__ */ jsxs("div", {
                        style: {
                          display: "flex",
                          alignItems: "center",
                          gap: 10
                        },
                        children: [/* @__PURE__ */ jsx("div", {
                          style: styles$b.routeColor(route57.color)
                        }), /* @__PURE__ */ jsx("div", {
                          style: styles$b.routeCode,
                          children: route57.code
                        }), /* @__PURE__ */ jsx("div", {
                          style: styles$b.routeRegion,
                          children: route57.region
                        })]
                      }), /* @__PURE__ */ jsxs("div", {
                        style: {
                          marginTop: 8,
                          color: "#e2e8f0",
                          fontWeight: 700
                        },
                        children: [route57.truck, " · ", route57.driver, " / ", route57.helper]
                      })]
                    }), /* @__PURE__ */ jsx("a", {
                      href: `${driverHref}?route=${encodeURIComponent(route57.id)}`,
                      draggable: false,
                      style: styles$b.assignButton,
                      children: "Driver View"
                    }), /* @__PURE__ */ jsxs("div", {
                      style: styles$b.routeTimePill,
                      children: ["Total Time: ", formatTravelMinutes$3(route57.totalTravelMinutes)]
                    }), selectedOrder ? selectedOrder.assignedRouteId === route57.id ? /* @__PURE__ */ jsxs(Form, {
                      method: "post",
                      style: styles$b.assignForm,
                      onSubmit: (event) => prepareAssignmentSubmit(selectedOrder, route57.id, event.currentTarget, event),
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "intent",
                        value: "unassign-order"
                      }), /* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "orderId",
                        value: selectedOrder.id
                      }), /* @__PURE__ */ jsx("button", {
                        type: "submit",
                        style: styles$b.secondaryButton,
                        children: "Unassign Selected"
                      })]
                    }) : /* @__PURE__ */ jsxs(Form, {
                      method: "post",
                      style: styles$b.assignForm,
                      onSubmit: (event) => prepareAssignmentSubmit(selectedOrder, route57.id, event.currentTarget, event),
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "intent",
                        value: "assign-order"
                      }), /* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "orderId",
                        value: selectedOrder.id
                      }), /* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "routeId",
                        value: route57.id
                      }), /* @__PURE__ */ jsx("input", {
                        name: "eta",
                        placeholder: "ETA",
                        defaultValue: selectedOrder.eta || "",
                        style: styles$b.compactInput
                      }), /* @__PURE__ */ jsx("button", {
                        type: "submit",
                        style: styles$b.assignButton,
                        children: "Assign Selected"
                      })]
                    }) : null]
                  }), draggedOrderId && canManageDispatch ? /* @__PURE__ */ jsxs("div", {
                    style: dragOverRouteId === route57.id ? styles$b.dropHintActive : styles$b.dropHint,
                    children: ["Drop here to assign to ", route57.code]
                  }) : null, /* @__PURE__ */ jsxs("div", {
                    style: styles$b.routeStats,
                    children: [/* @__PURE__ */ jsx("span", {
                      children: route57.shift
                    }), /* @__PURE__ */ jsxs("span", {
                      children: [route57.stops, " stops"]
                    }), /* @__PURE__ */ jsxs("span", {
                      children: ["Total route: ", formatTravelMinutes$3(route57.totalTravelMinutes)]
                    }), /* @__PURE__ */ jsx("span", {
                      children: route57.loadSummary || "No assigned loads yet"
                    })]
                  }), route57.orders.length ? /* @__PURE__ */ jsx("div", {
                    style: styles$b.stopList,
                    children: route57.orders.map((order, index) => /* @__PURE__ */ jsxs("div", {
                      draggable: canManageDispatch,
                      onDragStart: (event) => startOrderDrag(order.id, event),
                      onDragEnd: clearDragState,
                      style: {
                        ...styles$b.stopRow,
                        cursor: canManageDispatch ? "grab" : "default",
                        opacity: draggedOrderId === order.id ? 0.58 : 1
                      },
                      children: [/* @__PURE__ */ jsx("span", {
                        title: "Drag to another route or back to queue",
                        style: styles$b.dragHandle,
                        children: "::"
                      }), /* @__PURE__ */ jsxs("div", {
                        style: styles$b.stopSelectArea,
                        children: [/* @__PURE__ */ jsx("span", {
                          style: styles$b.stopNumber,
                          children: order.stopSequence || "-"
                        }), /* @__PURE__ */ jsxs("span", {
                          style: styles$b.stopMain,
                          children: [/* @__PURE__ */ jsx("strong", {
                            children: order.customer
                          }), /* @__PURE__ */ jsxs("small", {
                            children: [order.city, " · ", order.material, " ·", " ", order.travelSummary || `${formatTravelMinutes$3(getOrderTravelMinutes(order))} RT`]
                          })]
                        }), /* @__PURE__ */ jsx("span", {
                          style: styles$b.stopStatus(getDeliveryStatusColor(order.deliveryStatus)),
                          children: getDeliveryStatusLabel(order.deliveryStatus)
                        })]
                      }), /* @__PURE__ */ jsxs("div", {
                        style: styles$b.stopActions,
                        children: [canManageDispatch ? /* @__PURE__ */ jsxs(Form, {
                          method: "post",
                          style: styles$b.routeReorderForm,
                          children: [/* @__PURE__ */ jsx("input", {
                            type: "hidden",
                            name: "intent",
                            value: "move-route-stop"
                          }), /* @__PURE__ */ jsx("input", {
                            type: "hidden",
                            name: "routeId",
                            value: route57.id
                          }), /* @__PURE__ */ jsx("input", {
                            type: "hidden",
                            name: "orderId",
                            value: order.id
                          }), /* @__PURE__ */ jsx("button", {
                            name: "direction",
                            value: "up",
                            style: {
                              ...styles$b.stopDetailButton,
                              ...index === 0 ? styles$b.disabledStopButton : null
                            },
                            disabled: index === 0,
                            title: "Move stop up",
                            children: "Up"
                          }), /* @__PURE__ */ jsx("button", {
                            name: "direction",
                            value: "down",
                            style: {
                              ...styles$b.stopDetailButton,
                              ...index === route57.orders.length - 1 ? styles$b.disabledStopButton : null
                            },
                            disabled: index === route57.orders.length - 1,
                            title: "Move stop down",
                            children: "Down"
                          })]
                        }) : null, canManageDispatch ? /* @__PURE__ */ jsxs(Form, {
                          method: "post",
                          style: styles$b.routeReorderForm,
                          onSubmit: attachLoaderNote$1,
                          children: [/* @__PURE__ */ jsx("input", {
                            type: "hidden",
                            name: "intent",
                            value: "notify-loader"
                          }), /* @__PURE__ */ jsx("input", {
                            type: "hidden",
                            name: "orderId",
                            value: order.id
                          }), /* @__PURE__ */ jsx("button", {
                            type: "submit",
                            style: styles$b.stopDetailButton,
                            title: "Tell the loader this is the next load",
                            children: "Load Next"
                          })]
                        }) : null, /* @__PURE__ */ jsxs("select", {
                          defaultValue: order.assignedRouteId || "",
                          onChange: (event) => {
                            moveOrderWithSelect(order.id, event.currentTarget.value);
                          },
                          onClick: (event) => event.stopPropagation(),
                          style: styles$b.stopMoveSelect,
                          title: "Move order",
                          children: [/* @__PURE__ */ jsx("option", {
                            value: "",
                            children: "Queue"
                          }), routes2.map((moveRoute) => /* @__PURE__ */ jsx("option", {
                            value: moveRoute.id,
                            children: moveRoute.code
                          }, moveRoute.id))]
                        }), /* @__PURE__ */ jsx("a", {
                          href: dashboardSelectHref(order.id),
                          draggable: false,
                          style: styles$b.stopDetailButton,
                          children: "Select"
                        }), /* @__PURE__ */ jsx("a", {
                          href: dashboardDetailHref(order.id),
                          draggable: false,
                          style: styles$b.stopDetailButton,
                          children: "Open"
                        })]
                      })]
                    }, order.id))
                  }) : null]
                }, route57.id)), searchedRoutes.length === 0 ? /* @__PURE__ */ jsx("div", {
                  style: styles$b.emptySearch,
                  children: "No routes matched that search."
                }) : null]
              })]
            }), /* @__PURE__ */ jsxs("div", {
              style: styles$b.panel,
              children: [/* @__PURE__ */ jsx("div", {
                style: styles$b.panelHeader,
                children: /* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("h2", {
                    style: styles$b.panelTitle,
                    children: "Route Map Preview"
                  }), /* @__PURE__ */ jsx("p", {
                    style: styles$b.panelSub,
                    children: "Live Google route preview from the shop to each assigned stop and back."
                  })]
                })
              }), /* @__PURE__ */ jsx(RouteMapPreview, {
                googleMapsApiKey,
                originAddress: mapOriginAddress,
                routes: searchedRoutes
              })]
            })]
          }), dispatchDetailOpen ? /* @__PURE__ */ jsx("div", {
            style: styles$b.modalOverlay,
            children: /* @__PURE__ */ jsxs("div", {
              style: styles$b.dispatchModal,
              children: [/* @__PURE__ */ jsxs("div", {
                style: styles$b.panelHeader,
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("h2", {
                    style: styles$b.panelTitle,
                    children: "Dispatch Detail"
                  }), /* @__PURE__ */ jsx("p", {
                    style: styles$b.panelSub,
                    children: "Review the selected order, then assign it to a truck and crew or place it on hold."
                  })]
                }), /* @__PURE__ */ jsx("a", {
                  href: selectedOrder ? dashboardSelectHref(selectedOrder.id) : dispatchViewHref("dashboard"),
                  style: styles$b.modalCloseButton,
                  children: "Close"
                })]
              }), selectedOrder ? /* @__PURE__ */ jsxs("div", {
                style: {
                  display: "grid",
                  gap: 14
                },
                children: [/* @__PURE__ */ jsxs("div", {
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailId,
                    children: getOrderDisplayNumber$2(selectedOrder)
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.detailTitle,
                    children: selectedOrder.customer
                  }), /* @__PURE__ */ jsx("div", {
                    style: styles$b.detailMeta,
                    children: selectedOrder.contact
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.detailGrid,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Address"
                    }), /* @__PURE__ */ jsxs("div", {
                      style: styles$b.detailValue,
                      children: [selectedOrder.address, ", ", selectedOrder.city]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Load"
                    }), /* @__PURE__ */ jsxs("div", {
                      style: styles$b.detailValue,
                      children: [selectedOrder.quantity, " ", selectedOrder.unit, " ", selectedOrder.material]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Requested"
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.detailValue,
                      children: selectedOrder.requestedWindow
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Time Preference"
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.detailValue,
                      children: selectedOrder.timePreference || "Anytime"
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Travel Time"
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.detailValue,
                      children: selectedOrder.travelSummary || "Not calculated yet"
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Truck Preference"
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.detailValue,
                      children: selectedOrder.truckPreference || "No preference"
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Route Stop"
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.detailValue,
                      children: selectedOrder.assignedRouteId ? `Stop ${selectedOrder.stopSequence || "-"}` : "Unassigned"
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Delivery Status"
                    }), /* @__PURE__ */ jsxs("div", {
                      style: {
                        ...styles$b.detailValue,
                        color: getDeliveryStatusColor(selectedOrder.deliveryStatus)
                      },
                      children: [getDeliveryStatusLabel(selectedOrder.deliveryStatus), selectedOrder.eta ? ` · ETA ${selectedOrder.eta}` : ""]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Inspection"
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.detailValue,
                      children: selectedOrder.inspectionStatus || "Not completed"
                    })]
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.notesBlock,
                  children: [/* @__PURE__ */ jsx("div", {
                    style: styles$b.detailLabel,
                    children: "Notes"
                  }), /* @__PURE__ */ jsx("div", {
                    style: {
                      color: "#e2e8f0",
                      lineHeight: 1.55
                    },
                    children: selectedOrder.notes || "No dispatch notes yet."
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: styles$b.assignmentPanel,
                  children: [/* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("div", {
                      style: styles$b.detailLabel,
                      children: "Assigned Route"
                    }), /* @__PURE__ */ jsx("div", {
                      style: styles$b.detailValue,
                      children: selectedOrderRoute ? `${selectedOrderRoute.code} · ${selectedOrderRoute.truck} · ${selectedOrderRoute.driver}` : "Unassigned"
                    })]
                  }), /* @__PURE__ */ jsxs(Form, {
                    method: "post",
                    style: styles$b.assignmentForm,
                    onSubmit: (event) => {
                      const routeId = new FormData(event.currentTarget).get("routeId");
                      prepareAssignmentSubmit(selectedOrder, String(routeId || ""), event.currentTarget, event);
                    },
                    children: [/* @__PURE__ */ jsx("input", {
                      type: "hidden",
                      name: "intent",
                      value: "assign-order"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "hidden",
                      name: "orderId",
                      value: selectedOrder.id
                    }), /* @__PURE__ */ jsxs("div", {
                      style: styles$b.formGridTwo,
                      children: [/* @__PURE__ */ jsxs("div", {
                        children: [/* @__PURE__ */ jsx("label", {
                          style: styles$b.label,
                          children: "Assign To Route / Driver"
                        }), /* @__PURE__ */ jsxs("select", {
                          name: "routeId",
                          defaultValue: selectedOrder.assignedRouteId || "",
                          style: styles$b.input,
                          required: true,
                          children: [/* @__PURE__ */ jsx("option", {
                            value: "",
                            disabled: true,
                            children: "Select route"
                          }), routes2.map((route57) => /* @__PURE__ */ jsxs("option", {
                            value: route57.id,
                            children: [route57.code, " - ", route57.truck, " - ", route57.driver]
                          }, route57.id))]
                        })]
                      }), /* @__PURE__ */ jsxs("div", {
                        children: [/* @__PURE__ */ jsx("label", {
                          style: styles$b.label,
                          children: "ETA"
                        }), /* @__PURE__ */ jsx("input", {
                          name: "eta",
                          defaultValue: selectedOrder.eta || "",
                          placeholder: "Optional",
                          style: styles$b.input
                        })]
                      })]
                    }), /* @__PURE__ */ jsx("button", {
                      type: "submit",
                      style: styles$b.primaryButton,
                      children: selectedOrder.assignedRouteId ? "Reassign Order" : "Assign Order"
                    })]
                  })]
                }), /* @__PURE__ */ jsxs(Form, {
                  method: "post",
                  style: styles$b.stopStatusForm,
                  children: [/* @__PURE__ */ jsx("input", {
                    type: "hidden",
                    name: "intent",
                    value: "update-stop-status"
                  }), /* @__PURE__ */ jsx("input", {
                    type: "hidden",
                    name: "orderId",
                    value: selectedOrder.id
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Stop Status"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "deliveryStatus",
                      defaultValue: selectedOrder.deliveryStatus || "not_started",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "not_started",
                        children: "Dispatched"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "en_route",
                        children: "Enroute"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "delivered",
                        children: "Delivered"
                      })]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Proof Name"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "proofName",
                      defaultValue: selectedOrder.proofName || "",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Signature / Authorized Name"
                    }), /* @__PURE__ */ jsx("input", {
                      name: "signatureName",
                      defaultValue: selectedOrder.signatureName || (selectedOrderRoute == null ? void 0 : selectedOrderRoute.driver) || "",
                      style: styles$b.input
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Photo Links"
                    }), /* @__PURE__ */ jsx("textarea", {
                      name: "photoUrls",
                      defaultValue: selectedOrder.photoUrls || "",
                      rows: 3,
                      placeholder: "Paste photo URLs or file references, one per line",
                      style: {
                        ...styles$b.input,
                        resize: "vertical"
                      }
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Inspection Status"
                    }), /* @__PURE__ */ jsxs("select", {
                      name: "inspectionStatus",
                      defaultValue: selectedOrder.inspectionStatus || "",
                      style: styles$b.input,
                      children: [/* @__PURE__ */ jsx("option", {
                        value: "",
                        children: "Not completed"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Passed",
                        children: "Passed"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Needs review",
                        children: "Needs review"
                      }), /* @__PURE__ */ jsx("option", {
                        value: "Blocked",
                        children: "Blocked"
                      })]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    style: styles$b.checklistGrid,
                    children: [/* @__PURE__ */ jsxs("label", {
                      style: styles$b.checkboxLabel,
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "checkbox",
                        name: "siteSafe"
                      }), " Site safe"]
                    }), /* @__PURE__ */ jsxs("label", {
                      style: styles$b.checkboxLabel,
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "checkbox",
                        name: "loadMatchesTicket"
                      }), " Load matches ticket"]
                    }), /* @__PURE__ */ jsxs("label", {
                      style: styles$b.checkboxLabel,
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "checkbox",
                        name: "customerConfirmedPlacement"
                      }), " Placement confirmed"]
                    }), /* @__PURE__ */ jsxs("label", {
                      style: styles$b.checkboxLabel,
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "checkbox",
                        name: "photosTaken"
                      }), " Photos taken"]
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Custom Checklist Notes"
                    }), /* @__PURE__ */ jsx("textarea", {
                      name: "customChecklist",
                      rows: 3,
                      defaultValue: selectedOrder.checklistJson || "",
                      style: {
                        ...styles$b.input,
                        resize: "vertical"
                      }
                    })]
                  }), /* @__PURE__ */ jsxs("div", {
                    children: [/* @__PURE__ */ jsx("label", {
                      style: styles$b.label,
                      children: "Proof Notes"
                    }), /* @__PURE__ */ jsx("textarea", {
                      name: "proofNotes",
                      defaultValue: selectedOrder.proofNotes || "",
                      rows: 3,
                      style: {
                        ...styles$b.input,
                        resize: "vertical"
                      }
                    })]
                  }), /* @__PURE__ */ jsx("button", {
                    type: "submit",
                    style: styles$b.primaryButton,
                    children: "Update Stop"
                  })]
                }), /* @__PURE__ */ jsxs("div", {
                  style: {
                    display: "grid",
                    gap: 10
                  },
                  children: [/* @__PURE__ */ jsxs(Form, {
                    method: "post",
                    children: [/* @__PURE__ */ jsx("input", {
                      type: "hidden",
                      name: "intent",
                      value: "unassign-order"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "hidden",
                      name: "orderId",
                      value: selectedOrder.id
                    }), /* @__PURE__ */ jsx("button", {
                      type: "submit",
                      style: styles$b.secondaryButton,
                      children: "Move Back To Inbox"
                    })]
                  }), /* @__PURE__ */ jsxs(Form, {
                    method: "post",
                    children: [/* @__PURE__ */ jsx("input", {
                      type: "hidden",
                      name: "intent",
                      value: "hold-order"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "hidden",
                      name: "orderId",
                      value: selectedOrder.id
                    }), /* @__PURE__ */ jsx("button", {
                      type: "submit",
                      style: styles$b.secondaryButton,
                      children: "Put On Hold"
                    })]
                  }), /* @__PURE__ */ jsxs(Form, {
                    method: "post",
                    onSubmit: (event) => {
                      if (!window.confirm("Delete this order? This cannot be undone.")) {
                        event.preventDefault();
                      }
                    },
                    children: [/* @__PURE__ */ jsx("input", {
                      type: "hidden",
                      name: "intent",
                      value: "delete-order"
                    }), /* @__PURE__ */ jsx("input", {
                      type: "hidden",
                      name: "orderId",
                      value: selectedOrder.id
                    }), /* @__PURE__ */ jsx("button", {
                      type: "submit",
                      style: styles$b.dangerButton,
                      children: "Delete Order"
                    })]
                  })]
                })]
              }) : /* @__PURE__ */ jsx("div", {
                style: {
                  color: "#94a3b8"
                },
                children: "Select an order to view dispatch detail."
              })]
            })
          }) : null]
        }) : null]
      })]
    })]
  });
});
const styles$b = {
  page: {
    minHeight: "100vh",
    background: "#020617",
    color: "#f8fafc",
    padding: 0,
    fontFamily: "Verdana, Geneva, Tahoma, sans-serif",
    fontSize: 12
  },
  appFrame: {
    width: "100%",
    minHeight: "100vh",
    display: "grid",
    alignItems: "start",
    transition: "grid-template-columns 160ms ease"
  },
  sidebar: {
    position: "sticky",
    top: 0,
    minHeight: "100vh",
    display: "grid",
    gridTemplateRows: "auto 1fr auto",
    gap: 12,
    borderRadius: 0,
    borderRight: "1px solid #1e293b",
    background: "#020617",
    boxShadow: "none",
    overflow: "hidden",
    transition: "padding 160ms ease"
  },
  navToggle: {
    width: 34,
    height: 30,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 8,
    border: "1px solid #334155",
    background: "#1e293b",
    color: "#ff7a1a",
    fontWeight: 900,
    cursor: "pointer"
  },
  collapsedOnlyHidden: {
    display: "none"
  },
  brandBlock: {
    display: "flex",
    alignItems: "center",
    gap: 12
  },
  brandMark: {
    width: 70,
    height: 46,
    borderRadius: 8,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    background: "#0f172a",
    border: "1px solid #334155",
    overflow: "hidden",
    boxShadow: "none"
  },
  brandLogo: {
    width: "100%",
    height: "100%",
    objectFit: "contain",
    display: "block"
  },
  brandTitle: {
    color: "#f8fafc",
    fontSize: 15,
    fontWeight: 900,
    lineHeight: 1.1
  },
  brandSub: {
    marginTop: 3,
    color: "#cbd5e1",
    fontSize: 11,
    fontWeight: 800
  },
  sideNav: {
    display: "grid",
    alignContent: "start",
    gap: 8
  },
  sideNavLink: (active) => ({
    minHeight: 46,
    display: "flex",
    alignItems: "center",
    padding: "0 13px",
    borderRadius: 8,
    color: active ? "#ff7a1a" : "#e5e7eb",
    textDecoration: "none",
    fontWeight: 800,
    border: "1px solid transparent",
    background: active ? "#1e293b" : "transparent"
  }),
  sidebarFooter: {
    display: "grid",
    gap: 8
  },
  sideUtility: {
    minHeight: 40,
    display: "flex",
    alignItems: "center",
    padding: "0 12px",
    borderRadius: 14,
    border: "1px solid #334155",
    background: "transparent",
    color: "#e5e7eb",
    textDecoration: "none",
    fontSize: 13,
    fontWeight: 800
  },
  shell: {
    display: "grid",
    gap: 14,
    padding: 14
  },
  loginCard: {
    background: "#0f172a",
    border: "1px solid #334155",
    borderRadius: 10,
    padding: 28,
    boxShadow: "0 1px 2px rgba(0,0,0,0.08)"
  },
  hero: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) auto",
    gap: 12,
    padding: "14px 16px",
    borderRadius: 0,
    border: "1px solid #334155",
    background: "#0f172a",
    boxShadow: "0 1px 2px rgba(0,0,0,0.28)"
  },
  kicker: {
    color: "#e85d04",
    fontSize: 12,
    fontWeight: 800,
    textTransform: "uppercase",
    letterSpacing: "0.18em"
  },
  title: {
    margin: "8px 0 0",
    fontSize: "1.75rem",
    lineHeight: 1.04,
    letterSpacing: "-0.04em",
    fontWeight: 900
  },
  subtitle: {
    margin: "12px 0 0",
    color: "#94a3b8",
    fontSize: 13,
    lineHeight: 1.65,
    maxWidth: 780
  },
  heroActions: {
    display: "flex",
    gap: 12,
    flexWrap: "wrap",
    alignContent: "flex-start",
    justifyContent: "flex-end"
  },
  ghostButton: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    minHeight: 48,
    padding: "0 16px",
    borderRadius: 999,
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#38bdf8",
    textDecoration: "none",
    fontWeight: 700
  },
  metricsGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(160px, 1fr))",
    gap: 14
  },
  workspaceGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(min(100%, 420px), 1fr))",
    gap: 18,
    alignItems: "start"
  },
  focusGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(min(100%, 420px), 1fr))",
    gap: 18,
    alignItems: "start"
  },
  leftColumn: {
    display: "grid",
    gap: 18
  },
  centerColumn: {
    display: "grid",
    gap: 18
  },
  rightColumn: {
    display: "grid",
    gap: 18
  },
  panel: {
    borderRadius: 0,
    border: "1px solid #334155",
    background: "#0f172a",
    padding: 14,
    boxShadow: "0 1px 2px rgba(0,0,0,0.08)"
  },
  modalOverlay: {
    position: "fixed",
    inset: 0,
    zIndex: 80,
    display: "grid",
    placeItems: "center",
    padding: 22,
    background: "rgba(35, 35, 35, 0.38)",
    backdropFilter: "blur(10px)"
  },
  orderModal: {
    width: "min(920px, 100%)",
    maxHeight: "calc(100vh - 44px)",
    overflowY: "auto",
    borderRadius: 30,
    border: "1px solid #334155",
    background: "#0f172a",
    padding: 24,
    boxShadow: "0 18px 60px rgba(0,0,0,0.22)"
  },
  dispatchModal: {
    width: "min(980px, 100%)",
    maxHeight: "calc(100vh - 44px)",
    overflowY: "auto",
    borderRadius: 30,
    border: "1px solid #334155",
    background: "#0f172a",
    padding: 24,
    boxShadow: "0 18px 60px rgba(0,0,0,0.22)"
  },
  modalHeader: {
    display: "flex",
    justifyContent: "space-between",
    gap: 16,
    alignItems: "flex-start",
    marginBottom: 18
  },
  modalCloseButton: {
    minHeight: 40,
    padding: "0 14px",
    borderRadius: 999,
    border: "1px solid #334155",
    background: "#020617",
    color: "#f8fafc",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    textDecoration: "none",
    fontSize: 13,
    fontWeight: 800
  },
  editorNavActions: {
    display: "flex",
    gap: 8,
    alignItems: "center",
    flexWrap: "wrap",
    justifyContent: "flex-end"
  },
  disabledNavButton: {
    minHeight: 40,
    padding: "0 14px",
    borderRadius: 999,
    border: "1px solid #1f2937",
    background: "#020617",
    color: "#475569",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    textDecoration: "none",
    fontSize: 13,
    fontWeight: 800,
    pointerEvents: "none"
  },
  panelHeader: {
    display: "flex",
    justifyContent: "space-between",
    gap: 14,
    alignItems: "flex-start",
    marginBottom: 16
  },
  panelTitle: {
    margin: 0,
    fontSize: 20,
    fontWeight: 800,
    letterSpacing: "-0.02em"
  },
  panelSub: {
    margin: "6px 0 0",
    color: "#94a3b8",
    lineHeight: 1.55,
    fontSize: 14
  },
  searchBar: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) auto",
    gap: 10,
    margin: "0 0 16px"
  },
  searchInput: {
    width: "100%",
    minHeight: 44,
    boxSizing: "border-box",
    borderRadius: 14,
    border: "1px solid #334155",
    background: "#020617",
    color: "#f8fafc",
    padding: "0 14px",
    fontSize: 14,
    outline: "none"
  },
  clearSearchButton: {
    minHeight: 44,
    padding: "0 14px",
    borderRadius: 14,
    border: "1px solid #f97316",
    background: "#0f172a",
    color: "#e85d04",
    fontWeight: 900,
    cursor: "pointer"
  },
  emptySearch: {
    padding: 16,
    borderRadius: 16,
    border: "1px dashed #334155",
    background: "#0b1220",
    color: "#94a3b8",
    fontWeight: 800
  },
  headerPill: {
    minHeight: 36,
    padding: "0 12px",
    borderRadius: 999,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    border: "1px solid #334155",
    color: "#38bdf8",
    background: "#0b1220",
    fontSize: 12,
    fontWeight: 800,
    textTransform: "uppercase",
    letterSpacing: "0.08em"
  },
  autoRouteBar: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) auto auto",
    alignItems: "center",
    gap: 12,
    padding: 12,
    margin: "0 0 16px",
    borderRadius: 6,
    border: "1px solid #334155",
    background: "#020617"
  },
  autoRouteTitle: {
    color: "#f8fafc",
    fontWeight: 900,
    fontSize: 13
  },
  autoRouteSub: {
    marginTop: 4,
    color: "#94a3b8",
    fontSize: 12,
    lineHeight: 1.4
  },
  manualToggle: {
    display: "inline-flex",
    alignItems: "center",
    gap: 8,
    minHeight: 42,
    padding: "0 12px",
    borderRadius: 999,
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#e2e8f0",
    fontSize: 12,
    fontWeight: 900,
    whiteSpace: "nowrap"
  },
  queueCard: {
    width: "100%",
    textAlign: "left",
    borderRadius: 6,
    border: "1px solid #334155",
    background: "#0b1220",
    padding: 16,
    color: "#f8fafc",
    cursor: "pointer"
  },
  queueDropActive: {
    borderColor: "rgba(34, 197, 94, 0.68)",
    background: "rgba(34, 197, 94, 0.14)",
    boxShadow: "inset 0 0 0 1px rgba(34, 197, 94, 0.28)"
  },
  cardActionRow: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) auto",
    gap: 12,
    alignItems: "center"
  },
  cardSelectArea: {
    minWidth: 0,
    color: "inherit",
    textDecoration: "none"
  },
  detailButton: {
    minHeight: 36,
    padding: "0 12px",
    borderRadius: 12,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    background: "#0f172a",
    border: "1px solid #38bdf8",
    color: "#38bdf8",
    fontSize: 12,
    fontWeight: 900,
    textDecoration: "none",
    whiteSpace: "nowrap"
  },
  queueTitle: {
    fontSize: 16,
    fontWeight: 800
  },
  queueMeta: {
    marginTop: 6,
    color: "#94a3b8",
    fontSize: 13
  },
  queueDetails: {
    marginTop: 12,
    display: "flex",
    gap: 10,
    flexWrap: "wrap",
    color: "#cbd5e1",
    fontSize: 13
  },
  queueNotes: {
    marginTop: 10,
    padding: "9px 10px",
    borderRadius: 12,
    background: "#0b1220",
    border: "1px solid #334155",
    color: "#cbd5e1",
    fontSize: 12,
    lineHeight: 1.45
  },
  queueFooter: {
    marginTop: 12,
    display: "flex",
    justifyContent: "space-between",
    gap: 12,
    color: "#94a3b8",
    fontSize: 12
  },
  formGridTwo: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: 12
  },
  formGridThree: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1.4fr) 120px 140px",
    gap: 12
  },
  label: {
    display: "block",
    marginBottom: 8,
    fontSize: 12,
    fontWeight: 800,
    letterSpacing: "0.04em",
    textTransform: "uppercase",
    color: "#cbd5e1"
  },
  input: {
    width: "100%",
    boxSizing: "border-box",
    minHeight: 48,
    padding: "12px 14px",
    borderRadius: 14,
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#f8fafc",
    fontSize: 14,
    outline: "none"
  },
  colorInput: {
    width: "100%",
    boxSizing: "border-box",
    minHeight: 48,
    padding: 6,
    borderRadius: 14,
    border: "1px solid #334155",
    background: "#0f172a",
    cursor: "pointer"
  },
  compactInput: {
    width: 82,
    boxSizing: "border-box",
    minHeight: 42,
    padding: "10px 12px",
    borderRadius: 12,
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#f8fafc",
    fontSize: 13,
    outline: "none"
  },
  compactSelect: {
    minHeight: 42,
    boxSizing: "border-box",
    padding: "10px 12px",
    borderRadius: 12,
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#f8fafc",
    fontSize: 13,
    outline: "none"
  },
  primaryButton: {
    minHeight: 50,
    borderRadius: 15,
    border: "none",
    background: "linear-gradient(135deg, #f97316, #fb7185)",
    color: "#fff7ed",
    fontWeight: 800,
    fontSize: 14,
    cursor: "pointer"
  },
  disabledActionButton: {
    minHeight: 50,
    borderRadius: 15,
    border: "1px solid #334155",
    background: "#1e293b",
    color: "#64748b",
    fontWeight: 800,
    fontSize: 14,
    cursor: "not-allowed",
    padding: "0 18px"
  },
  secondaryButton: {
    width: "100%",
    minHeight: 46,
    borderRadius: 14,
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#f8fafc",
    fontWeight: 700,
    cursor: "pointer"
  },
  dangerButton: {
    width: "100%",
    minHeight: 46,
    borderRadius: 14,
    border: "1px solid rgba(248, 113, 113, 0.55)",
    background: "rgba(127, 29, 29, 0.42)",
    color: "#fecaca",
    fontWeight: 800,
    cursor: "pointer"
  },
  smallDangerButton: {
    minHeight: 36,
    padding: "0 12px",
    borderRadius: 12,
    border: "1px solid rgba(248, 113, 113, 0.55)",
    background: "rgba(127, 29, 29, 0.42)",
    color: "#fecaca",
    fontSize: 12,
    fontWeight: 900,
    cursor: "pointer"
  },
  smallWarningButton: {
    minHeight: 36,
    padding: "0 12px",
    borderRadius: 12,
    border: "1px solid rgba(251, 191, 36, 0.55)",
    background: "rgba(120, 53, 15, 0.42)",
    color: "#fde68a",
    fontSize: 12,
    fontWeight: 900,
    cursor: "pointer"
  },
  warningButton: {
    width: "100%",
    minHeight: 46,
    borderRadius: 14,
    border: "1px solid rgba(251, 191, 36, 0.55)",
    background: "rgba(120, 53, 15, 0.42)",
    color: "#fde68a",
    fontWeight: 900,
    cursor: "pointer"
  },
  deliveredActions: {
    marginTop: 14,
    display: "flex",
    justifyContent: "flex-end",
    gap: 8,
    flexWrap: "wrap"
  },
  routeCard: (color) => ({
    borderRadius: 20,
    padding: 18,
    border: `1px solid ${color}44`,
    background: "#0f172a",
    boxShadow: `inset 4px 0 0 ${color}, 0 1px 2px rgba(0,0,0,0.08)`
  }),
  routeDropActive: {
    borderColor: "#38bdf8",
    background: "rgba(56, 189, 248, 0.12)",
    boxShadow: "inset 0 0 0 1px rgba(56, 189, 248, 0.45)"
  },
  dropHint: {
    marginTop: 12,
    padding: "10px 12px",
    borderRadius: 14,
    border: "1px dashed rgba(56, 189, 248, 0.28)",
    background: "rgba(56, 189, 248, 0.12)",
    color: "#7dd3fc",
    fontSize: 12,
    fontWeight: 900,
    textTransform: "uppercase",
    letterSpacing: "0.08em"
  },
  dropHintActive: {
    marginTop: 12,
    padding: "10px 12px",
    borderRadius: 14,
    border: "1px solid rgba(56, 189, 248, 0.65)",
    background: "rgba(56, 189, 248, 0.18)",
    color: "#7dd3fc",
    fontSize: 12,
    fontWeight: 950,
    textTransform: "uppercase",
    letterSpacing: "0.08em"
  },
  routeColor: (color) => ({
    width: 12,
    height: 12,
    borderRadius: 999,
    background: color,
    boxShadow: `0 0 0 5px ${color}22`
  }),
  routeCode: {
    fontSize: 13,
    fontWeight: 800,
    color: "#f8fafc"
  },
  routeRegion: {
    fontSize: 12,
    color: "#94a3b8"
  },
  routeStats: {
    marginTop: 14,
    display: "flex",
    gap: 10,
    flexWrap: "wrap",
    color: "#cbd5e1",
    fontSize: 13
  },
  routeTimePill: {
    minHeight: 42,
    padding: "0 14px",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 12,
    border: "1px solid rgba(56, 189, 248, 0.4)",
    background: "rgba(14, 165, 233, 0.12)",
    color: "#bae6fd",
    fontSize: 12,
    fontWeight: 900,
    whiteSpace: "nowrap"
  },
  sequenceForm: {
    marginTop: 12,
    display: "flex",
    gap: 8,
    flexWrap: "wrap",
    justifyContent: "flex-end"
  },
  routeCreateForm: {
    marginTop: 16,
    paddingTop: 16,
    borderTop: "1px solid rgba(51, 65, 85, 0.82)",
    display: "grid",
    gap: 12
  },
  resourceSummary: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: 14
  },
  resourceList: {
    display: "flex",
    gap: 8,
    flexWrap: "wrap",
    marginTop: 8
  },
  resourceCard: {
    minWidth: 220,
    display: "grid",
    gap: 5,
    padding: 14,
    borderRadius: 16,
    background: "#0b1220",
    border: "1px solid #334155",
    color: "#f8fafc"
  },
  resourcePill: {
    display: "inline-flex",
    alignItems: "center",
    minHeight: 28,
    padding: "0 10px",
    borderRadius: 999,
    background: "#111827",
    border: "1px solid #334155",
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 700
  },
  assignForm: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    flexWrap: "wrap",
    justifyContent: "flex-end"
  },
  assignButton: {
    minHeight: 42,
    padding: "0 14px",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 12,
    border: "1px solid rgba(34, 197, 94, 0.4)",
    background: "rgba(34, 197, 94, 0.12)",
    color: "#bbf7d0",
    textDecoration: "none",
    fontWeight: 800,
    cursor: "pointer"
  },
  stopList: {
    marginTop: 14,
    display: "grid",
    gap: 8
  },
  stopRow: {
    display: "grid",
    gridTemplateColumns: "26px minmax(0, 1fr) auto",
    gap: 10,
    alignItems: "center",
    padding: "10px 12px",
    borderRadius: 14,
    background: "#0b1220",
    border: "1px solid #334155",
    textDecoration: "none",
    color: "#f8fafc",
    userSelect: "none"
  },
  dragHandle: {
    width: 26,
    minHeight: 42,
    borderRadius: 10,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    border: "1px solid rgba(56, 189, 248, 0.28)",
    background: "rgba(56, 189, 248, 0.12)",
    color: "#7dd3fc",
    fontWeight: 950,
    cursor: "grab",
    userSelect: "none",
    touchAction: "none"
  },
  stopSelectArea: {
    display: "grid",
    gridTemplateColumns: "32px minmax(0, 1fr) auto",
    gap: 10,
    alignItems: "center",
    minWidth: 0,
    color: "inherit",
    textDecoration: "none"
  },
  stopActions: {
    display: "flex",
    gap: 6,
    alignItems: "center",
    flexWrap: "wrap",
    justifyContent: "flex-end"
  },
  routeReorderForm: {
    display: "flex",
    gap: 4,
    alignItems: "center"
  },
  stopMoveSelect: {
    minHeight: 30,
    maxWidth: 96,
    borderRadius: 10,
    border: "1px solid rgba(34, 197, 94, 0.38)",
    background: "rgba(34, 197, 94, 0.14)",
    color: "#86efac",
    fontSize: 11,
    fontWeight: 900,
    padding: "0 8px",
    cursor: "pointer"
  },
  stopDetailButton: {
    minHeight: 30,
    padding: "0 9px",
    borderRadius: 10,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    background: "#0f172a",
    border: "1px solid #38bdf8",
    color: "#38bdf8",
    fontSize: 11,
    fontWeight: 900,
    textDecoration: "none",
    whiteSpace: "nowrap"
  },
  disabledStopButton: {
    opacity: 0.35,
    cursor: "not-allowed"
  },
  stopNumber: {
    width: 28,
    height: 28,
    borderRadius: 999,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    background: "#0f172a",
    border: "1px solid #334155",
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 900
  },
  stopMain: {
    display: "grid",
    gap: 3,
    minWidth: 0
  },
  stopStatus: (color) => ({
    minHeight: 28,
    padding: "0 9px",
    borderRadius: 999,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    color,
    background: `${color}1f`,
    border: `1px solid ${color}55`,
    fontSize: 11,
    fontWeight: 800,
    whiteSpace: "nowrap"
  }),
  stopStatusForm: {
    display: "grid",
    gap: 12,
    padding: 14,
    borderRadius: 18,
    background: "#0b1220",
    border: "1px solid #334155"
  },
  assignmentPanel: {
    display: "grid",
    gap: 12,
    padding: 14,
    borderRadius: 18,
    background: "rgba(56, 189, 248, 0.12)",
    border: "1px solid #b6e4f8"
  },
  assignmentForm: {
    display: "grid",
    gap: 12
  },
  checklistGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: 8
  },
  checkboxLabel: {
    minHeight: 38,
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "8px 10px",
    borderRadius: 12,
    border: "1px solid #334155",
    background: "#0b1220",
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 800
  },
  mapStage: {
    position: "relative",
    minHeight: 380,
    borderRadius: 0,
    overflow: "hidden",
    border: "1px solid #334155",
    background: "#dceecf"
  },
  googleMapCanvas: {
    position: "absolute",
    inset: 0,
    minHeight: 380
  },
  mapStatus: {
    position: "absolute",
    inset: 0,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    padding: 24,
    background: "rgba(255, 255, 255, 0.86)",
    color: "#f8fafc",
    fontSize: 14,
    fontWeight: 800,
    textAlign: "center"
  },
  mapNotice: {
    position: "absolute",
    top: 16,
    left: 16,
    right: 16,
    display: "grid",
    gap: 6,
    padding: "10px 12px",
    borderRadius: 14,
    background: "rgba(249, 115, 22, 0.16)",
    border: "1px solid rgba(249, 115, 22, 0.38)",
    color: "#fed7aa",
    fontSize: 12,
    fontWeight: 800,
    zIndex: 2
  },
  mapLegend: {
    position: "absolute",
    left: 16,
    bottom: 16,
    display: "grid",
    gap: 8,
    padding: 12,
    borderRadius: 16,
    background: "#0f172a",
    border: "1px solid #334155",
    color: "#f8fafc",
    fontSize: 12
  },
  detailId: {
    color: "#38bdf8",
    fontSize: 12,
    fontWeight: 800,
    letterSpacing: "0.12em",
    textTransform: "uppercase"
  },
  detailTitle: {
    marginTop: 8,
    fontSize: 24,
    fontWeight: 800,
    lineHeight: 1.12
  },
  detailMeta: {
    marginTop: 6,
    color: "#94a3b8"
  },
  detailGrid: {
    display: "grid",
    gap: 12
  },
  detailLabel: {
    color: "#94a3b8",
    fontSize: 12,
    textTransform: "uppercase",
    letterSpacing: "0.08em",
    fontWeight: 800
  },
  detailValue: {
    marginTop: 4,
    color: "#f8fafc",
    fontWeight: 700,
    lineHeight: 1.5
  },
  notesBlock: {
    borderRadius: 18,
    padding: 16,
    background: "#0b1220",
    border: "1px solid #334155"
  },
  deliveredPhoto: {
    width: "100%",
    maxHeight: 520,
    marginTop: 10,
    borderRadius: 16,
    objectFit: "contain",
    background: "#111827",
    border: "1px solid #334155"
  },
  todoItem: {
    display: "flex",
    gap: 10,
    alignItems: "flex-start",
    color: "#cbd5e1",
    lineHeight: 1.55
  },
  todoDot: {
    width: 8,
    height: 8,
    marginTop: 7,
    borderRadius: 999,
    background: "#38bdf8",
    boxShadow: "0 0 0 5px rgba(14, 165, 198, 0.12)",
    flex: "0 0 auto"
  },
  statusOk: {
    padding: "14px 16px",
    borderRadius: 14,
    background: "rgba(34, 197, 94, 0.14)",
    border: "1px solid rgba(34, 197, 94, 0.38)",
    color: "#86efac",
    fontWeight: 700
  },
  statusWarn: {
    padding: "14px 16px",
    borderRadius: 14,
    background: "rgba(245, 158, 11, 0.16)",
    border: "1px solid rgba(245, 158, 11, 0.38)",
    color: "#fde68a",
    fontWeight: 600,
    lineHeight: 1.6
  },
  statusErr: {
    padding: "14px 16px",
    borderRadius: 14,
    background: "rgba(127, 29, 29, 0.35)",
    border: "1px solid rgba(248, 113, 113, 0.4)",
    color: "#fecaca",
    fontWeight: 700
  },
  skipReasonList: {
    display: "grid",
    gap: 8,
    marginTop: 12
  },
  skipReasonItem: {
    display: "grid",
    gap: 3,
    padding: "10px 12px",
    borderRadius: 12,
    background: "#0b1220",
    border: "1px solid #334155",
    color: "inherit",
    fontSize: 13,
    lineHeight: 1.35
  },
  badge: (status) => {
    const palette = status === "scheduled" ? {
      color: "#bbf7d0",
      border: "rgba(34, 197, 94, 0.35)",
      bg: "rgba(34, 197, 94, 0.12)"
    } : status === "delivered" ? {
      color: "#bae6fd",
      border: "rgba(56, 189, 248, 0.35)",
      bg: "rgba(56, 189, 248, 0.12)"
    } : status === "cancelled" ? {
      color: "#fecaca",
      border: "rgba(248, 113, 113, 0.38)",
      bg: "rgba(127, 29, 29, 0.22)"
    } : status === "hold" ? {
      color: "#fde68a",
      border: "rgba(234, 179, 8, 0.35)",
      bg: "rgba(234, 179, 8, 0.12)"
    } : {
      color: "#fed7aa",
      border: "rgba(249, 115, 22, 0.35)",
      bg: "rgba(249, 115, 22, 0.12)"
    };
    return {
      minHeight: 30,
      padding: "0 10px",
      borderRadius: 999,
      border: `1px solid ${palette.border}`,
      background: palette.bg,
      color: palette.color,
      fontSize: 11,
      fontWeight: 800,
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      textTransform: "uppercase",
      letterSpacing: "0.08em"
    };
  }
};
const route4 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$p,
  default: dispatch,
  loader: loader$o,
  shouldRevalidate: shouldRevalidate$1
}, Symbol.toStringTag, { value: "Module" }));
const EXPORT_TIMEZONE = process.env.DISPATCH_RESET_TIMEZONE || "America/Chicago";
function getLocalDateKey(date = /* @__PURE__ */ new Date()) {
  return new Intl.DateTimeFormat("en-CA", {
    timeZone: EXPORT_TIMEZONE,
    year: "numeric",
    month: "2-digit",
    day: "2-digit"
  }).format(date);
}
function parseRequestedDate$6(value) {
  const trimmed = String(value || "").trim();
  if (!trimmed || /needs scheduling|unavailable|unknown/i.test(trimmed)) return null;
  const today = /* @__PURE__ */ new Date();
  if (/\btoday\b/i.test(trimmed)) {
    return new Date(today.getFullYear(), today.getMonth(), today.getDate());
  }
  if (/\btomorrow\b/i.test(trimmed)) {
    const tomorrow = new Date(today.getFullYear(), today.getMonth(), today.getDate());
    tomorrow.setDate(tomorrow.getDate() + 1);
    return tomorrow;
  }
  const isoDate = trimmed.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (isoDate) return new Date(Number(isoDate[1]), Number(isoDate[2]) - 1, Number(isoDate[3]));
  const slashDate = trimmed.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return new Date(year, Number(slashDate[1]) - 1, Number(slashDate[2]));
  }
  const parsed = new Date(trimmed);
  if (Number.isNaN(parsed.getTime())) return null;
  return new Date(parsed.getFullYear(), parsed.getMonth(), parsed.getDate());
}
function dateKey$5(date) {
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function timestampDateKey(date) {
  return new Intl.DateTimeFormat("en-CA", {
    timeZone: EXPORT_TIMEZONE,
    year: "numeric",
    month: "2-digit",
    day: "2-digit"
  }).format(date);
}
function getSelectedDateKey(dateParam, requestedDate) {
  return /^\d{4}-\d{2}-\d{2}$/.test(dateParam) ? dateParam : dateKey$5(requestedDate);
}
function formatSheetDate(date) {
  return `${date.getMonth() + 1}/${date.getDate()}/${date.getFullYear()}`;
}
function getOrderImportDate(order) {
  const value = order.created_at || "";
  if (!value) return null;
  const parsed = new Date(value);
  if (Number.isNaN(parsed.getTime())) return null;
  return parsed;
}
function escapeCell(value) {
  return String(value ?? "").replace(/\r?\n/g, " ").replace(/\t/g, " ").replace(/\s{2,}/g, " ").trim();
}
function escapeHtml(value) {
  return escapeCell(value).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
}
function cleanOrderNumber(order) {
  return String(order.orderNumber || order.id || "").replace(/^#/, "").trim();
}
function extractPhone$2(order) {
  const text = [order.contact, order.notes, order.rawEmail].filter(Boolean).join("\n");
  const match = text.match(/(?:\+?1[\s.-]?)?(?:\(?([2-9]\d{2})\)?[\s.-]?)([2-9]\d{2})[\s.-]?(\d{4})/);
  return match ? `${match[1]}-${match[2]}-${match[3]}` : "";
}
function getCity(order) {
  const city = String(order.city || "").trim();
  if (city) {
    return city.split(",")[0].replace(/\s+[A-Z]{2}\s+\d{5}(?:-\d{4})?$/i, "").trim();
  }
  const parts = String(order.address || "").split(",").map((part) => part.trim()).filter(Boolean);
  return parts.length >= 2 ? parts[parts.length - 2] : "";
}
function getUnitLabel(unit, quantity) {
  const normalized = String(unit || "Unit").trim().toLowerCase();
  const qty = Number(String(quantity || "").replace(/[^\d.]/g, ""));
  const plural = !Number.isFinite(qty) || qty !== 1;
  if (/yards?/.test(normalized)) return plural ? "YARDS" : "YARD";
  if (/tons?/.test(normalized)) return plural ? "TONS" : "TON";
  if (/bags?/.test(normalized)) return plural ? "BAGS" : "BAG";
  if (/gallons?/.test(normalized)) return plural ? "GALLONS" : "GALLON";
  return plural ? "UNITS" : "UNIT";
}
function getQuantity(order) {
  const quantity = String(order.quantity || "").trim();
  return [quantity, getUnitLabel(order.unit, quantity)].filter(Boolean).join(" ");
}
function getTimePreference(order) {
  const value = `${order.timePreference || ""} ${order.requestedWindow || ""} ${order.notes || ""}`;
  if (/\bmorning\b|\bam\b/i.test(value)) return "AM";
  if (/\bafternoon\b|\bpm\b/i.test(value)) return "PM";
  if (/\bevening\b/i.test(value)) return "EVENING";
  return "";
}
function getSheetStatus(order) {
  if (order.status === "delivered" || order.deliveryStatus === "delivered") {
    return "DELIVERED";
  }
  if (order.status === "new" || order.status === "hold") return "PENDING DISPATCH";
  return "DISPATCHED";
}
function orderSortValue(order) {
  const numeric = Number(String(order.orderNumber || "").replace(/\D/g, ""));
  return Number.isFinite(numeric) && numeric > 0 ? numeric : Number.MAX_SAFE_INTEGER;
}
function buildSpreadsheetRows(orders, requestedDate) {
  return orders.slice().sort((a, b) => orderSortValue(a) - orderSortValue(b)).map((order) => [cleanOrderNumber(order), order.customer, extractPhone$2(order), getCity(order), order.material, getQuantity(order), "DELIVERY", formatSheetDate(parseRequestedDate$6(order.requestedWindow) || requestedDate), getTimePreference(order), getSheetStatus(order)]);
}
function getStatusClass(status) {
  if (status === "DELIVERED") return "status-delivered";
  if (status === "PENDING DISPATCH") return "status-pending";
  return "status-dispatched";
}
function buildExcelHtml(orders, requestedDate) {
  if (!orders.length) {
    return `<!doctype html>
<html>
  <head><meta charset="utf-8" /></head>
  <body>No Shopify-imported dispatch orders found for ${escapeHtml(formatSheetDate(requestedDate))}</body>
</html>`;
  }
  const rows = buildSpreadsheetRows(orders, requestedDate).map((row) => {
    const status = String(row[9] || "");
    return `<tr>${row.map((cell, index) => {
      const className = index === 9 ? getStatusClass(status) : "";
      return `<td class="${className}">${escapeHtml(cell)}</td>`;
    }).join("")}</tr>`;
  }).join("\n");
  return `<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <style>
      table {
        border-collapse: collapse;
        font-family: Arial, Helvetica, sans-serif;
        font-size: 11pt;
      }
      td {
        border: 1px solid #000;
        padding: 2px 8px;
        height: 20px;
        font-weight: 700;
        text-align: center;
        vertical-align: middle;
        white-space: nowrap;
        background: #fff;
        color: #000;
        mso-number-format: "\\@";
      }
      td:nth-child(1) { min-width: 90px; }
      td:nth-child(2) { min-width: 220px; }
      td:nth-child(3) { min-width: 120px; }
      td:nth-child(4) { min-width: 150px; }
      td:nth-child(5) { min-width: 320px; }
      td:nth-child(6) { min-width: 120px; }
      td:nth-child(7) { min-width: 90px; }
      td:nth-child(8) { min-width: 110px; }
      td:nth-child(9) { min-width: 135px; }
      td:nth-child(10) { min-width: 220px; }
      .status-dispatched { background: #ffe600; }
      .status-delivered { background: #00ff00; }
      .status-pending { background: #ff00ff; }
    </style>
  </head>
  <body>
    <table>${rows}</table>
  </body>
</html>`;
}
async function loader$n({
  request
}) {
  const allowed = await hasAdminQuotePermissionAccess(request, "dispatch");
  if (!allowed) return new Response("Unauthorized", {
    status: 401
  });
  const url = new URL(request.url);
  const dateParam = url.searchParams.get("date") || getLocalDateKey();
  const requestedDate = parseRequestedDate$6(dateParam) || /* @__PURE__ */ new Date();
  const requestedKey = getSelectedDateKey(dateParam, requestedDate);
  const orders = (await getDispatchOrders()).filter((order) => {
    if (order.status === "cancelled") return false;
    if (!String(order.mailboxMessageId || "").startsWith("shopify:")) return false;
    const importDate = getOrderImportDate(order);
    return importDate ? timestampDateKey(importDate) === requestedKey : false;
  });
  return new Response(buildExcelHtml(orders, requestedDate), {
    headers: {
      "Content-Type": "application/vnd.ms-excel; charset=utf-8",
      "Content-Disposition": `attachment; filename="dispatch-orders-${requestedKey}.xls"`,
      "Cache-Control": "no-store"
    }
  });
}
const route5 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  loader: loader$n
}, Symbol.toStringTag, { value: "Module" }));
const loader$m = loader$o;
const action$o = action$p;
const shouldRevalidate = shouldRevalidate$1;
function getOrderNumber$5(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function getOrderAddress$4(order) {
  return [order.address, order.city].filter(Boolean).join(", ");
}
function getTravelMinutes$4(order) {
  const minutes = Number(order.travelMinutes || 0);
  return Number.isFinite(minutes) && minutes > 0 ? minutes : 0;
}
function formatTime$1(minutes) {
  if (!minutes) return "-";
  if (minutes < 60) return `${Math.round(minutes)} min`;
  const hours = Math.floor(minutes / 60);
  const remainder = Math.round(minutes % 60);
  return remainder ? `${hours}h ${remainder}m` : `${hours}h`;
}
function getTodayDateInputValue() {
  const today = /* @__PURE__ */ new Date();
  return `${today.getFullYear()}-${String(today.getMonth() + 1).padStart(2, "0")}-${String(today.getDate()).padStart(2, "0")}`;
}
function toDateInputValue(date) {
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function parseDateInputValue(value) {
  const match = value.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (!match) return null;
  return new Date(Number(match[1]), Number(match[2]) - 1, Number(match[3]));
}
function addDays(date, days) {
  const next = new Date(date.getFullYear(), date.getMonth(), date.getDate());
  next.setDate(next.getDate() + days);
  return next;
}
function startOfWeek$1(date) {
  const weekStart = new Date(date.getFullYear(), date.getMonth(), date.getDate());
  weekStart.setDate(weekStart.getDate() - weekStart.getDay());
  return weekStart;
}
function parseRequestedDate$5(value) {
  var _a2;
  const trimmed = String(value || "").trim();
  if (!trimmed || /needs scheduling|unavailable|unknown/i.test(trimmed)) return null;
  const today = /* @__PURE__ */ new Date();
  const lower = trimmed.toLowerCase();
  if (/\btoday\b/.test(lower)) {
    return new Date(today.getFullYear(), today.getMonth(), today.getDate());
  }
  if (/\btomorrow\b/.test(lower)) {
    return addDays(today, 1);
  }
  const isoDate = trimmed.match(/^(\d{4})-(\d{1,2})-(\d{1,2})/);
  if (isoDate) {
    return new Date(Number(isoDate[1]), Number(isoDate[2]) - 1, Number(isoDate[3]));
  }
  const slashDate = trimmed.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return new Date(year, Number(slashDate[1]) - 1, Number(slashDate[2]));
  }
  const monthDate = (_a2 = trimmed.match(/\b(?:jan(?:uary)?|feb(?:ruary)?|mar(?:ch)?|apr(?:il)?|may|jun(?:e)?|jul(?:y)?|aug(?:ust)?|sep(?:t(?:ember)?)?|oct(?:ober)?|nov(?:ember)?|dec(?:ember)?)\.?\s+\d{1,2}(?:,\s*\d{4})?/i)) == null ? void 0 : _a2[0];
  if (monthDate) {
    const parsed2 = new Date(/\d{4}/.test(monthDate) ? monthDate : `${monthDate}, ${today.getFullYear()}`);
    if (!Number.isNaN(parsed2.getTime())) {
      return new Date(parsed2.getFullYear(), parsed2.getMonth(), parsed2.getDate());
    }
  }
  const parsed = new Date(trimmed);
  if (Number.isNaN(parsed.getTime())) return null;
  return new Date(parsed.getFullYear(), parsed.getMonth(), parsed.getDate());
}
function getOrderPlanningDateKey(order) {
  const date = parseRequestedDate$5(order.requestedWindow);
  return date ? toDateInputValue(date) : "unscheduled";
}
function formatPlannerDate(date) {
  return date.toLocaleDateString("en-US", {
    weekday: "short",
    month: "short",
    day: "numeric"
  });
}
function statusLabel(order) {
  if (order.status === "delivered" || order.deliveryStatus === "delivered") return "Delivered";
  if (order.status === "cancelled") return "Cancelled";
  if (order.assignedRouteId || order.status === "scheduled") return "Scheduled";
  if (order.status === "hold") return "On hold";
  return "Unscheduled";
}
function getStatusTone(order) {
  if (order.status === "delivered" || order.deliveryStatus === "delivered") return "delivered";
  if (order.status === "cancelled") return "cancelled";
  if (order.status === "hold") return "hold";
  if (order.assignedRouteId || order.status === "scheduled") return "scheduled";
  return "unscheduled";
}
function isCancelledOrder(order) {
  return order.status === "cancelled";
}
function isDeliveredOrder(order) {
  return order.status === "delivered" || order.deliveryStatus === "delivered";
}
function isActiveBoardOrder(order) {
  return !isDeliveredOrder(order) && !isCancelledOrder(order);
}
function attachLoaderNote(event) {
  const note = window.prompt("Add a loader note for this load. Leave blank if no note is needed.");
  if (note === null) {
    event.preventDefault();
    return;
  }
  let noteInput = event.currentTarget.querySelector('input[name="loaderNote"]');
  if (!noteInput) {
    noteInput = document.createElement("input");
    noteInput.type = "hidden";
    noteInput.name = "loaderNote";
    event.currentTarget.appendChild(noteInput);
  }
  noteInput.value = note.trim();
}
async function playDispatchChime() {
  var _a2;
  if (typeof window === "undefined") return;
  const AudioContextClass = window.AudioContext || window.webkitAudioContext;
  if (!AudioContextClass) return;
  const audio = dispatchChimeAudio || new AudioContextClass();
  dispatchChimeAudio = audio;
  if (audio.state === "suspended") {
    await audio.resume();
  }
  const now = audio.currentTime;
  const masterGain = audio.createGain();
  const compressor = audio.createDynamicsCompressor();
  compressor.threshold.setValueAtTime(-24, now);
  compressor.knee.setValueAtTime(30, now);
  compressor.ratio.setValueAtTime(8, now);
  compressor.attack.setValueAtTime(3e-3, now);
  compressor.release.setValueAtTime(0.25, now);
  compressor.connect(audio.destination);
  masterGain.connect(compressor);
  masterGain.gain.setValueAtTime(1e-4, now);
  masterGain.gain.exponentialRampToValueAtTime(0.72, now + 0.025);
  masterGain.gain.exponentialRampToValueAtTime(1e-4, now + 1.25);
  [784, 1046.5, 1318.5].forEach((frequency, index) => {
    const oscillator = audio.createOscillator();
    const noteGain = audio.createGain();
    const start = now + index * 0.16;
    const end = start + 0.55;
    oscillator.type = index === 1 ? "triangle" : "square";
    oscillator.frequency.setValueAtTime(frequency, start);
    noteGain.gain.setValueAtTime(1e-4, start);
    noteGain.gain.exponentialRampToValueAtTime(0.4, start + 0.025);
    noteGain.gain.exponentialRampToValueAtTime(1e-4, end);
    oscillator.connect(noteGain);
    noteGain.connect(masterGain);
    oscillator.start(start);
    oscillator.stop(end);
  });
  (_a2 = navigator.vibrate) == null ? void 0 : _a2.call(navigator, [80, 40, 80]);
}
function dateSearchValues(value) {
  const raw = String(value || "").trim();
  if (!raw) return [];
  const normalized = raw.replace(/\s+/g, " ");
  const match = normalized.match(/^(\d{4})-(\d{1,2})-(\d{1,2})/) || normalized.match(/^(\d{1,2})\/(\d{1,2})\/(\d{4})/);
  if (!match) return [normalized];
  const isIso = match[1].length === 4;
  const year = isIso ? match[1] : match[3];
  const month = isIso ? match[2] : match[1];
  const day = isIso ? match[3] : match[2];
  const monthNumber = Number(month);
  const dayNumber = Number(day);
  const date = new Date(Number(year), monthNumber - 1, dayNumber);
  const monthName = Number.isFinite(date.getTime()) ? date.toLocaleString("en-US", {
    month: "long"
  }) : "";
  const shortMonthName = Number.isFinite(date.getTime()) ? date.toLocaleString("en-US", {
    month: "short"
  }) : "";
  return [normalized, `${year}-${String(monthNumber).padStart(2, "0")}-${String(dayNumber).padStart(2, "0")}`, `${monthNumber}/${dayNumber}/${year}`, `${String(monthNumber).padStart(2, "0")}/${String(dayNumber).padStart(2, "0")}/${year}`, `${monthName} ${dayNumber} ${year}`, `${shortMonthName} ${dayNumber} ${year}`].filter(Boolean);
}
function buildSearchText$2(order) {
  return [order.id, order.orderNumber, order.customer, order.contact, order.address, order.city, order.material, order.quantity, order.unit, order.requestedWindow, ...dateSearchValues(order.requestedWindow), order.eta, ...dateSearchValues(order.eta), order.arrivedAt, ...dateSearchValues(order.arrivedAt), order.departedAt, ...dateSearchValues(order.departedAt), order.deliveredAt, ...dateSearchValues(order.deliveredAt), order.timePreference, order.notes, order.status].filter(Boolean).join(" ").toLowerCase();
}
const CLASSIC_COLUMN_WIDTHS_KEY = "classicDispatchColumnWidths";
const CLASSIC_PANEL_LAYOUT_KEY = "classicDispatchPanelLayout";
const CLASSIC_SELECTED_ROUTE_KEY = "classicDispatchSelectedRouteId";
const CLASSIC_PLANNING_DATE_KEY = "classicDispatchPlanningDate";
const DISPATCH_NAV_COLLAPSED_KEY$3 = "dispatchNavCollapsed";
const DISPATCH_CHIME_ENABLED_KEY = "dispatchChimeEnabled";
const CLASSIC_MANUAL_ROUTING_KEY = "dispatchManualRoutingMode";
const MIN_CLASSIC_COLUMN_WIDTH = 48;
const DEFAULT_CLASSIC_PANEL_LAYOUT = {
  mainLeft: 50,
  leftRows: [25, 31, 44],
  rightRows: [55, 27, 18]
};
const defaultColumnWidths = {
  routes: {
    code: 90,
    driver: 170,
    status: 90,
    weight: 80,
    start: 95,
    finish: 95,
    distance: 110
  },
  sites: {
    stop: 60,
    orderNo: 95,
    sku: 110,
    customer: 160,
    product: 180,
    quantity: 80,
    unit: 80,
    address: 220,
    requested: 130,
    timePreference: 150,
    arrived: 95,
    departed: 95,
    eta: 110,
    miles: 70
  },
  orders: {
    type: 55,
    orderNo: 95,
    date: 100,
    client: 160,
    address: 220,
    weight: 80,
    volume: 85,
    status: 105,
    material: 180,
    timePreference: 150,
    route: 100
  },
  unscheduled: {
    orderNo: 95,
    date: 100,
    client: 160,
    address: 220,
    product: 180,
    roundTrip: 110,
    weight: 80,
    volume: 85,
    timePreference: 150,
    notes: 220,
    route: 100
  }
};
let dispatchChimeAudio = null;
function normalizeSortValue(value) {
  if (value === null || value === void 0 || value === "") return "";
  if (typeof value === "number") return value;
  const text = String(value).trim();
  const numeric = Number(text.replace(/[^0-9.-]/g, ""));
  if (text && !Number.isNaN(numeric) && /[0-9]/.test(text)) return numeric;
  const parsedDate = new Date(text);
  if (!Number.isNaN(parsedDate.getTime()) && /[0-9]/.test(text)) {
    return parsedDate.getTime();
  }
  return text.toLowerCase();
}
function normalizeRows(rows, fallback) {
  if (rows.length !== 3 || rows.some((row) => !Number.isFinite(row))) {
    return [...fallback];
  }
  const clamped = rows.map((row) => Math.max(12, row));
  const total = clamped.reduce((sum, row) => sum + row, 0) || 100;
  const normalized = clamped.map((row) => row / total * 100);
  return [normalized[0], normalized[1], normalized[2]];
}
function normalizePanelLayout(value) {
  const layout = value;
  const mainLeft = typeof (layout == null ? void 0 : layout.mainLeft) === "number" && Number.isFinite(layout.mainLeft) ? Math.min(68, Math.max(32, layout.mainLeft)) : DEFAULT_CLASSIC_PANEL_LAYOUT.mainLeft;
  return {
    mainLeft,
    leftRows: normalizeRows(Array.isArray(layout == null ? void 0 : layout.leftRows) ? layout.leftRows : DEFAULT_CLASSIC_PANEL_LAYOUT.leftRows, DEFAULT_CLASSIC_PANEL_LAYOUT.leftRows),
    rightRows: normalizeRows(Array.isArray(layout == null ? void 0 : layout.rightRows) ? layout.rightRows : DEFAULT_CLASSIC_PANEL_LAYOUT.rightRows, DEFAULT_CLASSIC_PANEL_LAYOUT.rightRows)
  };
}
function compareSortValues(left, right, direction) {
  const leftValue = normalizeSortValue(left);
  const rightValue = normalizeSortValue(right);
  const modifier = direction === "asc" ? 1 : -1;
  if (leftValue === rightValue) return 0;
  if (leftValue === "") return 1;
  if (rightValue === "") return -1;
  return leftValue > rightValue ? modifier : -modifier;
}
function sortItems(items, sort, getValue) {
  return [...items].sort((left, right) => compareSortValues(getValue(left, sort.key), getValue(right, sort.key), sort.direction));
}
let classicGoogleMapsLoader = null;
const classicDirectionsCache = /* @__PURE__ */ new Map();
function loadClassicGoogleMaps(apiKey) {
  var _a2;
  if (typeof window === "undefined") return Promise.resolve();
  if ((_a2 = window.google) == null ? void 0 : _a2.maps) return Promise.resolve();
  if (classicGoogleMapsLoader) return classicGoogleMapsLoader;
  classicGoogleMapsLoader = new Promise((resolve, reject) => {
    const existing = document.querySelector('script[data-classic-google-maps="true"]');
    if (existing) {
      existing.addEventListener("load", () => resolve(), {
        once: true
      });
      existing.addEventListener("error", () => reject(new Error("Failed to load Google Maps")), {
        once: true
      });
      return;
    }
    const script = document.createElement("script");
    script.dataset.classicGoogleMaps = "true";
    script.async = true;
    script.defer = true;
    script.src = `https://maps.googleapis.com/maps/api/js?key=${encodeURIComponent(apiKey)}`;
    script.onload = () => resolve();
    script.onerror = () => reject(new Error("Failed to load Google Maps"));
    document.head.appendChild(script);
  });
  return classicGoogleMapsLoader;
}
function ClassicMapFallback({
  routes: routes2
}) {
  const colors = ["#d93025", "#f97316", "#2563eb", "#0f9f9a", "#9333ea"];
  const activeRoutes = useMemo(() => routes2.filter((route57) => route57.orders.length), [routes2]);
  return /* @__PURE__ */ jsxs("div", {
    className: "classic-map-panel",
    style: styles$a.mapCanvas,
    children: [/* @__PURE__ */ jsxs("div", {
      style: styles$a.mapToolbar,
      children: [/* @__PURE__ */ jsx("button", {
        style: styles$a.mapToggle,
        children: "Map"
      }), /* @__PURE__ */ jsx("button", {
        style: styles$a.mapToggleMuted,
        children: "Hybrid"
      })]
    }), /* @__PURE__ */ jsxs("svg", {
      viewBox: "0 0 720 430",
      preserveAspectRatio: "none",
      style: styles$a.routeSvg,
      children: [/* @__PURE__ */ jsx("defs", {
        children: /* @__PURE__ */ jsx("pattern", {
          id: "classic-grid",
          width: "42",
          height: "42",
          patternUnits: "userSpaceOnUse",
          children: /* @__PURE__ */ jsx("path", {
            d: "M 42 0 L 0 0 0 42",
            fill: "none",
            stroke: "#d9e8d0",
            strokeWidth: "1"
          })
        })
      }), /* @__PURE__ */ jsx("rect", {
        width: "720",
        height: "430",
        fill: "#eef7e8"
      }), /* @__PURE__ */ jsx("rect", {
        width: "720",
        height: "430",
        fill: "url(#classic-grid)",
        opacity: "0.85"
      }), /* @__PURE__ */ jsx("path", {
        d: "M0 290 C115 250 175 300 260 250 S430 175 720 215 L720 430 L0 430Z",
        fill: "#b8dcff",
        opacity: "0.9"
      }), /* @__PURE__ */ jsx("path", {
        d: "M80 80 C190 125 265 95 375 145 C500 205 560 125 665 170",
        stroke: "#f3c478",
        strokeWidth: "12",
        fill: "none",
        opacity: "0.8"
      }), /* @__PURE__ */ jsx("path", {
        d: "M50 250 C145 215 220 235 335 200 C455 165 550 210 680 160",
        stroke: "#f3c478",
        strokeWidth: "10",
        fill: "none",
        opacity: "0.75"
      }), /* @__PURE__ */ jsx("path", {
        d: "M165 30 C180 120 250 145 255 250 C260 330 350 360 500 385",
        stroke: "#f3c478",
        strokeWidth: "10",
        fill: "none",
        opacity: "0.75"
      }), activeRoutes.slice(0, 5).map((route57, routeIndex) => {
        const color = route57.color || colors[routeIndex % colors.length];
        const startY = 88 + routeIndex * 60;
        const path = `M ${78 + routeIndex * 12} ${startY} C ${180 + routeIndex * 22} ${45 + routeIndex * 52}, ${260 + routeIndex * 18} ${220 - routeIndex * 20}, ${370 + routeIndex * 28} ${160 + routeIndex * 44} S ${570 - routeIndex * 25} ${280 - routeIndex * 18}, ${650 - routeIndex * 18} ${120 + routeIndex * 56}`;
        return /* @__PURE__ */ jsxs("g", {
          children: [/* @__PURE__ */ jsx("path", {
            d: path,
            stroke: color,
            strokeWidth: "8",
            fill: "none",
            strokeLinecap: "round",
            strokeLinejoin: "round"
          }), route57.orders.slice(0, 8).map((order, stopIndex) => {
            const x = 90 + stopIndex * 72 + routeIndex * 10;
            const y = startY + Math.sin(stopIndex + routeIndex) * 54 + stopIndex * 18;
            return /* @__PURE__ */ jsxs("g", {
              children: [/* @__PURE__ */ jsx("circle", {
                cx: x,
                cy: y,
                r: "13",
                fill: "#fff",
                stroke: color,
                strokeWidth: "5"
              }), /* @__PURE__ */ jsx("text", {
                x,
                y: y + 4,
                textAnchor: "middle",
                fontSize: "12",
                fontWeight: "800",
                fill: "#1f2937",
                children: stopIndex + 1
              })]
            }, order.id);
          })]
        }, route57.id);
      })]
    }), /* @__PURE__ */ jsx("div", {
      style: styles$a.mapLegend,
      children: activeRoutes.slice(0, 5).map((route57) => /* @__PURE__ */ jsxs("span", {
        style: styles$a.legendItem,
        children: [/* @__PURE__ */ jsx("span", {
          style: {
            ...styles$a.legendDot,
            background: route57.color
          }
        }), route57.code, " ", route57.truck || "Unassigned"]
      }, route57.id))
    })]
  });
}
function ClassicMap({
  googleMapsApiKey,
  originAddress,
  routes: routes2,
  driverLocations
}) {
  const mapRef = useRef(null);
  const mapInstanceRef = useRef(null);
  const mapObjectsRef = useRef([]);
  const driverMarkersRef = useRef([]);
  const [status, setStatus] = useState("");
  const [useFallback, setUseFallback] = useState(false);
  const [hiddenRouteIds, setHiddenRouteIds] = useState([]);
  const activeRoutes = useMemo(() => routes2.filter((route57) => route57.orders.length), [routes2]);
  const visibleRouteIds = useMemo(() => new Set(activeRoutes.map((route57) => route57.id).filter((id) => !hiddenRouteIds.includes(id))), [activeRoutes, hiddenRouteIds]);
  const routeColorById = useMemo(() => new Map(routes2.map((route57) => [route57.id, route57.color || "#f97316"])), [routes2]);
  const routePlan = useMemo(() => routes2.filter((route57) => route57.orders.length && visibleRouteIds.has(route57.id)).map((route57) => ({
    id: route57.id,
    code: route57.code,
    truck: route57.truck,
    color: route57.color || "#f97316",
    stops: route57.orders.map((order) => ({
      address: getOrderAddress$4(order),
      customer: order.customer,
      label: getOrderNumber$5(order)
    })).filter((stop) => stop.address)
  })).filter((route57) => route57.stops.length), [routes2, visibleRouteIds]);
  const routePlanKey = useMemo(() => JSON.stringify(routePlan.map((route57) => ({
    id: route57.id,
    color: route57.color,
    stops: route57.stops.map((stop) => stop.address),
    hidden: hiddenRouteIds.includes(route57.id)
  }))), [routePlan, hiddenRouteIds]);
  function toggleRoute(routeId) {
    setHiddenRouteIds((current) => current.includes(routeId) ? current.filter((id) => id !== routeId) : [...current, routeId]);
  }
  useEffect(() => {
    let cancelled = false;
    async function drawMap() {
      if (!mapRef.current) return;
      if (!googleMapsApiKey || !originAddress || !routePlan.length) {
        setUseFallback(true);
        return;
      }
      setStatus("Loading map...");
      setUseFallback(false);
      try {
        await loadClassicGoogleMaps(googleMapsApiKey);
        if (cancelled || !mapRef.current) return;
        const google = window.google;
        mapObjectsRef.current.forEach((object) => {
          var _a2;
          return (_a2 = object.setMap) == null ? void 0 : _a2.call(object, null);
        });
        mapObjectsRef.current = [];
        const map = mapInstanceRef.current || new google.maps.Map(mapRef.current, {
          center: {
            lat: 43.1789,
            lng: -88.1173
          },
          zoom: 9,
          mapTypeControl: true,
          streetViewControl: false,
          fullscreenControl: true
        });
        mapInstanceRef.current = map;
        const bounds = new google.maps.LatLngBounds();
        const directionsService = new google.maps.DirectionsService();
        let yardMarkerAdded = false;
        for (const route57 of routePlan.slice(0, 6)) {
          for (const [stopIndex, stop] of route57.stops.entries()) {
            await new Promise((resolve) => {
              const renderer = new google.maps.DirectionsRenderer({
                map,
                preserveViewport: true,
                suppressMarkers: true,
                polylineOptions: {
                  strokeColor: route57.color,
                  strokeOpacity: 0.78,
                  strokeWeight: 5
                }
              });
              mapObjectsRef.current.push(renderer);
              const renderResult = (result, routeStatus = "OK") => {
                var _a2, _b;
                if (cancelled) {
                  resolve();
                  return;
                }
                if (result && routeStatus === "OK") {
                  renderer.setDirections(result);
                  const legs = ((_b = (_a2 = result.routes) == null ? void 0 : _a2[0]) == null ? void 0 : _b.legs) || [];
                  const outboundLeg = legs[0];
                  const returnLeg = legs[1];
                  if (outboundLeg == null ? void 0 : outboundLeg.start_location) {
                    bounds.extend(outboundLeg.start_location);
                    if (!yardMarkerAdded) {
                      const yardMarker = new google.maps.Marker({
                        map,
                        position: outboundLeg.start_location,
                        label: "Y",
                        title: "Green Hills Supply"
                      });
                      mapObjectsRef.current.push(yardMarker);
                      yardMarkerAdded = true;
                    }
                  }
                  if (outboundLeg == null ? void 0 : outboundLeg.end_location) {
                    bounds.extend(outboundLeg.end_location);
                    const marker = new google.maps.Marker({
                      map,
                      position: outboundLeg.end_location,
                      label: String(stopIndex + 1),
                      title: `${stop.label} · ${stop.customer}`
                    });
                    mapObjectsRef.current.push(marker);
                  }
                  if (returnLeg == null ? void 0 : returnLeg.end_location) bounds.extend(returnLeg.end_location);
                } else {
                  console.warn("[CLASSIC MAP ROUND TRIP ERROR]", route57.code, stop.address, routeStatus);
                }
                resolve();
              };
              const directionsCacheKey = `${originAddress}::${stop.address}`;
              const cachedDirections = classicDirectionsCache.get(directionsCacheKey);
              if (cachedDirections) {
                renderResult(cachedDirections);
                return;
              }
              directionsService.route({
                origin: originAddress,
                destination: originAddress,
                waypoints: [{
                  location: stop.address,
                  stopover: true
                }],
                optimizeWaypoints: false,
                travelMode: google.maps.TravelMode.DRIVING
              }, (result, routeStatus) => {
                if (result && routeStatus === "OK") {
                  classicDirectionsCache.set(directionsCacheKey, result);
                }
                renderResult(result, routeStatus);
              });
            });
          }
        }
        if (!bounds.isEmpty()) map.fitBounds(bounds);
        setStatus("");
      } catch (error) {
        console.warn("[CLASSIC MAP ERROR]", error);
        setStatus("Google map unavailable. Showing route preview.");
        setUseFallback(true);
      }
    }
    const drawTimer = window.setTimeout(() => {
      void drawMap();
    }, 450);
    return () => {
      cancelled = true;
      window.clearTimeout(drawTimer);
      mapObjectsRef.current.forEach((object) => {
        var _a2;
        return (_a2 = object.setMap) == null ? void 0 : _a2.call(object, null);
      });
      mapObjectsRef.current = [];
    };
  }, [googleMapsApiKey, originAddress, routePlanKey]);
  useEffect(() => {
    let cancelled = false;
    async function drawDriverMarkers() {
      if (!googleMapsApiKey || !mapInstanceRef.current) return;
      try {
        await loadClassicGoogleMaps(googleMapsApiKey);
        if (cancelled || !mapInstanceRef.current) return;
        const google = window.google;
        const map = mapInstanceRef.current;
        driverMarkersRef.current.forEach((marker) => {
          var _a2;
          return (_a2 = marker.setMap) == null ? void 0 : _a2.call(marker, null);
        });
        driverMarkersRef.current = [];
        for (const location of driverLocations.filter((item) => item.routeId && visibleRouteIds.has(item.routeId) && Number.isFinite(item.latitude) && Number.isFinite(item.longitude))) {
          const routeColor = routeColorById.get(location.routeId || "") || "#22c55e";
          const marker = new google.maps.Marker({
            map,
            position: {
              lat: location.latitude,
              lng: location.longitude
            },
            title: `${location.truck || "Driver"} · ${location.driverName || "Driver"} · ${new Date(location.capturedAt).toLocaleTimeString([], {
              hour: "numeric",
              minute: "2-digit"
            })}`,
            icon: {
              path: google.maps.SymbolPath.FORWARD_CLOSED_ARROW,
              scale: 6,
              fillColor: routeColor,
              fillOpacity: 1,
              strokeColor: "#111827",
              strokeWeight: 2,
              rotation: Number.isFinite(Number(location.heading)) ? Number(location.heading) : 0
            },
            label: {
              text: location.truck || "DRV",
              color: "#111827",
              fontWeight: "900",
              fontSize: "11px"
            }
          });
          driverMarkersRef.current.push(marker);
        }
      } catch (error) {
        console.warn("[CLASSIC DRIVER MARKER ERROR]", error);
      }
    }
    void drawDriverMarkers();
    return () => {
      cancelled = true;
      driverMarkersRef.current.forEach((marker) => {
        var _a2;
        return (_a2 = marker.setMap) == null ? void 0 : _a2.call(marker, null);
      });
      driverMarkersRef.current = [];
    };
  }, [driverLocations, googleMapsApiKey, routeColorById, visibleRouteIds]);
  if (useFallback) {
    return /* @__PURE__ */ jsxs("div", {
      className: "classic-map-panel",
      style: styles$a.mapCanvas,
      children: [/* @__PURE__ */ jsx(ClassicMapFallback, {
        routes: routes2.filter((route57) => !hiddenRouteIds.includes(route57.id))
      }), status ? /* @__PURE__ */ jsx("div", {
        style: styles$a.classicMapStatus,
        children: status
      }) : null]
    });
  }
  return /* @__PURE__ */ jsxs("div", {
    className: "classic-map-panel",
    style: styles$a.mapCanvas,
    children: [/* @__PURE__ */ jsx("div", {
      ref: mapRef,
      style: styles$a.realMapCanvas
    }), status ? /* @__PURE__ */ jsx("div", {
      style: styles$a.classicMapStatus,
      children: status
    }) : null, /* @__PURE__ */ jsxs("div", {
      style: styles$a.mapLegend,
      children: [activeRoutes.slice(0, 6).map((route57) => /* @__PURE__ */ jsxs("button", {
        type: "button",
        style: hiddenRouteIds.includes(route57.id) ? styles$a.legendItemOff : styles$a.legendItem,
        onClick: () => toggleRoute(route57.id),
        title: `${hiddenRouteIds.includes(route57.id) ? "Show" : "Hide"} ${route57.code}`,
        children: [/* @__PURE__ */ jsx("span", {
          style: {
            ...styles$a.legendDot,
            background: route57.color
          }
        }), route57.code, " ", route57.truck || "Unassigned"]
      }, route57.id)), driverLocations.length ? /* @__PURE__ */ jsxs("span", {
        style: styles$a.driverLegendItem,
        children: [driverLocations.slice(0, 4).map((location) => /* @__PURE__ */ jsx("span", {
          style: {
            ...styles$a.driverLegendDot,
            borderBottomColor: routeColorById.get(location.routeId || "") || "#22c55e"
          }
        }, `${location.id}-${location.routeId || "route"}`)), driverLocations.length, " live driver", driverLocations.length === 1 ? "" : "s"]
      }) : null]
    })]
  });
}
const classic = UNSAFE_withComponentProps(function ClassicDispatchPage() {
  var _a2, _b;
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const location = useLocation();
  useSubmit();
  const mutationFetcher = useFetcher();
  const mutationData = mutationFetcher.data;
  const [query, setQuery] = useState("");
  const deferredQuery = useDeferredValue(query);
  const [selectedRouteId, setSelectedRouteId] = useState(() => {
    if (typeof window === "undefined") return "";
    return window.localStorage.getItem(CLASSIC_SELECTED_ROUTE_KEY) || "";
  });
  const [selectedPlanningDate, setSelectedPlanningDate] = useState(() => {
    if (typeof window === "undefined") return getTodayDateInputValue();
    return window.localStorage.getItem(CLASSIC_PLANNING_DATE_KEY) || getTodayDateInputValue();
  });
  const [draggedOrderId, setDraggedOrderId] = useState("");
  const [routeDrawerOpen, setRouteDrawerOpen] = useState(false);
  const [selectedOrderId, setSelectedOrderId] = useState("");
  const [orderDrawerOpen, setOrderDrawerOpen] = useState(false);
  const [chimeStatus, setChimeStatus] = useState("");
  const [chimeEnabled, setChimeEnabled] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(DISPATCH_CHIME_ENABLED_KEY) === "1";
  });
  const lastChimeKey = useRef("");
  const [navCollapsed, setNavCollapsed] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(DISPATCH_NAV_COLLAPSED_KEY$3) === "1";
  });
  const [manualRoutingMode, setManualRoutingMode] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(CLASSIC_MANUAL_ROUTING_KEY) === "1";
  });
  const [tableSorts, setTableSorts] = useState({
    routes: {
      key: "weight",
      direction: "desc"
    },
    sites: {
      key: "stop",
      direction: "asc"
    },
    orders: {
      key: "date",
      direction: "asc"
    },
    unscheduled: {
      key: "date",
      direction: "asc"
    }
  });
  const [columnWidths, setColumnWidths] = useState(() => {
    if (typeof window === "undefined") return {};
    try {
      const stored = window.localStorage.getItem(CLASSIC_COLUMN_WIDTHS_KEY);
      return stored ? JSON.parse(stored) : {};
    } catch {
      return {};
    }
  });
  const [panelLayout, setPanelLayout] = useState(() => {
    if (typeof window === "undefined") return DEFAULT_CLASSIC_PANEL_LAYOUT;
    try {
      const stored = window.localStorage.getItem(CLASSIC_PANEL_LAYOUT_KEY);
      return normalizePanelLayout(stored ? JSON.parse(stored) : DEFAULT_CLASSIC_PANEL_LAYOUT);
    } catch {
      return DEFAULT_CLASSIC_PANEL_LAYOUT;
    }
  });
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const serverOrders = (actionData == null ? void 0 : actionData.orders) ?? (mutationData == null ? void 0 : mutationData.orders) ?? loaderData.orders ?? [];
  const [localOrders, setLocalOrders] = useState(serverOrders);
  const orders = localOrders;
  const baseRoutes = (actionData == null ? void 0 : actionData.routes) ?? (mutationData == null ? void 0 : mutationData.routes) ?? loaderData.routes ?? [];
  const trucks = (actionData == null ? void 0 : actionData.trucks) ?? loaderData.trucks ?? [];
  const employees = (actionData == null ? void 0 : actionData.employees) ?? loaderData.employees ?? [];
  const initialDriverLocations = (actionData == null ? void 0 : actionData.driverLocations) ?? loaderData.driverLocations ?? [];
  const [driverLocations, setDriverLocations] = useState(initialDriverLocations);
  const materialOptions = (actionData == null ? void 0 : actionData.materialOptions) ?? loaderData.materialOptions ?? [];
  const classicColumnSettings = (actionData == null ? void 0 : actionData.classicColumnSettings) ?? loaderData.classicColumnSettings ?? defaultClassicColumnSettings;
  const productDetailsByMaterial = (actionData == null ? void 0 : actionData.productDetailsByMaterial) ?? loaderData.productDetailsByMaterial ?? {};
  const message = (mutationData == null ? void 0 : mutationData.message) || (actionData == null ? void 0 : actionData.message) || ((_a2 = loaderData == null ? void 0 : loaderData.mailboxStatus) == null ? void 0 : _a2.message) || "";
  const mailboxStatus = (mutationData == null ? void 0 : mutationData.mailboxStatus) || (actionData == null ? void 0 : actionData.mailboxStatus) || (loaderData == null ? void 0 : loaderData.mailboxStatus) || null;
  const shopifyImportStatus = (mutationData == null ? void 0 : mutationData.shopifyImportStatus) || (actionData == null ? void 0 : actionData.shopifyImportStatus) || null;
  const importStatus = shopifyImportStatus || mailboxStatus;
  const googleMapsApiKey = (actionData == null ? void 0 : actionData.googleMapsApiKey) ?? loaderData.googleMapsApiKey ?? "";
  const mapOriginAddress = (actionData == null ? void 0 : actionData.mapOriginAddress) ?? loaderData.mapOriginAddress ?? "";
  const currentUser = (actionData == null ? void 0 : actionData.currentUser) ?? loaderData.currentUser ?? null;
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const classicHref = isEmbeddedRoute ? "/app/classic" : "/classic";
  const monitorHref = isEmbeddedRoute ? "/app/monitor" : "/monitor";
  const calendarHref = isEmbeddedRoute ? "/app/calendar" : "/calendar";
  const allotmentHref = isEmbeddedRoute ? "/app/allotment" : "/allotment";
  const loaderHref = isEmbeddedRoute ? "/app/loader" : "/loader";
  const dispatchHref = isEmbeddedRoute ? "/app/dispatch" : "/dispatch";
  const dailyExportPath = `${dispatchHref}/export-day.xls`;
  const todayDateInputValue = getTodayDateInputValue();
  const quoteHref = isEmbeddedRoute ? "/app/custom-quote" : "/custom-quote";
  const driverHref = isEmbeddedRoute ? "/app/dispatch/driver" : "/dispatch/driver";
  const mobileHref = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  const editorHref = (orderId) => `${dispatchHref}?view=orders&order=${encodeURIComponent(orderId)}&returnTo=${encodeURIComponent(classicHref)}`;
  const dispatchViewHref = (view) => `${dispatchHref}?view=${view}`;
  const canAccess = (permission) => {
    var _a3;
    return !currentUser || ((_a3 = currentUser.permissions) == null ? void 0 : _a3.includes(permission));
  };
  const canManageDispatch = canAccess("manageDispatch");
  const logoutHref = currentUser ? "/login?logout=1" : `${dispatchHref}?logout=1`;
  const loginHref = `/login?next=${encodeURIComponent(location.pathname + location.search)}`;
  const drivers = employees.filter((employee) => employee.role === "driver");
  const helpers = employees.filter((employee) => employee.role === "helper");
  const routeColumnKeys = classicColumnSettings.routes || defaultClassicColumnSettings.routes;
  const savedSiteColumnKeys = classicColumnSettings.sites || defaultClassicColumnSettings.sites;
  const siteColumnKeys = savedSiteColumnKeys.includes("sku") ? savedSiteColumnKeys : [...savedSiteColumnKeys.slice(0, Math.max(2, savedSiteColumnKeys.indexOf("orderNo") + 1)), "sku", ...savedSiteColumnKeys.slice(Math.max(2, savedSiteColumnKeys.indexOf("orderNo") + 1))];
  const orderColumnKeys = classicColumnSettings.orders || defaultClassicColumnSettings.orders;
  const savedUnscheduledColumnKeys = classicColumnSettings.unscheduled || defaultClassicColumnSettings.unscheduled;
  const unscheduledColumnKeys = savedUnscheduledColumnKeys.includes("roundTrip") ? savedUnscheduledColumnKeys : [...savedUnscheduledColumnKeys.slice(0, Math.max(1, savedUnscheduledColumnKeys.indexOf("address") + 1)), "roundTrip", ...savedUnscheduledColumnKeys.slice(Math.max(1, savedUnscheduledColumnKeys.indexOf("address") + 1))];
  const selectedPlanningDateObject = parseDateInputValue(selectedPlanningDate) || parseDateInputValue(getTodayDateInputValue()) || /* @__PURE__ */ new Date();
  const planningWeekStart = startOfWeek$1(selectedPlanningDateObject);
  const planningWeekDays = Array.from({
    length: 7
  }, (_, index) => addDays(planningWeekStart, index));
  const planningKeyByOrderId = useMemo(() => new Map(orders.map((order) => [order.id, getOrderPlanningDateKey(order)])), [orders]);
  const planningOrders = useMemo(() => orders.filter((order) => {
    const planningKey = planningKeyByOrderId.get(order.id);
    return planningKey === selectedPlanningDate || planningKey === "unscheduled";
  }), [orders, planningKeyByOrderId, selectedPlanningDate]);
  const activePlanningCountsByDate = useMemo(() => {
    const counts = /* @__PURE__ */ new Map();
    for (const order of orders) {
      if (!isActiveBoardOrder(order)) continue;
      const planningKey = planningKeyByOrderId.get(order.id) || "unscheduled";
      counts.set(planningKey, (counts.get(planningKey) || 0) + 1);
    }
    return counts;
  }, [orders, planningKeyByOrderId]);
  const undatedPlanningCount = activePlanningCountsByDate.get("unscheduled") || 0;
  function selectPlanningDate(value) {
    setSelectedPlanningDate(value);
    try {
      window.localStorage.setItem(CLASSIC_PLANNING_DATE_KEY, value);
    } catch {
    }
  }
  function movePlanningWeek(days) {
    selectPlanningDate(toDateInputValue(addDays(selectedPlanningDateObject, days)));
  }
  function toggleNavCollapsed() {
    setNavCollapsed((current) => {
      const next = !current;
      try {
        window.localStorage.setItem(DISPATCH_NAV_COLLAPSED_KEY$3, next ? "1" : "0");
      } catch {
      }
      return next;
    });
  }
  function toggleManualRoutingMode() {
    setManualRoutingMode((current) => {
      const next = !current;
      try {
        window.localStorage.setItem(CLASSIC_MANUAL_ROUTING_KEY, next ? "1" : "0");
      } catch {
      }
      return next;
    });
  }
  async function testChime() {
    try {
      await playDispatchChime();
      setChimeEnabled(true);
      window.localStorage.setItem(DISPATCH_CHIME_ENABLED_KEY, "1");
      setChimeStatus("Chime armed");
      window.setTimeout(() => setChimeStatus(""), 2200);
    } catch {
      setChimeStatus("Click again or check browser sound");
      window.setTimeout(() => setChimeStatus(""), 3e3);
    }
  }
  useEffect(() => {
    setLocalOrders(serverOrders);
  }, [serverOrders]);
  useEffect(() => {
    setDriverLocations(initialDriverLocations);
  }, [initialDriverLocations]);
  useEffect(() => {
    const imported = Number((importStatus == null ? void 0 : importStatus.imported) || 0);
    if (imported <= 0) return;
    const chimeKey = [(importStatus == null ? void 0 : importStatus.message) || "", imported, (importStatus == null ? void 0 : importStatus.skipped) || 0].join("|");
    if (!chimeKey || lastChimeKey.current === chimeKey) return;
    lastChimeKey.current = chimeKey;
    if (!chimeEnabled) {
      setChimeStatus("Press Test Chime once to enable alerts");
      window.setTimeout(() => setChimeStatus(""), 4200);
      return;
    }
    void playDispatchChime().catch(() => {
    });
  }, [chimeEnabled, importStatus]);
  useEffect(() => {
    let cancelled = false;
    async function loadDriverLocations() {
      try {
        const response = await fetch("/api/dispatch-driver-location");
        const result = await response.json().catch(() => null);
        if (!cancelled && response.ok && (result == null ? void 0 : result.ok) !== false) {
          setDriverLocations(result.locations || []);
        }
      } catch {
      }
    }
    const timer = window.setInterval(loadDriverLocations, 6e4);
    void loadDriverLocations();
    return () => {
      cancelled = true;
      window.clearInterval(timer);
    };
  }, []);
  const routes2 = useMemo(() => baseRoutes.map((route57) => {
    const routeOrders = planningOrders.filter((order) => order.assignedRouteId === route57.id && isActiveBoardOrder(order)).sort((a, b) => Number(a.stopSequence || 9999) - Number(b.stopSequence || 9999));
    return {
      ...route57,
      orders: routeOrders,
      weight: routeOrders.length,
      totalMinutes: routeOrders.reduce((sum, order) => sum + getTravelMinutes$4(order), 0)
    };
  }), [baseRoutes, planningOrders]);
  const sortedRoutes = useMemo(() => sortItems(routes2, tableSorts.routes, (route57, key) => {
    var _a3, _b2, _c, _d;
    if (key === "code") return route57.code;
    if (key === "driver") return `${route57.truck || ""} ${route57.driver || ""}`;
    if (key === "status") return route57.orders.length ? "Active" : "Open";
    if (key === "weight") return route57.weight;
    if (key === "start") return ((_b2 = (_a3 = route57.shift) == null ? void 0 : _a3.split("-")[0]) == null ? void 0 : _b2.trim()) || "6:00 am";
    if (key === "finish") return ((_d = (_c = route57.shift) == null ? void 0 : _c.split("-")[1]) == null ? void 0 : _d.trim()) || formatTime$1(route57.totalMinutes);
    if (key === "distance") return route57.totalMinutes;
    return "";
  }), [routes2, tableSorts.routes]);
  const selectedRoute = sortedRoutes.find((route57) => route57.id === selectedRouteId) || sortedRoutes[0] || null;
  const selectedOrder = orders.find((order) => order.id === selectedOrderId) || null;
  function getOrderProductDetail(order) {
    var _a3;
    if (!order) return null;
    return productDetailsByMaterial[order.material] || ((_a3 = Object.entries(productDetailsByMaterial).find(([material]) => material.toLowerCase() === String(order.material || "").toLowerCase())) == null ? void 0 : _a3[1]) || null;
  }
  const selectedOrderProductDetail = getOrderProductDetail(selectedOrder);
  useEffect(() => {
    if (!sortedRoutes.length) return;
    if (selectedRouteId && sortedRoutes.some((route57) => route57.id === selectedRouteId)) return;
    setSelectedRouteId(sortedRoutes[0].id);
  }, [selectedRouteId, sortedRoutes]);
  useEffect(() => {
    if (!selectedRouteId) return;
    try {
      window.localStorage.setItem(CLASSIC_SELECTED_ROUTE_KEY, selectedRouteId);
    } catch {
    }
  }, [selectedRouteId]);
  const sortedSiteOrders = useMemo(() => sortItems((selectedRoute == null ? void 0 : selectedRoute.orders) || [], tableSorts.sites, (order, key) => {
    var _a3;
    if (key === "stop") return order.stopSequence || 9999;
    if (key === "orderNo") return getOrderNumber$5(order);
    if (key === "sku") return ((_a3 = getOrderProductDetail(order)) == null ? void 0 : _a3.sku) || "";
    if (key === "address") return getOrderAddress$4(order);
    if (key === "customer") return order.customer;
    if (key === "product") return order.material;
    if (key === "quantity") return order.quantity || "";
    if (key === "unit") return order.unit || "";
    if (key === "requested") return order.requestedWindow || "";
    if (key === "timePreference") return order.timePreference || "";
    if (key === "arrived") return order.arrivedAt || "";
    if (key === "departed") return order.departedAt || "";
    if (key === "eta") return order.eta || order.requestedWindow || "";
    if (key === "miles") return order.travelMiles || "";
    return "";
  }), [selectedRoute, tableSorts.sites, productDetailsByMaterial]);
  const search = deferredQuery.trim().toLowerCase();
  const searchTextByOrderId = useMemo(() => new Map(planningOrders.map((order) => [order.id, buildSearchText$2(order)])), [planningOrders]);
  const visibleOrders = useMemo(() => search ? planningOrders.filter((order) => {
    var _a3;
    return (_a3 = searchTextByOrderId.get(order.id)) == null ? void 0 : _a3.includes(search);
  }) : planningOrders, [planningOrders, search, searchTextByOrderId]);
  const unscheduledOrders = useMemo(() => visibleOrders.filter((order) => !order.assignedRouteId && isActiveBoardOrder(order) && order.status !== "scheduled"), [visibleOrders]);
  useMemo(() => visibleOrders.filter((order) => order.assignedRouteId && isActiveBoardOrder(order)), [visibleOrders]);
  const deliveredCount = useMemo(() => orders.filter((order) => order.status === "delivered" || order.deliveryStatus === "delivered").length, [orders]);
  const sortedVisibleOrders = useMemo(() => sortItems(visibleOrders, tableSorts.orders, (order, key) => {
    if (key === "orderNo") return getOrderNumber$5(order);
    if (key === "type") return order.source === "email" ? "D" : "M";
    if (key === "date") return order.requestedWindow || "";
    if (key === "client") return order.customer;
    if (key === "address") return getOrderAddress$4(order);
    if (key === "weight") return order.quantity || "";
    if (key === "volume") return order.unit;
    if (key === "status") return statusLabel(order);
    if (key === "material") return order.material;
    if (key === "timePreference") return order.timePreference || "";
    if (key === "route") return order.assignedRouteId || "";
    return "";
  }), [visibleOrders, tableSorts.orders]);
  const sortedUnscheduledOrders = useMemo(() => sortItems(unscheduledOrders, tableSorts.unscheduled, (order, key) => {
    if (key === "orderNo") return getOrderNumber$5(order);
    if (key === "date") return order.requestedWindow || "";
    if (key === "client") return order.customer;
    if (key === "address") return getOrderAddress$4(order);
    if (key === "product") return order.material;
    if (key === "roundTrip") return getTravelMinutes$4(order);
    if (key === "weight") return order.quantity || "";
    if (key === "volume") return order.unit;
    if (key === "timePreference") return order.timePreference || "";
    if (key === "notes") return order.notes || "";
    if (key === "route") return order.assignedRouteId || "";
    return "";
  }), [unscheduledOrders, tableSorts.unscheduled]);
  function updateTableSort(table, key) {
    setTableSorts((current) => {
      const currentSort = current[table];
      return {
        ...current,
        [table]: {
          key,
          direction: currentSort.key === key && currentSort.direction === "asc" ? "desc" : "asc"
        }
      };
    });
  }
  function getColumnWidth(table, key) {
    var _a3, _b2;
    return ((_a3 = columnWidths[table]) == null ? void 0 : _a3[key]) || ((_b2 = defaultColumnWidths[table]) == null ? void 0 : _b2[key]) || 120;
  }
  function getTableMinWidth(table, keys, fixedWidth = 0) {
    return keys.reduce((sum, key) => sum + getColumnWidth(table, key), fixedWidth);
  }
  function persistColumnWidths(nextWidths) {
    try {
      window.localStorage.setItem(CLASSIC_COLUMN_WIDTHS_KEY, JSON.stringify(nextWidths));
    } catch {
    }
  }
  function resizeColumn(table, key, nextWidth) {
    setColumnWidths((current) => {
      const next = {
        ...current,
        [table]: {
          ...current[table] || {},
          [key]: Math.max(MIN_CLASSIC_COLUMN_WIDTH, Math.round(nextWidth))
        }
      };
      persistColumnWidths(next);
      return next;
    });
  }
  function savePanelLayout(nextLayout) {
    const normalized = normalizePanelLayout(nextLayout);
    setPanelLayout(normalized);
    try {
      window.localStorage.setItem(CLASSIC_PANEL_LAYOUT_KEY, JSON.stringify(normalized));
    } catch {
    }
  }
  function beginMainPanelResize(event) {
    event.preventDefault();
    const startX = event.clientX;
    const startLeft = panelLayout.mainLeft;
    const container = event.currentTarget.parentElement;
    const containerWidth = (container == null ? void 0 : container.getBoundingClientRect().width) || 1;
    function handleMouseMove(moveEvent) {
      const deltaPercent = (moveEvent.clientX - startX) / containerWidth * 100;
      savePanelLayout({
        ...panelLayout,
        mainLeft: startLeft + deltaPercent
      });
    }
    function handleMouseUp() {
      window.removeEventListener("mousemove", handleMouseMove);
      window.removeEventListener("mouseup", handleMouseUp);
      document.body.style.cursor = "";
      document.body.style.userSelect = "";
    }
    document.body.style.cursor = "col-resize";
    document.body.style.userSelect = "none";
    window.addEventListener("mousemove", handleMouseMove);
    window.addEventListener("mouseup", handleMouseUp);
  }
  function beginRowResize(stack, dividerIndex, event) {
    event.preventDefault();
    const startY = event.clientY;
    const startRows = [...panelLayout[stack]];
    const container = event.currentTarget.parentElement;
    const containerHeight = (container == null ? void 0 : container.getBoundingClientRect().height) || 1;
    function handleMouseMove(moveEvent) {
      const deltaPercent = (moveEvent.clientY - startY) / containerHeight * 100;
      const nextRows = [...startRows];
      nextRows[dividerIndex] = startRows[dividerIndex] + deltaPercent;
      nextRows[dividerIndex + 1] = startRows[dividerIndex + 1] - deltaPercent;
      savePanelLayout({
        ...panelLayout,
        [stack]: normalizeRows(nextRows, DEFAULT_CLASSIC_PANEL_LAYOUT[stack])
      });
    }
    function handleMouseUp() {
      window.removeEventListener("mousemove", handleMouseMove);
      window.removeEventListener("mouseup", handleMouseUp);
      document.body.style.cursor = "";
      document.body.style.userSelect = "";
    }
    document.body.style.cursor = "row-resize";
    document.body.style.userSelect = "none";
    window.addEventListener("mousemove", handleMouseMove);
    window.addEventListener("mouseup", handleMouseUp);
  }
  function beginColumnResize(table, key, event) {
    event.preventDefault();
    event.stopPropagation();
    const startX = event.clientX;
    const startWidth = getColumnWidth(table, key);
    function handleMouseMove(moveEvent) {
      resizeColumn(table, key, startWidth + moveEvent.clientX - startX);
    }
    function handleMouseUp() {
      window.removeEventListener("mousemove", handleMouseMove);
      window.removeEventListener("mouseup", handleMouseUp);
      document.body.style.cursor = "";
      document.body.style.userSelect = "";
    }
    document.body.style.cursor = "col-resize";
    document.body.style.userSelect = "none";
    window.addEventListener("mousemove", handleMouseMove);
    window.addEventListener("mouseup", handleMouseUp);
  }
  function columnGroup(table, keys, leadingFixedColumns = [], trailingFixedColumns = []) {
    return /* @__PURE__ */ jsxs("colgroup", {
      children: [leadingFixedColumns.map((width, index) => /* @__PURE__ */ jsx("col", {
        style: {
          width
        }
      }, `leading-${index}`)), keys.map((key) => /* @__PURE__ */ jsx("col", {
        style: {
          width: getColumnWidth(table, key)
        }
      }, key)), trailingFixedColumns.map((width, index) => /* @__PURE__ */ jsx("col", {
        style: {
          width
        }
      }, `trailing-${index}`))]
    });
  }
  function sortHeader(table, key, label) {
    const active = tableSorts[table].key === key;
    const direction = tableSorts[table].direction === "asc" ? "↑" : "↓";
    return /* @__PURE__ */ jsxs("div", {
      style: styles$a.resizableHeader,
      children: [/* @__PURE__ */ jsxs("button", {
        type: "button",
        onClick: () => updateTableSort(table, key),
        style: active ? styles$a.sortHeaderActive : styles$a.sortHeader,
        title: `Sort ${label}`,
        children: [label, " ", active ? direction : ""]
      }), /* @__PURE__ */ jsx("span", {
        role: "separator",
        "aria-orientation": "vertical",
        title: "Drag to resize column",
        style: styles$a.columnResizeHandle,
        onMouseDown: (event) => beginColumnResize(table, key, event)
      })]
    });
  }
  function columnLabel(table, key) {
    var _a3;
    return ((_a3 = classicColumnOptions[table].find((option) => option.key === key)) == null ? void 0 : _a3.label) || key;
  }
  function routeColumnValue(route57, key) {
    var _a3, _b2, _c, _d;
    if (key === "code") {
      return /* @__PURE__ */ jsx("button", {
        type: "button",
        style: styles$a.rowRouteButton((selectedRoute == null ? void 0 : selectedRoute.id) === route57.id),
        onClick: () => {
          setSelectedRouteId(route57.id);
          setRouteDrawerOpen(false);
          setOrderDrawerOpen(false);
        },
        children: route57.code
      });
    }
    if (key === "driver") return `${route57.truck || "No truck"} (${route57.driver || "No driver"})`;
    if (key === "status") return route57.orders.length ? "Active" : "Open";
    if (key === "weight") return route57.orders.length || "-";
    if (key === "start") return ((_b2 = (_a3 = route57.shift) == null ? void 0 : _a3.split("-")[0]) == null ? void 0 : _b2.trim()) || "6:00 am";
    if (key === "finish") return ((_d = (_c = route57.shift) == null ? void 0 : _c.split("-")[1]) == null ? void 0 : _d.trim()) || formatTime$1(route57.totalMinutes);
    if (key === "distance") return formatTime$1(route57.totalMinutes);
    return "-";
  }
  function orderColumnValue(order, key, index = 0) {
    if (key === "type") return order.source === "email" ? "D" : "M";
    if (key === "stop") return order.stopSequence || index + 1;
    if (key === "orderNo") {
      return /* @__PURE__ */ jsx("button", {
        type: "button",
        style: styles$a.rowOrderButton,
        onClick: () => {
          setSelectedOrderId(order.id);
          setOrderDrawerOpen(true);
          setRouteDrawerOpen(false);
        },
        children: getOrderNumber$5(order)
      });
    }
    if (key === "sku") {
      const detail = getOrderProductDetail(order);
      const label = (detail == null ? void 0 : detail.sku) || "No SKU";
      return /* @__PURE__ */ jsx("button", {
        type: "button",
        style: (detail == null ? void 0 : detail.sku) ? styles$a.rowSkuButton : styles$a.rowSkuButtonMuted,
        onClick: () => {
          setSelectedOrderId(order.id);
          setOrderDrawerOpen(true);
          setRouteDrawerOpen(false);
        },
        title: (detail == null ? void 0 : detail.title) || order.material || "Product details",
        children: label
      });
    }
    if (key === "date" || key === "requested") return order.requestedWindow || "-";
    if (key === "client" || key === "customer") return order.customer || "-";
    if (key === "address") return getOrderAddress$4(order) || "-";
    if (key === "product" || key === "material") return order.material || "-";
    if (key === "roundTrip") return order.travelSummary || formatTime$1(getTravelMinutes$4(order));
    if (key === "quantity" || key === "weight") return order.quantity || "-";
    if (key === "unit" || key === "volume") return order.unit || "-";
    if (key === "timePreference") return order.timePreference || "-";
    if (key === "arrived") {
      return order.arrivedAt ? new Date(order.arrivedAt).toLocaleTimeString([], {
        hour: "numeric",
        minute: "2-digit"
      }) : "-";
    }
    if (key === "departed") {
      return order.departedAt ? new Date(order.departedAt).toLocaleTimeString([], {
        hour: "numeric",
        minute: "2-digit"
      }) : "-";
    }
    if (key === "eta") return order.eta || order.requestedWindow || "-";
    if (key === "miles") return order.travelMiles || "-";
    if (key === "status") {
      return /* @__PURE__ */ jsx("span", {
        style: styles$a.statusPill(getStatusTone(order)),
        children: statusLabel(order)
      });
    }
    if (key === "notes") return order.notes || "-";
    if (key === "route") return order.assignedRouteId || "Unassigned";
    return "-";
  }
  function beginOrderDrag(orderId) {
    setDraggedOrderId(orderId);
  }
  function getOrderedRouteOrders(routeId, sourceOrders = orders) {
    return sourceOrders.filter((order) => order.assignedRouteId === routeId && isActiveBoardOrder(order)).sort((a, b) => Number(a.stopSequence || 9999) - Number(b.stopSequence || 9999) || String(a.created_at || "").localeCompare(String(b.created_at || "")));
  }
  function submitClassicMutation(payload) {
    mutationFetcher.submit(payload, {
      method: "post"
    });
  }
  function optimisticallyAssignOrder(orderId, routeId) {
    setLocalOrders((currentOrders) => {
      const nextSequence = Math.max(0, ...getOrderedRouteOrders(routeId, currentOrders).map((order) => Number(order.stopSequence || 0))) + 1;
      return currentOrders.map((order) => order.id === orderId ? {
        ...order,
        assignedRouteId: routeId,
        status: "scheduled",
        deliveryStatus: order.deliveryStatus === "delivered" ? order.deliveryStatus : "not_started",
        stopSequence: nextSequence
      } : order);
    });
  }
  function optimisticallyUnassignOrder(orderId) {
    setLocalOrders((currentOrders) => currentOrders.map((order) => order.id === orderId ? {
      ...order,
      assignedRouteId: null,
      status: "new",
      deliveryStatus: "not_started",
      stopSequence: null,
      eta: null
    } : order));
  }
  function optimisticallyMoveRouteStop(orderId, routeId, direction) {
    setLocalOrders((currentOrders) => {
      const routeOrders = getOrderedRouteOrders(routeId, currentOrders);
      const currentIndex = routeOrders.findIndex((order) => order.id === orderId);
      const nextIndex = direction === "up" ? currentIndex - 1 : currentIndex + 1;
      if (currentIndex < 0 || nextIndex < 0 || nextIndex >= routeOrders.length) {
        return currentOrders;
      }
      const reordered = [...routeOrders];
      const [movedOrder] = reordered.splice(currentIndex, 1);
      reordered.splice(nextIndex, 0, movedOrder);
      const sequenceById = new Map(reordered.map((order, index) => [order.id, index + 1]));
      return currentOrders.map((order) => sequenceById.has(order.id) ? {
        ...order,
        stopSequence: sequenceById.get(order.id) || order.stopSequence
      } : order);
    });
  }
  function allowRouteDrop(event) {
    if (!selectedRoute) return;
    event.preventDefault();
  }
  function dropOrderOnCurrentRoute(event) {
    event.preventDefault();
    if (!selectedRoute || !draggedOrderId) return;
    const order = orders.find((entry2) => entry2.id === draggedOrderId);
    const splitCount = getAssignmentSplitCount(order, selectedRoute);
    if (splitCount === null) {
      setDraggedOrderId("");
      return;
    }
    optimisticallyAssignOrder(draggedOrderId, selectedRoute.id);
    submitClassicMutation({
      intent: "assign-order",
      orderId: draggedOrderId,
      routeId: selectedRoute.id,
      splitCount
    });
    setDraggedOrderId("");
  }
  function getRouteTruck(route57) {
    if (!route57) return null;
    return trucks.find((truck) => truck.id === route57.truckId) || trucks.find((truck) => truck.label === route57.truck) || null;
  }
  function getAssignmentSplitCount(order, route57) {
    if (!order || !route57 || !/yards?/i.test(order.unit)) return "";
    const truck = getRouteTruck(route57);
    const capacity = Number((truck == null ? void 0 : truck.yards) || 30);
    const quantity = Number(order.quantity || 0);
    if (!quantity || !capacity || quantity <= capacity) return "";
    const minimumSplits = Math.ceil(quantity / capacity);
    const routeLabel = `${route57.code}${(truck == null ? void 0 : truck.label) ? ` / ${truck.label}` : ""}`;
    const answer = window.prompt(`${getOrderNumber$5(order)} is ${quantity} yards, which is over the ${capacity} yard truck limit for ${routeLabel}.

How many tickets should I split it into?`, String(minimumSplits));
    if (answer === null) return null;
    const splitCount = Math.floor(Number(answer));
    if (!Number.isFinite(splitCount) || splitCount < minimumSplits) {
      window.alert(`Use at least ${minimumSplits} split tickets so each load fits the truck.`);
      return null;
    }
    return String(splitCount);
  }
  function prepareAssignmentSubmit(order, routeId, form, event) {
    const route57 = routes2.find((entry2) => entry2.id === routeId);
    const splitCount = getAssignmentSplitCount(order, route57);
    if (splitCount === null) {
      event.preventDefault();
      return;
    }
    let input = form.querySelector('input[name="splitCount"]');
    if (!input) {
      input = document.createElement("input");
      input.type = "hidden";
      input.name = "splitCount";
      form.appendChild(input);
    }
    input.value = splitCount;
  }
  if (!allowed) {
    return /* @__PURE__ */ jsx("main", {
      style: styles$a.loginPage,
      children: /* @__PURE__ */ jsxs("section", {
        style: styles$a.loginBox,
        children: [/* @__PURE__ */ jsx("h1", {
          style: styles$a.loginTitle,
          children: "Classic Dispatch"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$a.muted,
          children: "Sign in with your contractor user account to open the plan-and-track board."
        }), /* @__PURE__ */ jsx("a", {
          href: loginHref,
          style: {
            ...styles$a.orangeButton,
            textDecoration: "none"
          },
          children: "Sign In"
        })]
      })
    });
  }
  return /* @__PURE__ */ jsxs("main", {
    className: "classic-shell",
    style: {
      ...styles$a.page,
      gridTemplateColumns: navCollapsed ? "56px minmax(1420px, 1fr)" : "230px minmax(1420px, 1fr)"
    },
    children: [/* @__PURE__ */ jsx("style", {
      children: `
          .classic-shell {
            color-scheme: dark;
            --classic-bg: #0f172a;
            --classic-text: #e5e7eb;
            --classic-rail-bg: #020617;
            --classic-rail-border: #1e293b;
            --classic-rail-link: #cbd5e1;
            --classic-rail-active-bg: #1e293b;
            --classic-panel-bg: #111827;
            --classic-panel-header-bg: #0b1220;
            --classic-border: #334155;
            --classic-soft-border: #263449;
            --classic-input-bg: #020617;
            --classic-muted: #94a3b8;
            --classic-table-head-bg: #0b1220;
            --classic-table-head-text: #cbd5e1;
            --classic-table-hover: #172033;
            --classic-drawer-bg: #0f172a;
            --classic-map-bg: #101827;
          }

          .classic-table th,
          .classic-table td {
            height: 28px;
            padding: 3px 8px;
            border-bottom: 1px solid var(--classic-soft-border);
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            text-align: left;
          }

          .classic-table th {
            background: var(--classic-table-head-bg);
            color: var(--classic-table-head-text);
            font-size: 12px;
            font-weight: 700;
          }

          .classic-table tr:hover td {
            background: var(--classic-table-hover);
          }

          @media (max-width: 900px) {
            .classic-shell {
              display: block !important;
              overflow-x: hidden !important;
            }

            .classic-side-rail {
              position: sticky !important;
              top: 0 !important;
              z-index: 20 !important;
              min-height: auto !important;
              height: auto !important;
              width: auto !important;
              border-right: 0 !important;
              border-bottom: 1px solid var(--classic-border) !important;
            }

            .classic-workspace {
              min-width: 0 !important;
              width: 100% !important;
              overflow: visible !important;
            }

            .classic-topbar {
              min-height: auto !important;
              flex-wrap: wrap !important;
              align-items: stretch !important;
              padding: 10px !important;
              gap: 8px !important;
            }

            .classic-topbar input {
              order: 10 !important;
              width: 100% !important;
              max-width: none !important;
            }

            .classic-topbar form,
            .classic-topbar a,
            .classic-topbar button {
              flex: 1 1 128px !important;
            }

            .classic-topbar .classic-company {
              flex: 1 1 100% !important;
              margin-left: 0 !important;
            }

            .classic-main-grid {
              display: block !important;
              height: auto !important;
              overflow: visible !important;
            }

            .classic-left-stack,
            .classic-right-stack {
              display: block !important;
              overflow: visible !important;
            }

            .classic-panel,
            .classic-map-panel {
              min-height: 260px !important;
              max-height: 72vh !important;
              overflow: auto !important;
              border-bottom: 1px solid var(--classic-border) !important;
            }

            .classic-map-panel {
              height: 52vh !important;
            }

            .classic-resizer {
              display: none !important;
            }

            .classic-table {
              width: max-content !important;
            }
          }
        `
    }), /* @__PURE__ */ jsxs("aside", {
      className: "classic-side-rail",
      style: {
        ...styles$a.sideRail,
        padding: navCollapsed ? "12px 8px" : "16px 14px"
      },
      children: [/* @__PURE__ */ jsx("button", {
        type: "button",
        onClick: toggleNavCollapsed,
        style: styles$a.navToggle,
        title: navCollapsed ? "Open navigation" : "Close navigation",
        children: navCollapsed ? ">" : "<"
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$a.classicBrand,
        children: [/* @__PURE__ */ jsx("div", {
          style: styles$a.railLogo,
          children: "GH"
        }), /* @__PURE__ */ jsxs("div", {
          style: navCollapsed ? styles$a.collapsedOnlyHidden : void 0,
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$a.classicBrandTitle,
            children: "Contractor"
          }), /* @__PURE__ */ jsx("div", {
            style: styles$a.classicBrandSub,
            children: "Classic"
          })]
        })]
      }), /* @__PURE__ */ jsxs("nav", {
        style: navCollapsed ? styles$a.collapsedOnlyHidden : styles$a.classicNav,
        children: [/* @__PURE__ */ jsx(Link, {
          to: classicHref,
          style: styles$a.classicNavLinkActive,
          children: "Classic"
        }), /* @__PURE__ */ jsx(Link, {
          to: monitorHref,
          style: styles$a.classicNavLink,
          children: "Monitor"
        }), /* @__PURE__ */ jsx(Link, {
          to: calendarHref,
          style: styles$a.classicNavLink,
          children: "Calendar"
        }), /* @__PURE__ */ jsx(Link, {
          to: allotmentHref,
          style: styles$a.classicNavLink,
          children: "Allotment"
        }), canAccess("loader") ? /* @__PURE__ */ jsx(Link, {
          to: loaderHref,
          style: styles$a.classicNavLink,
          children: "Loader"
        }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("orders"),
          style: styles$a.classicNavLink,
          children: "Orders"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("scheduled"),
          style: styles$a.classicNavLink,
          children: "Scheduled"
        }), canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("routes"),
          style: styles$a.classicNavLink,
          children: "Routes"
        }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("trucks"),
          style: styles$a.classicNavLink,
          children: "Trucks"
        }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("employees"),
          style: styles$a.classicNavLink,
          children: "Employees"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("delivered"),
          style: styles$a.classicNavLink,
          children: "Delivered"
        }), /* @__PURE__ */ jsx("a", {
          href: "https://www.ghstickets.info",
          style: styles$a.classicNavLink,
          children: "Ticket Creator"
        })]
      }), /* @__PURE__ */ jsx("div", {
        style: {
          flex: 1
        }
      }), /* @__PURE__ */ jsxs("div", {
        style: navCollapsed ? styles$a.collapsedOnlyHidden : styles$a.classicFooterNav,
        children: [canAccess("driver") ? /* @__PURE__ */ jsx(Link, {
          to: driverHref,
          style: styles$a.classicUtility,
          children: "Driver Route"
        }) : null, canAccess("loader") ? /* @__PURE__ */ jsx(Link, {
          to: loaderHref,
          style: styles$a.classicUtility,
          children: "Loader View"
        }) : null, canAccess("quoteTool") ? /* @__PURE__ */ jsx(Link, {
          to: quoteHref,
          style: styles$a.classicUtility,
          children: "Quote Tool"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: mobileHref,
          style: styles$a.classicUtility,
          children: "Mobile"
        }), canAccess("manageUsers") ? /* @__PURE__ */ jsx(Link, {
          to: "/settings",
          style: styles$a.classicUtility,
          children: "Settings"
        }) : null, currentUser ? /* @__PURE__ */ jsx(Link, {
          to: "/change-password",
          style: styles$a.classicUtility,
          children: "Change Password"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: logoutHref,
          style: styles$a.classicUtility,
          children: "Log Out"
        })]
      })]
    }), /* @__PURE__ */ jsxs("section", {
      className: "classic-workspace",
      style: styles$a.workspace,
      children: [/* @__PURE__ */ jsxs("header", {
        className: "classic-topbar",
        style: styles$a.topBar,
        children: [/* @__PURE__ */ jsx("div", {
          style: styles$a.brandMark,
          children: "✓"
        }), /* @__PURE__ */ jsx("strong", {
          style: styles$a.brandText,
          children: "Plan & Track"
        }), /* @__PURE__ */ jsx("input", {
          value: query,
          onChange: (event) => setQuery(event.target.value),
          placeholder: "Search by Order Number",
          style: styles$a.search
        }), /* @__PURE__ */ jsxs(Form, {
          method: "post",
          style: styles$a.topForm,
          children: [/* @__PURE__ */ jsx("input", {
            type: "hidden",
            name: "intent",
            value: "poll-shopify-orders"
          }), /* @__PURE__ */ jsx("button", {
            type: "submit",
            style: styles$a.outlineButton,
            children: "Import Shopify"
          })]
        }), /* @__PURE__ */ jsxs(Form, {
          method: "post",
          style: styles$a.compactTopForm,
          children: [/* @__PURE__ */ jsx("input", {
            type: "hidden",
            name: "intent",
            value: "poll-mailbox"
          }), /* @__PURE__ */ jsx("button", {
            type: "submit",
            style: styles$a.outlineButton,
            children: "Import Mail"
          })]
        }), /* @__PURE__ */ jsxs("form", {
          method: "get",
          action: dailyExportPath,
          style: styles$a.exportForm,
          children: [/* @__PURE__ */ jsx("input", {
            type: "date",
            name: "date",
            defaultValue: todayDateInputValue,
            style: styles$a.exportDateInput,
            "aria-label": "Export delivery date"
          }), /* @__PURE__ */ jsx("button", {
            type: "submit",
            style: styles$a.outlineButton,
            children: "Export Shopify"
          })]
        }), canManageDispatch ? /* @__PURE__ */ jsxs(Fragment, {
          children: [/* @__PURE__ */ jsxs("label", {
            style: styles$a.manualRouteToggle,
            children: [/* @__PURE__ */ jsx("input", {
              type: "checkbox",
              checked: manualRoutingMode,
              onChange: toggleManualRoutingMode
            }), "Manual"]
          }), /* @__PURE__ */ jsxs(Form, {
            method: "post",
            style: styles$a.compactTopForm,
            onSubmit: (event) => {
              if (manualRoutingMode) {
                event.preventDefault();
                return;
              }
              if (!window.confirm("Auto Route will assign unscheduled orders by date, time preference, truck capacity, and similar materials. Existing route stops stay in place. Continue?")) {
                event.preventDefault();
              }
            },
            children: [/* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "intent",
              value: "auto-route-orders"
            }), manualRoutingMode ? /* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "manualRoutingMode",
              value: "on"
            }) : null, /* @__PURE__ */ jsx("button", {
              type: "submit",
              disabled: manualRoutingMode,
              style: manualRoutingMode ? {
                ...styles$a.outlineButton,
                ...styles$a.disabledOutlineButton
              } : styles$a.outlineButton,
              children: "Auto Route"
            })]
          })]
        }) : null, /* @__PURE__ */ jsx("button", {
          type: "button",
          style: styles$a.outlineButton,
          onClick: testChime,
          children: "Test Chime"
        }), chimeStatus ? /* @__PURE__ */ jsx("span", {
          style: styles$a.chimeStatus,
          children: chimeStatus
        }) : null, /* @__PURE__ */ jsx("a", {
          href: "#add-route",
          style: styles$a.outlineButton,
          children: "Add Route"
        }), /* @__PURE__ */ jsx("a", {
          href: "#add-order",
          style: styles$a.orangeButton,
          children: "Add Order"
        }), /* @__PURE__ */ jsx("div", {
          className: "classic-company",
          style: styles$a.company,
          children: "Green Hills Dispatch"
        })]
      }), /* @__PURE__ */ jsxs("div", {
        className: "classic-planner-bar",
        style: styles$a.plannerBar,
        children: [/* @__PURE__ */ jsx("button", {
          type: "button",
          style: styles$a.plannerNavButton,
          onClick: () => movePlanningWeek(-7),
          children: "Previous Week"
        }), /* @__PURE__ */ jsx("input", {
          type: "date",
          value: selectedPlanningDate,
          onChange: (event) => selectPlanningDate(event.target.value || getTodayDateInputValue()),
          style: styles$a.plannerDateInput,
          "aria-label": "Planning delivery date"
        }), /* @__PURE__ */ jsx("button", {
          type: "button",
          style: styles$a.plannerNavButton,
          onClick: () => selectPlanningDate(getTodayDateInputValue()),
          children: "Today"
        }), /* @__PURE__ */ jsx("div", {
          style: styles$a.plannerDayTabs,
          children: planningWeekDays.map((day) => {
            const value = toDateInputValue(day);
            const active = value === selectedPlanningDate;
            const dayOrdersCount = activePlanningCountsByDate.get(value) || 0;
            return /* @__PURE__ */ jsxs("button", {
              type: "button",
              onClick: () => selectPlanningDate(value),
              style: styles$a.plannerDayButton(active),
              children: [/* @__PURE__ */ jsx("span", {
                children: formatPlannerDate(day)
              }), /* @__PURE__ */ jsxs("small", {
                children: [dayOrdersCount, " dated"]
              })]
            }, value);
          })
        }), /* @__PURE__ */ jsx("button", {
          type: "button",
          style: styles$a.plannerNavButton,
          onClick: () => movePlanningWeek(7),
          children: "Next Week"
        }), /* @__PURE__ */ jsxs("span", {
          style: styles$a.plannerHint,
          children: ["Showing ", formatPlannerDate(selectedPlanningDateObject), " plus ", undatedPlanningCount, " undated"]
        })]
      }), message ? /* @__PURE__ */ jsx("div", {
        style: ((mutationData == null ? void 0 : mutationData.ok) ?? (actionData == null ? void 0 : actionData.ok)) === false ? styles$a.errorBanner : styles$a.messageBanner,
        children: message
      }) : null, /* @__PURE__ */ jsxs("div", {
        className: "classic-main-grid",
        style: {
          ...styles$a.mainGrid,
          gridTemplateColumns: `${panelLayout.mainLeft}fr 8px ${100 - panelLayout.mainLeft}fr`
        },
        children: [/* @__PURE__ */ jsxs("section", {
          className: "classic-left-stack",
          style: {
            ...styles$a.leftStack,
            gridTemplateRows: `${panelLayout.leftRows[0]}fr 8px ${panelLayout.leftRows[1]}fr 8px ${panelLayout.leftRows[2]}fr`
          },
          children: [/* @__PURE__ */ jsxs("div", {
            className: "classic-panel",
            style: styles$a.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$a.panelHeader,
              children: [/* @__PURE__ */ jsxs("strong", {
                children: ["Routes ", routes2.length]
              }), /* @__PURE__ */ jsx("span", {
                children: formatPlannerDate(selectedPlanningDateObject)
              })]
            }), /* @__PURE__ */ jsxs("table", {
              className: "classic-table",
              style: {
                ...styles$a.table,
                minWidth: getTableMinWidth("routes", routeColumnKeys, 104)
              },
              children: [columnGroup("routes", routeColumnKeys, [24], [80]), /* @__PURE__ */ jsx("thead", {
                children: /* @__PURE__ */ jsxs("tr", {
                  children: [/* @__PURE__ */ jsx("th", {}), routeColumnKeys.map((key) => /* @__PURE__ */ jsx("th", {
                    children: sortHeader("routes", key, columnLabel("routes", key))
                  }, key)), /* @__PURE__ */ jsx("th", {})]
                })
              }), /* @__PURE__ */ jsxs("tbody", {
                children: [sortedRoutes.map((route57) => /* @__PURE__ */ jsxs("tr", {
                  children: [/* @__PURE__ */ jsx("td", {
                    children: /* @__PURE__ */ jsx("span", {
                      style: {
                        ...styles$a.colorBar,
                        background: route57.color || "#f97316"
                      }
                    })
                  }), routeColumnKeys.map((key) => /* @__PURE__ */ jsx("td", {
                    children: routeColumnValue(route57, key)
                  }, key)), /* @__PURE__ */ jsx("td", {
                    children: /* @__PURE__ */ jsxs(Form, {
                      method: "post",
                      style: styles$a.inlineActions,
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "routeId",
                        value: route57.id
                      }), /* @__PURE__ */ jsx("button", {
                        name: "intent",
                        value: "delete-route",
                        style: styles$a.iconButton,
                        title: "Delete route",
                        onClick: (event) => {
                          if (!window.confirm(`Delete route ${route57.code}? Active orders must be moved first.`)) {
                            event.preventDefault();
                          }
                        },
                        children: "×"
                      })]
                    })
                  })]
                }, route57.id)), !routes2.length ? /* @__PURE__ */ jsx("tr", {
                  children: /* @__PURE__ */ jsx("td", {
                    colSpan: routeColumnKeys.length + 2,
                    style: styles$a.emptyCell,
                    children: "No routes have been set up yet."
                  })
                }) : null]
              })]
            })]
          }), /* @__PURE__ */ jsx("div", {
            className: "classic-resizer",
            style: styles$a.rowResizeHandle,
            onMouseDown: (event) => beginRowResize("leftRows", 0, event),
            title: "Drag to resize Routes and Sites"
          }), /* @__PURE__ */ jsxs("div", {
            className: "classic-panel",
            style: styles$a.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$a.panelHeader,
              children: [/* @__PURE__ */ jsxs("strong", {
                children: ["Sites ", (selectedRoute == null ? void 0 : selectedRoute.orders.length) || 0]
              }), /* @__PURE__ */ jsx("span", {
                children: selectedRoute ? `${selectedRoute.code} · ${selectedRoute.truck || "No truck"}` : "Select a route"
              })]
            }), /* @__PURE__ */ jsx("div", {
              style: draggedOrderId ? styles$a.dropZoneActive : styles$a.dropZone,
              onDragOver: allowRouteDrop,
              onDrop: dropOrderOnCurrentRoute,
              children: "Drag and Drop to the Current Route"
            }), /* @__PURE__ */ jsxs("table", {
              className: "classic-table",
              style: {
                ...styles$a.table,
                minWidth: getTableMinWidth("sites", siteColumnKeys, 190)
              },
              children: [columnGroup("sites", siteColumnKeys, [], [190]), /* @__PURE__ */ jsx("thead", {
                children: /* @__PURE__ */ jsxs("tr", {
                  children: [siteColumnKeys.map((key) => /* @__PURE__ */ jsx("th", {
                    children: sortHeader("sites", key, columnLabel("sites", key))
                  }, key)), /* @__PURE__ */ jsx("th", {})]
                })
              }), /* @__PURE__ */ jsxs("tbody", {
                children: [sortedSiteOrders.slice(0, 9).map((order, index) => /* @__PURE__ */ jsxs("tr", {
                  children: [siteColumnKeys.map((key) => /* @__PURE__ */ jsx("td", {
                    children: orderColumnValue(order, key, index)
                  }, key)), /* @__PURE__ */ jsx("td", {
                    children: /* @__PURE__ */ jsxs("div", {
                      style: styles$a.siteActions,
                      children: [/* @__PURE__ */ jsxs("div", {
                        style: styles$a.reorderForm,
                        children: [/* @__PURE__ */ jsx("button", {
                          type: "button",
                          style: {
                            ...styles$a.reorderButton,
                            ...index === 0 ? styles$a.reorderButtonDisabled : null
                          },
                          disabled: index === 0,
                          onClick: () => {
                            const routeId = (selectedRoute == null ? void 0 : selectedRoute.id) || "";
                            if (!routeId) return;
                            optimisticallyMoveRouteStop(order.id, routeId, "up");
                            submitClassicMutation({
                              intent: "move-route-stop",
                              routeId,
                              orderId: order.id,
                              direction: "up"
                            });
                          },
                          children: "Up"
                        }), /* @__PURE__ */ jsx("button", {
                          type: "button",
                          style: {
                            ...styles$a.reorderButton,
                            ...index === sortedSiteOrders.length - 1 ? styles$a.reorderButtonDisabled : null
                          },
                          disabled: index === sortedSiteOrders.length - 1,
                          onClick: () => {
                            const routeId = (selectedRoute == null ? void 0 : selectedRoute.id) || "";
                            if (!routeId) return;
                            optimisticallyMoveRouteStop(order.id, routeId, "down");
                            submitClassicMutation({
                              intent: "move-route-stop",
                              routeId,
                              orderId: order.id,
                              direction: "down"
                            });
                          },
                          children: "Down"
                        })]
                      }), /* @__PURE__ */ jsxs(Form, {
                        method: "post",
                        onSubmit: attachLoaderNote,
                        children: [/* @__PURE__ */ jsx("input", {
                          type: "hidden",
                          name: "intent",
                          value: "notify-loader"
                        }), /* @__PURE__ */ jsx("input", {
                          type: "hidden",
                          name: "orderId",
                          value: order.id
                        }), /* @__PURE__ */ jsx("button", {
                          style: styles$a.linkButton,
                          title: "Tell the loader this is the next load",
                          children: "Load Next"
                        })]
                      }), /* @__PURE__ */ jsx("button", {
                        type: "button",
                        style: styles$a.linkButton,
                        onClick: () => {
                          optimisticallyUnassignOrder(order.id);
                          submitClassicMutation({
                            intent: "unassign-order",
                            orderId: order.id
                          });
                        },
                        children: "Unassign"
                      })]
                    })
                  })]
                }, order.id)), !(selectedRoute == null ? void 0 : selectedRoute.orders.length) ? /* @__PURE__ */ jsx("tr", {
                  children: /* @__PURE__ */ jsx("td", {
                    colSpan: siteColumnKeys.length + 1,
                    style: styles$a.emptyCell,
                    children: "Pick a route, then drag unscheduled orders here."
                  })
                }) : null]
              })]
            })]
          }), /* @__PURE__ */ jsx("div", {
            className: "classic-resizer",
            style: styles$a.rowResizeHandle,
            onMouseDown: (event) => beginRowResize("leftRows", 1, event),
            title: "Drag to resize Sites and Orders"
          }), /* @__PURE__ */ jsxs("div", {
            className: "classic-panel",
            style: styles$a.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$a.panelHeader,
              children: [/* @__PURE__ */ jsxs("strong", {
                children: ["Orders ", visibleOrders.length]
              }), /* @__PURE__ */ jsxs("span", {
                children: [deliveredCount, " delivered"]
              })]
            }), /* @__PURE__ */ jsxs("table", {
              className: "classic-table",
              style: {
                ...styles$a.table,
                minWidth: getTableMinWidth("orders", orderColumnKeys)
              },
              children: [columnGroup("orders", orderColumnKeys), /* @__PURE__ */ jsx("thead", {
                children: /* @__PURE__ */ jsx("tr", {
                  children: orderColumnKeys.map((key) => /* @__PURE__ */ jsx("th", {
                    children: sortHeader("orders", key, columnLabel("orders", key))
                  }, key))
                })
              }), /* @__PURE__ */ jsx("tbody", {
                children: sortedVisibleOrders.slice(0, 12).map((order) => /* @__PURE__ */ jsx("tr", {
                  children: orderColumnKeys.map((key) => /* @__PURE__ */ jsx("td", {
                    children: orderColumnValue(order, key)
                  }, key))
                }, order.id))
              })]
            })]
          })]
        }), /* @__PURE__ */ jsx("div", {
          className: "classic-resizer",
          style: styles$a.columnResizePanelHandle,
          onMouseDown: beginMainPanelResize,
          title: "Drag to resize left and right work areas"
        }), /* @__PURE__ */ jsxs("section", {
          className: "classic-right-stack",
          style: {
            ...styles$a.rightStack,
            gridTemplateRows: `${panelLayout.rightRows[0]}fr 8px ${panelLayout.rightRows[1]}fr 8px ${panelLayout.rightRows[2]}fr`
          },
          children: [/* @__PURE__ */ jsx(ClassicMap, {
            googleMapsApiKey,
            originAddress: mapOriginAddress,
            routes: routes2,
            driverLocations
          }), /* @__PURE__ */ jsx("div", {
            className: "classic-resizer",
            style: styles$a.rowResizeHandle,
            onMouseDown: (event) => beginRowResize("rightRows", 0, event),
            title: "Drag to resize Map and Unscheduled"
          }), /* @__PURE__ */ jsxs("div", {
            className: "classic-panel",
            style: styles$a.panel,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles$a.panelHeader,
              children: [/* @__PURE__ */ jsxs("strong", {
                children: ["Unscheduled ", unscheduledOrders.length]
              }), /* @__PURE__ */ jsx("span", {
                children: "Routing"
              })]
            }), /* @__PURE__ */ jsxs("table", {
              className: "classic-table",
              style: {
                ...styles$a.table,
                minWidth: getTableMinWidth("unscheduled", unscheduledColumnKeys, 160)
              },
              children: [columnGroup("unscheduled", unscheduledColumnKeys, [], [160]), /* @__PURE__ */ jsx("thead", {
                children: /* @__PURE__ */ jsxs("tr", {
                  children: [unscheduledColumnKeys.map((key) => /* @__PURE__ */ jsx("th", {
                    children: sortHeader("unscheduled", key, columnLabel("unscheduled", key))
                  }, key)), /* @__PURE__ */ jsx("th", {})]
                })
              }), /* @__PURE__ */ jsxs("tbody", {
                children: [sortedUnscheduledOrders.slice(0, 12).map((order) => /* @__PURE__ */ jsxs("tr", {
                  draggable: true,
                  onDragStart: () => beginOrderDrag(order.id),
                  onDragEnd: () => setDraggedOrderId(""),
                  style: styles$a.draggableRow,
                  children: [unscheduledColumnKeys.map((key) => /* @__PURE__ */ jsx("td", {
                    children: orderColumnValue(order, key)
                  }, key)), /* @__PURE__ */ jsx("td", {
                    children: /* @__PURE__ */ jsxs(Form, {
                      method: "post",
                      style: styles$a.assignForm,
                      onSubmit: (event) => {
                        const routeId = new FormData(event.currentTarget).get("routeId");
                        prepareAssignmentSubmit(order, String(routeId || ""), event.currentTarget, event);
                        if (event.defaultPrevented) return;
                        event.preventDefault();
                        const formData = new FormData(event.currentTarget);
                        const nextRouteId = String(formData.get("routeId") || "");
                        optimisticallyAssignOrder(order.id, nextRouteId);
                        mutationFetcher.submit(formData, {
                          method: "post"
                        });
                      },
                      children: [/* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "intent",
                        value: "assign-order"
                      }), /* @__PURE__ */ jsx("input", {
                        type: "hidden",
                        name: "orderId",
                        value: order.id
                      }), /* @__PURE__ */ jsxs("select", {
                        name: "routeId",
                        style: styles$a.smallSelect,
                        required: true,
                        children: [/* @__PURE__ */ jsx("option", {
                          value: "",
                          children: "Route"
                        }), routes2.map((route57) => /* @__PURE__ */ jsx("option", {
                          value: route57.id,
                          children: route57.code
                        }, route57.id))]
                      }), /* @__PURE__ */ jsx("button", {
                        style: styles$a.assignMini,
                        children: "Assign"
                      })]
                    })
                  })]
                }, order.id)), !unscheduledOrders.length ? /* @__PURE__ */ jsx("tr", {
                  children: /* @__PURE__ */ jsx("td", {
                    colSpan: unscheduledColumnKeys.length + 1,
                    style: styles$a.emptyCell,
                    children: "No unscheduled orders match this search."
                  })
                }) : null]
              })]
            })]
          }), /* @__PURE__ */ jsx("div", {
            className: "classic-resizer",
            style: styles$a.rowResizeHandle,
            onMouseDown: (event) => beginRowResize("rightRows", 1, event),
            title: "Drag to resize Unscheduled and Add forms"
          }), /* @__PURE__ */ jsxs("div", {
            className: "classic-panel",
            style: styles$a.formGrid,
            children: [/* @__PURE__ */ jsxs(Form, {
              id: "add-route",
              method: "post",
              style: styles$a.compactForm,
              children: [/* @__PURE__ */ jsx("input", {
                type: "hidden",
                name: "intent",
                value: "create-route"
              }), /* @__PURE__ */ jsx("strong", {
                children: "Add Route"
              }), /* @__PURE__ */ jsx("input", {
                name: "code",
                placeholder: "R-22",
                style: styles$a.input
              }), /* @__PURE__ */ jsxs("select", {
                name: "truckId",
                style: styles$a.input,
                children: [/* @__PURE__ */ jsx("option", {
                  value: "",
                  children: "Truck"
                }), trucks.map((truck) => /* @__PURE__ */ jsx("option", {
                  value: truck.id,
                  children: truck.label
                }, truck.id))]
              }), /* @__PURE__ */ jsxs("select", {
                name: "driverId",
                style: styles$a.input,
                children: [/* @__PURE__ */ jsx("option", {
                  value: "",
                  children: "Driver"
                }), drivers.map((driver) => /* @__PURE__ */ jsx("option", {
                  value: driver.id,
                  children: driver.name
                }, driver.id))]
              }), /* @__PURE__ */ jsxs("select", {
                name: "helperId",
                style: styles$a.input,
                children: [/* @__PURE__ */ jsx("option", {
                  value: "",
                  children: "Helper"
                }), helpers.map((helper) => /* @__PURE__ */ jsx("option", {
                  value: helper.id,
                  children: helper.name
                }, helper.id))]
              }), /* @__PURE__ */ jsx("input", {
                name: "shift",
                placeholder: "6:00a - 2:30p",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("input", {
                name: "region",
                placeholder: "Region",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("input", {
                name: "color",
                type: "color",
                defaultValue: "#f97316",
                style: styles$a.colorInput
              }), /* @__PURE__ */ jsx("button", {
                style: styles$a.orangeButton,
                children: "Add Route"
              })]
            }), /* @__PURE__ */ jsxs(Form, {
              id: "add-order",
              method: "post",
              style: styles$a.compactForm,
              children: [/* @__PURE__ */ jsx("input", {
                type: "hidden",
                name: "intent",
                value: "create-order"
              }), /* @__PURE__ */ jsx("strong", {
                children: "Add Order"
              }), /* @__PURE__ */ jsx("input", {
                name: "orderNumber",
                placeholder: "Order number",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("input", {
                name: "customer",
                placeholder: "Customer",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("input", {
                name: "contact",
                placeholder: "Contact / email",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("input", {
                name: "address",
                placeholder: "Address",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("input", {
                name: "city",
                placeholder: "City, ST ZIP",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("input", {
                name: "material",
                placeholder: "Material",
                list: "classic-materials",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("datalist", {
                id: "classic-materials",
                children: materialOptions.map((material) => /* @__PURE__ */ jsx("option", {
                  value: material
                }, material))
              }), /* @__PURE__ */ jsx("input", {
                name: "quantity",
                placeholder: "Qty",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("input", {
                name: "requestedWindow",
                type: "date",
                style: styles$a.input
              }), /* @__PURE__ */ jsx("button", {
                style: styles$a.orangeButton,
                children: "Add Order"
              })]
            })]
          })]
        })]
      }), routeDrawerOpen && selectedRoute ? /* @__PURE__ */ jsxs("aside", {
        style: styles$a.routeDrawer,
        children: [/* @__PURE__ */ jsx("button", {
          type: "button",
          style: styles$a.drawerClose,
          onClick: () => setRouteDrawerOpen(false),
          children: "×"
        }), /* @__PURE__ */ jsx("h2", {
          style: styles$a.drawerTitle,
          children: "Route Attributes"
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$a.drawerTabs,
          children: [/* @__PURE__ */ jsx("span", {
            style: styles$a.drawerTabActive,
            children: "Info"
          }), /* @__PURE__ */ jsx("span", {
            style: styles$a.drawerTab,
            children: "History"
          })]
        }), /* @__PURE__ */ jsxs("dl", {
          style: styles$a.drawerDetails,
          children: [/* @__PURE__ */ jsx("dt", {
            children: "Code"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedRoute.code
          }), /* @__PURE__ */ jsx("dt", {
            children: "Truck"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedRoute.truck || "Unassigned"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Driver"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedRoute.driver || "Unassigned"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Helper"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedRoute.helper || "None"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Region"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedRoute.region || "No region"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Stops"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedRoute.orders.length
          }), /* @__PURE__ */ jsx("dt", {
            children: "Total route time"
          }), /* @__PURE__ */ jsx("dd", {
            children: formatTime$1(selectedRoute.totalMinutes)
          })]
        })]
      }) : null, orderDrawerOpen && selectedOrder ? /* @__PURE__ */ jsxs("aside", {
        style: styles$a.routeDrawer,
        children: [/* @__PURE__ */ jsx("button", {
          type: "button",
          style: styles$a.drawerClose,
          onClick: () => setOrderDrawerOpen(false),
          children: "×"
        }), /* @__PURE__ */ jsx("h2", {
          style: styles$a.drawerTitle,
          children: "Order Attributes"
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$a.drawerTabs,
          children: [/* @__PURE__ */ jsx("span", {
            style: styles$a.drawerTabActive,
            children: "Info"
          }), /* @__PURE__ */ jsx("span", {
            style: styles$a.drawerTab,
            children: "History"
          })]
        }), /* @__PURE__ */ jsxs("dl", {
          style: styles$a.drawerDetails,
          children: [/* @__PURE__ */ jsx("dt", {
            children: "Order"
          }), /* @__PURE__ */ jsx("dd", {
            children: getOrderNumber$5(selectedOrder)
          }), /* @__PURE__ */ jsx("dt", {
            children: "Customer"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedOrder.customer || "No customer"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Contact"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedOrder.contact || "No contact"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Address"
          }), /* @__PURE__ */ jsx("dd", {
            children: getOrderAddress$4(selectedOrder) || "No address"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Load"
          }), /* @__PURE__ */ jsxs("dd", {
            children: [selectedOrder.quantity || "-", " ", selectedOrder.unit, " ", selectedOrder.material]
          }), /* @__PURE__ */ jsx("dt", {
            children: "Product SKU"
          }), /* @__PURE__ */ jsx("dd", {
            children: (selectedOrderProductDetail == null ? void 0 : selectedOrderProductDetail.sku) || "No SKU matched"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Product Title"
          }), /* @__PURE__ */ jsx("dd", {
            children: (selectedOrderProductDetail == null ? void 0 : selectedOrderProductDetail.title) || selectedOrder.material || "No product matched"
          }), (selectedOrderProductDetail == null ? void 0 : selectedOrderProductDetail.unitLabel) ? /* @__PURE__ */ jsxs(Fragment, {
            children: [/* @__PURE__ */ jsx("dt", {
              children: "Unit Label"
            }), /* @__PURE__ */ jsx("dd", {
              children: selectedOrderProductDetail.unitLabel
            })]
          }) : null, (selectedOrderProductDetail == null ? void 0 : selectedOrderProductDetail.vendor) ? /* @__PURE__ */ jsxs(Fragment, {
            children: [/* @__PURE__ */ jsx("dt", {
              children: "Vendor"
            }), /* @__PURE__ */ jsx("dd", {
              children: selectedOrderProductDetail.vendor
            })]
          }) : null, /* @__PURE__ */ jsx("dt", {
            children: "Requested"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedOrder.requestedWindow || "Needs scheduling"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Status"
          }), /* @__PURE__ */ jsx("dd", {
            children: statusLabel(selectedOrder)
          }), /* @__PURE__ */ jsx("dt", {
            children: "Route"
          }), /* @__PURE__ */ jsx("dd", {
            children: ((_b = routes2.find((route57) => route57.id === selectedOrder.assignedRouteId)) == null ? void 0 : _b.code) || "Unassigned"
          }), /* @__PURE__ */ jsx("dt", {
            children: "Travel"
          }), /* @__PURE__ */ jsx("dd", {
            children: selectedOrder.travelSummary || formatTime$1(getTravelMinutes$4(selectedOrder))
          })]
        }), (selectedOrderProductDetail == null ? void 0 : selectedOrderProductDetail.imageUrl) ? /* @__PURE__ */ jsxs("div", {
          style: styles$a.productPreview,
          children: [/* @__PURE__ */ jsx("img", {
            src: selectedOrderProductDetail.imageUrl,
            alt: selectedOrderProductDetail.title,
            style: styles$a.productPreviewImage
          }), /* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsx("strong", {
              children: selectedOrderProductDetail.title
            }), /* @__PURE__ */ jsx("p", {
              children: "Use this synced product match to verify blend/mix details before loading."
            })]
          })]
        }) : null, selectedOrder.notes ? /* @__PURE__ */ jsxs("div", {
          style: styles$a.drawerNotes,
          children: [/* @__PURE__ */ jsx("strong", {
            children: "Notes"
          }), /* @__PURE__ */ jsx("p", {
            children: selectedOrder.notes
          })]
        }) : null, /* @__PURE__ */ jsxs("div", {
          style: styles$a.drawerButtonGrid,
          children: [/* @__PURE__ */ jsx("a", {
            href: editorHref(selectedOrder.id),
            style: styles$a.drawerLinkButton,
            children: "Open in editor"
          }), selectedOrder.assignedRouteId ? /* @__PURE__ */ jsxs(Form, {
            method: "post",
            children: [/* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "intent",
              value: "unassign-order"
            }), /* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "orderId",
              value: selectedOrder.id
            }), /* @__PURE__ */ jsx("button", {
              style: styles$a.drawerButton,
              children: "Unassign"
            })]
          }) : null]
        })]
      }) : null]
    })]
  });
});
const styles$a = {
  page: {
    minHeight: "100vh",
    display: "grid",
    background: "var(--classic-bg)",
    color: "var(--classic-text)",
    fontFamily: "Verdana, Geneva, Tahoma, sans-serif",
    fontSize: 12,
    overflowX: "auto",
    transition: "grid-template-columns 160ms ease"
  },
  sideRail: {
    minHeight: "100vh",
    display: "flex",
    flexDirection: "column",
    gap: 12,
    background: "var(--classic-rail-bg)",
    borderRight: "1px solid var(--classic-rail-border)",
    overflow: "hidden",
    transition: "padding 160ms ease"
  },
  navToggle: {
    width: 34,
    height: 30,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 8,
    border: "1px solid var(--classic-rail-border)",
    background: "var(--classic-rail-active-bg)",
    color: "#ff7a1a",
    fontWeight: 900,
    cursor: "pointer"
  },
  collapsedOnlyHidden: {
    display: "none"
  },
  classicBrand: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    color: "var(--classic-rail-link)",
    paddingBottom: 10,
    borderBottom: "1px solid var(--classic-rail-border)"
  },
  classicBrandTitle: {
    fontWeight: 900,
    fontSize: 15
  },
  classicBrandSub: {
    color: "var(--classic-muted)",
    fontSize: 11,
    fontWeight: 800
  },
  railLogo: {
    width: 36,
    height: 36,
    display: "grid",
    placeItems: "center",
    borderRadius: 8,
    background: "#ff7a1a",
    color: "#fff",
    fontWeight: 900
  },
  classicNav: {
    display: "grid",
    gap: 6
  },
  classicNavLink: {
    minHeight: 38,
    display: "flex",
    alignItems: "center",
    padding: "0 12px",
    borderRadius: 8,
    color: "var(--classic-rail-link)",
    textDecoration: "none",
    fontWeight: 900
  },
  classicNavLinkActive: {
    minHeight: 38,
    display: "flex",
    alignItems: "center",
    padding: "0 12px",
    borderRadius: 8,
    color: "#ff7a1a",
    background: "var(--classic-rail-active-bg)",
    textDecoration: "none",
    fontWeight: 900
  },
  classicFooterNav: {
    display: "grid",
    gap: 8
  },
  classicUtility: {
    minHeight: 34,
    display: "flex",
    alignItems: "center",
    padding: "0 10px",
    borderRadius: 8,
    border: "1px solid var(--classic-rail-border)",
    color: "var(--classic-rail-link)",
    textDecoration: "none",
    fontWeight: 800,
    fontSize: 12
  },
  workspace: {
    minWidth: 1420,
    overflow: "hidden"
  },
  topBar: {
    minHeight: 58,
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "0 14px",
    background: "var(--classic-panel-bg)",
    borderBottom: "1px solid var(--classic-border)",
    boxShadow: "0 1px 2px rgba(0,0,0,0.08)"
  },
  brandMark: {
    width: 32,
    height: 32,
    display: "grid",
    placeItems: "center",
    borderRadius: "50%",
    color: "#ff7a1a",
    border: "3px solid #ff7a1a",
    fontWeight: 900
  },
  brandText: {
    fontSize: 18,
    whiteSpace: "nowrap"
  },
  search: {
    width: 380,
    maxWidth: "34vw",
    height: 34,
    borderRadius: 999,
    border: "1px solid var(--classic-border)",
    background: "var(--classic-input-bg)",
    color: "var(--classic-text)",
    padding: "0 16px",
    outline: "none"
  },
  topForm: {
    marginLeft: "auto"
  },
  compactTopForm: {
    marginLeft: 0
  },
  exportForm: {
    display: "inline-flex",
    alignItems: "center",
    gap: 8,
    marginLeft: 0
  },
  exportDateInput: {
    height: 34,
    width: 136,
    borderRadius: 999,
    border: "1px solid var(--classic-border)",
    background: "var(--classic-input-bg)",
    color: "var(--classic-text)",
    padding: "0 10px",
    fontWeight: 800
  },
  company: {
    marginLeft: 12,
    fontWeight: 800,
    whiteSpace: "nowrap"
  },
  manualRouteToggle: {
    height: 34,
    display: "inline-flex",
    alignItems: "center",
    gap: 7,
    padding: "0 12px",
    borderRadius: 999,
    border: "1px solid var(--classic-border)",
    background: "var(--classic-panel-bg)",
    color: "var(--classic-text)",
    fontWeight: 800,
    whiteSpace: "nowrap"
  },
  outlineButton: {
    height: 34,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "0 16px",
    borderRadius: 999,
    border: "1px solid #f97316",
    background: "var(--classic-panel-bg)",
    color: "#e85d04",
    textDecoration: "none",
    fontWeight: 800,
    cursor: "pointer"
  },
  disabledOutlineButton: {
    borderColor: "var(--classic-border)",
    color: "var(--classic-muted)",
    opacity: 0.65,
    cursor: "not-allowed"
  },
  orangeButton: {
    minHeight: 34,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "0 16px",
    borderRadius: 999,
    border: "1px solid #ff7a1a",
    background: "linear-gradient(180deg, #ff9b25, #ff6b00)",
    color: "#fff",
    textDecoration: "none",
    fontWeight: 900,
    cursor: "pointer"
  },
  chimeStatus: {
    color: "#93c5fd",
    fontSize: 12,
    fontWeight: 800,
    whiteSpace: "nowrap"
  },
  plannerBar: {
    minHeight: 54,
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "8px 14px",
    background: "var(--classic-panel-header-bg)",
    borderBottom: "1px solid var(--classic-border)",
    overflowX: "auto"
  },
  plannerDayTabs: {
    display: "flex",
    alignItems: "center",
    gap: 6
  },
  plannerDayButton: (active) => ({
    minWidth: 96,
    minHeight: 38,
    display: "grid",
    gap: 2,
    alignContent: "center",
    justifyItems: "start",
    borderRadius: 8,
    border: active ? "1px solid #ff7a1a" : "1px solid var(--classic-border)",
    background: active ? "rgba(249, 115, 22, 0.18)" : "var(--classic-input-bg)",
    color: active ? "#ff9b25" : "var(--classic-text)",
    padding: "4px 10px",
    fontWeight: 900,
    cursor: "pointer",
    whiteSpace: "nowrap"
  }),
  plannerNavButton: {
    minHeight: 38,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "0 12px",
    borderRadius: 8,
    border: "1px solid var(--classic-border)",
    background: "var(--classic-input-bg)",
    color: "var(--classic-text)",
    fontWeight: 900,
    cursor: "pointer",
    whiteSpace: "nowrap"
  },
  plannerDateInput: {
    minHeight: 38,
    width: 140,
    borderRadius: 8,
    border: "1px solid var(--classic-border)",
    background: "var(--classic-input-bg)",
    color: "var(--classic-text)",
    padding: "0 10px",
    fontWeight: 900
  },
  plannerHint: {
    color: "var(--classic-muted)",
    fontWeight: 900,
    whiteSpace: "nowrap"
  },
  messageBanner: {
    padding: "8px 14px",
    background: "rgba(56, 189, 248, 0.16)",
    borderBottom: "1px solid rgba(56, 189, 248, 0.38)",
    color: "#e0f2fe",
    fontWeight: 700
  },
  errorBanner: {
    padding: "8px 14px",
    background: "rgba(127, 29, 29, 0.35)",
    borderBottom: "1px solid rgba(248, 113, 113, 0.4)",
    color: "#fecaca",
    fontWeight: 700
  },
  mainGrid: {
    height: "calc(100vh - 112px)",
    display: "grid",
    gap: 0,
    overflow: "hidden"
  },
  leftStack: {
    minWidth: 0,
    display: "grid",
    overflow: "hidden"
  },
  rightStack: {
    minWidth: 0,
    display: "grid",
    overflow: "hidden"
  },
  columnResizePanelHandle: {
    width: 8,
    height: "100%",
    cursor: "col-resize",
    background: "linear-gradient(90deg, transparent, rgba(56, 189, 248, 0.25), transparent)",
    borderLeft: "1px solid var(--classic-border)",
    borderRight: "1px solid var(--classic-border)"
  },
  rowResizeHandle: {
    height: 8,
    cursor: "row-resize",
    background: "linear-gradient(180deg, transparent, rgba(56, 189, 248, 0.24), transparent)",
    borderTop: "1px solid var(--classic-border)",
    borderBottom: "1px solid var(--classic-border)"
  },
  panel: {
    minHeight: 0,
    overflow: "auto",
    background: "var(--classic-panel-bg)",
    borderBottom: "1px solid var(--classic-border)"
  },
  panelHeader: {
    height: 34,
    display: "flex",
    alignItems: "center",
    gap: 12,
    padding: "0 10px",
    borderBottom: "1px solid var(--classic-border)",
    background: "var(--classic-panel-header-bg)",
    color: "var(--classic-text)"
  },
  table: {
    width: "100%",
    minWidth: 620,
    borderCollapse: "collapse",
    tableLayout: "fixed"
  },
  colorBar: {
    display: "inline-block",
    width: 8,
    height: 32,
    borderRadius: 2
  },
  statusPill: (tone) => {
    const tones = {
      delivered: {
        background: "rgba(34, 197, 94, 0.18)",
        border: "rgba(34, 197, 94, 0.55)",
        color: "#86efac"
      },
      scheduled: {
        background: "rgba(14, 165, 233, 0.18)",
        border: "rgba(14, 165, 233, 0.55)",
        color: "#7dd3fc"
      },
      hold: {
        background: "rgba(245, 158, 11, 0.2)",
        border: "rgba(245, 158, 11, 0.58)",
        color: "#fcd34d"
      },
      cancelled: {
        background: "rgba(127, 29, 29, 0.24)",
        border: "rgba(248, 113, 113, 0.55)",
        color: "#fecaca"
      },
      unscheduled: {
        background: "rgba(249, 115, 22, 0.18)",
        border: "rgba(249, 115, 22, 0.55)",
        color: "#fdba74"
      }
    };
    const selected = tones[tone] || tones.unscheduled;
    return {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      minWidth: 76,
      padding: "3px 8px",
      borderRadius: 999,
      border: `1px solid ${selected.border}`,
      background: selected.background,
      color: selected.color,
      fontSize: 11,
      fontWeight: 950,
      lineHeight: 1.2,
      textTransform: "uppercase",
      letterSpacing: "0.035em"
    };
  },
  rowRouteButton: (active) => ({
    border: "none",
    background: "transparent",
    color: active ? "#f97316" : "#0ea5c6",
    fontWeight: 900,
    cursor: "pointer",
    textDecoration: active ? "underline" : "none"
  }),
  rowOrderButton: {
    border: "none",
    background: "transparent",
    color: "#0ea5c6",
    fontWeight: 900,
    cursor: "pointer",
    textDecoration: "underline"
  },
  rowSkuButton: {
    border: "none",
    background: "transparent",
    color: "#22c55e",
    fontWeight: 900,
    cursor: "pointer",
    textDecoration: "underline"
  },
  rowSkuButtonMuted: {
    border: "none",
    background: "transparent",
    color: "var(--classic-muted)",
    fontWeight: 800,
    cursor: "pointer"
  },
  dropZone: {
    height: 24,
    display: "grid",
    placeItems: "center",
    borderBottom: "1px dashed var(--classic-border)",
    background: "var(--classic-panel-header-bg)",
    color: "var(--classic-muted)",
    fontSize: 11
  },
  dropZoneActive: {
    height: 24,
    display: "grid",
    placeItems: "center",
    borderBottom: "1px dashed #f97316",
    background: "#fff7ed",
    color: "#c2410c",
    fontSize: 11,
    fontWeight: 900
  },
  draggableRow: {
    cursor: "grab"
  },
  inlineActions: {
    display: "flex",
    gap: 4
  },
  siteActions: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-end",
    gap: 8,
    flexWrap: "wrap"
  },
  reorderForm: {
    display: "flex",
    gap: 4
  },
  reorderButton: {
    minHeight: 24,
    border: "1px solid rgba(14, 165, 198, 0.5)",
    borderRadius: 4,
    background: "rgba(14, 165, 198, 0.1)",
    color: "#0ea5c6",
    fontSize: 10,
    fontWeight: 900,
    cursor: "pointer"
  },
  reorderButtonDisabled: {
    opacity: 0.35,
    cursor: "not-allowed"
  },
  iconButton: {
    width: 25,
    height: 25,
    border: "1px solid var(--classic-border)",
    borderRadius: 4,
    background: "var(--classic-input-bg)",
    color: "var(--classic-text)",
    cursor: "pointer"
  },
  linkButton: {
    border: "none",
    background: "transparent",
    color: "#0ea5c6",
    fontWeight: 800,
    cursor: "pointer"
  },
  resizableHeader: {
    position: "relative",
    minHeight: 22,
    display: "flex",
    alignItems: "center",
    paddingRight: 8
  },
  columnResizeHandle: {
    position: "absolute",
    top: -3,
    right: -7,
    width: 12,
    height: "calc(100% + 6px)",
    cursor: "col-resize",
    zIndex: 2
  },
  sortHeader: {
    width: "100%",
    border: "none",
    background: "transparent",
    color: "inherit",
    cursor: "pointer",
    font: "inherit",
    fontWeight: 800,
    padding: 0,
    textAlign: "left",
    whiteSpace: "nowrap"
  },
  sortHeaderActive: {
    width: "100%",
    border: "none",
    background: "transparent",
    color: "#f97316",
    cursor: "pointer",
    font: "inherit",
    fontWeight: 900,
    padding: 0,
    textAlign: "left",
    whiteSpace: "nowrap"
  },
  emptyCell: {
    padding: 18,
    color: "var(--classic-muted)",
    textAlign: "center"
  },
  mapCanvas: {
    position: "relative",
    minHeight: 0,
    overflow: "hidden",
    background: "var(--classic-map-bg)",
    borderBottom: "1px solid var(--classic-border)"
  },
  routeSvg: {
    position: "absolute",
    inset: 0,
    width: "100%",
    height: "100%"
  },
  realMapCanvas: {
    position: "absolute",
    inset: 0,
    width: "100%",
    height: "100%"
  },
  classicMapStatus: {
    position: "absolute",
    left: 12,
    top: 12,
    zIndex: 3,
    padding: "7px 10px",
    borderRadius: 4,
    background: "rgba(255, 255, 255, 0.94)",
    boxShadow: "0 1px 4px rgba(0,0,0,0.18)",
    color: "#444",
    fontWeight: 800
  },
  mapToolbar: {
    position: "absolute",
    top: 12,
    right: 12,
    zIndex: 2,
    display: "flex",
    border: "1px solid #aaa",
    background: "var(--classic-panel-bg)",
    boxShadow: "0 1px 4px rgba(0,0,0,0.18)"
  },
  mapToggle: {
    border: "none",
    background: "#e8eefb",
    padding: "8px 14px",
    fontWeight: 800
  },
  mapToggleMuted: {
    border: "none",
    background: "var(--classic-panel-bg)",
    padding: "8px 14px"
  },
  mapLegend: {
    position: "absolute",
    left: 12,
    bottom: 10,
    zIndex: 2,
    display: "flex",
    gap: 8,
    flexWrap: "wrap",
    maxWidth: "70%"
  },
  legendItem: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "5px 8px",
    borderRadius: 4,
    border: "1px solid var(--classic-border)",
    background: "var(--classic-panel-bg)",
    color: "var(--classic-text)",
    boxShadow: "0 1px 3px rgba(0,0,0,0.16)",
    fontWeight: 700,
    cursor: "pointer"
  },
  legendItemOff: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "5px 8px",
    borderRadius: 4,
    border: "1px solid var(--classic-border)",
    background: "var(--classic-panel-bg)",
    color: "var(--classic-muted)",
    boxShadow: "0 1px 3px rgba(0,0,0,0.12)",
    fontWeight: 700,
    opacity: 0.48,
    cursor: "pointer",
    textDecoration: "line-through"
  },
  legendDot: {
    width: 10,
    height: 10,
    borderRadius: "50%"
  },
  driverLegendItem: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "5px 8px",
    borderRadius: 4,
    border: "1px solid rgba(34, 197, 94, 0.5)",
    background: "rgba(220, 252, 231, 0.96)",
    color: "#052e16",
    boxShadow: "0 1px 3px rgba(0,0,0,0.16)",
    fontWeight: 900
  },
  driverLegendDot: {
    width: 0,
    height: 0,
    borderLeft: "6px solid transparent",
    borderRight: "6px solid transparent",
    borderBottom: "12px solid #22c55e"
  },
  assignForm: {
    display: "flex",
    gap: 4
  },
  smallSelect: {
    width: 78,
    height: 26,
    border: "1px solid var(--classic-border)",
    background: "var(--classic-input-bg)",
    color: "var(--classic-text)",
    borderRadius: 4
  },
  assignMini: {
    height: 26,
    border: "none",
    borderRadius: 4,
    background: "#22c55e",
    color: "#fff",
    fontWeight: 800,
    cursor: "pointer"
  },
  formGrid: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: 8,
    padding: 8,
    overflow: "auto",
    background: "var(--classic-panel-header-bg)"
  },
  compactForm: {
    display: "grid",
    gridTemplateColumns: "repeat(4, minmax(100px, 1fr))",
    gap: 6,
    alignItems: "center",
    padding: 8,
    border: "1px solid var(--classic-border)",
    borderRadius: 6,
    background: "var(--classic-panel-bg)"
  },
  input: {
    width: "100%",
    minHeight: 32,
    boxSizing: "border-box",
    border: "1px solid var(--classic-border)",
    borderRadius: 4,
    padding: "0 8px",
    background: "var(--classic-input-bg)",
    color: "var(--classic-text)"
  },
  colorInput: {
    width: "100%",
    height: 32,
    border: "1px solid var(--classic-border)",
    borderRadius: 4,
    background: "var(--classic-input-bg)"
  },
  routeDrawer: {
    position: "absolute",
    top: 58,
    right: 0,
    bottom: 0,
    zIndex: 9,
    width: 300,
    background: "var(--classic-drawer-bg)",
    borderLeft: "1px solid var(--classic-border)",
    boxShadow: "-16px 0 35px rgba(0,0,0,0.18)",
    padding: 18,
    color: "var(--classic-text)"
  },
  drawerClose: {
    position: "absolute",
    top: 10,
    right: 10,
    width: 28,
    height: 28,
    border: "1px solid var(--classic-border)",
    borderRadius: "50%",
    background: "var(--classic-panel-bg)",
    color: "var(--classic-text)",
    cursor: "pointer",
    fontSize: 18
  },
  drawerTitle: {
    margin: "0 0 14px",
    fontSize: 18
  },
  drawerTabs: {
    display: "flex",
    gap: 14,
    borderBottom: "1px solid var(--classic-border)",
    marginBottom: 12
  },
  drawerTabActive: {
    padding: "0 0 8px",
    color: "#f97316",
    borderBottom: "2px solid #f97316",
    fontWeight: 900
  },
  drawerTab: {
    padding: "0 0 8px",
    color: "var(--classic-muted)"
  },
  drawerDetails: {
    display: "grid",
    gridTemplateColumns: "100px 1fr",
    gap: "8px 10px",
    margin: 0
  },
  drawerForm: {
    marginTop: 18
  },
  drawerNotes: {
    marginTop: 18,
    padding: 10,
    border: "1px solid var(--classic-border)",
    borderRadius: 6,
    background: "var(--classic-panel-header-bg)"
  },
  productPreview: {
    marginTop: 16,
    display: "grid",
    gridTemplateColumns: "72px 1fr",
    gap: 12,
    alignItems: "center",
    padding: 10,
    border: "1px solid var(--classic-border)",
    borderRadius: 6,
    background: "var(--classic-panel-header-bg)"
  },
  productPreviewImage: {
    width: 72,
    height: 72,
    objectFit: "cover",
    borderRadius: 6,
    border: "1px solid var(--classic-border)"
  },
  drawerButtonGrid: {
    display: "grid",
    gap: 8,
    marginTop: 18
  },
  drawerLinkButton: {
    minHeight: 36,
    display: "grid",
    placeItems: "center",
    borderRadius: 6,
    background: "#0ea5c6",
    color: "#fff",
    textDecoration: "none",
    fontWeight: 900
  },
  drawerButton: {
    width: "100%",
    minHeight: 36,
    border: "none",
    borderRadius: 6,
    background: "#22c55e",
    color: "#fff",
    fontWeight: 900,
    cursor: "pointer"
  },
  loginPage: {
    minHeight: "100vh",
    display: "grid",
    placeItems: "center",
    background: "#f3f4f6",
    color: "#111827",
    fontFamily: "Verdana, Geneva, Tahoma, sans-serif"
  },
  loginBox: {
    width: 360,
    display: "grid",
    gap: 12,
    padding: 24,
    borderRadius: 10,
    background: "#fff",
    boxShadow: "0 18px 55px rgba(0,0,0,0.16)"
  },
  loginTitle: {
    margin: 0,
    fontSize: 24
  },
  muted: {
    margin: 0,
    color: "#6b7280"
  }
};
const route6 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$o,
  default: classic,
  loader: loader$m,
  shouldRevalidate
}, Symbol.toStringTag, { value: "Module" }));
const loader$l = loader$o;
const action$n = action$p;
const MONITOR_VIEWPORT_KEY = "dispatchMonitorMapViewport";
const MONITOR_HIDDEN_ROUTES_KEY = "dispatchMonitorHiddenRoutes";
const MONITOR_SELECTED_DATE_KEY = "dispatchMonitorSelectedDate";
const MONITOR_DATE_FILTER_KEY = "dispatchMonitorDateFilter";
const MONITOR_COMPACT_ROUTES_KEY = "dispatchMonitorCompactRoutes";
const DISPATCH_NAV_COLLAPSED_KEY$2 = "dispatchNavCollapsed";
let monitorGoogleMapsLoader = null;
function loadMonitorGoogleMaps(apiKey) {
  var _a2;
  if (typeof window === "undefined") return Promise.resolve();
  if ((_a2 = window.google) == null ? void 0 : _a2.maps) return Promise.resolve();
  if (monitorGoogleMapsLoader) return monitorGoogleMapsLoader;
  monitorGoogleMapsLoader = new Promise((resolve, reject) => {
    var _a3;
    const existing = document.querySelector('script[data-monitor-google-maps="true"], script[data-classic-google-maps="true"], script[data-dispatch-google-maps="true"]');
    if (existing) {
      if ((_a3 = window.google) == null ? void 0 : _a3.maps) {
        resolve();
        return;
      }
      existing.addEventListener("load", () => resolve(), {
        once: true
      });
      existing.addEventListener("error", () => reject(new Error("Failed to load Google Maps")), {
        once: true
      });
      return;
    }
    const script = document.createElement("script");
    script.dataset.monitorGoogleMaps = "true";
    script.async = true;
    script.defer = true;
    script.src = `https://maps.googleapis.com/maps/api/js?key=${encodeURIComponent(apiKey)}`;
    script.onload = () => resolve();
    script.onerror = () => reject(new Error("Failed to load Google Maps"));
    document.head.appendChild(script);
  });
  return monitorGoogleMapsLoader;
}
function getOrderNumber$4(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function getOrderAddress$3(order) {
  return [order.address, order.city].filter(Boolean).join(", ");
}
function getLoadLabel$3(order) {
  return [order.quantity, order.unit, order.material].filter(Boolean).join(" ");
}
function parseRequestedDate$4(value) {
  const trimmed = String(value || "").trim();
  if (!trimmed || /needs scheduling|unavailable|unknown/i.test(trimmed)) return null;
  const today = /* @__PURE__ */ new Date();
  if (/\btoday\b/i.test(trimmed)) {
    return new Date(today.getFullYear(), today.getMonth(), today.getDate());
  }
  if (/\btomorrow\b/i.test(trimmed)) {
    const tomorrow = new Date(today.getFullYear(), today.getMonth(), today.getDate());
    tomorrow.setDate(tomorrow.getDate() + 1);
    return tomorrow;
  }
  const isoDate = trimmed.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (isoDate) return new Date(Number(isoDate[1]), Number(isoDate[2]) - 1, Number(isoDate[3]));
  const slashDate = trimmed.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return new Date(year, Number(slashDate[1]) - 1, Number(slashDate[2]));
  }
  const parsed = new Date(trimmed);
  if (Number.isNaN(parsed.getTime())) return null;
  return new Date(parsed.getFullYear(), parsed.getMonth(), parsed.getDate());
}
function dateKey$4(date) {
  if (!date) return "unscheduled";
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function formatDateLabel$3(date) {
  if (!date) return "Needs scheduling";
  return date.toLocaleDateString("en-US", {
    weekday: "short",
    month: "short",
    day: "numeric"
  });
}
function getTravelMinutes$3(order) {
  const minutes = Number(order.travelMinutes || 0);
  return Number.isFinite(minutes) && minutes > 0 ? minutes : 0;
}
function formatTravel(minutes) {
  if (!minutes) return "No travel time";
  if (minutes < 60) return `${Math.round(minutes)} min`;
  const hours = Math.floor(minutes / 60);
  const remainder = Math.round(minutes % 60);
  return remainder ? `${hours}h ${remainder}m` : `${hours}h`;
}
function readStorageJson(key, fallback) {
  if (typeof window === "undefined") return fallback;
  try {
    const value = window.localStorage.getItem(key);
    return value ? JSON.parse(value) : fallback;
  } catch {
    return fallback;
  }
}
function MonitorMap({
  googleMapsApiKey,
  originAddress,
  routes: routes2,
  driverLocations,
  hiddenRouteIds,
  selectedDateKey,
  filterByDate
}) {
  const mapRef = useRef(null);
  const mapInstanceRef = useRef(null);
  const routeObjectsRef = useRef([]);
  const driverMarkersRef = useRef([]);
  const [status, setStatus] = useState("");
  const visibleRouteIds = useMemo(() => new Set(routes2.map((route57) => route57.id).filter((id) => !hiddenRouteIds.includes(id))), [routes2, hiddenRouteIds]);
  const routeColorById = useMemo(() => new Map(routes2.map((route57) => [route57.id, route57.color || "#f97316"])), [routes2]);
  const routePlan = useMemo(() => routes2.filter((route57) => route57.orders.length && visibleRouteIds.has(route57.id)).map((route57) => {
    const orders = filterByDate ? route57.orders.filter((order) => dateKey$4(parseRequestedDate$4(order.requestedWindow)) === selectedDateKey) : route57.orders;
    return {
      id: route57.id,
      code: route57.code,
      truck: route57.truck,
      color: route57.color || "#f97316",
      stops: orders.map((order) => ({
        address: getOrderAddress$3(order),
        customer: order.customer,
        label: getOrderNumber$4(order),
        load: getLoadLabel$3(order)
      })).filter((stop) => stop.address)
    };
  }).filter((route57) => route57.stops.length), [filterByDate, routes2, selectedDateKey, visibleRouteIds]);
  const routePlanKey = useMemo(() => JSON.stringify(routePlan.map((route57) => ({
    id: route57.id,
    color: route57.color,
    stops: route57.stops.map((stop) => stop.address)
  }))), [routePlan]);
  useEffect(() => {
    let cancelled = false;
    async function drawRoutes() {
      if (!mapRef.current) return;
      if (!googleMapsApiKey || !originAddress) {
        setStatus("Google Maps is not configured.");
        return;
      }
      try {
        await loadMonitorGoogleMaps(googleMapsApiKey);
        if (cancelled || !mapRef.current) return;
        const google = window.google;
        const savedViewport = readStorageJson(MONITOR_VIEWPORT_KEY, null);
        const map = mapInstanceRef.current || new google.maps.Map(mapRef.current, {
          center: (savedViewport == null ? void 0 : savedViewport.center) || {
            lat: 43.1789,
            lng: -88.1173
          },
          zoom: (savedViewport == null ? void 0 : savedViewport.zoom) || 9,
          mapTypeId: (savedViewport == null ? void 0 : savedViewport.mapTypeId) || "roadmap",
          mapTypeControl: true,
          streetViewControl: false,
          fullscreenControl: true
        });
        mapInstanceRef.current = map;
        const saveViewport = () => {
          const center = map.getCenter();
          if (!center) return;
          window.localStorage.setItem(MONITOR_VIEWPORT_KEY, JSON.stringify({
            center: {
              lat: center.lat(),
              lng: center.lng()
            },
            zoom: map.getZoom(),
            mapTypeId: map.getMapTypeId()
          }));
        };
        google.maps.event.clearListeners(map, "idle");
        map.addListener("idle", saveViewport);
        routeObjectsRef.current.forEach((object) => {
          var _a2;
          return (_a2 = object.setMap) == null ? void 0 : _a2.call(object, null);
        });
        routeObjectsRef.current = [];
        const bounds = new google.maps.LatLngBounds();
        const directionsService = new google.maps.DirectionsService();
        let yardMarkerAdded = false;
        for (const route57 of routePlan.slice(0, 8)) {
          for (const [stopIndex, stop] of route57.stops.entries()) {
            await new Promise((resolve) => {
              const renderer = new google.maps.DirectionsRenderer({
                map,
                preserveViewport: true,
                suppressMarkers: true,
                polylineOptions: {
                  strokeColor: route57.color,
                  strokeOpacity: 0.8,
                  strokeWeight: 5
                }
              });
              routeObjectsRef.current.push(renderer);
              directionsService.route({
                origin: originAddress,
                destination: originAddress,
                waypoints: [{
                  location: stop.address,
                  stopover: true
                }],
                optimizeWaypoints: false,
                travelMode: google.maps.TravelMode.DRIVING
              }, (result, routeStatus) => {
                var _a2, _b;
                if (cancelled) {
                  resolve();
                  return;
                }
                if (result && routeStatus === "OK") {
                  renderer.setDirections(result);
                  const legs = ((_b = (_a2 = result.routes) == null ? void 0 : _a2[0]) == null ? void 0 : _b.legs) || [];
                  const outboundLeg = legs[0];
                  const returnLeg = legs[1];
                  if (outboundLeg == null ? void 0 : outboundLeg.start_location) {
                    bounds.extend(outboundLeg.start_location);
                    if (!yardMarkerAdded) {
                      const yardMarker = new google.maps.Marker({
                        map,
                        position: outboundLeg.start_location,
                        label: "Y",
                        title: "Green Hills Supply"
                      });
                      routeObjectsRef.current.push(yardMarker);
                      yardMarkerAdded = true;
                    }
                  }
                  if (outboundLeg == null ? void 0 : outboundLeg.end_location) {
                    bounds.extend(outboundLeg.end_location);
                    const marker = new google.maps.Marker({
                      map,
                      position: outboundLeg.end_location,
                      label: String(stopIndex + 1),
                      title: `${route57.code} ${stop.label} · ${stop.customer} · ${stop.load}`
                    });
                    routeObjectsRef.current.push(marker);
                  }
                  if (returnLeg == null ? void 0 : returnLeg.end_location) bounds.extend(returnLeg.end_location);
                } else {
                  console.warn("[MONITOR MAP ROUTE ERROR]", route57.code, stop.address, routeStatus);
                }
                resolve();
              });
            });
          }
        }
        if (!savedViewport && !bounds.isEmpty()) map.fitBounds(bounds);
        setStatus(routePlan.length ? "" : "No visible route stops for this view.");
      } catch (error) {
        console.warn("[MONITOR MAP ERROR]", error);
        setStatus("Google map unavailable.");
      }
    }
    void drawRoutes();
    return () => {
      cancelled = true;
      routeObjectsRef.current.forEach((object) => {
        var _a2;
        return (_a2 = object.setMap) == null ? void 0 : _a2.call(object, null);
      });
      routeObjectsRef.current = [];
    };
  }, [googleMapsApiKey, originAddress, routePlanKey]);
  useEffect(() => {
    let cancelled = false;
    async function drawDriverMarkers() {
      if (!googleMapsApiKey || !mapInstanceRef.current) return;
      await loadMonitorGoogleMaps(googleMapsApiKey);
      if (cancelled || !mapInstanceRef.current) return;
      const google = window.google;
      const map = mapInstanceRef.current;
      driverMarkersRef.current.forEach((marker) => {
        var _a2;
        return (_a2 = marker.setMap) == null ? void 0 : _a2.call(marker, null);
      });
      driverMarkersRef.current = [];
      for (const location of driverLocations.filter((item) => item.routeId && visibleRouteIds.has(item.routeId) && Number.isFinite(item.latitude) && Number.isFinite(item.longitude))) {
        const routeColor = routeColorById.get(location.routeId || "") || "#22c55e";
        const marker = new google.maps.Marker({
          map,
          position: {
            lat: location.latitude,
            lng: location.longitude
          },
          title: `${location.truck || "Driver"} · ${location.driverName || "Driver"}`,
          icon: {
            path: google.maps.SymbolPath.FORWARD_CLOSED_ARROW,
            scale: 7,
            fillColor: routeColor,
            fillOpacity: 1,
            strokeColor: "#020617",
            strokeWeight: 2,
            rotation: Number.isFinite(Number(location.heading)) ? Number(location.heading) : 0
          },
          label: {
            text: location.truck || "DRV",
            color: "#020617",
            fontWeight: "900",
            fontSize: "11px"
          }
        });
        driverMarkersRef.current.push(marker);
      }
    }
    void drawDriverMarkers();
    return () => {
      cancelled = true;
      driverMarkersRef.current.forEach((marker) => {
        var _a2;
        return (_a2 = marker.setMap) == null ? void 0 : _a2.call(marker, null);
      });
      driverMarkersRef.current = [];
    };
  }, [driverLocations, googleMapsApiKey, routeColorById, visibleRouteIds]);
  return /* @__PURE__ */ jsxs("section", {
    style: styles$9.mapPanel,
    children: [/* @__PURE__ */ jsx("div", {
      ref: mapRef,
      style: styles$9.mapCanvas
    }), status ? /* @__PURE__ */ jsx("div", {
      style: styles$9.mapStatus,
      children: status
    }) : null]
  });
}
const monitor = UNSAFE_withComponentProps(function DispatchMonitorPage() {
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const location = useLocation();
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const orders = (actionData == null ? void 0 : actionData.orders) ?? loaderData.orders ?? [];
  const baseRoutes = (actionData == null ? void 0 : actionData.routes) ?? loaderData.routes ?? [];
  const initialDriverLocations = (actionData == null ? void 0 : actionData.driverLocations) ?? loaderData.driverLocations ?? [];
  const [driverLocations, setDriverLocations] = useState(initialDriverLocations);
  const [filterByDate, setFilterByDate] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(MONITOR_DATE_FILTER_KEY) === "1";
  });
  const [selectedDateKey] = useState(() => {
    if (typeof window === "undefined") return dateKey$4(/* @__PURE__ */ new Date());
    return window.localStorage.getItem(MONITOR_SELECTED_DATE_KEY) || dateKey$4(/* @__PURE__ */ new Date());
  });
  const [hiddenRouteIds, setHiddenRouteIds] = useState(() => readStorageJson(MONITOR_HIDDEN_ROUTES_KEY, []));
  const [navCollapsed, setNavCollapsed] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(DISPATCH_NAV_COLLAPSED_KEY$2) === "1";
  });
  const [compactRoutes, setCompactRoutes] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(MONITOR_COMPACT_ROUTES_KEY) === "1";
  });
  const currentUser = (actionData == null ? void 0 : actionData.currentUser) ?? loaderData.currentUser ?? null;
  const googleMapsApiKey = (actionData == null ? void 0 : actionData.googleMapsApiKey) ?? loaderData.googleMapsApiKey ?? "";
  const mapOriginAddress = (actionData == null ? void 0 : actionData.mapOriginAddress) ?? loaderData.mapOriginAddress ?? "";
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const classicHref = isEmbeddedRoute ? "/app/classic" : "/classic";
  const monitorHref = isEmbeddedRoute ? "/app/monitor" : "/monitor";
  const calendarHref = isEmbeddedRoute ? "/app/calendar" : "/calendar";
  const allotmentHref = isEmbeddedRoute ? "/app/allotment" : "/allotment";
  const dispatchHref = isEmbeddedRoute ? "/app/dispatch" : "/dispatch";
  const quoteHref = isEmbeddedRoute ? "/app/custom-quote" : "/custom-quote";
  const driverHref = isEmbeddedRoute ? "/app/dispatch/driver" : "/dispatch/driver";
  const mobileHref = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  const dispatchViewHref = (view) => `${dispatchHref}?view=${view}`;
  const logoutHref = currentUser ? "/login?logout=1" : `${dispatchHref}?logout=1`;
  const canAccess = (permission) => {
    var _a2;
    return !currentUser || ((_a2 = currentUser.permissions) == null ? void 0 : _a2.includes(permission));
  };
  useEffect(() => {
    setDriverLocations(initialDriverLocations);
  }, [initialDriverLocations]);
  useEffect(() => {
    let cancelled = false;
    async function loadDriverLocations() {
      try {
        const response = await fetch("/api/dispatch-driver-location");
        const result = await response.json().catch(() => null);
        if (!cancelled && response.ok && (result == null ? void 0 : result.ok) !== false) {
          setDriverLocations(result.locations || []);
        }
      } catch {
      }
    }
    const timer = window.setInterval(loadDriverLocations, 6e4);
    void loadDriverLocations();
    return () => {
      cancelled = true;
      window.clearInterval(timer);
    };
  }, []);
  const routes2 = useMemo(() => baseRoutes.map((route57) => {
    const routeOrders = orders.filter((order) => order.assignedRouteId === route57.id && order.status !== "delivered" && order.deliveryStatus !== "delivered").sort((a, b) => Number(a.stopSequence || 9999) - Number(b.stopSequence || 9999));
    return {
      ...route57,
      orders: routeOrders
    };
  }), [baseRoutes, orders]);
  const activeRoutes = routes2.filter((route57) => route57.orders.length);
  const visibleRouteOrders = activeRoutes.map((route57) => {
    const routeOrders = filterByDate ? route57.orders.filter((order) => dateKey$4(parseRequestedDate$4(order.requestedWindow)) === selectedDateKey) : route57.orders;
    return {
      ...route57,
      orders: routeOrders,
      totalTravelMinutes: routeOrders.reduce((sum, order) => sum + getTravelMinutes$3(order), 0)
    };
  });
  const visibleRouteOrderCount = visibleRouteOrders.reduce((sum, route57) => sum + route57.orders.length, 0);
  function toggleNavCollapsed() {
    setNavCollapsed((current) => {
      const next = !current;
      window.localStorage.setItem(DISPATCH_NAV_COLLAPSED_KEY$2, next ? "1" : "0");
      return next;
    });
  }
  function toggleDateFilter() {
    setFilterByDate((current) => {
      const next = !current;
      window.localStorage.setItem(MONITOR_DATE_FILTER_KEY, next ? "1" : "0");
      return next;
    });
  }
  function toggleCompactRoutes() {
    setCompactRoutes((current) => {
      const next = !current;
      window.localStorage.setItem(MONITOR_COMPACT_ROUTES_KEY, next ? "1" : "0");
      return next;
    });
  }
  function toggleRoute(routeId) {
    setHiddenRouteIds((current) => {
      const next = current.includes(routeId) ? current.filter((id) => id !== routeId) : [...current, routeId];
      window.localStorage.setItem(MONITOR_HIDDEN_ROUTES_KEY, JSON.stringify(next));
      return next;
    });
  }
  function resetMapView() {
    window.localStorage.removeItem(MONITOR_VIEWPORT_KEY);
    window.location.reload();
  }
  if (!allowed) {
    return /* @__PURE__ */ jsx("main", {
      style: styles$9.loginPage,
      children: /* @__PURE__ */ jsxs("section", {
        style: styles$9.loginCard,
        children: [/* @__PURE__ */ jsx("h1", {
          style: styles$9.loginTitle,
          children: "Dispatch Monitor"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$9.muted,
          children: "Open Dispatch first, then return to this monitor page."
        }), /* @__PURE__ */ jsx(Link, {
          to: dispatchHref,
          style: styles$9.primaryLink,
          children: "Open Dispatch"
        })]
      })
    });
  }
  return /* @__PURE__ */ jsxs("main", {
    style: styles$9.page,
    children: [/* @__PURE__ */ jsxs("aside", {
      style: {
        ...styles$9.sideRail,
        width: navCollapsed ? 56 : 250,
        padding: navCollapsed ? "12px 8px" : "16px 14px"
      },
      children: [/* @__PURE__ */ jsx("button", {
        type: "button",
        onClick: toggleNavCollapsed,
        style: styles$9.navToggle,
        children: navCollapsed ? ">" : "<"
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$9.brand,
        children: [/* @__PURE__ */ jsx("img", {
          src: "/green-hills-logo.png",
          alt: "Green Hills Supply",
          style: styles$9.logo
        }), /* @__PURE__ */ jsxs("div", {
          style: navCollapsed ? styles$9.hidden : void 0,
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$9.brandTitle,
            children: "Contractor"
          }), /* @__PURE__ */ jsx("div", {
            style: styles$9.brandSub,
            children: "Monitor"
          })]
        })]
      }), /* @__PURE__ */ jsxs("nav", {
        style: navCollapsed ? styles$9.hidden : styles$9.nav,
        children: [/* @__PURE__ */ jsx(Link, {
          to: classicHref,
          style: styles$9.navLink,
          children: "Classic"
        }), /* @__PURE__ */ jsx(Link, {
          to: monitorHref,
          style: styles$9.navLinkActive,
          children: "Monitor"
        }), /* @__PURE__ */ jsx(Link, {
          to: calendarHref,
          style: styles$9.navLink,
          children: "Calendar"
        }), /* @__PURE__ */ jsx(Link, {
          to: allotmentHref,
          style: styles$9.navLink,
          children: "Allotment"
        }), canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("orders"),
          style: styles$9.navLink,
          children: "Orders"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("scheduled"),
          style: styles$9.navLink,
          children: "Scheduled"
        }), canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("routes"),
          style: styles$9.navLink,
          children: "Routes"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("delivered"),
          style: styles$9.navLink,
          children: "Delivered"
        }), /* @__PURE__ */ jsx("a", {
          href: "https://www.ghstickets.info",
          style: styles$9.navLink,
          children: "Ticket Creator"
        })]
      }), /* @__PURE__ */ jsx("div", {
        style: {
          flex: 1
        }
      }), /* @__PURE__ */ jsxs("div", {
        style: navCollapsed ? styles$9.hidden : styles$9.footerNav,
        children: [canAccess("driver") ? /* @__PURE__ */ jsx(Link, {
          to: driverHref,
          style: styles$9.utility,
          children: "Driver Route"
        }) : null, canAccess("quoteTool") ? /* @__PURE__ */ jsx(Link, {
          to: quoteHref,
          style: styles$9.utility,
          children: "Quote Tool"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: mobileHref,
          style: styles$9.utility,
          children: "Mobile"
        }), /* @__PURE__ */ jsx(Link, {
          to: logoutHref,
          style: styles$9.utility,
          children: "Log Out"
        })]
      })]
    }), /* @__PURE__ */ jsxs("section", {
      style: styles$9.monitorGrid,
      children: [/* @__PURE__ */ jsxs("section", {
        style: styles$9.orderPane,
        children: [/* @__PURE__ */ jsxs("header", {
          style: styles$9.header,
          children: [/* @__PURE__ */ jsx("p", {
            style: styles$9.kicker,
            children: "Dispatch Monitor"
          }), /* @__PURE__ */ jsx("h1", {
            style: styles$9.title,
            children: "Orders by Truck"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$9.muted,
            children: "Map zoom, position, route visibility, and filters stay saved after refresh."
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$9.controls,
          children: [/* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: toggleDateFilter,
            style: filterByDate ? styles$9.activeButton : styles$9.ghostButton,
            children: filterByDate ? "Showing selected date" : "Showing all active routes"
          }), /* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: toggleCompactRoutes,
            style: compactRoutes ? styles$9.activeButton : styles$9.ghostButton,
            children: compactRoutes ? "Compact on" : "Compact routes"
          }), /* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: resetMapView,
            style: styles$9.ghostButton,
            children: "Reset map view"
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$9.summaryBar,
          children: [/* @__PURE__ */ jsxs("strong", {
            children: [visibleRouteOrderCount, " active load", visibleRouteOrderCount === 1 ? "" : "s"]
          }), filterByDate ? /* @__PURE__ */ jsx("span", {
            children: formatDateLabel$3(parseRequestedDate$4(selectedDateKey))
          }) : /* @__PURE__ */ jsx("span", {
            children: "All scheduled route loads"
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$9.truckList,
          children: [visibleRouteOrders.map((route57) => /* @__PURE__ */ jsxs("section", {
            style: compactRoutes ? styles$9.truckCardCompact : styles$9.truckCard,
            children: [/* @__PURE__ */ jsxs("div", {
              style: compactRoutes ? styles$9.truckHeaderCompact : styles$9.truckHeader,
              children: [/* @__PURE__ */ jsx("span", {
                style: {
                  ...styles$9.routeDot,
                  background: route57.color || "#f97316"
                }
              }), /* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsxs("strong", {
                  children: [route57.code, " · ", route57.truck || "No truck"]
                }), /* @__PURE__ */ jsxs("small", {
                  children: [route57.driver || "No driver", " · ", route57.orders.length, " stop", route57.orders.length === 1 ? "" : "s", " · ", formatTravel(route57.totalTravelMinutes)]
                })]
              })]
            }), /* @__PURE__ */ jsxs("div", {
              style: compactRoutes ? styles$9.truckOrdersCompact : styles$9.truckOrders,
              children: [route57.orders.map((order, index) => /* @__PURE__ */ jsxs(Link, {
                to: `${dispatchHref}?view=orders&order=${encodeURIComponent(order.id)}&returnTo=${encodeURIComponent(monitorHref)}`,
                style: compactRoutes ? styles$9.orderCardCompact : styles$9.orderCard,
                children: [/* @__PURE__ */ jsx("span", {
                  style: compactRoutes ? styles$9.stopBadgeCompact : styles$9.stopBadge,
                  children: order.stopSequence || index + 1
                }), /* @__PURE__ */ jsxs("div", {
                  style: compactRoutes ? styles$9.orderTextCompact : styles$9.orderText,
                  children: [/* @__PURE__ */ jsxs("strong", {
                    children: [getOrderNumber$4(order), " ", order.customer]
                  }), compactRoutes ? /* @__PURE__ */ jsx("span", {
                    children: getLoadLabel$3(order)
                  }) : /* @__PURE__ */ jsxs(Fragment, {
                    children: [/* @__PURE__ */ jsx("span", {
                      children: getLoadLabel$3(order)
                    }), /* @__PURE__ */ jsx("small", {
                      children: getOrderAddress$3(order)
                    })]
                  })]
                })]
              }, order.id)), !route57.orders.length ? /* @__PURE__ */ jsx("div", {
                style: styles$9.empty,
                children: "No loads for this filter."
              }) : null]
            })]
          }, route57.id)), !visibleRouteOrders.length ? /* @__PURE__ */ jsx("div", {
            style: styles$9.empty,
            children: "No active routes yet."
          }) : null]
        })]
      }), /* @__PURE__ */ jsxs("section", {
        style: styles$9.mapSide,
        children: [/* @__PURE__ */ jsx("div", {
          style: styles$9.routeLegend,
          children: activeRoutes.map((route57) => {
            const hidden = hiddenRouteIds.includes(route57.id);
            return /* @__PURE__ */ jsxs("button", {
              type: "button",
              onClick: () => toggleRoute(route57.id),
              style: hidden ? styles$9.routeButtonHidden : styles$9.routeButton,
              children: [/* @__PURE__ */ jsx("span", {
                style: {
                  ...styles$9.routeDot,
                  background: route57.color || "#f97316"
                }
              }), route57.code, " ", route57.truck || ""]
            }, route57.id);
          })
        }), /* @__PURE__ */ jsx(MonitorMap, {
          googleMapsApiKey,
          originAddress: mapOriginAddress,
          routes: routes2,
          driverLocations,
          hiddenRouteIds,
          selectedDateKey,
          filterByDate
        })]
      })]
    })]
  });
});
const navBase$2 = {
  borderRadius: 8,
  color: "#e5e7eb",
  fontWeight: 800,
  padding: "14px 16px",
  textDecoration: "none"
};
const styles$9 = {
  page: {
    minHeight: "100vh",
    display: "flex",
    background: "#020617",
    color: "#f8fafc",
    fontFamily: "Verdana, Geneva, Tahoma, sans-serif",
    fontSize: 12
  },
  loginPage: {
    minHeight: "100vh",
    display: "grid",
    placeItems: "center",
    background: "#020617",
    color: "#f8fafc",
    padding: 24
  },
  loginCard: {
    width: "100%",
    maxWidth: 460,
    padding: 28,
    borderRadius: 14,
    border: "1px solid #334155",
    background: "#0f172a"
  },
  loginTitle: {
    margin: "0 0 8px",
    fontSize: 30
  },
  primaryLink: {
    display: "inline-flex",
    marginTop: 18,
    padding: "12px 18px",
    borderRadius: 999,
    background: "#ff7a1a",
    color: "#fff",
    fontWeight: 900,
    textDecoration: "none"
  },
  sideRail: {
    minHeight: "100vh",
    position: "sticky",
    top: 0,
    display: "flex",
    flexDirection: "column",
    gap: 12,
    borderRight: "1px solid #1e293b",
    background: "#020617",
    overflow: "hidden",
    transition: "width 160ms ease, padding 160ms ease"
  },
  navToggle: {
    width: 34,
    height: 30,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: 8,
    border: "1px solid #334155",
    background: "#1e293b",
    color: "#ff7a1a",
    fontWeight: 900,
    cursor: "pointer"
  },
  brand: {
    display: "flex",
    alignItems: "center",
    gap: 12
  },
  logo: {
    width: 52,
    height: 52,
    objectFit: "contain"
  },
  brandTitle: {
    fontSize: 18,
    fontWeight: 900
  },
  brandSub: {
    color: "#cbd5e1",
    fontWeight: 800
  },
  hidden: {
    display: "none"
  },
  nav: {
    display: "grid",
    gap: 8
  },
  navLink: navBase$2,
  navLinkActive: {
    ...navBase$2,
    background: "#1e293b",
    color: "#ff7a1a"
  },
  footerNav: {
    display: "grid",
    gap: 8
  },
  utility: {
    ...navBase$2,
    border: "1px solid #334155",
    padding: "11px 14px"
  },
  monitorGrid: {
    flex: 1,
    minWidth: 0,
    height: "100vh",
    display: "grid",
    gridTemplateColumns: "420px minmax(0, 1fr)"
  },
  orderPane: {
    minHeight: 0,
    display: "grid",
    gridTemplateRows: "auto auto auto minmax(0, 1fr)",
    gap: 12,
    padding: 14,
    borderRight: "1px solid #334155",
    overflow: "hidden"
  },
  header: {
    padding: 16,
    border: "1px solid #334155",
    background: "#0f172a"
  },
  kicker: {
    margin: 0,
    color: "#ff7a1a",
    fontSize: 11,
    fontWeight: 900,
    letterSpacing: 2,
    textTransform: "uppercase"
  },
  title: {
    margin: "4px 0 4px",
    fontSize: 28
  },
  muted: {
    margin: 0,
    color: "#94a3b8"
  },
  controls: {
    display: "flex",
    gap: 8,
    flexWrap: "wrap"
  },
  ghostButton: {
    minHeight: 34,
    padding: "0 13px",
    borderRadius: 999,
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#e5e7eb",
    fontWeight: 900,
    cursor: "pointer"
  },
  activeButton: {
    minHeight: 34,
    padding: "0 13px",
    borderRadius: 999,
    border: "1px solid #ff7a1a",
    background: "#ff7a1a",
    color: "#fff",
    fontWeight: 900,
    cursor: "pointer"
  },
  summaryBar: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 12,
    padding: "10px 12px",
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#e5e7eb"
  },
  truckList: {
    minHeight: 0,
    display: "grid",
    alignContent: "start",
    gap: 10,
    overflow: "auto",
    paddingRight: 4
  },
  truckCard: {
    border: "1px solid #334155",
    background: "#0f172a",
    overflow: "hidden"
  },
  truckCardCompact: {
    border: "1px solid #334155",
    background: "#0f172a",
    overflow: "hidden"
  },
  truckHeader: {
    display: "grid",
    gridTemplateColumns: "12px minmax(0, 1fr)",
    gap: 10,
    alignItems: "center",
    padding: 12,
    borderBottom: "1px solid #334155"
  },
  truckHeaderCompact: {
    display: "grid",
    gridTemplateColumns: "10px minmax(0, 1fr)",
    gap: 8,
    alignItems: "center",
    padding: "6px 8px",
    borderBottom: "1px solid #334155"
  },
  truckOrders: {
    display: "grid",
    gap: 8,
    padding: 10
  },
  truckOrdersCompact: {
    display: "grid",
    gap: 4,
    padding: 6
  },
  orderCard: {
    display: "grid",
    gridTemplateColumns: "30px minmax(0, 1fr)",
    alignItems: "start",
    gap: 4,
    padding: 10,
    borderRadius: 10,
    border: "1px solid #334155",
    background: "#111827",
    color: "#f8fafc",
    textDecoration: "none"
  },
  orderCardCompact: {
    display: "grid",
    gridTemplateColumns: "24px minmax(0, 1fr)",
    alignItems: "center",
    gap: 5,
    padding: "4px 6px",
    borderRadius: 7,
    border: "1px solid #263449",
    background: "#111827",
    color: "#f8fafc",
    textDecoration: "none",
    fontSize: 11
  },
  stopBadge: {
    width: 24,
    height: 24,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: "50%",
    background: "#1e293b",
    color: "#38bdf8",
    fontWeight: 900
  },
  stopBadgeCompact: {
    width: 20,
    height: 20,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: "50%",
    background: "#1e293b",
    color: "#38bdf8",
    fontSize: 10,
    fontWeight: 900
  },
  orderText: {
    minWidth: 0,
    display: "grid",
    gap: 3
  },
  orderTextCompact: {
    minWidth: 0,
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) auto",
    gap: 8,
    alignItems: "center"
  },
  empty: {
    color: "#94a3b8",
    padding: 12
  },
  mapSide: {
    minWidth: 0,
    display: "grid",
    gridTemplateRows: "auto minmax(0, 1fr)",
    background: "#020617"
  },
  routeLegend: {
    display: "flex",
    gap: 8,
    alignItems: "center",
    flexWrap: "wrap",
    padding: 10,
    borderBottom: "1px solid #334155",
    background: "#0f172a"
  },
  routeButton: {
    minHeight: 32,
    display: "inline-flex",
    alignItems: "center",
    gap: 7,
    borderRadius: 999,
    border: "1px solid #334155",
    background: "#111827",
    color: "#f8fafc",
    fontWeight: 900,
    cursor: "pointer"
  },
  routeButtonHidden: {
    minHeight: 32,
    display: "inline-flex",
    alignItems: "center",
    gap: 7,
    borderRadius: 999,
    border: "1px solid #1e293b",
    background: "#020617",
    color: "#64748b",
    fontWeight: 900,
    cursor: "pointer",
    opacity: 0.65
  },
  routeDot: {
    width: 10,
    height: 10,
    borderRadius: "50%"
  },
  mapPanel: {
    position: "relative",
    minHeight: 0
  },
  mapCanvas: {
    width: "100%",
    height: "100%"
  },
  mapStatus: {
    position: "absolute",
    top: 14,
    left: 14,
    zIndex: 2,
    padding: "9px 12px",
    borderRadius: 10,
    background: "rgba(15, 23, 42, 0.9)",
    border: "1px solid #334155",
    color: "#e5e7eb",
    fontWeight: 900
  }
};
const route7 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$n,
  default: monitor,
  loader: loader$l
}, Symbol.toStringTag, { value: "Module" }));
async function loader$k({
  request
}) {
  const currentUser = await requireUserPermission(request, "loader");
  const notifications = await listLoaderNotifications(currentUser, 30);
  return data({
    currentUser,
    notifications,
    supabaseUrl: process.env.SUPABASE_URL || "",
    supabaseAnonKey: process.env.SUPABASE_ANON_KEY || ""
  });
}
async function action$m({
  request
}) {
  const currentUser = await requireUserPermission(request, "loader");
  const form = await request.formData();
  const intent = String(form.get("intent") || "");
  if (intent === "mark-read") {
    await markLoaderNotificationRead(String(form.get("id") || ""), currentUser);
    const notifications = await listLoaderNotifications(currentUser, 30);
    return data({
      ok: true,
      message: "Marked loaded.",
      notifications
    });
  }
  if (intent === "clear-history") {
    await clearLoaderNotificationHistory(currentUser);
    const notifications = await listLoaderNotifications(currentUser, 30);
    return data({
      ok: true,
      message: "Loaded history cleared.",
      notifications
    });
  }
  return data({
    ok: false,
    message: "Unknown loader action."
  }, {
    status: 400
  });
}
function formatTime(value) {
  if (!value) return "";
  return new Date(value).toLocaleTimeString([], {
    hour: "numeric",
    minute: "2-digit"
  });
}
const loader_default = UNSAFE_withComponentProps(function LoaderPage() {
  var _a2;
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const [notifications, setNotifications] = useState((actionData == null ? void 0 : actionData.notifications) || loaderData.notifications || []);
  const lastSeenId = useRef(((_a2 = notifications[0]) == null ? void 0 : _a2.id) || "");
  const currentUser = loaderData.currentUser;
  const unread = notifications.filter((notification) => notification.status === "unread");
  const history = notifications.filter((notification) => notification.status === "read");
  const currentLoad = unread[0] || null;
  const canUseRealtime = Boolean(loaderData.supabaseUrl && loaderData.supabaseAnonKey);
  const [pushStatus, setPushStatus] = useState("Push alerts not enabled on this device.");
  const [pushBusy, setPushBusy] = useState(false);
  const [testPushBusy, setTestPushBusy] = useState(false);
  const pushSupported = typeof window !== "undefined" && "serviceWorker" in navigator && "PushManager" in window && "Notification" in window;
  useEffect(() => {
    if (actionData == null ? void 0 : actionData.notifications) setNotifications(actionData.notifications);
  }, [actionData]);
  useEffect(() => {
    async function refreshNotifications() {
      try {
        const response = await fetch("/api/loader-notifications");
        const result = await response.json();
        if (response.ok && (result == null ? void 0 : result.notifications)) {
          setNotifications(result.notifications);
        }
      } catch {
      }
    }
    const timer = window.setInterval(refreshNotifications, 1e4);
    return () => window.clearInterval(timer);
  }, []);
  useEffect(() => {
    if (!canUseRealtime) return;
    const client = createClient(loaderData.supabaseUrl, loaderData.supabaseAnonKey);
    const channel = client.channel("loader-notifications").on("postgres_changes", {
      event: "INSERT",
      schema: "public",
      table: "dispatch_notifications",
      filter: "target_role=eq.loader"
    }, () => {
      void fetch("/api/loader-notifications").then((response) => response.json()).then((result) => {
        if (result == null ? void 0 : result.notifications) setNotifications(result.notifications);
      }).catch(() => null);
    }).subscribe();
    return () => {
      void client.removeChannel(channel);
    };
  }, [canUseRealtime, loaderData.supabaseAnonKey, loaderData.supabaseUrl]);
  useEffect(() => {
    var _a3;
    const newestId = ((_a3 = notifications[0]) == null ? void 0 : _a3.id) || "";
    if (!newestId || newestId === lastSeenId.current) return;
    lastSeenId.current = newestId;
    document.title = "New load ready";
    window.setTimeout(() => {
      document.title = "Loader";
    }, 5e3);
  }, [notifications]);
  const connectionLabel = useMemo(() => canUseRealtime ? "Live + polling backup" : "Polling every 10 seconds", [canUseRealtime]);
  function urlBase64ToUint8Array(value) {
    const padding = "=".repeat((4 - value.length % 4) % 4);
    const base64 = `${value}${padding}`.replace(/-/g, "+").replace(/_/g, "/");
    const rawData = window.atob(base64);
    return Uint8Array.from([...rawData].map((char) => char.charCodeAt(0)));
  }
  async function enablePushAlerts() {
    if (!pushSupported) {
      setPushStatus("Push is not supported in this browser. On iPad, install the site to Home Screen first.");
      return;
    }
    setPushBusy(true);
    try {
      const keyResponse = await fetch("/api/loader-push-subscription");
      const keyResult = await keyResponse.json();
      if (!(keyResult == null ? void 0 : keyResult.configured) || !(keyResult == null ? void 0 : keyResult.publicKey)) {
        setPushStatus("Push is not configured yet. Add VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY to the server environment.");
        return;
      }
      const permission = await Notification.requestPermission();
      if (permission !== "granted") {
        setPushStatus("Push permission was not allowed on this device.");
        return;
      }
      const registration = await navigator.serviceWorker.register("/loader-push-sw.js");
      const existing = await registration.pushManager.getSubscription();
      const subscription = existing || await registration.pushManager.subscribe({
        userVisibleOnly: true,
        applicationServerKey: urlBase64ToUint8Array(keyResult.publicKey)
      });
      const response = await fetch("/api/loader-push-subscription", {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({
          intent: "subscribe",
          subscription: subscription.toJSON()
        })
      });
      const result = await response.json();
      setPushStatus((result == null ? void 0 : result.message) || "Push alerts enabled.");
    } catch (error) {
      setPushStatus(error instanceof Error ? error.message : "Unable to enable push alerts.");
    } finally {
      setPushBusy(false);
    }
  }
  async function sendTestPush() {
    setTestPushBusy(true);
    try {
      const response = await fetch("/api/loader-push-subscription", {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({
          intent: "test-push"
        })
      });
      const result = await response.json();
      setPushStatus((result == null ? void 0 : result.message) || "Test push requested.");
    } catch (error) {
      setPushStatus(error instanceof Error ? error.message : "Unable to send test push.");
    } finally {
      setTestPushBusy(false);
    }
  }
  return /* @__PURE__ */ jsx("main", {
    style: styles$8.page,
    children: /* @__PURE__ */ jsxs("section", {
      style: styles$8.shell,
      children: [/* @__PURE__ */ jsxs("header", {
        style: styles$8.header,
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("p", {
            style: styles$8.kicker,
            children: "Loader Mode"
          }), /* @__PURE__ */ jsx("h1", {
            style: styles$8.title,
            children: "What to Load Next"
          }), /* @__PURE__ */ jsxs("p", {
            style: styles$8.subtitle,
            children: ["Signed in as ", currentUser.name || currentUser.email, ". ", connectionLabel, "."]
          })]
        }), /* @__PURE__ */ jsxs("nav", {
          style: styles$8.nav,
          children: [/* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: enablePushAlerts,
            style: styles$8.navButton,
            disabled: pushBusy,
            children: pushBusy ? "Enabling..." : "Enable Push Alerts"
          }), /* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: sendTestPush,
            style: styles$8.navButton,
            disabled: testPushBusy,
            children: testPushBusy ? "Sending..." : "Test Push"
          }), /* @__PURE__ */ jsx(Link, {
            to: "/classic",
            style: styles$8.navButton,
            children: "Dispatch"
          }), /* @__PURE__ */ jsx(Link, {
            to: "/login?logout=1",
            style: styles$8.navButton,
            children: "Log Out"
          })]
        })]
      }), /* @__PURE__ */ jsx("div", {
        style: styles$8.pushStatus,
        children: pushStatus
      }), (actionData == null ? void 0 : actionData.message) ? /* @__PURE__ */ jsx("div", {
        style: actionData.ok ? styles$8.success : styles$8.error,
        children: actionData.message
      }) : null, /* @__PURE__ */ jsxs("section", {
        style: styles$8.hero,
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("p", {
            style: styles$8.heroLabel,
            children: "Current Load"
          }), /* @__PURE__ */ jsx("h2", {
            style: styles$8.heroTitle,
            children: (currentLoad == null ? void 0 : currentLoad.title) || "Waiting for dispatch"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$8.heroMessage,
            children: (currentLoad == null ? void 0 : currentLoad.message) || "When dispatch sends the next load, it will show up here."
          }), currentLoad ? /* @__PURE__ */ jsxs("p", {
            style: styles$8.heroTime,
            children: ["Sent ", formatTime(currentLoad.createdAt)]
          }) : null]
        }), currentLoad ? /* @__PURE__ */ jsxs(Form, {
          method: "post",
          style: styles$8.heroAction,
          children: [/* @__PURE__ */ jsx("input", {
            type: "hidden",
            name: "intent",
            value: "mark-read"
          }), /* @__PURE__ */ jsx("input", {
            type: "hidden",
            name: "id",
            value: currentLoad.id
          }), /* @__PURE__ */ jsx("button", {
            type: "submit",
            style: styles$8.primaryButton,
            children: "Mark Loaded"
          })]
        }) : null]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$8.grid,
        children: [/* @__PURE__ */ jsxs("section", {
          style: styles$8.panel,
          children: [/* @__PURE__ */ jsx("h2", {
            style: styles$8.panelTitle,
            children: "Unread"
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$8.list,
            children: [unread.map((notification) => /* @__PURE__ */ jsx(NotificationCard, {
              notification
            }, notification.id)), !unread.length ? /* @__PURE__ */ jsx("p", {
              style: styles$8.empty,
              children: "No waiting loads."
            }) : null]
          })]
        }), /* @__PURE__ */ jsxs("section", {
          style: styles$8.panel,
          children: [/* @__PURE__ */ jsxs("div", {
            style: styles$8.panelHeader,
            children: [/* @__PURE__ */ jsx("h2", {
              style: styles$8.panelTitle,
              children: "Loaded History"
            }), history.length ? /* @__PURE__ */ jsxs(Form, {
              method: "post",
              children: [/* @__PURE__ */ jsx("input", {
                type: "hidden",
                name: "intent",
                value: "clear-history"
              }), /* @__PURE__ */ jsx("button", {
                type: "submit",
                style: styles$8.clearButton,
                children: "Clear History"
              })]
            }) : null]
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$8.list,
            children: [history.slice(0, 10).map((notification) => /* @__PURE__ */ jsx(NotificationCard, {
              notification
            }, notification.id)), !history.length ? /* @__PURE__ */ jsx("p", {
              style: styles$8.empty,
              children: "Loaded confirmations will show here."
            }) : null]
          })]
        })]
      })]
    })
  });
});
function NotificationCard({
  notification
}) {
  return /* @__PURE__ */ jsxs("article", {
    style: styles$8.card,
    children: [/* @__PURE__ */ jsxs("div", {
      children: [/* @__PURE__ */ jsx("strong", {
        children: notification.title
      }), /* @__PURE__ */ jsx("p", {
        style: styles$8.cardMessage,
        children: notification.message
      }), /* @__PURE__ */ jsx("small", {
        children: formatTime(notification.createdAt)
      })]
    }), notification.status === "unread" ? /* @__PURE__ */ jsxs(Form, {
      method: "post",
      children: [/* @__PURE__ */ jsx("input", {
        type: "hidden",
        name: "intent",
        value: "mark-read"
      }), /* @__PURE__ */ jsx("input", {
        type: "hidden",
        name: "id",
        value: notification.id
      }), /* @__PURE__ */ jsx("button", {
        type: "submit",
        style: styles$8.smallButton,
        children: "Loaded"
      })]
    }) : /* @__PURE__ */ jsx("span", {
      style: styles$8.readPill,
      children: "Loaded"
    })]
  });
}
const styles$8 = {
  page: {
    minHeight: "100vh",
    background: "#020617",
    color: "#e5e7eb",
    fontFamily: "'Arial Rounded MT Bold', Arial, sans-serif",
    padding: 20
  },
  shell: {
    maxWidth: 1180,
    margin: "0 auto",
    display: "grid",
    gap: 18
  },
  header: {
    display: "flex",
    justifyContent: "space-between",
    gap: 20,
    alignItems: "center",
    background: "#0f172a",
    border: "1px solid #1e293b",
    borderRadius: 22,
    padding: 24
  },
  kicker: {
    margin: 0,
    color: "#38bdf8",
    textTransform: "uppercase",
    letterSpacing: 3,
    fontSize: 13
  },
  title: {
    margin: "6px 0",
    fontSize: 38
  },
  subtitle: {
    margin: 0,
    color: "#94a3b8"
  },
  nav: {
    display: "flex",
    gap: 10,
    flexWrap: "wrap"
  },
  navButton: {
    color: "#e5e7eb",
    textDecoration: "none",
    border: "1px solid #334155",
    borderRadius: 999,
    padding: "10px 14px",
    background: "#020617",
    fontWeight: 900
  },
  pushStatus: {
    border: "1px solid #1e293b",
    borderRadius: 14,
    padding: 12,
    background: "#0f172a",
    color: "#cbd5e1"
  },
  success: {
    background: "#052e16",
    border: "1px solid #16a34a",
    borderRadius: 14,
    padding: 12
  },
  error: {
    background: "#450a0a",
    border: "1px solid #dc2626",
    borderRadius: 14,
    padding: 12
  },
  hero: {
    display: "flex",
    justifyContent: "space-between",
    gap: 22,
    alignItems: "center",
    background: "linear-gradient(135deg, #0ea5e9, #22c55e)",
    color: "#00111d",
    borderRadius: 28,
    padding: 30,
    boxShadow: "0 28px 80px rgba(14, 165, 233, 0.18)"
  },
  heroLabel: {
    margin: 0,
    textTransform: "uppercase",
    letterSpacing: 3,
    fontSize: 13,
    opacity: 0.72
  },
  heroTitle: {
    margin: "8px 0",
    fontSize: 46
  },
  heroMessage: {
    margin: 0,
    fontSize: 23,
    lineHeight: 1.3,
    whiteSpace: "pre-wrap"
  },
  heroTime: {
    marginBottom: 0,
    opacity: 0.75
  },
  heroAction: {
    minWidth: 220
  },
  primaryButton: {
    width: "100%",
    border: 0,
    borderRadius: 18,
    padding: "18px 22px",
    fontWeight: 900,
    fontSize: 18,
    background: "#020617",
    color: "#fff"
  },
  grid: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: 18
  },
  panel: {
    background: "#0f172a",
    border: "1px solid #1e293b",
    borderRadius: 22,
    padding: 20
  },
  panelHeader: {
    display: "flex",
    justifyContent: "space-between",
    gap: 12,
    alignItems: "center",
    marginBottom: 14
  },
  panelTitle: {
    marginTop: 0
  },
  clearButton: {
    border: "1px solid #7f1d1d",
    borderRadius: 999,
    padding: "9px 12px",
    fontWeight: 900,
    background: "#450a0a",
    color: "#fecaca"
  },
  list: {
    display: "grid",
    gap: 12
  },
  card: {
    display: "flex",
    justifyContent: "space-between",
    gap: 12,
    alignItems: "center",
    background: "#020617",
    border: "1px solid #334155",
    borderRadius: 18,
    padding: 16
  },
  cardMessage: {
    whiteSpace: "pre-wrap",
    lineHeight: 1.35
  },
  smallButton: {
    border: 0,
    borderRadius: 12,
    padding: "10px 14px",
    fontWeight: 900,
    background: "#22c55e",
    color: "#052e16"
  },
  readPill: {
    borderRadius: 999,
    padding: "8px 12px",
    background: "#052e16",
    color: "#86efac",
    fontSize: 12,
    fontWeight: 900,
    textTransform: "uppercase"
  },
  empty: {
    color: "#94a3b8"
  }
};
const route8 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$m,
  default: loader_default,
  loader: loader$k
}, Symbol.toStringTag, { value: "Module" }));
const loader$j = loader$o;
const action$l = action$p;
const DISPATCH_NAV_COLLAPSED_KEY$1 = "dispatchNavCollapsed";
const monthNames$1 = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
function parseRequestedDate$3(value) {
  var _a2;
  const trimmed = String(value || "").trim();
  if (!trimmed || /needs scheduling|unavailable|unknown/i.test(trimmed)) return null;
  const today = /* @__PURE__ */ new Date();
  if (/\btoday\b/i.test(trimmed)) {
    return new Date(today.getFullYear(), today.getMonth(), today.getDate());
  }
  if (/\btomorrow\b/i.test(trimmed)) {
    const tomorrow = new Date(today.getFullYear(), today.getMonth(), today.getDate());
    tomorrow.setDate(tomorrow.getDate() + 1);
    return tomorrow;
  }
  const isoDate = trimmed.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (isoDate) {
    return new Date(Number(isoDate[1]), Number(isoDate[2]) - 1, Number(isoDate[3]));
  }
  const slashDate = trimmed.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return new Date(year, Number(slashDate[1]) - 1, Number(slashDate[2]));
  }
  const monthDate = (_a2 = trimmed.match(/\b(?:jan(?:uary)?|feb(?:ruary)?|mar(?:ch)?|apr(?:il)?|may|jun(?:e)?|jul(?:y)?|aug(?:ust)?|sep(?:t(?:ember)?)?|oct(?:ober)?|nov(?:ember)?|dec(?:ember)?)\.?\s+\d{1,2}(?:,\s*\d{4})?/i)) == null ? void 0 : _a2[0];
  if (monthDate) {
    const parsed2 = new Date(/\d{4}/.test(monthDate) ? monthDate : `${monthDate}, ${today.getFullYear()}`);
    if (!Number.isNaN(parsed2.getTime())) {
      return new Date(parsed2.getFullYear(), parsed2.getMonth(), parsed2.getDate());
    }
  }
  const parsed = new Date(trimmed);
  if (Number.isNaN(parsed.getTime())) return null;
  return new Date(parsed.getFullYear(), parsed.getMonth(), parsed.getDate());
}
function dateKey$3(date) {
  if (!date) return "unscheduled";
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function startOfWeek(date) {
  const weekStart = new Date(date.getFullYear(), date.getMonth(), date.getDate());
  weekStart.setDate(weekStart.getDate() - weekStart.getDay());
  return weekStart;
}
function formatDateLabel$2(date) {
  if (!date) return "Needs scheduling";
  return date.toLocaleDateString("en-US", {
    weekday: "short",
    month: "short",
    day: "numeric",
    year: "numeric"
  });
}
function getOrderNumber$3(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function getLoadLabel$2(order) {
  return [order.quantity, order.unit, order.material].filter(Boolean).join(" ");
}
function getTravelMinutes$2(order) {
  const minutes = Number(order.travelMinutes || 0);
  return Number.isFinite(minutes) && minutes > 0 ? minutes : 0;
}
function formatTravelMinutes$2(minutes) {
  const rounded = Math.round(minutes);
  if (!rounded) return "0 min";
  if (rounded < 60) return `${rounded} min`;
  const hours = Math.floor(rounded / 60);
  const remainder = rounded % 60;
  return remainder ? `${hours} hr ${remainder} min` : `${hours} hr`;
}
function getOrderAddress$2(order) {
  return [order.address, order.city].filter(Boolean).join(", ");
}
function getOrderStatus$3(order) {
  if (order.status === "delivered" || order.deliveryStatus === "delivered") return "Delivered";
  if (order.status === "cancelled") return "Cancelled";
  if (order.assignedRouteId || order.status === "scheduled") return "Scheduled";
  if (order.status === "hold") return "On hold";
  return "New";
}
function shouldShowOnCalendar(order) {
  const status = getOrderStatus$3(order);
  return status !== "Delivered" && status !== "Cancelled";
}
function buildSearchText$1(order) {
  return [order.id, order.orderNumber, order.customer, order.contact, order.address, order.city, order.material, order.quantity, order.unit, order.requestedWindow, order.timePreference, order.notes, order.status].filter(Boolean).join(" ").toLowerCase();
}
const calendar = UNSAFE_withComponentProps(function DispatchCalendarPage() {
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const location = useLocation();
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const orders = (actionData == null ? void 0 : actionData.orders) ?? loaderData.orders ?? [];
  const currentUser = (actionData == null ? void 0 : actionData.currentUser) ?? loaderData.currentUser ?? null;
  const [viewMode, setViewMode] = useState("month");
  const [query, setQuery] = useState("");
  const [cursorMonth, setCursorMonth] = useState(() => /* @__PURE__ */ new Date());
  const [expandedDayKeys, setExpandedDayKeys] = useState([]);
  const [navCollapsed, setNavCollapsed] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(DISPATCH_NAV_COLLAPSED_KEY$1) === "1";
  });
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const classicHref = isEmbeddedRoute ? "/app/classic" : "/classic";
  const monitorHref = isEmbeddedRoute ? "/app/monitor" : "/monitor";
  const calendarHref = isEmbeddedRoute ? "/app/calendar" : "/calendar";
  const allotmentHref = isEmbeddedRoute ? "/app/allotment" : "/allotment";
  const dispatchHref = isEmbeddedRoute ? "/app/dispatch" : "/dispatch";
  const quoteHref = isEmbeddedRoute ? "/app/custom-quote" : "/custom-quote";
  const driverHref = isEmbeddedRoute ? "/app/dispatch/driver" : "/dispatch/driver";
  const mobileHref = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  const dispatchViewHref = (view) => `${dispatchHref}?view=${view}`;
  const editorHref = (orderId) => `${dispatchHref}?view=orders&order=${encodeURIComponent(orderId)}&returnTo=${encodeURIComponent(calendarHref)}`;
  const canAccess = (permission) => {
    var _a2;
    return !currentUser || ((_a2 = currentUser.permissions) == null ? void 0 : _a2.includes(permission));
  };
  const logoutHref = currentUser ? "/login?logout=1" : `${dispatchHref}?logout=1`;
  const loginHref = `/login?next=${encodeURIComponent(location.pathname + location.search)}`;
  function toggleNavCollapsed() {
    setNavCollapsed((current) => {
      const next = !current;
      try {
        window.localStorage.setItem(DISPATCH_NAV_COLLAPSED_KEY$1, next ? "1" : "0");
      } catch {
      }
      return next;
    });
  }
  const calendarOrders = useMemo(() => {
    const search = query.trim().toLowerCase();
    return orders.filter(shouldShowOnCalendar).filter((order) => !search || buildSearchText$1(order).includes(search)).map((order) => {
      const date = parseRequestedDate$3(order.requestedWindow);
      return {
        order,
        date,
        dateKey: dateKey$3(date)
      };
    }).sort((a, b) => {
      if (!a.date && !b.date) return getOrderNumber$3(a.order).localeCompare(getOrderNumber$3(b.order));
      if (!a.date) return 1;
      if (!b.date) return -1;
      return a.date.getTime() - b.date.getTime();
    });
  }, [orders, query]);
  const ordersByDay = useMemo(() => {
    const grouped = /* @__PURE__ */ new Map();
    for (const item of calendarOrders) {
      if (!item.date) continue;
      grouped.set(item.dateKey, [...grouped.get(item.dateKey) || [], item]);
    }
    return grouped;
  }, [calendarOrders]);
  const travelMinutesByDay = useMemo(() => {
    const grouped = /* @__PURE__ */ new Map();
    for (const item of calendarOrders) {
      if (!item.date) continue;
      grouped.set(item.dateKey, (grouped.get(item.dateKey) || 0) + getTravelMinutes$2(item.order));
    }
    return grouped;
  }, [calendarOrders]);
  const unscheduledOrders = calendarOrders.filter((item) => !item.date);
  const todayDate = /* @__PURE__ */ new Date();
  const isViewingCurrentMonth = cursorMonth.getFullYear() === todayDate.getFullYear() && cursorMonth.getMonth() === todayDate.getMonth();
  const monthStart = new Date(cursorMonth.getFullYear(), cursorMonth.getMonth(), 1);
  const gridStart = isViewingCurrentMonth ? new Date(todayDate.getFullYear(), todayDate.getMonth(), todayDate.getDate()) : new Date(monthStart);
  gridStart.setDate(gridStart.getDate() - gridStart.getDay());
  const calendarDays = Array.from({
    length: 42
  }, (_, index) => {
    const day = new Date(gridStart);
    day.setDate(gridStart.getDate() + index);
    return day;
  });
  const selectedDay = new Date(cursorMonth.getFullYear(), cursorMonth.getMonth(), cursorMonth.getDate());
  const selectedWeekStart = startOfWeek(selectedDay);
  const weekDays = Array.from({
    length: 7
  }, (_, index) => {
    const day = new Date(selectedWeekStart);
    day.setDate(selectedWeekStart.getDate() + index);
    return day;
  });
  const selectedDayTravelMinutes = travelMinutesByDay.get(dateKey$3(selectedDay)) || 0;
  const weekTravelMinutes = weekDays.reduce((sum, day) => sum + (travelMinutesByDay.get(dateKey$3(day)) || 0), 0);
  const monthLabel = viewMode === "day" ? selectedDay.toLocaleDateString("en-US", {
    weekday: "long",
    month: "long",
    day: "numeric",
    year: "numeric"
  }) : viewMode === "week" ? `${weekDays[0].toLocaleDateString("en-US", {
    month: "short",
    day: "numeric"
  })} - ${weekDays[6].toLocaleDateString("en-US", {
    month: "short",
    day: "numeric",
    year: "numeric"
  })}` : `${monthNames$1[cursorMonth.getMonth()]} ${cursorMonth.getFullYear()}`;
  function shiftMonth(amount) {
    setCursorMonth((current) => new Date(current.getFullYear(), current.getMonth() + amount, 1));
  }
  function shiftCalendar(amount) {
    if (viewMode === "day") {
      setCursorMonth((current) => new Date(current.getFullYear(), current.getMonth(), current.getDate() + amount));
      return;
    }
    if (viewMode === "week") {
      setCursorMonth((current) => new Date(current.getFullYear(), current.getMonth(), current.getDate() + amount * 7));
      return;
    }
    shiftMonth(amount);
  }
  function toggleDayExpanded(key) {
    setExpandedDayKeys((current) => current.includes(key) ? current.filter((entry2) => entry2 !== key) : [...current, key]);
  }
  function renderDayCell(day, options = {}) {
    const key = dateKey$3(day);
    const dayOrders = ordersByDay.get(key) || [];
    const dayTravelMinutes = travelMinutesByDay.get(key) || 0;
    const expanded = expandedDayKeys.includes(key) || options.tall;
    const visibleOrders = expanded ? dayOrders : dayOrders.slice(0, 5);
    const isToday = dateKey$3(day) === dateKey$3(/* @__PURE__ */ new Date());
    return /* @__PURE__ */ jsxs("div", {
      style: options.tall ? styles$7.dayLarge : options.muted ? styles$7.dayMuted : styles$7.day,
      children: [/* @__PURE__ */ jsxs("div", {
        style: styles$7.dayHeader,
        children: [/* @__PURE__ */ jsx("span", {
          style: isToday ? options.tall ? styles$7.todayLabelBadge : styles$7.todayBadge : void 0,
          children: options.tall ? day.toLocaleDateString("en-US", {
            weekday: "long",
            month: "long",
            day: "numeric"
          }) : day.getDate()
        }), dayOrders.length ? /* @__PURE__ */ jsx("span", {
          style: styles$7.dayCount,
          children: dayOrders.length
        }) : null]
      }), dayOrders.length ? /* @__PURE__ */ jsxs("div", {
        style: styles$7.dayTotal,
        children: ["Total delivery time: ", formatTravelMinutes$2(dayTravelMinutes)]
      }) : null, /* @__PURE__ */ jsxs("div", {
        style: styles$7.dayOrders,
        children: [visibleOrders.map(({
          order
        }) => /* @__PURE__ */ jsxs(Link, {
          to: editorHref(order.id),
          style: styles$7.orderChip,
          children: [/* @__PURE__ */ jsx("strong", {
            children: getOrderNumber$3(order)
          }), /* @__PURE__ */ jsx("span", {
            children: order.customer
          }), /* @__PURE__ */ jsx("small", {
            children: getLoadLabel$2(order)
          })]
        }, order.id)), !options.tall && dayOrders.length > 5 ? /* @__PURE__ */ jsx("button", {
          type: "button",
          onClick: () => toggleDayExpanded(key),
          style: styles$7.moreChip,
          children: expanded ? "Show less" : `+${dayOrders.length - 5} more`
        }) : null, !dayOrders.length ? /* @__PURE__ */ jsx("div", {
          style: styles$7.emptyDay,
          children: "No deliveries scheduled."
        }) : null]
      })]
    }, key);
  }
  if (!allowed) {
    return /* @__PURE__ */ jsx("main", {
      style: styles$7.loginPage,
      children: /* @__PURE__ */ jsxs("section", {
        style: styles$7.loginCard,
        children: [/* @__PURE__ */ jsx("h1", {
          style: styles$7.loginTitle,
          children: "Dispatch Calendar"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$7.subtle,
          children: "Sign in with your contractor user account to open the contractor calendar."
        }), /* @__PURE__ */ jsx("a", {
          href: loginHref,
          style: {
            ...styles$7.primaryButton,
            textDecoration: "none"
          },
          children: "Sign In"
        })]
      })
    });
  }
  return /* @__PURE__ */ jsxs("main", {
    style: styles$7.page,
    children: [/* @__PURE__ */ jsxs("aside", {
      style: {
        ...styles$7.sideRail,
        width: navCollapsed ? 56 : 250,
        padding: navCollapsed ? "12px 8px" : "16px 14px"
      },
      children: [/* @__PURE__ */ jsx("button", {
        type: "button",
        onClick: toggleNavCollapsed,
        style: styles$7.navToggle,
        title: navCollapsed ? "Open navigation" : "Close navigation",
        children: navCollapsed ? ">" : "<"
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$7.brand,
        children: [/* @__PURE__ */ jsx("img", {
          src: "/green-hills-logo.png",
          alt: "Green Hills Supply",
          style: styles$7.logo
        }), /* @__PURE__ */ jsxs("div", {
          style: navCollapsed ? styles$7.collapsedOnlyHidden : void 0,
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$7.brandTitle,
            children: "Contractor"
          }), /* @__PURE__ */ jsx("div", {
            style: styles$7.brandSub,
            children: "Calendar"
          })]
        })]
      }), /* @__PURE__ */ jsxs("nav", {
        style: navCollapsed ? styles$7.collapsedOnlyHidden : styles$7.nav,
        children: [/* @__PURE__ */ jsx(Link, {
          to: classicHref,
          style: styles$7.navLink,
          children: "Classic"
        }), /* @__PURE__ */ jsx(Link, {
          to: monitorHref,
          style: styles$7.navLink,
          children: "Monitor"
        }), /* @__PURE__ */ jsx(Link, {
          to: calendarHref,
          style: styles$7.navLinkActive,
          children: "Calendar"
        }), /* @__PURE__ */ jsx(Link, {
          to: allotmentHref,
          style: styles$7.navLink,
          children: "Allotment"
        }), canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("orders"),
          style: styles$7.navLink,
          children: "Orders"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("scheduled"),
          style: styles$7.navLink,
          children: "Scheduled"
        }), canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("routes"),
          style: styles$7.navLink,
          children: "Routes"
        }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("trucks"),
          style: styles$7.navLink,
          children: "Trucks"
        }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("employees"),
          style: styles$7.navLink,
          children: "Employees"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("delivered"),
          style: styles$7.navLink,
          children: "Delivered"
        }), /* @__PURE__ */ jsx("a", {
          href: "https://www.ghstickets.info",
          style: styles$7.navLink,
          children: "Ticket Creator"
        })]
      }), /* @__PURE__ */ jsx("div", {
        style: {
          flex: 1
        }
      }), /* @__PURE__ */ jsxs("div", {
        style: navCollapsed ? styles$7.collapsedOnlyHidden : styles$7.footerNav,
        children: [canAccess("driver") ? /* @__PURE__ */ jsx(Link, {
          to: driverHref,
          style: styles$7.utility,
          children: "Driver Route"
        }) : null, canAccess("quoteTool") ? /* @__PURE__ */ jsx(Link, {
          to: quoteHref,
          style: styles$7.utility,
          children: "Quote Tool"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: mobileHref,
          style: styles$7.utility,
          children: "Mobile"
        }), canAccess("manageUsers") ? /* @__PURE__ */ jsx(Link, {
          to: "/settings",
          style: styles$7.utility,
          children: "Settings"
        }) : null, currentUser ? /* @__PURE__ */ jsx(Link, {
          to: "/change-password",
          style: styles$7.utility,
          children: "Change Password"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: logoutHref,
          style: styles$7.utility,
          children: "Log Out"
        })]
      })]
    }), /* @__PURE__ */ jsxs("section", {
      style: styles$7.workspace,
      children: [/* @__PURE__ */ jsxs("header", {
        style: styles$7.header,
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("p", {
            style: styles$7.kicker,
            children: "Dispatch Calendar"
          }), /* @__PURE__ */ jsx("h1", {
            style: styles$7.title,
            children: "Requested Delivery Dates"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$7.subtle,
            children: "Orders are placed by requested date. Switch to List when you want a compact schedule."
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$7.headerActions,
          children: [/* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: () => setViewMode("day"),
            style: viewMode === "day" ? styles$7.toggleActive : styles$7.toggle,
            children: "Day"
          }), /* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: () => setViewMode("week"),
            style: viewMode === "week" ? styles$7.toggleActive : styles$7.toggle,
            children: "Week"
          }), /* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: () => setViewMode("month"),
            style: viewMode === "month" ? styles$7.toggleActive : styles$7.toggle,
            children: "Month"
          }), /* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: () => setViewMode("list"),
            style: viewMode === "list" ? styles$7.toggleActive : styles$7.toggle,
            children: "List"
          }), /* @__PURE__ */ jsx("a", {
            href: "/calendar.rss",
            target: "_blank",
            rel: "noreferrer",
            style: styles$7.rssButton,
            children: "RSS Feed"
          })]
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$7.toolbar,
        children: [/* @__PURE__ */ jsx("input", {
          value: query,
          onChange: (event) => setQuery(event.target.value),
          placeholder: "Search orders, customers, material, address, notes...",
          style: styles$7.search
        }), viewMode !== "list" ? /* @__PURE__ */ jsxs("div", {
          style: styles$7.monthControls,
          children: [/* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: () => shiftCalendar(-1),
            style: styles$7.smallButton,
            children: "Previous"
          }), /* @__PURE__ */ jsx("strong", {
            style: styles$7.monthLabel,
            children: monthLabel
          }), /* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: () => shiftCalendar(1),
            style: styles$7.smallButton,
            children: "Next"
          })]
        }) : null]
      }), viewMode === "month" ? /* @__PURE__ */ jsxs("section", {
        style: styles$7.calendarPanel,
        children: [/* @__PURE__ */ jsx("div", {
          style: styles$7.weekHeader,
          children: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"].map((day) => /* @__PURE__ */ jsx("div", {
            style: styles$7.weekDay,
            children: day
          }, day))
        }), /* @__PURE__ */ jsx("div", {
          style: styles$7.calendarGrid,
          children: calendarDays.map((day) => {
            const muted = day.getMonth() !== cursorMonth.getMonth();
            return renderDayCell(day, {
              muted
            });
          })
        })]
      }) : viewMode === "week" ? /* @__PURE__ */ jsxs("section", {
        style: styles$7.calendarPanel,
        children: [/* @__PURE__ */ jsxs("div", {
          style: styles$7.viewSummary,
          children: ["Week total delivery time: ", formatTravelMinutes$2(weekTravelMinutes)]
        }), /* @__PURE__ */ jsx("div", {
          style: styles$7.weekHeader,
          children: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"].map((day) => /* @__PURE__ */ jsx("div", {
            style: styles$7.weekDay,
            children: day
          }, day))
        }), /* @__PURE__ */ jsx("div", {
          style: styles$7.calendarGrid,
          children: weekDays.map((day) => renderDayCell(day, {
            tall: true
          }))
        })]
      }) : viewMode === "day" ? /* @__PURE__ */ jsxs("section", {
        style: styles$7.dayViewPanel,
        children: [/* @__PURE__ */ jsxs("div", {
          style: styles$7.viewSummary,
          children: ["Day total delivery time: ", formatTravelMinutes$2(selectedDayTravelMinutes)]
        }), renderDayCell(selectedDay, {
          tall: true
        })]
      }) : /* @__PURE__ */ jsx("section", {
        style: styles$7.listPanel,
        children: calendarOrders.map(({
          order,
          date
        }) => /* @__PURE__ */ jsxs(Link, {
          to: editorHref(order.id),
          style: styles$7.listRow,
          children: [/* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsx("strong", {
              style: styles$7.listDate,
              children: formatDateLabel$2(date)
            }), /* @__PURE__ */ jsx("div", {
              style: styles$7.subtle,
              children: order.requestedWindow || "Needs scheduling"
            })]
          }), /* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsxs("strong", {
              children: [getOrderNumber$3(order), " ", order.customer]
            }), /* @__PURE__ */ jsx("div", {
              style: styles$7.subtle,
              children: getOrderAddress$2(order)
            })]
          }), /* @__PURE__ */ jsx("div", {
            children: getLoadLabel$2(order) || "No material"
          }), /* @__PURE__ */ jsx("span", {
            style: styles$7.statusPill,
            children: getOrderStatus$3(order)
          })]
        }, order.id))
      }), unscheduledOrders.length ? /* @__PURE__ */ jsxs("section", {
        style: styles$7.unscheduledPanel,
        children: [/* @__PURE__ */ jsx("h2", {
          style: styles$7.panelTitle,
          children: "Needs Scheduling"
        }), /* @__PURE__ */ jsx("div", {
          style: styles$7.unscheduledGrid,
          children: unscheduledOrders.map(({
            order
          }) => /* @__PURE__ */ jsxs(Link, {
            to: editorHref(order.id),
            style: styles$7.unscheduledCard,
            children: [/* @__PURE__ */ jsxs("strong", {
              children: [getOrderNumber$3(order), " ", order.customer]
            }), /* @__PURE__ */ jsx("span", {
              children: getOrderAddress$2(order)
            }), /* @__PURE__ */ jsx("small", {
              children: getLoadLabel$2(order) || "No material"
            })]
          }, order.id))
        })]
      }) : null]
    })]
  });
});
const navBase$1 = {
  borderRadius: 8,
  color: "#e5e7eb",
  fontWeight: 800,
  padding: "14px 16px",
  textDecoration: "none"
};
const utilityBase$1 = {
  border: "1px solid #334155",
  borderRadius: 8,
  color: "#e5e7eb",
  fontWeight: 800,
  padding: "11px 14px",
  textDecoration: "none"
};
const styles$7 = {
  page: {
    ["--calendar-bg"]: "#020617",
    ["--calendar-panel"]: "#0f172a",
    ["--calendar-soft"]: "#111827",
    ["--calendar-text"]: "#f8fafc",
    ["--calendar-muted"]: "#94a3b8",
    ["--calendar-border"]: "#334155",
    ["--calendar-blue"]: "#38bdf8",
    ["--calendar-green"]: "#22c55e",
    background: "#020617",
    color: "var(--calendar-text)",
    display: "flex",
    minHeight: "100vh"
  },
  loginPage: {
    ["--calendar-panel"]: "#0f172a",
    ["--calendar-text"]: "#f8fafc",
    ["--calendar-muted"]: "#94a3b8",
    ["--calendar-border"]: "#334155",
    alignItems: "center",
    background: "#020617",
    color: "#f8fafc",
    display: "flex",
    justifyContent: "center",
    minHeight: "100vh",
    padding: 24
  },
  loginCard: {
    background: "#0f172a",
    border: "1px solid #334155",
    borderRadius: 10,
    boxShadow: "0 1px 2px rgba(0,0,0,0.08)",
    maxWidth: 480,
    padding: 32,
    width: "100%"
  },
  loginTitle: {
    fontSize: 34,
    margin: "0 0 8px"
  },
  sideRail: {
    background: "#020617",
    borderRight: "1px solid #1e293b",
    boxShadow: "none",
    display: "flex",
    flexDirection: "column",
    gap: 12,
    minHeight: "100vh",
    position: "sticky",
    top: 0,
    overflow: "hidden",
    transition: "width 160ms ease, padding 160ms ease"
  },
  navToggle: {
    alignItems: "center",
    background: "#1e293b",
    border: "1px solid #334155",
    borderRadius: 8,
    color: "#ff7a1a",
    cursor: "pointer",
    display: "inline-flex",
    fontWeight: 900,
    height: 30,
    justifyContent: "center",
    width: 34
  },
  collapsedOnlyHidden: {
    display: "none"
  },
  brand: {
    alignItems: "center",
    display: "flex",
    gap: 12
  },
  logo: {
    height: 52,
    objectFit: "contain",
    width: 52
  },
  brandTitle: {
    color: "#f8fafc",
    fontSize: 18,
    fontWeight: 900
  },
  brandSub: {
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 800
  },
  nav: {
    display: "grid",
    gap: 8
  },
  navLink: navBase$1,
  navLinkActive: {
    ...navBase$1,
    background: "#1e293b",
    border: "1px solid transparent",
    color: "#ff7a1a"
  },
  footerNav: {
    display: "grid",
    gap: 8
  },
  utility: utilityBase$1,
  workspace: {
    display: "grid",
    flex: 1,
    gap: 14,
    padding: 14
  },
  header: {
    alignItems: "flex-start",
    background: "var(--calendar-panel)",
    border: "1px solid var(--calendar-border)",
    borderRadius: 0,
    boxShadow: "0 1px 2px rgba(0,0,0,0.08)",
    display: "flex",
    justifyContent: "space-between",
    gap: 18,
    padding: "14px 16px"
  },
  kicker: {
    color: "#e85d04",
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: 1.6,
    margin: 0,
    textTransform: "uppercase"
  },
  title: {
    fontSize: 28,
    lineHeight: 1,
    margin: "8px 0 10px"
  },
  subtle: {
    color: "var(--calendar-muted)",
    fontSize: 14,
    lineHeight: 1.45,
    margin: 0
  },
  headerActions: {
    display: "flex",
    gap: 10
  },
  toggle: {
    background: "#0f172a",
    border: "1px solid var(--calendar-border)",
    borderRadius: 999,
    color: "var(--calendar-text)",
    cursor: "pointer",
    fontWeight: 900,
    padding: "12px 18px"
  },
  toggleActive: {
    background: "linear-gradient(180deg, #ff9b25, #ff6b00)",
    border: "1px solid #ff7a1a",
    borderRadius: 999,
    color: "#fff7ed",
    cursor: "pointer",
    fontWeight: 900,
    padding: "12px 18px"
  },
  rssButton: {
    alignItems: "center",
    background: "#0f172a",
    border: "1px solid #f97316",
    borderRadius: 999,
    color: "#e85d04",
    display: "inline-flex",
    fontWeight: 900,
    padding: "12px 18px",
    textDecoration: "none"
  },
  toolbar: {
    alignItems: "center",
    display: "flex",
    gap: 16,
    justifyContent: "space-between"
  },
  search: {
    background: "var(--calendar-panel)",
    border: "1px solid var(--calendar-border)",
    borderRadius: 999,
    color: "var(--calendar-text)",
    flex: 1,
    fontSize: 15,
    minWidth: 240,
    padding: "14px 16px"
  },
  monthControls: {
    alignItems: "center",
    display: "flex",
    gap: 10
  },
  monthLabel: {
    minWidth: 160,
    textAlign: "center"
  },
  smallButton: {
    background: "var(--calendar-panel)",
    border: "1px solid var(--calendar-border)",
    borderRadius: 999,
    cursor: "pointer",
    color: "var(--calendar-text)",
    fontWeight: 900,
    padding: "12px 14px"
  },
  calendarPanel: {
    background: "var(--calendar-panel)",
    border: "1px solid var(--calendar-border)",
    borderRadius: 0,
    overflow: "hidden"
  },
  dayViewPanel: {
    background: "var(--calendar-panel)",
    border: "1px solid var(--calendar-border)",
    borderRadius: 0,
    display: "grid",
    overflow: "hidden"
  },
  viewSummary: {
    background: "#0b1220",
    borderBottom: "1px solid var(--calendar-border)",
    color: "var(--calendar-blue)",
    fontSize: 13,
    fontWeight: 900,
    padding: "12px 14px"
  },
  weekHeader: {
    display: "grid",
    gridTemplateColumns: "repeat(7, minmax(0, 1fr))"
  },
  weekDay: {
    background: "var(--calendar-soft)",
    borderBottom: "1px solid var(--calendar-border)",
    color: "var(--calendar-muted)",
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: 1,
    padding: "12px 14px",
    textTransform: "uppercase"
  },
  calendarGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(7, minmax(0, 1fr))"
  },
  day: {
    background: "#0f172a",
    borderBottom: "1px solid var(--calendar-border)",
    borderRight: "1px solid var(--calendar-border)",
    minHeight: 150,
    padding: 12
  },
  dayLarge: {
    background: "#0f172a",
    borderBottom: "1px solid var(--calendar-border)",
    borderRight: "1px solid var(--calendar-border)",
    minHeight: 520,
    padding: 14
  },
  dayMuted: {
    background: "#111827",
    borderBottom: "1px solid var(--calendar-border)",
    borderRight: "1px solid var(--calendar-border)",
    minHeight: 150,
    opacity: 0.48,
    padding: 12
  },
  dayHeader: {
    alignItems: "center",
    display: "flex",
    justifyContent: "space-between"
  },
  dayTotal: {
    color: "var(--calendar-blue)",
    fontSize: 12,
    fontWeight: 900,
    marginTop: 8
  },
  todayBadge: {
    alignItems: "center",
    background: "var(--calendar-blue)",
    borderRadius: 999,
    color: "#fff",
    display: "inline-flex",
    height: 28,
    justifyContent: "center",
    width: 28
  },
  todayLabelBadge: {
    alignItems: "center",
    background: "var(--calendar-blue)",
    borderRadius: 999,
    color: "#fff",
    display: "inline-flex",
    justifyContent: "center",
    padding: "7px 12px"
  },
  dayCount: {
    background: "rgba(34, 197, 94, 0.18)",
    borderRadius: 999,
    color: "#86efac",
    fontSize: 12,
    fontWeight: 900,
    padding: "4px 8px"
  },
  dayOrders: {
    display: "grid",
    gap: 7,
    marginTop: 10
  },
  orderChip: {
    background: "#0b1220",
    border: "1px solid var(--calendar-border)",
    borderLeft: "5px solid var(--calendar-blue)",
    borderRadius: 12,
    color: "var(--calendar-text)",
    display: "grid",
    gap: 2,
    padding: 9,
    textDecoration: "none"
  },
  moreChip: {
    background: "transparent",
    border: "1px dashed var(--calendar-border)",
    borderRadius: 10,
    color: "var(--calendar-muted)",
    cursor: "pointer",
    fontSize: 12,
    fontWeight: 900,
    padding: "7px 8px",
    textAlign: "left"
  },
  emptyDay: {
    border: "1px dashed var(--calendar-border)",
    borderRadius: 12,
    color: "var(--calendar-muted)",
    fontSize: 13,
    fontWeight: 800,
    padding: 12
  },
  listPanel: {
    background: "var(--calendar-panel)",
    border: "1px solid var(--calendar-border)",
    borderRadius: 0,
    display: "grid",
    overflow: "hidden"
  },
  listRow: {
    alignItems: "center",
    borderBottom: "1px solid var(--calendar-border)",
    color: "var(--calendar-text)",
    display: "grid",
    gap: 16,
    gridTemplateColumns: "220px 1.5fr 1fr auto",
    padding: "16px 18px",
    textDecoration: "none"
  },
  listDate: {
    display: "block",
    marginBottom: 2
  },
  statusPill: {
    background: "rgba(56, 189, 248, 0.16)",
    borderRadius: 999,
    color: "#7dd3fc",
    fontSize: 12,
    fontWeight: 900,
    padding: "7px 10px",
    textAlign: "center"
  },
  unscheduledPanel: {
    background: "var(--calendar-panel)",
    border: "1px solid var(--calendar-border)",
    borderRadius: 0,
    padding: 22
  },
  panelTitle: {
    fontSize: 22,
    margin: "0 0 14px"
  },
  unscheduledGrid: {
    display: "grid",
    gap: 10,
    gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))"
  },
  unscheduledCard: {
    background: "rgba(249, 115, 22, 0.12)",
    border: "1px solid rgba(249, 115, 22, 0.35)",
    borderRadius: 16,
    color: "var(--calendar-text)",
    display: "grid",
    gap: 4,
    padding: 14,
    textDecoration: "none"
  },
  primaryButton: {
    background: "linear-gradient(180deg, #ff9b25, #ff6b00)",
    border: "1px solid #ff7a1a",
    borderRadius: 16,
    color: "#fff7ed",
    cursor: "pointer",
    fontWeight: 900,
    padding: "14px 16px"
  }
};
const route9 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$l,
  default: calendar,
  loader: loader$j
}, Symbol.toStringTag, { value: "Module" }));
const loader$i = loader$o;
const action$k = action$p;
const DISPATCH_NAV_COLLAPSED_KEY = "dispatchNavCollapsed";
const monthNames = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
function parseRequestedDate$2(value) {
  var _a2;
  const trimmed = String(value || "").trim();
  if (!trimmed || /needs scheduling|unavailable|unknown/i.test(trimmed)) return null;
  const today = /* @__PURE__ */ new Date();
  if (/\btoday\b/i.test(trimmed)) {
    return new Date(today.getFullYear(), today.getMonth(), today.getDate());
  }
  if (/\btomorrow\b/i.test(trimmed)) {
    const tomorrow = new Date(today.getFullYear(), today.getMonth(), today.getDate());
    tomorrow.setDate(tomorrow.getDate() + 1);
    return tomorrow;
  }
  const isoDate = trimmed.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (isoDate) {
    return new Date(Number(isoDate[1]), Number(isoDate[2]) - 1, Number(isoDate[3]));
  }
  const slashDate = trimmed.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return new Date(year, Number(slashDate[1]) - 1, Number(slashDate[2]));
  }
  const monthDate = (_a2 = trimmed.match(/\b(?:jan(?:uary)?|feb(?:ruary)?|mar(?:ch)?|apr(?:il)?|may|jun(?:e)?|jul(?:y)?|aug(?:ust)?|sep(?:t(?:ember)?)?|oct(?:ober)?|nov(?:ember)?|dec(?:ember)?)\.?\s+\d{1,2}(?:,\s*\d{4})?/i)) == null ? void 0 : _a2[0];
  if (monthDate) {
    const parsed2 = new Date(/\d{4}/.test(monthDate) ? monthDate : `${monthDate}, ${today.getFullYear()}`);
    if (!Number.isNaN(parsed2.getTime())) {
      return new Date(parsed2.getFullYear(), parsed2.getMonth(), parsed2.getDate());
    }
  }
  const parsed = new Date(trimmed);
  if (Number.isNaN(parsed.getTime())) return null;
  return new Date(parsed.getFullYear(), parsed.getMonth(), parsed.getDate());
}
function dateKey$2(date) {
  if (!date) return "unscheduled";
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function formatDateLabel$1(date) {
  if (!date) return "Needs scheduling";
  return date.toLocaleDateString("en-US", {
    weekday: "long",
    month: "long",
    day: "numeric",
    year: "numeric"
  });
}
function getOrderNumber$2(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function getOrderStatus$2(order) {
  if (order.status === "delivered" || order.deliveryStatus === "delivered") return "Delivered";
  if (order.status === "cancelled") return "Cancelled";
  if (order.assignedRouteId || order.status === "scheduled") return "Scheduled";
  if (order.status === "hold") return "On hold";
  return "New";
}
function shouldShowOnAllotment(order) {
  const status = getOrderStatus$2(order);
  return status !== "Delivered" && status !== "Cancelled";
}
function normalizeQuantity$1(value) {
  const match = String(value || "").match(/-?\d+(?:\.\d+)?/);
  return match ? Number(match[0]) : 0;
}
function formatQuantity(value) {
  if (!Number.isFinite(value)) return "0";
  return Number.isInteger(value) ? String(value) : value.toFixed(2).replace(/\.?0+$/, "");
}
function buildSearchText(order) {
  return [order.id, order.orderNumber, order.customer, order.contact, order.address, order.city, order.material, order.quantity, order.unit, order.requestedWindow, order.timePreference, order.notes, order.status].filter(Boolean).join(" ").toLowerCase();
}
const allotment = UNSAFE_withComponentProps(function AllotmentPage() {
  var _a2;
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const location = useLocation();
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const orders = (actionData == null ? void 0 : actionData.orders) ?? loaderData.orders ?? [];
  const currentUser = (actionData == null ? void 0 : actionData.currentUser) ?? loaderData.currentUser ?? null;
  const [query, setQuery] = useState("");
  const [cursorMonth, setCursorMonth] = useState(() => /* @__PURE__ */ new Date());
  const [selectedDayKey, setSelectedDayKey] = useState(() => dateKey$2(/* @__PURE__ */ new Date()));
  const [navCollapsed, setNavCollapsed] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem(DISPATCH_NAV_COLLAPSED_KEY) === "1";
  });
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const classicHref = isEmbeddedRoute ? "/app/classic" : "/classic";
  const monitorHref = isEmbeddedRoute ? "/app/monitor" : "/monitor";
  const calendarHref = isEmbeddedRoute ? "/app/calendar" : "/calendar";
  const allotmentHref = isEmbeddedRoute ? "/app/allotment" : "/allotment";
  const dispatchHref = isEmbeddedRoute ? "/app/dispatch" : "/dispatch";
  const quoteHref = isEmbeddedRoute ? "/app/custom-quote" : "/custom-quote";
  const driverHref = isEmbeddedRoute ? "/app/dispatch/driver" : "/dispatch/driver";
  const mobileHref = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  const dispatchViewHref = (view) => `${dispatchHref}?view=${view}`;
  const canAccess = (permission) => {
    var _a3;
    return !currentUser || ((_a3 = currentUser.permissions) == null ? void 0 : _a3.includes(permission));
  };
  const logoutHref = currentUser ? "/login?logout=1" : `${dispatchHref}?logout=1`;
  const loginHref = `/login?next=${encodeURIComponent(location.pathname + location.search)}`;
  function toggleNavCollapsed() {
    setNavCollapsed((current) => {
      const next = !current;
      try {
        window.localStorage.setItem(DISPATCH_NAV_COLLAPSED_KEY, next ? "1" : "0");
      } catch {
      }
      return next;
    });
  }
  const allotmentOrders = useMemo(() => {
    const search = query.trim().toLowerCase();
    return orders.filter(shouldShowOnAllotment).filter((order) => !search || buildSearchText(order).includes(search)).map((order) => {
      const date = parseRequestedDate$2(order.requestedWindow);
      return {
        order,
        date,
        dateKey: dateKey$2(date)
      };
    }).sort((a, b) => {
      if (!a.date && !b.date) return getOrderNumber$2(a.order).localeCompare(getOrderNumber$2(b.order));
      if (!a.date) return 1;
      if (!b.date) return -1;
      return a.date.getTime() - b.date.getTime();
    });
  }, [orders, query]);
  const ordersByDay = useMemo(() => {
    const grouped = /* @__PURE__ */ new Map();
    for (const item of allotmentOrders) {
      if (!item.date) continue;
      grouped.set(item.dateKey, [...grouped.get(item.dateKey) || [], item]);
    }
    return grouped;
  }, [allotmentOrders]);
  const selectedItems = ordersByDay.get(selectedDayKey) || [];
  const selectedDate = ((_a2 = selectedItems[0]) == null ? void 0 : _a2.date) || parseRequestedDate$2(selectedDayKey);
  const materialTotals = useMemo(() => {
    const totals = /* @__PURE__ */ new Map();
    for (const {
      order
    } of selectedItems) {
      const material = String(order.material || "Unknown Material").trim();
      const unit = String(order.unit || "Unit").trim();
      const key = `${material.toLowerCase()}::${unit.toLowerCase()}`;
      const current = totals.get(key) || {
        key,
        material,
        unit,
        quantity: 0,
        orders: []
      };
      current.quantity += normalizeQuantity$1(order.quantity);
      current.orders.push(order);
      totals.set(key, current);
    }
    return [...totals.values()].sort((a, b) => a.material.localeCompare(b.material));
  }, [selectedItems]);
  const todayDate = /* @__PURE__ */ new Date();
  const isViewingCurrentMonth = cursorMonth.getFullYear() === todayDate.getFullYear() && cursorMonth.getMonth() === todayDate.getMonth();
  const monthStart = new Date(cursorMonth.getFullYear(), cursorMonth.getMonth(), 1);
  const gridStart = isViewingCurrentMonth ? new Date(todayDate.getFullYear(), todayDate.getMonth(), todayDate.getDate()) : new Date(monthStart);
  gridStart.setDate(gridStart.getDate() - gridStart.getDay());
  const calendarDays = Array.from({
    length: 42
  }, (_, index) => {
    const day = new Date(gridStart);
    day.setDate(gridStart.getDate() + index);
    return day;
  });
  const monthLabel = `${monthNames[cursorMonth.getMonth()]} ${cursorMonth.getFullYear()}`;
  function shiftMonth(amount) {
    setCursorMonth((current) => new Date(current.getFullYear(), current.getMonth() + amount, 1));
  }
  if (!allowed) {
    return /* @__PURE__ */ jsx("main", {
      style: styles$6.loginPage,
      children: /* @__PURE__ */ jsxs("section", {
        style: styles$6.loginCard,
        children: [/* @__PURE__ */ jsx("h1", {
          style: styles$6.loginTitle,
          children: "Material Allotment"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$6.subtle,
          children: "Sign in with your contractor user account to open the daily material totals."
        }), /* @__PURE__ */ jsx("a", {
          href: loginHref,
          style: {
            ...styles$6.primaryButton,
            textDecoration: "none"
          },
          children: "Sign In"
        })]
      })
    });
  }
  return /* @__PURE__ */ jsxs("main", {
    style: styles$6.page,
    children: [/* @__PURE__ */ jsxs("aside", {
      style: {
        ...styles$6.sideRail,
        width: navCollapsed ? 56 : 250,
        padding: navCollapsed ? "12px 8px" : "16px 14px"
      },
      children: [/* @__PURE__ */ jsx("button", {
        type: "button",
        onClick: toggleNavCollapsed,
        style: styles$6.navToggle,
        title: navCollapsed ? "Open navigation" : "Close navigation",
        children: navCollapsed ? ">" : "<"
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$6.brand,
        children: [/* @__PURE__ */ jsx("img", {
          src: "/green-hills-logo.png",
          alt: "Green Hills Supply",
          style: styles$6.logo
        }), /* @__PURE__ */ jsxs("div", {
          style: navCollapsed ? styles$6.collapsedOnlyHidden : void 0,
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$6.brandTitle,
            children: "Contractor"
          }), /* @__PURE__ */ jsx("div", {
            style: styles$6.brandSub,
            children: "Allotment"
          })]
        })]
      }), /* @__PURE__ */ jsxs("nav", {
        style: navCollapsed ? styles$6.collapsedOnlyHidden : styles$6.nav,
        children: [/* @__PURE__ */ jsx(Link, {
          to: classicHref,
          style: styles$6.navLink,
          children: "Classic"
        }), /* @__PURE__ */ jsx(Link, {
          to: monitorHref,
          style: styles$6.navLink,
          children: "Monitor"
        }), /* @__PURE__ */ jsx(Link, {
          to: calendarHref,
          style: styles$6.navLink,
          children: "Calendar"
        }), /* @__PURE__ */ jsx(Link, {
          to: allotmentHref,
          style: styles$6.navLinkActive,
          children: "Allotment"
        }), canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("orders"),
          style: styles$6.navLink,
          children: "Orders"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("scheduled"),
          style: styles$6.navLink,
          children: "Scheduled"
        }), canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("routes"),
          style: styles$6.navLink,
          children: "Routes"
        }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("trucks"),
          style: styles$6.navLink,
          children: "Trucks"
        }) : null, canAccess("manageDispatch") ? /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("employees"),
          style: styles$6.navLink,
          children: "Employees"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: dispatchViewHref("delivered"),
          style: styles$6.navLink,
          children: "Delivered"
        }), /* @__PURE__ */ jsx("a", {
          href: "https://www.ghstickets.info",
          style: styles$6.navLink,
          children: "Ticket Creator"
        })]
      }), /* @__PURE__ */ jsx("div", {
        style: {
          flex: 1
        }
      }), /* @__PURE__ */ jsxs("div", {
        style: navCollapsed ? styles$6.collapsedOnlyHidden : styles$6.footerNav,
        children: [canAccess("driver") ? /* @__PURE__ */ jsx(Link, {
          to: driverHref,
          style: styles$6.utility,
          children: "Driver Route"
        }) : null, canAccess("quoteTool") ? /* @__PURE__ */ jsx(Link, {
          to: quoteHref,
          style: styles$6.utility,
          children: "Quote Tool"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: mobileHref,
          style: styles$6.utility,
          children: "Mobile"
        }), canAccess("manageUsers") ? /* @__PURE__ */ jsx(Link, {
          to: "/settings",
          style: styles$6.utility,
          children: "Settings"
        }) : null, currentUser ? /* @__PURE__ */ jsx(Link, {
          to: "/change-password",
          style: styles$6.utility,
          children: "Change Password"
        }) : null, /* @__PURE__ */ jsx(Link, {
          to: logoutHref,
          style: styles$6.utility,
          children: "Log Out"
        })]
      })]
    }), /* @__PURE__ */ jsxs("section", {
      style: styles$6.workspace,
      children: [/* @__PURE__ */ jsxs("header", {
        style: styles$6.header,
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("p", {
            style: styles$6.kicker,
            children: "Material Allotment"
          }), /* @__PURE__ */ jsx("h1", {
            style: styles$6.title,
            children: "Daily Material Going Out"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$6.subtle,
            children: "Select a requested delivery date to see total material quantities for that day."
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$6.monthControls,
          children: [/* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: () => shiftMonth(-1),
            style: styles$6.smallButton,
            children: "Previous"
          }), /* @__PURE__ */ jsx("strong", {
            style: styles$6.monthLabel,
            children: monthLabel
          }), /* @__PURE__ */ jsx("button", {
            type: "button",
            onClick: () => shiftMonth(1),
            style: styles$6.smallButton,
            children: "Next"
          })]
        })]
      }), /* @__PURE__ */ jsx("div", {
        style: styles$6.toolbar,
        children: /* @__PURE__ */ jsx("input", {
          value: query,
          onChange: (event) => setQuery(event.target.value),
          placeholder: "Search orders, customers, material, address, notes...",
          style: styles$6.search
        })
      }), /* @__PURE__ */ jsxs("section", {
        style: styles$6.layoutGrid,
        children: [/* @__PURE__ */ jsxs("div", {
          style: styles$6.calendarPanel,
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$6.weekHeader,
            children: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"].map((day) => /* @__PURE__ */ jsx("div", {
              style: styles$6.weekDay,
              children: day
            }, day))
          }), /* @__PURE__ */ jsx("div", {
            style: styles$6.calendarGrid,
            children: calendarDays.map((day) => {
              const key = dateKey$2(day);
              const dayOrders = ordersByDay.get(key) || [];
              const muted = day.getMonth() !== cursorMonth.getMonth();
              const selected = selectedDayKey === key;
              const dayQuantity = dayOrders.reduce((sum, item) => sum + normalizeQuantity$1(item.order.quantity), 0);
              return /* @__PURE__ */ jsxs("button", {
                type: "button",
                onClick: () => setSelectedDayKey(key),
                style: {
                  ...muted ? styles$6.dayMuted : styles$6.day,
                  ...selected ? styles$6.daySelected : {}
                },
                children: [/* @__PURE__ */ jsx("span", {
                  style: styles$6.dayNumber,
                  children: day.getDate()
                }), dayOrders.length ? /* @__PURE__ */ jsxs(Fragment, {
                  children: [/* @__PURE__ */ jsxs("span", {
                    style: styles$6.dayCount,
                    children: [dayOrders.length, " loads"]
                  }), /* @__PURE__ */ jsxs("span", {
                    style: styles$6.dayTotal,
                    children: [formatQuantity(dayQuantity), " total units"]
                  })]
                }) : null]
              }, key);
            })
          })]
        }), /* @__PURE__ */ jsxs("aside", {
          style: styles$6.summaryPanel,
          children: [/* @__PURE__ */ jsx("p", {
            style: styles$6.kicker,
            children: "Selected Day"
          }), /* @__PURE__ */ jsx("h2", {
            style: styles$6.panelTitle,
            children: formatDateLabel$1(selectedDate)
          }), /* @__PURE__ */ jsxs("p", {
            style: styles$6.subtle,
            children: [selectedItems.length, " active load", selectedItems.length === 1 ? "" : "s", " included in these material totals."]
          }), /* @__PURE__ */ jsxs("div", {
            style: styles$6.totalsGrid,
            children: [materialTotals.map((total) => /* @__PURE__ */ jsxs("article", {
              style: styles$6.totalCard,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: styles$6.materialName,
                  children: total.material
                }), /* @__PURE__ */ jsxs("p", {
                  style: styles$6.subtle,
                  children: [total.orders.length, " load", total.orders.length === 1 ? "" : "s"]
                })]
              }), /* @__PURE__ */ jsxs("div", {
                style: styles$6.totalQuantity,
                children: [formatQuantity(total.quantity), " ", /* @__PURE__ */ jsx("span", {
                  children: total.unit
                })]
              }), /* @__PURE__ */ jsx("div", {
                style: styles$6.materialBreakdown,
                children: total.orders.map((order) => /* @__PURE__ */ jsxs("span", {
                  style: styles$6.materialBreakdownItem,
                  children: [formatQuantity(normalizeQuantity$1(order.quantity)), " ", order.unit]
                }, order.id))
              })]
            }, total.key)), !materialTotals.length ? /* @__PURE__ */ jsx("div", {
              style: styles$6.emptyState,
              children: "No active material loads on this date."
            }) : null]
          })]
        })]
      })]
    })]
  });
});
const navBase = {
  borderRadius: 8,
  color: "#e5e7eb",
  fontWeight: 800,
  padding: "14px 16px",
  textDecoration: "none"
};
const utilityBase = {
  border: "1px solid #334155",
  borderRadius: 8,
  color: "#e5e7eb",
  fontWeight: 800,
  padding: "11px 14px",
  textDecoration: "none"
};
const styles$6 = {
  page: {
    ["--allotment-bg"]: "#020617",
    ["--allotment-panel"]: "#0f172a",
    ["--allotment-soft"]: "#111827",
    ["--allotment-text"]: "#f8fafc",
    ["--allotment-muted"]: "#94a3b8",
    ["--allotment-border"]: "#334155",
    ["--allotment-blue"]: "#38bdf8",
    background: "#020617",
    color: "var(--allotment-text)",
    display: "flex",
    minHeight: "100vh"
  },
  loginPage: {
    alignItems: "center",
    background: "#020617",
    color: "#f8fafc",
    display: "flex",
    justifyContent: "center",
    minHeight: "100vh",
    padding: 24
  },
  loginCard: {
    background: "#0f172a",
    border: "1px solid #334155",
    borderRadius: 10,
    boxShadow: "0 1px 2px rgba(0,0,0,0.08)",
    maxWidth: 480,
    padding: 32,
    width: "100%"
  },
  loginTitle: {
    fontSize: 34,
    margin: "0 0 8px"
  },
  sideRail: {
    background: "#020617",
    borderRight: "1px solid #1e293b",
    display: "flex",
    flexDirection: "column",
    gap: 12,
    minHeight: "100vh",
    position: "sticky",
    top: 0,
    overflow: "hidden",
    transition: "width 160ms ease, padding 160ms ease"
  },
  navToggle: {
    alignItems: "center",
    background: "#1e293b",
    border: "1px solid #334155",
    borderRadius: 8,
    color: "#ff7a1a",
    cursor: "pointer",
    display: "inline-flex",
    fontWeight: 900,
    height: 30,
    justifyContent: "center",
    width: 34
  },
  collapsedOnlyHidden: {
    display: "none"
  },
  brand: {
    alignItems: "center",
    display: "flex",
    gap: 12
  },
  logo: {
    height: 52,
    objectFit: "contain",
    width: 52
  },
  brandTitle: {
    color: "#f8fafc",
    fontSize: 18,
    fontWeight: 900
  },
  brandSub: {
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 800
  },
  nav: {
    display: "grid",
    gap: 8
  },
  navLink: navBase,
  navLinkActive: {
    ...navBase,
    background: "#1e293b",
    border: "1px solid transparent",
    color: "#ff7a1a"
  },
  footerNav: {
    display: "grid",
    gap: 8
  },
  utility: utilityBase,
  workspace: {
    display: "grid",
    flex: 1,
    gap: 14,
    padding: 14
  },
  header: {
    alignItems: "flex-start",
    background: "var(--allotment-panel)",
    border: "1px solid var(--allotment-border)",
    display: "flex",
    justifyContent: "space-between",
    gap: 18,
    padding: "14px 16px"
  },
  kicker: {
    color: "#e85d04",
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: 1.6,
    margin: 0,
    textTransform: "uppercase"
  },
  title: {
    fontSize: 28,
    lineHeight: 1,
    margin: "8px 0 10px"
  },
  subtle: {
    color: "var(--allotment-muted)",
    fontSize: 14,
    lineHeight: 1.45,
    margin: 0
  },
  toolbar: {
    display: "flex",
    gap: 16
  },
  search: {
    background: "var(--allotment-panel)",
    border: "1px solid var(--allotment-border)",
    borderRadius: 999,
    color: "var(--allotment-text)",
    flex: 1,
    fontSize: 15,
    minWidth: 240,
    padding: "14px 16px"
  },
  monthControls: {
    alignItems: "center",
    display: "flex",
    gap: 10
  },
  monthLabel: {
    minWidth: 160,
    textAlign: "center"
  },
  smallButton: {
    background: "var(--allotment-panel)",
    border: "1px solid var(--allotment-border)",
    borderRadius: 999,
    color: "var(--allotment-text)",
    cursor: "pointer",
    fontWeight: 900,
    padding: "12px 14px"
  },
  layoutGrid: {
    alignItems: "start",
    display: "grid",
    gap: 14,
    gridTemplateColumns: "minmax(520px, 1fr) minmax(360px, 0.62fr)"
  },
  calendarPanel: {
    background: "var(--allotment-panel)",
    border: "1px solid var(--allotment-border)",
    overflow: "hidden"
  },
  weekHeader: {
    display: "grid",
    gridTemplateColumns: "repeat(7, minmax(0, 1fr))"
  },
  weekDay: {
    background: "var(--allotment-soft)",
    borderBottom: "1px solid var(--allotment-border)",
    color: "var(--allotment-muted)",
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: 1,
    padding: "12px 14px",
    textTransform: "uppercase"
  },
  calendarGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(7, minmax(0, 1fr))"
  },
  day: {
    background: "#0f172a",
    border: 0,
    borderBottom: "1px solid var(--allotment-border)",
    borderRight: "1px solid var(--allotment-border)",
    color: "var(--allotment-text)",
    cursor: "pointer",
    display: "grid",
    gap: 8,
    minHeight: 116,
    padding: 12,
    textAlign: "left"
  },
  dayMuted: {
    background: "#111827",
    border: 0,
    borderBottom: "1px solid var(--allotment-border)",
    borderRight: "1px solid var(--allotment-border)",
    color: "var(--allotment-muted)",
    cursor: "pointer",
    display: "grid",
    gap: 8,
    minHeight: 116,
    opacity: 0.48,
    padding: 12,
    textAlign: "left"
  },
  daySelected: {
    boxShadow: "inset 0 0 0 3px #f97316"
  },
  dayNumber: {
    fontSize: 16,
    fontWeight: 900
  },
  dayCount: {
    background: "rgba(34, 197, 94, 0.18)",
    borderRadius: 999,
    color: "#86efac",
    fontSize: 12,
    fontWeight: 900,
    justifySelf: "start",
    padding: "4px 8px"
  },
  dayTotal: {
    color: "var(--allotment-blue)",
    fontSize: 12,
    fontWeight: 900
  },
  summaryPanel: {
    background: "var(--allotment-panel)",
    border: "1px solid var(--allotment-border)",
    display: "grid",
    gap: 14,
    padding: 18,
    position: "sticky",
    top: 14
  },
  panelTitle: {
    fontSize: 24,
    lineHeight: 1.1,
    margin: "6px 0 0"
  },
  totalsGrid: {
    display: "grid",
    gap: 12
  },
  totalCard: {
    background: "#0b1220",
    border: "1px solid var(--allotment-border)",
    borderLeft: "5px solid #f97316",
    display: "grid",
    gap: 12,
    padding: 14
  },
  materialName: {
    display: "block",
    fontSize: 18
  },
  totalQuantity: {
    color: "#38bdf8",
    fontSize: 30,
    fontWeight: 950,
    lineHeight: 1
  },
  materialBreakdown: {
    display: "flex",
    flexWrap: "wrap",
    gap: 8
  },
  materialBreakdownItem: {
    background: "#020617",
    border: "1px solid var(--allotment-border)",
    borderRadius: 999,
    color: "var(--allotment-text)",
    fontSize: 12,
    fontWeight: 900,
    padding: "7px 10px"
  },
  emptyState: {
    background: "rgba(249, 115, 22, 0.12)",
    border: "1px solid rgba(249, 115, 22, 0.35)",
    color: "#fed7aa",
    fontWeight: 900,
    padding: 16
  },
  primaryButton: {
    background: "linear-gradient(180deg, #ff9b25, #ff6b00)",
    border: "1px solid #ff7a1a",
    borderRadius: 16,
    color: "#fff7ed",
    cursor: "pointer",
    fontWeight: 900,
    padding: "14px 16px"
  }
};
const route10 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$k,
  default: allotment,
  loader: loader$i
}, Symbol.toStringTag, { value: "Module" }));
function isAuthorized$3(request, params) {
  const expected = process.env.DISPATCH_CALENDAR_FEED_SECRET || process.env.DISPATCH_POLL_SECRET || "";
  if (!expected) return false;
  const url = new URL(request.url);
  const provided = request.headers.get("x-dispatch-calendar-secret") || (params == null ? void 0 : params.secret) || url.searchParams.get("secret") || "";
  return provided === expected;
}
function escapeIcsText(value) {
  return value.replace(/\\/g, "\\\\").replace(/\r?\n/g, "\\n").replace(/,/g, "\\,").replace(/;/g, "\\;");
}
function foldIcsLine(line) {
  const chunks = [];
  let current = line;
  while (current.length > 74) {
    chunks.push(current.slice(0, 74));
    current = ` ${current.slice(74)}`;
  }
  chunks.push(current);
  return chunks.join("\r\n");
}
function formatIcsDate(date) {
  return `${date.getFullYear()}${String(date.getMonth() + 1).padStart(2, "0")}${String(date.getDate()).padStart(2, "0")}`;
}
function formatIcsDateTime(date) {
  return date.toISOString().replace(/[-:]/g, "").replace(/\.\d{3}Z$/, "Z");
}
function parseRequestedDate$1(value) {
  const trimmed = String(value || "").trim();
  if (!trimmed || /needs scheduling|unavailable|unknown/i.test(trimmed)) return null;
  const today = /* @__PURE__ */ new Date();
  if (/\btoday\b/i.test(trimmed)) {
    return new Date(today.getFullYear(), today.getMonth(), today.getDate());
  }
  if (/\btomorrow\b/i.test(trimmed)) {
    const tomorrow = new Date(today.getFullYear(), today.getMonth(), today.getDate());
    tomorrow.setDate(tomorrow.getDate() + 1);
    return tomorrow;
  }
  const isoDate = trimmed.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (isoDate) {
    return new Date(Number(isoDate[1]), Number(isoDate[2]) - 1, Number(isoDate[3]));
  }
  const slashDate = trimmed.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return new Date(year, Number(slashDate[1]) - 1, Number(slashDate[2]));
  }
  const parsed = new Date(trimmed);
  if (Number.isNaN(parsed.getTime())) return null;
  return new Date(parsed.getFullYear(), parsed.getMonth(), parsed.getDate());
}
function dateKey$1(date) {
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function getOrderNumber$1(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function getLoadLabel$1(order) {
  return [order.quantity, order.unit, order.material].filter(Boolean).join(" ");
}
function getOrderAddress$1(order) {
  return [order.address, order.city].filter(Boolean).join(", ");
}
function getTravelMinutes$1(order) {
  const minutes = Number(order.travelMinutes || 0);
  return Number.isFinite(minutes) && minutes > 0 ? minutes : 0;
}
function formatTravelMinutes$1(minutes) {
  const rounded = Math.round(minutes);
  if (!rounded) return "0 min";
  if (rounded < 60) return `${rounded} min`;
  const hours = Math.floor(rounded / 60);
  const remainder = rounded % 60;
  return remainder ? `${hours} hr ${remainder} min` : `${hours} hr`;
}
function getOrderStatus$1(order) {
  if (order.status === "delivered" || order.deliveryStatus === "delivered") return "Delivered";
  if (order.status === "cancelled") return "Cancelled";
  if (order.assignedRouteId || order.status === "scheduled") return "Scheduled";
  if (order.status === "hold") return "On hold";
  return "New";
}
function shouldIncludeInCalendarFeed$1(order) {
  const status = getOrderStatus$1(order);
  return status !== "Delivered" && status !== "Cancelled";
}
async function loader$h({
  request,
  params
}) {
  if (!isAuthorized$3(request, params)) {
    return new Response("Unauthorized", {
      status: 401
    });
  }
  const orders = await getDispatchOrders();
  const grouped = /* @__PURE__ */ new Map();
  for (const order of orders) {
    if (!shouldIncludeInCalendarFeed$1(order)) continue;
    const date = parseRequestedDate$1(order.requestedWindow);
    if (!date) continue;
    const key = dateKey$1(date);
    const group = grouped.get(key) || {
      date,
      orders: []
    };
    group.orders.push(order);
    grouped.set(key, group);
  }
  const now = formatIcsDateTime(/* @__PURE__ */ new Date());
  const events = [...grouped.values()].sort((a, b) => a.date.getTime() - b.date.getTime()).slice(0, 90).map((group) => {
    const endDate = new Date(group.date);
    endDate.setDate(group.date.getDate() + 1);
    const totalMinutes = group.orders.reduce((sum, order) => sum + getTravelMinutes$1(order), 0);
    const summary = `Green Hills Deliveries: ${group.orders.length} loads (${formatTravelMinutes$1(totalMinutes)})`;
    const description = group.orders.map((order) => [`${getOrderNumber$1(order)} ${order.customer}`, getLoadLabel$1(order), getOrderAddress$1(order), order.timePreference ? `Time: ${order.timePreference}` : "", order.travelSummary || (getTravelMinutes$1(order) ? `Travel: ${formatTravelMinutes$1(getTravelMinutes$1(order))}` : "")].filter(Boolean).join(" | ")).join("\n");
    return ["BEGIN:VEVENT", `UID:green-hills-delivery-${dateKey$1(group.date)}@contractor.ghstickets.com`, `DTSTAMP:${now}`, `DTSTART;VALUE=DATE:${formatIcsDate(group.date)}`, `DTEND;VALUE=DATE:${formatIcsDate(endDate)}`, foldIcsLine(`SUMMARY:${escapeIcsText(summary)}`), foldIcsLine(`DESCRIPTION:${escapeIcsText(description)}`), "END:VEVENT"].join("\r\n");
  }).join("\r\n");
  const ics = ["BEGIN:VCALENDAR", "VERSION:2.0", "PRODID:-//Green Hills Supply//Delivery Calendar//EN", "CALSCALE:GREGORIAN", "METHOD:PUBLISH", "X-WR-CALNAME:Green Hills Deliveries", "X-WR-TIMEZONE:America/Chicago", events, "END:VCALENDAR"].filter(Boolean).join("\r\n");
  return new Response(ics, {
    headers: {
      "Content-Type": "text/calendar; charset=utf-8",
      "Content-Disposition": 'inline; filename="green-hills-deliveries.ics"',
      "Cache-Control": "no-store"
    }
  });
}
const route11 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  loader: loader$h
}, Symbol.toStringTag, { value: "Module" }));
const route12 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  loader: loader$h
}, Symbol.toStringTag, { value: "Module" }));
function isAuthorized$2(request) {
  const expected = process.env.DISPATCH_CALENDAR_FEED_SECRET || process.env.DISPATCH_POLL_SECRET || "";
  if (!expected) return false;
  const url = new URL(request.url);
  const provided = request.headers.get("x-dispatch-calendar-secret") || url.searchParams.get("secret") || "";
  return provided === expected;
}
function escapeXml(value) {
  return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&apos;");
}
function parseRequestedDate(value) {
  const trimmed = String(value || "").trim();
  if (!trimmed || /needs scheduling|unavailable|unknown/i.test(trimmed)) return null;
  const today = /* @__PURE__ */ new Date();
  if (/\btoday\b/i.test(trimmed)) {
    return new Date(today.getFullYear(), today.getMonth(), today.getDate());
  }
  if (/\btomorrow\b/i.test(trimmed)) {
    const tomorrow = new Date(today.getFullYear(), today.getMonth(), today.getDate());
    tomorrow.setDate(tomorrow.getDate() + 1);
    return tomorrow;
  }
  const isoDate = trimmed.match(/^(\d{4})-(\d{2})-(\d{2})$/);
  if (isoDate) {
    return new Date(Number(isoDate[1]), Number(isoDate[2]) - 1, Number(isoDate[3]));
  }
  const slashDate = trimmed.match(/\b(\d{1,2})\/(\d{1,2})\/(\d{2,4})\b/);
  if (slashDate) {
    const year = slashDate[3].length === 2 ? 2e3 + Number(slashDate[3]) : Number(slashDate[3]);
    return new Date(year, Number(slashDate[1]) - 1, Number(slashDate[2]));
  }
  const parsed = new Date(trimmed);
  if (Number.isNaN(parsed.getTime())) return null;
  return new Date(parsed.getFullYear(), parsed.getMonth(), parsed.getDate());
}
function dateKey(date) {
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function formatDateLabel(date) {
  return date.toLocaleDateString("en-US", {
    weekday: "long",
    month: "long",
    day: "numeric",
    year: "numeric"
  });
}
function getOrderNumber(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function getLoadLabel(order) {
  return [order.quantity, order.unit, order.material].filter(Boolean).join(" ");
}
function getOrderAddress(order) {
  return [order.address, order.city].filter(Boolean).join(", ");
}
function getTravelMinutes(order) {
  const minutes = Number(order.travelMinutes || 0);
  return Number.isFinite(minutes) && minutes > 0 ? minutes : 0;
}
function formatTravelMinutes(minutes) {
  const rounded = Math.round(minutes);
  if (!rounded) return "0 min";
  if (rounded < 60) return `${rounded} min`;
  const hours = Math.floor(rounded / 60);
  const remainder = rounded % 60;
  return remainder ? `${hours} hr ${remainder} min` : `${hours} hr`;
}
function getOrderStatus(order) {
  if (order.status === "delivered" || order.deliveryStatus === "delivered") return "Delivered";
  if (order.status === "cancelled") return "Cancelled";
  if (order.assignedRouteId || order.status === "scheduled") return "Scheduled";
  if (order.status === "hold") return "On hold";
  return "New";
}
function shouldIncludeInCalendarFeed(order) {
  const status = getOrderStatus(order);
  return status !== "Delivered" && status !== "Cancelled";
}
async function loader$g({
  request
}) {
  if (!isAuthorized$2(request)) {
    return new Response("Unauthorized", {
      status: 401
    });
  }
  const url = new URL(request.url);
  const orders = await getDispatchOrders();
  const grouped = /* @__PURE__ */ new Map();
  for (const order of orders) {
    if (!shouldIncludeInCalendarFeed(order)) continue;
    const date = parseRequestedDate(order.requestedWindow);
    if (!date) continue;
    const key = dateKey(date);
    const group = grouped.get(key) || {
      date,
      orders: []
    };
    group.orders.push(order);
    grouped.set(key, group);
  }
  const items = [...grouped.values()].sort((a, b) => a.date.getTime() - b.date.getTime()).slice(0, 45).map((group) => {
    const totalMinutes = group.orders.reduce((sum, order) => sum + getTravelMinutes(order), 0);
    const title = `${formatDateLabel(group.date)} - ${group.orders.length} deliveries - ${formatTravelMinutes(totalMinutes)}`;
    const description = group.orders.map((order) => [`${getOrderNumber(order)} ${order.customer}`, getLoadLabel(order), getOrderAddress(order), order.timePreference ? `Time: ${order.timePreference}` : "", order.travelSummary || (getTravelMinutes(order) ? `Travel: ${formatTravelMinutes(getTravelMinutes(order))}` : "")].filter(Boolean).join(" | ")).join("\n");
    return `
        <item>
          <title>${escapeXml(title)}</title>
          <link>${escapeXml(`${url.origin}/calendar?date=${dateKey(group.date)}`)}</link>
          <guid isPermaLink="false">delivery-calendar-${escapeXml(dateKey(group.date))}</guid>
          <pubDate>${group.date.toUTCString()}</pubDate>
          <description>${escapeXml(description)}</description>
        </item>`;
  }).join("");
  const xml = `<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
  <channel>
    <title>Green Hills Delivery Calendar</title>
    <link>${escapeXml(`${url.origin}/calendar`)}</link>
    <description>Requested delivery dates with daily order counts and total delivery time.</description>
    <language>en-us</language>
    <lastBuildDate>${(/* @__PURE__ */ new Date()).toUTCString()}</lastBuildDate>
    ${items}
  </channel>
</rss>`;
  return new Response(xml, {
    headers: {
      "Content-Type": "application/rss+xml; charset=utf-8",
      "Cache-Control": "no-store"
    }
  });
}
const route13 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  loader: loader$g
}, Symbol.toStringTag, { value: "Module" }));
function extractPhone$1(value) {
  const digits = String(value || "").replace(/\D/g, "");
  if (digits.length === 10) return `+1${digits}`;
  if (digits.length === 11 && digits.startsWith("1")) return `+${digits}`;
  return "";
}
function getOneWayTravelMinutes$1(order) {
  const roundTripMinutes = Number(order.travelMinutes || 0);
  if (!Number.isFinite(roundTripMinutes) || roundTripMinutes <= 0) return 0;
  return Math.max(1, Math.round(roundTripMinutes / 2));
}
function getCustomerEtaText$1(order) {
  const oneWayMinutes = getOneWayTravelMinutes$1(order);
  return oneWayMinutes ? `${oneWayMinutes} minute${oneWayMinutes === 1 ? "" : "s"}` : "soon";
}
function getOrderExternalId(order) {
  if (!order) return void 0;
  return order.orderNumber ? `dispatch-order-${order.orderNumber}` : order.id;
}
function getKenectMessageEndpoint() {
  const rawEndpoint = process.env.KENECT_MESSAGES_URL || process.env.KENECT_API_URL || process.env.KENECT_API_BASE_URL || "https://integrations-api.kenect.com";
  try {
    const url = new URL(rawEndpoint);
    const normalizedPath = url.pathname.replace(/\/+$/, "");
    const looksLikeFullMessageEndpoint = normalizedPath === "/api/v1/messages";
    const looksLikeDocsUrl = normalizedPath.startsWith("/q/swagger-ui") || normalizedPath.startsWith("/q/openapi");
    if (looksLikeFullMessageEndpoint) return url.toString();
    if (!looksLikeDocsUrl && normalizedPath && normalizedPath !== "/") {
      return url.toString();
    }
    return `${url.origin}/api/v1/messages`;
  } catch {
    return "https://integrations-api.kenect.com/api/v1/messages";
  }
}
function getKenectHeaders() {
  const apiKey = process.env.KENECT_API_KEY || "";
  const apiSecret = process.env.KENECT_API_SECRET || "";
  const locationId = process.env.KENECT_LOCATION_ID || "";
  const partnerLocationId = process.env.KENECT_PARTNER_LOCATION_ID || "";
  const partnerLocationZip = process.env.KENECT_PARTNER_LOCATION_ZIP || "";
  const headers2 = {
    "Content-Type": "application/json",
    "X-Api-Key": apiKey,
    "X-Api-Secret": apiSecret
  };
  if (locationId) headers2["X-Location-Id"] = locationId;
  if (partnerLocationId) headers2["X-Partner-Location-Id"] = partnerLocationId;
  if (partnerLocationZip) headers2["X-Partner-Location-Zip"] = partnerLocationZip;
  return {
    headers: headers2,
    configured: Boolean(apiKey && apiSecret && (locationId || partnerLocationId))
  };
}
function formatKenectError(status, responseText) {
  const trimmed = responseText.trim();
  const suffix = status === 404 ? " Check that KENECT_API_BASE_URL is https://integrations-api.kenect.com and that the location ID belongs to your Kenect account." : "";
  return `Kenect text failed (${status}): ${trimmed || "No response body."}${suffix}`;
}
function buildEnrouteTextMessage({
  order,
  route: route57
}) {
  const to = extractPhone$1(order.contact);
  const body = [
    `Green Hills Supply: your delivery is en route and should arrive in about ${getCustomerEtaText$1(order)}.`,
    (route57 == null ? void 0 : route57.code) ? `Route: ${route57.code}.` : "",
    (route57 == null ? void 0 : route57.truck) ? `Truck: ${route57.truck}.` : "",
    "Please make sure the drop area is clear."
  ].filter(Boolean).join(" ");
  return {
    to,
    body,
    ready: Boolean(to),
    reason: to ? "Text message is ready." : "No customer phone number was found in the contact field."
  };
}
async function sendKenectTextMessage({
  to,
  body,
  order,
  route: route57
}) {
  const endpoint = getKenectMessageEndpoint();
  const { headers: headers2, configured } = getKenectHeaders();
  if (!configured) {
    return {
      sent: false,
      skipped: true,
      provider: "kenect",
      reason: "Kenect texting is not configured. Set KENECT_API_KEY, KENECT_API_SECRET, and KENECT_LOCATION_ID."
    };
  }
  const response = await fetch(endpoint, {
    method: "POST",
    headers: headers2,
    body: JSON.stringify({
      contactName: (order == null ? void 0 : order.customer) || void 0,
      contactPhone: to,
      messageBody: body,
      externalContactId: getOrderExternalId(order),
      sentByUserEmail: process.env.KENECT_SENT_BY_USER_EMAIL || void 0
    })
  });
  if (!response.ok) {
    const errorText = await response.text();
    throw new Error(formatKenectError(response.status, errorText));
  }
  return { sent: true, provider: "kenect" };
}
async function sendCustomerEnrouteText({
  order,
  route: route57
}) {
  const message = buildEnrouteTextMessage({ order, route: route57 });
  if (!message.ready) {
    return {
      sent: false,
      skipped: true,
      provider: "kenect",
      reason: message.reason
    };
  }
  return sendKenectTextMessage({
    to: message.to,
    body: message.body,
    order,
    route: route57
  });
}
function getDriverPath(url) {
  return url.pathname.startsWith("/app/") ? "/app/dispatch/driver" : "/dispatch/driver";
}
function getDispatchPath(pathname) {
  return pathname.startsWith("/app/") ? "/app/classic" : "/classic";
}
function getStatusLabel$1(status) {
  if (status === "en_route") return "Enroute";
  if (status === "delivered") return "Delivered";
  return "Dispatched";
}
function getStatusColor$1(status) {
  if (status === "delivered") return "#16a34a";
  if (status === "en_route") return "#ea580c";
  return "#0284c7";
}
function getNativeDispatchLocationPlugin() {
  var _a2, _b;
  if (typeof window === "undefined") return null;
  const nativeWindow = window;
  return ((_b = (_a2 = nativeWindow.Capacitor) == null ? void 0 : _a2.Plugins) == null ? void 0 : _b.DispatchLocation) || null;
}
function getOrderDisplayNumber$1(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function extractPhone(value) {
  const digits = String(value || "").replace(/\D/g, "");
  if (digits.length === 10) return `+1${digits}`;
  if (digits.length === 11 && digits.startsWith("1")) return `+${digits}`;
  return "";
}
function formatPhone(value) {
  const phone = extractPhone(value);
  const digits = phone.replace(/\D/g, "");
  if (digits.length === 11 && digits.startsWith("1")) {
    return `(${digits.slice(1, 4)}) ${digits.slice(4, 7)}-${digits.slice(7)}`;
  }
  return phone || "Not captured";
}
function getOneWayTravelMinutes(order) {
  const roundTripMinutes = Number(order.travelMinutes || 0);
  if (!Number.isFinite(roundTripMinutes) || roundTripMinutes <= 0) return 0;
  return Math.max(1, Math.round(roundTripMinutes / 2));
}
function getCustomerEtaText(order) {
  const oneWayMinutes = getOneWayTravelMinutes(order);
  return oneWayMinutes ? `${oneWayMinutes} minute${oneWayMinutes === 1 ? "" : "s"}` : "soon";
}
function getReleaseDelayMs(order) {
  const oneWayMinutes = getOneWayTravelMinutes(order);
  return Math.max(0, oneWayMinutes - 5) * 60 * 1e3;
}
function getReleaseRemainingMs(order, nowMs) {
  if (order.status === "delivered" || order.deliveryStatus === "delivered") return 0;
  if (order.deliveryStatus !== "en_route" || !order.departedAt) return null;
  const departedMs = new Date(order.departedAt).getTime();
  if (Number.isNaN(departedMs)) return null;
  return Math.max(0, getReleaseDelayMs(order) - (nowMs - departedMs));
}
function formatCountdown(ms) {
  const totalSeconds = Math.max(0, Math.ceil(ms / 1e3));
  const minutes = Math.floor(totalSeconds / 60);
  const seconds = totalSeconds % 60;
  return `${minutes}:${String(seconds).padStart(2, "0")}`;
}
function buildChecklistJson(form) {
  const existing = parseChecklistJson(String(form.get("customChecklist") || ""));
  return JSON.stringify({
    ...existing,
    loadedQuantity: String(form.get("loadedQuantity") || "").trim(),
    siteSafe: form.get("siteSafe") === "on",
    loadMatchesTicket: form.get("loadMatchesTicket") === "on",
    customerConfirmedPlacement: form.get("customerConfirmedPlacement") === "on",
    photosTaken: form.get("photosTaken") === "on",
    customChecklist: String(form.get("customChecklist") || "").trim()
  });
}
function parseChecklistJson(value) {
  try {
    const parsed = JSON.parse(String(value || "{}"));
    return parsed && typeof parsed === "object" && !Array.isArray(parsed) ? parsed : {};
  } catch {
    return {};
  }
}
function getLoadedQuantity(order) {
  const checklist = parseChecklistJson(order.checklistJson);
  return String(checklist.loadedQuantity || "").trim();
}
function canViewAllDriverRoutes(user) {
  if (!user) return true;
  return user.role === "admin" || user.permissions.includes("manageDispatch");
}
function filterDriverStateForUser(state, user) {
  if (canViewAllDriverRoutes(user)) return state;
  const employeeId = (user == null ? void 0 : user.driverEmployeeId) || "";
  if (!employeeId) {
    return {
      ...state,
      routes: [],
      orders: [],
      driverScopeMessage: "No driver employee is assigned to this login yet. Ask an admin to match this user to an employee in Settings."
    };
  }
  const routes2 = state.routes.filter((route57) => route57.driverId === employeeId);
  const routeIds = new Set(routes2.map((route57) => route57.id));
  return {
    ...state,
    routes: routes2,
    orders: state.orders.filter((order) => order.assignedRouteId && routeIds.has(order.assignedRouteId)),
    driverScopeMessage: routes2.length ? null : "No active routes are assigned to your driver employee yet."
  };
}
async function loadDriverStateForRequest(request) {
  const currentUser = await getCurrentUser(request);
  const state = await loadDriverState();
  return {
    currentUser,
    ...filterDriverStateForUser(state, currentUser)
  };
}
async function loadDriverState() {
  try {
    await ensureSeedDispatchTrucks();
    await ensureSeedDispatchEmployees();
    await ensureSeedDispatchOrders();
    await ensureSeedDispatchRoutes();
    if (process.env.DISPATCH_AUTO_DAILY_RESET === "true") {
      await resetDispatchRoutesForNewDay();
    }
    return {
      orders: await getDispatchOrders(),
      routes: await getDispatchRoutes(),
      storageReady: true,
      storageError: null
    };
  } catch (error) {
    const message = error instanceof Error ? error.message : "Unable to load driver route data";
    console.error("[DISPATCH DRIVER STORAGE ERROR]", message);
    return {
      orders: seedDispatchOrders,
      routes: seedDispatchRoutes,
      storageReady: false,
      storageError: message
    };
  }
}
async function loader$f({
  request
}) {
  const url = new URL(request.url);
  const driverPath = getDriverPath(url);
  if (url.searchParams.get("logout") === "1") {
    return redirect(driverPath, {
      headers: [["Set-Cookie", await userAuthCookie.serialize("", {
        maxAge: 0
      })], ["Set-Cookie", await adminQuoteCookie.serialize("", {
        maxAge: 0
      })]]
    });
  }
  const allowed = await hasAdminQuotePermissionAccess(request, "driver");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  return data({
    allowed: true,
    ...await loadDriverStateForRequest(request)
  });
}
async function action$j({
  request
}) {
  const url = new URL(request.url);
  const form = await request.formData();
  const intent = String(form.get("intent") || "");
  const allowed = await hasAdminQuotePermissionAccess(request, "driver");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  if (intent !== "update-stop-status") {
    return data({
      allowed: true,
      ok: false,
      message: "Unknown driver action.",
      ...await loadDriverStateForRequest(request)
    }, {
      status: 400
    });
  }
  const orderId = String(form.get("orderId") || "").trim();
  const routeId = String(form.get("routeId") || "").trim();
  const loadedQuantity = String(form.get("loadedQuantity") || "").trim();
  const rawStatus = String(form.get("deliveryStatus") || "").trim();
  const deliveryStatus = rawStatus === "en_route" || rawStatus === "delivered" || rawStatus === "not_started" ? rawStatus : "not_started";
  if (!orderId) {
    return data({
      allowed: true,
      ok: false,
      message: "Missing stop selection.",
      selectedRouteId: routeId || null,
      ...await loadDriverStateForRequest(request)
    }, {
      status: 400
    });
  }
  const now = (/* @__PURE__ */ new Date()).toISOString();
  const currentState = await loadDriverState();
  const currentUser = await getCurrentUser(request);
  const scopedState = filterDriverStateForUser(currentState, currentUser);
  const currentOrder = scopedState.orders.find((order) => order.id === orderId) || null;
  const currentRoute = scopedState.routes.find((route57) => route57.id === (routeId || (currentOrder == null ? void 0 : currentOrder.assignedRouteId))) || null;
  if (!currentOrder || !currentRoute) {
    return data({
      allowed: true,
      ok: false,
      message: "That stop is not assigned to your driver route.",
      selectedRouteId: routeId || null,
      ...await loadDriverStateForRequest(request)
    }, {
      status: 403
    });
  }
  if (deliveryStatus === "en_route" && !loadedQuantity) {
    return data({
      allowed: true,
      ok: false,
      message: "Enter the quantity loaded before marking the stop enroute.",
      selectedRouteId: routeId || currentRoute.id || null,
      selectedOrderId: orderId,
      ...await loadDriverStateForRequest(request)
    }, {
      status: 400
    });
  }
  const patch = {
    deliveryStatus,
    proofName: String(form.get("proofName") || "").trim() || null,
    proofNotes: String(form.get("proofNotes") || "").trim() || null,
    signatureName: String(form.get("signatureName") || "").trim() || null,
    signatureData: String(form.get("signatureData") || "").trim() || null,
    photoUrls: String(form.get("photoUrls") || "").trim() || null,
    inspectionStatus: String(form.get("inspectionStatus") || "").trim() || null,
    checklistJson: buildChecklistJson(form)
  };
  if (deliveryStatus === "en_route") patch.departedAt = now;
  if (deliveryStatus === "delivered") {
    patch.status = "delivered";
    patch.departedAt = patch.departedAt || now;
    patch.deliveredAt = now;
  }
  const updatedOrder = await updateDispatchOrder(orderId, patch);
  let smsNote = "";
  if (deliveryStatus === "en_route") {
    try {
      const smsResult = await sendCustomerEnrouteText({
        order: updatedOrder,
        route: currentRoute
      });
      if (smsResult.sent) {
        const textMessage = buildEnrouteTextMessage({
          order: updatedOrder,
          route: currentRoute
        });
        smsNote = ` Customer enroute text sent with Kenect to ${textMessage.to}.`;
      } else {
        smsNote = ` Customer enroute text skipped: ${smsResult.reason}`;
      }
    } catch (error) {
      const message = error instanceof Error ? error.message : "Unknown text message error.";
      smsNote = ` Customer enroute text failed: ${message}`;
    }
  }
  let emailNote = "";
  if (deliveryStatus === "delivered") {
    try {
      const emailResult = await sendDeliveryConfirmationEmail({
        order: updatedOrder,
        route: currentRoute
      });
      emailNote = emailResult.sent ? " Delivery confirmation email sent." : ` Delivery confirmation email skipped: ${emailResult.reason}`;
    } catch (error) {
      const message = error instanceof Error ? error.message : "Unknown email error.";
      emailNote = ` Delivery confirmation email failed: ${message}`;
    }
  }
  return data({
    allowed: true,
    ok: true,
    message: `Stop marked ${getStatusLabel$1(deliveryStatus).toLowerCase()}.${smsNote}${emailNote}`,
    selectedRouteId: routeId || null,
    selectedOrderId: orderId,
    ...await loadDriverStateForRequest(request)
  });
}
const dispatchDriver = UNSAFE_withComponentProps(function DispatchDriverPage() {
  var _a2;
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const location = useLocation();
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const driverHref = isEmbeddedRoute ? "/app/dispatch/driver" : "/dispatch/driver";
  const detailHref = isEmbeddedRoute ? "/app/dispatch/driver/detail" : "/dispatch/driver/detail";
  const dispatchHref = getDispatchPath(location.pathname);
  const logoutHref = `${driverHref}?logout=1`;
  const loginHref = `/login?next=${encodeURIComponent(location.pathname + location.search)}`;
  const orders = (actionData == null ? void 0 : actionData.orders) ?? loaderData.orders ?? [];
  const routes2 = (actionData == null ? void 0 : actionData.routes) ?? loaderData.routes ?? [];
  const storageReady = (actionData == null ? void 0 : actionData.storageReady) ?? loaderData.storageReady ?? false;
  const storageError = (actionData == null ? void 0 : actionData.storageError) ?? loaderData.storageError ?? null;
  const driverScopeMessage = (actionData == null ? void 0 : actionData.driverScopeMessage) ?? loaderData.driverScopeMessage ?? null;
  const [nowMs, setNowMs] = useState(() => Date.now());
  const searchParams = new URLSearchParams(location.search);
  const selectedRouteId = (actionData == null ? void 0 : actionData.selectedRouteId) || searchParams.get("route") || ((_a2 = routes2[0]) == null ? void 0 : _a2.id) || "";
  const selectedRoute = routes2.find((route57) => route57.id === selectedRouteId) || routes2[0] || null;
  const routeStops = useMemo(() => selectedRoute ? orders.filter((order) => order.assignedRouteId === selectedRoute.id && order.status !== "delivered" && order.deliveryStatus !== "delivered").sort((a, b) => Number(a.stopSequence || 9999) - Number(b.stopSequence || 9999)) : [], [orders, selectedRoute]);
  const completedCount = routeStops.filter((stop) => stop.deliveryStatus === "delivered").length;
  const visibleStopCount = routeStops.reduce((count, stop, index) => {
    if (index === 0) return Math.max(count, 1);
    const previousStop = routeStops[index - 1];
    const previousRemainingMs = getReleaseRemainingMs(previousStop, nowMs);
    const previousReleased = previousStop.status === "delivered" || previousStop.deliveryStatus === "delivered" || previousRemainingMs === 0;
    return previousReleased ? index + 1 : count;
  }, 0);
  const visibleStops = routeStops.slice(0, Math.max(visibleStopCount, routeStops.length ? 1 : 0));
  const nextLockedStopCount = Math.max(routeStops.length - visibleStops.length, 0);
  const activeCountdownStop = visibleStops.find((stop) => {
    const remainingMs = getReleaseRemainingMs(stop, nowMs);
    return remainingMs !== null && remainingMs > 0;
  });
  const activeCountdownMs = activeCountdownStop ? getReleaseRemainingMs(activeCountdownStop, nowMs) || 0 : 0;
  const trackingStop = visibleStops.find((stop) => stop.deliveryStatus === "en_route") || visibleStops[0] || routeStops[0] || null;
  useEffect(() => {
    const timer = window.setInterval(() => setNowMs(Date.now()), 1e3);
    return () => window.clearInterval(timer);
  }, []);
  if (!allowed) {
    return /* @__PURE__ */ jsx("div", {
      style: styles$5.page,
      children: /* @__PURE__ */ jsxs("div", {
        style: styles$5.loginCard,
        children: [/* @__PURE__ */ jsx("h1", {
          style: styles$5.title,
          children: "Driver Route"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$5.subtle,
          children: "Sign in with your contractor user account to open route stops."
        }), /* @__PURE__ */ jsx("a", {
          href: loginHref,
          style: {
            ...styles$5.primaryButton,
            marginTop: 18,
            display: "inline-flex",
            alignItems: "center",
            justifyContent: "center",
            textDecoration: "none"
          },
          children: "Sign In"
        })]
      })
    });
  }
  return /* @__PURE__ */ jsxs("div", {
    style: styles$5.page,
    children: [/* @__PURE__ */ jsx("style", {
      children: `
          @media (max-width: 760px) {
            .driver-shell {
              max-width: none !important;
              width: 100% !important;
              gap: 12px !important;
            }

            .driver-header {
              display: grid !important;
              grid-template-columns: minmax(0, 1fr) !important;
              padding: 14px !important;
            }

            .driver-header-actions {
              justify-content: stretch !important;
            }

            .driver-header-actions > a {
              flex: 1 1 120px !important;
            }

            .driver-route-picker {
              display: flex !important;
              overflow-x: auto !important;
              padding-bottom: 4px !important;
              scroll-snap-type: x proximity;
            }

            .driver-route-picker > a {
              min-width: 175px !important;
              scroll-snap-align: start;
            }

            .driver-summary-grid {
              grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
              gap: 8px !important;
            }

            .driver-summary-grid > div {
              padding: 11px !important;
            }

            .driver-stop-card {
              padding: 14px !important;
              border-radius: 16px !important;
            }

            .driver-stop-top {
              grid-template-columns: 36px minmax(0, 1fr) !important;
              align-items: start !important;
            }

            .driver-status-pill {
              grid-column: 1 / -1 !important;
              justify-self: start !important;
              margin-left: 46px !important;
            }

            .driver-sheet-grid,
            .driver-loaded-panel,
            .driver-proof-grid {
              grid-template-columns: minmax(0, 1fr) !important;
            }

            .driver-utility-row {
              justify-content: stretch !important;
            }

            .driver-utility-row > a,
            .driver-utility-row > button {
              flex: 1 1 150px !important;
            }
          }
        `
    }), /* @__PURE__ */ jsxs("div", {
      className: "driver-shell",
      style: styles$5.shell,
      children: [/* @__PURE__ */ jsxs("header", {
        className: "driver-header",
        style: styles$5.header,
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$5.kicker,
            children: "Driver Mode"
          }), /* @__PURE__ */ jsx("h1", {
            style: styles$5.title,
            children: (selectedRoute == null ? void 0 : selectedRoute.truck) || "Driver Route"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$5.subtle,
            children: selectedRoute ? `${selectedRoute.driver}${selectedRoute.helper ? ` / ${selectedRoute.helper}` : ""} · ${selectedRoute.shift || "Shift not set"}` : "No active route selected"
          })]
        }), /* @__PURE__ */ jsxs("div", {
          className: "driver-header-actions",
          style: styles$5.headerActions,
          children: [/* @__PURE__ */ jsx("a", {
            href: dispatchHref,
            style: styles$5.ghostButton,
            children: "Dispatch"
          }), /* @__PURE__ */ jsx("a", {
            href: logoutHref,
            style: styles$5.ghostButton,
            children: "Log Out"
          })]
        })]
      }), selectedRoute ? /* @__PURE__ */ jsx(DriverLiveTracking, {
        route: selectedRoute,
        activeStop: trackingStop
      }) : null, !storageReady ? /* @__PURE__ */ jsxs("div", {
        style: styles$5.warning,
        children: ["Run `dispatch_schema.sql` in Supabase, then refresh.", storageError ? ` Storage error: ${storageError}` : ""]
      }) : null, driverScopeMessage ? /* @__PURE__ */ jsx("div", {
        style: styles$5.warning,
        children: driverScopeMessage
      }) : null, (actionData == null ? void 0 : actionData.message) ? /* @__PURE__ */ jsx("div", {
        style: actionData.ok ? styles$5.success : styles$5.error,
        children: actionData.message
      }) : null, /* @__PURE__ */ jsx("section", {
        className: "driver-route-picker",
        style: styles$5.routePicker,
        children: routes2.map((route57) => /* @__PURE__ */ jsxs("a", {
          href: `${driverHref}?route=${encodeURIComponent(route57.id)}`,
          style: {
            ...styles$5.routeChip,
            borderColor: route57.id === (selectedRoute == null ? void 0 : selectedRoute.id) ? route57.color : "rgba(203, 213, 225, 0.28)",
            background: route57.id === (selectedRoute == null ? void 0 : selectedRoute.id) ? `${route57.color}22` : "#0f172a"
          },
          children: [/* @__PURE__ */ jsx("span", {
            style: {
              ...styles$5.routeDot,
              background: route57.color
            }
          }), /* @__PURE__ */ jsx("span", {
            children: route57.code
          }), /* @__PURE__ */ jsx("small", {
            children: route57.truck
          })]
        }, route57.id))
      }), /* @__PURE__ */ jsxs("section", {
        className: "driver-summary-grid",
        style: styles$5.summaryGrid,
        children: [/* @__PURE__ */ jsxs("div", {
          style: styles$5.summaryCard,
          children: [/* @__PURE__ */ jsx("span", {
            children: "Stops"
          }), /* @__PURE__ */ jsx("strong", {
            children: routeStops.length
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$5.summaryCard,
          children: [/* @__PURE__ */ jsx("span", {
            children: "Delivered"
          }), /* @__PURE__ */ jsx("strong", {
            children: completedCount
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$5.summaryCard,
          children: [/* @__PURE__ */ jsx("span", {
            children: "Remaining"
          }), /* @__PURE__ */ jsx("strong", {
            children: Math.max(routeStops.length - completedCount, 0)
          })]
        })]
      }), /* @__PURE__ */ jsx("main", {
        style: styles$5.stopList,
        children: routeStops.length === 0 ? /* @__PURE__ */ jsx("div", {
          style: styles$5.empty,
          children: "No stops assigned to this route yet."
        }) : /* @__PURE__ */ jsxs(Fragment, {
          children: [activeCountdownStop ? /* @__PURE__ */ jsxs("div", {
            style: styles$5.countdownPanel,
            children: [/* @__PURE__ */ jsx("span", {
              children: "Next stop releases in"
            }), /* @__PURE__ */ jsx("strong", {
              children: formatCountdown(activeCountdownMs)
            }), /* @__PURE__ */ jsxs("small", {
              children: ["Based on ", getCustomerEtaText(activeCountdownStop), " travel time minus 5 minutes."]
            })]
          }) : null, nextLockedStopCount ? /* @__PURE__ */ jsxs("div", {
            style: styles$5.lockedNotice,
            children: [nextLockedStopCount, " later stop", nextLockedStopCount === 1 ? "" : "s", " hidden until the route timer releases them."]
          }) : null, visibleStops.map((stop) => /* @__PURE__ */ jsxs("article", {
            className: "driver-stop-card",
            style: styles$5.stopCard,
            children: [/* @__PURE__ */ jsxs("div", {
              className: "driver-stop-top",
              style: styles$5.stopTop,
              children: [/* @__PURE__ */ jsx("div", {
                style: styles$5.stopNumber,
                children: stop.stopSequence || "-"
              }), /* @__PURE__ */ jsxs("div", {
                style: {
                  minWidth: 0
                },
                children: [/* @__PURE__ */ jsx("h2", {
                  style: styles$5.stopTitle,
                  children: stop.customer
                }), /* @__PURE__ */ jsxs("p", {
                  style: styles$5.stopAddress,
                  children: [stop.address, ", ", stop.city]
                })]
              }), /* @__PURE__ */ jsx("span", {
                className: "driver-status-pill",
                style: {
                  ...styles$5.statusPill,
                  color: getStatusColor$1(stop.deliveryStatus),
                  borderColor: `${getStatusColor$1(stop.deliveryStatus)}55`,
                  background: `${getStatusColor$1(stop.deliveryStatus)}18`
                },
                children: getStatusLabel$1(stop.deliveryStatus)
              })]
            }), /* @__PURE__ */ jsxs("div", {
              className: "driver-sheet-grid",
              style: styles$5.sheetGrid,
              children: [/* @__PURE__ */ jsxs("section", {
                style: styles$5.sheetSection,
                children: [/* @__PURE__ */ jsx("h3", {
                  style: styles$5.sheetTitle,
                  children: "Driver & Truck"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Truck",
                  value: (selectedRoute == null ? void 0 : selectedRoute.truck) || "Not set"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Driver",
                  value: (selectedRoute == null ? void 0 : selectedRoute.driver) || "Not set"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Order Number",
                  value: getOrderDisplayNumber$1(stop)
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Requested Date",
                  value: stop.requestedWindow || "Not set"
                })]
              }), /* @__PURE__ */ jsxs("section", {
                style: styles$5.sheetSection,
                children: [/* @__PURE__ */ jsx("h3", {
                  style: styles$5.sheetTitle,
                  children: "Ordered Product"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Product Ordered",
                  value: stop.material || "Not set"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Product Type",
                  value: stop.unit || "Not set"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Quantity Ordered",
                  value: `${stop.quantity || "-"} ${stop.unit || ""}`.trim()
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Truck Load",
                  value: `${stop.quantity || "-"} ${stop.unit || ""}`.trim()
                })]
              }), /* @__PURE__ */ jsxs("section", {
                style: styles$5.sheetSection,
                children: [/* @__PURE__ */ jsx("h3", {
                  style: styles$5.sheetTitle,
                  children: "Customer Information"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Customer Name",
                  value: stop.customer || "Not set"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Phone",
                  value: formatPhone(stop.contact)
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Contact",
                  value: stop.contact || "Not captured"
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "Address",
                  value: `${stop.address}, ${stop.city}`
                }), /* @__PURE__ */ jsx(SheetLine, {
                  label: "ETA",
                  value: stop.eta || "Not set"
                })]
              })]
            }), stop.notes ? /* @__PURE__ */ jsx("p", {
              style: styles$5.notes,
              children: stop.notes
            }) : null, /* @__PURE__ */ jsx(StopDeliveryForm, {
              stop,
              routeId: (selectedRoute == null ? void 0 : selectedRoute.id) || "",
              driverName: (selectedRoute == null ? void 0 : selectedRoute.driver) || "",
              detailHref
            })]
          }, stop.id))]
        })
      })]
    })]
  });
});
function DriverLiveTracking({
  route: route57,
  activeStop
}) {
  const [trackingEnabled, setTrackingEnabled] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.localStorage.getItem("dispatchDriverGpsEnabled") === "true";
  });
  const [trackingStatus, setTrackingStatus] = useState(trackingEnabled ? "Starting GPS tracking..." : "Live GPS is off on this device.");
  const [lastPing, setLastPing] = useState("");
  const routeRef = useRef(route57);
  const activeStopRef = useRef(activeStop);
  const latestPositionRef = useRef(null);
  const wakeLockRef = useRef(null);
  useEffect(() => {
    routeRef.current = route57;
    activeStopRef.current = activeStop;
  }, [activeStop, route57]);
  useEffect(() => {
    if (!trackingEnabled) return;
    const nativeLocation = getNativeDispatchLocationPlugin();
    if (!nativeLocation) return;
    nativeLocation.startTracking({
      endpoint: `${window.location.origin}/api/dispatch-driver-location`,
      routeId: route57.id,
      orderId: (activeStop == null ? void 0 : activeStop.id) || null,
      driverId: route57.driverId || null,
      driverName: route57.driver || "",
      truck: route57.truck || route57.code
    }).then((result) => {
      if ((result == null ? void 0 : result.ok) !== false) {
        setTrackingStatus("Native background GPS tracking active.");
      }
    }).catch((error) => {
      setTrackingStatus(error instanceof Error ? error.message : "Native GPS could not start; using browser GPS while the page is open.");
    });
  }, [activeStop == null ? void 0 : activeStop.id, route57.code, route57.driver, route57.driverId, route57.id, route57.truck, trackingEnabled]);
  useEffect(() => {
    if (!trackingEnabled) return;
    if (!navigator.geolocation) {
      setTrackingStatus("Live GPS is not available on this device.");
      return;
    }
    let cancelled = false;
    let lastSentAt = 0;
    const geoOptions = {
      enableHighAccuracy: true,
      maximumAge: 5e3,
      timeout: 2e4
    };
    async function requestWakeLock() {
      const wakeLock = navigator.wakeLock;
      if (!wakeLock || wakeLockRef.current) return;
      try {
        wakeLockRef.current = await wakeLock.request("screen");
      } catch {
      }
    }
    async function sendLocation(position, force = false) {
      const now = Date.now();
      if (!force && now - lastSentAt < 1e4) return;
      lastSentAt = now;
      latestPositionRef.current = position;
      const {
        latitude,
        longitude,
        accuracy,
        heading,
        speed
      } = position.coords;
      const currentRoute = routeRef.current;
      const currentStop = activeStopRef.current;
      setTrackingStatus("GPS tracking active for this route.");
      try {
        const response = await fetch("/api/dispatch-driver-location", {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify({
            routeId: currentRoute.id,
            orderId: (currentStop == null ? void 0 : currentStop.id) || null,
            driverId: currentRoute.driverId || null,
            driverName: currentRoute.driver || "",
            truck: currentRoute.truck || currentRoute.code,
            latitude,
            longitude,
            accuracy,
            heading,
            speed,
            capturedAt: (/* @__PURE__ */ new Date()).toISOString()
          })
        });
        const result = await response.json().catch(() => null);
        if (!response.ok || (result == null ? void 0 : result.ok) === false) {
          throw new Error((result == null ? void 0 : result.message) || "Unable to save GPS location.");
        }
        if (!cancelled) {
          setLastPing((/* @__PURE__ */ new Date()).toLocaleTimeString([], {
            hour: "numeric",
            minute: "2-digit",
            second: "2-digit"
          }));
        }
      } catch (error) {
        if (!cancelled) {
          setTrackingStatus(error instanceof Error ? error.message : "Unable to save GPS location.");
        }
      }
    }
    function requestFreshPosition(force = false) {
      navigator.geolocation.getCurrentPosition((position) => {
        void sendLocation(position, force);
      }, (error) => {
        if (!cancelled) {
          setTrackingStatus(error.message || "Unable to refresh GPS location.");
        }
      }, geoOptions);
    }
    const watchId = navigator.geolocation.watchPosition((position) => {
      void sendLocation(position);
    }, (error) => {
      setTrackingStatus(error.message || "Unable to start GPS tracking.");
    }, geoOptions);
    const heartbeatId = window.setInterval(() => {
      if (latestPositionRef.current) {
        void sendLocation(latestPositionRef.current, true);
      } else {
        requestFreshPosition(true);
      }
    }, 3e4);
    const visibilityHandler = () => {
      if (document.visibilityState === "visible") {
        void requestWakeLock();
        requestFreshPosition(true);
      }
    };
    document.addEventListener("visibilitychange", visibilityHandler);
    void requestWakeLock();
    requestFreshPosition(true);
    return () => {
      cancelled = true;
      window.clearInterval(heartbeatId);
      document.removeEventListener("visibilitychange", visibilityHandler);
      navigator.geolocation.clearWatch(watchId);
      if (wakeLockRef.current) {
        void wakeLockRef.current.release().catch(() => void 0);
        wakeLockRef.current = null;
      }
    };
  }, [trackingEnabled]);
  function enableTracking() {
    if (!navigator.geolocation) {
      setTrackingStatus("Live GPS is not available on this device.");
      return;
    }
    setTrackingStatus("Requesting location access...");
    navigator.geolocation.getCurrentPosition(() => {
      window.localStorage.setItem("dispatchDriverGpsEnabled", "true");
      setTrackingEnabled(true);
      setTrackingStatus("GPS tracking active.");
    }, (error) => {
      window.localStorage.removeItem("dispatchDriverGpsEnabled");
      setTrackingEnabled(false);
      setTrackingStatus(error.message || "Location access was not allowed.");
    }, {
      enableHighAccuracy: true,
      maximumAge: 0,
      timeout: 15e3
    });
  }
  return /* @__PURE__ */ jsxs("section", {
    style: styles$5.trackingPanel,
    children: [/* @__PURE__ */ jsxs("div", {
      children: [/* @__PURE__ */ jsx("span", {
        style: styles$5.trackingLabel,
        children: "Live GPS"
      }), /* @__PURE__ */ jsx("strong", {
        children: trackingStatus
      })]
    }), trackingEnabled ? /* @__PURE__ */ jsx("small", {
      children: lastPing ? `Last map update ${lastPing}` : "Waiting for the first GPS update."
    }) : /* @__PURE__ */ jsx("button", {
      type: "button",
      style: styles$5.trackingButton,
      onClick: enableTracking,
      children: "Enable Live GPS"
    })]
  });
}
function SheetLine({
  label,
  value
}) {
  return /* @__PURE__ */ jsxs("div", {
    style: styles$5.sheetLine,
    children: [/* @__PURE__ */ jsx("span", {
      children: label
    }), /* @__PURE__ */ jsx("strong", {
      children: value
    })]
  });
}
function isImageProof$1(value) {
  return /^data:image\//i.test(value) || /^https?:\/\/.+\.(?:png|jpe?g|webp|gif)(?:\?.*)?$/i.test(value);
}
function readCompressedImageDataUrl(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onerror = () => reject(new Error("Unable to read selected photo."));
    reader.onload = () => {
      const originalDataUrl = String(reader.result || "");
      const image = new Image();
      image.onerror = () => resolve(originalDataUrl);
      image.onload = () => {
        var _a2;
        const maxDimension = 1280;
        const scale = Math.min(1, maxDimension / Math.max(image.width, image.height));
        const width = Math.max(1, Math.round(image.width * scale));
        const height = Math.max(1, Math.round(image.height * scale));
        const canvas = document.createElement("canvas");
        canvas.width = width;
        canvas.height = height;
        (_a2 = canvas.getContext("2d")) == null ? void 0 : _a2.drawImage(image, 0, 0, width, height);
        resolve(canvas.toDataURL("image/jpeg", 0.78));
      };
      image.src = originalDataUrl;
    };
    reader.readAsDataURL(file);
  });
}
function StopDeliveryForm({
  stop,
  routeId,
  driverName,
  detailHref
}) {
  const [photoProof, setPhotoProof] = useState(stop.photoUrls || "");
  const [photoPreviewUrl, setPhotoPreviewUrl] = useState(stop.photoUrls && isImageProof$1(stop.photoUrls) ? stop.photoUrls : "");
  const [gpsProof, setGpsProof] = useState(stop.signatureData || "");
  const [gpsStatus, setGpsStatus] = useState("");
  const [loadedQuantity, setLoadedQuantity] = useState(getLoadedQuantity(stop));
  useEffect(() => {
    return () => {
      if (photoPreviewUrl.startsWith("blob:")) {
        URL.revokeObjectURL(photoPreviewUrl);
      }
    };
  }, [photoPreviewUrl]);
  function captureGps() {
    if (!navigator.geolocation) {
      setGpsStatus("GPS is not available on this device.");
      return;
    }
    setGpsStatus("Getting GPS location...");
    navigator.geolocation.getCurrentPosition((position) => {
      const {
        latitude,
        longitude,
        accuracy
      } = position.coords;
      const capturedAt = (/* @__PURE__ */ new Date()).toLocaleString();
      setGpsProof(`GPS: ${latitude.toFixed(6)}, ${longitude.toFixed(6)} accuracy ${Math.round(accuracy)}m captured ${capturedAt}`);
      setGpsStatus("GPS location captured.");
    }, (error) => {
      setGpsStatus(error.message || "Unable to capture GPS location.");
    }, {
      enableHighAccuracy: true,
      timeout: 12e3,
      maximumAge: 0
    });
  }
  return /* @__PURE__ */ jsxs(Form, {
    method: "post",
    style: styles$5.stopForm,
    children: [/* @__PURE__ */ jsx("input", {
      type: "hidden",
      name: "intent",
      value: "update-stop-status"
    }), /* @__PURE__ */ jsx("input", {
      type: "hidden",
      name: "routeId",
      value: routeId
    }), /* @__PURE__ */ jsx("input", {
      type: "hidden",
      name: "orderId",
      value: stop.id
    }), /* @__PURE__ */ jsxs("section", {
      className: "driver-loaded-panel",
      style: styles$5.loadedQuantityPanel,
      children: [/* @__PURE__ */ jsxs("div", {
        children: [/* @__PURE__ */ jsx("label", {
          style: styles$5.label,
          children: "Qty Loaded On Truck"
        }), /* @__PURE__ */ jsx("input", {
          name: "loadedQuantity",
          value: loadedQuantity,
          onChange: (event) => setLoadedQuantity(event.currentTarget.value),
          placeholder: `Example: ${stop.quantity || "10"} ${stop.unit || ""}`.trim(),
          style: styles$5.input,
          inputMode: "decimal"
        })]
      }), /* @__PURE__ */ jsx("button", {
        type: "submit",
        name: "deliveryStatus",
        value: "en_route",
        style: {
          ...styles$5.enrouteButton,
          ...!loadedQuantity.trim() ? styles$5.disabledButton : null
        },
        disabled: !loadedQuantity.trim(),
        title: !loadedQuantity.trim() ? "Enter qty loaded before going enroute." : "Mark this stop enroute",
        children: "Enroute"
      })]
    }), /* @__PURE__ */ jsxs("section", {
      style: styles$5.capturePanel,
      children: [/* @__PURE__ */ jsxs("div", {
        children: [/* @__PURE__ */ jsx("div", {
          style: styles$5.captureTitle,
          children: "Delivery Proof"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$5.captureHelp,
          children: "Capture the delivery photo and GPS before submitting the stop as delivered."
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$5.captureGrid,
        children: [/* @__PURE__ */ jsxs("label", {
          style: styles$5.cameraButton,
          children: ["Take Picture", /* @__PURE__ */ jsx("input", {
            type: "file",
            accept: "image/*",
            capture: "environment",
            style: {
              display: "none"
            },
            onChange: (event) => {
              var _a2;
              const file = (_a2 = event.currentTarget.files) == null ? void 0 : _a2[0];
              if (file) {
                readCompressedImageDataUrl(file).then((dataUrl) => {
                  setPhotoPreviewUrl((currentPreviewUrl) => {
                    if (currentPreviewUrl.startsWith("blob:")) {
                      URL.revokeObjectURL(currentPreviewUrl);
                    }
                    return dataUrl;
                  });
                  setPhotoProof(dataUrl);
                }).catch((error) => {
                  setPhotoProof(error instanceof Error ? error.message : "Unable to load photo.");
                });
              }
            }
          })]
        }), /* @__PURE__ */ jsx("button", {
          type: "button",
          style: styles$5.gpsButton,
          onClick: captureGps,
          children: "Capture GPS"
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$5.proofStatusGrid,
        children: [/* @__PURE__ */ jsxs("div", {
          style: styles$5.proofStatusBox,
          children: [/* @__PURE__ */ jsx("span", {
            children: "Photo"
          }), photoPreviewUrl ? /* @__PURE__ */ jsx("img", {
            src: photoPreviewUrl,
            alt: "Delivery proof preview",
            style: styles$5.photoPreview
          }) : /* @__PURE__ */ jsx("strong", {
            children: "Not captured"
          })]
        }), /* @__PURE__ */ jsxs("div", {
          style: styles$5.proofStatusBox,
          children: [/* @__PURE__ */ jsx("span", {
            children: "GPS"
          }), /* @__PURE__ */ jsx("strong", {
            children: gpsProof || gpsStatus || "Not captured"
          })]
        })]
      })]
    }), /* @__PURE__ */ jsx("div", {
      className: "driver-proof-grid",
      style: styles$5.proofGrid,
      children: /* @__PURE__ */ jsxs("div", {
        children: [/* @__PURE__ */ jsx("label", {
          style: styles$5.label,
          children: "Driver Signature / Name"
        }), /* @__PURE__ */ jsx("input", {
          name: "signatureName",
          defaultValue: stop.signatureName || driverName,
          placeholder: "Driver name",
          style: styles$5.input
        })]
      })
    }), /* @__PURE__ */ jsxs("div", {
      children: [/* @__PURE__ */ jsx("label", {
        style: styles$5.label,
        children: "Driver Notes Upon Delivery"
      }), /* @__PURE__ */ jsx("textarea", {
        name: "proofNotes",
        defaultValue: stop.proofNotes || "",
        rows: 3,
        placeholder: "Gate code, placement note, blocked access, customer request...",
        style: styles$5.textarea
      })]
    }), /* @__PURE__ */ jsx("input", {
      type: "hidden",
      name: "proofName",
      value: driverName || stop.proofName || ""
    }), /* @__PURE__ */ jsx("input", {
      type: "hidden",
      name: "signatureData",
      value: gpsProof
    }), /* @__PURE__ */ jsx("input", {
      type: "hidden",
      name: "photoUrls",
      value: photoProof
    }), /* @__PURE__ */ jsx("input", {
      type: "hidden",
      name: "inspectionStatus",
      value: stop.inspectionStatus || ""
    }), /* @__PURE__ */ jsx("input", {
      type: "hidden",
      name: "customChecklist",
      value: stop.checklistJson || ""
    }), /* @__PURE__ */ jsxs("div", {
      className: "driver-utility-row",
      style: styles$5.utilityRow,
      children: [/* @__PURE__ */ jsx("a", {
        href: `https://maps.google.com/?q=${encodeURIComponent(`${stop.address}, ${stop.city}`)}`,
        target: "_blank",
        rel: "noreferrer",
        style: styles$5.mapButton,
        children: "Open Map"
      }), /* @__PURE__ */ jsx("button", {
        type: "button",
        style: styles$5.detailButton,
        onClick: () => {
          const url = `${detailHref}?order=${encodeURIComponent(stop.id)}`;
          window.open(url, `dispatch-stop-${stop.id}`, "width=720,height=860,menubar=no,toolbar=no,location=no,status=no");
        },
        children: "Full Detail"
      })]
    }), /* @__PURE__ */ jsx("button", {
      type: "submit",
      name: "deliveryStatus",
      value: "delivered",
      style: styles$5.deliveredButton,
      children: "Submit Delivery"
    })]
  });
}
const styles$5 = {
  page: {
    minHeight: "100vh",
    background: "#020617",
    color: "#f8fafc",
    padding: "16px 14px 34px",
    fontFamily: '"Plus Jakarta Sans", "Inter", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif'
  },
  shell: {
    maxWidth: 980,
    margin: "0 auto",
    display: "grid",
    gap: 14
  },
  loginCard: {
    maxWidth: 460,
    margin: "12vh auto 0",
    padding: 20,
    borderRadius: 10,
    background: "#0f172a",
    border: "1px solid #334155",
    boxShadow: "0 18px 45px rgba(0, 0, 0, 0.28)"
  },
  header: {
    display: "flex",
    justifyContent: "space-between",
    gap: 14,
    alignItems: "flex-start",
    padding: 16,
    borderRadius: 10,
    background: "#0f172a",
    border: "1px solid #334155"
  },
  headerActions: {
    display: "flex",
    gap: 8,
    flexWrap: "wrap",
    justifyContent: "flex-end"
  },
  kicker: {
    color: "#38bdf8",
    fontSize: 12,
    fontWeight: 800,
    textTransform: "uppercase",
    letterSpacing: "0.08em"
  },
  title: {
    margin: "4px 0 0",
    fontSize: 28,
    lineHeight: 1.1,
    fontWeight: 900,
    letterSpacing: 0
  },
  subtle: {
    margin: "6px 0 0",
    color: "#94a3b8",
    lineHeight: 1.45
  },
  ghostButton: {
    minHeight: 40,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "0 12px",
    borderRadius: 8,
    border: "1px solid #334155",
    background: "#111827",
    color: "#f8fafc",
    textDecoration: "none",
    fontWeight: 800,
    fontSize: 13
  },
  routePicker: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(150px, 1fr))",
    gap: 10
  },
  trackingPanel: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 14,
    padding: "12px 14px",
    borderRadius: 12,
    border: "1px solid rgba(34, 197, 94, 0.34)",
    background: "linear-gradient(135deg, rgba(20, 83, 45, 0.5), rgba(15, 23, 42, 0.96))",
    color: "#dcfce7",
    flexWrap: "wrap"
  },
  trackingLabel: {
    display: "block",
    marginBottom: 4,
    color: "#86efac",
    fontSize: 11,
    fontWeight: 950,
    letterSpacing: "0.08em",
    textTransform: "uppercase"
  },
  trackingButton: {
    minHeight: 38,
    borderRadius: 999,
    border: "1px solid rgba(34, 197, 94, 0.55)",
    background: "#22c55e",
    color: "#052e16",
    padding: "0 14px",
    fontWeight: 950,
    cursor: "pointer"
  },
  routeChip: {
    display: "grid",
    gridTemplateColumns: "12px minmax(0, 1fr) auto",
    gap: 8,
    alignItems: "center",
    padding: 12,
    borderRadius: 8,
    border: "1px solid #334155",
    color: "#f8fafc",
    textDecoration: "none",
    fontWeight: 900
  },
  routeDot: {
    width: 10,
    height: 10,
    borderRadius: 999
  },
  summaryGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(3, minmax(0, 1fr))",
    gap: 10
  },
  summaryCard: {
    display: "grid",
    gap: 4,
    padding: 14,
    borderRadius: 8,
    background: "#0f172a",
    border: "1px solid #334155"
  },
  stopList: {
    display: "grid",
    gap: 12
  },
  countdownPanel: {
    display: "grid",
    gap: 4,
    padding: 16,
    borderRadius: 14,
    background: "rgba(249, 115, 22, 0.14)",
    border: "1px solid rgba(249, 115, 22, 0.38)",
    color: "#fed7aa"
  },
  lockedNotice: {
    padding: 14,
    borderRadius: 12,
    background: "rgba(56, 189, 248, 0.12)",
    border: "1px solid rgba(56, 189, 248, 0.34)",
    color: "#7dd3fc",
    fontWeight: 900
  },
  stopCard: {
    padding: 16,
    borderRadius: 14,
    background: "#0f172a",
    border: "1px solid #334155",
    boxShadow: "0 14px 28px rgba(0, 0, 0, 0.24)"
  },
  stopTop: {
    display: "grid",
    gridTemplateColumns: "40px minmax(0, 1fr) auto",
    gap: 10,
    alignItems: "center"
  },
  stopNumber: {
    width: 36,
    height: 36,
    borderRadius: 999,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    background: "#0f172a",
    color: "#ffffff",
    fontWeight: 900
  },
  stopTitle: {
    margin: 0,
    fontSize: 17,
    fontWeight: 900,
    letterSpacing: 0
  },
  stopAddress: {
    margin: "3px 0 0",
    color: "#94a3b8",
    lineHeight: 1.35
  },
  statusPill: {
    minHeight: 30,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "0 10px",
    borderRadius: 999,
    border: "1px solid",
    fontSize: 12,
    fontWeight: 900,
    whiteSpace: "nowrap"
  },
  detailButton: {
    minHeight: 42,
    borderRadius: 999,
    border: "1px solid rgba(56, 189, 248, 0.45)",
    background: "rgba(56, 189, 248, 0.16)",
    color: "#7dd3fc",
    fontSize: 12,
    fontWeight: 900,
    cursor: "pointer",
    padding: "0 12px",
    whiteSpace: "nowrap"
  },
  mapButton: {
    minHeight: 42,
    borderRadius: 999,
    border: "1px solid rgba(34, 197, 94, 0.45)",
    background: "rgba(34, 197, 94, 0.16)",
    color: "#86efac",
    fontSize: 12,
    fontWeight: 900,
    cursor: "pointer",
    padding: "0 14px",
    whiteSpace: "nowrap",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    textDecoration: "none"
  },
  sheetGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
    gap: 12,
    marginTop: 14
  },
  sheetSection: {
    borderRadius: 10,
    border: "1px solid #334155",
    background: "#0b1220",
    padding: 12
  },
  sheetTitle: {
    margin: "0 0 10px",
    paddingBottom: 8,
    borderBottom: "2px solid #334155",
    color: "#f8fafc",
    fontSize: 12,
    fontWeight: 950,
    letterSpacing: "0.08em",
    textTransform: "uppercase"
  },
  sheetLine: {
    display: "grid",
    gridTemplateColumns: "minmax(100px, 0.8fr) minmax(0, 1.2fr)",
    gap: 10,
    alignItems: "baseline",
    padding: "7px 0",
    borderBottom: "1px solid #334155",
    color: "#94a3b8",
    fontSize: 12
  },
  notes: {
    margin: "12px 0 0",
    padding: 12,
    borderRadius: 8,
    background: "rgba(249, 115, 22, 0.14)",
    border: "1px solid rgba(249, 115, 22, 0.38)",
    color: "#fed7aa",
    lineHeight: 1.45
  },
  stopForm: {
    display: "grid",
    gap: 12,
    marginTop: 12
  },
  loadedQuantityPanel: {
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr) minmax(150px, 220px)",
    gap: 10,
    alignItems: "end",
    padding: 12,
    borderRadius: 12,
    border: "1px solid rgba(249, 115, 22, 0.38)",
    background: "rgba(249, 115, 22, 0.12)"
  },
  enrouteButton: {
    minHeight: 52,
    borderRadius: 10,
    border: "1px solid #ea580c",
    background: "linear-gradient(135deg, #f97316, #fb923c)",
    color: "#431407",
    fontWeight: 950,
    cursor: "pointer"
  },
  disabledButton: {
    opacity: 0.45,
    cursor: "not-allowed",
    filter: "grayscale(0.65)"
  },
  proofGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
    gap: 10
  },
  capturePanel: {
    display: "grid",
    gap: 12,
    padding: 14,
    borderRadius: 12,
    border: "1px solid #334155",
    background: "linear-gradient(145deg, #0b1220, #111827)"
  },
  captureTitle: {
    fontSize: 16,
    fontWeight: 950,
    color: "#f8fafc"
  },
  captureHelp: {
    margin: "4px 0 0",
    color: "#94a3b8",
    lineHeight: 1.4,
    fontSize: 13
  },
  captureGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(160px, 1fr))",
    gap: 10
  },
  cameraButton: {
    minHeight: 52,
    borderRadius: 10,
    border: "1px solid #0284c7",
    background: "#0ea5e9",
    color: "#ffffff",
    fontWeight: 950,
    cursor: "pointer",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    textAlign: "center"
  },
  gpsButton: {
    minHeight: 52,
    borderRadius: 10,
    border: "1px solid #16a34a",
    background: "#22c55e",
    color: "#052e16",
    fontWeight: 950,
    cursor: "pointer"
  },
  proofStatusGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
    gap: 10
  },
  proofStatusBox: {
    minHeight: 58,
    display: "grid",
    gap: 4,
    alignContent: "center",
    padding: "10px 12px",
    borderRadius: 10,
    border: "1px solid #334155",
    background: "#0f172a",
    color: "#cbd5e1",
    fontSize: 12
  },
  photoPreview: {
    width: "100%",
    maxHeight: 260,
    borderRadius: 10,
    objectFit: "cover",
    border: "1px solid #334155",
    background: "#111827"
  },
  utilityRow: {
    display: "flex",
    gap: 10,
    justifyContent: "flex-end",
    flexWrap: "wrap"
  },
  label: {
    display: "block",
    marginBottom: 6,
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 900,
    textTransform: "uppercase",
    letterSpacing: "0.04em"
  },
  input: {
    width: "100%",
    minHeight: 42,
    boxSizing: "border-box",
    borderRadius: 8,
    border: "1px solid #334155",
    background: "#020617",
    color: "#f8fafc",
    padding: "10px 11px",
    fontSize: 14
  },
  textarea: {
    width: "100%",
    boxSizing: "border-box",
    borderRadius: 8,
    border: "1px solid #334155",
    background: "#020617",
    color: "#f8fafc",
    padding: "10px 11px",
    fontSize: 14,
    resize: "vertical"
  },
  primaryButton: {
    minHeight: 44,
    borderRadius: 8,
    border: "none",
    background: "#2563eb",
    color: "#ffffff",
    fontWeight: 900,
    cursor: "pointer"
  },
  deliveredButton: {
    minHeight: 58,
    borderRadius: 12,
    border: "none",
    background: "linear-gradient(135deg, #16a34a, #22c55e)",
    color: "#052e16",
    fontWeight: 950,
    fontSize: 16,
    cursor: "pointer",
    boxShadow: "0 12px 24px rgba(22, 163, 74, 0.24)"
  },
  success: {
    padding: 12,
    borderRadius: 8,
    background: "rgba(34, 197, 94, 0.14)",
    border: "1px solid rgba(34, 197, 94, 0.38)",
    color: "#86efac",
    fontWeight: 800
  },
  warning: {
    padding: 12,
    borderRadius: 8,
    background: "rgba(245, 158, 11, 0.16)",
    border: "1px solid rgba(245, 158, 11, 0.38)",
    color: "#fde68a",
    fontWeight: 800
  },
  error: {
    padding: 12,
    borderRadius: 8,
    background: "rgba(127, 29, 29, 0.35)",
    border: "1px solid rgba(248, 113, 113, 0.4)",
    color: "#fecaca",
    fontWeight: 800
  },
  empty: {
    padding: 18,
    borderRadius: 10,
    background: "#0f172a",
    border: "1px solid #334155",
    color: "#94a3b8",
    fontWeight: 800,
    textAlign: "center"
  }
};
const route14 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$j,
  default: dispatchDriver,
  loader: loader$f
}, Symbol.toStringTag, { value: "Module" }));
function getDetailPath(url) {
  return url.pathname.startsWith("/app/") ? "/app/dispatch/driver/detail" : "/dispatch/driver/detail";
}
function getStatusLabel(status) {
  if (status === "en_route") return "Enroute";
  if (status === "delivered") return "Delivered";
  return "Dispatched";
}
function getStatusColor(status) {
  if (status === "delivered") return "#16a34a";
  if (status === "en_route") return "#ea580c";
  return "#0284c7";
}
function getOrderDisplayNumber(order) {
  return order.orderNumber ? `#${order.orderNumber}` : order.id;
}
function isImageProof(value) {
  return /^data:image\//i.test(String(value || "")) || /^https?:\/\/.+\.(?:png|jpe?g|webp|gif)(?:\?.*)?$/i.test(String(value || ""));
}
async function loadOrder(orderId) {
  await ensureSeedDispatchTrucks();
  await ensureSeedDispatchEmployees();
  await ensureSeedDispatchOrders();
  await ensureSeedDispatchRoutes();
  const orders = await getDispatchOrders();
  return orders.find((order) => order.id === orderId) || null;
}
async function loader$e({
  request
}) {
  const url = new URL(request.url);
  const detailPath = getDetailPath(url);
  if (url.searchParams.get("logout") === "1") {
    return redirect(detailPath, {
      headers: [["Set-Cookie", await userAuthCookie.serialize("", {
        maxAge: 0
      })], ["Set-Cookie", await adminQuoteCookie.serialize("", {
        maxAge: 0
      })]]
    });
  }
  const allowed = await hasAdminQuotePermissionAccess(request, "driver");
  if (!allowed) {
    return data({
      allowed: false,
      order: null
    });
  }
  const orderId = url.searchParams.get("order") || "";
  const order = orderId ? await loadOrder(orderId) : null;
  return data({
    allowed: true,
    order
  });
}
const dispatchDriverDetail = UNSAFE_withComponentProps(function DispatchDriverDetailPage() {
  const loaderData = useLoaderData();
  const order = loaderData.order;
  if (!loaderData.allowed) {
    return /* @__PURE__ */ jsx("main", {
      style: styles$4.page,
      children: /* @__PURE__ */ jsxs("section", {
        style: styles$4.card,
        children: [/* @__PURE__ */ jsx("h1", {
          style: styles$4.title,
          children: "Driver Stop Detail"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$4.muted,
          children: "Please open the driver route and log in first."
        })]
      })
    });
  }
  if (!order) {
    return /* @__PURE__ */ jsx("main", {
      style: styles$4.page,
      children: /* @__PURE__ */ jsxs("section", {
        style: styles$4.card,
        children: [/* @__PURE__ */ jsx("h1", {
          style: styles$4.title,
          children: "Stop Not Found"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$4.muted,
          children: "This stop may have been unassigned or deleted."
        })]
      })
    });
  }
  return /* @__PURE__ */ jsx("main", {
    style: styles$4.page,
    children: /* @__PURE__ */ jsxs("section", {
      style: styles$4.card,
      children: [/* @__PURE__ */ jsxs("div", {
        style: styles$4.header,
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("div", {
            style: styles$4.kicker,
            children: getOrderDisplayNumber(order)
          }), /* @__PURE__ */ jsx("h1", {
            style: styles$4.title,
            children: order.customer
          }), /* @__PURE__ */ jsx("p", {
            style: styles$4.muted,
            children: order.contact || "No contact captured"
          })]
        }), /* @__PURE__ */ jsx("span", {
          style: {
            ...styles$4.status,
            color: getStatusColor(order.deliveryStatus),
            borderColor: `${getStatusColor(order.deliveryStatus)}55`,
            background: `${getStatusColor(order.deliveryStatus)}18`
          },
          children: getStatusLabel(order.deliveryStatus)
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$4.grid,
        children: [/* @__PURE__ */ jsx(Info, {
          label: "Address",
          value: `${order.address}, ${order.city}`
        }), /* @__PURE__ */ jsx(Info, {
          label: "Load",
          value: `${order.quantity} ${order.unit} ${order.material}`
        }), /* @__PURE__ */ jsx(Info, {
          label: "Requested",
          value: order.requestedWindow || "Not set"
        }), /* @__PURE__ */ jsx(Info, {
          label: "Time Preference",
          value: order.timePreference || "Anytime"
        }), /* @__PURE__ */ jsx(Info, {
          label: "Travel Time",
          value: order.travelSummary || "Not calculated"
        }), /* @__PURE__ */ jsx(Info, {
          label: "ETA",
          value: order.eta || "Not set"
        }), /* @__PURE__ */ jsx(Info, {
          label: "Stop",
          value: order.stopSequence ? `Stop ${order.stopSequence}` : "Unassigned"
        }), /* @__PURE__ */ jsx(Info, {
          label: "Inspection",
          value: order.inspectionStatus || "Not completed"
        }), /* @__PURE__ */ jsx(Info, {
          label: "Proof Name",
          value: order.proofName || "Not captured"
        })]
      }), /* @__PURE__ */ jsxs("section", {
        style: styles$4.noteBox,
        children: [/* @__PURE__ */ jsx("div", {
          style: styles$4.label,
          children: "Notes"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$4.noteText,
          children: order.notes || "No dispatch notes yet."
        })]
      }), order.proofNotes || order.photoUrls || order.signatureName ? /* @__PURE__ */ jsxs("section", {
        style: styles$4.noteBox,
        children: [/* @__PURE__ */ jsx("div", {
          style: styles$4.label,
          children: "Driver Proof"
        }), order.signatureName ? /* @__PURE__ */ jsxs("p", {
          style: styles$4.noteText,
          children: ["Signature: ", order.signatureName]
        }) : null, order.proofNotes ? /* @__PURE__ */ jsxs("p", {
          style: styles$4.noteText,
          children: ["Proof notes: ", order.proofNotes]
        }) : null, order.photoUrls ? isImageProof(order.photoUrls) ? /* @__PURE__ */ jsx("img", {
          src: order.photoUrls,
          alt: "Delivered material proof",
          style: styles$4.photoPreview
        }) : /* @__PURE__ */ jsxs("p", {
          style: styles$4.noteText,
          children: ["Photos: ", order.photoUrls]
        }) : null]
      }) : null]
    })
  });
});
function Info({
  label,
  value
}) {
  return /* @__PURE__ */ jsxs("div", {
    style: styles$4.infoCard,
    children: [/* @__PURE__ */ jsx("div", {
      style: styles$4.label,
      children: label
    }), /* @__PURE__ */ jsx("div", {
      style: styles$4.value,
      children: value
    })]
  });
}
const styles$4 = {
  page: {
    minHeight: "100vh",
    background: "#f8fafc",
    color: "#0f172a",
    padding: 16,
    fontFamily: '"Plus Jakarta Sans", "Inter", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif'
  },
  card: {
    maxWidth: 760,
    margin: "0 auto",
    padding: 18,
    borderRadius: 12,
    background: "#ffffff",
    border: "1px solid #e2e8f0",
    boxShadow: "0 16px 40px rgba(15, 23, 42, 0.12)"
  },
  header: {
    display: "flex",
    justifyContent: "space-between",
    gap: 14,
    alignItems: "flex-start",
    marginBottom: 16
  },
  kicker: {
    color: "#0369a1",
    fontSize: 12,
    fontWeight: 900,
    textTransform: "uppercase",
    letterSpacing: "0.08em"
  },
  title: {
    margin: "4px 0 0",
    fontSize: 28,
    lineHeight: 1.1,
    fontWeight: 900
  },
  muted: {
    margin: "6px 0 0",
    color: "#64748b",
    lineHeight: 1.4
  },
  status: {
    minHeight: 32,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    padding: "0 12px",
    borderRadius: 999,
    border: "1px solid",
    fontSize: 12,
    fontWeight: 900,
    whiteSpace: "nowrap"
  },
  grid: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: 10
  },
  infoCard: {
    padding: 12,
    borderRadius: 10,
    background: "#f8fafc",
    border: "1px solid #e2e8f0"
  },
  label: {
    color: "#64748b",
    fontSize: 11,
    fontWeight: 900,
    textTransform: "uppercase",
    letterSpacing: "0.06em"
  },
  value: {
    marginTop: 5,
    color: "#0f172a",
    fontWeight: 850,
    lineHeight: 1.35
  },
  noteBox: {
    marginTop: 12,
    padding: 12,
    borderRadius: 10,
    background: "#f1f5f9",
    border: "1px solid #e2e8f0"
  },
  noteText: {
    margin: "6px 0 0",
    color: "#334155",
    lineHeight: 1.45,
    whiteSpace: "pre-wrap"
  },
  photoPreview: {
    width: "100%",
    maxHeight: 460,
    marginTop: 8,
    borderRadius: 10,
    objectFit: "contain",
    background: "#e2e8f0",
    border: "1px solid #cbd5e1"
  }
};
const route15 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: dispatchDriverDetail,
  loader: loader$e
}, Symbol.toStringTag, { value: "Module" }));
function formatMoney(cents) {
  return `$${(Number(cents || 0) / 100).toFixed(2)}`;
}
const styles$3 = {
  page: {
    minHeight: "100vh",
    background: "radial-gradient(circle at top, #1f2937 0%, #111827 45%, #030712 100%)",
    color: "#f9fafb",
    padding: "20px 14px 120px",
    fontFamily: 'Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
    overflowX: "clip"
  },
  shell: {
    maxWidth: "760px",
    margin: "0 auto",
    display: "grid",
    gap: "16px"
  },
  card: {
    background: "rgba(17, 24, 39, 0.9)",
    border: "1px solid rgba(51, 65, 85, 0.95)",
    borderRadius: "20px",
    padding: "18px",
    boxShadow: "0 18px 34px rgba(2, 6, 23, 0.35)",
    backdropFilter: "blur(12px)"
  },
  title: {
    margin: 0,
    fontSize: "30px",
    fontWeight: 800,
    letterSpacing: "-0.03em"
  },
  subtitle: {
    margin: "8px 0 0",
    color: "#94a3b8",
    lineHeight: 1.5
  },
  sectionTitle: {
    margin: 0,
    fontSize: "18px",
    fontWeight: 800,
    color: "#f8fafc"
  },
  sectionSub: {
    margin: "6px 0 0",
    color: "#94a3b8",
    fontSize: "14px"
  },
  button: {
    display: "flex",
    flexDirection: "column",
    gap: "6px",
    textDecoration: "none",
    color: "#f8fafc",
    borderRadius: "18px",
    padding: "18px 16px",
    background: "linear-gradient(135deg, rgba(37, 99, 235, 0.22), rgba(15, 118, 110, 0.18))",
    border: "1px solid rgba(96, 165, 250, 0.28)",
    minHeight: "96px",
    justifyContent: "center"
  },
  buttonTitle: {
    fontSize: "17px",
    fontWeight: 800
  },
  buttonSub: {
    fontSize: "13px",
    color: "#bfdbfe",
    lineHeight: 1.45
  },
  smallButton: {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    width: "100%",
    minHeight: 46,
    borderRadius: 14,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "rgba(2, 6, 23, 0.86)",
    color: "#e2e8f0",
    textDecoration: "none",
    fontWeight: 700
  },
  bottomNav: {
    position: "fixed",
    left: 12,
    right: 12,
    bottom: "calc(env(safe-area-inset-bottom, 0px) + 12px)",
    zIndex: 30,
    display: "grid",
    gridTemplateColumns: "repeat(4, 1fr)",
    gap: 10,
    padding: "8px 10px",
    borderRadius: 20,
    background: "rgba(15, 23, 42, 0.96)",
    border: "1px solid rgba(30, 41, 59, 0.95)",
    boxShadow: "0 18px 38px rgba(2, 6, 23, 0.45)",
    backdropFilter: "blur(14px)"
  },
  navLink: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    gap: 4,
    minHeight: 56,
    borderRadius: 14,
    color: "#94a3b8",
    textDecoration: "none",
    fontWeight: 700,
    fontSize: "11px",
    letterSpacing: "0.01em"
  }
};
function navLinkStyle(active) {
  return {
    ...styles$3.navLink,
    color: active ? "#38bdf8" : "#94a3b8",
    background: active ? "rgba(14, 165, 233, 0.12)" : "transparent"
  };
}
function navIconStyle(active) {
  return {
    width: 24,
    height: 24,
    borderRadius: 999,
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    background: active ? "rgba(14, 165, 233, 0.18)" : "rgba(51, 65, 85, 0.35)",
    color: active ? "#38bdf8" : "#cbd5e1",
    fontSize: 12,
    lineHeight: 1
  };
}
async function loader$d({
  request
}) {
  const url = new URL(request.url);
  const isEmbeddedRoute = url.pathname.startsWith("/app/");
  const dashboardPath = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  if (url.searchParams.get("logout") === "1") {
    return redirect(dashboardPath, {
      headers: [["Set-Cookie", await userAuthCookie.serialize("", {
        maxAge: 0
      })], ["Set-Cookie", await adminQuoteCookie.serialize("", {
        maxAge: 0
      })]]
    });
  }
  const allowed = await hasAdminQuotePermissionAccess(request, "quoteTool");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  const recentQuotes = await getRecentCustomQuotes(8);
  return data({
    allowed,
    recentQuotes
  });
}
async function action$i({
  request
}) {
  const url = new URL(request.url);
  const allowed = await hasAdminQuotePermissionAccess(request, "quoteTool");
  if (!allowed) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  return data({
    allowed: true,
    recentQuotes: await getRecentCustomQuotes(8)
  });
}
const mobileDashboard = UNSAFE_withComponentProps(function MobileDashboardPage() {
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const location = useLocation();
  const allowed = (actionData == null ? void 0 : actionData.allowed) ?? loaderData.allowed;
  const recentQuotes = (actionData == null ? void 0 : actionData.recentQuotes) || loaderData.recentQuotes || [];
  const isEmbeddedRoute = location.pathname.startsWith("/app/");
  const quoteToolBase = isEmbeddedRoute ? "/app/custom-quote" : "/custom-quote";
  const reviewHref = isEmbeddedRoute ? "/app/quote-review" : "/quote-review";
  const dashboardHref = isEmbeddedRoute ? "/app/mobile" : "/mobile";
  const dispatchHref = isEmbeddedRoute ? "/app/classic" : "/classic";
  const driverHref = isEmbeddedRoute ? "/app/dispatch/driver" : "/dispatch/driver";
  const loginHref = `/login?next=${encodeURIComponent(location.pathname + location.search)}`;
  if (!allowed) {
    return /* @__PURE__ */ jsx("div", {
      style: styles$3.page,
      children: /* @__PURE__ */ jsx("div", {
        style: styles$3.shell,
        children: /* @__PURE__ */ jsxs("div", {
          style: styles$3.card,
          children: [/* @__PURE__ */ jsx("h1", {
            style: styles$3.title,
            children: "Mobile Dashboard"
          }), /* @__PURE__ */ jsx("p", {
            style: styles$3.subtitle,
            children: "Sign in with your contractor user account to open the mobile quote workspace."
          }), /* @__PURE__ */ jsx("a", {
            href: loginHref,
            style: {
              ...styles$3.smallButton,
              marginTop: 16,
              background: "linear-gradient(135deg, #2563eb, #14b8a6)",
              color: "#eff6ff",
              textDecoration: "none"
            },
            children: "Sign In"
          })]
        })
      })
    });
  }
  return /* @__PURE__ */ jsxs("div", {
    style: styles$3.page,
    children: [/* @__PURE__ */ jsxs("div", {
      style: styles$3.shell,
      children: [/* @__PURE__ */ jsxs("div", {
        style: {
          ...styles$3.card,
          position: "sticky",
          top: 10,
          zIndex: 18
        },
        children: [/* @__PURE__ */ jsx("h1", {
          style: styles$3.title,
          children: "Local Contractor Quote"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$3.subtitle,
          children: "Quick mobile entry point for building quotes, reviewing history, and jumping into the right pricing mode fast."
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$3.card,
        children: [/* @__PURE__ */ jsx("h2", {
          style: styles$3.sectionTitle,
          children: "Start A Quote"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$3.sectionSub,
          children: "Choose the quote mode you want before opening the full builder."
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            display: "grid",
            gap: 12,
            marginTop: 14
          },
          children: [/* @__PURE__ */ jsxs("a", {
            href: `${quoteToolBase}?audience=customer`,
            style: styles$3.button,
            children: [/* @__PURE__ */ jsx("span", {
              style: styles$3.buttonTitle,
              children: "Customer Quote"
            }), /* @__PURE__ */ jsx("span", {
              style: styles$3.buttonSub,
              children: "Standard customer pricing and normal quote flow."
            })]
          }), /* @__PURE__ */ jsxs("a", {
            href: `${quoteToolBase}?audience=contractor&tier=tier1`,
            style: styles$3.button,
            children: [/* @__PURE__ */ jsx("span", {
              style: styles$3.buttonTitle,
              children: "Contractor Quote"
            }), /* @__PURE__ */ jsx("span", {
              style: styles$3.buttonSub,
              children: "Open the builder with contractor pricing ready to go."
            })]
          }), /* @__PURE__ */ jsxs("a", {
            href: `${quoteToolBase}?audience=custom`,
            style: styles$3.button,
            children: [/* @__PURE__ */ jsx("span", {
              style: styles$3.buttonTitle,
              children: "Custom Quote"
            }), /* @__PURE__ */ jsx("span", {
              style: styles$3.buttonSub,
              children: "Editable pricing, shipping math, and manual adjustments."
            })]
          })]
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$3.card,
        children: [/* @__PURE__ */ jsx("h2", {
          style: styles$3.sectionTitle,
          children: "Quick Actions"
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            display: "grid",
            gap: 12,
            marginTop: 14
          },
          children: [/* @__PURE__ */ jsx("a", {
            href: quoteToolBase,
            style: styles$3.smallButton,
            children: "Open Quote Tool"
          }), /* @__PURE__ */ jsx("a", {
            href: reviewHref,
            style: styles$3.smallButton,
            children: "Review Quotes"
          }), /* @__PURE__ */ jsx("a", {
            href: dispatchHref,
            style: styles$3.smallButton,
            children: "Dispatch"
          }), /* @__PURE__ */ jsx("a", {
            href: driverHref,
            style: styles$3.smallButton,
            children: "Driver Route"
          }), /* @__PURE__ */ jsx("a", {
            href: `${dashboardHref}?logout=1`,
            style: styles$3.smallButton,
            children: "Log Out"
          })]
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$3.card,
        children: [/* @__PURE__ */ jsx("h2", {
          style: styles$3.sectionTitle,
          children: "Recent Quotes"
        }), /* @__PURE__ */ jsx("p", {
          style: styles$3.sectionSub,
          children: "Open review with a recent quote ready to inspect."
        }), /* @__PURE__ */ jsx("div", {
          style: {
            display: "grid",
            gap: 12,
            marginTop: 14
          },
          children: recentQuotes.length === 0 ? /* @__PURE__ */ jsx("div", {
            style: {
              color: "#94a3b8"
            },
            children: "No recent quotes yet."
          }) : recentQuotes.map((quote) => /* @__PURE__ */ jsxs("a", {
            href: `${reviewHref}?quote=${encodeURIComponent(quote.id)}`,
            style: {
              ...styles$3.button,
              minHeight: "unset",
              overflowWrap: "anywhere"
            },
            children: [/* @__PURE__ */ jsx("span", {
              style: styles$3.buttonTitle,
              children: quote.customer_name || quote.customer_email || "Unnamed quote"
            }), /* @__PURE__ */ jsxs("span", {
              style: styles$3.buttonSub,
              children: [formatMoney(quote.quote_total_cents), " · ", quote.city, ", ", quote.province]
            }), /* @__PURE__ */ jsx("span", {
              style: {
                ...styles$3.buttonSub,
                color: "#94a3b8"
              },
              children: new Date(quote.created_at).toLocaleString()
            })]
          }, quote.id))
        })]
      })]
    }), /* @__PURE__ */ jsxs("div", {
      style: styles$3.bottomNav,
      children: [/* @__PURE__ */ jsxs("a", {
        href: dashboardHref,
        style: navLinkStyle(true),
        children: [/* @__PURE__ */ jsx("span", {
          style: navIconStyle(true),
          children: "D"
        }), /* @__PURE__ */ jsx("span", {
          children: "Dashboard"
        })]
      }), /* @__PURE__ */ jsxs("a", {
        href: quoteToolBase,
        style: navLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: navIconStyle(false),
          children: "Q"
        }), /* @__PURE__ */ jsx("span", {
          children: "Quote Tool"
        })]
      }), /* @__PURE__ */ jsxs("a", {
        href: reviewHref,
        style: navLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: navIconStyle(false),
          children: "R"
        }), /* @__PURE__ */ jsx("span", {
          children: "Review"
        })]
      }), /* @__PURE__ */ jsxs("a", {
        href: dispatchHref,
        style: navLinkStyle(false),
        children: [/* @__PURE__ */ jsx("span", {
          style: navIconStyle(false),
          children: "X"
        }), /* @__PURE__ */ jsx("span", {
          children: "Dispatch"
        })]
      })]
    })]
  });
});
const route16 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$i,
  default: mobileDashboard,
  loader: loader$d
}, Symbol.toStringTag, { value: "Module" }));
async function loader$c({
  request
}) {
  const url = new URL(request.url);
  if (url.searchParams.get("logout") === "1") {
    return redirect("/login", {
      headers: [["Set-Cookie", await userAuthCookie.serialize("", {
        maxAge: 0
      })], ["Set-Cookie", await adminQuoteCookie.serialize("", {
        maxAge: 0
      })]]
    });
  }
  const user = await getCurrentUser(request);
  const next = url.searchParams.get("next") || "/custom-quote";
  if (user) return redirect(next);
  return data({
    next
  });
}
async function action$h({
  request
}) {
  const form = await request.formData();
  const email = String(form.get("email") || "").trim();
  const password = String(form.get("password") || "");
  const next = String(form.get("next") || "/custom-quote");
  try {
    const session = await signInContractorUser(email, password);
    return redirect(session.profile.mustChangePassword ? `/change-password?next=${encodeURIComponent(next)}` : next, {
      headers: {
        "Set-Cookie": session.cookie
      }
    });
  } catch (error) {
    return data({
      next,
      error: error instanceof Error ? error.message : "Unable to sign in."
    }, {
      status: 400
    });
  }
}
const login = UNSAFE_withComponentProps(function LoginPage() {
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const navigation = useNavigation();
  const isSubmitting = navigation.state === "submitting";
  return /* @__PURE__ */ jsx("main", {
    style: styles$2.page,
    children: /* @__PURE__ */ jsxs("section", {
      style: styles$2.card,
      children: [/* @__PURE__ */ jsx("img", {
        src: "/green-hills-logo.png",
        alt: "Green Hills Supply",
        style: styles$2.logo
      }), /* @__PURE__ */ jsx("p", {
        style: styles$2.kicker,
        children: "Contractor Workspace"
      }), /* @__PURE__ */ jsx("h1", {
        style: styles$2.title,
        children: "Sign in"
      }), /* @__PURE__ */ jsx("p", {
        style: styles$2.subtitle,
        children: "Use your Supabase user account to open the quote, dispatch, and driver tools."
      }), /* @__PURE__ */ jsxs(Form, {
        method: "post",
        style: styles$2.form,
        children: [/* @__PURE__ */ jsx("input", {
          type: "hidden",
          name: "next",
          value: (actionData == null ? void 0 : actionData.next) || loaderData.next
        }), /* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("label", {
            style: styles$2.label,
            children: "Email"
          }), /* @__PURE__ */ jsx("input", {
            name: "email",
            type: "email",
            autoComplete: "email",
            required: true,
            style: styles$2.input
          })]
        }), /* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("label", {
            style: styles$2.label,
            children: "Password"
          }), /* @__PURE__ */ jsx("input", {
            name: "password",
            type: "password",
            autoComplete: "current-password",
            required: true,
            style: styles$2.input
          })]
        }), (actionData == null ? void 0 : actionData.error) ? /* @__PURE__ */ jsx("div", {
          style: styles$2.error,
          children: actionData.error
        }) : null, /* @__PURE__ */ jsx("button", {
          type: "submit",
          style: styles$2.button,
          disabled: isSubmitting,
          children: isSubmitting ? "Signing in..." : "Sign In"
        })]
      }), /* @__PURE__ */ jsxs("div", {
        style: styles$2.links,
        children: [/* @__PURE__ */ jsx(Link, {
          to: "/custom-quote",
          style: styles$2.link,
          children: "Quote Tool"
        }), /* @__PURE__ */ jsx(Link, {
          to: "/classic",
          style: styles$2.link,
          children: "Dispatch"
        })]
      })]
    })
  });
});
const styles$2 = {
  page: {
    minHeight: "100vh",
    display: "grid",
    placeItems: "center",
    padding: 18,
    background: "radial-gradient(circle at top left, rgba(132, 204, 22, 0.2), transparent 28%), radial-gradient(circle at bottom right, rgba(14, 165, 233, 0.2), transparent 30%), linear-gradient(180deg, #0f172a, #020617)",
    color: "#f8fafc",
    fontFamily: '"Plus Jakarta Sans", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif'
  },
  card: {
    width: "min(440px, 100%)",
    padding: 28,
    borderRadius: 28,
    border: "1px solid rgba(132, 204, 22, 0.28)",
    background: "rgba(15, 23, 42, 0.92)",
    boxShadow: "0 34px 90px rgba(2, 6, 23, 0.5)"
  },
  logo: {
    width: 132,
    height: "auto",
    display: "block",
    marginBottom: 18
  },
  kicker: {
    margin: 0,
    color: "#38bdf8",
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: "0.1em",
    textTransform: "uppercase"
  },
  title: {
    margin: "8px 0 0",
    fontSize: 36,
    lineHeight: 1
  },
  subtitle: {
    margin: "10px 0 0",
    color: "#cbd5e1",
    lineHeight: 1.5
  },
  form: {
    display: "grid",
    gap: 14,
    marginTop: 22
  },
  label: {
    display: "block",
    marginBottom: 6,
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 900,
    textTransform: "uppercase",
    letterSpacing: "0.06em"
  },
  input: {
    width: "100%",
    minHeight: 48,
    boxSizing: "border-box",
    borderRadius: 14,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "#020617",
    color: "#f8fafc",
    padding: "0 14px",
    fontSize: 16
  },
  error: {
    padding: 12,
    borderRadius: 14,
    border: "1px solid rgba(248, 113, 113, 0.4)",
    background: "rgba(127, 29, 29, 0.35)",
    color: "#fecaca",
    fontWeight: 800
  },
  button: {
    minHeight: 52,
    border: "none",
    borderRadius: 16,
    background: "linear-gradient(135deg, #84cc16, #22c55e)",
    color: "#052e16",
    fontWeight: 950,
    cursor: "pointer"
  },
  links: {
    display: "flex",
    gap: 10,
    marginTop: 18,
    flexWrap: "wrap"
  },
  link: {
    color: "#93c5fd",
    fontWeight: 800,
    textDecoration: "none"
  }
};
const route17 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$h,
  default: login,
  loader: loader$c
}, Symbol.toStringTag, { value: "Module" }));
async function loader$b({
  request
}) {
  const url = new URL(request.url);
  const user = await getCurrentUser(request);
  if (!user) {
    return redirect(`/login?next=${encodeURIComponent(url.pathname + url.search)}`);
  }
  return data({
    user,
    next: url.searchParams.get("next") || "/custom-quote"
  });
}
async function action$g({
  request
}) {
  const form = await request.formData();
  const password = String(form.get("password") || "");
  const confirmPassword = String(form.get("confirmPassword") || "");
  const next = String(form.get("next") || "/custom-quote");
  if (password.length < 8) {
    return data({
      next,
      error: "Password must be at least 8 characters."
    }, {
      status: 400
    });
  }
  if (password !== confirmPassword) {
    return data({
      next,
      error: "Passwords do not match."
    }, {
      status: 400
    });
  }
  try {
    const cookie = await changeCurrentUserPassword(request, password);
    return redirect(next, {
      headers: {
        "Set-Cookie": cookie
      }
    });
  } catch (error) {
    return data({
      next,
      error: error instanceof Error ? error.message : "Unable to change password."
    }, {
      status: 400
    });
  }
}
const changePassword = UNSAFE_withComponentProps(function ChangePasswordPage() {
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const navigation = useNavigation();
  const isSubmitting = navigation.state === "submitting";
  return /* @__PURE__ */ jsx("main", {
    style: styles$1.page,
    children: /* @__PURE__ */ jsxs("section", {
      style: styles$1.card,
      children: [/* @__PURE__ */ jsx("img", {
        src: "/green-hills-logo.png",
        alt: "Green Hills Supply",
        style: styles$1.logo
      }), /* @__PURE__ */ jsx("p", {
        style: styles$1.kicker,
        children: "Account Security"
      }), /* @__PURE__ */ jsx("h1", {
        style: styles$1.title,
        children: "Change Password"
      }), /* @__PURE__ */ jsx("p", {
        style: styles$1.subtitle,
        children: loaderData.user.mustChangePassword ? "This is your first login with a temporary password, so choose a new password to continue." : "Update your password whenever you need to."
      }), /* @__PURE__ */ jsxs(Form, {
        method: "post",
        style: styles$1.form,
        children: [/* @__PURE__ */ jsx("input", {
          type: "hidden",
          name: "next",
          value: (actionData == null ? void 0 : actionData.next) || loaderData.next
        }), /* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("label", {
            style: styles$1.label,
            children: "New Password"
          }), /* @__PURE__ */ jsx("input", {
            name: "password",
            type: "password",
            autoComplete: "new-password",
            required: true,
            style: styles$1.input
          })]
        }), /* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("label", {
            style: styles$1.label,
            children: "Confirm Password"
          }), /* @__PURE__ */ jsx("input", {
            name: "confirmPassword",
            type: "password",
            autoComplete: "new-password",
            required: true,
            style: styles$1.input
          })]
        }), (actionData == null ? void 0 : actionData.error) ? /* @__PURE__ */ jsx("div", {
          style: styles$1.error,
          children: actionData.error
        }) : null, /* @__PURE__ */ jsx("button", {
          type: "submit",
          style: styles$1.button,
          disabled: isSubmitting,
          children: isSubmitting ? "Saving..." : "Save Password"
        })]
      }), !loaderData.user.mustChangePassword ? /* @__PURE__ */ jsx(Link, {
        to: loaderData.next,
        style: styles$1.link,
        children: "Back to app"
      }) : null]
    })
  });
});
const styles$1 = {
  page: {
    minHeight: "100vh",
    display: "grid",
    placeItems: "center",
    padding: 18,
    background: "radial-gradient(circle at top left, rgba(132, 204, 22, 0.2), transparent 28%), radial-gradient(circle at bottom right, rgba(14, 165, 233, 0.2), transparent 30%), linear-gradient(180deg, #0f172a, #020617)",
    color: "#f8fafc",
    fontFamily: '"Plus Jakarta Sans", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif'
  },
  card: {
    width: "min(440px, 100%)",
    padding: 28,
    borderRadius: 28,
    border: "1px solid rgba(132, 204, 22, 0.28)",
    background: "rgba(15, 23, 42, 0.92)",
    boxShadow: "0 34px 90px rgba(2, 6, 23, 0.5)"
  },
  logo: {
    width: 132,
    height: "auto",
    display: "block",
    marginBottom: 18
  },
  kicker: {
    margin: 0,
    color: "#38bdf8",
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: "0.1em",
    textTransform: "uppercase"
  },
  title: {
    margin: "8px 0 0",
    fontSize: 36,
    lineHeight: 1
  },
  subtitle: {
    margin: "10px 0 0",
    color: "#cbd5e1",
    lineHeight: 1.5
  },
  form: {
    display: "grid",
    gap: 14,
    marginTop: 22
  },
  label: {
    display: "block",
    marginBottom: 6,
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 900,
    textTransform: "uppercase",
    letterSpacing: "0.06em"
  },
  input: {
    width: "100%",
    minHeight: 48,
    boxSizing: "border-box",
    borderRadius: 14,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "#020617",
    color: "#f8fafc",
    padding: "0 14px",
    fontSize: 16
  },
  error: {
    padding: 12,
    borderRadius: 14,
    border: "1px solid rgba(248, 113, 113, 0.4)",
    background: "rgba(127, 29, 29, 0.35)",
    color: "#fecaca",
    fontWeight: 800
  },
  button: {
    minHeight: 52,
    border: "none",
    borderRadius: 16,
    background: "linear-gradient(135deg, #84cc16, #22c55e)",
    color: "#052e16",
    fontWeight: 950,
    cursor: "pointer"
  },
  link: {
    display: "inline-flex",
    marginTop: 16,
    color: "#93c5fd",
    fontWeight: 850,
    textDecoration: "none"
  }
};
const route18 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$g,
  default: changePassword,
  loader: loader$b
}, Symbol.toStringTag, { value: "Module" }));
async function loader$a({
  request
}) {
  const currentUser = await requireUserPermission(request, "manageUsers");
  const [users, auditEvents, classicColumnSettings, employees] = await Promise.all([listAppUsers(), currentUser.permissions.includes("auditLog") ? listAuditEvents(150) : [], getClassicColumnSettings(), getDispatchEmployees().catch(() => [])]);
  return data({
    currentUser,
    users,
    auditEvents,
    employees,
    allPermissions,
    permissionLabels,
    classicColumnSettings,
    classicColumnOptions
  });
}
async function action$f({
  request
}) {
  const currentUser = await requireUserPermission(request, "manageUsers");
  const form = await request.formData();
  const intent = String(form.get("intent") || "");
  const permissions = form.getAll("permissions").map(String);
  const loadSettingsData = async () => ({
    ...Object.fromEntries(await Promise.all([listAppUsers().then((users) => ["users", users]), (currentUser.permissions.includes("auditLog") ? listAuditEvents(150) : Promise.resolve([])).then((auditEvents) => ["auditEvents", auditEvents]), getClassicColumnSettings().then((classicColumnSettings) => ["classicColumnSettings", classicColumnSettings]), getDispatchEmployees().catch(() => []).then((employees) => ["employees", employees])]))
  });
  try {
    if (intent === "create-user") {
      const createdUser = await createAppUser({
        email: String(form.get("email") || "").trim(),
        password: String(form.get("password") || ""),
        name: String(form.get("name") || "").trim(),
        role: String(form.get("role") || "user").trim(),
        permissions,
        driverEmployeeId: String(form.get("driverEmployeeId") || "").trim() || null
      });
      await logAuditEvent({
        actor: currentUser,
        action: "create_user",
        targetType: "user",
        targetId: createdUser.id,
        targetLabel: createdUser.email,
        details: {
          name: createdUser.name,
          role: createdUser.role,
          permissions: createdUser.permissions,
          isActive: createdUser.isActive,
          driverEmployeeId: createdUser.driverEmployeeId || null,
          temporaryPasswordRequired: true
        }
      });
      return data({
        ok: true,
        message: "User created.",
        ...await loadSettingsData()
      });
    }
    if (intent === "update-user") {
      const updatedUser = await updateAppUserProfile({
        id: String(form.get("userId") || ""),
        name: String(form.get("name") || "").trim(),
        role: String(form.get("role") || "user").trim(),
        permissions,
        driverEmployeeId: String(form.get("driverEmployeeId") || "").trim() || null,
        isActive: form.get("isActive") === "on"
      });
      await logAuditEvent({
        actor: currentUser,
        action: "update_user",
        targetType: "user",
        targetId: updatedUser.id,
        targetLabel: updatedUser.email,
        details: {
          name: updatedUser.name,
          role: updatedUser.role,
          permissions: updatedUser.permissions,
          isActive: updatedUser.isActive,
          driverEmployeeId: updatedUser.driverEmployeeId || null
        }
      });
      return data({
        ok: true,
        message: "User updated.",
        ...await loadSettingsData()
      });
    }
    if (intent === "save-classic-columns") {
      const nextSettings = Object.keys(classicColumnOptions).reduce((settings2, table) => {
        settings2[table] = form.getAll(`classicColumns.${table}`).map(String).filter((key) => classicColumnOptions[table].some((option) => option.key === key));
        if (!settings2[table].length) {
          settings2[table] = defaultClassicColumnSettings[table];
        }
        return settings2;
      }, {});
      const savedSettings = await saveClassicColumnSettings(nextSettings);
      await logAuditEvent({
        actor: currentUser,
        action: "update_classic_columns",
        targetType: "settings",
        targetId: "classic_columns",
        targetLabel: "Classic Columns",
        details: savedSettings
      });
      return data({
        ok: true,
        message: "Classic page columns updated.",
        ...await loadSettingsData()
      });
    }
    return data({
      ok: false,
      message: "Unknown settings action."
    }, {
      status: 400
    });
  } catch (error) {
    return data({
      ok: false,
      message: error instanceof Error ? error.message : "Unable to save user settings.",
      ...await loadSettingsData()
    }, {
      status: 400
    });
  }
}
const settings = UNSAFE_withComponentProps(function SettingsPage() {
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const navigation = useNavigation();
  const users = (actionData == null ? void 0 : actionData.users) || loaderData.users;
  const auditEvents = (actionData == null ? void 0 : actionData.auditEvents) || loaderData.auditEvents;
  const employees = (actionData == null ? void 0 : actionData.employees) || loaderData.employees || [];
  const classicColumns = (actionData == null ? void 0 : actionData.classicColumnSettings) || loaderData.classicColumnSettings || defaultClassicColumnSettings;
  const isSubmitting = navigation.state === "submitting";
  const canAccess = (permission) => {
    var _a2;
    return (_a2 = loaderData.currentUser.permissions) == null ? void 0 : _a2.includes(permission);
  };
  return /* @__PURE__ */ jsx("main", {
    style: styles.page,
    children: /* @__PURE__ */ jsxs("div", {
      style: styles.shell,
      children: [/* @__PURE__ */ jsxs("header", {
        style: styles.header,
        children: [/* @__PURE__ */ jsxs("div", {
          children: [/* @__PURE__ */ jsx("p", {
            style: styles.kicker,
            children: "Admin Settings"
          }), /* @__PURE__ */ jsx("h1", {
            style: styles.title,
            children: "Users, Roles & Privileges"
          }), /* @__PURE__ */ jsx("p", {
            style: styles.subtitle,
            children: "Control which parts of the contractor app each user can see and what they can do."
          })]
        }), /* @__PURE__ */ jsxs("nav", {
          style: styles.nav,
          children: [canAccess("quoteTool") ? /* @__PURE__ */ jsx(Link, {
            to: "/custom-quote",
            style: styles.navButton,
            children: "Quote Tool"
          }) : null, canAccess("dispatch") ? /* @__PURE__ */ jsx(Link, {
            to: "/classic",
            style: styles.navButton,
            children: "Dispatch"
          }) : null, /* @__PURE__ */ jsx(Link, {
            to: "/change-password",
            style: styles.navButton,
            children: "Change Password"
          }), /* @__PURE__ */ jsx(Link, {
            to: "/login?logout=1",
            style: styles.navButton,
            children: "Log Out"
          })]
        })]
      }), (actionData == null ? void 0 : actionData.message) ? /* @__PURE__ */ jsx("div", {
        style: actionData.ok ? styles.success : styles.error,
        children: actionData.message
      }) : null, /* @__PURE__ */ jsxs("section", {
        style: styles.panel,
        children: [/* @__PURE__ */ jsx("h2", {
          style: styles.panelTitle,
          children: "Create User"
        }), /* @__PURE__ */ jsxs(Form, {
          method: "post",
          style: styles.form,
          children: [/* @__PURE__ */ jsx("input", {
            type: "hidden",
            name: "intent",
            value: "create-user"
          }), /* @__PURE__ */ jsxs("div", {
            style: styles.formGrid,
            children: [/* @__PURE__ */ jsx(Field, {
              label: "Name",
              name: "name"
            }), /* @__PURE__ */ jsx(Field, {
              label: "Email",
              name: "email",
              type: "email"
            }), /* @__PURE__ */ jsx(Field, {
              label: "Temporary Password",
              name: "password",
              type: "password"
            }), /* @__PURE__ */ jsx(RoleSelect, {
              defaultValue: "user"
            }), /* @__PURE__ */ jsx(EmployeeSelect, {
              employees,
              defaultValue: ""
            })]
          }), /* @__PURE__ */ jsx(PermissionGrid, {
            selected: ["quoteTool"]
          }), /* @__PURE__ */ jsx("button", {
            type: "submit",
            style: styles.primaryButton,
            disabled: isSubmitting,
            children: "Create User"
          })]
        })]
      }), /* @__PURE__ */ jsx("section", {
        style: styles.userGrid,
        children: users.map((user) => /* @__PURE__ */ jsxs("article", {
          style: styles.panel,
          children: [/* @__PURE__ */ jsxs("div", {
            style: styles.userHeader,
            children: [/* @__PURE__ */ jsxs("div", {
              children: [/* @__PURE__ */ jsx("h2", {
                style: styles.panelTitle,
                children: user.name || user.email
              }), /* @__PURE__ */ jsx("p", {
                style: styles.subtitle,
                children: user.email
              })]
            }), /* @__PURE__ */ jsx("span", {
              style: user.isActive ? styles.activeBadge : styles.disabledBadge,
              children: user.isActive ? "Active" : "Disabled"
            })]
          }), /* @__PURE__ */ jsxs(Form, {
            method: "post",
            style: styles.form,
            children: [/* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "intent",
              value: "update-user"
            }), /* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "userId",
              value: user.id
            }), /* @__PURE__ */ jsxs("div", {
              style: styles.formGrid,
              children: [/* @__PURE__ */ jsx(Field, {
                label: "Name",
                name: "name",
                defaultValue: user.name
              }), /* @__PURE__ */ jsx(RoleSelect, {
                defaultValue: user.role
              }), /* @__PURE__ */ jsx(EmployeeSelect, {
                employees,
                defaultValue: user.driverEmployeeId || ""
              })]
            }), /* @__PURE__ */ jsxs("label", {
              style: styles.checkboxLine,
              children: [/* @__PURE__ */ jsx("input", {
                type: "checkbox",
                name: "isActive",
                defaultChecked: user.isActive
              }), "User is active"]
            }), /* @__PURE__ */ jsx(PermissionGrid, {
              selected: user.permissions
            }), /* @__PURE__ */ jsx("button", {
              type: "submit",
              style: styles.secondaryButton,
              disabled: isSubmitting,
              children: "Save User"
            })]
          })]
        }, user.id))
      }), /* @__PURE__ */ jsxs("section", {
        style: styles.panel,
        children: [/* @__PURE__ */ jsx("div", {
          style: styles.userHeader,
          children: /* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsx("h2", {
              style: styles.panelTitle,
              children: "Classic Page Columns"
            }), /* @__PURE__ */ jsx("p", {
              style: styles.subtitle,
              children: "Choose which fields show on each Classic tab/table. Route controls and assign buttons stay visible so dispatch remains usable."
            })]
          })
        }), /* @__PURE__ */ jsxs(Form, {
          method: "post",
          style: styles.form,
          children: [/* @__PURE__ */ jsx("input", {
            type: "hidden",
            name: "intent",
            value: "save-classic-columns"
          }), /* @__PURE__ */ jsx("div", {
            style: styles.columnSettingsGrid,
            children: Object.keys(classicColumnOptions).map((table) => /* @__PURE__ */ jsxs("section", {
              style: styles.columnSettingsCard,
              children: [/* @__PURE__ */ jsx("h3", {
                style: styles.columnSettingsTitle,
                children: formatClassicTableName(table)
              }), /* @__PURE__ */ jsx("div", {
                style: styles.permissionGrid,
                children: classicColumnOptions[table].map((option) => /* @__PURE__ */ jsxs("label", {
                  style: styles.permissionItem,
                  children: [/* @__PURE__ */ jsx("input", {
                    type: "checkbox",
                    name: `classicColumns.${table}`,
                    value: option.key,
                    defaultChecked: (classicColumns[table] || []).includes(option.key)
                  }), /* @__PURE__ */ jsx("span", {
                    children: option.label
                  })]
                }, option.key))
              })]
            }, table))
          }), /* @__PURE__ */ jsx("button", {
            type: "submit",
            style: styles.primaryButton,
            disabled: isSubmitting,
            children: "Save Classic Columns"
          })]
        })]
      }), canAccess("auditLog") ? /* @__PURE__ */ jsxs("section", {
        style: styles.panel,
        children: [/* @__PURE__ */ jsxs("div", {
          style: styles.userHeader,
          children: [/* @__PURE__ */ jsxs("div", {
            children: [/* @__PURE__ */ jsx("h2", {
              style: styles.panelTitle,
              children: "Audit Log"
            }), /* @__PURE__ */ jsx("p", {
              style: styles.subtitle,
              children: "Recent user and settings activity, including who performed each action."
            })]
          }), /* @__PURE__ */ jsxs("span", {
            style: styles.auditCountBadge,
            children: [auditEvents.length, " Events"]
          })]
        }), /* @__PURE__ */ jsx("div", {
          style: styles.auditList,
          children: auditEvents.length ? auditEvents.map((event) => /* @__PURE__ */ jsxs("article", {
            style: styles.auditItem,
            children: [/* @__PURE__ */ jsxs("div", {
              style: styles.auditHeader,
              children: [/* @__PURE__ */ jsxs("div", {
                children: [/* @__PURE__ */ jsx("strong", {
                  style: styles.auditAction,
                  children: formatAuditAction(event.action)
                }), /* @__PURE__ */ jsxs("p", {
                  style: styles.auditMeta,
                  children: [event.actorName, event.actorEmail ? ` (${event.actorEmail})` : "", " on", " ", formatAuditDate(event.createdAt)]
                })]
              }), /* @__PURE__ */ jsxs("span", {
                style: styles.auditTarget,
                children: [event.targetType, event.targetLabel ? `: ${event.targetLabel}` : ""]
              })]
            }), Object.keys(event.details || {}).length ? /* @__PURE__ */ jsx("pre", {
              style: styles.auditDetails,
              children: JSON.stringify(event.details, null, 2)
            }) : null]
          }, event.id)) : /* @__PURE__ */ jsx("p", {
            style: styles.subtitle,
            children: "No audit events have been recorded yet."
          })
        })]
      }) : null]
    })
  });
});
function formatAuditAction(action2) {
  return action2.split("_").filter(Boolean).map((part) => part.charAt(0).toUpperCase() + part.slice(1)).join(" ");
}
function formatAuditDate(value) {
  if (!value) return "Unknown time";
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return value;
  return date.toLocaleString();
}
function formatClassicTableName(table) {
  if (table === "sites") return "Route Sites";
  return table.charAt(0).toUpperCase() + table.slice(1);
}
function Field({
  label,
  name,
  type = "text",
  defaultValue = ""
}) {
  return /* @__PURE__ */ jsxs("div", {
    children: [/* @__PURE__ */ jsx("label", {
      style: styles.label,
      children: label
    }), /* @__PURE__ */ jsx("input", {
      name,
      type,
      defaultValue,
      style: styles.input,
      required: true
    })]
  });
}
function RoleSelect({
  defaultValue
}) {
  return /* @__PURE__ */ jsxs("div", {
    children: [/* @__PURE__ */ jsx("label", {
      style: styles.label,
      children: "Role"
    }), /* @__PURE__ */ jsxs("select", {
      name: "role",
      defaultValue,
      style: styles.input,
      children: [/* @__PURE__ */ jsx("option", {
        value: "admin",
        children: "Admin"
      }), /* @__PURE__ */ jsx("option", {
        value: "dispatcher",
        children: "Dispatcher"
      }), /* @__PURE__ */ jsx("option", {
        value: "driver",
        children: "Driver"
      }), /* @__PURE__ */ jsx("option", {
        value: "loader",
        children: "Loader"
      }), /* @__PURE__ */ jsx("option", {
        value: "sales",
        children: "Sales"
      }), /* @__PURE__ */ jsx("option", {
        value: "user",
        children: "User"
      })]
    })]
  });
}
function EmployeeSelect({
  employees,
  defaultValue
}) {
  const driverEmployees = employees.filter((employee) => employee.isActive !== false && employee.role === "driver").sort((a, b) => a.name.localeCompare(b.name));
  return /* @__PURE__ */ jsxs("div", {
    children: [/* @__PURE__ */ jsx("label", {
      style: styles.label,
      children: "Driver Employee Match"
    }), /* @__PURE__ */ jsxs("select", {
      name: "driverEmployeeId",
      defaultValue,
      style: styles.input,
      children: [/* @__PURE__ */ jsx("option", {
        value: "",
        children: "No driver restriction"
      }), driverEmployees.map((employee) => /* @__PURE__ */ jsx("option", {
        value: employee.id,
        children: employee.name
      }, employee.id))]
    })]
  });
}
function PermissionGrid({
  selected
}) {
  const selectedSet = new Set(selected);
  return /* @__PURE__ */ jsx("div", {
    style: styles.permissionGrid,
    children: allPermissions.map((permission) => /* @__PURE__ */ jsxs("label", {
      style: styles.permissionItem,
      children: [/* @__PURE__ */ jsx("input", {
        type: "checkbox",
        name: "permissions",
        value: permission,
        defaultChecked: selectedSet.has(permission)
      }), /* @__PURE__ */ jsx("span", {
        children: permissionLabels[permission]
      })]
    }, permission))
  });
}
const styles = {
  page: {
    minHeight: "100vh",
    padding: 18,
    background: "radial-gradient(circle at top left, rgba(14, 165, 233, 0.16), transparent 28%), linear-gradient(180deg, #0f172a, #020617)",
    color: "#f8fafc",
    fontFamily: '"Plus Jakarta Sans", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif'
  },
  shell: {
    maxWidth: 1280,
    margin: "0 auto",
    display: "grid",
    gap: 18
  },
  header: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "flex-start",
    gap: 18,
    padding: 22,
    borderRadius: 26,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "rgba(15, 23, 42, 0.88)"
  },
  kicker: {
    margin: 0,
    color: "#38bdf8",
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: "0.1em",
    textTransform: "uppercase"
  },
  title: {
    margin: "8px 0 0",
    fontSize: 36,
    lineHeight: 1
  },
  subtitle: {
    margin: "8px 0 0",
    color: "#94a3b8",
    lineHeight: 1.45
  },
  nav: {
    display: "flex",
    gap: 10,
    flexWrap: "wrap",
    justifyContent: "flex-end"
  },
  navButton: {
    minHeight: 38,
    display: "inline-flex",
    alignItems: "center",
    padding: "0 12px",
    borderRadius: 999,
    border: "1px solid rgba(56, 189, 248, 0.35)",
    color: "#e0f2fe",
    textDecoration: "none",
    fontWeight: 850
  },
  panel: {
    padding: 20,
    borderRadius: 24,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "rgba(15, 23, 42, 0.92)",
    boxShadow: "0 24px 64px rgba(2, 6, 23, 0.36)"
  },
  panelTitle: {
    margin: 0,
    fontSize: 22
  },
  form: {
    display: "grid",
    gap: 14,
    marginTop: 16
  },
  formGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
    gap: 12
  },
  userGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(360px, 1fr))",
    gap: 16
  },
  userHeader: {
    display: "flex",
    justifyContent: "space-between",
    gap: 12,
    alignItems: "flex-start"
  },
  label: {
    display: "block",
    marginBottom: 6,
    color: "#cbd5e1",
    fontSize: 12,
    fontWeight: 900,
    textTransform: "uppercase",
    letterSpacing: "0.06em"
  },
  input: {
    width: "100%",
    minHeight: 44,
    boxSizing: "border-box",
    borderRadius: 12,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "#020617",
    color: "#f8fafc",
    padding: "0 12px"
  },
  permissionGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))",
    gap: 10
  },
  columnSettingsGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))",
    gap: 14
  },
  columnSettingsCard: {
    display: "grid",
    gap: 12,
    padding: 14,
    borderRadius: 18,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "rgba(2, 6, 23, 0.42)"
  },
  columnSettingsTitle: {
    margin: 0,
    fontSize: 16
  },
  permissionItem: {
    minHeight: 42,
    display: "flex",
    alignItems: "center",
    gap: 9,
    padding: "8px 10px",
    borderRadius: 12,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "rgba(2, 6, 23, 0.72)",
    color: "#e2e8f0",
    fontWeight: 800
  },
  checkboxLine: {
    display: "flex",
    alignItems: "center",
    gap: 9,
    color: "#e2e8f0",
    fontWeight: 850
  },
  primaryButton: {
    minHeight: 48,
    border: "none",
    borderRadius: 14,
    background: "linear-gradient(135deg, #84cc16, #22c55e)",
    color: "#052e16",
    fontWeight: 950,
    cursor: "pointer"
  },
  secondaryButton: {
    minHeight: 46,
    borderRadius: 14,
    border: "1px solid rgba(56, 189, 248, 0.45)",
    background: "rgba(14, 165, 233, 0.14)",
    color: "#e0f2fe",
    fontWeight: 950,
    cursor: "pointer"
  },
  success: {
    padding: 14,
    borderRadius: 16,
    border: "1px solid rgba(34, 197, 94, 0.4)",
    background: "rgba(20, 83, 45, 0.42)",
    color: "#bbf7d0",
    fontWeight: 850
  },
  error: {
    padding: 14,
    borderRadius: 16,
    border: "1px solid rgba(248, 113, 113, 0.4)",
    background: "rgba(127, 29, 29, 0.35)",
    color: "#fecaca",
    fontWeight: 850
  },
  activeBadge: {
    padding: "6px 10px",
    borderRadius: 999,
    background: "rgba(34, 197, 94, 0.16)",
    color: "#86efac",
    fontSize: 12,
    fontWeight: 950,
    textTransform: "uppercase"
  },
  disabledBadge: {
    padding: "6px 10px",
    borderRadius: 999,
    background: "rgba(248, 113, 113, 0.16)",
    color: "#fecaca",
    fontSize: 12,
    fontWeight: 950,
    textTransform: "uppercase"
  },
  auditCountBadge: {
    padding: "8px 12px",
    borderRadius: 999,
    border: "1px solid rgba(56, 189, 248, 0.42)",
    background: "rgba(14, 165, 233, 0.14)",
    color: "#7dd3fc",
    fontSize: 12,
    fontWeight: 950,
    textTransform: "uppercase",
    letterSpacing: "0.08em"
  },
  auditList: {
    display: "grid",
    gap: 12,
    marginTop: 16
  },
  auditItem: {
    padding: 14,
    borderRadius: 16,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "rgba(2, 6, 23, 0.62)"
  },
  auditHeader: {
    display: "flex",
    justifyContent: "space-between",
    gap: 12,
    alignItems: "flex-start"
  },
  auditAction: {
    color: "#f8fafc",
    fontSize: 15
  },
  auditMeta: {
    margin: "5px 0 0",
    color: "#94a3b8",
    fontSize: 13
  },
  auditTarget: {
    color: "#bae6fd",
    fontSize: 12,
    fontWeight: 900,
    textAlign: "right",
    textTransform: "uppercase",
    letterSpacing: "0.06em"
  },
  auditDetails: {
    margin: "12px 0 0",
    padding: 12,
    maxHeight: 180,
    overflow: "auto",
    borderRadius: 12,
    border: "1px solid rgba(51, 65, 85, 0.95)",
    background: "#020617",
    color: "#cbd5e1",
    fontSize: 12,
    whiteSpace: "pre-wrap"
  }
};
const route19 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$f,
  default: settings,
  loader: loader$a
}, Symbol.toStringTag, { value: "Module" }));
function loginErrorMessage(loginErrors) {
  if ((loginErrors == null ? void 0 : loginErrors.shop) === LoginErrorType.MissingShop) {
    return { shop: "Please enter your shop domain to log in" };
  } else if ((loginErrors == null ? void 0 : loginErrors.shop) === LoginErrorType.InvalidShop) {
    return { shop: "Please enter a valid shop domain to log in" };
  }
  return {};
}
const loader$9 = async ({
  request
}) => {
  const url = new URL(request.url);
  const shop = url.searchParams.get("shop") || "";
  const errors = loginErrorMessage(await login$1(request));
  return {
    errors,
    shop
  };
};
const action$e = async ({
  request
}) => {
  const errors = loginErrorMessage(await login$1(request));
  return {
    errors
  };
};
const route = UNSAFE_withComponentProps(function Auth() {
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const {
    errors
  } = actionData || loaderData;
  const [shop, setShop] = useState(loaderData.shop || "");
  return /* @__PURE__ */ jsx(AppProvider, {
    embedded: false,
    children: /* @__PURE__ */ jsx("s-page", {
      children: /* @__PURE__ */ jsx(Form, {
        method: "post",
        children: /* @__PURE__ */ jsxs("s-section", {
          heading: "Log in",
          children: [/* @__PURE__ */ jsx("s-text-field", {
            name: "shop",
            label: "Shop domain",
            details: "example.myshopify.com",
            value: shop,
            onChange: (e) => setShop(e.currentTarget.value),
            autocomplete: "off",
            error: errors.shop
          }), /* @__PURE__ */ jsx("s-button", {
            type: "submit",
            children: "Log in"
          })]
        })
      })
    })
  });
});
const route20 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$e,
  default: route,
  loader: loader$9
}, Symbol.toStringTag, { value: "Module" }));
async function loader$8({
  request
}) {
  await authenticate.admin(request);
  return null;
}
const auth_$ = UNSAFE_withComponentProps(function AuthCatchAll() {
  return null;
});
const route21 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: auth_$,
  loader: loader$8
}, Symbol.toStringTag, { value: "Module" }));
async function loader$7({
  request
}) {
  const {
    admin
  } = await authenticate.admin(request);
  try {
    const syncStatus = await ensureProductOptionsFresh(admin);
    return data({
      lastProductSyncAt: syncStatus.lastUpdatedAt,
      justSynced: syncStatus.synced,
      syncedCount: syncStatus.syncedCount
    });
  } catch (error) {
    console.error("[AUTO PRODUCT SYNC ERROR]", error);
    return data({
      lastProductSyncAt: await getLatestProductSyncTimestamp(),
      justSynced: false,
      syncedCount: 0
    });
  }
}
const ErrorBoundary = UNSAFE_withErrorBoundaryProps(function ErrorBoundary2() {
  return boundary.error(null);
});
const headers = (headersArgs) => {
  return boundary.headers(headersArgs);
};
const app = UNSAFE_withComponentProps(function AppLayout() {
  const loaderData = useLoaderData();
  const location = useLocation();
  const qs = location.search || "";
  const isClassicRoute = location.pathname.startsWith("/app/classic");
  const lastSyncLabel = (loaderData == null ? void 0 : loaderData.lastProductSyncAt) ? new Date(loaderData.lastProductSyncAt).toLocaleString() : "Never";
  if (isClassicRoute) {
    return /* @__PURE__ */ jsx(Outlet, {});
  }
  return /* @__PURE__ */ jsxs("div", {
    style: {
      minHeight: "100vh",
      background: "#0f172a",
      color: "#f8fafc"
    },
    children: [/* @__PURE__ */ jsxs("nav", {
      style: {
        display: "flex",
        gap: "12px",
        padding: "16px 20px",
        borderBottom: "1px solid #1e293b",
        alignItems: "center",
        flexWrap: "wrap",
        background: "#111827"
      },
      children: [/* @__PURE__ */ jsx(Link, {
        to: `/app${qs}`,
        style: {
          color: "#e5e7eb",
          textDecoration: "none",
          padding: "10px 14px",
          borderRadius: "10px",
          border: "1px solid #334155",
          background: "#0f172a"
        },
        children: "Dashboard"
      }), /* @__PURE__ */ jsx(Link, {
        to: `/app/admin${qs}`,
        style: {
          color: "#e5e7eb",
          textDecoration: "none",
          padding: "10px 14px",
          borderRadius: "10px",
          border: "1px solid #334155",
          background: "#0f172a"
        },
        children: "Admin"
      }), /* @__PURE__ */ jsx(Link, {
        to: `/app/custom-quote${qs}`,
        style: {
          color: "#e5e7eb",
          textDecoration: "none",
          padding: "10px 14px",
          borderRadius: "10px",
          border: "1px solid #334155",
          background: "#0f172a"
        },
        children: "Custom Quote"
      }), /* @__PURE__ */ jsx(Link, {
        to: `/app/classic${qs}`,
        style: {
          color: "#e5e7eb",
          textDecoration: "none",
          padding: "10px 14px",
          borderRadius: "10px",
          border: "1px solid #334155",
          background: "#0f172a"
        },
        children: "Dispatch"
      }), /* @__PURE__ */ jsx(Link, {
        to: `/app/classic${qs}`,
        style: {
          color: "#e5e7eb",
          textDecoration: "none",
          padding: "10px 14px",
          borderRadius: "10px",
          border: "1px solid #334155",
          background: "#0f172a"
        },
        children: "Classic"
      }), /* @__PURE__ */ jsx(Link, {
        to: `/app/calendar${qs}`,
        style: {
          color: "#e5e7eb",
          textDecoration: "none",
          padding: "10px 14px",
          borderRadius: "10px",
          border: "1px solid #334155",
          background: "#0f172a"
        },
        children: "Calendar"
      }), /* @__PURE__ */ jsx(Link, {
        to: `/app/dispatch/driver${qs}`,
        style: {
          color: "#e5e7eb",
          textDecoration: "none",
          padding: "10px 14px",
          borderRadius: "10px",
          border: "1px solid #334155",
          background: "#0f172a"
        },
        children: "Driver Route"
      }), /* @__PURE__ */ jsx("a", {
        href: "/custom-quote",
        target: "_blank",
        rel: "noreferrer",
        style: {
          color: "#e5e7eb",
          textDecoration: "none",
          padding: "10px 14px",
          borderRadius: "10px",
          border: "1px solid #334155",
          background: "#0f172a"
        },
        children: "Quote Portal"
      }), /* @__PURE__ */ jsxs(Form, {
        method: "post",
        action: `/api/sync-products${qs}`,
        style: {
          marginLeft: "auto",
          display: "flex",
          alignItems: "center",
          gap: 12
        },
        children: [/* @__PURE__ */ jsxs("div", {
          style: {
            padding: "8px 12px",
            borderRadius: "999px",
            border: "1px solid #334155",
            background: (loaderData == null ? void 0 : loaderData.justSynced) ? "rgba(22, 163, 74, 0.16)" : "#0f172a",
            color: (loaderData == null ? void 0 : loaderData.justSynced) ? "#86efac" : "#cbd5e1",
            fontSize: 12,
            lineHeight: 1.2,
            whiteSpace: "nowrap"
          },
          children: [(loaderData == null ? void 0 : loaderData.justSynced) ? "Auto-synced now" : "Product sync", /* @__PURE__ */ jsx("span", {
            style: {
              color: "#94a3b8",
              marginLeft: 6
            },
            children: lastSyncLabel
          })]
        }), /* @__PURE__ */ jsx("button", {
          type: "submit",
          style: {
            padding: "10px 14px",
            borderRadius: "10px",
            border: "none",
            background: "#2563eb",
            color: "#fff",
            fontWeight: 700,
            cursor: "pointer"
          },
          children: "Sync Shopify Products"
        })]
      })]
    }), /* @__PURE__ */ jsx("div", {
      style: {
        padding: "20px"
      },
      children: /* @__PURE__ */ jsx(Outlet, {})
    })]
  });
});
const route22 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  ErrorBoundary,
  default: app,
  headers,
  loader: loader$7
}, Symbol.toStringTag, { value: "Module" }));
const CARRIER_NAME = "GHS Shipping Calc";
const LIST_CARRIER_SERVICES = `#graphql
  query CarrierServices {
    carrierServices(first: 20) {
      nodes {
        id
        name
        active
        callbackUrl
        supportsServiceDiscovery
      }
    }
  }
`;
const CREATE_CARRIER_SERVICE = `#graphql
  mutation CreateCarrierService($input: DeliveryCarrierServiceCreateInput!) {
    carrierServiceCreate(input: $input) {
      carrierService {
        id
        name
        active
        callbackUrl
        supportsServiceDiscovery
      }
      userErrors {
        field
        message
      }
    }
  }
`;
const UPDATE_CARRIER_SERVICE = `#graphql
  mutation UpdateCarrierService($id: ID!, $input: DeliveryCarrierServiceUpdateInput!) {
    carrierServiceUpdate(id: $id, input: $input) {
      carrierService {
        id
        name
        active
        callbackUrl
        supportsServiceDiscovery
      }
      userErrors {
        field
        message
      }
    }
  }
`;
async function registerCarrierService(admin) {
  var _a2, _b, _c, _d, _e, _f, _g, _h, _i, _j;
  const appUrl = process.env.SHOPIFY_APP_URL;
  if (!appUrl) {
    return {
      ok: false,
      step: "env",
      message: "SHOPIFY_APP_URL is missing"
    };
  }
  const callbackUrl = `${appUrl.replace(/\/$/, "")}/api/carrier-service`;
  try {
    const listResponse = await admin.graphql(LIST_CARRIER_SERVICES);
    const listJson = await listResponse.json();
    const carriers = ((_b = (_a2 = listJson == null ? void 0 : listJson.data) == null ? void 0 : _a2.carrierServices) == null ? void 0 : _b.nodes) || [];
    const existing = carriers.find((service) => service.name === CARRIER_NAME);
    if (existing) {
      const needsUpdate = existing.callbackUrl !== callbackUrl || existing.active !== true || existing.supportsServiceDiscovery !== true;
      if (!needsUpdate) {
        return {
          ok: true,
          step: "exists",
          message: "Carrier service already exists and is correct",
          carrier: existing,
          callbackUrl
        };
      }
      const updateResponse = await admin.graphql(UPDATE_CARRIER_SERVICE, {
        variables: {
          id: existing.id,
          input: {
            name: CARRIER_NAME,
            callbackUrl,
            active: true,
            supportsServiceDiscovery: true
          }
        }
      });
      const updateJson = await updateResponse.json();
      const updateErrors = ((_d = (_c = updateJson == null ? void 0 : updateJson.data) == null ? void 0 : _c.carrierServiceUpdate) == null ? void 0 : _d.userErrors) || [];
      if (updateErrors.length > 0) {
        return {
          ok: false,
          step: "update",
          message: "Carrier update failed",
          errors: updateErrors,
          raw: updateJson,
          callbackUrl
        };
      }
      return {
        ok: true,
        step: "updated",
        message: "Carrier service updated",
        carrier: (_f = (_e = updateJson == null ? void 0 : updateJson.data) == null ? void 0 : _e.carrierServiceUpdate) == null ? void 0 : _f.carrierService,
        callbackUrl
      };
    }
    const createResponse = await admin.graphql(CREATE_CARRIER_SERVICE, {
      variables: {
        input: {
          name: CARRIER_NAME,
          callbackUrl,
          active: true,
          supportsServiceDiscovery: true
        }
      }
    });
    const createJson = await createResponse.json();
    const createErrors = ((_h = (_g = createJson == null ? void 0 : createJson.data) == null ? void 0 : _g.carrierServiceCreate) == null ? void 0 : _h.userErrors) || [];
    if (createErrors.length > 0) {
      return {
        ok: false,
        step: "create",
        message: "Carrier creation failed",
        errors: createErrors,
        raw: createJson,
        callbackUrl
      };
    }
    return {
      ok: true,
      step: "created",
      message: "Carrier service created",
      carrier: (_j = (_i = createJson == null ? void 0 : createJson.data) == null ? void 0 : _i.carrierServiceCreate) == null ? void 0 : _j.carrierService,
      callbackUrl
    };
  } catch (error) {
    return {
      ok: false,
      step: "exception",
      message: (error == null ? void 0 : error.message) || "Unknown error",
      error: String(error),
      callbackUrl
    };
  }
}
async function loader$6({
  request
}) {
  const {
    admin,
    session
  } = await authenticate.admin(request);
  await registerCarrierService(admin);
  const settings2 = await getAppSettings(session.shop);
  return data({
    settings: settings2
  });
}
async function action$d({
  request
}) {
  const {
    session
  } = await authenticate.admin(request);
  const form = await request.formData();
  try {
    const savedSettings = await saveAppSettings(session.shop, {
      useTestFlatRate: form.get("useTestFlatRate") === "on",
      testFlatRateCents: Number(form.get("testFlatRateCents") || 5e3),
      enableCalculatedRates: form.get("enableCalculatedRates") === "on",
      enableRemoteSurcharge: form.get("enableRemoteSurcharge") === "on",
      enableDebugLogging: form.get("enableDebugLogging") === "on",
      showVendorSource: form.get("showVendorSource") === "on"
    });
    return data({
      ok: true,
      message: "Settings saved successfully.",
      settings: savedSettings
    });
  } catch (error) {
    console.error("[APP SETTINGS ACTION ERROR]", error);
    return data({
      ok: false,
      message: (error == null ? void 0 : error.message) || "Failed to save settings."
    }, {
      status: 500
    });
  }
}
const app__index = UNSAFE_withComponentProps(function AppIndex() {
  const loaderData = useLoaderData();
  const actionData = useActionData();
  const navigation = useNavigation();
  const settings2 = (actionData == null ? void 0 : actionData.settings) ?? loaderData.settings;
  const isSaving = navigation.state === "submitting";
  return /* @__PURE__ */ jsxs("div", {
    style: {
      padding: 30,
      maxWidth: 800
    },
    children: [/* @__PURE__ */ jsx("h1", {
      style: {
        fontSize: 28,
        marginBottom: 8
      },
      children: "Local Delivery Admin"
    }), /* @__PURE__ */ jsx("p", {
      style: {
        marginBottom: 24
      },
      children: "Manage shipping behavior for testing and live checkout."
    }), (actionData == null ? void 0 : actionData.message) ? /* @__PURE__ */ jsx("div", {
      style: {
        marginBottom: 20,
        padding: "12px 14px",
        borderRadius: 8,
        border: "1px solid",
        borderColor: actionData.ok ? "#16a34a" : "#dc2626",
        background: actionData.ok ? "#f0fdf4" : "#fef2f2",
        color: "#111827"
      },
      children: actionData.message
    }) : null, /* @__PURE__ */ jsx(Form, {
      method: "post",
      children: /* @__PURE__ */ jsxs("div", {
        style: {
          display: "grid",
          gap: 18
        },
        children: [/* @__PURE__ */ jsxs("label", {
          children: [/* @__PURE__ */ jsx("input", {
            type: "checkbox",
            name: "enableCalculatedRates",
            defaultChecked: settings2.enableCalculatedRates
          }), " ", "Enable calculated shipping rates"]
        }), /* @__PURE__ */ jsxs("label", {
          children: [/* @__PURE__ */ jsx("input", {
            type: "checkbox",
            name: "useTestFlatRate",
            defaultChecked: settings2.useTestFlatRate
          }), " ", "Use test flat rate"]
        }), /* @__PURE__ */ jsxs("label", {
          children: ["Test flat rate (cents)", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
            type: "number",
            name: "testFlatRateCents",
            defaultValue: settings2.testFlatRateCents,
            min: 0,
            style: {
              width: 220,
              marginTop: 5
            }
          })]
        }), /* @__PURE__ */ jsxs("label", {
          children: [/* @__PURE__ */ jsx("input", {
            type: "checkbox",
            name: "enableRemoteSurcharge",
            defaultChecked: settings2.enableRemoteSurcharge
          }), " ", "Enable remote ZIP surcharge"]
        }), /* @__PURE__ */ jsxs("label", {
          children: [/* @__PURE__ */ jsx("input", {
            type: "checkbox",
            name: "enableDebugLogging",
            defaultChecked: settings2.enableDebugLogging
          }), " ", "Enable debug logging"]
        }), /* @__PURE__ */ jsxs("label", {
          children: [/* @__PURE__ */ jsx("input", {
            type: "checkbox",
            name: "showVendorSource",
            defaultChecked: settings2.showVendorSource
          }), " ", "Show vendor source on checkout"]
        }), /* @__PURE__ */ jsx("button", {
          type: "submit",
          disabled: isSaving,
          style: {
            marginTop: 10,
            padding: "10px 16px",
            background: isSaving ? "#6b7280" : "#111",
            color: "#fff",
            borderRadius: 6,
            border: "none",
            cursor: isSaving ? "default" : "pointer",
            width: 180
          },
          children: isSaving ? "Saving..." : "Save Settings"
        })]
      })
    })]
  });
});
const route23 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$d,
  default: app__index,
  loader: loader$6
}, Symbol.toStringTag, { value: "Module" }));
async function getOriginAddresses() {
  const { data: data2, error } = await supabaseAdmin.from("origin_addresses").select("id, label, address, is_active").order("label", { ascending: true });
  if (error) {
    console.error("[GET ORIGIN ADDRESSES ERROR]", error);
    return [];
  }
  return data2 || [];
}
async function saveOriginAddress(row) {
  const payload = {
    ...row.id ? { id: row.id } : {},
    label: row.label,
    address: row.address,
    is_active: row.is_active
  };
  const { data: data2, error } = await supabaseAdmin.from("origin_addresses").upsert(payload, row.id ? { onConflict: "id" } : void 0).select("*").single();
  if (error) {
    console.error("[SAVE ORIGIN ADDRESS ERROR]", error);
    throw error;
  }
  return data2;
}
async function getShippingMaterialRules() {
  const { data: data2, error } = await supabaseAdmin.from("shipping_material_rules").select("prefix, material_name, truck_capacity, vendor_source, is_active, sort_order").order("sort_order", { ascending: true });
  if (error) {
    console.error("[GET SHIPPING MATERIAL RULES ERROR]", error);
    return [];
  }
  return data2 || [];
}
async function saveShippingMaterialRule(row) {
  const payload = {
    prefix: row.prefix,
    material_name: row.material_name,
    truck_capacity: row.truck_capacity,
    vendor_source: row.vendor_source || null,
    is_active: row.is_active,
    sort_order: row.sort_order,
    updated_at: (/* @__PURE__ */ new Date()).toISOString()
  };
  const { data: data2, error } = await supabaseAdmin.from("shipping_material_rules").upsert(payload, { onConflict: "prefix" }).select("*").single();
  if (error) {
    console.error("[SAVE SHIPPING MATERIAL RULE ERROR]", error);
    throw error;
  }
  return data2;
}
async function loader$5({
  request
}) {
  await authenticate.admin(request);
  const [origins, rules] = await Promise.all([getOriginAddresses(), getShippingMaterialRules()]);
  return data({
    origins,
    rules
  });
}
async function action$c({
  request
}) {
  await authenticate.admin(request);
  const form = await request.formData();
  const intent = String(form.get("intent") || "");
  try {
    if (intent === "save-origin") {
      await saveOriginAddress({
        id: String(form.get("id") || "") || void 0,
        label: String(form.get("label") || "").trim(),
        address: String(form.get("address") || "").trim(),
        is_active: form.get("is_active") === "on"
      });
      return data({
        ok: true,
        message: "Pickup vendor saved."
      });
    }
    if (intent === "save-rule") {
      await saveShippingMaterialRule({
        prefix: String(form.get("prefix") || "").trim(),
        material_name: String(form.get("material_name") || "").trim(),
        truck_capacity: Number(form.get("truck_capacity") || 22),
        vendor_source: String(form.get("vendor_source") || "").trim(),
        is_active: form.get("is_active") === "on",
        sort_order: Number(form.get("sort_order") || 0)
      });
      return data({
        ok: true,
        message: "Material rule saved."
      });
    }
    return data({
      ok: false,
      message: "Unknown action."
    }, {
      status: 400
    });
  } catch (error) {
    console.error("[ADMIN ACTION ERROR]", error);
    return data({
      ok: false,
      message: (error == null ? void 0 : error.message) || "Save failed."
    }, {
      status: 500
    });
  }
}
const app_admin = UNSAFE_withComponentProps(function AdminPage() {
  const {
    origins,
    rules
  } = useLoaderData();
  const actionData = useActionData();
  const navigation = useNavigation();
  const location = useLocation();
  const qs = location.search || "";
  const isSaving = navigation.state === "submitting";
  return /* @__PURE__ */ jsxs("div", {
    style: {
      padding: 30,
      maxWidth: 1100
    },
    children: [/* @__PURE__ */ jsx("h1", {
      style: {
        fontSize: 28,
        marginBottom: 8
      },
      children: "Delivery Admin"
    }), /* @__PURE__ */ jsx("p", {
      style: {
        marginBottom: 24
      },
      children: "Manage pickup vendors and SKU prefix rules."
    }), (actionData == null ? void 0 : actionData.message) ? /* @__PURE__ */ jsx("div", {
      style: {
        marginBottom: 20,
        padding: "12px 14px",
        borderRadius: 8,
        border: "1px solid",
        borderColor: actionData.ok ? "#16a34a" : "#dc2626",
        background: actionData.ok ? "#f0fdf4" : "#fef2f2"
      },
      children: actionData.message
    }) : null, /* @__PURE__ */ jsxs("div", {
      style: {
        display: "grid",
        gap: 32
      },
      children: [/* @__PURE__ */ jsxs("section", {
        children: [/* @__PURE__ */ jsx("h2", {
          style: {
            fontSize: 22,
            marginBottom: 12
          },
          children: "Pickup Vendors"
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            display: "grid",
            gap: 14
          },
          children: [origins.map((origin) => /* @__PURE__ */ jsxs(Form, {
            method: "post",
            style: {
              border: "1px solid #e5e7eb",
              borderRadius: 10,
              padding: 16,
              display: "grid",
              gap: 12
            },
            children: [/* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "intent",
              value: "save-origin"
            }), /* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "id",
              value: origin.id || ""
            }), /* @__PURE__ */ jsxs("label", {
              children: ["Vendor label", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                type: "text",
                name: "label",
                defaultValue: origin.label,
                style: {
                  width: "100%",
                  marginTop: 6
                }
              })]
            }), /* @__PURE__ */ jsxs("label", {
              children: ["Pickup address", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                type: "text",
                name: "address",
                defaultValue: origin.address,
                style: {
                  width: "100%",
                  marginTop: 6
                }
              })]
            }), /* @__PURE__ */ jsxs("label", {
              children: [/* @__PURE__ */ jsx("input", {
                type: "checkbox",
                name: "is_active",
                defaultChecked: origin.is_active
              }), " ", "Active"]
            }), /* @__PURE__ */ jsx("button", {
              type: "submit",
              disabled: isSaving,
              style: {
                width: 160,
                padding: "10px 14px",
                background: "#111",
                color: "#fff",
                border: "none",
                borderRadius: 6
              },
              children: isSaving ? "Saving..." : "Save Vendor"
            })]
          }, origin.id || origin.label)), /* @__PURE__ */ jsxs(Form, {
            method: "post",
            style: {
              border: "1px dashed #cbd5e1",
              borderRadius: 10,
              padding: 16,
              display: "grid",
              gap: 12
            },
            children: [/* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "intent",
              value: "save-origin"
            }), /* @__PURE__ */ jsxs("label", {
              children: ["New vendor label", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                type: "text",
                name: "label",
                style: {
                  width: "100%",
                  marginTop: 6
                }
              })]
            }), /* @__PURE__ */ jsxs("label", {
              children: ["New pickup address", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                type: "text",
                name: "address",
                style: {
                  width: "100%",
                  marginTop: 6
                }
              })]
            }), /* @__PURE__ */ jsxs("label", {
              children: [/* @__PURE__ */ jsx("input", {
                type: "checkbox",
                name: "is_active",
                defaultChecked: true
              }), " Active"]
            }), /* @__PURE__ */ jsx("button", {
              type: "submit",
              disabled: isSaving,
              style: {
                width: 180,
                padding: "10px 14px",
                background: "#111",
                color: "#fff",
                border: "none",
                borderRadius: 6
              },
              children: isSaving ? "Saving..." : "Add Vendor"
            })]
          })]
        })]
      }), /* @__PURE__ */ jsxs("section", {
        children: [/* @__PURE__ */ jsx("h2", {
          style: {
            fontSize: 22,
            marginBottom: 12
          },
          children: "SKU Prefix Rules"
        }), /* @__PURE__ */ jsxs("div", {
          style: {
            display: "grid",
            gap: 14
          },
          children: [rules.map((rule) => /* @__PURE__ */ jsxs(Form, {
            method: "post",
            style: {
              border: "1px solid #e5e7eb",
              borderRadius: 10,
              padding: 16,
              display: "grid",
              gap: 12
            },
            children: [/* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "intent",
              value: "save-rule"
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gridTemplateColumns: "120px 1fr 160px 1fr 120px",
                gap: 12
              },
              children: [/* @__PURE__ */ jsxs("label", {
                children: ["Prefix", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "text",
                  name: "prefix",
                  defaultValue: rule.prefix,
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              }), /* @__PURE__ */ jsxs("label", {
                children: ["Material name", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "text",
                  name: "material_name",
                  defaultValue: rule.material_name,
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              }), /* @__PURE__ */ jsxs("label", {
                children: ["Truck cap", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "truck_capacity",
                  defaultValue: rule.truck_capacity,
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              }), /* @__PURE__ */ jsxs("label", {
                children: ["Vendor source", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "text",
                  name: "vendor_source",
                  defaultValue: rule.vendor_source || "",
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              }), /* @__PURE__ */ jsxs("label", {
                children: ["Sort order", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "sort_order",
                  defaultValue: rule.sort_order,
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              })]
            }), /* @__PURE__ */ jsxs("label", {
              children: [/* @__PURE__ */ jsx("input", {
                type: "checkbox",
                name: "is_active",
                defaultChecked: rule.is_active
              }), " ", "Active"]
            }), /* @__PURE__ */ jsx("button", {
              type: "submit",
              disabled: isSaving,
              style: {
                width: 160,
                padding: "10px 14px",
                background: "#111",
                color: "#fff",
                border: "none",
                borderRadius: 6
              },
              children: isSaving ? "Saving..." : "Save Rule"
            })]
          }, rule.prefix)), /* @__PURE__ */ jsxs(Form, {
            method: "post",
            style: {
              border: "1px dashed #cbd5e1",
              borderRadius: 10,
              padding: 16,
              display: "grid",
              gap: 12
            },
            children: [/* @__PURE__ */ jsx("input", {
              type: "hidden",
              name: "intent",
              value: "save-rule"
            }), /* @__PURE__ */ jsxs("div", {
              style: {
                display: "grid",
                gridTemplateColumns: "120px 1fr 160px 1fr 120px",
                gap: 12
              },
              children: [/* @__PURE__ */ jsxs("label", {
                children: ["Prefix", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "text",
                  name: "prefix",
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              }), /* @__PURE__ */ jsxs("label", {
                children: ["Material name", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "text",
                  name: "material_name",
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              }), /* @__PURE__ */ jsxs("label", {
                children: ["Truck cap", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "truck_capacity",
                  defaultValue: 22,
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              }), /* @__PURE__ */ jsxs("label", {
                children: ["Vendor source", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "text",
                  name: "vendor_source",
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              }), /* @__PURE__ */ jsxs("label", {
                children: ["Sort order", /* @__PURE__ */ jsx("br", {}), /* @__PURE__ */ jsx("input", {
                  type: "number",
                  name: "sort_order",
                  defaultValue: 0,
                  style: {
                    width: "100%",
                    marginTop: 6
                  }
                })]
              })]
            }), /* @__PURE__ */ jsxs("label", {
              children: [/* @__PURE__ */ jsx("input", {
                type: "checkbox",
                name: "is_active",
                defaultChecked: true
              }), " Active"]
            }), /* @__PURE__ */ jsx("button", {
              type: "submit",
              disabled: isSaving,
              style: {
                width: 180,
                padding: "10px 14px",
                background: "#111",
                color: "#fff",
                border: "none",
                borderRadius: 6
              },
              children: isSaving ? "Saving..." : "Add Rule"
            })]
          }), /* @__PURE__ */ jsx(Form, {
            method: "post",
            action: `/api/sync-products${qs}`,
            children: /* @__PURE__ */ jsx("button", {
              type: "submit",
              children: "Sync Shopify Products"
            })
          })]
        })]
      })]
    })]
  });
});
const route24 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$c,
  default: app_admin,
  loader: loader$5
}, Symbol.toStringTag, { value: "Module" }));
const app_additional = UNSAFE_withComponentProps(function AdditionalPage() {
  return /* @__PURE__ */ jsxs("s-page", {
    heading: "Additional page",
    children: [/* @__PURE__ */ jsxs("s-section", {
      heading: "Multiple pages",
      children: [/* @__PURE__ */ jsxs("s-paragraph", {
        children: ["The app template comes with an additional page which demonstrates how to create multiple pages within app navigation using", " ", /* @__PURE__ */ jsx("s-link", {
          href: "https://shopify.dev/docs/apps/tools/app-bridge",
          target: "_blank",
          children: "App Bridge"
        }), "."]
      }), /* @__PURE__ */ jsxs("s-paragraph", {
        children: ["To create your own page and have it show up in the app navigation, add a page inside ", /* @__PURE__ */ jsx("code", {
          children: "app/routes"
        }), ", and a link to it in the", " ", /* @__PURE__ */ jsx("code", {
          children: "<ui-nav-menu>"
        }), " component found in", " ", /* @__PURE__ */ jsx("code", {
          children: "app/routes/app.jsx"
        }), "."]
      })]
    }), /* @__PURE__ */ jsx("s-section", {
      slot: "aside",
      heading: "Resources",
      children: /* @__PURE__ */ jsx("s-unordered-list", {
        children: /* @__PURE__ */ jsx("s-list-item", {
          children: /* @__PURE__ */ jsx("s-link", {
            href: "https://shopify.dev/docs/apps/design-guidelines/navigation#app-nav",
            target: "_blank",
            children: "App nav best practices"
          })
        })
      })
    })]
  });
});
const route25 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: app_additional
}, Symbol.toStringTag, { value: "Module" }));
const route26 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$r,
  default: customQuote,
  loader: loader$q
}, Symbol.toStringTag, { value: "Module" }));
const route27 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$q,
  default: quoteReview,
  loader: loader$p
}, Symbol.toStringTag, { value: "Module" }));
const route28 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$p,
  default: dispatch,
  loader: loader$o
}, Symbol.toStringTag, { value: "Module" }));
const route29 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  loader: loader$n
}, Symbol.toStringTag, { value: "Module" }));
const route30 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$o,
  default: classic,
  loader: loader$m
}, Symbol.toStringTag, { value: "Module" }));
const route31 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$n,
  default: monitor,
  loader: loader$l
}, Symbol.toStringTag, { value: "Module" }));
const route32 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$m,
  default: loader_default,
  loader: loader$k
}, Symbol.toStringTag, { value: "Module" }));
const route33 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$l,
  default: calendar,
  loader: loader$j
}, Symbol.toStringTag, { value: "Module" }));
const route34 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$k,
  default: allotment,
  loader: loader$i
}, Symbol.toStringTag, { value: "Module" }));
const route35 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$j,
  default: dispatchDriver,
  loader: loader$f
}, Symbol.toStringTag, { value: "Module" }));
const route36 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: dispatchDriverDetail,
  loader: loader$e
}, Symbol.toStringTag, { value: "Module" }));
const route37 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$i,
  default: mobileDashboard,
  loader: loader$d
}, Symbol.toStringTag, { value: "Module" }));
async function action$b({
  request
}) {
  const body = await request.json();
  const shippingAddress = (body == null ? void 0 : body.shippingAddress) ?? {};
  const lines = Array.isArray(body == null ? void 0 : body.lines) ? body.lines : [];
  const url = new URL(request.url);
  const shop = url.searchParams.get("shop") || (body == null ? void 0 : body.shop) || request.headers.get("x-shopify-shop-domain") || process.env.SHOPIFY_STORE_DOMAIN || "";
  if (!shop) {
    throw new Error("Missing shop parameter");
  }
  const quote = await getQuote({
    shop,
    postalCode: shippingAddress.zip ?? "",
    country: shippingAddress.countryCode ?? "US",
    province: shippingAddress.provinceCode ?? "",
    city: shippingAddress.city ?? "",
    address1: shippingAddress.address1 ?? "",
    address2: shippingAddress.address2 ?? "",
    items: lines.map((line) => ({
      sku: line.sku,
      quantity: line.quantity ?? 0,
      grams: line.grams ?? 0,
      price: line.price ?? 0,
      requiresShipping: true,
      productVendor: line.vendor || line.product_vendor || ""
    }))
  });
  return data({
    summary: quote.summary,
    eta: quote.eta,
    description: quote.description,
    cents: quote.cents,
    serviceName: quote.serviceName,
    outsideDeliveryArea: quote.outsideDeliveryArea ?? false,
    outsideDeliveryMiles: quote.outsideDeliveryMiles ?? 0,
    outsideDeliveryRadius: quote.outsideDeliveryRadius ?? 50,
    outsideDeliveryPhone: quote.outsideDeliveryPhone ?? "(262) 345-4001"
  });
}
const route38 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$b
}, Symbol.toStringTag, { value: "Module" }));
async function action$a({
  request
}) {
  const url = new URL(request.url);
  const body = await request.json();
  const rate = (body == null ? void 0 : body.rate) ?? {};
  const destination = rate.destination ?? {};
  const items = Array.isArray(rate.items) ? rate.items : [];
  const shop = url.searchParams.get("shop") || (body == null ? void 0 : body.shop) || request.headers.get("x-shopify-shop-domain") || process.env.SHOPIFY_STORE_DOMAIN || "";
  if (!shop) {
    throw new Error("Missing shop parameter");
  }
  const mappedItems = items.map((item) => ({
    sku: item.sku,
    quantity: item.quantity ?? 0,
    grams: item.grams ?? 0,
    price: item.price ?? 0,
    requiresShipping: item.requires_shipping !== false,
    pickupVendor: item.vendor || item.product_vendor || ""
  }));
  console.log("[CARRIER SHOP]", shop);
  console.log("[MAPPED ITEMS]", JSON.stringify(mappedItems, null, 2));
  const quote = await getQuote({
    shop,
    postalCode: destination.postal_code ?? "",
    country: destination.country ?? "US",
    province: destination.province ?? "",
    city: destination.city ?? "",
    address1: destination.address1 ?? "",
    address2: destination.address2 ?? "",
    items: mappedItems
  });
  console.log("[QUOTE RESULT]", JSON.stringify(quote, null, 2));
  if (quote.outsideDeliveryArea) {
    return data({
      rates: [{
        service_name: "Call for delivery quote",
        service_code: "CALL_FOR_QUOTE",
        total_price: "1",
        description: "Outside delivery area — please call for custom quote",
        currency: rate.currency ?? "USD"
      }]
    });
  }
  return data({
    rates: [{
      service_name: quote.serviceName,
      service_code: quote.serviceCode,
      total_price: String(quote.cents),
      description: quote.description,
      currency: rate.currency ?? "USD"
    }]
  });
}
const route39 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$a
}, Symbol.toStringTag, { value: "Module" }));
async function action$9({
  request
}) {
  const {
    admin,
    session
  } = await authenticate.admin(request);
  try {
    const products = await fetchProductOptionsFromShopify(admin);
    await syncProductOptionsToSupabase(products);
    console.log("[SYNC PRODUCTS]", session.shop, "synced", products.length, "variants");
    return data({
      ok: true,
      shop: session.shop,
      syncedCount: products.length
    });
  } catch (error) {
    console.error("[SYNC PRODUCTS ERROR]", error);
    return data({
      ok: false,
      message: (error == null ? void 0 : error.message) || "Failed to sync products"
    }, {
      status: 500
    });
  }
}
const route40 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$9
}, Symbol.toStringTag, { value: "Module" }));
const SHOPIFY_TITLE_LIMIT = 40;
function getStoreHandle(shop) {
  return shop.replace(".myshopify.com", "");
}
function truncateShopifyTitle(value) {
  const normalized = String(value || "").trim();
  if (!normalized) return "Quoted Item";
  if (normalized.length <= SHOPIFY_TITLE_LIMIT) return normalized;
  return `${normalized.slice(0, SHOPIFY_TITLE_LIMIT - 1).trimEnd()}…`;
}
function buildQuoteTag(quoteId) {
  const normalized = String(quoteId || "").trim();
  if (!normalized) return "quote";
  return `quote:${normalized.slice(0, 34)}`;
}
function usdMoney(amount) {
  return {
    amount: Number(amount || 0).toFixed(2),
    currencyCode: "USD"
  };
}
function parseSavedDeliveryAmountCents(shippingDetails) {
  if (!shippingDetails) return null;
  const exactMatch = shippingDetails.match(/=\s*\$?\s*(\d+(?:\.\d{1,2})?)/);
  const fallbackMatch = shippingDetails.match(/delivery(?: fee| amount)?:?\s*\$?\s*(\d+(?:\.\d{1,2})?)/i) || shippingDetails.match(/\$\s*(\d+(?:\.\d{1,2})?)/);
  const amount = Number((exactMatch == null ? void 0 : exactMatch[1]) || (fallbackMatch == null ? void 0 : fallbackMatch[1]) || NaN);
  return Number.isFinite(amount) ? Math.round(amount * 100) : null;
}
function getConfiguredQuoteTaxRate() {
  const rate = Number(process.env.QUOTE_TAX_RATE || "0");
  return Number.isFinite(rate) && rate > 0 ? rate : 0;
}
function getDeliveryChargeCents({
  quoteTotalCents,
  productsSubtotalCents,
  shippingDetails
}) {
  const parsedDeliveryCents = parseSavedDeliveryAmountCents(shippingDetails);
  if (parsedDeliveryCents !== null) return Math.max(0, parsedDeliveryCents);
  const taxRate = getConfiguredQuoteTaxRate();
  if (taxRate > 0) {
    const subtotalBeforeTaxCents = Math.round(quoteTotalCents / (1 + taxRate));
    return Math.max(0, subtotalBeforeTaxCents - productsSubtotalCents);
  }
  return Math.max(0, quoteTotalCents - productsSubtotalCents);
}
function splitCustomerName(name) {
  const normalized = String(name || "").trim();
  if (!normalized) return {
    firstName: void 0,
    lastName: void 0
  };
  const parts = normalized.split(/\s+/);
  if (parts.length === 1) {
    return {
      firstName: parts[0],
      lastName: void 0
    };
  }
  return {
    firstName: parts.slice(0, -1).join(" "),
    lastName: parts.at(-1)
  };
}
async function findOrCreateCustomerId(admin, input) {
  var _a2, _b, _c, _d, _e, _f, _g, _h, _i;
  const email = String(input.email || "").trim();
  if (!email) return null;
  const findResponse = await admin.graphql(`#graphql
      query FindCustomerByEmail($query: String!) {
        customers(first: 1, query: $query) {
          nodes {
            id
          }
        }
      }
    `, {
    variables: {
      query: `email:${email}`
    }
  });
  const findJson = await findResponse.json();
  const existingCustomerId = (_d = (_c = (_b = (_a2 = findJson == null ? void 0 : findJson.data) == null ? void 0 : _a2.customers) == null ? void 0 : _b.nodes) == null ? void 0 : _c[0]) == null ? void 0 : _d.id;
  if (existingCustomerId) return existingCustomerId;
  const createResponse = await admin.graphql(`#graphql
      mutation CreateQuoteCustomer($input: CustomerInput!) {
        customerCreate(input: $input) {
          customer {
            id
          }
          userErrors {
            field
            message
          }
        }
      }
    `, {
    variables: {
      input: {
        email,
        firstName: input.firstName || void 0,
        lastName: input.lastName || void 0
      }
    }
  });
  const createJson = await createResponse.json();
  const userErrors = ((_f = (_e = createJson == null ? void 0 : createJson.data) == null ? void 0 : _e.customerCreate) == null ? void 0 : _f.userErrors) || [];
  if (userErrors.length > 0) {
    throw new Error(userErrors.map((error) => {
      var _a3;
      return ((_a3 = error.field) == null ? void 0 : _a3.length) ? `${error.field.join(".")}: ${error.message}` : error.message;
    }).join(", "));
  }
  return ((_i = (_h = (_g = createJson == null ? void 0 : createJson.data) == null ? void 0 : _g.customerCreate) == null ? void 0 : _h.customer) == null ? void 0 : _i.id) || null;
}
async function action$8({
  request
}) {
  var _a2;
  const form = await request.formData();
  const quoteId = String(form.get("quoteId") || "").trim();
  if (!quoteId) {
    return data({
      ok: false,
      message: "Missing quote id."
    }, {
      status: 400
    });
  }
  const quote = await getCustomQuoteById(quoteId);
  if (!quote) {
    return data({
      ok: false,
      message: "Quote not found."
    }, {
      status: 404
    });
  }
  const isEmbeddedRequest = new URL(request.url).pathname.startsWith("/app/");
  const shop = quote.shop || process.env.SHOPIFY_STORE_DOMAIN || "";
  if (!isEmbeddedRequest && !await hasUserPermission(request, "sendToShopify")) {
    return data({
      ok: false,
      message: "You do not have permission to send quotes to Shopify."
    }, {
      status: 403
    });
  }
  if (!shop) {
    return data({
      ok: false,
      message: "Quote is missing a Shopify shop domain."
    }, {
      status: 400
    });
  }
  const adminClient = isEmbeddedRequest ? await authenticate.admin(request) : await shopify.unauthenticated.admin(shop);
  const admin = adminClient.admin;
  const products = await getProductOptionsFromSupabase();
  const lineItems = quote.line_items || [];
  const customerName = splitCustomerName(quote.customer_name);
  let customerId = null;
  if (lineItems.length === 0) {
    return data({
      ok: false,
      message: "Quote has no line items."
    }, {
      status: 400
    });
  }
  const productsSubtotalCents = lineItems.reduce((sum, line) => sum + Math.round(Number(line.price || 0) * 100) * Number(line.quantity || 0), 0);
  const deliveryChargeCents = getDeliveryChargeCents({
    quoteTotalCents: Number(quote.quote_total_cents || 0),
    productsSubtotalCents,
    shippingDetails: quote.shipping_details
  });
  const draftLineItems = lineItems.map((line) => {
    var _a3;
    const variantId = line.variantId || ((_a3 = products.find((product) => product.sku === line.sku)) == null ? void 0 : _a3.variantId) || null;
    if (variantId) {
      return {
        variantId,
        quantity: Number(line.quantity || 0),
        priceOverride: usdMoney(Number(line.price || 0)),
        customAttributes: [{
          key: "Quote ID",
          value: quote.id
        }, {
          key: "Quoted Unit Price",
          value: Number(line.price || 0).toFixed(2)
        }]
      };
    }
    return {
      title: truncateShopifyTitle(line.title),
      sku: line.sku,
      quantity: Number(line.quantity || 0),
      requiresShipping: true,
      taxable: false,
      originalUnitPriceWithCurrency: usdMoney(Number(line.price || 0)),
      customAttributes: [{
        key: "Quote ID",
        value: quote.id
      }]
    };
  });
  try {
    customerId = await findOrCreateCustomerId(admin, {
      email: quote.customer_email,
      firstName: customerName.firstName,
      lastName: customerName.lastName
    });
  } catch (error) {
    return data({
      ok: false,
      message: (error == null ? void 0 : error.message) || "Could not attach a Shopify customer to this draft order."
    }, {
      status: 400
    });
  }
  const response = await admin.graphql(`#graphql
      mutation draftOrderCreate($input: DraftOrderInput!) {
        draftOrderCreate(input: $input) {
          draftOrder {
            id
            legacyResourceId
            invoiceUrl
            name
          }
          userErrors {
            field
            message
          }
        }
      }
    `, {
    variables: {
      input: {
        note: [`Quote ID: ${quote.id}`, quote.summary ? `Summary: ${quote.summary}` : null, quote.description ? `Notes: ${quote.description}` : null].filter(Boolean).join("\n"),
        email: quote.customer_email || void 0,
        customerId: customerId || void 0,
        tags: ["custom-quote", buildQuoteTag(quote.id)],
        acceptAutomaticDiscounts: false,
        allowDiscountCodesInCheckout: false,
        shippingAddress: {
          firstName: customerName.firstName,
          lastName: customerName.lastName,
          address1: quote.address1,
          address2: quote.address2 || void 0,
          city: quote.city,
          province: quote.province,
          country: quote.country,
          zip: quote.postal_code,
          phone: quote.customer_phone || void 0
        },
        billingAddress: {
          firstName: customerName.firstName,
          lastName: customerName.lastName,
          address1: quote.address1,
          address2: quote.address2 || void 0,
          city: quote.city,
          province: quote.province,
          country: quote.country,
          zip: quote.postal_code,
          phone: quote.customer_phone || void 0
        },
        lineItems: draftLineItems,
        ...deliveryChargeCents > 0 ? {
          shippingLine: {
            title: truncateShopifyTitle(quote.service_name || "Quoted Delivery"),
            priceWithCurrency: {
              amount: (deliveryChargeCents / 100).toFixed(2),
              currencyCode: "USD"
            }
          }
        } : {}
      }
    }
  });
  const json = await response.json();
  const payload = (_a2 = json == null ? void 0 : json.data) == null ? void 0 : _a2.draftOrderCreate;
  const userErrors = (payload == null ? void 0 : payload.userErrors) || [];
  if (userErrors.length > 0) {
    return data({
      ok: false,
      message: userErrors.map((error) => {
        var _a3;
        return ((_a3 = error.field) == null ? void 0 : _a3.length) ? `${error.field.join(".")}: ${error.message}` : error.message;
      }).join(", ")
    }, {
      status: 400
    });
  }
  const draftOrder = payload == null ? void 0 : payload.draftOrder;
  if (!(draftOrder == null ? void 0 : draftOrder.id)) {
    return data({
      ok: false,
      message: "Draft order was not created."
    }, {
      status: 500
    });
  }
  return data({
    ok: true,
    message: `Draft order ${draftOrder.name} created in Shopify.`,
    draftOrderId: draftOrder.id,
    draftOrderName: draftOrder.name,
    draftOrderInvoiceUrl: draftOrder.invoiceUrl || null,
    draftOrderAdminUrl: draftOrder.legacyResourceId ? `https://admin.shopify.com/store/${getStoreHandle(shop)}/draft_orders/${draftOrder.legacyResourceId}` : null
  });
}
const route51 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$8
}, Symbol.toStringTag, { value: "Module" }));
const route41 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$8
}, Symbol.toStringTag, { value: "Module" }));
async function action$7({
  request
}) {
  const url = new URL(request.url);
  const isEmbeddedRequest = url.pathname.startsWith("/app/");
  if (isEmbeddedRequest) {
    await authenticate.admin(request);
  } else {
    const allowed = await hasAdminQuoteAccess(request);
    if (!allowed) {
      return data({
        ok: false,
        message: "Please log in."
      }, {
        status: 401
      });
    }
  }
  const form = await request.formData();
  const quoteId = String(form.get("quoteId") || "").trim();
  if (!quoteId) {
    return data({
      ok: false,
      message: "Missing quote id."
    }, {
      status: 400
    });
  }
  const existing = await getCustomQuoteById(quoteId);
  if (!existing) {
    return data({
      ok: false,
      message: "Quote not found."
    }, {
      status: 404
    });
  }
  await deleteCustomQuote(quoteId);
  return data({
    ok: true,
    message: "Quote deleted. This action cannot be undone.",
    deletedQuoteId: quoteId
  });
}
const route52 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$7
}, Symbol.toStringTag, { value: "Module" }));
const route42 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$7
}, Symbol.toStringTag, { value: "Module" }));
function normalizeQuantity(value) {
  const quantity = Number(value || 0);
  return Number.isFinite(quantity) && quantity > 0 ? quantity : 0;
}
function buildSourceBreakdown(lineItems) {
  const grouped = /* @__PURE__ */ new Map();
  for (const line of lineItems) {
    const vendor = line.vendor || "Unknown";
    const existing = grouped.get(vendor) || {
      vendor,
      quantity: 0,
      items: []
    };
    existing.quantity += line.quantity;
    existing.items.push(`${line.title} (${line.sku})`);
    grouped.set(vendor, existing);
  }
  return Array.from(grouped.values());
}
async function action$6({
  request
}) {
  const url = new URL(request.url);
  const isEmbeddedRequest = url.pathname.startsWith("/app/");
  if (isEmbeddedRequest) {
    await authenticate.admin(request);
  } else {
    const allowed = await hasAdminQuoteAccess(request);
    if (!allowed) {
      return data({
        ok: false,
        message: "Please log in."
      }, {
        status: 401
      });
    }
  }
  const form = await request.formData();
  const quoteId = String(form.get("quoteId") || "").trim();
  if (!quoteId) {
    return data({
      ok: false,
      message: "Missing quote id."
    }, {
      status: 400
    });
  }
  const existing = await getCustomQuoteById(quoteId);
  if (!existing) {
    return data({
      ok: false,
      message: "Quote not found."
    }, {
      status: 404
    });
  }
  const oldLineItems = existing.line_items || [];
  const lineItems = oldLineItems.map((line, index) => ({
    ...line,
    quantity: normalizeQuantity(form.get(`lineQuantity::${index}`))
  })).filter((line) => line.quantity > 0);
  if (lineItems.length === 0) {
    return data({
      ok: false,
      message: "At least one line item must have quantity greater than 0."
    }, {
      status: 400
    });
  }
  const customerName = String(form.get("customerName") || "").trim();
  const customerEmail = String(form.get("customerEmail") || "").trim();
  const customerPhone = String(form.get("customerPhone") || "").trim();
  const address1 = String(form.get("address1") || "").trim();
  const address2 = String(form.get("address2") || "").trim();
  const city = String(form.get("city") || "").trim();
  const province = String(form.get("province") || "").trim();
  const postalCode = String(form.get("postalCode") || "").trim();
  const country = String(form.get("country") || "US").trim() || "US";
  if (!address1 || !city || !province || !postalCode) {
    return data({
      ok: false,
      message: "Address 1, city, state, and ZIP are required to regenerate."
    }, {
      status: 400
    });
  }
  const productsSubtotal = lineItems.reduce((sum, line) => sum + Number(line.price || 0) * Number(line.quantity || 0), 0);
  const deliveryQuote = await getQuote({
    shop: existing.shop || process.env.SHOPIFY_STORE_DOMAIN || "darfaz-2e.myshopify.com",
    postalCode,
    country,
    province,
    city,
    address1,
    address2,
    items: lineItems.map((line) => ({
      sku: line.sku,
      quantity: Number(line.quantity || 0),
      requiresShipping: true,
      pickupVendor: line.vendor,
      price: Number(line.price || 0)
    }))
  });
  const deliveryAmount = Number(deliveryQuote.cents || 0) / 100;
  const taxRate = Number(process.env.QUOTE_TAX_RATE || "0");
  const taxAmount = (productsSubtotal + deliveryAmount) * taxRate;
  const totalAmount = productsSubtotal + deliveryAmount + taxAmount;
  const updatedQuote = await updateCustomQuote(quoteId, {
    customerName,
    customerEmail,
    customerPhone,
    address1,
    address2,
    city,
    province,
    postalCode,
    country,
    quoteTotalCents: Math.round(totalAmount * 100),
    serviceName: deliveryQuote.serviceName,
    description: deliveryQuote.description,
    eta: deliveryQuote.eta,
    summary: deliveryQuote.summary,
    sourceBreakdown: buildSourceBreakdown(lineItems),
    lineItems
  });
  return data({
    ok: true,
    message: "Quote regenerated.",
    quote: updatedQuote
  });
}
const route53 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$6
}, Symbol.toStringTag, { value: "Module" }));
const route43 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$6
}, Symbol.toStringTag, { value: "Module" }));
function isAuthorized$1(request) {
  const expected = process.env.DISPATCH_POLL_SECRET || "";
  if (!expected) return false;
  const url = new URL(request.url);
  const provided = request.headers.get("x-dispatch-poll-secret") || url.searchParams.get("secret") || "";
  return provided === expected;
}
async function loader$4({
  request
}) {
  if (!isAuthorized$1(request)) {
    return data({
      ok: false,
      message: "Unauthorized"
    }, {
      status: 401
    });
  }
  try {
    const result = await pollDispatchMailbox();
    return data({
      ok: result.configured,
      ...result
    });
  } catch (error) {
    return data({
      ok: false,
      message: error instanceof Error ? error.message : "Mailbox poll failed."
    }, {
      status: 500
    });
  }
}
const route44 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  loader: loader$4
}, Symbol.toStringTag, { value: "Module" }));
function isAuthorized(request) {
  const expected = process.env.DISPATCH_POLL_SECRET || "";
  if (!expected) return false;
  const url = new URL(request.url);
  const provided = request.headers.get("x-dispatch-poll-secret") || url.searchParams.get("secret") || "";
  return provided === expected;
}
async function loader$3({
  request
}) {
  if (!isAuthorized(request)) {
    return data({
      ok: false,
      message: "Unauthorized"
    }, {
      status: 401
    });
  }
  try {
    const result = await importDispatchShopifyOrders();
    return data({
      ok: result.configured,
      ...result
    });
  } catch (error) {
    return data({
      ok: false,
      message: error instanceof Error ? error.message : "Shopify order import failed."
    }, {
      status: 500
    });
  }
}
const route45 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  loader: loader$3
}, Symbol.toStringTag, { value: "Module" }));
async function loader$2({
  request
}) {
  const allowed = await hasAdminQuotePermissionAccess(request, "dispatch") || await hasAdminQuotePermissionAccess(request, "driver");
  if (!allowed) {
    return data({
      ok: false,
      message: "Unauthorized",
      locations: []
    }, {
      status: 401
    });
  }
  try {
    return data({
      ok: true,
      locations: await getLatestDispatchDriverLocations()
    });
  } catch (error) {
    return data({
      ok: false,
      message: error instanceof Error ? error.message : "Unable to load driver locations.",
      locations: []
    }, {
      status: 500
    });
  }
}
async function action$5({
  request
}) {
  const allowed = await hasAdminQuotePermissionAccess(request, "driver");
  if (!allowed) {
    return data({
      ok: false,
      message: "Unauthorized"
    }, {
      status: 401
    });
  }
  const body = await request.json().catch(() => null);
  const latitude = Number(body == null ? void 0 : body.latitude);
  const longitude = Number(body == null ? void 0 : body.longitude);
  if (!Number.isFinite(latitude) || !Number.isFinite(longitude)) {
    return data({
      ok: false,
      message: "Missing GPS coordinates."
    }, {
      status: 400
    });
  }
  try {
    const location = await upsertDispatchDriverLocation({
      routeId: (body == null ? void 0 : body.routeId) || null,
      orderId: (body == null ? void 0 : body.orderId) || null,
      driverId: (body == null ? void 0 : body.driverId) || null,
      driverName: String((body == null ? void 0 : body.driverName) || ""),
      truck: String((body == null ? void 0 : body.truck) || ""),
      latitude,
      longitude,
      accuracy: Number.isFinite(Number(body == null ? void 0 : body.accuracy)) ? Number(body.accuracy) : null,
      heading: Number.isFinite(Number(body == null ? void 0 : body.heading)) ? Number(body.heading) : null,
      speed: Number.isFinite(Number(body == null ? void 0 : body.speed)) ? Number(body.speed) : null,
      capturedAt: (body == null ? void 0 : body.capturedAt) || (/* @__PURE__ */ new Date()).toISOString()
    });
    return data({
      ok: true,
      location
    });
  } catch (error) {
    return data({
      ok: false,
      message: error instanceof Error ? error.message : "Unable to save driver location."
    }, {
      status: 500
    });
  }
}
const route46 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$5,
  loader: loader$2
}, Symbol.toStringTag, { value: "Module" }));
async function loader$1({
  request
}) {
  const currentUser = await requireUserPermission(request, "loader");
  const notifications = await listLoaderNotifications(currentUser, 30);
  return data({
    ok: true,
    notifications
  });
}
async function action$4({
  request
}) {
  const currentUser = await requireUserPermission(request, "loader");
  const form = await request.formData();
  const intent = String(form.get("intent") || "");
  if (intent === "mark-read") {
    const id = String(form.get("id") || "");
    if (!id) return data({
      ok: false,
      message: "Notification is required."
    }, {
      status: 400
    });
    const notification = await markLoaderNotificationRead(id, currentUser);
    return data({
      ok: true,
      notification
    });
  }
  if (intent === "clear-history") {
    await clearLoaderNotificationHistory(currentUser);
    const notifications = await listLoaderNotifications(currentUser, 30);
    return data({
      ok: true,
      message: "Loaded history cleared.",
      notifications
    });
  }
  return data({
    ok: false,
    message: "Unknown notification action."
  }, {
    status: 400
  });
}
const route47 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$4,
  loader: loader$1
}, Symbol.toStringTag, { value: "Module" }));
async function loader() {
  return data({
    ok: true,
    publicKey: getVapidPublicKey(),
    configured: isPushConfigured()
  });
}
async function action$3({
  request
}) {
  const currentUser = await requireUserPermission(request, "loader");
  const contentType = request.headers.get("content-type") || "";
  const body = contentType.includes("application/json") ? await request.json() : Object.fromEntries(await request.formData());
  const intent = String(body.intent || "");
  if (intent === "subscribe") {
    await savePushSubscription({
      user: currentUser,
      subscription: body.subscription,
      userAgent: request.headers.get("user-agent") || ""
    });
    return data({
      ok: true,
      message: "Push alerts enabled."
    });
  }
  if (intent === "unsubscribe") {
    await deletePushSubscription(String(body.endpoint || ""), currentUser);
    return data({
      ok: true,
      message: "Push alerts disabled."
    });
  }
  if (intent === "test-push") {
    const result = await sendPushToTarget({
      targetUserId: currentUser.id,
      title: "Loader push test",
      message: "If you see this while locked, push is working.",
      url: "/loader",
      tag: "loader-test-push"
    });
    return data({
      ok: !result.skipped,
      message: result.skipped ? result.reason || "Push is not configured." : `Test push sent to ${result.sent} device${result.sent === 1 ? "" : "s"}.`,
      result
    });
  }
  return data({
    ok: false,
    message: "Unknown push action."
  }, {
    status: 400
  });
}
const route48 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$3,
  loader
}, Symbol.toStringTag, { value: "Module" }));
const route49 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$b
}, Symbol.toStringTag, { value: "Module" }));
const route50 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$a
}, Symbol.toStringTag, { value: "Module" }));
function toVariantGid(variant) {
  if (variant == null ? void 0 : variant.admin_graphql_api_id) return String(variant.admin_graphql_api_id);
  if ((variant == null ? void 0 : variant.id) === null || (variant == null ? void 0 : variant.id) === void 0) return "";
  return `gid://shopify/ProductVariant/${variant.id}`;
}
async function action$2({
  request
}) {
  var _a2, _b;
  const {
    topic,
    shop,
    payload
  } = await authenticate.webhook(request);
  if (topic !== "PRODUCTS_UPDATE") {
    return new Response();
  }
  const product = payload;
  const options = [];
  for (const variant of product.variants || []) {
    if (!variant.sku) continue;
    const variantTitle = String(variant.title || "").trim();
    const title = variantTitle && variantTitle !== "Default Title" ? `${product.title} - ${variantTitle}` : product.title;
    options.push({
      sku: variant.sku,
      variantId: toVariantGid(variant),
      title,
      vendor: product.vendor,
      imageUrl: ((_a2 = variant.image) == null ? void 0 : _a2.src) || ((_b = product.image) == null ? void 0 : _b.src) || "",
      price: variant.price === null || variant.price === void 0 ? void 0 : Number(variant.price)
    });
  }
  await syncProductOptionsToSupabase(options);
  console.log("[WEBHOOK SYNC]", shop, product.title);
  return new Response();
}
const route54 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$2
}, Symbol.toStringTag, { value: "Module" }));
const action$1 = async ({
  request
}) => {
  const {
    payload,
    session,
    topic,
    shop
  } = await authenticate.webhook(request);
  console.log(`Received ${topic} webhook for ${shop}`);
  const current = payload.current;
  if (session) {
    await prisma.session.update({
      where: {
        id: session.id
      },
      data: {
        scope: current.toString()
      }
    });
  }
  return new Response();
};
const route55 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action: action$1
}, Symbol.toStringTag, { value: "Module" }));
const action = async ({
  request
}) => {
  const {
    shop,
    session,
    topic
  } = await authenticate.webhook(request);
  console.log(`Received ${topic} webhook for ${shop}`);
  if (session) {
    await prisma.session.deleteMany({
      where: {
        shop
      }
    });
  }
  return new Response();
};
const route56 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  action
}, Symbol.toStringTag, { value: "Module" }));
const serverManifest = { "entry": { "module": "/assets/entry.client-oNGMiw6i.js", "imports": ["/assets/jsx-runtime-DJxbfBsx.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js"], "css": [] }, "routes": { "root": { "id": "root", "parentId": void 0, "path": "", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/root-BjGothWo.js", "imports": ["/assets/jsx-runtime-DJxbfBsx.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/_index/route": { "id": "routes/_index/route", "parentId": "root", "path": void 0, "index": true, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/route-Cmf56Jwf.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/custom-quote": { "id": "routes/custom-quote", "parentId": "root", "path": "custom-quote", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/custom-quote-PtRp4dDd.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js", "/assets/google-places-Xg9p-f4E.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/quote-review": { "id": "routes/quote-review", "parentId": "root", "path": "quote-review", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/quote-review-gYhVWCz5.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/dispatch": { "id": "routes/dispatch", "parentId": "root", "path": "dispatch", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/dispatch-CKA_CjLI.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js", "/assets/google-places-Xg9p-f4E.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/dispatch.export-day.xls": { "id": "routes/dispatch.export-day.xls", "parentId": "root", "path": "dispatch/export-day.xls", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/dispatch.export-day.xls-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/classic": { "id": "routes/classic", "parentId": "root", "path": "classic", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/classic-CG2TcJhG.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js", "/assets/dispatch-CKA_CjLI.js", "/assets/classic-columns-DXDdBy6P.js", "/assets/google-places-Xg9p-f4E.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/monitor": { "id": "routes/monitor", "parentId": "root", "path": "monitor", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/monitor-CP9j--6U.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/loader": { "id": "routes/loader", "parentId": "root", "path": "loader", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/loader-bzDTNw5Q.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/calendar": { "id": "routes/calendar", "parentId": "root", "path": "calendar", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/calendar-_f92uAy-.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/allotment": { "id": "routes/allotment", "parentId": "root", "path": "allotment", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/allotment-_8-OdAVr.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/calendar.ics": { "id": "routes/calendar.ics", "parentId": "root", "path": "calendar.ics", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/calendar.ics-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/calendar-feed.$secret": { "id": "routes/calendar-feed.$secret", "parentId": "root", "path": "calendar-feed/:secret", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/calendar-feed._secret-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/calendar.rss": { "id": "routes/calendar.rss", "parentId": "root", "path": "calendar.rss", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/calendar.rss-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/dispatch-driver": { "id": "routes/dispatch-driver", "parentId": "root", "path": "dispatch/driver", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/dispatch-driver-CPd1uDeP.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/dispatch-driver-detail": { "id": "routes/dispatch-driver-detail", "parentId": "root", "path": "dispatch/driver/detail", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/dispatch-driver-detail-B6Qm5IKz.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/mobile-dashboard": { "id": "routes/mobile-dashboard", "parentId": "root", "path": "mobile", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/mobile-dashboard-yPRn0BhP.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/login": { "id": "routes/login", "parentId": "root", "path": "login", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/login-BLokXFd3.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/change-password": { "id": "routes/change-password", "parentId": "root", "path": "change-password", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/change-password-DNG9C_4s.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/settings": { "id": "routes/settings", "parentId": "root", "path": "settings", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/settings-Cu2cBxdi.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js", "/assets/classic-columns-DXDdBy6P.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/auth.login/route": { "id": "routes/auth.login/route", "parentId": "root", "path": "auth/login", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/route-CG8tQ2tr.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/auth.$": { "id": "routes/auth.$", "parentId": "root", "path": "auth/*", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/auth._-p3tFrFib.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app": { "id": "routes/app", "parentId": "root", "path": "app", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": true, "module": "/assets/app--bd_F85Q.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app._index": { "id": "routes/app._index", "parentId": "routes/app", "path": void 0, "index": true, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app._index-BoDF1DO_.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.admin": { "id": "routes/app.admin", "parentId": "routes/app", "path": "admin", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.admin-CW6O1zQ4.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.additional": { "id": "routes/app.additional", "parentId": "routes/app", "path": "additional", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.additional-CCXuFT6z.js", "imports": ["/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.custom-quote": { "id": "routes/app.custom-quote", "parentId": "routes/app", "path": "custom-quote", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.custom-quote-SjgVOtqI.js", "imports": ["/assets/custom-quote-PtRp4dDd.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js", "/assets/google-places-Xg9p-f4E.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.quote-review": { "id": "routes/app.quote-review", "parentId": "routes/app", "path": "quote-review", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.quote-review-CTzZcwLR.js", "imports": ["/assets/quote-review-gYhVWCz5.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.dispatch": { "id": "routes/app.dispatch", "parentId": "routes/app", "path": "dispatch", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.dispatch-B-FOweLT.js", "imports": ["/assets/dispatch-CKA_CjLI.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js", "/assets/google-places-Xg9p-f4E.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.dispatch-export-day.xls": { "id": "routes/app.dispatch-export-day.xls", "parentId": "routes/app", "path": "dispatch/export-day.xls", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/app.dispatch-export-day.xls-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.classic": { "id": "routes/app.classic", "parentId": "routes/app", "path": "classic", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.classic-CJAkjfhL.js", "imports": ["/assets/classic-CG2TcJhG.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js", "/assets/dispatch-CKA_CjLI.js", "/assets/google-places-Xg9p-f4E.js", "/assets/classic-columns-DXDdBy6P.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.monitor": { "id": "routes/app.monitor", "parentId": "routes/app", "path": "monitor", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.monitor-BW46tMvu.js", "imports": ["/assets/monitor-CP9j--6U.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.loader": { "id": "routes/app.loader", "parentId": "routes/app", "path": "loader", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.loader-MAKSPBga.js", "imports": ["/assets/loader-bzDTNw5Q.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.calendar": { "id": "routes/app.calendar", "parentId": "routes/app", "path": "calendar", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.calendar-CLK4RqYV.js", "imports": ["/assets/calendar-_f92uAy-.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.allotment": { "id": "routes/app.allotment", "parentId": "routes/app", "path": "allotment", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.allotment-CFna6-5-.js", "imports": ["/assets/allotment-_8-OdAVr.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.dispatch-driver": { "id": "routes/app.dispatch-driver", "parentId": "routes/app", "path": "dispatch/driver", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.dispatch-driver-DXBFEAtS.js", "imports": ["/assets/dispatch-driver-CPd1uDeP.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.dispatch-driver-detail": { "id": "routes/app.dispatch-driver-detail", "parentId": "routes/app", "path": "dispatch/driver/detail", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.dispatch-driver-detail-DasPWBXc.js", "imports": ["/assets/dispatch-driver-detail-B6Qm5IKz.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.mobile": { "id": "routes/app.mobile", "parentId": "routes/app", "path": "mobile", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": true, "hasErrorBoundary": false, "module": "/assets/app.mobile-BCRA_95T.js", "imports": ["/assets/mobile-dashboard-yPRn0BhP.js", "/assets/chunk-UVKPFVEO-BzvOWzz9.js", "/assets/jsx-runtime-DJxbfBsx.js"], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.shipping-estimate": { "id": "routes/api.shipping-estimate", "parentId": "root", "path": "api/shipping-estimate", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.shipping-estimate-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.carrier-service": { "id": "routes/api.carrier-service", "parentId": "root", "path": "api/carrier-service", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.carrier-service-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.sync-products": { "id": "routes/api.sync-products", "parentId": "root", "path": "api/sync-products", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.sync-products-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.create-draft-order": { "id": "routes/api.create-draft-order", "parentId": "root", "path": "api/create-draft-order", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.create-draft-order-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.delete-quote": { "id": "routes/api.delete-quote", "parentId": "root", "path": "api/delete-quote", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.delete-quote-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.update-quote": { "id": "routes/api.update-quote", "parentId": "root", "path": "api/update-quote", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.update-quote-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.dispatch-poll-mailbox": { "id": "routes/api.dispatch-poll-mailbox", "parentId": "root", "path": "api/dispatch-poll-mailbox", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.dispatch-poll-mailbox-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.dispatch-poll-shopify": { "id": "routes/api.dispatch-poll-shopify", "parentId": "root", "path": "api/dispatch-poll-shopify", "index": void 0, "caseSensitive": void 0, "hasAction": false, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.dispatch-poll-shopify-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.dispatch-driver-location": { "id": "routes/api.dispatch-driver-location", "parentId": "root", "path": "api/dispatch-driver-location", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.dispatch-driver-location-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.loader-notifications": { "id": "routes/api.loader-notifications", "parentId": "root", "path": "api/loader-notifications", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.loader-notifications-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/api.loader-push-subscription": { "id": "routes/api.loader-push-subscription", "parentId": "root", "path": "api/loader-push-subscription", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": true, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/api.loader-push-subscription-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.api.shipping-estimate": { "id": "routes/app.api.shipping-estimate", "parentId": "root", "path": "app/api/shipping-estimate", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/app.api.shipping-estimate-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.api.carrier-service": { "id": "routes/app.api.carrier-service", "parentId": "root", "path": "app/api/carrier-service", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/app.api.carrier-service-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.api.create-draft-order": { "id": "routes/app.api.create-draft-order", "parentId": "root", "path": "app/api/create-draft-order", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/app.api.create-draft-order-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.api.delete-quote": { "id": "routes/app.api.delete-quote", "parentId": "root", "path": "app/api/delete-quote", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/app.api.delete-quote-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/app.api.update-quote": { "id": "routes/app.api.update-quote", "parentId": "root", "path": "app/api/update-quote", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/app.api.update-quote-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/webhooks.products.update": { "id": "routes/webhooks.products.update", "parentId": "root", "path": "webhooks/products/update", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/webhooks.products.update-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/webhooks.app.scopes_update": { "id": "routes/webhooks.app.scopes_update", "parentId": "root", "path": "webhooks/app/scopes_update", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/webhooks.app.scopes_update-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 }, "routes/webhooks.app.uninstalled": { "id": "routes/webhooks.app.uninstalled", "parentId": "root", "path": "webhooks/app/uninstalled", "index": void 0, "caseSensitive": void 0, "hasAction": true, "hasLoader": false, "hasClientAction": false, "hasClientLoader": false, "hasClientMiddleware": false, "hasDefaultExport": false, "hasErrorBoundary": false, "module": "/assets/webhooks.app.uninstalled-l0sNRNKZ.js", "imports": [], "css": [], "clientActionModule": void 0, "clientLoaderModule": void 0, "clientMiddlewareModule": void 0, "hydrateFallbackModule": void 0 } }, "url": "/assets/manifest-20ff7a6e.js", "version": "20ff7a6e", "sri": void 0 };
const assetsBuildDirectory = "build/client";
const basename = "/";
const future = { "unstable_optimizeDeps": false, "unstable_passThroughRequests": false, "unstable_subResourceIntegrity": false, "unstable_trailingSlashAwareDataRequests": false, "unstable_previewServerPrerendering": false, "v8_middleware": false, "v8_splitRouteModules": false, "v8_viteEnvironmentApi": false };
const ssr = true;
const isSpaMode = false;
const prerender = [];
const routeDiscovery = { "mode": "lazy", "manifestPath": "/__manifest" };
const publicPath = "/";
const entry = { module: entryServer };
const routes = {
  "root": {
    id: "root",
    parentId: void 0,
    path: "",
    index: void 0,
    caseSensitive: void 0,
    module: route0
  },
  "routes/_index/route": {
    id: "routes/_index/route",
    parentId: "root",
    path: void 0,
    index: true,
    caseSensitive: void 0,
    module: route1
  },
  "routes/custom-quote": {
    id: "routes/custom-quote",
    parentId: "root",
    path: "custom-quote",
    index: void 0,
    caseSensitive: void 0,
    module: route2
  },
  "routes/quote-review": {
    id: "routes/quote-review",
    parentId: "root",
    path: "quote-review",
    index: void 0,
    caseSensitive: void 0,
    module: route3
  },
  "routes/dispatch": {
    id: "routes/dispatch",
    parentId: "root",
    path: "dispatch",
    index: void 0,
    caseSensitive: void 0,
    module: route4
  },
  "routes/dispatch.export-day.xls": {
    id: "routes/dispatch.export-day.xls",
    parentId: "root",
    path: "dispatch/export-day.xls",
    index: void 0,
    caseSensitive: void 0,
    module: route5
  },
  "routes/classic": {
    id: "routes/classic",
    parentId: "root",
    path: "classic",
    index: void 0,
    caseSensitive: void 0,
    module: route6
  },
  "routes/monitor": {
    id: "routes/monitor",
    parentId: "root",
    path: "monitor",
    index: void 0,
    caseSensitive: void 0,
    module: route7
  },
  "routes/loader": {
    id: "routes/loader",
    parentId: "root",
    path: "loader",
    index: void 0,
    caseSensitive: void 0,
    module: route8
  },
  "routes/calendar": {
    id: "routes/calendar",
    parentId: "root",
    path: "calendar",
    index: void 0,
    caseSensitive: void 0,
    module: route9
  },
  "routes/allotment": {
    id: "routes/allotment",
    parentId: "root",
    path: "allotment",
    index: void 0,
    caseSensitive: void 0,
    module: route10
  },
  "routes/calendar.ics": {
    id: "routes/calendar.ics",
    parentId: "root",
    path: "calendar.ics",
    index: void 0,
    caseSensitive: void 0,
    module: route11
  },
  "routes/calendar-feed.$secret": {
    id: "routes/calendar-feed.$secret",
    parentId: "root",
    path: "calendar-feed/:secret",
    index: void 0,
    caseSensitive: void 0,
    module: route12
  },
  "routes/calendar.rss": {
    id: "routes/calendar.rss",
    parentId: "root",
    path: "calendar.rss",
    index: void 0,
    caseSensitive: void 0,
    module: route13
  },
  "routes/dispatch-driver": {
    id: "routes/dispatch-driver",
    parentId: "root",
    path: "dispatch/driver",
    index: void 0,
    caseSensitive: void 0,
    module: route14
  },
  "routes/dispatch-driver-detail": {
    id: "routes/dispatch-driver-detail",
    parentId: "root",
    path: "dispatch/driver/detail",
    index: void 0,
    caseSensitive: void 0,
    module: route15
  },
  "routes/mobile-dashboard": {
    id: "routes/mobile-dashboard",
    parentId: "root",
    path: "mobile",
    index: void 0,
    caseSensitive: void 0,
    module: route16
  },
  "routes/login": {
    id: "routes/login",
    parentId: "root",
    path: "login",
    index: void 0,
    caseSensitive: void 0,
    module: route17
  },
  "routes/change-password": {
    id: "routes/change-password",
    parentId: "root",
    path: "change-password",
    index: void 0,
    caseSensitive: void 0,
    module: route18
  },
  "routes/settings": {
    id: "routes/settings",
    parentId: "root",
    path: "settings",
    index: void 0,
    caseSensitive: void 0,
    module: route19
  },
  "routes/auth.login/route": {
    id: "routes/auth.login/route",
    parentId: "root",
    path: "auth/login",
    index: void 0,
    caseSensitive: void 0,
    module: route20
  },
  "routes/auth.$": {
    id: "routes/auth.$",
    parentId: "root",
    path: "auth/*",
    index: void 0,
    caseSensitive: void 0,
    module: route21
  },
  "routes/app": {
    id: "routes/app",
    parentId: "root",
    path: "app",
    index: void 0,
    caseSensitive: void 0,
    module: route22
  },
  "routes/app._index": {
    id: "routes/app._index",
    parentId: "routes/app",
    path: void 0,
    index: true,
    caseSensitive: void 0,
    module: route23
  },
  "routes/app.admin": {
    id: "routes/app.admin",
    parentId: "routes/app",
    path: "admin",
    index: void 0,
    caseSensitive: void 0,
    module: route24
  },
  "routes/app.additional": {
    id: "routes/app.additional",
    parentId: "routes/app",
    path: "additional",
    index: void 0,
    caseSensitive: void 0,
    module: route25
  },
  "routes/app.custom-quote": {
    id: "routes/app.custom-quote",
    parentId: "routes/app",
    path: "custom-quote",
    index: void 0,
    caseSensitive: void 0,
    module: route26
  },
  "routes/app.quote-review": {
    id: "routes/app.quote-review",
    parentId: "routes/app",
    path: "quote-review",
    index: void 0,
    caseSensitive: void 0,
    module: route27
  },
  "routes/app.dispatch": {
    id: "routes/app.dispatch",
    parentId: "routes/app",
    path: "dispatch",
    index: void 0,
    caseSensitive: void 0,
    module: route28
  },
  "routes/app.dispatch-export-day.xls": {
    id: "routes/app.dispatch-export-day.xls",
    parentId: "routes/app",
    path: "dispatch/export-day.xls",
    index: void 0,
    caseSensitive: void 0,
    module: route29
  },
  "routes/app.classic": {
    id: "routes/app.classic",
    parentId: "routes/app",
    path: "classic",
    index: void 0,
    caseSensitive: void 0,
    module: route30
  },
  "routes/app.monitor": {
    id: "routes/app.monitor",
    parentId: "routes/app",
    path: "monitor",
    index: void 0,
    caseSensitive: void 0,
    module: route31
  },
  "routes/app.loader": {
    id: "routes/app.loader",
    parentId: "routes/app",
    path: "loader",
    index: void 0,
    caseSensitive: void 0,
    module: route32
  },
  "routes/app.calendar": {
    id: "routes/app.calendar",
    parentId: "routes/app",
    path: "calendar",
    index: void 0,
    caseSensitive: void 0,
    module: route33
  },
  "routes/app.allotment": {
    id: "routes/app.allotment",
    parentId: "routes/app",
    path: "allotment",
    index: void 0,
    caseSensitive: void 0,
    module: route34
  },
  "routes/app.dispatch-driver": {
    id: "routes/app.dispatch-driver",
    parentId: "routes/app",
    path: "dispatch/driver",
    index: void 0,
    caseSensitive: void 0,
    module: route35
  },
  "routes/app.dispatch-driver-detail": {
    id: "routes/app.dispatch-driver-detail",
    parentId: "routes/app",
    path: "dispatch/driver/detail",
    index: void 0,
    caseSensitive: void 0,
    module: route36
  },
  "routes/app.mobile": {
    id: "routes/app.mobile",
    parentId: "routes/app",
    path: "mobile",
    index: void 0,
    caseSensitive: void 0,
    module: route37
  },
  "routes/api.shipping-estimate": {
    id: "routes/api.shipping-estimate",
    parentId: "root",
    path: "api/shipping-estimate",
    index: void 0,
    caseSensitive: void 0,
    module: route38
  },
  "routes/api.carrier-service": {
    id: "routes/api.carrier-service",
    parentId: "root",
    path: "api/carrier-service",
    index: void 0,
    caseSensitive: void 0,
    module: route39
  },
  "routes/api.sync-products": {
    id: "routes/api.sync-products",
    parentId: "root",
    path: "api/sync-products",
    index: void 0,
    caseSensitive: void 0,
    module: route40
  },
  "routes/api.create-draft-order": {
    id: "routes/api.create-draft-order",
    parentId: "root",
    path: "api/create-draft-order",
    index: void 0,
    caseSensitive: void 0,
    module: route41
  },
  "routes/api.delete-quote": {
    id: "routes/api.delete-quote",
    parentId: "root",
    path: "api/delete-quote",
    index: void 0,
    caseSensitive: void 0,
    module: route42
  },
  "routes/api.update-quote": {
    id: "routes/api.update-quote",
    parentId: "root",
    path: "api/update-quote",
    index: void 0,
    caseSensitive: void 0,
    module: route43
  },
  "routes/api.dispatch-poll-mailbox": {
    id: "routes/api.dispatch-poll-mailbox",
    parentId: "root",
    path: "api/dispatch-poll-mailbox",
    index: void 0,
    caseSensitive: void 0,
    module: route44
  },
  "routes/api.dispatch-poll-shopify": {
    id: "routes/api.dispatch-poll-shopify",
    parentId: "root",
    path: "api/dispatch-poll-shopify",
    index: void 0,
    caseSensitive: void 0,
    module: route45
  },
  "routes/api.dispatch-driver-location": {
    id: "routes/api.dispatch-driver-location",
    parentId: "root",
    path: "api/dispatch-driver-location",
    index: void 0,
    caseSensitive: void 0,
    module: route46
  },
  "routes/api.loader-notifications": {
    id: "routes/api.loader-notifications",
    parentId: "root",
    path: "api/loader-notifications",
    index: void 0,
    caseSensitive: void 0,
    module: route47
  },
  "routes/api.loader-push-subscription": {
    id: "routes/api.loader-push-subscription",
    parentId: "root",
    path: "api/loader-push-subscription",
    index: void 0,
    caseSensitive: void 0,
    module: route48
  },
  "routes/app.api.shipping-estimate": {
    id: "routes/app.api.shipping-estimate",
    parentId: "root",
    path: "app/api/shipping-estimate",
    index: void 0,
    caseSensitive: void 0,
    module: route49
  },
  "routes/app.api.carrier-service": {
    id: "routes/app.api.carrier-service",
    parentId: "root",
    path: "app/api/carrier-service",
    index: void 0,
    caseSensitive: void 0,
    module: route50
  },
  "routes/app.api.create-draft-order": {
    id: "routes/app.api.create-draft-order",
    parentId: "root",
    path: "app/api/create-draft-order",
    index: void 0,
    caseSensitive: void 0,
    module: route51
  },
  "routes/app.api.delete-quote": {
    id: "routes/app.api.delete-quote",
    parentId: "root",
    path: "app/api/delete-quote",
    index: void 0,
    caseSensitive: void 0,
    module: route52
  },
  "routes/app.api.update-quote": {
    id: "routes/app.api.update-quote",
    parentId: "root",
    path: "app/api/update-quote",
    index: void 0,
    caseSensitive: void 0,
    module: route53
  },
  "routes/webhooks.products.update": {
    id: "routes/webhooks.products.update",
    parentId: "root",
    path: "webhooks/products/update",
    index: void 0,
    caseSensitive: void 0,
    module: route54
  },
  "routes/webhooks.app.scopes_update": {
    id: "routes/webhooks.app.scopes_update",
    parentId: "root",
    path: "webhooks/app/scopes_update",
    index: void 0,
    caseSensitive: void 0,
    module: route55
  },
  "routes/webhooks.app.uninstalled": {
    id: "routes/webhooks.app.uninstalled",
    parentId: "root",
    path: "webhooks/app/uninstalled",
    index: void 0,
    caseSensitive: void 0,
    module: route56
  }
};
const allowedActionOrigins = false;
export {
  allowedActionOrigins,
  serverManifest as assets,
  assetsBuildDirectory,
  basename,
  entry,
  future,
  isSpaMode,
  prerender,
  publicPath,
  routeDiscovery,
  routes,
  ssr
};
